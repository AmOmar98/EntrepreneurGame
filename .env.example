NEXT_PUBLIC_SUPABASE_URL=https://api.eic-game.uemf.ma
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-me
# Server-only. Required for /admin/players/import to send magic-link invites.
# When absent, the import still creates Players + links existing users; missing
# users are reported as "service role missing - invite skipped".
SUPABASE_SERVICE_ROLE_KEY=replace-me
NEXT_PUBLIC_APP_URL=https://eic-game.uemf.ma
