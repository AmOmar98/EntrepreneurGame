// Phase 10 / Section 13 — Root App Router loading boundary.
import { SysLoading } from "@/components/system/SysLoading";

export default function Loading() {
  return (
    <main className="main">
      <SysLoading />
    </main>
  );
}
