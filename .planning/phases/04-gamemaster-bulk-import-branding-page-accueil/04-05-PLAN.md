---
phase: 04-gamemaster-bulk-import-branding-page-accueil
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/login/page.tsx
  - app/page.tsx
  - components/app-shell.tsx
  - components/partner-banner.tsx
  - public/brand/logo-eic.svg
  - public/brand/partners/tamwilcom.svg
  - public/brand/partners/bank-of-africa.svg
  - public/brand/partners/innov-invest.svg
  - public/brand/partners/bluespace.svg
  - public/brand/partners/eic.svg
  - public/brand/partners/uemf.svg
  - public/brand/partners/README.md
  - lib/i18n.ts
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, BRAND-05, AUTH-02]
must_haves:
  truths:
    - "L'app a une palette EIC coherente (bleu institutionnel + or accent) appliquee via tokens CSS"
    - "Toutes les pages authentifiees affichent le logo EIC dans le header (via AppShell)"
    - "La page /login affiche le bandeau partenaires avec 6 logos : Tamwilcom, Bank of Africa Academy, Innov Invest, Bluespace, EIC, UEMF"
    - "La racine / redirige vers /login si non authentifie, sinon vers le dashboard du role"
    - "Les metadata (titre, description) parlent d'Entrepreneur Game (UEMF/EIC), plus de 'EIC Venture Journey'"
    - "Aucune mention de seed (atlas-soil, etc.) visible dans l'UI ou les metadata"
  artifacts:
    - path: "app/globals.css"
      provides: "EIC palette tokens + typography + button/card/badge primitives"
    - path: "components/partner-banner.tsx"
      provides: "Reusable partner logos strip with proper alt text"
    - path: "app/login/page.tsx"
      provides: "Polished login page with logo + partner banner"
    - path: "app/page.tsx"
      provides: "Root redirect to /login or role dashboard"
    - path: "public/brand/logo-eic.svg"
      provides: "EIC primary logo (placeholder until Omar provides final asset)"
    - path: "public/brand/partners/*.svg"
      provides: "6 partner logo placeholders"
  key_links:
    - from: "components/app-shell.tsx"
      to: "public/brand/logo-eic.svg"
      via: "next/image"
      pattern: "logo-eic\\.svg"
    - from: "app/login/page.tsx"
      to: "components/partner-banner.tsx"
      via: "<PartnerBanner />"
      pattern: "PartnerBanner"
    - from: "app/page.tsx"
      to: "lib/auth.ts redirectForRole"
      via: "server redirect"
      pattern: "redirectForRole|pathForRole"
---

<objective>
Implementer BRAND-01..05 et AUTH-02 (page accueil) : identite visuelle EIC professionnelle (palette, logo, typo, polish), bandeau partenaires sur `/login`, page accueil `/` redirigeant proprement, suppression definitive des mentions seed/demo apparentes.

Purpose: Le 13 mai 8h30, les partenaires (Tamwilcom, Bank of Africa Academy, Innov Invest, Bluespace) verront cette interface. Aucune mention "EIC Venture Journey" obsolete, aucune chaine `atlas-soil`, aucun placeholder ne doit apparaitre. Credibilite institutionnelle UEMF non negociable.
Output: Tokens CSS + composant PartnerBanner + assets SVG placeholders + page login polish + page accueil + AppShell brand header + metadata propre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-CONTEXT.md
@CLAUDE.md
@app/globals.css
@app/layout.tsx
@app/login/page.tsx
@app/page.tsx
@components/app-shell.tsx
@lib/auth.ts
@lib/i18n.ts

<interfaces>
- `lib/auth.ts` exports `getCurrentRole()` and `pathForRole(role)`.
- AppShell currently has hardcoded `<strong>{t.brand_name}</strong>` text only — no image.
- `app/page.tsx` (root) exists ; behavior to confirm + replace with redirect.
- `next/image` is available via Next 15 default.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish EIC brand tokens, logos, and global polish</name>
  <files>app/globals.css, app/layout.tsx, public/brand/logo-eic.svg, public/brand/partners/tamwilcom.svg, public/brand/partners/bank-of-africa.svg, public/brand/partners/innov-invest.svg, public/brand/partners/bluespace.svg, public/brand/partners/eic.svg, public/brand/partners/uemf.svg, public/brand/partners/README.md</files>
  <action>
1. **Create SVG placeholder assets** in `public/brand/`:
   - `logo-eic.svg` : 240x64 viewBox. Simple text-based logo: dark navy rounded rect background `#0B2545` with white text "EIC" + smaller "UEMF" subtitle. Pure SVG (no font deps; use SVG `<text>` with system serif/sans-serif fallback OR convert to paths if quick). Mark with `<title>EIC - UEMF</title>` for accessibility.
   - `partners/tamwilcom.svg`, `partners/bank-of-africa.svg`, `partners/innov-invest.svg`, `partners/bluespace.svg`, `partners/eic.svg`, `partners/uemf.svg` : each 200x80 viewBox. Each has a light-gray rounded rect background `#F4F5F7` with the partner name centered, written exactly as the brand name (e.g., "Tamwilcom", "Bank of Africa Academy", "Innov Invest", "Bluespace", "EIC", "UEMF"). Use SVG `<title>` with the partner name. NO fake logo marks. They are intentionally textual placeholders.
   - `public/brand/partners/README.md` : 10-line note explaining these are placeholders ; instruct Omar to drop final logos here with the same filenames before deploy. Mention that `partner-banner.tsx` reads from this folder.

2. **app/globals.css** — add at the TOP (before any existing rules), wrapped in `:root { ... }`:
   ```css
   :root {
     --brand-primary: #0B2545;        /* EIC navy */
     --brand-primary-hover: #163A6E;
     --brand-accent: #C9A227;         /* gold accent UEMF */
     --brand-bg: #FAFAF7;
     --brand-surface: #FFFFFF;
     --brand-border: #E5E7EB;
     --brand-text: #111827;
     --brand-text-muted: #6B7280;
     --brand-success: #16A34A;
     --brand-warning: #D97706;
     --brand-danger: #DC2626;
     --brand-radius: 8px;
     --brand-radius-lg: 12px;
     --brand-shadow-card: 0 1px 2px rgba(15,23,42,0.06), 0 1px 3px rgba(15,23,42,0.05);
     --font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
   }
   body { background: var(--brand-bg); color: var(--brand-text); font-family: var(--font-sans); }
   ```
   Then update / add primitives consistent with existing classes (`.button`, `.button.primary`, `.input`, `.stack`, `.card`, `.badge`, `.app-shell`, `.sidebar`, `.brand` ...). Preserve any existing class names already used in components (audit `components/*.tsx` and `app/**/page.tsx` for class strings before deleting). For existing `.sidebar` background, set to `var(--brand-primary)` with white text.
   Add new utility classes used by login + partner banner:
   - `.partner-banner { display:flex; flex-wrap:wrap; gap:16px; align-items:center; justify-content:center; padding:16px; border-top:1px solid var(--brand-border); }`
   - `.partner-banner img { height:40px; width:auto; opacity:0.85; }`
   - `.auth-card { max-width:420px; margin:8vh auto; padding:32px; background:var(--brand-surface); border:1px solid var(--brand-border); border-radius:var(--brand-radius-lg); box-shadow:var(--brand-shadow-card); }`
   - `.brand-logo { height:40px; width:auto; }`
   IMPORTANT: do not break any class already in use ; instead extend. Before editing, read `app/globals.css` fully and audit class names referenced in components to know which to preserve.

3. **app/layout.tsx** — update metadata (BRAND-05):
   ```typescript
   export const metadata: Metadata = {
     title: { default: "Entrepreneur Game - EIC / UEMF", template: "%s - Entrepreneur Game" },
     description: "Plateforme d'accompagnement entrepreneurial gamifiee EIC / UEMF.",
     icons: { icon: "/brand/logo-eic.svg" },
   };
   ```
   Keep `<html lang="fr">`. No additional providers required.

4. Final audit : `grep -r -i -n "atlas-soil\|EIC Venture Journey\|venture journey\|founders, reviewers" app/ components/ lib/ public/ --include="*.{ts,tsx,css,md,html}"` — for each match in source files (not in `.planning/`), replace with the new branded copy or delete. Document removals in the plan SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint && npx grep -r "atlas-soil\|EIC Venture Journey" app components lib public 2>$null; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>
- 7 SVG assets exist in `public/brand/` with proper `<title>` text.
- `app/globals.css` declares `--brand-*` tokens at `:root` ; sidebar uses `--brand-primary`.
- `metadata.title` = "Entrepreneur Game - EIC / UEMF".
- No occurrences of "atlas-soil", "EIC Venture Journey", "venture journey" remain in `app/`, `components/`, `lib/`, `public/`.
- Lint + typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build PartnerBanner component + polish login page</name>
  <files>components/partner-banner.tsx, app/login/page.tsx, lib/i18n.ts</files>
  <action>
1. **components/partner-banner.tsx** (server component, NO `"use client"`):
```typescript
import Image from "next/image";

const PARTNERS = [
  { slug: "tamwilcom", name: "Tamwilcom" },
  { slug: "bank-of-africa", name: "Bank of Africa Academy" },
  { slug: "innov-invest", name: "Innov Invest" },
  { slug: "bluespace", name: "Bluespace" },
  { slug: "eic", name: "EIC" },
  { slug: "uemf", name: "UEMF" },
] as const;

export function PartnerBanner() {
  return (
    <section aria-label="Partenaires" className="partner-banner">
      {PARTNERS.map((p) => (
        <Image key={p.slug} src={`/brand/partners/${p.slug}.svg`} alt={p.name}
               width={160} height={40} unoptimized />
      ))}
    </section>
  );
}
```
- Use `unoptimized` so Next doesn't try to re-encode small SVG placeholders.
- Order is non-alphabetical : it follows the canonical partner order from CONTEXT.md.

2. **app/login/page.tsx** — refactor (it must remain a `"use client"` component because it uses `useActionState`) :
   - Wrap content in `<div className="auth-card">`.
   - Replace text-only header with `<Image src="/brand/logo-eic.svg" alt="EIC - UEMF" width={180} height={48} className="brand-logo" priority />` and `<h1>{t.login_title}</h1>` and `<p>{t.login_subtitle}</p>`.
   - Render the form using existing `useActionState(signIn, ...)` — keep the action wiring intact.
   - Below the auth card, render `<PartnerBanner />`. Since `PartnerBanner` is server-friendly but parent is client, that's fine — Next allows server components imported as JSX into client subtrees only via Server Components Boundary. Easiest: convert login page to: server component shell (renders PartnerBanner + heading) + extract the form into a small client subcomponent (`components/login-form.tsx`). Choose this route :
     - Create `components/login-form.tsx` (`"use client"`) holding the form + `useActionState`.
     - Convert `app/login/page.tsx` to a server component that renders logo + heading + `<LoginForm />` + `<PartnerBanner />`.
   - Add a footnote at the very bottom (small grey text) : `t.login_partners_caption` = "En partenariat avec Tamwilcom, Bank of Africa Academy, Innov Invest, Bluespace - EIC / UEMF".

3. Add i18n keys (FR + EN, ASCII) to `lib/i18n.ts`:
   - `login_title` : "Entrepreneur Game"
   - `login_subtitle` : "Hack-Days Fes-Meknes - 13 & 14 mai 2026"
   - `login_email` : "Email"
   - `login_password` : "Mot de passe"
   - `login_submit` : "Se connecter"
   - `login_partners_caption` : "En partenariat avec Tamwilcom, Bank of Africa Academy, Innov Invest, Bluespace - EIC / UEMF"

   Refactor the login form to read these via `dictionaries.fr` (for now FR only on login, EN values still added for future).

4. Confirm logos render with `next.config.ts` defaults. If `next.config.ts` has remote image patterns, ensure local `/public/brand/...` is allowed by default (it is — only remote URLs need patterns). Do NOT touch `next.config.ts` unless an error occurs at build.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint</automated>
  </verify>
  <done>
- `/login` shows : EIC logo, title "Entrepreneur Game", subtitle, login form, partner banner with 6 logos, footnote caption.
- Form still authenticates (signIn unchanged).
- TS / lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Brand AppShell header + root redirect</name>
  <files>components/app-shell.tsx, app/page.tsx, lib/i18n.ts</files>
  <action>
1. **components/app-shell.tsx** :
   - Replace the text-only `<div className="brand"><strong>...</strong><span>...</span></div>` with:
     ```tsx
     <div className="brand">
       <Image src="/brand/logo-eic.svg" alt="EIC - UEMF" width={160} height={40} className="brand-logo" />
       <span className="brand-tagline">{t.brand_tagline_short}</span>
     </div>
     ```
   - Add i18n key `brand_tagline_short`: "Entrepreneur Game" (FR + EN both).
   - Keep the `"use client"` directive. `next/image` works in client components.
   - Adjust `.sidebar .brand` styles in globals.css if needed for vertical alignment (image + tagline stacked or inline). Keep it simple : flex column, gap 4px.

2. **app/page.tsx** : convert to a server component that does:
   ```typescript
   import { redirect } from "next/navigation";
   import { getCurrentUser, getCurrentRole, pathForRole } from "@/lib/auth";

   export default async function RootPage() {
     const user = await getCurrentUser();
     if (!user) redirect("/login");
     const role = await getCurrentRole();
     redirect(pathForRole(role ?? "player"));
   }
   ```
   - Replace whatever the current `app/page.tsx` does (was an EIC cockpit per CLAUDE.md but Phase 1 may have changed it — re-read before editing).

3. Final audit : start `npm run dev`, manually navigate `/` -> `/login` (anon) and `/` -> `/journey | /mentor | /admin` (auth). Cover this in Plan 06's smoke checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint && npx next build</automated>
  </verify>
  <done>
- Sidebar (AppShell) header shows EIC logo + tagline on every authenticated page.
- `/` redirects properly based on auth state + role.
- Production build succeeds (`next build`).
- TS / lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon -> /login | unauthenticated visitor reaches login + sees partner banner |
| anon -> / | root must redirect, never expose dashboards |
| anon -> /admin/export/players.csv | covered in Plan 04 ; not in scope here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information disclosure) | app/page.tsx | mitigate | Server-side `redirect()` based on `getCurrentUser()` ; never render dashboard data on `/` |
| T-04-02 | T (Tampering) | partner SVGs | accept | Static public assets, no user input ; Omar replaces files manually |
| T-04-03 | R (Repudiation) | Branding metadata | accept | No security implication ; metadata.title is static |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `npm run lint` clean.
- `npm run build` succeeds.
- Manual : visit `/login` -> see logo + form + 6-partner banner. Visit `/` anon -> redirected to `/login`. Login as game_master -> redirected to `/admin`, sidebar shows EIC logo.
- `grep -r "atlas-soil\|EIC Venture Journey" app components lib public` returns 0 matches.
</verification>

<success_criteria>
1. BRAND-01 : logo EIC dans header AppShell sur toutes pages auth.
2. BRAND-02 : bandeau 6 partenaires sur `/login`.
3. BRAND-03 : palette EIC tokens en place ; cohesion visuelle login + journey + mentor + admin.
4. BRAND-04 : 5 ecrans cles polish (login + onboarding + journey + mentor + admin) — l'application des tokens via globals.css les couvre tous.
5. BRAND-05 : aucune chaine `atlas-soil` / `EIC Venture Journey` dans le code source.
6. AUTH-02 (partiel - root redirect) : `/` redirige vers `/login` ou dashboard role.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-05-SUMMARY.md`.
</output>
