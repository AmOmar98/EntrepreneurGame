---
phase: 04-gamemaster-bulk-import-branding-page-accueil
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - lib/i18n.ts
  - .planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-SMOKE-TEST.md
autonomous: false
requirements: [BRAND-04, ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ONBOARD-01]
must_haves:
  truths:
    - "Tous les flows GameMaster (cohort dashboard, import, detail, export, branding login) sont testes manuellement bout en bout"
    - "Cles i18n FR/EN coherentes (pas de cle manquante en EN)"
    - "Aucune mention seed visible sur les ecrans cles"
    - "npm run lint et npm run typecheck et npm run build clean"
  artifacts:
    - path: ".planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-SMOKE-TEST.md"
      provides: "Smoke test checklist validee par GameMaster"
  key_links:
    - from: "smoke test"
      to: "/admin, /admin/players/import, /admin/players/[id], /admin/export/players.csv, /login"
      via: "manual GM walkthrough"
      pattern: "smoke"
---

<objective>
Polish final + smoke test E2E manuel de la phase 4 : audit i18n FR/EN, audit anti-leak, build prod, parcours GM complet (login -> import -> dashboard -> detail -> export). Bloquant : si un test casse, retour en arriere ou correction avant Phase 5.

Purpose: Phase 4 livre l'interface visible aux partenaires + le pilotage GM. Aucune regression tolerable avant Phase 5 (jury + deploy). Le checkpoint humain valide visuellement la cohesion.
Output: Checklist smoke test signee + fixes mineurs i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-CONTEXT.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-01-PLAN.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-02-PLAN.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-03-PLAN.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-04-PLAN.md
@.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-05-PLAN.md
@CLAUDE.md
@lib/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n parity audit + final anti-leak grep</name>
  <files>lib/i18n.ts</files>
  <action>
1. Read `lib/i18n.ts`. Compute the set of keys present in `dictionaries.fr` vs `dictionaries.en`. For every key missing in `en`, copy the FR value as an English-language placeholder (translate when obvious — e.g., FR "Importer la cohorte" -> EN "Import cohort"). Plain ASCII only.

2. Anti-leak grep across `app/`, `components/`, `lib/`, `public/` (PowerShell-friendly):
   ```
   grep -r -n -i "atlas-soil\|EIC Venture Journey\|venture journey\|founders, reviewers, and EIC" app/ components/ lib/ public/
   ```
   Must return zero matches. If any remain, fix in place.

3. Verify env vars in `.env.example` document `SUPABASE_SERVICE_ROLE_KEY` (added by Plan 02). If missing, add it with comment `# Server-only. Required for /admin/players/import to send magic links.`

4. Run `npm run lint`, `npm run typecheck`, `npm run build`. All three must pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint && npx next build</automated>
  </verify>
  <done>
- i18n keys identical between fr and en.
- 0 matches for forbidden seed/legacy strings in source.
- `.env.example` mentions `SUPABASE_SERVICE_ROLE_KEY`.
- Lint + typecheck + build clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke test GameMaster end-to-end</name>
  <what-built>
GameMaster cockpit complet : login -> import CSV -> dashboard cohorte -> detail Player -> export CSV. Branding EIC + bandeau partenaires sur /login.
  </what-built>
  <how-to-verify>
Start `npm run dev` (port 3000 or 3001). Then:

1. **Anon root + login** : visit `http://localhost:3000/`. Expected : redirected to `/login`.
2. **Login partner banner** : on `/login`, verify :
   - EIC logo visible at top of card.
   - Title "Entrepreneur Game" + subtitle "Hack-Days Fes-Meknes - 13 & 14 mai 2026".
   - 6 partner logos visible in order : Tamwilcom, Bank of Africa Academy, Innov Invest, Bluespace, EIC, UEMF.
   - Footnote caption with all partner names.
   - No mention of "atlas-soil", "EIC Venture Journey", "demo".
3. **Login as game_master** : enter GM credentials. Expected : redirected to `/admin`.
4. **Cohort dashboard** : on `/admin`, verify :
   - Header `Pilotage cohorte` with subtitle.
   - 3 counters : `Livrables soumis ... sur ...`, `En attente revue`, `Valides`.
   - Cohort table with one row per Player (or empty state if no players yet).
   - Action buttons : `Importer un CSV` and `Exporter players.csv`.
5. **Import CSV** : click `Importer un CSV` -> `/admin/players/import`. Paste this 2-row CSV (or use a file):
   ```
   team_name,project_name,project_pitch,leader_email,member_emails
   Equipe Test,Projet Test,Un pitch court,test+leader@example.com,test+member@example.com
   ```
   Submit. Expected : report shows `created=1, membersAdded=1 or 2, invitesSent=2 (or invitesSkipped=2 if no SERVICE_ROLE_KEY)`.
6. **Idempotency** : re-submit the SAME CSV. Expected : `created=0, alreadyExisted=1, membersAdded=0`.
7. **Player detail** : back to `/admin`, click on the new Player. Expected : `/admin/players/<id>` shows team header, member list (1-2 emails), "Aucune soumission".
8. **Export CSV** : back to `/admin`, click `Exporter players.csv`. Expected : file `players.csv` downloads ; first line is the header ; rows readable in a spreadsheet.
9. **Sidebar branding** : verify EIC logo visible in sidebar on `/admin`, `/admin/players/import`, `/admin/players/<id>`.
10. **403 on export** : open private window, hit `http://localhost:3000/admin/export/players.csv` directly. Expected : 403 Forbidden (not a download).

If any step fails, describe what went wrong. If all pass, type `approved`.
  </how-to-verify>
  <files>(no file changes — verification only)</files>
  <action>Operator runs the 10-step manual smoke walkthrough described in <how-to-verify>. No code changes in this task.</action>
  <verify><automated>echo "manual smoke - operator confirms"</automated></verify>
  <done>Operator types "approved" or describes issues. Issues block until fixed.</done>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- All automated checks (lint/typecheck/build) green.
- Manual smoke walkthrough passes 10 points above.
- i18n parity FR/EN.
</verification>

<success_criteria>
1. Phase 4 deployable as-is to staging Vercel.
2. Smoke test approved by user.
3. Aucune regression visible sur ecrans Phase 1/2/3 (login, journey, mentor) — verifie au passage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-06-SUMMARY.md` AND `.planning/phases/04-gamemaster-bulk-import-branding-page-accueil/04-PHASE-SUMMARY.md` consolidant les 6 plans.
</output>
