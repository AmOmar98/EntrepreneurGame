---
phase: 17-observabilite-perf
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - scripts/perf-seed-500.sql
  - scripts/perf-seed-500.mjs
  - scripts/perf-p95.mjs
  - docs/OBSERVABILITY.md
  - .planning/phases/17-observabilite-perf/17-VERIFICATION.md
autonomous: false
user_setup:
  - service: sentry
    why: "QUAL-04 — error monitoring DSN"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard -> Settings -> Client Keys (DSN)"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Same DSN value (client init)"
    dashboard_config:
      - task: "Create email alert rule on new issues"
        location: "Sentry -> Alerts -> Issues"
  - service: posthog
    why: "QUAL-05 — product funnel analytics"
    env_vars:
      - name: NEXT_PUBLIC_POSTHOG_KEY
        source: "PostHog -> Project Settings -> Project API Key"
      - name: NEXT_PUBLIC_POSTHOG_HOST
        source: "PostHog -> Project Settings (optional, defaults us.i.posthog.com)"
    dashboard_config:
      - task: "Build funnel insight (onboarding -> submit -> validated -> mentor eval)"
        location: "PostHog -> Insights -> New Funnel"
requirements: [QUAL-06]
must_haves:
  truths:
    - "An idempotent seed script can create ~500 synthetic users on a disposable Supabase project (or PROD off-event) with a documented full-cleanup path"
    - "A measurement script times N PostgREST queries on the critical paths (/journey data, evaluation, jury) and reports P95"
    - "RLS policies are statically verified to use (SELECT auth.uid()) initplan form (no per-row auth.uid() call) via grep over the source-of-truth SQL"
    - "P95 numbers + RLS initplan verdict are documented in 17-VERIFICATION.md, mapping QUAL-04/05/06 to evidence"
  artifacts:
    - path: "scripts/perf-seed-500.sql"
      provides: "Idempotent 500-user seed, disposable-project default, integral cleanup block"
      contains: "on conflict"
    - path: "scripts/perf-p95.mjs"
      provides: "Timed PostgREST queries on critical paths -> P95 readout"
      contains: "p95"
    - path: ".planning/phases/17-observabilite-perf/17-VERIFICATION.md"
      provides: "QUAL-04/05/06 evidence + P95 table + RLS initplan verdict"
      contains: "QUAL-06"
  key_links:
    - from: "scripts/perf-p95.mjs"
      to: "Supabase PostgREST"
      via: "@supabase/supabase-js timed selects on critical paths"
      pattern: "createClient"
    - from: "17-VERIFICATION.md"
      to: "scripts/perf-p95.mjs"
      via: "documented P95 output"
      pattern: "P95"
---

<objective>
Deliver the QUAL-06 perf toolkit WITHOUT Docker or staging: an idempotent 500-user seed
script (file-first, disposable-project default, full cleanup documented), a P95 measurement
script that times PostgREST queries on the critical read paths, and a static RLS-initplan
verification (grep that every auth.uid() call is wrapped as (SELECT auth.uid())). Close the
phase with 17-VERIFICATION.md and a single operator checkpoint that hands Omar the complete
manual list for all three requirements (Sentry keys, PostHog key, optional perf run).

Purpose: QUAL-06 — prove the platform holds ~500 concurrent users with an acceptable P95 and
that RLS auth checks are initplan-cached, not per-row. NO migrations this phase.

Output: scripts/perf-seed-500.{sql,mjs}, scripts/perf-p95.mjs, docs/OBSERVABILITY.md perf
section, 17-VERIFICATION.md, and the operator checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-observabilite-perf/17-CONTEXT.md
@.planning/phases/17-observabilite-perf/17-01-SUMMARY.md
@.planning/phases/17-observabilite-perf/17-02-SUMMARY.md

<interfaces>
<!-- Existing Node-script conventions to match. -->

From scripts/smoke-rls-prod.mjs (PostgREST timing pattern — anon client + signInWithPassword + sb.from(table).select):
```js
import { createClient } from "@supabase/supabase-js";
const sb = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);
await sb.auth.signInWithPassword({ email, password });
// timed: const t0 = performance.now(); await sb.from("submissions").select(...); const dt = performance.now() - t0;
```

From scripts/perf-baseline.mjs (existing perf script: argv usage, writeFileSync JSON output, console.table).

Critical read paths to time (from CLAUDE.md routes + RLS tables):
- /journey data: player_members + submissions + missions/deliverable_templates for a Player session
- evaluation: submissions + evaluations for a Mentor session
- jury: pitch_scores / pitch_criteria for a Jury session

database/** is Write/Edit-DENIED (settings.local.json). The seed .sql lives in scripts/ (allowed),
applied by the operator via Supabase MCP execute_sql or psql on a DISPOSABLE project. NO migration.
RLS source of truth for the grep: database/rls.sql (read-only grep, no edit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent 500-user seed (.sql + .mjs) with integral cleanup</name>
  <read_first>scripts/smoke-rls-prod.mjs, scripts/perf-baseline.mjs, database/rls.sql</read_first>
  <files>scripts/perf-seed-500.sql, scripts/perf-seed-500.mjs</files>
  <action>
    Create scripts/perf-seed-500.sql — an idempotent seed that inserts ~500 synthetic Players (+ minimal player_members + a batch of submissions across deliverable_templates) under a single dedicated marker so cleanup is total. Requirements:
    - All inserts use ON CONFLICT DO NOTHING (idempotent — safe to re-run).
    - Tag every synthetic row with an identifiable marker (e.g. a fixed perf_seed organization/cohort id or an email domain like @perf-seed.invalid) so the cleanup block deletes EXACTLY the synthetic rows and nothing else.
    - Include a clearly fenced "-- CLEANUP (run to fully remove perf seed)" section at the bottom (commented out) that DELETEs every synthetic row by the marker, in FK-safe order. This is the integral cleanup path required by CONTEXT.md.
    - Header comment: default target = a DISPOSABLE Supabase project; PROD-off-event use is an operator decision requiring the cleanup block to be run afterward (CONTEXT.md).
    - Respect dual-mode: this is operator-run SQL, not app code; do not touch lib/ or database/**.

    Create scripts/perf-seed-500.mjs — a Node variant (matching perf-baseline.mjs conventions) that does the same seeding through @supabase/supabase-js with the service role key (env: SUPABASE_SERVICE_ROLE_KEY + NEXT_PUBLIC_SUPABASE_URL), batched inserts, idempotent upserts, and a `--cleanup` flag that removes all marker-tagged rows. Print a summary (rows created / skipped). Guard: refuse to run without an explicit COUNT target arg and the service role key (fail fast like smoke-rls-prod.mjs).
  </action>
  <verify>
    <automated>node --check scripts/perf-seed-500.mjs && node -e "const t=require('fs').readFileSync('scripts/perf-seed-500.sql','utf8'); const ok=/on conflict/i.test(t) && /CLEANUP/i.test(t); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/perf-seed-500.mjs` passes (valid syntax).
    - perf-seed-500.sql contains ON CONFLICT (idempotent) AND a CLEANUP section keyed on the synthetic marker.
    - perf-seed-500.mjs supports a `--cleanup` flag and fails fast without service role key + count arg.
    - No edits to database/** or lib/**.
  </acceptance_criteria>
  <done>Idempotent disposable-default 500-user seed exists in both SQL and Node form with an integral cleanup path.</done>
</task>

<task type="auto">
  <name>Task 2: P95 measurement script + static RLS initplan verification</name>
  <read_first>scripts/smoke-rls-prod.mjs, scripts/perf-baseline.mjs, database/rls.sql</read_first>
  <files>scripts/perf-p95.mjs</files>
  <action>
    Create scripts/perf-p95.mjs (matching perf-baseline.mjs conventions: argv, writeFileSync JSON, console.table):
    - Env-driven (NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY + per-role SMOKE creds, like smoke-rls-prod.mjs). Fail fast if missing.
    - For each critical path, sign in as the appropriate role and run the representative PostgREST select N times (N from argv, default 100), recording per-call latency via performance.now():
        path "journey"   -> Player: player_members + submissions selects
        path "evaluation"-> Mentor: submissions + evaluations selects
        path "jury"      -> Jury: pitch_scores + pitch_criteria selects
    - Compute and report per-path P50, P95, P99, min, max (sort latencies, pick the index at ceil(0.95*N)-1 for P95). Implement a small percentile(sortedArr, p) helper containing the literal token `p95` in code/output.
    - Write a JSON report to the argv output path and console.table the summary.
    - This runs against whatever project the operator points it at (disposable project seeded in Task 1, or PROD off-event). It does NOT itself seed or clean up.

    Also add, in the same script OR as a documented grep in Task 3, the RLS initplan static check. Implement it here as an optional `--check-rls` mode: read database/rls.sql, assert that EVERY occurrence of auth.uid() is wrapped as (select auth.uid()) — i.e. there are ZERO bare `auth.uid()` calls not preceded by `select ` (case-insensitive). Per CONTEXT.md this is already true since kc2/14/16; the script proves it and exits non-zero if any per-row auth.uid() is found. Use a comment-stripped scan (ignore lines starting with --) to avoid false positives from prose.
  </action>
  <verify>
    <automated>node --check scripts/perf-p95.mjs && node scripts/perf-p95.mjs --check-rls</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/perf-p95.mjs` passes.
    - `node scripts/perf-p95.mjs --check-rls` exits 0 (every auth.uid() in database/rls.sql is wrapped as (select auth.uid())).
    - The script computes P50/P95/P99 per critical path and writes a JSON report.
    - Fails fast without required env (no silent run against an empty target).
  </acceptance_criteria>
  <done>P95 measurement script times the 3 critical paths and a static RLS initplan check confirms (SELECT auth.uid()) wrapping.</done>
</task>

<task type="auto">
  <name>Task 3: Perf runbook section + 17-VERIFICATION.md (QUAL-04/05/06 evidence)</name>
  <read_first>docs/OBSERVABILITY.md, .planning/phases/15-mission-engine/15-VERIFICATION.md</read_first>
  <files>docs/OBSERVABILITY.md, .planning/phases/17-observabilite-perf/17-VERIFICATION.md</files>
  <action>
    Append a "## Perf (QUAL-06)" section to docs/OBSERVABILITY.md documenting the perf runbook:
      1. Decide target: DISPOSABLE Supabase project (default) vs PROD off-event (operator decision; if PROD, the cleanup block MUST be run after).
      2. Seed: `node scripts/perf-seed-500.mjs 500 <report>` (or apply scripts/perf-seed-500.sql via MCP/psql).
      3. Measure: `node scripts/perf-p95.mjs <N> <out.json>` with the per-role SMOKE creds env.
      4. RLS check: `node scripts/perf-p95.mjs --check-rls` + how to run EXPLAIN ANALYZE in Supabase SQL editor to confirm initplan caching of auth.uid().
      5. Cleanup: `node scripts/perf-seed-500.mjs --cleanup` (or the SQL CLEANUP block).

    Create .planning/phases/17-observabilite-perf/17-VERIFICATION.md (follow the 15-VERIFICATION.md structure): map QUAL-04, QUAL-05, QUAL-06 each to its evidence:
      - QUAL-04: instrumentation files + global-error + reportServerError call sites; note "keys pasted at operator checkpoint, capture verifiable post-deploy".
      - QUAL-05: analytics-provider + 4 eg_* events + R1 grep result; note key at checkpoint.
      - QUAL-06: a P95 results table (fill with the operator's run output, or mark "to be run at checkpoint — script ready" with the command) + RLS initplan verdict (`--check-rls` exit 0).
      Map the 3 ROADMAP success criteria (Sentry error visible + alert; PostHog funnel visible; P95 measured + RLS initplan verified) to each requirement's evidence. List any item deferred to the operator checkpoint explicitly.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const o=fs.readFileSync('docs/OBSERVABILITY.md','utf8'); const v=fs.readFileSync('.planning/phases/17-observabilite-perf/17-VERIFICATION.md','utf8'); const ok=/## Perf/.test(o) && /perf-p95/.test(o) && /QUAL-04/.test(v) && /QUAL-05/.test(v) && /QUAL-06/.test(v); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/OBSERVABILITY.md gains a "## Perf (QUAL-06)" section with the seed/measure/check/cleanup commands.
    - 17-VERIFICATION.md maps QUAL-04, QUAL-05, QUAL-06 to evidence and the 3 ROADMAP success criteria.
    - Items requiring the operator (keys, optional perf run) are listed explicitly.
  </acceptance_criteria>
  <done>Perf runbook documented; 17-VERIFICATION.md traces all 3 requirements + 3 success criteria with evidence and operator-deferred items.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Phase 17 wiring is complete and fully no-op without keys: Sentry (server/client/edge + global-error + 3 critical-branch captures), PostHog (lazy env-gated provider + 4 R1-safe funnel events), and the perf toolkit (500-user seed + P95 script + RLS initplan check). Quality gate (typecheck/lint/build/105 unit/24 e2e) is green with NO keys present. The remaining steps are the ones only Omar can do (create third-party accounts, paste secrets into Vercel, optionally run the perf seed).
  </what-built>
  <how-to-verify>
    Complete the operator setup (all documented in docs/OBSERVABILITY.md):

    1. SENTRY (QUAL-04): create a free Sentry account + Next.js project, copy the DSN, paste SENTRY_DSN + NEXT_PUBLIC_SENTRY_DSN into Vercel Production env, configure the email alert rule on new issues, redeploy. Then trigger a known server-error path and confirm the issue appears in the Sentry dashboard with a stack trace (ROADMAP SC #1).

    2. POSTHOG (QUAL-05): create a free PostHog account + project, copy the Project API key, paste NEXT_PUBLIC_POSTHOG_KEY (+ optional NEXT_PUBLIC_POSTHOG_HOST) into Vercel Production env, redeploy. Build the funnel insight (onboarding -> submit -> validated -> mentor eval) and confirm test events flow (ROADMAP SC #2).

    3. PERF (QUAL-06, optional now / before freeze): decide disposable project vs PROD off-event. Run `node scripts/perf-seed-500.mjs 500 ...`, `node scripts/perf-p95.mjs <N> out.json`, `node scripts/perf-p95.mjs --check-rls`. Paste the P95 table + RLS verdict into 17-VERIFICATION.md. If PROD was used, run `--cleanup` afterward (ROADMAP SC #3).
  </how-to-verify>
  <resume-signal>Type "done" once keys are in Vercel (Sentry capture + PostHog funnel verified) and the perf run is either complete or explicitly deferred to pre-freeze.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perf seed -> Supabase | Bulk synthetic rows written to a project (disposable or PROD off-event) |
| perf-p95 -> PostgREST | Authenticated read load against critical-path tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | perf-seed-500 against PROD | mitigate | Default target = disposable project; every synthetic row carries a unique marker; integral cleanup block deletes exactly the seed; operator checkpoint forces explicit target decision |
| T-17-08 | Denial of service | P95 load run against live PROD | mitigate | CONTEXT.md mandates PROD use only off-event; checkpoint is blocking-human; N bounded by argv |
| T-17-09 | Repudiation | RLS initplan regression | mitigate | `--check-rls` static gate fails non-zero if any bare auth.uid() (per-row) is reintroduced |
</threat_model>

<verification>
- `node --check scripts/perf-seed-500.mjs` and `node --check scripts/perf-p95.mjs` pass.
- `node scripts/perf-p95.mjs --check-rls` exits 0 (RLS initplan confirmed).
- scripts/perf-seed-500.sql is idempotent (ON CONFLICT) with an integral CLEANUP block.
- docs/OBSERVABILITY.md has Sentry + PostHog + Perf sections; 17-VERIFICATION.md maps QUAL-04/05/06.
- Operator checkpoint reached with the complete manual list for all 3 requirements.
</verification>

<success_criteria>
- Idempotent 500-user seed (disposable default, integral cleanup) in both SQL and Node form.
- P95 measurement script times /journey, evaluation, jury critical paths and reports P50/P95/P99.
- Static RLS initplan check confirms every auth.uid() is wrapped as (SELECT auth.uid()).
- 17-VERIFICATION.md traces QUAL-04/05/06 + the 3 ROADMAP success criteria with evidence.
- Operator checkpoint hands Omar the complete keys + perf-run list for all three requirements.
</success_criteria>

<output>
Create `.planning/phases/17-observabilite-perf/17-03-SUMMARY.md` when done.
</output>
