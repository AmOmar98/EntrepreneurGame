---
phase: 17-observabilite-perf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - instrumentation.ts
  - instrumentation-client.ts
  - app/global-error.tsx
  - app/actions.ts
  - .env.example
  - docs/OBSERVABILITY.md
autonomous: true
requirements: [QUAL-04]
must_haves:
  truths:
    - "With no SENTRY_DSN set, build + typecheck + lint + 105 unit + 24 e2e stay green (no-op Sentry)"
    - "A server-side error thrown inside a Next request is routed to Sentry.captureRequestError via onRequestError"
    - "A client-side render crash is captured by Sentry through the global-error boundary"
    - "A critical server-action error branch reports the failure to Sentry without changing the WorkflowState contract (still returns, never throws)"
  artifacts:
    - path: "instrumentation.ts"
      provides: "Server/edge Sentry.init (env-gated) + onRequestError export"
      contains: "onRequestError"
    - path: "instrumentation-client.ts"
      provides: "Client Sentry.init (env-gated on NEXT_PUBLIC_SENTRY_DSN)"
      contains: "Sentry.init"
    - path: "app/global-error.tsx"
      provides: "React error boundary that calls Sentry.captureException"
      contains: "captureException"
    - path: "lib/observability.ts"
      provides: "reportServerError no-op-safe helper wrapping captureException"
      exports: ["reportServerError"]
  key_links:
    - from: "app/actions.ts"
      to: "lib/observability.ts"
      via: "reportServerError in supabase-error branches"
      pattern: "reportServerError"
    - from: "instrumentation.ts"
      to: "@sentry/nextjs"
      via: "captureRequestError"
      pattern: "captureRequestError"
---

<objective>
Wire @sentry/nextjs into the app, fully env-gated, so production server and client
errors land in a Sentry dashboard with stack traces — without any effect (and zero
build/CI degradation) when SENTRY_DSN / NEXT_PUBLIC_SENTRY_DSN are absent.

Purpose: QUAL-04 — prod errors are captured and alertable. The DSN keys are pasted
by Omar in Vercel at the Phase 17 operator checkpoint (see 17-03); this plan delivers
the wiring so the requirement is "implemented, keys at checkpoint".

Output: instrumentation.ts (server/edge), instrumentation-client.ts (client),
app/global-error.tsx (client boundary), lib/observability.ts (no-op-safe server helper),
captureException calls in the 3 most critical app/actions.ts error branches,
.env.example documentation, and the Sentry section of docs/OBSERVABILITY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observabilite-perf/17-CONTEXT.md

<interfaces>
<!-- Existing anchors the executor builds against. No exploration needed. -->

From next.config.ts (current — minimal, do NOT wrap with withSentryConfig; sourcemaps
upload is explicitly deferred per CONTEXT.md "sourcemaps optionnels — éviter de compliquer le build CI"):
```ts
const nextConfig: NextConfig = { images: { remotePatterns: [{ protocol: "https", hostname: "api.dicebear.com" }] } };
```

From app/actions.ts (WorkflowState contract — NEVER throw; failures return a shape):
```ts
export type WorkflowState = { ok: boolean; message: string; mailto?: string };
// Critical error branches already return { ok: false, message: error.message }:
//   submitDeliverable  -> insErr branch (~line 454) and subErr branch (~line 378)
//   evaluateSubmission -> supabase error branch (~line 490+)
//   saveOnboarding     -> updateError branch (~line 180)
```

Next 15 instrumentation contract (@sentry/nextjs >= 8):
- instrumentation.ts: `export async function register()` + `export const onRequestError = Sentry.captureRequestError`
- instrumentation-client.ts: top-level Sentry.init() (replaces deprecated sentry.client.config.ts)
- app/global-error.tsx: client component, calls Sentry.captureException(error) in useEffect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @sentry/nextjs + env-gated server/edge/client init</name>
  <read_first>next.config.ts, .env.example, package.json</read_first>
  <files>package.json, package-lock.json, instrumentation.ts, instrumentation-client.ts, .env.example</files>
  <action>
    Install @sentry/nextjs (npm install @sentry/nextjs — pin a current v8/v9 line compatible with Next 15.5). Do NOT run the Sentry wizard and do NOT wrap next.config.ts with withSentryConfig (sourcemap upload deferred per CONTEXT.md).

    Create instrumentation.ts at repo root:
    - export async function register(): if process.env.NEXT_RUNTIME === "nodejs" or "edge" AND process.env.SENTRY_DSN is set, call Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0, enabled: true }). If SENTRY_DSN is absent, do nothing (no-op).
    - export const onRequestError = Sentry.captureRequestError (this routes server request errors to Sentry; harmless no-op when init never ran).

    Create instrumentation-client.ts at repo root:
    - At top level, guard on process.env.NEXT_PUBLIC_SENTRY_DSN. If set, call Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 0, replaysSessionSampleRate: 0, replaysOnErrorSampleRate: 0 }). Session/replay recording MUST be OFF (R1/privacy — CONTEXT.md). If absent, no init.

    Append to .env.example a "# Sentry - error monitoring (env-gated, no-op when absent)" block documenting SENTRY_DSN (server, set in Vercel) and NEXT_PUBLIC_SENTRY_DSN (client, set in Vercel). Note that both absent = zero effect, CI/demo unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - With no SENTRY_DSN / NEXT_PUBLIC_SENTRY_DSN env vars set, `npm run build` succeeds.
    - instrumentation.ts exports both `register` and `onRequestError`.
    - instrumentation-client.ts contains no replay/session-recording sampling above 0.
    - .env.example documents both new keys.
  </acceptance_criteria>
  <done>@sentry/nextjs installed; server+client init are env-gated and no-op without keys; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Client error boundary + no-op-safe server helper + critical-branch capture</name>
  <read_first>app/actions.ts (lines 200-470), app/layout.tsx</read_first>
  <files>app/global-error.tsx, lib/observability.ts, app/actions.ts</files>
  <action>
    Create app/global-error.tsx ("use client"): a Next global-error boundary component accepting { error, reset }, calling Sentry.captureException(error) inside a useEffect, rendering a minimal French fallback ("Une erreur est survenue") with a retry button calling reset(). Wrap in <html lang="fr"><body>...</body></html> as required for global-error.

    Create lib/observability.ts: export function reportServerError(error: unknown, context?: Record<string, string>): void that calls Sentry.captureException with the optional context as tags/extra. Sentry.captureException is itself a no-op when init never ran, so this helper is safe in demo/CI mode — but ALSO short-circuit cleanly if needed. Keep it side-effect-free beyond the capture. This helper centralizes capture so action code stays clean and never throws.

    In app/actions.ts, import reportServerError from @/lib/observability and call it in EXACTLY these 3 critical error branches (the supabase-write failures most likely to indicate prod breakage), immediately before the existing `return { ok: false, message: ... }`:
      1. submitDeliverable — the insErr branch (~line 454)
      2. evaluateSubmission — its supabase error branch
      3. saveOnboarding — the updateError branch (~line 180)
    Pass a context tag like { action: "submitDeliverable" }. Do NOT change the returned WorkflowState shape or messages. Do NOT add try/catch or throw. This is additive capture only (R: surgical changes).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n reportServerError app/actions.ts` returns exactly 3 call sites plus 1 import.
    - app/global-error.tsx contains `captureException` inside a useEffect.
    - 105 unit tests still green (no WorkflowState contract change).
    - No `throw` introduced in app/actions.ts diff.
  </acceptance_criteria>
  <done>Client crashes captured via global-error; 3 critical server-action branches report to Sentry no-op-safely; WorkflowState contract unchanged; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Sentry section of docs/OBSERVABILITY.md runbook</name>
  <read_first>docs/DEPLOY.md, .env.example</read_first>
  <files>docs/OBSERVABILITY.md</files>
  <action>
    Create docs/OBSERVABILITY.md with a top-level intro and a "## Sentry (QUAL-04)" section (the PostHog and Perf sections are appended by plans 17-02 and 17-03 — leave clear section anchors). The Sentry section documents, as a runbook for Omar:
      1. Create a free Sentry account + a Next.js project; copy the DSN.
      2. Paste SENTRY_DSN and NEXT_PUBLIC_SENTRY_DSN into Vercel Production env vars (same DSN value works for both); redeploy.
      3. Configure the default email alert rule (Sentry Alerts → "Issues" → email on new issue) — note that Slack/WhatsApp webhook alerting is backlog v0.5 (deferred).
      4. How to verify capture end-to-end: trigger a known server error path (e.g. a deliberately invalid submission against a misconfigured supabase) and confirm the issue appears in the Sentry dashboard with a stack trace.
      5. Note that without the DSN env vars the SDK is fully no-op (demo/CI unaffected).
    Keep it ASCII (no accented chars in code-resident strings per repo convention is for code; prose docs may use French but keep mailto/CSV-style payloads ASCII — plain French prose is fine here).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/OBSERVABILITY.md','utf8'); const ok=/## Sentry/.test(t) && /SENTRY_DSN/.test(t) && /Vercel/.test(t); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/OBSERVABILITY.md exists with a "## Sentry (QUAL-04)" heading.
    - References SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN, Vercel env, and the email alert rule.
    - Leaves room (separate headings) for PostHog and Perf sections.
  </acceptance_criteria>
  <done>OBSERVABILITY.md runbook created with the Sentry account/keys/alert/verify procedure documented for the operator checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Sentry SaaS | Error payloads (stack traces, request context) leave the app to a third party |
| npm registry → build | New dependency @sentry/nextjs is installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | Sentry error payloads | mitigate | Session/replay recording OFF; tracesSampleRate 0; do not attach PII beyond technical request context; R1 — no score/rank in captured action context tags |
| T-17-02 | Tampering | @sentry/nextjs npm install | mitigate | slopcheck — @sentry/nextjs is the official first-party Sentry SDK (npmjs.com/package/@sentry/nextjs, Sentry org), legitimate; pin to a current published version |
| T-17-03 | Denial of service | Sentry quota exhaustion | accept | Free tier quota; pilot volume ~30 sessions; tracesSampleRate 0 keeps event volume to errors only |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build` green with NO Sentry env vars set.
- `npm run test:unit` → 105 green; `npm run test:e2e` → 24 green (env-gated SDK must not degrade CI).
- instrumentation.ts, instrumentation-client.ts, app/global-error.tsx, lib/observability.ts exist.
- `grep -n reportServerError app/actions.ts` → 3 call sites + 1 import.
</verification>

<success_criteria>
- @sentry/nextjs wired server + client + edge, fully env-gated (no-op without DSN).
- Client crashes captured via global-error boundary; server request errors via onRequestError.
- 3 critical server-action error branches report to Sentry without breaking the never-throw WorkflowState contract.
- .env.example + docs/OBSERVABILITY.md document the keys + alert rule for the operator checkpoint.
- Full quality gate (typecheck/lint/build/105 unit/24 e2e) green with no keys present.
</success_criteria>

<output>
Create `.planning/phases/17-observabilite-perf/17-01-SUMMARY.md` when done.
</output>
