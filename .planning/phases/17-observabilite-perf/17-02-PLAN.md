---
phase: 17-observabilite-perf
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - package.json
  - package-lock.json
  - lib/analytics.ts
  - components/analytics-provider.tsx
  - app/layout.tsx
  - components/submission-form.tsx
  - components/onboarding-stepper.tsx
  - components/mentor-evaluation-panel.tsx
  - .env.example
  - docs/OBSERVABILITY.md
autonomous: true
requirements: [QUAL-05]
must_haves:
  truths:
    - "With no NEXT_PUBLIC_POSTHOG_KEY set, posthog-js never initializes; build + 105 unit + 24 e2e stay green"
    - "posthog-js is lazy-loaded (dynamic import) so the main bundle is not inflated when the key is present"
    - "Funnel events eg_onboarding_completed, eg_deliverable_submitted, eg_deliverable_validated, eg_mentor_eval_submitted fire from the existing state.ok touchpoints"
    - "No score/rank/note payload is sent to PostHog from any Player-facing capture (R1 cardinal)"
  artifacts:
    - path: "lib/analytics.ts"
      provides: "captureEvent no-op-safe helper + event name constants (eg_*)"
      exports: ["captureEvent", "ANALYTICS_EVENTS"]
    - path: "components/analytics-provider.tsx"
      provides: "Env-gated lazy posthog-js init + pageview capture (client)"
      contains: "NEXT_PUBLIC_POSTHOG_KEY"
  key_links:
    - from: "app/layout.tsx"
      to: "components/analytics-provider.tsx"
      via: "AnalyticsProvider mounted in body"
      pattern: "AnalyticsProvider"
    - from: "components/submission-form.tsx"
      to: "lib/analytics.ts"
      via: "captureEvent in state.ok useEffect"
      pattern: "captureEvent"
    - from: "components/mentor-evaluation-panel.tsx"
      to: "lib/analytics.ts"
      via: "captureEvent in state.ok useEffect"
      pattern: "captureEvent"
---

<objective>
Wire posthog-js (client-only, lazy-loaded, env-gated) to trace the product funnel:
onboarding -> deliverable submitted -> deliverable validated -> mentor eval submitted.
Events are technical-id-only and carry ZERO score/rank data (R1 cardinal). Fully no-op
without NEXT_PUBLIC_POSTHOG_KEY, so demo mode and CI are unchanged.

Purpose: QUAL-05 — funnel completion per deliverable and drop-offs become visible in
PostHog. The key is pasted by Omar in Vercel at the Phase 17 operator checkpoint (17-03).

Output: lib/analytics.ts (no-op-safe captureEvent helper + eg_* event constants),
components/analytics-provider.tsx (lazy env-gated init + pageviews), AnalyticsProvider
mounted in app/layout.tsx, captureEvent calls in the 4 funnel touchpoints, .env.example
+ docs/OBSERVABILITY.md PostHog section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-observabilite-perf/17-CONTEXT.md
@.planning/phases/17-observabilite-perf/17-01-SUMMARY.md

<interfaces>
<!-- Existing funnel touchpoints. All already have a state.ok useEffect — add capture there. -->

From app/layout.tsx (root anchor — AnalyticsProvider mounts inside body, sibling of children + SpeedInsights):
```tsx
<html className={...} lang="fr"><body>{children}<SpeedInsights /></body></html>
```

From components/submission-form.tsx (~line 44 — existing success effect; ADD captureEvent here):
```tsx
useEffect(() => { if (state.ok) { clear(); router.refresh(); } }, [state.ok, clear, router]);
```

From components/onboarding-stepper.tsx (~line 75 — existing success effect; ADD captureEvent here):
```tsx
useEffect(() => { if (state.ok) { router.push("/journey"); } }, [state.ok, router]);
```

From components/mentor-evaluation-panel.tsx (~line 59 — existing success effect; ADD captureEvent here).
The component owns a `verdict` local state ("validate_v1" | "request_v2" | "validate_v2" | "reject")
and `version` (1|2) + `submissionId` props.

R1 cardinal (CLAUDE.md): score/rank/note are forbidden in Player-facing payloads.
PostHog events MUST carry only technical ids (deliverableTemplateId, submissionId, version)
— NEVER total, scores, verdict-as-numeric-score, rank, percentile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install posthog-js + no-op-safe analytics helper + lazy env-gated provider</name>
  <read_first>app/layout.tsx, package.json, .env.example</read_first>
  <files>package.json, package-lock.json, lib/analytics.ts, components/analytics-provider.tsx, app/layout.tsx, .env.example</files>
  <action>
    Install posthog-js (npm install posthog-js — current published version).

    Create lib/analytics.ts (client-safe module, no "use server"):
    - export const ANALYTICS_EVENTS with the 4 funnel event names: eg_onboarding_completed, eg_deliverable_submitted, eg_deliverable_validated, eg_mentor_eval_submitted.
    - export function captureEvent(event: string, props?: Record<string, string | number | boolean>): void. It must reference the already-initialized posthog instance (import posthog from "posthog-js" and check posthog.__loaded, or a module flag the provider sets). If posthog is not initialized (no key), it is a silent no-op. NEVER throw. Add a comment: props MUST NOT include score/rank/note (R1).

    Create components/analytics-provider.tsx ("use client"):
    - On mount (useEffect), if process.env.NEXT_PUBLIC_POSTHOG_KEY is set, dynamically import("posthog-js") (lazy — keeps it out of the main bundle) and call posthog.init(key, { api_host: process.env.NEXT_PUBLIC_POSTHOG_HOST ?? "https://us.i.posthog.com", capture_pageview: false, disable_session_recording: true, autocapture: false }). Session recording OFF (R1/privacy); autocapture off so we only emit explicit eg_* events. Capture pageviews manually on pathname change via usePathname + posthog.capture("$pageview").
    - If the key is absent, never import posthog (the dynamic import sits behind the env check, so zero bundle/runtime cost).
    - Component returns null (no DOM); it owns init + pageview side effects only.

    Mount <AnalyticsProvider /> in app/layout.tsx inside <body>, as a sibling of {children} (alongside <SpeedInsights />). Keep layout a server component (no "use client").

    Append to .env.example a "# PostHog - product funnel analytics (env-gated, lazy, no-op when absent)" block: NEXT_PUBLIC_POSTHOG_KEY and optional NEXT_PUBLIC_POSTHOG_HOST. Note R1: no score/rank captured; session recording OFF.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - With no NEXT_PUBLIC_POSTHOG_KEY set, `npm run build` succeeds.
    - components/analytics-provider.tsx uses a dynamic `import("posthog-js")` (lazy), not a top-level static import of the SDK.
    - posthog init options set disable_session_recording true and autocapture false.
    - app/layout.tsx mounts AnalyticsProvider; layout has no "use client" directive.
    - .env.example documents NEXT_PUBLIC_POSTHOG_KEY.
  </acceptance_criteria>
  <done>posthog-js installed; analytics helper + lazy env-gated provider created and mounted; build green with no key.</done>
</task>

<task type="auto">
  <name>Task 2: Fire the 4 funnel events from existing state.ok touchpoints (R1-safe)</name>
  <read_first>components/submission-form.tsx, components/onboarding-stepper.tsx, components/mentor-evaluation-panel.tsx, lib/analytics.ts</read_first>
  <files>components/submission-form.tsx, components/onboarding-stepper.tsx, components/mentor-evaluation-panel.tsx</files>
  <action>
    Add captureEvent calls inside the EXISTING `if (state.ok)` useEffect blocks — no new effects, no new state, surgical additions only. Import captureEvent + ANALYTICS_EVENTS from @/lib/analytics in each file.

    1. components/onboarding-stepper.tsx (~line 75): inside `if (state.ok)`, before router.push, call captureEvent(ANALYTICS_EVENTS.eg_onboarding_completed). No props (or a non-score technical prop only).

    2. components/submission-form.tsx (~line 44): inside `if (state.ok)`, call captureEvent(ANALYTICS_EVENTS.eg_deliverable_submitted, { deliverableTemplateId, version }). version is the 1|2 prop; deliverableTemplateId is the existing prop. NO score/total.

    3. components/mentor-evaluation-panel.tsx (~line 59): inside `if (state.ok)`, call captureEvent(ANALYTICS_EVENTS.eg_mentor_eval_submitted, { submissionId, version }). CRITICAL R1: do NOT pass total, scores, scoresJson, or any numeric grade. submissionId + version only.

    4. eg_deliverable_validated (funnel stage marker): also from the mentor-evaluation-panel state.ok effect, fire captureEvent(ANALYTICS_EVENTS.eg_deliverable_validated, { submissionId, version }) ONLY when the existing `verdict` local state is a validate verdict (validate_v1 or validate_v2). Gate solely on the verdict literal (a stage label) — never pass the numeric total. The verdict literal is a funnel-stage label, not a score/rank, so it is R1-safe.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test:unit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn captureEvent components/submission-form.tsx components/onboarding-stepper.tsx components/mentor-evaluation-panel.tsx` shows the 4 event calls.
    - R1 audit: `grep -rn captureEvent components/ | grep -iE "score|total|rank|note|/100|/140|percentile"` returns ZERO matches.
    - 105 unit + 24 e2e green (capture is no-op in test/demo mode).
  </acceptance_criteria>
  <done>4 funnel events fire from existing success effects with technical-id-only props; R1 grep clean; full gate green.</done>
</task>

<task type="auto">
  <name>Task 3: PostHog section of docs/OBSERVABILITY.md</name>
  <read_first>docs/OBSERVABILITY.md, .env.example</read_first>
  <files>docs/OBSERVABILITY.md</files>
  <action>
    Append a "## PostHog (QUAL-05)" section to the existing docs/OBSERVABILITY.md (created in 17-01). Document for Omar:
      1. Create a free PostHog account + project; copy the Project API key + host.
      2. Paste NEXT_PUBLIC_POSTHOG_KEY (and optional NEXT_PUBLIC_POSTHOG_HOST) into Vercel Production env; redeploy.
      3. The 4 funnel events captured (eg_onboarding_completed, eg_deliverable_submitted, eg_deliverable_validated, eg_mentor_eval_submitted) and what each marks.
      4. How to build the funnel insight in PostHog UI: New insight -> Funnel -> ordered steps onboarding_completed -> deliverable_submitted -> deliverable_validated -> mentor_eval_submitted; drop-off between steps = product friction.
      5. Privacy/R1 note: session recording OFF, autocapture OFF, no score/rank in payloads, technical ids only.
      6. No key = fully no-op (CI/demo unaffected; SDK lazy-loaded so bundle is unaffected too).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/OBSERVABILITY.md','utf8'); const ok=/## PostHog/.test(t) && /NEXT_PUBLIC_POSTHOG_KEY/.test(t) && /eg_deliverable_submitted/.test(t) && /Funnel/i.test(t); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/OBSERVABILITY.md gains a "## PostHog (QUAL-05)" section.
    - References NEXT_PUBLIC_POSTHOG_KEY, the 4 eg_* events, and the funnel build steps.
  </acceptance_criteria>
  <done>PostHog runbook section appended: account/key/funnel-build/privacy documented for the operator checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PostHog SaaS | Event payloads (technical ids) leave the browser to a third party |
| npm registry -> build | New dependency posthog-js is installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information disclosure | PostHog event payloads | mitigate | R1 — captureEvent props carry only technical ids; grep gate forbids score/rank/note; session recording + autocapture OFF |
| T-17-05 | Tampering | posthog-js npm install | mitigate | slopcheck — posthog-js is the official first-party PostHog SDK (npmjs.com/package/posthog-js, PostHog org), legitimate; pin to a current published version |
| T-17-06 | Information disclosure | Auto-captured PII / IP | mitigate | autocapture disabled (no incidental DOM capture); no PII beyond technical user/submission ids |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build` green with NO PostHog env var set.
- `npm run test:unit` -> 105 green; `npm run test:e2e` -> 24 green.
- lib/analytics.ts + components/analytics-provider.tsx exist; provider uses dynamic import.
- R1 grep: no score/rank/note in any captureEvent call across components/.
</verification>

<success_criteria>
- posthog-js wired client-only, lazy-loaded, env-gated (no-op without key, no bundle inflation).
- 4 funnel events fire from existing success effects, technical-ids only, R1-clean.
- AnalyticsProvider mounted in layout without making layout a client component.
- .env.example + docs/OBSERVABILITY.md document the key + funnel build for the operator checkpoint.
- Full quality gate (typecheck/lint/build/105 unit/24 e2e) green with no key present.
</success_criteria>

<output>
Create `.planning/phases/17-observabilite-perf/17-02-SUMMARY.md` when done.
</output>
