---
phase: 03-mentor-flow-evaluation-boucle-v1-v2-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/mentor/page.tsx
  - lib/mentor.ts
  - lib/i18n.ts
  - components/mentor-players-table.tsx
  - components/mentor-pending-filter.tsx
autonomous: true
requirements:
  - EVAL-01
  - SCORE-02
must_haves:
  truths:
    - "Mentor connecté voit la liste de tous les Players de la cohorte de l'event seed sur /mentor"
    - "Chaque ligne affiche nom équipe, projet, niveau, score Projet courant, nb soumis / nb total de DeliverableTemplates de l'event"
    - "Mentor peut activer un filtre 'En attente de revue' qui restreint la liste aux Players ayant au moins une Submission sans Evaluation du mentor courant"
    - "Mentor peut cliquer sur une ligne pour aller voir le détail (Submissions à évaluer) — lien vers /mentor/submission/[id] depuis Plan 02"
    - "Un visiteur non-mentor ouvrant /mentor est redirigé via pathForRole(role)"
  artifacts:
    - path: "lib/mentor.ts"
      provides: "Server data layer: getMentorPlayersOverview(userId) returning per-Player aggregates (score, submitted/total, pending submissions list)"
      min_lines: 80
    - path: "app/mentor/page.tsx"
      provides: "Server component rendering mentor list with role gate, filter URL state"
    - path: "components/mentor-players-table.tsx"
      provides: "Client component rendering the table + 'En attente revue' toggle"
    - path: "components/mentor-pending-filter.tsx"
      provides: "Client filter toggle that updates ?pending=1 search param"
  key_links:
    - from: "app/mentor/page.tsx"
      to: "lib/mentor.ts:getMentorPlayersOverview"
      via: "server fetch"
      pattern: "getMentorPlayersOverview"
    - from: "lib/mentor.ts"
      to: "Supabase tables players, player_members, submissions, evaluations, deliverable_templates, missions"
      via: "createClient queries"
      pattern: "from\\(\"(players|submissions|evaluations|deliverable_templates|missions)\""
---

<objective>
Construire la page d'accueil Mentor `/mentor` : liste des Players de l'event avec score Projet courant, compteur soumis/total et filtre "En attente de revue". Couvre EVAL-01 et SCORE-02 (vue serveur du score Projet).

Purpose: donner au Mentor un point d'entrée pour repérer les Submissions qui attendent son verdict.
Output: page `/mentor` fonctionnelle, data layer `lib/mentor.ts` réutilisable par Plan 02, composants table/filter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mentor-flow-evaluation-boucle-v1-v2-scoring/03-CONTEXT.md
@CLAUDE.md
@database/schema.sql
@database/rls.sql
@lib/types.ts
@lib/auth.ts
@lib/journey.ts
@lib/i18n.ts
@app/mentor/page.tsx

<interfaces>
<!-- Existing contracts the executor must reuse - no exploration needed. -->

From lib/auth.ts:
```typescript
export async function getCurrentUser(): Promise<User | null>;
export async function getCurrentRole(): Promise<AppRole | null>;
export function pathForRole(role: AppRole): string; // "mentor" -> "/mentor"
```

From lib/types.ts (relevant subset):
```typescript
export type AppRole = "player" | "mentor" | "game_master";
export type Player = {
  id: string;
  cohortId: string;
  slug: string;
  name: string;
  idea: string | null;
  currentLevel: LevelId;
  status: PlayerStatus;
  scoreProject: number;
  scoreEngagement: number;
  onboardedAt: string | null;
};
```

From lib/journey.ts (reuse):
```typescript
export function levelLabel(levelId: LevelId): string;
// Mapper pattern (snake_case -> camelCase) used throughout - mirror it in lib/mentor.ts.
```

From utils/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient | null>; // null in demo mode
```

DB shape (database/schema.sql):
- players(id, cohort_id, name, idea, current_level, score_project, status)
- player_members(player_id, user_id, role)
- submissions(id, player_id, deliverable_template_id, version, status, submitted_at)
- evaluations(submission_id, evaluator_id, verdict)  // unique(submission_id, evaluator_id)
- deliverable_templates(id, mission_id, ord, title)
- missions(id, event_id, level_id)
- cohorts(id, event_id)

RLS: mentor role passes `is_mentor()`, sees all players/submissions/evaluations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/mentor.ts - server data layer for /mentor overview</name>
  <files>lib/mentor.ts</files>
  <action>
Créer `lib/mentor.ts` qui expose `getMentorPlayersOverview(opts?: { onlyPending?: boolean })`.

Pattern : suivre `lib/journey.ts` exactement (mappers snake_case→camelCase, `createClient()` peut être null → retourner `[]`).

Étapes serveur (en mode Supabase) :
1. Résoudre l'event courant : prendre l'unique row de `events` via `supabase.from("events").select("id").order("starts_at", { ascending: false }).limit(1).maybeSingle()`. Pour le pilote il n'y a qu'un event seed (EVENT-01).
2. Charger toutes les `cohorts` de l'event puis tous les `players` de ces cohorts (`select id, cohort_id, name, idea, current_level, score_project, status` ordered by `name`).
3. Compter le total de `deliverable_templates` rattachés aux missions de l'event (via join missions→deliverable_templates) → `totalDeliverables: number`.
4. Pour chaque player : compter ses `submissions` (`count: "exact"` ou groupBy en mémoire) → `submittedCount`.
5. Identifier les Submissions "en attente de revue par moi" : `submissions` du player qui n'ont AUCUNE row `evaluations` avec `evaluator_id = currentUserId` ET dont `status` ∈ {`submitted_v1`, `submitted_v2`}. Stocker dans `pendingSubmissionIds: string[]` par player.

Exporter le type :
```typescript
export type MentorPlayerOverview = {
  player: Player;
  levelLabel: string;
  submittedCount: number;
  totalDeliverables: number;
  pendingSubmissionIds: string[];
};
export async function getMentorPlayersOverview(opts?: { onlyPending?: boolean }): Promise<MentorPlayerOverview[]>;
```

Filtre `onlyPending`: si true, retirer les players avec `pendingSubmissionIds.length === 0`.

Mode démo (`!supabase` ou pas d'event) → retourner `[]`. Aucune fuite de seed (DATA-03).

Aucune erreur ne doit fuiter ; en cas d'erreur Supabase, return `[]` et `console.error` (cohérent avec lib/journey.ts).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- Module exporte `MentorPlayerOverview` et `getMentorPlayersOverview`.
- Mode démo retourne tableau vide.
- typecheck propre.
  </done>
</task>

<task type="auto">
  <name>Task 2: composants mentor-players-table + mentor-pending-filter</name>
  <files>components/mentor-players-table.tsx, components/mentor-pending-filter.tsx</files>
  <action>
**components/mentor-pending-filter.tsx** (`"use client"`) :
- Toggle (button ou checkbox) `Filtrer en attente de revue`.
- Lit `useSearchParams` ; au clic, push `?pending=1` ou retire le param via `useRouter().push(...)`.
- Petit composant ~30 lignes.

**components/mentor-players-table.tsx** (server component, pas de "use client") :
- Reçoit `rows: MentorPlayerOverview[]`.
- Rend un `<table>` avec colonnes : Équipe (`player.name`), Idée (`player.idea` truncated 60 chars), Niveau (`levelLabel`), Score Projet (`player.scoreProject.toFixed(2)`), Soumis (`submittedCount/totalDeliverables`), En attente (`pendingSubmissionIds.length` — badge orange si > 0).
- La colonne "En attente" est un lien `<Link href={`/mentor/submission/${pendingSubmissionIds[0]}`}>` quand au moins 1 pending (le détail Plan 02 paginera entre Submissions du Player) ; sinon "—".
- État vide : `<p>Aucun Player dans la cohorte.</p>` si rows.length === 0.
- Style inline cohérent avec composants existants (`#0f172a` titres, `#64748b` muted, `#fef3c7`/`#92400e` badge orange).
- Toutes les chaînes via `dictionaries.fr` (clés ajoutées Task 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- 2 composants créés, props typées.
- Filtre met à jour searchParams.
- Table accessible (`<thead><th scope="col">`).
  </done>
</task>

<task type="auto">
  <name>Task 3: page /mentor + clés i18n</name>
  <files>app/mentor/page.tsx, lib/i18n.ts</files>
  <action>
**lib/i18n.ts** : ajouter les clés FR + EN (toujours en duo) :
- `mentor_title: "Espace Mentor"`
- `mentor_subtitle: "Évaluez les livrables de la cohorte."`
- `mentor_filter_pending: "Afficher uniquement les Players avec livrables en attente"`
- `mentor_col_team: "Équipe"`
- `mentor_col_idea: "Idée"`
- `mentor_col_level: "Niveau"`
- `mentor_col_score: "Score Projet"`
- `mentor_col_submitted: "Soumis / Total"`
- `mentor_col_pending: "En attente"`
- `mentor_review_action: "Évaluer"`
- `mentor_empty: "Aucun Player dans la cohorte."`
- `mentor_demo_disabled: "Mode démo : connectez-vous à Supabase pour voir les Players."`

(Respecter la convention "pas d'accents dans les chaînes code-resident" présente dans i18n.ts existant — utiliser `Idee` au lieu de `Idée` si conformité ASCII souhaitée. Vérifier les clés existantes du fichier ; si elles utilisent des accents `é/è/à`, conserver les accents ; sinon les éviter. **Décision** : suivre le style observé dans lib/i18n.ts ligne par ligne — actuellement plain ASCII, donc utiliser plain ASCII : "Espace Mentor", "Evaluez les livrables...", "Idee", "Evaluer".)

**app/mentor/page.tsx** : remplacer le scaffold par :

```tsx
import { redirect } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { MentorPlayersTable } from "@/components/mentor-players-table";
import { MentorPendingFilter } from "@/components/mentor-pending-filter";
import { getCurrentRole, getCurrentUser, pathForRole } from "@/lib/auth";
import { dictionaries } from "@/lib/i18n";
import { hasSupabaseEnv } from "@/lib/supabase-status";
import { getMentorPlayersOverview } from "@/lib/mentor";

const t = dictionaries.fr;

export default async function MentorPage({
  searchParams,
}: {
  searchParams: Promise<{ pending?: string }>;
}) {
  const user = await getCurrentUser();
  if (!user) redirect("/login");

  const role = await getCurrentRole();
  if (role && role !== "mentor" && role !== "game_master") {
    redirect(pathForRole(role));
  }

  const { pending } = await searchParams;
  const onlyPending = pending === "1";

  const rows = hasSupabaseEnv()
    ? await getMentorPlayersOverview({ onlyPending })
    : [];

  return (
    <AppShell role={role ?? "mentor"}>
      <main style={{ padding: 24, maxWidth: 1100 }}>
        <h1 style={{ fontSize: 22, fontWeight: 600, margin: "0 0 4px", color: "#0f172a" }}>
          {t.mentor_title}
        </h1>
        <p style={{ color: "#64748b", fontSize: 14, margin: "0 0 16px" }}>{t.mentor_subtitle}</p>
        <MentorPendingFilter active={onlyPending} />
        {!hasSupabaseEnv() ? (
          <p style={{ color: "#64748b", fontSize: 14, marginTop: 16 }}>{t.mentor_demo_disabled}</p>
        ) : (
          <MentorPlayersTable rows={rows} />
        )}
      </main>
    </AppShell>
  );
}
```

GameMaster (qui passe `is_mentor()` côté DB) doit aussi pouvoir consulter `/mentor` → autoriser `mentor` ET `game_master`.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <done>
- `/mentor` rend la liste en mode Supabase configuré, message démo sinon.
- Filtre `?pending=1` filtre la liste.
- Tous les rôles non-mentor/non-GM redirigent vers leur dashboard.
- lint + typecheck clean.
  </done>
</task>

</tasks>

<verification>
- En mode démo : `/mentor` affiche le titre + message démo, aucune ligne, aucun crash.
- En mode Supabase avec event seed Phase 2 chargé : `/mentor` affiche les Players de la cohorte avec scores et compteurs corrects.
- Filtre `?pending=1` : aucune ligne tant qu'aucune Submission Player n'existe ; sinon ne montre que ceux avec `pendingSubmissionIds.length > 0`.
- Player connecté → redirigé vers `/journey`.
</verification>

<success_criteria>
EVAL-01 partiellement couvert : la liste + filtre existent. Le clic vers la page d'évaluation Submission est implémenté en Plan 02.
SCORE-02 (côté Mentor) : score Projet temps réel visible.
</success_criteria>

<output>
Créer `.planning/phases/03-mentor-flow-evaluation-boucle-v1-v2-scoring/03-01-SUMMARY.md`.
</output>
