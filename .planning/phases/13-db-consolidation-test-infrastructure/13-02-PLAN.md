---
phase: 13-db-consolidation-test-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - lib/schemas.ts
  - app/actions.ts
  - tests/unit/actions-schemas.test.ts
  - tests/unit/score.test.ts
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "Vitest is installed and `npm run test:unit` runs in demo mode (no Supabase env) without importing Next.js runtime modules"
    - "The Zod schemas backing the critical server actions (submission, evaluation, kyc) are testable in isolation"
    - "At least 5 unit tests pass covering submission/evaluation schema validation + pure scoring logic"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with node environment and @/* alias resolution"
      contains: "resolve"
    - path: "lib/schemas.ts"
      provides: "Extracted pure Zod schemas re-imported by app/actions.ts"
      exports: ["submissionSchema", "evaluationSchema", "httpsUrl"]
    - path: "tests/unit/actions-schemas.test.ts"
      provides: "Schema validation unit tests"
      contains: "describe"
    - path: "tests/unit/score.test.ts"
      provides: "Pure scoring-logic unit tests"
      contains: "describe"
  key_links:
    - from: "app/actions.ts"
      to: "lib/schemas.ts"
      via: "import of extracted schemas"
      pattern: "from \"@/lib/schemas\""
    - from: "vitest.config.ts"
      to: "lib/*"
      via: "resolve.alias @ -> repo root"
      pattern: "alias"
---

<objective>
Install Vitest and create the unit-test safety net for the pure-logic surface of the critical server actions — without executing any `"use server"` action (which is impossible in a plain Node/Vitest runtime). The testable surface is the Zod schemas (extracted to `lib/schemas.ts`) plus the pure functions in `lib/score.ts`.

Purpose: QUAL-01 requires green unit tests covering the validation logic of the submission, evaluation, and onboarding flows. The schema-extraction approach satisfies the ROADMAP intent ("couvre les server actions critiques") while honoring the locked constraint "do NOT attempt to execute 'use server' actions in Vitest". Extracting schemas to a thin module is the cleanest seam and is reused by phases 15-16 when the validation engine becomes data-driven.

Output: `vitest.config.ts`, `lib/schemas.ts`, `tests/unit/*.test.ts`, a `test:unit` npm script, and Vitest as a devDependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-db-consolidation-test-infrastructure/13-PATTERNS.md

# Schema source (extract these — do not re-implement)
@app/actions.ts
# Pure-logic source to test
@lib/score.ts
@lib/supabase-status.ts
# Config conventions to mirror
@package.json
@playwright.config.ts

<interfaces>
<!-- Schemas to extract from app/actions.ts into lib/schemas.ts. Move definitions, then re-import in actions.ts so behavior is unchanged. -->

From app/actions.ts (lines 184-240, 491-513):
- `httpsUrl` — z.string().url().refine(startsWith "https://", "URL doit commencer par https://")
- `submissionSchema` — { deliverableTemplateId: uuid, kind: enum["proof_url","proof_text"], proofUrl?, proofText?(max 16000) } with superRefine: proof_url requires valid httpsUrl; proof_text requires trimmed length >= 10
- `fichesEntretienSchema` — { fiches: array({url: httpsUrl}).length(10) }
- `evaluationSchema` — { submissionId: uuid, feedback: string(0..4000), verdict: enum["validate_v1","request_v2","validate_v2","reject"], expectedAction?(max 500), scores: record(string, coerce.number 0..25) } with superRefine: verdict request_v2 requires non-empty trimmed expectedAction

From lib/score.ts:
- `scoreFromEvaluation(evaluation: Evaluation): number`
- `combineScores(...)` (signature at line 41)
- `getEngagementMilestones(...)` (line 94)
- `applyBonusMultiplier(args): ...` (line 186) — pure, no Supabase

From lib/supabase-status.ts:
- `hasSupabaseEnv()` — returns false when NEXT_PUBLIC_SUPABASE_URL / ANON_KEY are unset (the demo-mode seam; leave env unset in tests)
</interfaces>

<constraints>
- `package.json` declares `"type": "commonjs"`. Use `vitest.config.ts` (plain .ts) — NOT `.mts` (forces ESM) and NOT `.js` (would be CJS and break `export default`). Vitest compiles `.ts` with its own bundler.
- `tsconfig.json` uses `module: esnext` / `moduleResolution: bundler` — these are Next.js bundler settings Vitest does NOT use. The `@/*` alias is NOT auto-resolved; you MUST set `resolve.alias` in vitest.config.ts mapping `@` to the repo root.
- Do NOT import `app/actions.ts` in any unit test — it has `"use server"` at line 1 and imports `next/navigation` (redirect) + `next/cache` (revalidatePath). Import schemas from `lib/schemas.ts` instead.
- `environment: "node"` (not jsdom) — pure logic and process.env checks only.
- Cardinal rules R1/R2/R3 untouched (no Player-facing code). The schema extraction must leave `app/actions.ts` behavior identical — actions.ts re-imports the moved schemas; do not change validation semantics.
- Branch: `milestone/v0.4-scale-foundation`. No push to main.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest, create vitest.config.ts, extract schemas to lib/schemas.ts</name>
  <read_first>
    - app/actions.ts (lines 184-240 for httpsUrl/submissionSchema/fichesEntretienSchema; lines 491-513 for evaluationSchema; find kycSchema for saveOnboardingKyc) — these are the definitions to MOVE, not copy
    - package.json (current scripts + "type": "commonjs" + devDependencies block)
    - playwright.config.ts (config-file conventions: defineConfig import, ESM export default in a .ts file)
    - 13-PATTERNS.md (vitest.config.ts template lines 49-67, the "type: commonjs" pitfall, the alias pitfall)
  </read_first>
  <action>
    (1) Add Vitest to devDependencies: run `npm install -D vitest` (this installs vitest + its bundler; no jsdom needed for node environment). Confirm it lands in package.json devDependencies.
    (2) Create `vitest.config.ts` (plain .ts) importing `defineConfig` from `vitest/config` and `path` from node. Set `test.environment = "node"`, `test.include = ["tests/unit/**/*.test.ts"]`, `test.globals = false`. Set `resolve.alias` with key `"@"` mapped to `path.resolve(__dirname, ".")` so `@/lib/...` resolves to repo root (mirrors tsconfig `@/*` -> `./*`).
    (3) Create `lib/schemas.ts` (new module, `import { z } from "zod"`). MOVE these schema definitions out of `app/actions.ts` and export them: `httpsUrl`, `submissionSchema`, `fichesEntretienSchema`, `evaluationSchema`, and the onboarding kyc schema (find its current name in actions.ts — likely `kycSchema`). Preserve the superRefine logic and all messages EXACTLY. lib/schemas.ts must import ONLY from `zod` — no `next/*`, no `"use server"`.
    (4) Edit `app/actions.ts` to import the moved schemas from `@/lib/schemas` (e.g., `import { submissionSchema, evaluationSchema, httpsUrl, fichesEntretienSchema, kycSchema } from "@/lib/schemas"`) and DELETE the now-duplicated local definitions. Every existing usage of these schema identifiers in actions.ts must still resolve. Run `npm run typecheck` to prove no broken references.
    (5) Add a `"test:unit": "vitest run"` script to package.json (alongside the existing `smoke` script).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (tsc --noEmit, no errors) — proving the schema move left actions.ts references intact
    - package.json devDependencies includes `vitest` and scripts includes `"test:unit": "vitest run"`
    - vitest.config.ts sets `environment: "node"`, `include: ["tests/unit/**/*.test.ts"]`, and `resolve.alias` mapping `"@"` to the repo root
    - lib/schemas.ts exports `httpsUrl`, `submissionSchema`, `fichesEntretienSchema`, `evaluationSchema`, and the kyc schema, and imports only from `zod` (grep shows no `next/` import and no `"use server"` in lib/schemas.ts)
    - app/actions.ts imports the schemas from `@/lib/schemas` and no longer redeclares them locally (grep for `const submissionSchema =` in actions.ts returns 0)
  </acceptance_criteria>
  <done>Vitest installed and configured for Node + @/* alias; schemas extracted to lib/schemas.ts and re-imported by actions.ts with typecheck green; test:unit script added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for schemas and pure scoring logic</name>
  <read_first>
    - lib/schemas.ts (output of Task 1 — the schemas under test)
    - lib/score.ts (pure functions: scoreFromEvaluation, combineScores, applyBonusMultiplier — confirm they have no Supabase import before testing)
    - tests/smoke/r1-no-score.spec.ts (test-style conventions: describe/it, structural assertions, no exact French-string assertions)
    - 13-PATTERNS.md (the "WorkflowState Return Shape" + "Zod safeParse Pattern" shared-pattern sections, and the test-file structure example lines 138-162)
  </read_first>
  <behavior>
    submissionSchema:
    - Test 1: kind="proof_url" with proofUrl="http://example.com" -> safeParse success === false
    - Test 2: kind="proof_url" with proofUrl="https://example.com" + valid uuid -> success === true
    - Test 3: kind="proof_text" with proofText="short" (<10 chars) -> success === false
    evaluationSchema:
    - Test 4: verdict="request_v2" with empty expectedAction -> success === false, issue path includes "expectedAction"
    - Test 5: verdict="validate_v1" with empty expectedAction + valid scores -> success === true
    - Test 6 (edge): scores value 26 (> max 25) -> success === false
    score.ts pure logic:
    - Test 7: scoreFromEvaluation / combineScores returns the expected numeric value for a known input (assert exact number, not "looks right")
  </behavior>
  <action>
    Create `tests/unit/actions-schemas.test.ts` importing `{ describe, it, expect }` from `vitest` and the schemas from `@/lib/schemas`. Implement Tests 1-6 from the behavior block. Call `schema.safeParse(plainObject)` directly — do NOT construct FormData. For the uuid field use `"00000000-0000-0000-0000-000000000001"`. For Test 4 assert `result.success === false` AND that some issue has `path` containing `"expectedAction"` (structural assertion, not the French message text). Do not assert exact French strings anywhere.
    Create `tests/unit/score.test.ts` importing from `@/lib/score`. Implement Test 7+ against the pure functions only (`scoreFromEvaluation`, `combineScores`, `applyBonusMultiplier`). Construct minimal `Evaluation`-shaped input objects from `lib/types.ts` shapes. Assert exact numeric outputs so the test is a real regression guard (e.g., for combineScores with DEFAULT_PITCH_WEIGHT 0.8: assert the computed combined value equals the hand-computed expected number). Do NOT import anything that reaches Supabase (skip `consumeBonusMultiplier`/`hasActiveBonus` if they are async/Supabase-bound).
    Run `npm run test:unit` and confirm at least 7 tests pass and 0 fail.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` exits 0 with at least 7 passing tests and 0 failures
    - tests/unit/actions-schemas.test.ts implements all 6 schema tests from the behavior block, calling safeParse on plain objects (grep shows no `FormData` usage in the file)
    - The request_v2 test asserts an issue path containing "expectedAction" structurally (not by French message string)
    - tests/unit/score.test.ts asserts at least one exact numeric output of a pure lib/score.ts function
    - No unit test imports `app/actions.ts` (grep for `app/actions` in tests/unit returns 0)
    - No unit test sets NEXT_PUBLIC_SUPABASE_URL or NEXT_PUBLIC_SUPABASE_ANON_KEY (demo mode preserved)
  </acceptance_criteria>
  <done>At least 7 green Vitest unit tests cover submission/evaluation schema validation (incl. the request_v2 expected-action rule) and exact pure-scoring outputs; no test touches Next.js runtime or Supabase.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes after schema extraction.
- `npm run test:unit` passes with >= 7 tests, 0 failures.
- `npm run build` still succeeds (actions.ts re-imports work in the Next.js bundler) — run once to confirm the extraction did not break the production build.
- lib/schemas.ts has no Next.js or "use server" coupling.
</verification>

<success_criteria>
- ROADMAP Phase 13 success criterion 3 satisfied: Vitest configured and >= 5 unit tests pass covering the critical actions' validation logic (we deliver >= 7).
- The schema extraction is behavior-preserving (typecheck + build green; actions.ts validation semantics unchanged).
</success_criteria>

<output>
Create `.planning/phases/13-db-consolidation-test-infrastructure/13-02-SUMMARY.md` when done. Record: Vitest version installed, schemas extracted, test count, and confirmation that build + typecheck stayed green.
</output>
