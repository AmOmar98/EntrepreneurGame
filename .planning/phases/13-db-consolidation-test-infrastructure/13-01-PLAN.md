---
phase: 13-db-consolidation-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md
  - .planning/phases/13-db-consolidation-test-infrastructure/OPS-02-verdict.md
  - .planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql
  - database/triggers.sql
  - database/rls.sql
autonomous: false
requirements: [OPS-01, OPS-02]
must_haves:
  truths:
    - "A drift report lists every divergence between database/*.sql source and PROD (functions, grants, policies, columns)"
    - "13-NEW.sql contains the consolidation SQL needed to make database/ the source of truth (2 PROD-only functions + grant reconciliation)"
    - "Applying database/ to a fresh Supabase project would produce a state matching PROD (verifiable via MCP function/grant listing)"
    - "DIGI-08 is closed: a verdict doc states whether the 15/05 pre-event publish suffices or a Digi pitch_scores backfill is required, backed by a read-only PROD query"
  artifacts:
    - path: ".planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md"
      provides: "Enumerated drift table database/ vs PROD"
      min_lines: 30
    - path: ".planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql"
      provides: "Idempotent consolidation SQL (CREATE OR REPLACE + REVOKE/GRANT)"
      contains: "set_help_requests_updated_at"
    - path: ".planning/phases/13-db-consolidation-test-infrastructure/OPS-02-verdict.md"
      provides: "DIGI-08 backfill decision with PROD evidence"
      min_lines: 20
  key_links:
    - from: "13-NEW.sql"
      to: "database/triggers.sql"
      via: "operator-applied mirror of CREATE OR REPLACE for 2 PROD-only functions"
      pattern: "set_(help_requests_updated_at|pitch_mode_closed_at)"
    - from: "13-NEW.sql"
      to: "database/rls.sql"
      via: "operator-applied mirror of REVOKE FROM PUBLIC + GRANT TO authenticated grants"
      pattern: "REVOKE EXECUTE"
---

<objective>
Resolve the known drift between `database/*.sql` (the committed source of truth) and the live PROD Supabase schema, and close the DIGI-08 tech-debt item by deciding whether the Digi event's `pitch_scores` need a backfill.

Purpose: OPS-01 is the blocking pre-requisite for every schema refactor in phases 14+. If `database/` is not aligned with PROD, applying it to a fresh project (the basis of multi-tenant work) would silently lose the 2 PROD-only trigger functions and the grant hardening from quick-260523-kc2. DIGI-08 must be closed with evidence so the v0.3 archive is provably clean.

Output: A drift report (`OPS-01-drift-report.md`), the consolidation SQL (`13-NEW.sql`), a DIGI-08 verdict (`OPS-02-verdict.md`), and operator-applied mirrors into `database/triggers.sql` + `database/rls.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-db-consolidation-test-infrastructure/13-PATTERNS.md

# Drift origin and exact known divergences (read these — they ARE the spec)
@.planning/quick/260523-kc2-advisors-fix/SUMMARY.md
@.planning/quick/260523-kc2-advisors-fix/D1.sql
@.planning/quick/260523-kc2-advisors-fix/D2.sql
@.planning/quick/260523-kc2-advisors-fix/D3.sql
@.planning/quick/260515-gu4-publish-results-hybrid-pitch-proxy-pilot/260515-gu4-SUMMARY.md
@.planning/quick/260515-gu4-publish-results-hybrid-pitch-proxy-pilot/backfill.sql

# Current source-of-truth files (the canonical state to reconcile)
@database/triggers.sql
@database/rls.sql

<constraints>
- `database/**` is Write/Edit-DENIED in settings.local.json. You CANNOT edit `database/triggers.sql` or `database/rls.sql` directly with Write/Edit.
  Workaround (memory `feedback_database_deny_workaround`): author all SQL in `13-NEW.sql` (phase dir, which IS writable), apply to PROD via MCP `execute_sql`, then mirror into `database/*.sql` via the documented Node-script paste OR an operator checkpoint where Omar applies the diff. This plan uses an operator checkpoint (autonomous: false).
- PROD writes for OPS-01: NONE expected. The grants and search_path were already applied to PROD by quick-260523-kc2. OPS-01 is reconciling the SOURCE files to match PROD, not changing PROD. Verify state read-only first.
- PROD writes for OPS-02: NONE in this plan. OPS-02 is a read-only investigation + verdict. Any actual backfill SQL is gated behind the verdict and an operator checkpoint — do not write to PROD pitch_scores without explicit Omar approval.
- Branch: `milestone/v0.4-scale-foundation`. No push to main.
- Supabase MCP token has a ~1h lifetime (memory): run MCP reads in one focused pass; if a token-expired error appears, surface it as a checkpoint rather than retrying blindly.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff database/ source against PROD and write the drift report</name>
  <read_first>
    - .planning/quick/260523-kc2-advisors-fix/SUMMARY.md (the exact known drifts: 2 PROD-only functions, 4 search_path fixes, 11 secdef grants)
    - .planning/quick/260523-kc2-advisors-fix/D1.sql (the 4 ALTER FUNCTION search_path lines applied to PROD)
    - .planning/quick/260523-kc2-advisors-fix/D3.sql (the 11 REVOKE/GRANT pairs applied to PROD)
    - database/triggers.sql (current source — note lines 169-178 already document the 2-function gap)
    - database/rls.sql (current source — check whether D2 policy rewrites and D3 grants are present in the file)
  </read_first>
  <files>.planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md</files>
  <action>
    Build an authoritative drift table comparing committed `database/*.sql` against live PROD across four dimensions: functions, grants, policies, columns. Use the Supabase MCP tool `mcp__plugin_supabase_supabase__execute_sql` with READ-ONLY queries:
    (a) Functions + proconfig: query `pg_proc` joined to `pg_namespace` (schema `public`) selecting `proname`, `proconfig`, `prosecdef` — confirm `set_help_requests_updated_at` and `set_pitch_mode_closed_at` exist in PROD with `proconfig` containing `search_path=""`, and confirm the other 4 simple trigger functions carry the same search_path.
    (b) Grants: query `information_schema.role_routine_grants` (or `pg_proc.proacl`) for the 11 SECURITY DEFINER functions named in D3.sql — confirm PUBLIC has 0 EXECUTE and `authenticated` has explicit EXECUTE.
    (c) Policies: query `pg_policies` for the 5 policies rewritten in D2.sql (`help_requests_player_insert_own`, `jurors_self_select`, `pitch_scores_juror_self_insert`, `pitch_scores_juror_self_update`, `pitch_scores_select_visibility`) — confirm `qual`/`with_check` use `(SELECT auth.uid())` form.
    (d) Columns: spot-check that `pitch_scores.total_score` is a generated stored column and `events.results_published_at` exists (these are referenced by OPS-02).
    For EACH dimension, classify every PROD object as one of: PRESENT-IN-SOURCE, PROD-ONLY (missing from source — drift), SOURCE-ONLY (stale — drift). Write `.planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md` as a markdown table with columns: Dimension | Object | PROD state | Source (database/*.sql) state | Drift? | Consolidation action. The two PROD-only functions and any D2/D3 changes absent from the source files are the expected primary drift entries.
  </action>
  <verify>
    <automated>grep -c "PROD-ONLY\|SOURCE-ONLY\|PRESENT-IN-SOURCE" .planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md</automated>
  </verify>
  <acceptance_criteria>
    - OPS-01-drift-report.md exists with a markdown table covering all 4 dimensions (functions, grants, policies, columns)
    - The table lists `set_help_requests_updated_at` and `set_pitch_mode_closed_at` as PROD-ONLY drift entries with consolidation action "source into database/triggers.sql"
    - Each of the 11 D3 secdef functions has a grant-dimension row stating PROD PUBLIC EXECUTE = 0 and authenticated EXECUTE = present
    - Every row has a non-empty "Consolidation action" cell
    - The report cites the PROD evidence source (which MCP query produced each finding)
  </acceptance_criteria>
  <done>Drift report enumerates all database/ vs PROD divergences across functions/grants/policies/columns with a consolidation action per drift row, backed by read-only MCP query evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Author 13-NEW.sql consolidation + investigate DIGI-08 and write OPS-02 verdict</name>
  <read_first>
    - .planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md (output of Task 1 — drives the consolidation SQL)
    - .planning/quick/260523-kc2-advisors-fix/D1.sql (ALTER FUNCTION search_path pattern + the 2 PROD-only function names)
    - .planning/quick/260523-kc2-advisors-fix/D3.sql (verbatim REVOKE/GRANT block to mirror)
    - database/triggers.sql lines 8-16, 57-84, 169-178 (CREATE OR REPLACE pattern + search_path conventions + the documented gap comment)
    - .planning/quick/260515-gu4-publish-results-hybrid-pitch-proxy-pilot/260515-gu4-SUMMARY.md (the AgreenTech backfill — DIGI-08 is the analogous question for the Digi event)
    - .planning/quick/260515-gu4-publish-results-hybrid-pitch-proxy-pilot/backfill.sql (UPSERT-on-conflict idempotent pattern, in case Digi backfill is needed)
  </read_first>
  <files>.planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql, .planning/phases/13-db-consolidation-test-infrastructure/OPS-02-verdict.md</files>
  <action>
    PART A — Consolidation SQL. Write `.planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql` (phase dir is writable; do NOT touch database/* here). Header comment must state: idempotent, apply via MCP execute_sql or Omar manual, no destructive statements. Include:
    (1) `create or replace function public.set_help_requests_updated_at()` returning trigger, `language plpgsql`, `set search_path = ''`, body assigns `new.updated_at = now(); return new;` — copy the body EXACTLY from PROD (retrieve via MCP `pg_get_functiondef` if the simple body is uncertain).
    (2) `create or replace function public.set_pitch_mode_closed_at()` — retrieve the exact PROD body via MCP `select pg_get_functiondef(oid)` since this function sets `pitch_mode_closed_at`; do NOT guess the body. Preserve `set search_path = ''` and its security/volatility class as it exists in PROD.
    (3) The grant reconciliation block: copy the 11 REVOKE EXECUTE ... FROM PUBLIC + GRANT EXECUTE ... TO authenticated pairs verbatim from D3.sql (function signatures must match exactly, including arg types like `(p_event_id uuid)`).
    Because these are already applied to PROD, running 13-NEW.sql against PROD must be a no-op (CREATE OR REPLACE + idempotent GRANT). The PRIMARY purpose of 13-NEW.sql is to be the artifact mirrored into the source files in Task 3.
    PART B — DIGI-08 investigation. Using MCP read-only queries, determine the Digi event's pitch state: (i) find the Digi event row in `events` (slug containing `digi`) and read its `results_published_at` and `pitch_mode_state`; (ii) count `pitch_scores` rows scoped to that `event_id`, grouped by `juror_id`. Decide the verdict: if results were already published (proxy 15/05 path or a real Digi publish) AND the partner/archive audience does not require live per-juror Digi scores, the existing state SUFFICES — no backfill. If pitch_scores is empty for the Digi event AND a published ranking is expected, a backfill is REQUIRED. Write `.planning/phases/13-db-consolidation-test-infrastructure/OPS-02-verdict.md` stating: the PROD query results (event id, results_published_at value, pitch_scores count by juror), the decision (SUFFICIENT / BACKFILL-REQUIRED), and the rationale. If BACKFILL-REQUIRED, append a 13-NEW.sql PART 3 stub using the `backfill.sql` ON CONFLICT DO UPDATE pattern and flag it for the operator checkpoint — DO NOT execute any pitch_scores write in this task.
  </action>
  <verify>
    <automated>grep -c "create or replace function public.set_help_requests_updated_at\|create or replace function public.set_pitch_mode_closed_at\|REVOKE EXECUTE" .planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql</automated>
  </verify>
  <acceptance_criteria>
    - 13-NEW.sql contains `create or replace function public.set_help_requests_updated_at()` with `set search_path = ''`
    - 13-NEW.sql contains `create or replace function public.set_pitch_mode_closed_at()` with a body retrieved from PROD (not a placeholder) and `set search_path = ''`
    - 13-NEW.sql contains all 11 `REVOKE EXECUTE ... FROM PUBLIC` + matching `GRANT EXECUTE ... TO authenticated` pairs with exact function signatures from D3.sql
    - 13-NEW.sql has no `DROP`, no `DELETE`, no `TRUNCATE`, and no unconditional `UPDATE`/`INSERT` against PROD data outside the gated DIGI-08 stub
    - OPS-02-verdict.md states the Digi event id, its `results_published_at` value, the per-juror `pitch_scores` count from PROD, a SUFFICIENT-or-BACKFILL-REQUIRED decision, and the rationale
    - If verdict is BACKFILL-REQUIRED, the backfill SQL is present as a PART 3 stub flagged for operator approval and is NOT executed against PROD by this task
  </acceptance_criteria>
  <done>13-NEW.sql is an idempotent, no-destructive consolidation that re-applies cleanly to PROD; OPS-02-verdict.md closes DIGI-08 with a PROD-backed decision; any backfill is gated, not executed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator applies 13-NEW.sql mirror into database/*.sql (Write/Edit-deny workaround)</name>
  <files>database/triggers.sql, database/rls.sql</files>
  <action>
    BLOCKING OPERATOR CHECKPOINT — the agent cannot edit `database/**` (Write/Edit-denied). Present 13-NEW.sql and OPS-01-drift-report.md to Omar and request he apply (or confirm already-applied) the mirror into `database/triggers.sql` (the 2 CREATE OR REPLACE function blocks, replacing the gap comment at lines 169-178) and `database/rls.sql` (the 11 REVOKE FROM PUBLIC + GRANT TO authenticated pairs). Do not proceed past this checkpoint until the operator signals "applied" or "already present". Optionally run 13-NEW.sql against PROD via MCP execute_sql first to confirm it is a no-op. If OPS-02 verdict is BACKFILL-REQUIRED, also collect the operator's defer/execute decision on the gated PART 3 backfill.
  </action>
  <verify>
    <human-check>Operator confirms database/triggers.sql + database/rls.sql mirror 13-NEW.sql (or confirms no source change needed) and states the DIGI-08 backfill decision.</human-check>
  </verify>
  <done>database/triggers.sql + database/rls.sql reflect the consolidation (or are confirmed already-aligned), and the DIGI-08 backfill decision (defer/execute) is recorded.</done>
  <what-built>
    13-NEW.sql consolidation SQL (2 PROD-only function definitions + 11 grant reconciliation pairs) and the OPS-01 drift report. Because `database/**` is Write/Edit-denied for the agent, the mirror into `database/triggers.sql` and `database/rls.sql` must be applied by the operator (or via the documented Node-script paste workaround).
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql` and `OPS-01-drift-report.md`.
    2. Apply the 2 `create or replace function` blocks into `database/triggers.sql`, replacing the comment block at lines 169-178 (which documents the gap) with the actual function definitions.
    3. Apply the 11 REVOKE/GRANT pairs into `database/rls.sql` if not already present (D3.sql was committed against rls.sql in quick-260523-kc2 commit fe30c6b — verify whether they are already there; if so, this is a no-op and the drift report should already reflect that).
    4. (Optional integrity check) Run the 13-NEW.sql against PROD via MCP execute_sql to confirm it is a no-op (CREATE OR REPLACE returns success, grants unchanged).
    5. If OPS-02 verdict is BACKFILL-REQUIRED, decide whether to execute the gated PART 3 backfill now or defer.
  </how-to-verify>
  <resume-signal>Type "applied" once database/triggers.sql + database/rls.sql mirror 13-NEW.sql (or confirm "already present, no source change needed"), and state the DIGI-08 backfill decision (defer / execute).</resume-signal>
</task>

</tasks>

<verification>
- `database/triggers.sql` defines `set_help_requests_updated_at` and `set_pitch_mode_closed_at` (the 169-178 gap comment is resolved into real definitions) OR the drift report documents they live in `database/migrations/` and the source is intentionally split.
- `database/rls.sql` contains the 11 REVOKE FROM PUBLIC + GRANT TO authenticated pairs (verify via grep).
- OPS-01-drift-report.md has zero remaining PROD-ONLY drift rows without a consolidation action.
- OPS-02-verdict.md closes DIGI-08 with an explicit decision and PROD evidence.
</verification>

<success_criteria>
- ROADMAP Phase 13 success criterion 1 satisfied: applying `database/` to a fresh Supabase project produces a state matching PROD (the 2 functions + grants are now in source).
- ROADMAP Phase 13 success criterion 2 satisfied: DIGI-08 is closed with a documented verdict in OPS-02-verdict.md.
- No PROD data mutated by this plan (consolidation is no-op; any backfill is operator-gated).
</success_criteria>

<output>
Create `.planning/phases/13-db-consolidation-test-infrastructure/13-01-SUMMARY.md` when done. Record: drift entries found, whether database/ source was changed or already aligned, the DIGI-08 verdict, and the SHA of any commit mirroring the source files.
</output>
