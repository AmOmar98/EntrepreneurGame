---
phase: 13-db-consolidation-test-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [QUAL-03]
must_haves:
  truths:
    - "A GitHub Actions workflow runs on every push and pull request"
    - "The CI gate runs typecheck + lint + build + vitest unit tests; build runs in demo mode (no Supabase secrets)"
    - "Playwright E2E runs in CI on a Linux runner with browsers installed and demo-mode env"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI gate: typecheck, lint, build, unit, e2e"
      contains: "npm run typecheck"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run typecheck/lint/build/test:unit/test:e2e"
      pattern: "npm run (typecheck|lint|build|test:unit|test:e2e)"
    - from: ".github/workflows/ci.yml"
      to: "playwright browsers"
      via: "npx playwright install --with-deps chromium"
      pattern: "playwright install"
---

<objective>
Add a GitHub Actions CI workflow that gates every push and pull request on the full quality suite: typecheck, lint, build, Vitest unit tests, and Playwright E2E — all in demo mode so no Supabase secrets are needed.

Purpose: QUAL-03 (and ROADMAP success criterion 5) require a CI gate that turns the local safety net into an automatic guard for the schema refactor in phases 14-18. This is the final piece that makes the test infrastructure self-enforcing.

Output: `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-db-consolidation-test-infrastructure/13-PATTERNS.md

# Scripts the workflow invokes (must exist before this plan runs — created in 13-02 + 13-03)
@package.json
@playwright.config.ts

<constraints>
- No `.github/` directory exists yet — this plan creates it.
- Dev machine is Windows 11; CI runs `ubuntu-latest` (Linux). Keep all paths/casing Linux-safe. The workflow file is the only artifact, so the main risk is referencing scripts that do not exist — they are created by 13-02 (`test:unit`) and 13-03 (`test:e2e`), hence the depends_on.
- Demo mode in CI: set `NEXT_PUBLIC_SUPABASE_URL: ""` and `NEXT_PUBLIC_SUPABASE_ANON_KEY: ""` as env on the build and e2e steps. No Supabase secrets are configured or needed.
- `"type": "commonjs"` + ESM/TS configs: do NOT add `.cjs` config variants. The existing `.ts`/`.mjs` configs work in CI as-is.
- Playwright on Linux CI requires browser install: `npx playwright install --with-deps chromium`. `reuseExistingServer: !process.env.CI` in playwright.config.ts means CI boots a fresh dev server (set `CI: "true"` in the e2e job env).
- Node 20 (Next 15 supported). Use `actions/setup-node@v4` with `cache: "npm"` and `npm ci`.
- Branch: `milestone/v0.4-scale-foundation`. The workflow `on.push.branches` must include this branch (use `["**"]` to gate all branches).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml with check / unit / e2e jobs</name>
  <read_first>
    - package.json (confirm the exact script names exist: `typecheck`, `lint`, `build`, `test:unit` from 13-02, `test:e2e` from 13-03)
    - playwright.config.ts (confirm webServer uses `reuseExistingServer: !process.env.CI` and the demo-mode env sentinels — the workflow must set CI=true so a fresh server boots)
    - 13-PATTERNS.md (the ci.yml structure pattern lines 379-432 and the 4 critical CI constraints lines 364-377)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. Name the workflow `CI`. Trigger on `push` with `branches: ["**"]` and on `pull_request`. Define three jobs on `runs-on: ubuntu-latest`, each starting with `actions/checkout@v4` then `actions/setup-node@v4` (node-version "20", `cache: "npm"`) then `npm ci`:
    (1) job `check`: run `npm run typecheck`, then `npm run lint`, then `npm run build` — set `env` on the build step (or job) with `NEXT_PUBLIC_SUPABASE_URL: ""` and `NEXT_PUBLIC_SUPABASE_ANON_KEY: ""` for demo mode.
    (2) job `unit` (`needs: check`): run `npm run test:unit`.
    (3) job `e2e` (`needs: check`): run `npx playwright install --with-deps chromium`, then `npm run test:e2e` with job env `CI: "true"`, `NEXT_PUBLIC_SUPABASE_URL: ""`, `NEXT_PUBLIC_SUPABASE_ANON_KEY: ""`.
    Use only the script names that exist in package.json. Do not invent steps that reference secrets. The workflow must be valid YAML (2-space indent, no tabs).
  </action>
  <verify>
    <automated>npx --yes js-yaml .github/workflows/ci.yml > /dev/null && echo "YAML_VALID"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml is valid YAML (the js-yaml parse prints YAML_VALID)
    - The workflow triggers on `push` (branches `["**"]`) and `pull_request`
    - A `check` job runs `npm run typecheck`, `npm run lint`, and `npm run build` with empty Supabase env vars on the build
    - A `unit` job with `needs: check` runs `npm run test:unit`
    - An `e2e` job with `needs: check` runs `npx playwright install --with-deps chromium` then `npm run test:e2e` with `CI: "true"` and empty Supabase env vars
    - The workflow references no GitHub secrets (grep for `secrets.` in ci.yml returns 0)
    - Every `npm run X` referenced (typecheck, lint, build, test:unit, test:e2e) exists as a script in package.json
  </acceptance_criteria>
  <done>.github/workflows/ci.yml is valid YAML gating push/PR on typecheck+lint+build+unit+e2e in demo mode, no secrets, Linux runner with Playwright browsers installed.</done>
</task>

</tasks>

<verification>
- ci.yml parses as valid YAML.
- Every `npm run` script the workflow calls exists in package.json (typecheck, lint, build, test:unit, test:e2e).
- No `secrets.*` references — the gate runs fully on demo mode.
- (Post-merge, observed on GitHub) the workflow appears in the Actions tab and runs green on the first push to milestone/v0.4-scale-foundation.
</verification>

<success_criteria>
- ROADMAP Phase 13 success criterion 5 satisfied: GitHub Actions CI gates every push with typecheck + lint + build + vitest tests green (plus E2E).
- The test infrastructure from 13-02 and 13-03 is now enforced automatically.
</success_criteria>

<output>
Create `.planning/phases/13-db-consolidation-test-infrastructure/13-04-SUMMARY.md` when done. Record: the jobs defined, the demo-mode env strategy, and the SHA of the commit adding the workflow.
</output>
