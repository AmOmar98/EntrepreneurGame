---
phase: 13-db-consolidation-test-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - playwright.config.ts
  - package.json
  - tests/e2e/onboarding.spec.ts
  - tests/e2e/submit-deliverable.spec.ts
  - tests/e2e/mentor-eval.spec.ts
  - tests/e2e/jury-pitch.spec.ts
  - tests/e2e/gm-export.spec.ts
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "Playwright runs the 5 target E2E flows in demo mode (no Supabase env) and all pass"
    - "Each E2E flow asserts non-5xx (or expected redirect) for its primary route plus one meaningful demo-mode assertion"
    - "`npm run test:e2e` runs the new tests/e2e specs without breaking the existing tests/smoke specs"
  artifacts:
    - path: "tests/e2e/onboarding.spec.ts"
      provides: "Onboarding flow demo-mode E2E"
      contains: "test"
    - path: "tests/e2e/submit-deliverable.spec.ts"
      provides: "Submit-deliverable flow demo-mode E2E"
      contains: "test"
    - path: "tests/e2e/mentor-eval.spec.ts"
      provides: "Mentor-eval flow demo-mode E2E"
      contains: "test"
    - path: "tests/e2e/jury-pitch.spec.ts"
      provides: "Jury-pitch flow demo-mode E2E"
      contains: "test"
    - path: "tests/e2e/gm-export.spec.ts"
      provides: "GM CSV-export flow demo-mode E2E"
      contains: "team_slug"
  key_links:
    - from: "tests/e2e/*.spec.ts"
      to: "playwright.config.ts"
      via: "testDir includes tests/e2e + webServer demo-mode env"
      pattern: "tests/e2e"
    - from: "package.json"
      to: "playwright"
      via: "test:e2e script"
      pattern: "test:e2e"
---

<objective>
Build the Playwright E2E safety net covering the 5 critical user flows (onboarding, submit deliverable, mentor eval, jury pitch, GM export), all runnable in demo mode (no Supabase) so they work in CI without secrets.

Purpose: QUAL-02 requires 5 E2E flows; the ROADMAP minimally requires 2 passing (onboarding + submit). Demo mode is the reliable, secret-free harness — the existing `playwright.config.ts` already forces it via empty Supabase env sentinels. These tests lock the contract that the app is fully navigable without a backend (a core CLAUDE.md dual-mode guarantee) before phases 14-16 refactor the schema and routes underneath it.

Output: 5 new specs under `tests/e2e/`, a `playwright.config.ts` that includes both `tests/smoke` and `tests/e2e`, and a `test:e2e` npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-db-consolidation-test-infrastructure/13-PATTERNS.md

# Existing E2E conventions to mirror (header comment, demo-mode notes, structural assertions)
@tests/smoke/r1-no-score.spec.ts
@tests/smoke/r2-warn-only.spec.ts
@tests/smoke/r3-no-hardcoded-block.spec.ts
@playwright.config.ts

# Route demo-mode behavior (so assertions match reality, not assumptions)
@app/onboarding/page.tsx
@app/jury/page.tsx
@app/admin/export/players.csv/route.ts

<interfaces>
<!-- Demo-mode behavior per route (from 13-PATTERNS.md). Assert against THESE, not assumed forms. -->

- /onboarding (app/onboarding/page.tsx lines 34-41): demo mode renders a static disabled <main> message, NO form. Assert: status < 500 AND <main> visible. Do NOT assert form submission.
- /journey (tests/smoke/r1 line 27): renders main.eic-journey even in empty demo state. Assert: main.eic-journey visible + no [disabled] anchors (R3).
- /journey/deliverable/<uuid>: demo mode likely redirects to /login or renders empty. Assert: status < 500.
- /mentor + /mentor/submission/<uuid>: demo mode degrades (empty rows / redirect). Assert: status < 500.
- /jury (app/jury/page.tsx lines 30-36): getCurrentUser() redirects to /login when unauthenticated in demo mode. Assert: status < 500 (302 allowed).
- /admin/export/players.csv (route.ts lines 26-35): demo mode BYPASSES the role gate and returns 200 + header-only CSV. Assert: status === 200 AND body contains the CSV header "team_slug".

Demo seed player UUIDs (lib/seed/players.ts): "00000000-0000-0000-0000-000000000001", "...0002".
</interfaces>

<constraints>
- All E2E specs run in DEMO mode. The existing playwright.config.ts webServer block forces it via empty `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Do NOT add Supabase env.
- Do NOT assert exact French copy strings (they change). Assert structural/behavioral properties: status codes, element visibility, CSV header tokens, absence of forbidden patterns.
- Cardinal rules: the submit-deliverable spec must NOT introduce score/rank visibility expectations on /journey (R1); reuse the absence-of-score discipline from r1-no-score.spec.ts where relevant. R3: assert no `[disabled]` anchor on /journey index.
- This plan depends on 13-02 (Vitest) ONLY because both touch package.json — sequence after it to avoid a script-merge collision. There is no code dependency on the Vitest work.
- Windows dev / Linux CI: keep file names lowercase-hyphen; tests must pass on both. No OS-specific paths in specs.
- Branch: `milestone/v0.4-scale-foundation`. No push to main.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend playwright.config.ts to include tests/e2e + add test:e2e script</name>
  <read_first>
    - playwright.config.ts (current testDir is "./tests/smoke"; webServer demo-mode env block at lines 29-43)
    - package.json (current `smoke` script; add `test:e2e` alongside)
    - 13-PATTERNS.md ("Playwright webServer Demo Mode" shared section + the testDir extension note lines 593-607)
  </read_first>
  <action>
    Update `playwright.config.ts` so it discovers BOTH the existing smoke specs and the new e2e specs without breaking the smoke run. Change `testDir` from `"./tests/smoke"` to `"./tests"` (so both `tests/smoke/**` and `tests/e2e/**` are picked up), keeping `testMatch` at the Playwright default (`**/*.spec.ts`). Leave the webServer demo-mode env block, port 3001, workers, and reporter unchanged. Add a `"test:e2e": "playwright test"` script to package.json (the existing `smoke` script can remain as an alias; do not remove it). Confirm the config still type-checks by running `npm run typecheck`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts `testDir` is `"./tests"` and the webServer env block still sets `NEXT_PUBLIC_SUPABASE_URL: ""` and `NEXT_PUBLIC_SUPABASE_ANON_KEY: ""`
    - package.json scripts contains `"test:e2e": "playwright test"` and the existing `"smoke"` script is unchanged
    - `npm run typecheck` passes
    - The existing 3 smoke specs are still discoverable (running `npx playwright test --list` shows tests from tests/smoke AND tests/e2e once specs are added)
  </acceptance_criteria>
  <done>playwright.config.ts discovers tests/smoke + tests/e2e in demo mode; test:e2e script added; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 5 E2E flow specs (demo mode)</name>
  <read_first>
    - tests/smoke/r1-no-score.spec.ts (header-comment convention + main.eic-journey assertion + score-pattern absence)
    - tests/smoke/r2-warn-only.spec.ts (the /onboarding render-without-500 pattern)
    - tests/smoke/r3-no-hardcoded-block.spec.ts (the multi-route non-5xx loop + no-[disabled]-anchor R3 check)
    - app/onboarding/page.tsx (demo-mode static disabled message — no form)
    - app/jury/page.tsx (getCurrentUser redirect to /login in demo mode)
    - app/admin/export/players.csv/route.ts (demo-mode 200 + header-only CSV containing "team_slug")
    - 13-PATTERNS.md (per-spec analog blocks + the exact assertion patterns for each of the 5 flows)
  </read_first>
  <action>
    Create 5 specs under `tests/e2e/`, each beginning with the header-comment convention (what flow, demo-mode fallback behavior, relevant cardinal rule) and importing `{ test, expect }` from `@playwright/test`:
    (1) `tests/e2e/onboarding.spec.ts` — goto `/onboarding`; assert `response.status() < 500` and `main` first element visible (demo mode renders the static disabled <main>; do NOT assert a form).
    (2) `tests/e2e/submit-deliverable.spec.ts` — goto `/journey`; assert `main.eic-journey` visible; assert zero `[disabled]` anchor tags (R3); then goto `/journey/deliverable/00000000-0000-0000-0000-000000000001` and assert `status() < 500` (redirect to /login allowed). Reuse R1 discipline: assert the /journey body does NOT contain a `\d{1,3}\s*/\s*100` score pattern.
    (3) `tests/e2e/mentor-eval.spec.ts` — goto `/mentor` assert `< 500`; goto `/mentor/submission/00000000-0000-0000-0000-000000000001` assert `< 500` (redirect/404 allowed, never 500).
    (4) `tests/e2e/jury-pitch.spec.ts` — goto `/jury`; assert `status() < 500` (302 to /login allowed in demo mode); reuse the R1 absence-of-rank check (`/\brang\s*#?\d+/i` must not appear).
    (5) `tests/e2e/gm-export.spec.ts` — goto `/admin/export/players.csv`; assert `status() === 200`; read `response.text()` and assert it contains the CSV header token `team_slug`.
    Run `npm run test:e2e` (it will boot the demo dev server on 3001 automatically) and confirm all 5 new specs + the 3 existing smoke specs pass.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e` exits 0 with all 5 tests/e2e specs passing AND the 3 tests/smoke specs still passing (8 spec files green)
    - Each of the 5 specs opens with a header comment naming the flow + demo-mode behavior
    - gm-export.spec.ts asserts `status() === 200` and that the CSV body contains `team_slug`
    - submit-deliverable.spec.ts asserts `main.eic-journey` visible, zero `[disabled]` anchors, and absence of a `/100` score pattern on /journey (R1+R3 preserved)
    - jury-pitch.spec.ts asserts no `rang #N` pattern (R1) and a non-5xx status
    - No spec sets Supabase env vars and no spec asserts an exact French copy string
  </acceptance_criteria>
  <done>5 demo-mode E2E flow specs pass alongside the 3 existing smoke specs; assertions are structural and preserve R1/R3; CSV export verified at 200 + header.</done>
</task>

</tasks>

<verification>
- `npm run test:e2e` passes (8 spec files: 5 new + 3 smoke), 0 failures, in demo mode.
- `npm run typecheck` still passes.
- No spec requires Supabase secrets — proves CI-readiness without env.
</verification>

<success_criteria>
- ROADMAP Phase 13 success criterion 4 satisfied: Playwright E2E configured and >= 2 flows pass in demo mode (we deliver all 5: onboarding, submit, mentor, jury, GM export).
- The dual-mode demo guarantee (CLAUDE.md) is now regression-locked by E2E for all 5 surfaces.
</success_criteria>

<output>
Create `.planning/phases/13-db-consolidation-test-infrastructure/13-03-SUMMARY.md` when done. Record: 5 spec files added, the test:e2e run result (pass counts), and confirmation that the smoke specs still pass under the widened testDir.
</output>
