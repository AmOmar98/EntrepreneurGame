# Phase 11 — Plan 11-01 : Design Audit Refinements (13 items)

**Phase** : 11 — Design Audit Refinements
**Plan** : 11-01 (orchestrator with 4 waves)
**Created** : 2026-05-10
**Target execution** : Before 12/05 23h00 (cutoff merge `main`)
**Author** : Claude Code (post audit-design-v2-vs-impl)
**Source** : `.planning/ui-reviews/AUDIT-DESIGN-V2-VS-IMPL-2026-05-10.md`

---

## Goal

Combler les 13 écarts identifiés entre le bundle design Anthropic v2 (`.planning/design-v2/`) et l'implémentation actuelle, sans casser les 3 règles cardinales EIC (R1/R2/R3) ni le freeze pré-pilote AgreenTech 13-14 mai.

**Definition of done** : 13/13 items résolus, build vert, smoke E2E régression OK sur `/journey`, `/results`, `/login`, `/landing`. Aucun warning advisor R1/R2/R3.

---

## Risk gating (CRITICAL)

Cette phase est exécutée **À T-1 du pilote**. Tout commit doit :
1. Être commit atomique (1 sub-task = 1 commit) → rollback granulaire possible.
2. Passer `npm run typecheck` + `npm run lint` + `npm run build` clean avant push.
3. Pour Wave C (logic/UX) : **spawn `eic-pedagogical-advisor` AVANT édition** des zones sensibles listées dans CLAUDE.md `Pre-edit guards`.
4. Pour items touchant `app/page.tsx` ou `middleware.ts` : smoke prod sur Vercel preview avant merge `main`.
5. Si advisor verdict `BLOCK` sur un item → skip cet item, documenter dans `deferred-items.md`, ne pas forcer.

---

## Waves

### Wave A — CSS-only, zero risk (parallelizable)

| ID | Task | Files | Effort | Risk |
|---|---|---|---|---|
| A1 | Shimmer cap on `.eic-track__fill` top via `::after` radial gradient | `app/globals.css` | ~10 lines | 0 |
| A2 | Mount animation `@keyframes eic-track-charge` 0%→progress, 600ms ease-out, motion-safe | `app/globals.css` | ~15 lines | 0 |
| A3 | First-paint stagger on nodes via `--node-delay` custom prop | `app/globals.css` + `components/journey-level-node.tsx` | ~20 lines | 0 |
| A4 | Audit + add missing pills in topbar lite | `components/topbar-lite.tsx` | ~10 lines | 0 |

**Wave A acceptance** : visual diff vs design `player-screens.jsx:118-191` matches. No DOM changes that affect tab order or aria-tree. No new accessibility regressions.

**Commit msg pattern** : `feat(11-A): <slug>`

### Wave B — Animation / scroll, low risk (sequential within wave, advisor optional)

| ID | Task | Files | Effort | Risk |
|---|---|---|---|---|
| B1 | Smooth-scroll hero CTA on mission change (forwardRef) | `components/journey-client.tsx` + `journey-hero-next-step.tsx` | ~25 lines | low |
| B2 | Mobile track scroll-snap + scroll-margin-top is-current | `app/globals.css` | ~10 lines | low |
| B3 | IntersectionObserver reveal on `/results` 3 sections | `hooks/use-in-view.ts` (new) + `results-podium.tsx` + `results-stats-strip.tsx` + `results-timeline-moments.tsx` + `app/globals.css` | ~80 lines | low-med |
| B4 | GM radar dashed lines between simultaneously-active teams | `components/admin-radar.tsx` | ~30 lines | low |
| B5 | Hero CTA compact mode mobile | `components/journey-hero-next-step.tsx` + `app/globals.css` | ~20 lines | low |

**Wave B acceptance** : reduced-motion guards on every new keyframe. `useInView` hook self-contained (no dep). Smoke check on `/results` (gated by `isGameMaster`) and `/journey` mobile.

**Commit msg pattern** : `feat(11-B): <slug>`

### Wave C — Logic / UX, advisor REQUIRED before edit (sequential)

| ID | Task | Files | Effort | Risk |
|---|---|---|---|---|
| C1 | Public landing page (3 role doors + AgreenTech kicker) | `app/page.tsx` (modify), `app/landing/page.tsx` (new), `lib/i18n.ts`, `app/globals.css` | ~150 lines | MEDIUM |
| C2 | Audit + fix dual-mode demo guard on `/journey` | `app/journey/page.tsx` | ~10 lines | MEDIUM |
| C3 | Locked-level click softening (R3 fix) | `components/journey-track.tsx` + `components/journey-drawer.tsx` | ~40 lines | MEDIUM |

**Wave C acceptance** : 
- Spawn `eic-pedagogical-advisor` BEFORE each item with task description and target files.
- Advisor verdict `OK` → proceed. Verdict `WARN` → adjust per advisor copy. Verdict `BLOCK` → defer to v0.3, document in `deferred-items.md`.
- Post-edit grep R1 audit: `grep -rn "score\|rank\|note\|/100\|/140\|points\|toFixed" app/journey app/results components/results-* components/submission-* --include="*.tsx"` — zero unexpected matches Player-facing.

**Commit msg pattern** : `feat(11-C): <slug>`

### Wave D — Cardinal rules audit (D1 only)

| ID | Task | Files | Effort | Risk |
|---|---|---|---|---|
| D1 | Add R1 protective comment on `journey-client.tsx:122` totalXp display | `components/journey-client.tsx` | ~3 lines | 0 |

**Commit msg pattern** : `docs(11-D): <slug>`

---

## Execution order

1. **Wave A** parallelisable in 1 commit if grouped (or 4 atomic commits)
2. **Wave B** in sequence (B1 → B2 → B3 → B4 → B5), 5 commits
3. **Wave C** in sequence with advisor spawn before each (C1 → C2 → C3), 3 commits
4. **Wave D** at end, 1 commit (R1 comment)
5. **Smoke E2E** régression on swarm harness (cf. `.planning/quick/260510-t3*` pattern) before merge

Total commits : 13 atomic OR 4 grouped (one per wave). Recommend **atomic 13** for granular rollback at T-1.

---

## Sub-plans (per wave)

This orchestrator plan is intentionally not exploded into 13 separate sub-PLAN.md files (would be over-engineered for ~30min of work per item). Each Wave acceptance criterion above is the contract.

If a Wave C item needs deeper plan after advisor verdict, create `11-C1-PLAN.md` etc. on-the-fly.

---

## Files touched (full list)

### New
- `app/landing/page.tsx` (C1)
- `hooks/use-in-view.ts` (B3)

### Modified
- `app/globals.css` (A1, A2, A3, B2, B3, B5, C1)
- `app/page.tsx` (C1)
- `app/journey/page.tsx` (C2)
- `components/journey-client.tsx` (B1, D1)
- `components/journey-hero-next-step.tsx` (B1, B5)
- `components/journey-level-node.tsx` (A3)
- `components/journey-track.tsx` (C3)
- `components/journey-drawer.tsx` (C3)
- `components/topbar-lite.tsx` (A4)
- `components/results-podium.tsx` (B3)
- `components/results-stats-strip.tsx` (B3)
- `components/results-timeline-moments.tsx` (B3)
- `components/admin-radar.tsx` (B4)
- `lib/i18n.ts` (C1)

---

## Smoke test plan (before merge)

1. `npm run typecheck && npm run lint && npm run build` — all clean.
2. Demo mode (no Supabase env) : navigate `/landing` → `/login` → `/journey` (seed) → `/results`. No `redirect("/login")` loop.
3. Prod-like (Vercel preview) : login `omar.ameur98@gmail.com`, navigate full Player journey, submit 1 deliverable, verify ticket render.
4. Mobile viewport 390px : track scroll-snap snaps to current node, hero CTA compact, reveal animations on `/results` if reduced-motion off.
5. Cardinal grep audit (R1 leak check) post all C-wave commits.

---

## Rollback procedure

Each commit atomic on `main`. To rollback Phase 11 entirely:
```bash
git log --oneline | grep "feat(11-" | head -20  # identify commits
git revert <oldest>..<newest>  # batch revert preserving history
```

Or surgical : `git revert <single-sha>` for one item.

---

## Reviewer protocol

Three subagent reviewers will independently audit this plan **BEFORE execution**:
1. `eic-pedagogical-advisor` — R1/R2/R3 cardinal rules per item.
2. `gsd-ui-checker` — 6 quality dimensions on UI deltas.
3. `codex:rescue` — independent non-Claude perspective.

If any reviewer returns `BLOCK` on an item, that item is deferred to v0.3 and documented in `deferred-items.md`. If all reviewers return `OK`/`WARN with notes`, execution proceeds with notes incorporated.

---

## Reviewer verdicts (2026-05-10, post-PLAN authoring)

3 reviewers ran in parallel. Consolidated synthesis : `REVIEW-SYNTHESIS.md`. Individual verdicts : `ADVISOR-VERDICT.md`, `UI-CHECK-VERDICT.md`, `CODEX-VERDICT.md`.

**Outcome** : 0 outright BLOCK. 5 FLAGs incorporated into acceptance criteria below. C1 has downscope option awaiting operator green-light.

### Acceptance criteria amendments (consensus)

- **Preflight** : tag `v0.2.1-pre-phase11` BEFORE first Phase 11 commit. Baseline `typecheck/lint/build` clean.
- **A2** : `animation-fill-mode: forwards`, one-shot (no loop).
- **A3** : `animation-delay` (not `transition-delay` on opacity — breaks AT focus).
- **A4** : audit `topbar-lite.tsx` ; refuse port of any `TOTAL_XP / 2000` denominator (R1 violation).
- **B1** : wrap `scrollIntoView` with `matchMedia('(prefers-reduced-motion: reduce)')` ; effect depends on `next.template.id` change, NOT on focus.
- **B3** : IO threshold `0.15` (not 0.5) ; observer SKIPPED + immediate `is-revealed` when reduced-motion. Post-edit grep R1 audit MANDATORY on `components/results-*`. **B3 conditional execution** : defer to v0.3 if Waves A + C smoke not green by 12/05 21h00 (cutoff-2h gate).
- **C1** : file scope EXPANDED to `utils/supabase/middleware.ts` (otherwise `/landing` unreachable in Supabase mode). Re-spawn advisor for copy review BEFORE commit. **Downscope option** : if advisor WARN/BLOCK on copy, ship text-only minimal (2 paragraphs + "Connexion" CTA) without 3 doors / mascot / hills.
- **C3** : (a) no XP mutation, no `currentLevel` increment, no server action on locked-click. (b) `aria-disabled` only, never `disabled` DOM attr. (c) tooltip copy VERBATIM from `T3-IMPROVEMENTS.md § R3`. (d) replace `cursor:not-allowed` with `cursor:pointer` + light desaturation, no opacity drop.
- **D1** : verbatim comment :
  ```
  // R1 (CARDINAL) — DO NOT add a "/{totalXpMax}" denominator here.
  // Showing a max creates a comparison/ranking frame forbidden Player-side.
  // XP gauge is progression-only. Ref: T3-IMPROVEMENTS.md R1.
  ```
- **Rollback** : force atomic 13 commits (NOT grouped per wave) — T-1 needs granular revert.

### Revised execution order

0. Preflight : tag `v0.2.1-pre-phase11` + baseline build.
1. **Wave D** (D1) — 1 commit, fastest, zero risk.
2. **Wave A** (A1→A2→A3→A4) — 4 atomic commits.
3. **C2** dual-mode fix — 1 commit. Validate demo mode + prod login.
4. **C3** locked click — re-spawn advisor for tooltip copy ; 1 commit.
5. **C1** public landing — re-spawn advisor for copy review ; ship-3-doors OR ship-text-minimal ; 1 commit (+ middleware in same commit).
6. **Wave B** (B1→B2→B5→B4→B3) — up to 5 commits ; B3 conditional on cutoff-2h gate.
7. **Smoke E2E** régression : demo mode + prod-like + mobile 390px + grep R1 audit.

Total commits expected : 12-13 (B3 may be skipped).

## Status

📥 **Created** 2026-05-10. ✅ **Reviewed** 2026-05-10 by 3 parallel reviewers — 0 BLOCK, 5 FLAGs incorporated.

⚠️ **Operator green-light required on 3 decisions before execution** :
1. C1 downscope : ship 3-doors-with-mascot OR ship text-minimal landing.
2. B3 conditional defer : accept cutoff-2h gate OR defer outright to v0.3.
3. v0.2.1-pre-phase11 tag : confirm to create tag locally (no remote push without Omar OK).
