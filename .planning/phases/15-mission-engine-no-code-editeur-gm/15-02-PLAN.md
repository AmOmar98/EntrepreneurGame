---
phase: 15-mission-engine-no-code-editeur-gm
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - lib/admin-events.ts
  - app/admin/events/page.tsx
  - components/admin-events-table.tsx
  - app/actions.ts
  - lib/i18n.ts
autonomous: true
requirements: [ENGINE-04, ENGINE-03]
must_haves:
  truths:
    - "GM on /admin/events sees the org's events with name/slug/dates/active-status and can create a new event + cohort with zero SQL"
    - "GM can activate one event; activating it deactivates all other events in the same org (single-active invariant)"
    - "GM can clone an event in one click; missions + deliverable_templates + settings are deep-copied with new UUIDs and a suffixed slug; editing the clone does not touch the original"
    - "soft_recommends_before self-FK is re-mapped old→new in the clone (no clone points at the source's templates)"
    - "In demo mode the editor renders read-only with an amber notice; the 15 E2E demo flows stay green"
  artifacts:
    - path: "app/admin/events/page.tsx"
      provides: "GM-only event list/create page (AppShell staff variant, demo guard)"
      contains: "AppShell"
    - path: "components/admin-events-table.tsx"
      provides: "Client event table: rows + activate toggle + clone button + inline create form"
      contains: "useActionState"
    - path: "lib/admin-events.ts"
      provides: "getAdminEvents() accessor (events + active flag + org)"
      exports: ["getAdminEvents"]
    - path: "app/actions.ts"
      provides: "createEventFlow, activateEventFlow, cloneEventFlow"
      contains: "cloneEventFlow"
  key_links:
    - from: "components/admin-events-table.tsx"
      to: "app/actions.ts:cloneEventFlow"
      via: "useActionState form action"
      pattern: "cloneEventFlow"
    - from: "app/actions.ts:activateEventFlow"
      to: "events.is_active single-active invariant"
      via: "deactivate-all-then-activate-one per org"
      pattern: "is_active"
---

<objective>
Build the events surface of the no-code editor: a GM-only `/admin/events` page that lists the organization's events and lets the GM create an event + cohort (ENGINE-04), activate one event (single-active invariant, consuming the phase-14 `is_active` flag), and clone an entire event — missions + deliverable_templates + settings — in one click with re-mapped self-referential FKs (ENGINE-03).

Purpose: This is the entry point of the whole editor. Clone is the proof of "personnalisation réelle" the July event depends on. The single-active invariant is the TENANT-03 editor side.
Output: `lib/admin-events.ts` accessor, `/admin/events` page, `components/admin-events-table.tsx`, three Flow actions, i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-CONTEXT.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-UI-SPEC.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-PATTERNS.md

<interfaces>
<!-- Contracts from prior plan + codebase. Use directly. -->

From lib/active-event.ts (built phase 14, consumed here):
```typescript
export async function getActiveEvent(): Promise<Event | null>;
export async function getActiveEventId(): Promise<string | null>;
```

From lib/types.ts (Event shape, composer columns added in 15-01):
```typescript
export type Event = { id; slug; name; startsAt; endsAt; resultsPublishedAt; isActive?; organizationId? };
export type ComposerKind = "simple" | "moscow" | "multi_url";
// DeliverableTemplate now has composerKind, templateUrl, autoValidate, softRecommendsBefore, validationRules
```

From app/actions.ts (action skeleton to copy — toggleDeliverableActiveFlow, lines ~1383-1447):
  - demo guard: `if (!hasSupabaseEnv()) return { ok:false, message:"Backend non configure." }`
  - `const supabase = await createClient(); if (!supabase) return {...}`
  - GM role gate (lines 1412-1431): getUser → profiles.app_role === "game_master" else reject
  - revalidatePath on all affected routes
  - return WorkflowState { ok, message }

Clone structural reference: database/seed_event_digi_hackathon.sql lines 56-88 (event→missions→templates insert order). No TS clone analog exists — first clone action.

Page/table analogs (exact match): app/admin/deliverables/page.tsx, components/admin-deliverables-table.tsx.
Data accessor analog: lib/admin-deliverables.ts (getAdminDeliverables join pattern).
i18n keys to add (from UI-SPEC Copywriting Contract): admin_engine_create_event, admin_engine_clone_event, admin_engine_clone_success, admin_engine_clone_error, admin_engine_events_empty, admin_engine_deactivate_confirm, admin_engine_demo_disabled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getAdminEvents accessor + i18n keys</name>
  <read_first>
    - lib/admin-deliverables.ts (accessor join pattern, dual-mode null/empty handling)
    - lib/active-event.ts (Event row mapping, is_active read)
    - lib/i18n.ts (dictionaries fr block — add keys here, ASCII only, no inline French)
    - 15-UI-SPEC.md Copywriting Contract (exact French copy per key)
  </read_first>
  <files>lib/admin-events.ts, lib/i18n.ts</files>
  <action>
    Create lib/admin-events.ts exporting `getAdminEvents(): Promise<AdminEventRow[]>` where AdminEventRow = { id, slug, name, startsAt, endsAt, isActive, organizationId, missionCount }. Read `events` (id, slug, name, starts_at, ends_at, is_active, organization_id) ordered by starts_at desc; for missionCount, do a grouped count or a per-event count of `missions`. Follow lib/admin-deliverables.ts: `const supabase = await createClient(); if (!supabase) return [];` and tolerate the pre-migration window (is_active null → false). Never throw.
    In lib/i18n.ts add the keys from the UI-SPEC Copywriting Contract that this plan uses: admin_engine_create_event, admin_engine_clone_event, admin_engine_clone_success, admin_engine_clone_error, admin_engine_events_empty, admin_engine_deactivate_confirm, admin_engine_demo_disabled, plus admin_engine_save_error and admin_engine_simulate_date_active (shared by later plans — add now since lib/i18n.ts is owned by this wave). Use the exact French strings from the spec but ASCII-safe (strip diacritics per the i18n convention, e.g. "Creer un event"). Add to the `fr` dictionary; mirror under `en` with the same value (FR is primary).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - getAdminEvents returns [] in demo mode (no throw).
    - All listed i18n keys exist in the fr dictionary; typecheck passes (no missing-key errors at call sites added later).
    - ASCII-only strings (no accented characters in code-resident copy).
  </acceptance_criteria>
  <done>Accessor compiles dual-mode; i18n keys for the events + clone + demo surfaces present.</done>
</task>

<task type="auto">
  <name>Task 2: createEventFlow + activateEventFlow + cloneEventFlow server actions</name>
  <read_first>
    - app/actions.ts lines ~1383-1447 (toggleDeliverableActiveFlow — demo guard, client, Zod, GM gate, revalidatePath skeleton)
    - app/actions.ts lines ~1906-1913 (batch UPDATE loop pattern for clone inserts)
    - database/seed_event_digi_hackathon.sql lines 56-88 (event→mission→template insert order + fields)
    - 15-PATTERNS.md "Clone event server action" + "lib/active-event.ts" sections (single-active invariant, old→new UUID remap for soft_recommends_before)
  </read_first>
  <files>app/actions.ts</files>
  <action>
    Add three actions following the toggleDeliverableActiveFlow skeleton (demo guard → createClient → Zod safeParse → GM role gate → mutation → revalidatePath → WorkflowState). Co-locate each Zod schema before its action.
    1. createEventFlow: schema { name: z.string().min(1), slug: z.string().min(1).regex(slug-safe), organizationId: z.string().uuid().nullable().optional(), startsAt: z.string(), endsAt: z.string(), cohortName: z.string().min(1) }. Insert into `events` (is_active defaults false — do NOT auto-activate), then insert one `cohorts` row (event_id = new event id, slug, name = cohortName). revalidatePath("/admin/events"). Return { ok:true, message } or error.
    2. activateEventFlow: schema { eventId: z.string().uuid() }. Resolve the event's organization_id; UPDATE events SET is_active=false WHERE organization_id = <org> (deactivate all in org); then UPDATE events SET is_active=true WHERE id = eventId. (If organization_id is null in the pre-migration window, scope the deactivate to all events as a safe fallback.) revalidatePath("/admin/events"), revalidatePath("/admin"), revalidatePath("/journey"). Single-active invariant (TENANT-03).
    3. cloneEventFlow: schema { eventId: z.string().uuid() }. Fetch source event + its missions + their deliverable_templates. Insert a cloned event (name + " (copie)", slug = source.slug + "-clone-" + Date.now(), is_active=false, same org). Build an old→new mission UUID map: for each source mission insert a clone with new id, event_id = new event, preserve level_id/ord/kind/title/scheduled_at. Build an old→new template UUID map: for each source template insert a clone with new id, mission_id = mapped mission, preserving rubric/max_score/ord/is_bonus/composer_kind/template_url/auto_validate/validation_rules. CRITICAL: soft_recommends_before is a self-FK — first insert all clones with soft_recommends_before=null, then a second pass UPDATE remaps it via the old→new template map (so clones reference clones, never the originals). Use the batch UPDATE loop pattern; on any error return { ok:false, message: admin_engine_clone_error } (no partial-success silent). revalidatePath("/admin/events"). Return { ok:true, message: admin_engine_clone_success }.
    Never throw; all failures via WorkflowState. Reuse the GM role gate verbatim (defense-in-depth alongside RLS *_gm_all).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Three exported Flow actions returning WorkflowState; each has demo guard + GM role gate.
    - cloneEventFlow does the two-pass soft_recommends_before remap (grep shows the second UPDATE pass keyed on the old→new map).
    - activateEventFlow deactivates org siblings before activating target.
    - No `throw` added; typecheck + lint green.
  </acceptance_criteria>
  <done>Create/activate/clone actions implemented with single-active invariant and self-FK remap; gate green.</done>
</task>

<task type="auto">
  <name>Task 3: /admin/events page + admin-events-table client component</name>
  <read_first>
    - app/admin/deliverables/page.tsx (server page: auth/role gate, AppShell staff, demo-mode guard)
    - components/admin-deliverables-table.tsx (client: useActionState, toggle form, hidden inputs, aria-labels, state message display)
    - app/admin/page.tsx lines 141-175 + 227-240 (topbar + demo amber pill)
    - 15-UI-SPEC.md "/admin/events" surface + Interaction Contracts (clone flow, activate toggle, form pattern, demo guard)
  </read_first>
  <files>app/admin/events/page.tsx, components/admin-events-table.tsx</files>
  <action>
    Page (server component, app/admin/events/page.tsx): copy app/admin/deliverables/page.tsx structure — getCurrentUser → redirect("/login") if absent; getCurrentRole → redirect(pathForRole(role)) if role && role !== "game_master"; `const rows = hasSupabaseEnv() ? await getAdminEvents() : []`. Render `<AppShell role="game_master" variant="staff">` with the topbar pattern. When `!hasSupabaseEnv()` render the amber demo pill (admin_engine_demo_disabled) and the table read-only. Pass rows to `<AdminEventsTable rows={rows} demo={!hasSupabaseEnv()} />`.
    Client component (components/admin-events-table.tsx): "use client". Per the UI-SPEC /admin/events surface — render a .wf-card table: columns Nom, Slug, Dates, Statut (is_active → .eic-pill--green "Actif" / .eic-pill--rose "Inactif"), Organisation, Actions. Actions per row: "Voir missions" link → `/admin/events/${row.id}/missions`; "Cloner" button wired to cloneEventFlow via useActionState (pending → "Clonage en cours…", on success show message + "Voir le clone →"); activate toggle (.eic-toggle-switch pattern from admin-deliverables-table) wired to activateEventFlow with hidden input eventId. Inline create form (.admin-form-grid 2-col) wired to createEventFlow: nom, slug, org (.select), date_debut, date_fin, cohortName, description. Submit `.eic-button--primary` admin_engine_create_event with disabled={pending}. Empty state: admin_engine_events_empty. Every icon button carries an explicit aria-label. Reuse only existing EIC primitive classes (UI-SPEC primitives list) — no new CSS. In demo mode the submit/toggle/clone return their server demo message; do not add `disabled` that blocks Player anything (this is GM-only, but keep the demo pill informative).
    Copy strings come from dictionaries (lib/i18n.ts) — no inline French.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/events` builds and renders under AppShell staff variant; non-GM roles are redirected.
    - Demo mode shows the amber pill (admin_engine_demo_disabled); the 15 E2E demo flows stay green (new route, no existing-route change).
    - Activate toggle + Clone button + inline create form each wired to their Flow action via useActionState.
    - Icon buttons have aria-labels; only existing EIC primitive classes used.
  </acceptance_criteria>
  <done>GM can list/create/activate/clone events from /admin/events with zero SQL; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | GM form input crosses into DB writes |
| client role claim → server | App-shell role is advisory; server must re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Elevation of Privilege | createEvent/activate/clone actions | mitigate | GM role gate (profiles.app_role check) + RLS *_gm_all defense-in-depth |
| T-15-05 | Tampering | clone soft_recommends_before self-FK | mitigate | two-pass insert + old→new UUID remap; clone never references source rows |
| T-15-06 | Information Disclosure | cross-org event listing | mitigate | activate scopes deactivation to organization_id; RLS org-scope (phase 14) filters reads |
| T-15-07 | Repudiation | partial clone leaves orphan rows | accept | clone returns error on any failure; additive inserts, GM can delete-by-deactivate; full transactionality deferred (Supabase JS no multi-stmt tx) |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build` green.
- `npm run test:e2e` — 15 demo specs green (new routes only).
- Post-edit R1 grep audit (app/actions.ts + components touched are GM-only, no Player surface — confirm no score/rank leak introduced).
</verification>

<success_criteria>
- ENGINE-04: GM creates event + cohort from /admin/events with zero SQL.
- ENGINE-03: GM clones an event (missions + templates + settings) in one click; clone is independent (self-FK remapped); editing the clone does not affect the original.
- Single-active invariant holds (activating one deactivates org siblings).
</success_criteria>

<output>
Create `.planning/phases/15-mission-engine-no-code-editeur-gm/15-02-SUMMARY.md` when done.
</output>
