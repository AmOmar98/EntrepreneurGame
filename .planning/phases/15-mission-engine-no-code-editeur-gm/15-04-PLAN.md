---
phase: 15-mission-engine-no-code-editeur-gm
plan: 04
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - lib/get-simulated-now.ts
  - lib/admin-levels.ts
  - app/admin/levels/page.tsx
  - components/admin-levels-editor.tsx
  - app/journey/deliverable/[id]/page.tsx
  - app/actions.ts
  - lib/admin.ts
  - lib/i18n.ts
autonomous: true
requirements: [LEVELS-04, ENGINE-05, ENGINE-06, ENGINE-07]
must_haves:
  truths:
    - "GM can rename/reorder/add/remove a program's levels in /admin/levels; delete is blocked when missions reference the level"
    - "The Player deliverable page reads composer_kind/template_url from the DB column instead of MOSCOW_DELIVERABLE_SLUG and the fiches-entretien slug literal (ENGINE-05)"
    - "A configured soft_recommends_before renders a non-blocking amber hint on the Player deliverable page — zero disabled DOM, zero pointer-events:none (ENGINE-06 / R3)"
    - "scheduled_date logic can be simulated to an arbitrary date via getSimulatedNow() (GM-only ?simulate_date=) for smoke (ENGINE-07)"
    - "Pre-migration tolerance: missing columns fall back to current slug-based behavior; the 15 E2E demo flows stay green"
  artifacts:
    - path: "app/admin/levels/page.tsx"
      provides: "GM-only program levels CRUD page (LEVELS-04)"
      contains: "AppShell"
    - path: "lib/get-simulated-now.ts"
      provides: "getSimulatedNow() date-injection helper (ENGINE-07)"
      exports: ["getSimulatedNow"]
    - path: "app/journey/deliverable/[id]/page.tsx"
      provides: "composer_kind/template_url data-driven reads + soft_recommends_before amber hint"
      contains: "eic-locked-hint--amber"
    - path: "app/actions.ts"
      provides: "createLevelFlow, updateLevelFlow, reorderLevelFlow, deleteLevelFlow"
      contains: "deleteLevelFlow"
  key_links:
    - from: "app/journey/deliverable/[id]/page.tsx"
      to: "deliverable_templates.composer_kind / template_url columns"
      via: "data-driven read replacing slug literals (ENGINE-05)"
      pattern: "composer_kind|template_url"
    - from: "app/journey/deliverable/[id]/page.tsx soft_recommends_before"
      to: ".eic-locked-hint--amber render (no disabled DOM)"
      via: "advisory amber hint (ENGINE-06/R3)"
      pattern: "eic-locked-hint--amber"
---

<objective>
Close the editor (levels CRUD — LEVELS-04) and complete the data-driven cutover on the consumer side: the Player deliverable page reads `composer_kind` / `template_url` from columns instead of `MOSCOW_DELIVERABLE_SLUG` / `fiches-entretien-v1` / `getTemplateLink()` literals (ENGINE-05), renders a non-blocking amber hint for `soft_recommends_before` (ENGINE-06 / R3), and `scheduled_date` comparisons go through `getSimulatedNow()` so the BMC-J1 incident can be replayed at any date (ENGINE-07).

Purpose: De-hardcoding is the heart of the engine — after this, behaviour is data, not code. R3 compliance (no configurable hard-block, soft hint only) and the simulate-date smoke capability are cardinal.
Output: levels editor (page + component + 4 actions), `lib/get-simulated-now.ts`, consumer rewrites in the deliverable page + lib/admin.ts, i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-CONTEXT.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-UI-SPEC.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-PATTERNS.md

<interfaces>
<!-- Contracts from prior plans + codebase. -->

From lib/levels.ts: getLevels(): Promise<Level[]>, getLevelsMap(). Level = { id: LevelId; ord; label; description }.
From lib/types.ts (after 15-01): DeliverableTemplate has composerKind, templateUrl, autoValidate, softRecommendsBefore, validationRules.

De-hardcoding sites (exhaustive — 15-PATTERNS "Hardcoded behavior sites"):
  Site 3 — app/journey/deliverable/[id]/page.tsx line 60: `MOSCOW_DELIVERABLE_SLUG = "fiche-produit-plan-dev-v1"`, used ~line 175 `tpl.slug === MOSCOW_DELIVERABLE_SLUG`. Replace with `tpl.composer_kind === "moscow"` (read column). NOTE: PATTERNS migration backfill uses slug 'moscow-v1' but this page references 'fiche-produit-plan-dev-v1' — the executor MUST read the actual PROD/seed data to confirm which slug carries the MoSCoW composer and ensure the backfill (15-01) tagged the right row composer_kind='moscow'; reconcile by composer_kind, not slug.
  Site 4 — same page ~line 183: `tpl.slug === "fiches-entretien-v1"`. Replace with `tpl.composer_kind === "multi_url" && tpl.auto_validate === true`.
  Site 5 — same page ~lines 401-428: `getTemplateLink(tpl.slug)` → use `tpl.template_url` column; remove import of getTemplateLink from @/lib/template-links.
  Site 6 — app/journey/page.tsx line 19 WELCOME_GUIDE_URL: OUT OF SCOPE this plan (event-level, not per-template). Leave the import intact.
  Site 1/2 — HARD_BLOCK_DEPENDENCIES + fiches-entretien auto-validate flow in app/actions.ts: the L2 literal STAYS (R3 exception, VALID-02). The auto-validate flow's slug check becomes `auto_validate === true` (already generalized in the trigger by 15-01; the TS-side multi_url submission flow keys on composer_kind/auto_validate). De-hardcode the TS slug check to read the column; KEEP the HARD_BLOCK_DEPENDENCIES literal untouched.

Pre-migration tolerance (15-PATTERNS pitfall): use defensive reads `tpl?.composer_kind ?? "simple"` so a missing column (pre-migration PROD) falls back to current behavior.

soft_recommends_before hint analog: components/journey-drawer.tsx lines 146-153 (.eic-locked-hint--amber, role="note"). CSS in globals.css lines 2059-2068 (already defined). NEVER add disabled / pointer-events:none.
ENGINE-07: lib/admin.ts line 222 `const now = Date.now()` → `getSimulatedNow()`. Helper reads ?simulate_date=YYYY-MM-DD GM-only, falls back to Date.now().
Levels CRUD analog: lib/levels.ts getLevels + toggleDeliverableActiveFlow skeleton + reorder loop (~1906-1913).
i18n keys to add: admin_engine_create_level, admin_engine_levels_empty, admin_engine_level_delete_confirm, admin_engine_soft_recommends_hint. (admin_engine_simulate_date_active added in 15-02.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getSimulatedNow helper + levels CRUD actions + levels editor page/component</name>
  <read_first>
    - lib/admin.ts lines 221-228 (Date.now() scheduled_at comparison — the ENGINE-07 call site)
    - lib/levels.ts (getLevels accessor, levels_v2 columns, dual-mode)
    - app/actions.ts toggleDeliverableActiveFlow skeleton + ~1906-1913 reorder loop
    - app/admin/deliverables/page.tsx + components/admin-deliverables-table.tsx (page guard + client editor pattern)
    - 15-UI-SPEC.md "/admin/levels" surface + Interaction Contracts (order up/down, delete inline confirm)
  </read_first>
  <files>lib/get-simulated-now.ts, lib/admin-levels.ts, app/admin/levels/page.tsx, components/admin-levels-editor.tsx, app/actions.ts, lib/i18n.ts</files>
  <action>
    lib/get-simulated-now.ts: export `async function getSimulatedNow(): Promise<number>`. Read the `simulate_date` value GM-only (server: from the request via headers()/searchParams passed in, or a cookie set by the admin page). Parse YYYY-MM-DD → epoch ms; on missing/invalid return `Date.now()`. Keep it dependency-light (analog: lib/hack-status.ts date math). Only meaningful on /admin/** — no Player behavior change.
    lib/admin-levels.ts: export `getAdminLevels(): Promise<Level[]>` (thin wrapper over getLevels for the editor page; dual-mode []).
    app/actions.ts — add four level actions (toggle skeleton each):
      - createLevelFlow: schema { id: z.string().min(1), label: z.string().min(1), description: z.string().optional(), ord: z.coerce.number().int().min(0) }. INSERT into levels_v2. revalidatePath("/admin/levels") + "/journey".
      - updateLevelFlow: schema adds existing id key; UPDATE label/description by id.
      - reorderLevelFlow: { items: [{id, ord}] } JSON; batch UPDATE loop.
      - deleteLevelFlow: { id }. GUARD: count missions WHERE level_id = id; if >0 return { ok:false, message: "Ce niveau est utilise par N missions." } (use admin_engine_level_delete_confirm copy family). Else DELETE. revalidatePath.
    app/admin/levels/page.tsx (server): admin page guard + AppShell staff; `const levels = hasSupabaseEnv() ? await getAdminLevels() : []`; demo amber pill; render <AdminLevelsEditor levels={levels} />. When ?simulate_date present, render the amber topbar pill admin_engine_simulate_date_active.
    components/admin-levels-editor.tsx ("use client"): per UI-SPEC — `.stack` of level rows, inline `.input` edit for label, ord with up/down `.button.icon` (ChevronUp/ChevronDown, aria-labels) → reorderLevelFlow; "Ajouter un niveau" (.button.primary admin_engine_create_level) → createLevelFlow; delete `.button.icon` Trash2 with INLINE confirmation text (admin_engine_level_delete_confirm "Oui, supprimer"/"Annuler" in `.toolbar`) → deleteLevelFlow. Empty state admin_engine_levels_empty. useActionState; only EIC primitives; copy via dictionaries.
    Add the new i18n keys (admin_engine_create_level, admin_engine_levels_empty, admin_engine_level_delete_confirm, admin_engine_soft_recommends_hint) to fr + en.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - getSimulatedNow returns Date.now() when no simulate_date; parses YYYY-MM-DD otherwise.
    - createLevelFlow/updateLevelFlow/reorderLevelFlow/deleteLevelFlow exported with GM gate + demo guard; deleteLevelFlow blocks when missions reference the level.
    - /admin/levels builds; non-GM redirected; demo amber pill shown; reorder/delete buttons carry aria-labels.
    - New i18n keys present; build green.
  </acceptance_criteria>
  <done>Levels editor live (LEVELS-04) + getSimulatedNow helper ready (ENGINE-07 plumbing).</done>
</task>

<task type="auto">
  <name>Task 2: De-hardcode the Player deliverable page (composer_kind/template_url) + soft_recommends_before amber hint</name>
  <read_first>
    - app/journey/deliverable/[id]/page.tsx (lines 50-60 type + MOSCOW_DELIVERABLE_SLUG; ~175 isMoscow; ~183 isFichesEntretien; ~401-428 getTemplateLink usage)
    - components/journey-drawer.tsx lines 146-153 (.eic-locked-hint--amber render pattern, role="note")
    - app/globals.css lines 2059-2068 (eic-locked-hint--amber definition — already exists, do not re-add)
    - 15-PATTERNS.md "soft_recommends_before amber hint" + "Critical Pitfalls" (pre-migration defensive reads)
    - CLAUDE.md Pre-edit guards (this is a hot zone — R1/R3 grep audit mandatory post-edit)
  </read_first>
  <files>app/journey/deliverable/[id]/page.tsx</files>
  <action>
    1. Extend the template SELECT in this page to also fetch composer_kind, template_url, auto_validate, soft_recommends_before from deliverable_templates. Use defensive reads (`tpl?.composer_kind ?? "simple"`, `tpl?.template_url ?? null`) so the pre-migration window (columns absent) falls back to current behavior.
    2. Replace `const isMoscowDeliverable = tpl.slug === MOSCOW_DELIVERABLE_SLUG` with `tpl.composer_kind === "moscow"`. Remove the MOSCOW_DELIVERABLE_SLUG constant. (Reconcile against actual data: confirm the MoSCoW row's composer_kind was backfilled to 'moscow' in 15-01 regardless of which slug it carries.)
    3. Replace `tpl.slug === "fiches-entretien-v1"` with `tpl.composer_kind === "multi_url" && tpl.auto_validate === true`. Remove the slug literal.
    4. Replace `getTemplateLink(tpl.slug)` rendering with `tpl.template_url` (when null, omit the template link block or keep the demo fallback). Remove the `getTemplateLink` import from @/lib/template-links. Keep WELCOME_GUIDE_URL untouched (out of scope here).
    5. Add the soft_recommends_before amber hint per the PATTERNS analog: when `tpl.soft_recommends_before` is non-null AND the referenced template is not yet validated for this player, render a `<p className="eic-locked-hint--amber" role="note">` with lucide Info icon (size 14, aria-hidden) + `t.admin_engine_soft_recommends_hint.replace("[titre]", prerequisiteTitle)`. Fetch the prerequisite template's title for the copy. ABSOLUTELY NO `disabled`, `pointer-events:none`, grayed CTA, or any click-blocking — advisory only (ENGINE-06 / R3 CARDINAL).
    Run the CLAUDE.md R1 grep audit after editing (score/rank must not leak; this page is the legitimate score-detail hot zone but the soft hint adds no score/rank).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `MOSCOW_DELIVERABLE_SLUG`, `"fiches-entretien-v1"`, and `getTemplateLink` in this file returns 0 matches.
    - composer_kind/template_url/auto_validate read from columns with defensive `?? "simple"` fallbacks.
    - soft_recommends_before renders `.eic-locked-hint--amber` only; grep confirms no `disabled`/`pointer-events` tied to soft_recommends_before.
    - R1 grep audit (CLAUDE.md post-edit) returns no Player score/rank leak from the hint.
  </acceptance_criteria>
  <done>Deliverable behaviour is data-driven (composer_kind/template_url); soft_recommends_before is a non-blocking amber hint.</done>
</task>

<task type="auto">
  <name>Task 3: De-hardcode the actions-side fiches-entretien slug check + wire getSimulatedNow into scheduled_date</name>
  <read_first>
    - app/actions.ts lines ~195-197 (HARD_BLOCK_DEPENDENCIES — KEEP) + ~309-372 (fiches-entretien auto-validate flow keyed on slug)
    - lib/admin.ts lines 221-228 (Date.now() scheduled_at comparison)
    - lib/get-simulated-now.ts (helper from Task 1)
    - 15-PATTERNS.md sites 1/2 + "auto_validate semantics" pitfall
  </read_first>
  <files>app/actions.ts, lib/admin.ts</files>
  <action>
    1. In app/actions.ts, locate the fiches-entretien auto-validate flow (`if (templateSlug === "fiches-entretien-v1")`). Replace the slug check with a column read: fetch the template's composer_kind + auto_validate and gate on `tplRow.composer_kind === "multi_url" && tplRow.auto_validate === true` (defensive: if columns absent pre-migration, fall back to the slug check so PROD keeps working until the migration applies). The multi-URL JSON parse (fichesEntretienSchema) is retained for that branch.
    2. KEEP HARD_BLOCK_DEPENDENCIES literal exactly as-is (R3 exception / VALID-02). Do not generalize it, do not read it from a column.
    3. In lib/admin.ts replace `const now = Date.now()` (line ~222) with `const now = await getSimulatedNow()` and make the enclosing function async-await compatible (it already awaits Supabase). This routes the "elapsed missions" scheduled_at comparison through the simulate-date override (ENGINE-07). Do NOT touch lib/pitch-order.ts Date.now (RNG seed — unrelated).
    Run R3 grep audit (CLAUDE.md): no new hard-block, no `blocks_progression_to`, no `disabled` DOM derived from configurable deps.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The fiches-entretien auto-validate branch keys on composer_kind/auto_validate columns (with pre-migration slug fallback); grep shows no bare `=== "fiches-entretien-v1"` gate without the column read.
    - HARD_BLOCK_DEPENDENCIES literal unchanged (grep still finds it; it is the single R3 exception).
    - lib/admin.ts elapsed-mission comparison uses getSimulatedNow(); lib/pitch-order.ts untouched.
    - typecheck + lint + build green.
  </acceptance_criteria>
  <done>Server-side behaviour data-driven (auto_validate column); ENGINE-07 wired; L2 exception preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player deliverable page → template columns | Player reads behaviour config; must not gain score/rank or hard-block |
| ?simulate_date query → date logic | GM-only override; must not affect Player surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Elevation | soft_recommends_before becomes a hard-block (R3) | mitigate | render `.eic-locked-hint--amber` only; static grep gate for `disabled`/`pointer-events` near soft_recommends_before |
| T-15-13 | Tampering | simulate_date abused by Player | mitigate | getSimulatedNow only consulted on /admin/** code paths; Player journey unaffected |
| T-15-14 | Denial of Service | pre-migration column-absent crash | mitigate | defensive `?? "simple"` reads + slug fallback; deliverable page never throws on missing columns |
| T-15-15 | Information Disclosure | R1 score/rank leak via new hint | mitigate | CLAUDE.md R1 grep audit post-edit; hint carries no score/rank/note |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build` green.
- `npm run test:e2e` — 15 demo specs green (deliverable demo flow unchanged; defensive fallbacks preserve demo behavior).
- CLAUDE.md R1 grep audit (Player-facing components) + R3 grep (no new hard-block) — both clean.
- grep for MOSCOW_DELIVERABLE_SLUG / getTemplateLink / bare fiches-entretien-v1 gate → 0 application-code matches (ENGINE-05 success criterion).
</verification>

<success_criteria>
- LEVELS-04: GM renames/reorders/adds/removes levels; delete guarded by mission references.
- ENGINE-05: composer_kind/template_url/auto_validate replace MOSCOW_DELIVERABLE_SLUG + fiches slug + getTemplateLink; HARD_BLOCK_DEPENDENCIES literal retained as the sole R3 exception.
- ENGINE-06: soft_recommends_before → non-blocking amber hint, zero disabled DOM.
- ENGINE-07: scheduled_date simulable via getSimulatedNow().
</success_criteria>

<output>
Create `.planning/phases/15-mission-engine-no-code-editeur-gm/15-04-SUMMARY.md` when done.
</output>
