---
phase: 15-mission-engine-no-code-editeur-gm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260611230000_phase15_engine_columns.sql
  - lib/types.ts
  - lib/schemas.ts
  - lib/seed/deliverableTemplates.ts
autonomous: true
requirements: [VALID-01]
must_haves:
  truths:
    - "deliverable_templates carries composer_kind/template_url/auto_validate/soft_recommends_before/validation_rules columns (file-first migration, not yet applied to PROD)"
    - "validation_rules with any severity other than 'warn' is rejected at the DB level (CHECK) and at the TS level (z.literal)"
    - "The 13 Digi templates' real composer_kind/template_url/auto_validate values are backfilled in the migration"
    - "fn_auto_eval_fiches_entretien fires on auto_validate=true instead of the slug literal"
    - "Demo seed entries have safe defaults for all 5 new fields; the 15 E2E demo tests stay green"
  artifacts:
    - path: "supabase/migrations/20260611230000_phase15_engine_columns.sql"
      provides: "Additive idempotent migration: 5 columns + CHECK + backfill + trigger generalization"
      contains: "validation_rules_severity_warn_only"
    - path: "lib/types.ts"
      provides: "DeliverableTemplate gains composerKind/templateUrl/autoValidate/softRecommendsBefore/validationRules; ComposerKind + ValidationRule types"
      contains: "ComposerKind"
    - path: "lib/schemas.ts"
      provides: "validationRuleSchema with z.literal('warn') — severity 'error' impossible (VALID-01)"
      contains: "z.literal(\"warn\")"
    - path: "lib/seed/deliverableTemplates.ts"
      provides: "Safe defaults for the 5 new fields on every demo template"
      contains: "composerKind"
  key_links:
    - from: "lib/schemas.ts:validationRuleSchema"
      to: "DB CHECK validation_rules_severity_warn_only"
      via: "structural warn-only mirror (VALID-01 / R2)"
      pattern: "z\\.literal\\(\"warn\"\\)"
---

<objective>
Lay the data foundation for the no-code mission engine: a file-first additive migration that adds the five data-driven behaviour columns to `deliverable_templates`, a DB CHECK constraint that makes `severity: "error"` impossible (R2 / VALID-01), a backfill of the 13 current Digi templates, generalization of the auto-eval trigger to read `auto_validate` instead of the `fiches-entretien-v1` slug literal, and the matching TypeScript type + Zod schema + demo seed defaults.

Purpose: Every later plan (events/missions/template editor, de-hardcoding) reads these columns and types. Defining the contracts first prevents downstream scavenger hunts. The warn-only structural guarantee (VALID-01) must live in both SQL and TS from the start.
Output: 1 migration file (PROD apply deferred to operator checkpoint), extended `DeliverableTemplate` type, `validationRuleSchema`, demo seed defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-CONTEXT.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds on. Extracted from codebase — use directly. -->

From lib/types.ts (lines 63-82) — the type being extended:
```typescript
export type RubricCriterion = { key: string; label: string; max: number };

export type DeliverableTemplate = {
  id: string;
  missionId: string;
  slug: string;
  title: string;
  description: string;
  rubric: RubricCriterion[];
  maxScore: number;
  ord: number;
  isBonus: boolean;
};
```

From lib/schemas.ts (line 16) — reuse, do not recreate:
```typescript
export const httpsUrl = z /* enforces https:// only */;
```

From lib/seed/deliverableTemplates.ts (lines 9-25) — current demo entry shape:
```typescript
{
  id: "...", missionId: "...", slug: "...", title: "...", description: "...",
  rubric: [{ key: "clarity", label: "Clarte", max: 25 }],
  maxScore: 100, ord: 1, isBonus: false,
}
```

Migration analog: supabase/migrations/20260611120200_levels_data_driven.sql (phase 14 — additive idempotent, BEGIN/COMMIT, rollback comment block).

The 13 backfill values (composer_kind / template_url / auto_validate) come from lib/template-links.ts:TEMPLATE_LINKS (read it for the slug→URL map) and CONTEXT.md:
  - composer_kind='moscow' for slug 'moscow-v1'
  - composer_kind='multi_url' AND auto_validate=true for slug 'fiches-entretien-v1'
  - composer_kind='simple', auto_validate=false for all others
  - template_url = TEMPLATE_LINKS[slug].templateUrl for each of the 13 slugs

Trigger to generalize (currently slug-literal): fn_auto_eval_fiches_entretien — current body checks `v_slug <> 'fiches-entretien-v1'`. Reference: .planning/quick/260519-smoke-prod-j1/fix_h1_auto_eval_trigger.sql. The G01 evaluator UUID 59a2b0f7-fa2c-41dd-b3ee-408b0eaf1334 STAYS as the canonical system evaluator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive engine-columns migration</name>
  <read_first>
    - supabase/migrations/20260611120200_levels_data_driven.sql (additive/idempotent migration conventions, BEGIN/COMMIT, rollback comment block)
    - lib/template-links.ts (the 13 slug→templateUrl values to inline in the backfill)
    - .planning/quick/260519-smoke-prod-j1/fix_h1_auto_eval_trigger.sql (current fn_auto_eval_fiches_entretien body + G01 UUID)
    - 15-PATTERNS.md "Migration" section + "Critical Pitfalls" (auto_validate semantics, is_active pre-migration window)
  </read_first>
  <files>supabase/migrations/20260611230000_phase15_engine_columns.sql</files>
  <action>
    Create the migration with header comment block (phase 15, ADDITIVE ONLY, "apply after 20260611220000_events_org_scope_enforce.sql", security tag v0.4-pre-phase15). Wrap in BEGIN/COMMIT.
    1. `ALTER TABLE public.deliverable_templates ADD COLUMN IF NOT EXISTS` for: composer_kind text NOT NULL DEFAULT 'simple', template_url text, auto_validate boolean NOT NULL DEFAULT false, soft_recommends_before uuid REFERENCES public.deliverable_templates(id), validation_rules jsonb NOT NULL DEFAULT '[]'::jsonb.
    2. Add CHECK constraint named validation_rules_severity_warn_only (drop-if-exists then add, for idempotency): passes when validation_rules = '[]'::jsonb OR no element has severity <> 'warn' (use NOT EXISTS over jsonb_array_elements). This makes severity:'error' impossible at DB level (VALID-01 / R2).
    3. Add a composer_kind CHECK constraint restricting to ('simple','moscow','multi_url').
    4. Backfill UPDATE on the 13 Digi templates: composer_kind via CASE on slug (moscow-v1→'moscow', fiches-entretien-v1→'multi_url', else 'simple'); auto_validate = (slug = 'fiches-entretien-v1'); template_url via CASE on slug using the exact URLs from lib/template-links.ts (inline all 13). Scope the UPDATE to templates whose mission belongs to the Digi event (join missions→events where events.slug = 'hack-days-fes-meknes-mai-2026'). Use the actual TEMPLATE_LINKS values, not placeholders.
    5. CREATE OR REPLACE FUNCTION public.fn_auto_eval_fiches_entretien() — copy the existing body but replace the slug-literal guard `v_slug <> 'fiches-entretien-v1'` with a lookup: skip auto-eval unless the row's deliverable_template has auto_validate = true (SELECT auto_validate INTO ... FROM deliverable_templates WHERE id = NEW.deliverable_template_id; IF NOT COALESCE(v_auto, false) THEN RETURN NEW). Keep the G01 UUID 59a2b0f7-fa2c-41dd-b3ee-408b0eaf1334 as the inserted evaluator. Do NOT change the trigger binding.
    6. Append a `-- Rollback` comment block: DROP the 5 columns + 2 constraints + note to restore the prior trigger body.
    Do NOT edit anything under database/** (Write/Edit-denied) — only the supabase/migrations file. PROD apply is deferred to the batched operator checkpoint.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260611230000_phase15_engine_columns.sql','utf8'); const must=['validation_rules_severity_warn_only','composer_kind','soft_recommends_before','auto_validate','fn_auto_eval_fiches_entretien','59a2b0f7-fa2c-41dd-b3ee-408b0eaf1334']; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(!s.includes('BEGIN')||!s.includes('COMMIT')){console.error('no tx');process.exit(1)} console.log('migration ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; grep for each of the 5 new column names returns a match.
    - CHECK constraint validation_rules_severity_warn_only present; uses jsonb_array_elements + severity comparison.
    - Backfill UPDATE contains real OneDrive URLs from TEMPLATE_LINKS (not `<DESIGN_THINKING_URL>` placeholders) and is scoped to the Digi event.
    - Trigger body checks auto_validate (no surviving `<> 'fiches-entretien-v1'` slug literal); G01 UUID retained.
    - Rollback comment block present.
  </acceptance_criteria>
  <done>Migration is additive, idempotent, backfills 13 real values, generalizes the trigger to auto_validate, and is NOT applied to PROD (operator-gated).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DeliverableTemplate type + add validationRuleSchema (warn-only)</name>
  <read_first>
    - lib/types.ts lines 63-82 (RubricCriterion + DeliverableTemplate to extend)
    - lib/schemas.ts lines 16-48 (httpsUrl, existing schema style, z.object/z.enum conventions)
    - tests/unit/actions-schemas.test.ts lines 1-40 (Vitest import-from-lib/schemas pattern, VALID_UUID constant)
  </read_first>
  <files>lib/types.ts, lib/schemas.ts</files>
  <behavior>
    - validationRuleSchema accepts { rule: "non-empty", severity: "warn", message: "..." } → success
    - validationRuleSchema rejects { rule, severity: "error", message } → failure (R2 / VALID-01 structural)
    - validationRuleSchema rejects empty rule string
    - composerKindSchema accepts "simple" | "moscow" | "multi_url"; rejects "other"
  </behavior>
  <action>
    In lib/types.ts: add `export type ComposerKind = "simple" | "moscow" | "multi_url";` and `export type ValidationRule = { rule: string; severity: "warn"; message: string };`. Extend `DeliverableTemplate` with: `composerKind: ComposerKind; templateUrl: string | null; autoValidate: boolean; softRecommendsBefore: string | null; validationRules: ValidationRule[];`. Follow the existing string-literal-union convention (no `enum` keyword).
    In lib/schemas.ts: add `export const composerKindSchema = z.enum(["simple","moscow","multi_url"]);` and `export const validationRuleSchema = z.object({ rule: z.string().min(1), severity: z.literal("warn"), message: z.string().min(1) });`. The `z.literal("warn")` is the TS-side VALID-01 guarantee — it must mirror the DB CHECK. Do NOT add an `error`/`info` branch.
    Add a small test block to tests/unit/actions-schemas.test.ts (new describe for validationRuleSchema + composerKindSchema) covering the 4 behaviors above. Import the new schemas from @/lib/schemas.
  </action>
  <verify>
    <automated>npm run test:unit -- actions-schemas</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (new DeliverableTemplate fields compile; no missing-field errors yet — seed defaults added in Task 3).
    - validationRuleSchema unit test: severity 'error' → safeParse.success === false; severity 'warn' → success === true.
    - composerKindSchema rejects unknown kind.
  </acceptance_criteria>
  <done>ComposerKind + ValidationRule types exported; validationRuleSchema enforces warn-only via z.literal; new unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe defaults for the 5 new fields to the demo seed</name>
  <read_first>
    - lib/seed/deliverableTemplates.ts (every demo entry — current shape ends at isBonus)
    - lib/types.ts (the DeliverableTemplate fields added in Task 2 — required vs nullable)
  </read_first>
  <files>lib/seed/deliverableTemplates.ts</files>
  <action>
    For every entry in `demoDeliverableTemplates`, add the 5 new fields with safe defaults so the array satisfies the extended `DeliverableTemplate` type: `composerKind: "simple"`, `templateUrl: null`, `autoValidate: false`, `softRecommendsBefore: null`, `validationRules: []`. Do NOT change any existing field, id, or rubric — additive only. This keeps the 15 E2E demo flows green (new fields are inert defaults; no demo surface reads them yet).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes with zero errors (seed now satisfies extended type).
    - `npm run test:e2e` — the 15 existing demo specs stay green (no behavioral change).
    - grep confirms `composerKind` appears once per demo template entry.
  </acceptance_criteria>
  <done>Demo seed compiles against the extended type; all 15 E2E demo flows unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → PROD DB | Schema-altering DDL; applied later at operator checkpoint, not by this plan |
| GM editor input (later plans) → validation_rules jsonb | Untrusted severity values must never reach 'error' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | validation_rules severity | mitigate | DB CHECK validation_rules_severity_warn_only + TS z.literal("warn") — defense in depth (VALID-01/R2) |
| T-15-02 | Elevation | composer_kind free text | mitigate | CHECK restricts to ('simple','moscow','multi_url') + z.enum mirror |
| T-15-03 | Denial of Service | migration applied to wrong state | accept | Additive/idempotent (ADD COLUMN IF NOT EXISTS, DROP-then-ADD constraint); PROD apply operator-gated with smoke |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build` green.
- `npm run test:unit` green (new validationRuleSchema/composerKindSchema tests included).
- `npm run test:e2e` — 15 demo specs green.
- Migration NOT applied to PROD (deferred to operator checkpoint with pending 20260611220000).
</verification>

<success_criteria>
- 5 data-driven columns + CHECK + backfill + generalized trigger present in one additive migration file.
- severity:'error' impossible at both DB (CHECK) and TS (z.literal) layers — VALID-01 satisfied structurally.
- TS type + Zod schema + demo seed all compile and pass the full gate.
</success_criteria>

<output>
Create `.planning/phases/15-mission-engine-no-code-editeur-gm/15-01-SUMMARY.md` when done.
</output>
