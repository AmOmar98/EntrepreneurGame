---
phase: 15-mission-engine-no-code-editeur-gm
plan: 05
type: execute
wave: 5
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - tests/unit/engine-schemas.test.ts
  - tests/unit/clone-remap.test.ts
  - tests/e2e/admin-engine.spec.ts
  - tests/smoke/r3-no-hardcoded-block.spec.ts
  - .planning/phases/15-mission-engine-no-code-editeur-gm/15-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Unit tests cover the editor Zod schemas (validationRuleSchema warn-only, composerKindSchema, saveDeliverableTemplate rubric shape) and the clone old→new soft_recommends_before remap logic"
    - "An E2E spec exercises the /admin/events + /admin/levels + missions editor routes in demo mode with read-only assertions"
    - "The R3 grep gate confirms zero new hard-block: no configurable disabled/blocks_progression_to derived from soft_recommends_before"
    - "Full quality gate (typecheck+lint+build+test:unit+test:e2e) is green and recorded"
    - "All 10 phase requirements (ENGINE-01..07, LEVELS-04, VALID-01, VALID-02) are traced to evidence"
  artifacts:
    - path: "tests/unit/engine-schemas.test.ts"
      provides: "Editor Zod schema unit tests (warn-only, composer kind, rubric min-1 {key,label,max})"
      contains: "validationRuleSchema"
    - path: "tests/unit/clone-remap.test.ts"
      provides: "Pure clone remap unit test (old→new UUID map for soft_recommends_before)"
      contains: "softRecommendsBefore"
    - path: "tests/e2e/admin-engine.spec.ts"
      provides: "Demo-mode editor smoke (routes render, read-only notice)"
      contains: "/admin/events"
    - path: ".planning/phases/15-mission-engine-no-code-editeur-gm/15-VERIFICATION.md"
      provides: "Requirement→evidence trace + gate log + R1/R3 audit results"
      contains: "ENGINE-05"
  key_links:
    - from: "tests/unit/engine-schemas.test.ts"
      to: "lib/schemas.ts validationRuleSchema/composerKindSchema"
      via: "import-from-lib (no Next runtime)"
      pattern: "from \"@/lib/schemas\""
---

<objective>
Lock the phase with the quality net the CONTEXT requires: new unit tests for the editor Zod schemas and the clone remap logic, a new E2E smoke for the editor routes in demo mode (read-only assertions), the R3 grep gate extended to confirm no new hard-block, and a VERIFICATION.md tracing all 10 requirements to evidence.

Purpose: CONTEXT mandates "nouveaux tests unitaires pour les schémas Zod de l'éditeur et le clonage ; E2E GM editor smoke en mode demo" and a green gate per push. This plan provides the proof the phase goal is met.
Output: 2 unit test files, 1 E2E spec, R3 smoke extension, 15-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-CONTEXT.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-PATTERNS.md

<interfaces>
<!-- Test scaffolds + contracts to assert against. -->

Unit test analog: tests/unit/actions-schemas.test.ts (Vitest, import from @/lib/schemas, VALID_UUID = all-zeros).
E2E analog: tests/e2e/onboarding.spec.ts + submit-deliverable.spec.ts (Playwright, demo mode, no Supabase env). testMatch widened to tests/ (phase 13).
R3 smoke analog: tests/smoke/r3-no-hardcoded-block.spec.ts (existing grep-style assertion of no DOM hard-block).

Schemas under test (from 15-01/15-03): composerKindSchema, validationRuleSchema (z.literal("warn")), the rubric array schema inside saveDeliverableTemplateFlow ({key,label,max}.min(1)).
Clone remap logic (from 15-02): cloneEventFlow builds an old→new template UUID map and remaps soft_recommends_before. Extract the pure remap step into a small exported helper (e.g. lib/clone-remap.ts:remapSoftRecommends(templates, idMap)) if not already pure, so it is unit-testable without Supabase. If 15-02 inlined it, refactor minimally to expose the pure function.

R1/R3 audit commands: CLAUDE.md Pre-edit guards section (the grep one-liners).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests — editor schemas + clone remap</name>
  <read_first>
    - tests/unit/actions-schemas.test.ts (Vitest structure, import-from-lib pattern, VALID_UUID)
    - lib/schemas.ts (composerKindSchema, validationRuleSchema from 15-01)
    - app/actions.ts saveDeliverableTemplateFlow rubric schema (15-03) — extract the rubric array schema to lib/schemas.ts as `rubricSchema` if it is inline, so it is importable without Next runtime
    - app/actions.ts cloneEventFlow remap (15-02) — locate the soft_recommends_before old→new remap
  </read_first>
  <files>tests/unit/engine-schemas.test.ts, tests/unit/clone-remap.test.ts</files>
  <behavior>
    engine-schemas.test.ts:
    - validationRuleSchema: {rule,severity:"warn",message} → success; severity:"error" → failure; empty rule → failure
    - composerKindSchema: "moscow"/"simple"/"multi_url" → success; "hard_block" → failure
    - rubricSchema: [{label:"X",max:25}] with key derived → success; [] (empty) → failure; max>100 → failure
    clone-remap.test.ts:
    - remapSoftRecommends maps a template's softRecommendsBefore from an old id to the new id via idMap
    - a template whose softRecommendsBefore points outside the map (or null) → result is null (never references a source row)
  </behavior>
  <action>
    If saveDeliverableTemplateFlow's rubric array schema is inline, extract it to `export const rubricSchema` in lib/schemas.ts (and have the action import it) so it is unit-testable. If cloneEventFlow's remap is inline, extract the pure mapping step to `export function remapSoftRecommends(softRecommendsBefore: string | null, idMap: Map<string,string>): string | null` in a small lib/clone-remap.ts and call it from the action. Keep refactors minimal and behavior-preserving.
    Write tests/unit/engine-schemas.test.ts covering the validationRuleSchema / composerKindSchema / rubricSchema behaviors above (import from @/lib/schemas; describe/it/expect like the analog).
    Write tests/unit/clone-remap.test.ts covering remapSoftRecommends (import from @/lib/clone-remap). Assert the clone never resolves to a source-row id.
  </action>
  <verify>
    <automated>npm run test:unit -- engine-schemas clone-remap</automated>
  </verify>
  <acceptance_criteria>
    - engine-schemas: severity "error" → success===false; rubric [] → failure; composerKind "hard_block" → failure.
    - clone-remap: out-of-map / null soft_recommends_before → null; in-map → new id.
    - typecheck passes (any extraction refactor compiles); both files green.
  </acceptance_criteria>
  <done>Editor schema + clone remap covered by unit tests (CONTEXT requirement satisfied).</done>
</task>

<task type="auto">
  <name>Task 2: E2E editor demo smoke + R3 gate extension</name>
  <read_first>
    - tests/e2e/onboarding.spec.ts (Playwright demo-mode spec structure, no Supabase env)
    - tests/smoke/r3-no-hardcoded-block.spec.ts (existing R3 assertion pattern)
    - 15-UI-SPEC.md Demo mode guard + Cardinal Rules Encoded in UI
  </read_first>
  <files>tests/e2e/admin-engine.spec.ts, tests/smoke/r3-no-hardcoded-block.spec.ts</files>
  <action>
    tests/e2e/admin-engine.spec.ts: a Playwright spec (demo mode) that visits `/admin/events`, `/admin/levels`, and one `/admin/events/[id]/missions` route (use a demo event id from the seed). Assert each route renders the AppShell staff chrome and the demo notice (admin_engine_demo_disabled copy) is visible — i.e. read-only editor in demo mode. Assert NO server write occurs (forms present but demo). Do not require a real backend. These are NEW routes — they must not perturb the existing 15 demo specs.
    tests/smoke/r3-no-hardcoded-block.spec.ts: extend the existing R3 assertion to also cover the new editor + the soft_recommends_before hint — assert that no editor surface or the Player deliverable page emits a `disabled` attribute or `pointer-events: none` that is derived from a configurable dependency (soft_recommends_before). Keep the existing assertions intact; add cases for the new files. If this file is a grep-style guard, add the new globbed paths (app/admin/events, app/admin/levels, components/admin-*-editor.tsx, the deliverable page soft hint) and assert 0 matches for hard-block patterns there.
  </action>
  <verify>
    <automated>npm run test:e2e -- admin-engine && npm run test:e2e -- r3-no-hardcoded-block</automated>
  </verify>
  <acceptance_criteria>
    - admin-engine.spec passes in demo mode: 3 editor routes render with the demo read-only notice.
    - The 15 pre-existing E2E demo specs remain green (run full `npm run test:e2e`).
    - r3 smoke passes with new editor/hint paths included; 0 hard-block matches.
  </acceptance_criteria>
  <done>Editor demo smoke + extended R3 gate green; full E2E suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate run + R1/R3 audit + 15-VERIFICATION.md requirement trace</name>
  <read_first>
    - CLAUDE.md Pre-edit guards (R1 grep one-liner + R3 audit) + Merge policy (smoke regression)
    - ROADMAP.md Phase 15 success criteria (6 items) — map each to evidence
    - REQUIREMENTS.md ENGINE-01..07, LEVELS-04, VALID-01, VALID-02
  </read_first>
  <files>.planning/phases/15-mission-engine-no-code-editeur-gm/15-VERIFICATION.md</files>
  <action>
    Run the full gate: `npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e`. Run the CLAUDE.md R1 grep audit (Player-facing components: score/rank/note must not leak — the new editor is GM-only; the deliverable soft hint must carry no score/rank) and the R3 grep (no new hard-block). Run the ENGINE-05 grep gate: confirm `MOSCOW_DELIVERABLE_SLUG`, `getTemplateLink`, and bare `"fiches-entretien-v1"` gates return 0 application-code matches (the HARD_BLOCK_DEPENDENCIES literal is the documented R3 exception and MAY remain — note it explicitly).
    Write 15-VERIFICATION.md: a table mapping each of the 10 requirements (ENGINE-01..07, LEVELS-04, VALID-01, VALID-02) AND the 6 ROADMAP success criteria to concrete evidence (file:line, test name, grep result). Record the gate log (pass/fail per command), the R1 and R3 audit outputs, and the ENGINE-05 grep results. Flag any residual (e.g. WELCOME_GUIDE_URL deferred, PROD migration apply deferred to operator checkpoint) under a "Deferred / operator-gated" section. If any requirement lacks evidence, mark it GAP — do not claim green falsely.
    Use grep -v '^#' before grep -c on any count gate (header-comment hygiene per planner rules).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Full gate (5 commands) green.
    - ENGINE-05 grep: 0 application-code matches for MOSCOW_DELIVERABLE_SLUG / getTemplateLink / bare fiches-entretien-v1 gate; HARD_BLOCK_DEPENDENCIES noted as the sole retained exception.
    - R1 grep audit clean (no Player score/rank leak); R3 grep clean (no new hard-block).
    - 15-VERIFICATION.md maps all 10 requirements + 6 success criteria to evidence; deferred items (WELCOME_GUIDE_URL, PROD apply) flagged.
  </acceptance_criteria>
  <done>Phase 15 verified green with full requirement→evidence trace; audits clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → CI gate | tests run without Supabase env (demo mode); must not require secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Repudiation | false-green verification | mitigate | 15-VERIFICATION.md requires file:line/test/grep evidence per requirement; GAP marked if absent |
| T-15-17 | Tampering | R3 regression slips past gate | mitigate | r3 smoke extended to new editor + soft-hint paths; grep 0-match gate |
</threat_model>

<verification>
- Full gate green: typecheck + lint + build + test:unit + test:e2e.
- 15-VERIFICATION.md present with 10 requirements + 6 success criteria traced.
- R1/R3/ENGINE-05 grep audits recorded and clean.
</verification>

<success_criteria>
- New unit tests cover editor Zod schemas + clone remap (CONTEXT requirement).
- New E2E editor demo smoke green; 15 pre-existing demo specs unaffected.
- R3 gate extended and green; ENGINE-05 de-hardcoding proven by grep.
- Every phase requirement traced to evidence in 15-VERIFICATION.md.
</success_criteria>

<output>
Create `.planning/phases/15-mission-engine-no-code-editeur-gm/15-05-SUMMARY.md` when done.
</output>
