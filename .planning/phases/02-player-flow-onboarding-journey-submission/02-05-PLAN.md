---
phase: 02-player-flow-onboarding-journey-submission
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - app/journey/page.tsx
  - app/journey/deliverable/[id]/page.tsx
  - app/onboarding/page.tsx
  - components/app-shell.tsx
  - lib/i18n.ts
  - .planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md
autonomous: false
requirements:
  - JOURNEY-01
  - JOURNEY-02
  - JOURNEY-03
  - SUBMIT-01
  - SUBMIT-02
  - SUBMIT-04
  - ONBOARD-02
  - ONBOARD-03
  - DATA-03
must_haves:
  truths:
    - "Le smoke test E2E manuel passe : login Player -> /onboarding -> form -> /journey -> submit deliverable -> readonly"
    - "Aucune fuite seed apparente en mode prod sur les 3 pages (/journey, /onboarding, /journey/deliverable/[id])"
    - "RLS verifie : un Player B ne voit pas les submissions du Player A"
    - "Tous les libelles user-facing passent par lib/i18n.ts (FR ASCII)"
    - "Les etats vides (no player, no missions, no deliverables) ont des messages explicites non-techniques"
  artifacts:
    - path: ".planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md"
      provides: "Procedure de smoke test E2E executable a la main par Omar"
    - path: "lib/i18n.ts"
      provides: "Cles FR ASCII completes pour Phase 2"
  key_links:
    - from: "components/app-shell.tsx"
      to: "lib/i18n.ts"
      via: "t(locale).key"
      pattern: "t\\(.*\\)"
---

<objective>
Polish d'integration cross-plan, smoke test E2E manuel, garantie aucune regression.

Purpose: Sceller la Phase 2 avec un parcours integre qui passe en bout-en-bout. Le 13 mai 8h30 il ne doit y avoir AUCUNE surprise.
Output: Document de smoke test, audit i18n, audit anti-leak, audit RLS basique. Ce plan est sequentiel et a un checkpoint de validation Omar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-CONTEXT.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-01-PLAN.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-02-PLAN.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-03-PLAN.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-04-PLAN.md
@lib/i18n.ts
@components/app-shell.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Polish i18n + etats vides + audit anti-leak</name>
  <files>lib/i18n.ts, app/journey/page.tsx, app/journey/deliverable/[id]/page.tsx, app/onboarding/page.tsx, components/app-shell.tsx</files>
  <action>
1. Auditer toutes les chaines user-facing introduites par Plans 01-03 :
   - /onboarding (form labels, button, errors)
   - /journey (header, timeline, deliverables list)
   - /journey/deliverable/[id] (form, readonly, lock banner)
   - AppShell nav items pour role=player
   Centraliser TOUTES dans `lib/i18n.ts` cle par cle. Utiliser ASCII strict (pas d'accents) — convention Phase 1.

2. Verifier que les 3 pages ont des etats vides explicites :
   - /journey sans player rattache : "Aucune equipe rattachee a votre compte. Contactez le GameMaster."
   - /journey sans missions du jour : "Aucun atelier prevu aujourd'hui."
   - /journey sans deliverables : "Aucun livrable a rendre pour le moment."
   - /journey/deliverable/[id] template introuvable : `notFound()` (next/navigation)
   - /onboarding mode demo : "Mode demo : connectez-vous a Supabase pour completer l'onboarding."

3. Audit anti-leak (DATA-03 / BRAND-05) — executer (et logger les commandes dans le SUMMARY) :
   ```bash
   # Aucun match attendu en prod-mode :
   grep -ri "atlas-soil" app/ lib/ components/ database/seed_event_hackdays.sql
   grep -ri "Tamwilcom\|Bank of Africa\|Innov Invest\|Bluespace" app/ components/ lib/seed/
   ```
   Si match -> remediation immediate.

4. Smoke RLS basique (sans creer 2 comptes — validation visuelle code) :
   - Confirmer dans `app/journey/deliverable/[id]/page.tsx` que l'acces a un id externe -> notFound() (selectionne via RLS donc 0 rows).
   - Confirmer dans `app/actions.ts:submitDeliverable` que l'ownership check applicatif via player_members existe (cf Plan 03).

5. Confirmer suppression de tout `pre`/`debug` JSON visible (cf placeholder existant `app/journey/page.tsx` qui affichait `user.email | role`). Aucun debug user-visible en prod.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
- Toutes les chaines passent par i18n FR ASCII.
- Etats vides explicites sur /onboarding, /journey, /journey/deliverable/[id].
- Aucun match grep "atlas-soil" / partenaires dans le code applicatif et les seeds applicables.
- Aucun bloc debug visible en prod.
- Build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ecrire 02-SMOKE.md (procedure E2E manuelle)</name>
  <files>.planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md</files>
  <action>
Creer `.planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md` avec une procedure executable par Omar :

Sections :
1. **Prerequis** : Supabase prod fresh + schema/triggers/rls/seed_event_hackdays appliques. 1 GameMaster bootstrap (cree manuellement). 2 comptes Players factices (PlayerA, PlayerB) chacun rattache a un Player different (1 player_member chacun).

2. **Scenario A - Onboarding** :
   - Login PlayerA email/password
   - Verifier redirect automatique /journey -> /onboarding
   - Remplir form (nom equipe "Equipe A Test", idee 50 chars, Likert 3-3-3-3-3, cocher membres)
   - Submit
   - Verifier redirect /journey
   - Verifier en SQL : `select onboarded_at, score_engagement from players where id=...` -> onboarded_at NOT NULL, score_engagement = 10
   - Re-visiter /onboarding -> redirect /journey

3. **Scenario B - Journey display** :
   - /journey rend header (nom equipe, niveau L0/L1, score 0)
   - Timeline affiche les missions du 13 mai (apres seed)
   - Liste deliverables affiche 9 entrees status "a_rendre"

4. **Scenario C - Submit V1** :
   - Click sur le 1er deliverable -> /journey/deliverable/[id]
   - Selectionner kind=proof_url, saisir "https://example.com/test"
   - Submit -> banniere "verrouille"
   - Refresh -> readonly persist
   - Tenter submit avec http:// non https -> erreur Zod
   - Tenter 2eme submit du meme deliverable -> erreur "deja existe"

5. **Scenario D - RLS** :
   - Logout PlayerA, login PlayerB
   - PlayerB visite /journey -> ses propres donnees, pas celles de PlayerA
   - PlayerB tente d'acceder /journey/deliverable/<id_du_template_soumis_par_A> directement -> il VOIT le formulaire (template publique) MAIS ne voit pas la submission de A (RLS). Status affiche "a_rendre" pour B.
   - PlayerB submit -> insert reussit pour son propre player_id.

6. **Scenario E - Anti-leak** :
   - Verifier visuellement aucune mention "demo", "atlas", noms de partenaires dans /journey, /onboarding.
   - Verifier en SQL : `select count(*) from events where slug='hack-days-fes-meknes-mai-2026'` = 1.

7. **Resultat attendu** : tous les scenarios PASS. Logger PASS/FAIL par scenario avec timestamp.

Le document doit etre auto-suffisant : Omar doit pouvoir l'executer sans relire les plans.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md','utf8');for(const k of ['Scenario A','Scenario B','Scenario C','Scenario D','Scenario E']){if(!s.includes(k))throw new Error('missing '+k);}console.log('ok')"</automated>
  </verify>
  <done>
- 02-SMOKE.md cree avec 5 scenarios A-E executables par Omar.
- Chaque scenario a des etapes numerotees + criteres PASS explicites.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validation manuelle smoke test E2E</name>
  <what-built>Phase 2 complete : onboarding + journey + submission V1 + seed Event + anti-leak.</what-built>
  <how-to-verify>
1. Appliquer database/seed_event_hackdays.sql sur Supabase prod (si pas deja fait).
2. Creer 2 comptes Players factices via Supabase Auth + 2 lignes player_members.
3. Executer integralement `.planning/phases/02-player-flow-onboarding-journey-submission/02-SMOKE.md` (Scenarios A a E).
4. Logger PASS/FAIL pour chaque scenario.
  </how-to-verify>
  <resume-signal>Repondre "approved" si tous PASS, sinon decrire le scenario et l'etape qui FAIL.</resume-signal>
</task>

</tasks>

<verification>
- typecheck + lint + build clean.
- 02-SMOKE.md complet et execute par Omar avec PASS sur tous les scenarios.
- Aucune fuite seed visible.
</verification>

<success_criteria>
- Tous les requirements Phase 2 (ONBOARD-02/03, JOURNEY-01/02/03, SUBMIT-01/02/04, EVENT-01/02, DATA-03) verifies en bout-en-bout. ✓
- Phase 2 prete pour Phase 3 (Mentor Flow). ✓
</success_criteria>

<output>
Apres completion + checkpoint approuve, creer `.planning/phases/02-player-flow-onboarding-journey-submission/02-05-SUMMARY.md` documentant :
- Resultat de chaque scenario smoke test (PASS/FAIL + timestamp).
- Bugs decouverts et corriges en cours de smoke (le cas echeant).
- Etat final Phase 2 + handoff a Phase 3.
</output>
</content>
