---
phase: "12-quick-260510-t3x"
plan: "12-06"
type: execute
wave: 2
depends_on:
  - "12-02"
  - "12-03"
  - "12-05"
files_modified:
  - app/actions.ts
  - lib/i18n.ts
autonomous: false
requirements:
  - T3X-EXPANSION-SERVER-ACTIONS
must_haves:
  truths:
    - "app/actions.ts exporte claimBonusEventFlow(_prev, formData) -> WorkflowState — Player submit URL preuve, INSERT row bonus_events status='submitted', multiplier_factor + multiplier_scope copiés depuis BONUS_DEFAULTS[type]"
    - "app/actions.ts exporte reviewBonusEventFlow(_prev, formData) -> WorkflowState — Mentor validate Y/N + feedback, UPDATE bonus_events status='validated' OU 'rejected' + reviewed_by + reviewed_at"
    - "app/actions.ts exporte createMoscowCardFlow(_prev, formData) -> WorkflowState — Player INSERT 1 carte (project_id, deliverable_template_id, bucket, ord, feature, pourquoi, contrainte)"
    - "app/actions.ts exporte updateMoscowCardFlow(_prev, formData) -> WorkflowState — Player UPDATE 1 carte (bucket, ord, feature, pourquoi, contrainte)"
    - "app/actions.ts exporte deleteMoscowCardFlow(_prev, formData) -> WorkflowState — Player DELETE 1 carte"
    - "app/actions.ts exporte reorderMoscowCardsFlow(_prev, formData) -> WorkflowState — Player batch UPDATE après DnD (formData = JSON serialized {cardId, bucket, ord}[])"
    - "app/actions.ts exporte submitMoscowDeliverableFlow(_prev, formData) -> WorkflowState — INSERT submission row kind='proof_url' avec URL pointant vers /journey/deliverable/{templateId}/moscow-snapshot (Plan 10) — passe par submitDeliverable interne ou crée directement"
    - "Toutes les actions valident via zod safeParse, retournent {ok, message} sans throw (R2 warn-only)"
    - "claimBonusEventFlow utilise httpsUrl shared schema (lignes 161-164 app/actions.ts)"
    - "Toutes les actions check hasSupabaseEnv() + createClient() avec fallback message FR"
    - "claimBonusEventFlow check ownership : user doit être membre du player_id (player_members.user_id = auth.uid())"
    - "reviewBonusEventFlow check ownership : user doit être mentor (current_app_role IN ('mentor','game_master'))"
    - "Toutes les actions revalidatePath approprié (/journey, /journey/bonus/:type, /mentor, /mentor/bonus/:id, /journey/deliverable/:id)"
    - "lib/i18n.ts ajoute >= 12 keys en français (claim_success, claim_error, claim_url_required, review_success, review_rejected, review_validated, moscow_card_created, moscow_card_updated, moscow_card_deleted, moscow_reorder_saved, moscow_submission_saved, bonus_already_claimed, etc.)"
    - "npm run typecheck clean, npm run lint clean"
    - "R1 préservé : aucune action n'expose multiplier_factor ou score brut dans WorkflowState.message"
    - "R2 préservé : zod errors retournent {ok:false, message} - aucun throw"
    - "R3 préservé : aucune action ne bloque une autre mission selon état bonus/moscow"
  artifacts:
    - path: "app/actions.ts"
      provides: "7 nouvelles server actions (claimBonusEventFlow, reviewBonusEventFlow, createMoscowCardFlow, updateMoscowCardFlow, deleteMoscowCardFlow, reorderMoscowCardsFlow, submitMoscowDeliverableFlow)"
      contains: "claimBonusEventFlow|reviewBonusEventFlow|createMoscowCardFlow|reorderMoscowCardsFlow"
    - path: "lib/i18n.ts"
      provides: "12+ FR copy keys pour bonus + moscow workflows"
      contains: "claim_|review_bonus_|moscow_"
  key_links:
    - from: "app/actions.ts claimBonusEventFlow"
      to: "supabase.from('bonus_events').insert(...)"
      via: "INSERT row avec multiplier_factor copy depuis BONUS_DEFAULTS[type]"
      pattern: "BONUS_DEFAULTS\\[.*\\]\\.multiplierFactor"
    - from: "app/actions.ts reviewBonusEventFlow"
      to: "supabase.from('bonus_events').update({status, reviewed_by, reviewed_at, feedback})"
      via: "UPDATE WHERE id (RLS gate via is_mentor() helper)"
      pattern: "from\\('bonus_events'\\)\\.update"
    - from: "app/actions.ts reorderMoscowCardsFlow"
      to: "supabase.from('moscow_cards').upsert(rows, {onConflict: 'id'})"
      via: "Batch UPDATE post-DnD : array of {id, bucket, ord} via upsert"
      pattern: "upsert.*onConflict.*id"
---

<objective>
Wave 2 — Implémenter les 7 server actions pour bonus_events et moscow_cards. Pattern strict aligné sur les actions existantes : `_prev: WorkflowState`, `formData: FormData`, `Promise<WorkflowState>`, zod safeParse, hasSupabaseEnv check, RLS handle, revalidatePath.

**Architecture** :
- `claimBonusEventFlow` : Player submit. Valide URL HTTPS, vérifie ownership, INSERT row bonus_events. Multiplier_factor et multiplier_scope copiés depuis BONUS_DEFAULTS[type].
- `reviewBonusEventFlow` : Mentor review. UPDATE status (validated|rejected), feedback, reviewed_by, reviewed_at.
- `createMoscowCardFlow` / `updateMoscowCardFlow` / `deleteMoscowCardFlow` : CRUD individuel par carte.
- `reorderMoscowCardsFlow` : batch UPDATE après DnD (Plan 09 envoie un JSON serialized des cartes mutées).
- `submitMoscowDeliverableFlow` : crée une submission row pour le livrable #4 avec proof_url pointing vers une route SSR snapshot (Plan 10).

**R3 garanti** : claim/review bonus = OPTIONNEL (R3). MoSCoW Kanban submission = optionnel (R3).
**R2 garanti** : zod retourne `{ok:false, message}` sans throw. Recommandations rubric (>=2 MUST, >=1 WONT) NE sont PAS validées hard ici — c'est warn-only via UI Plan 09.
**R1 garanti** : aucune action n'inclut score/rank/multiplier_factor brut dans `message`.

**Scope** : ajout dans `app/actions.ts` + ajout keys i18n. PAS de UI (Plans 08-09-10). PAS de score logic (Plan 07).

**Output** :
- `app/actions.ts` enrichi (~250-350 lignes ajoutées en bas du fichier, après les actions existantes)
- `lib/i18n.ts` 12+ nouvelles keys FR
- npm checks clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/actions.ts
@lib/types.ts
@lib/i18n.ts
@components/submission-form.tsx
@supabase/migrations/20260510170000_bonus_events_recreate.sql
@supabase/migrations/20260510170100_moscow_cards.sql

<interfaces>
<!-- Pattern existant à imiter (app/actions.ts lignes 161-308) -->

```typescript
// httpsUrl schema réutilisable :
const httpsUrl = z
  .string()
  .url()
  .refine((u) => u.startsWith("https://"), "URL doit commencer par https://");

// WorkflowState shape :
export type WorkflowState = { ok: boolean; message: string };

// Pattern action standard :
export async function actionFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) {
    return { ok: false, message: "Backend non configure." };
  }
  const supabase = await createClient();
  if (!supabase) {
    return { ok: false, message: "Backend non configure." };
  }

  // 1. Parse formData via zod
  const parsed = SchemaName.safeParse({...});
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  // 2. Get current user
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifie." };

  // 3. Ownership check (defense-in-depth + RLS)
  const { data: membership } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (!membership) return { ok: false, message: "Aucun Player rattache." };

  // 4. INSERT/UPDATE/DELETE
  const { error } = await supabase.from("bonus_events").insert({...});
  if (error) return { ok: false, message: error.message };

  // 5. Revalidate
  revalidatePath("/journey");
  return { ok: true, message: "OK." };
}
```

Convention nommage : actions avec WorkflowState retournent `Flow` suffix (cf. CLAUDE.md "Server actions ending in Flow return WorkflowState").

Imports à ajouter (en haut de app/actions.ts) :
```typescript
import { BONUS_DEFAULTS, BONUS_MULTIPLIER_CAP, type BonusType, type MoscowBucket } from "@/lib/types";
```

Dictionary i18n keys existing pattern (lib/i18n.ts) :
```typescript
submission_url_label: "Lien HTTPS de la preuve",
submission_url_placeholder: "https://...",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les zod schemas + 7 server actions à app/actions.ts</name>
  <files>app/actions.ts</files>
  <read_first>
    - `app/actions.ts` (lignes 1-50 imports, lignes 161-308 pattern submitDeliverable, lignes 320-560 pattern evaluateSubmission, lignes 562-680 pattern addEvaluationCommentFlow)
    - `lib/types.ts` (BonusType, BonusEvent, MoscowCard, BONUS_DEFAULTS — créés Plan 05)
    - `supabase/migrations/20260510170000_bonus_events_recreate.sql` (DDL bonus_events pour mapping snake_case)
    - `supabase/migrations/20260510170100_moscow_cards.sql` (DDL moscow_cards)
  </read_first>
  <action>
**Stratégie** : appender en bas de `app/actions.ts` (après la dernière action existante, probablement après ligne 1231 createAnnouncementFlow). Ajouter d'abord l'import des nouveaux types, puis les schemas, puis les 7 actions.

**Étape 1 — Ajouter les imports** au tout début du fichier (vers ligne 10-11, à côté des autres imports `@/lib/types`) :

```typescript
import {
  BONUS_DEFAULTS,
  BONUS_MULTIPLIER_CAP,
  type BonusType,
  type BonusStatus,
  type MoscowBucket,
} from "@/lib/types";
```

(Ne PAS dupliquer si déjà importé — vérifier d'abord.)

**Étape 2 — Ajouter une nouvelle section en BAS du fichier** (après `createAnnouncementFlow` ligne 1231 ou plus loin) :

```typescript

// ============================================================================
// Bonus events (T3X-EXPANSION wave 2 / plan 06 — D-02 / D-03)
// Player submit URL preuve -> INSERT bonus_events status='submitted'
// Mentor review -> UPDATE status='validated'|'rejected' + reviewed_by/at + feedback
// R1 preserved : multiplier_factor stocke en DB / TS, JAMAIS dans WorkflowState.message
// R2 preserved : zod safeParse + return {ok:false, message}, no throw
// R3 preserved : aucun blocage cross-mission via cette action
// ============================================================================

const bonusClaimSchema = z.object({
  type: z.enum(["bonus_verbatims_terrain", "bonus_dev_plan", "bonus_prototype_draft"]),
  title: z.string().min(3).max(200),
  description: z.string().max(2000).optional(),
  docUrl: httpsUrl,
});

export async function claimBonusEventFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = bonusClaimSchema.safeParse({
    type: formData.get("type"),
    title: formData.get("title"),
    description: formData.get("description") ?? undefined,
    docUrl: formData.get("docUrl"),
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifie." };

  const { data: membership } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (!membership) {
    return { ok: false, message: "Aucun Player rattache a votre compte." };
  }

  // R3 : pas de check "deja claime" hard. On autorise re-claim apres rejet.
  // Mais on bloque double "submitted" en attente review (UX clarity, pas pedagogical block).
  const { data: pending } = await supabase
    .from("bonus_events")
    .select("id, status")
    .eq("project_id", membership.player_id)
    .eq("type", parsed.data.type)
    .eq("status", "submitted")
    .maybeSingle();
  if (pending) {
    return { ok: false, message: "Bonus deja soumis, en attente de validation Mentor." };
  }

  const defaults = BONUS_DEFAULTS[parsed.data.type as BonusType];
  // Cap multiplier_factor a BONUS_MULTIPLIER_CAP (defense-in-depth, le DDL CHECK l'enforce aussi)
  const multiplierFactor = Math.min(defaults.multiplierFactor, BONUS_MULTIPLIER_CAP);

  const { error: insErr } = await supabase.from("bonus_events").insert({
    project_id: membership.player_id,
    type: parsed.data.type,
    title: parsed.data.title,
    description: parsed.data.description ?? "",
    doc_url: parsed.data.docUrl,
    status: "submitted",
    multiplier_factor: multiplierFactor,
    multiplier_scope: defaults.scope,
    claimed_by: user.id,
    claimed_at: new Date().toISOString(),
  });
  if (insErr) return { ok: false, message: insErr.message };

  revalidatePath("/journey");
  revalidatePath(`/journey/bonus/${parsed.data.type}`);
  revalidatePath("/mentor");
  return { ok: true, message: "Bonus soumis. Le Mentor va le valider." };
}

const bonusReviewSchema = z.object({
  bonusEventId: z.string().uuid(),
  decision: z.enum(["validated", "rejected"]),
  feedback: z.string().min(0).max(2000),
});

export async function reviewBonusEventFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = bonusReviewSchema.safeParse({
    bonusEventId: formData.get("bonusEventId"),
    decision: formData.get("decision"),
    feedback: formData.get("feedback") ?? "",
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifie." };

  // RLS bonus_events_mentor_update gate via is_mentor() — defense-in-depth applicative aussi
  const { data: profile } = await supabase
    .from("profiles")
    .select("app_role")
    .eq("user_id", user.id)
    .maybeSingle();
  const role = profile?.app_role;
  if (role !== "mentor" && role !== "game_master") {
    return { ok: false, message: "Action reservee aux Mentors." };
  }

  const { error: updErr } = await supabase
    .from("bonus_events")
    .update({
      status: parsed.data.decision,
      reviewed_by: user.id,
      reviewed_at: new Date().toISOString(),
      feedback: parsed.data.feedback,
    })
    .eq("id", parsed.data.bonusEventId);
  if (updErr) return { ok: false, message: updErr.message };

  revalidatePath("/mentor");
  revalidatePath(`/mentor/bonus/${parsed.data.bonusEventId}`);
  revalidatePath("/journey");
  return {
    ok: true,
    message:
      parsed.data.decision === "validated"
        ? "Bonus valide. Le Player verra son badge Boost."
        : "Bonus rejete avec feedback.",
  };
}

// ============================================================================
// MoSCoW Kanban (T3X-EXPANSION wave 2 / plan 06 — D-04)
// Player CRUD ses cartes + reorder DnD batch + submit deliverable
// R3 preserve : pas de validation hard >= 2 MUST / >= 1 WONT (warn-only via rubric)
// ============================================================================

const moscowBucketEnum = z.enum(["must", "should", "could", "wont"]);

const moscowCreateSchema = z.object({
  deliverableTemplateId: z.string().uuid(),
  bucket: moscowBucketEnum,
  ord: z.coerce.number().int().min(0).max(999).default(0),
  feature: z.string().min(1).max(200),
  pourquoi: z.string().max(500).default(""),
  contrainte: z.string().max(200).default(""),
});

export async function createMoscowCardFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = moscowCreateSchema.safeParse({
    deliverableTemplateId: formData.get("deliverableTemplateId"),
    bucket: formData.get("bucket"),
    ord: formData.get("ord") ?? 0,
    feature: formData.get("feature"),
    pourquoi: formData.get("pourquoi") ?? "",
    contrainte: formData.get("contrainte") ?? "",
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifie." };

  const { data: membership } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (!membership) return { ok: false, message: "Aucun Player rattache." };

  const { error: insErr } = await supabase.from("moscow_cards").insert({
    project_id: membership.player_id,
    deliverable_template_id: parsed.data.deliverableTemplateId,
    bucket: parsed.data.bucket,
    ord: parsed.data.ord,
    feature: parsed.data.feature,
    pourquoi: parsed.data.pourquoi,
    contrainte: parsed.data.contrainte,
    created_by: user.id,
  });
  if (insErr) return { ok: false, message: insErr.message };

  revalidatePath(`/journey/deliverable/${parsed.data.deliverableTemplateId}`);
  return { ok: true, message: "Carte ajoutee." };
}

const moscowUpdateSchema = z.object({
  cardId: z.string().uuid(),
  bucket: moscowBucketEnum,
  ord: z.coerce.number().int().min(0).max(999),
  feature: z.string().min(1).max(200),
  pourquoi: z.string().max(500).default(""),
  contrainte: z.string().max(200).default(""),
});

export async function updateMoscowCardFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = moscowUpdateSchema.safeParse({
    cardId: formData.get("cardId"),
    bucket: formData.get("bucket"),
    ord: formData.get("ord") ?? 0,
    feature: formData.get("feature"),
    pourquoi: formData.get("pourquoi") ?? "",
    contrainte: formData.get("contrainte") ?? "",
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  const { error: updErr } = await supabase
    .from("moscow_cards")
    .update({
      bucket: parsed.data.bucket,
      ord: parsed.data.ord,
      feature: parsed.data.feature,
      pourquoi: parsed.data.pourquoi,
      contrainte: parsed.data.contrainte,
    })
    .eq("id", parsed.data.cardId);
  if (updErr) return { ok: false, message: updErr.message };

  revalidatePath("/journey");
  return { ok: true, message: "Carte mise a jour." };
}

const moscowDeleteSchema = z.object({
  cardId: z.string().uuid(),
});

export async function deleteMoscowCardFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = moscowDeleteSchema.safeParse({ cardId: formData.get("cardId") });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "ID invalide" };
  }

  const { error: delErr } = await supabase
    .from("moscow_cards")
    .delete()
    .eq("id", parsed.data.cardId);
  if (delErr) return { ok: false, message: delErr.message };

  revalidatePath("/journey");
  return { ok: true, message: "Carte supprimee." };
}

const moscowReorderItemSchema = z.object({
  id: z.string().uuid(),
  bucket: moscowBucketEnum,
  ord: z.coerce.number().int().min(0).max(999),
});

const moscowReorderSchema = z.object({
  deliverableTemplateId: z.string().uuid(),
  items: z.array(moscowReorderItemSchema).min(1).max(200),
});

export async function reorderMoscowCardsFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const rawItems = formData.get("items");
  let items: unknown;
  try {
    items = typeof rawItems === "string" ? JSON.parse(rawItems) : null;
  } catch {
    return { ok: false, message: "Items JSON invalide." };
  }

  const parsed = moscowReorderSchema.safeParse({
    deliverableTemplateId: formData.get("deliverableTemplateId"),
    items,
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  // Batch UPDATE via boucle (Supabase JS lib n'a pas de bulk update conditionnel propre)
  for (const it of parsed.data.items) {
    const { error: updErr } = await supabase
      .from("moscow_cards")
      .update({ bucket: it.bucket, ord: it.ord })
      .eq("id", it.id);
    if (updErr) return { ok: false, message: updErr.message };
  }

  revalidatePath(`/journey/deliverable/${parsed.data.deliverableTemplateId}`);
  return { ok: true, message: "Ordre Kanban sauvegarde." };
}

const moscowSubmitSchema = z.object({
  deliverableTemplateId: z.string().uuid(),
});

export async function submitMoscowDeliverableFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok: false, message: "Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok: false, message: "Backend non configure." };

  const parsed = moscowSubmitSchema.safeParse({
    deliverableTemplateId: formData.get("deliverableTemplateId"),
  });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };
  }

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifie." };

  const { data: membership } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (!membership) return { ok: false, message: "Aucun Player rattache." };

  // R3 warn-only : count >=2 MUST + >=1 WONT pour message d'avertissement (NOT blocage)
  const { data: cards } = await supabase
    .from("moscow_cards")
    .select("bucket")
    .eq("project_id", membership.player_id)
    .eq("deliverable_template_id", parsed.data.deliverableTemplateId);
  const counts = { must: 0, should: 0, could: 0, wont: 0 };
  for (const c of cards ?? []) counts[c.bucket as MoscowBucket]++;

  // Build snapshot URL (Plan 10 implements the route SSR viewer)
  const snapshotUrl = `https://entrepreneur-game-six.vercel.app/journey/deliverable/${parsed.data.deliverableTemplateId}/moscow-snapshot?p=${membership.player_id}`;

  // Reuse submitDeliverable internal logic — INSERT submission row directement
  const { error: insErr } = await supabase.from("submissions").insert({
    player_id: membership.player_id,
    deliverable_template_id: parsed.data.deliverableTemplateId,
    version: 1,
    kind: "proof_url",
    proof_url: snapshotUrl,
    status: "submitted_v1",
    submitted_by: user.id,
  });
  if (insErr) {
    // Si deja submitted_v1 (unique constraint), ne pas bloquer
    if (insErr.code === "23505") {
      return {
        ok: false,
        message: "Soumission V1 deja existante. Editez vos cartes puis attendez le feedback Mentor.",
      };
    }
    return { ok: false, message: insErr.message };
  }

  revalidatePath("/journey");
  revalidatePath(`/journey/deliverable/${parsed.data.deliverableTemplateId}`);

  // R2/R3 : warn-only message si recommandations non remplies
  const warns: string[] = [];
  if (counts.must < 2) warns.push("recommandation : >=2 cartes MUST");
  if (counts.wont < 1) warns.push("recommandation : >=1 carte WONT (anti scope-creep)");
  const warningSuffix = warns.length > 0 ? ` (${warns.join(" ; ")})` : "";

  return {
    ok: true,
    message: `Kanban MoSCoW soumis V1.${warningSuffix} Le Mentor va le valider.`,
  };
}
```

**IMPORTANT** :
- ASCII pur dans tous les `message` retournés (cf. CLAUDE.md "avoid accented characters in code-resident strings"). `validee` pas `validée`, `soumise` pas `soumise avec accent`. Note : `lib/i18n.ts` peut contenir des accents (consommé via React render), mais les messages dans `app/actions.ts` sont retournés via WorkflowState qui peut être loggé/affiché brut → garder ASCII.
- Importer `BONUS_DEFAULTS`, `BONUS_MULTIPLIER_CAP`, `BonusType`, `MoscowBucket` depuis `@/lib/types` en HAUT du fichier.
- Pas d'expose multiplier_factor dans les messages (R1).
- Pas de throw — tout retourne WorkflowState (R2).
- Pas de blocage si `counts.must < 2` — warn-only via suffix message (R3).
- Pas de `redirect()` côté action — laisser le client useEffect rediriger si besoin.

**NE PAS** :
- Modifier les actions existantes (submitDeliverable, evaluateSubmission, etc.).
- Stocker logique de score dans ces actions (Plan 07 fera lib/score.ts).
- Ajouter de la logique d'expiry multiplier (Plan 07 fera la consume side).
- Importer Realtime channels / push notifications (Phase 9 utilise reload-based).
- Faire un `select` complet du bonus_event après INSERT pour le retourner (les Player UI fait revalidatePath puis re-fetch via getJourneyData).

**Vérification post-edit** :
```powershell
npm run typecheck
# Attendu : 0 erreur

npm run lint
# Attendu : 0 erreur

(Select-String -Pattern "claimBonusEventFlow|reviewBonusEventFlow|createMoscowCardFlow|updateMoscowCardFlow|deleteMoscowCardFlow|reorderMoscowCardsFlow|submitMoscowDeliverableFlow" app/actions.ts).Count
# Attendu : >= 7 (chaque action declaree au moins 1x via export async function)
```
  </action>
  <acceptance_criteria>
    <automated>
      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "export async function claimBonusEventFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function reviewBonusEventFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function createMoscowCardFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function updateMoscowCardFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function deleteMoscowCardFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function reorderMoscowCardsFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export async function submitMoscowDeliverableFlow" app/actions.ts).Count
      # Attendu : 1

      (Select-String -Pattern "BONUS_DEFAULTS\[" app/actions.ts).Count
      # Attendu : >= 1

      Select-String -Pattern "throw new Error|throw " app/actions.ts | Select-Object -First 5
      # Attendu : 0 hit dans les NOUVELLES actions (R2 — pas de throw)
      # (les actions existantes ne sont pas notre scope, ne pas s'inquiéter)
    </automated>
  </acceptance_criteria>
  <done>
- 7 server actions créées
- Imports `BONUS_DEFAULTS`, `BONUS_MULTIPLIER_CAP`, `BonusType`, `MoscowBucket` ajoutés
- Toutes les actions : zod safeParse + hasSupabaseEnv + ownership check + RLS handle + revalidatePath
- ASCII pur dans messages
- R1/R2/R3 préservés
- npm typecheck + lint clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Ajouter 12+ keys i18n FR dans lib/i18n.ts pour bonus + moscow workflows</name>
  <files>lib/i18n.ts</files>
  <read_first>
    - `lib/i18n.ts` (lignes 1-80 pour pattern et localiser une section appropriée)
  </read_first>
  <action>
**Stratégie** : ajouter une section dédiée "Bonus events / MoSCoW Kanban (T3X-EXPANSION)" dans le dictionnaire `fr` (et optionnellement `en`).

**Localisation** : insérer avant la ligne `} as const;` qui termine le `dictionaries.fr` block (vers ligne ~990 avant la fin du fr dict). Si le fichier n'a qu'un seul locale `fr`, insérer en bas de ce dict.

**Keys minimum à ajouter dans `fr`** :

```typescript
    // ---- Bonus events (T3X-EXPANSION) ---------------------------------------
    bonus_section_title: "Bonus optionnels",
    bonus_section_subtitle: "Activez un boost en livrant une preuve terrain ou un artefact",
    bonus_card_verbatims_terrain: "3 verbatims terrain agriculteurs",
    bonus_card_dev_plan: "Plan de developpement (roadmap technique)",
    bonus_card_prototype_draft: "Prototype draft (croquis / wireframe / photo)",
    bonus_claim_url_label: "Lien HTTPS de la preuve",
    bonus_claim_url_placeholder: "https://...",
    bonus_claim_title_label: "Titre du bonus",
    bonus_claim_description_label: "Description (optionnelle)",
    bonus_claim_submit: "Soumettre le bonus",
    bonus_claim_submitting: "Soumission en cours...",
    bonus_claim_success: "Bonus soumis. Le Mentor va le valider.",
    bonus_claim_pending: "Bonus deja soumis, en attente de validation Mentor.",
    bonus_status_submitted: "En attente de validation",
    bonus_status_validated: "Boost actif",
    bonus_status_rejected: "Bonus rejete (voir feedback)",
    bonus_review_title: "Reviewer un bonus",
    bonus_review_validate: "Valider le bonus",
    bonus_review_reject: "Rejeter le bonus",
    bonus_review_feedback_label: "Feedback Mentor",
    bonus_review_success_validated: "Bonus valide. Le Player verra son badge Boost.",
    bonus_review_success_rejected: "Bonus rejete avec feedback.",

    // ---- MoSCoW Kanban (T3X-EXPANSION) --------------------------------------
    moscow_kanban_title: "Kanban MoSCoW",
    moscow_kanban_subtitle: "Glissez les cartes entre les colonnes pour prioriser",
    moscow_bucket_must: "Must",
    moscow_bucket_should: "Should",
    moscow_bucket_could: "Could",
    moscow_bucket_wont: "Won't",
    moscow_card_add: "Ajouter une carte",
    moscow_card_feature_label: "Fonctionnalite",
    moscow_card_pourquoi_label: "Pourquoi cette priorite ?",
    moscow_card_contrainte_label: "Contrainte terrain levee (Must/Should)",
    moscow_card_save: "Enregistrer",
    moscow_card_delete: "Supprimer",
    moscow_card_created: "Carte ajoutee.",
    moscow_card_updated: "Carte mise a jour.",
    moscow_card_deleted: "Carte supprimee.",
    moscow_reorder_saved: "Ordre Kanban sauvegarde.",
    moscow_submit_deliverable: "Soumettre mon Kanban (livrable)",
    moscow_submit_warn_must: "Recommandation : >=2 cartes MUST.",
    moscow_submit_warn_wont: "Recommandation : >=1 carte WONT (anti scope-creep).",
    moscow_submit_success: "Kanban MoSCoW soumis V1.",
```

**IMPORTANT** :
- Les VALUES (côté droit) en FR PEUVENT contenir des accents (cf. lib/i18n.ts existant qui utilise `connexion en cours…`, `Vérifiez vos identifiants`). MAIS les keys (côté gauche) restent ASCII pur snake_case. Ce plan recommande ASCII pur dans les valeurs aussi (cohérent avec convention "current files use plain ASCII e.g. 'XP confirme'") car certaines parties du code peuvent les passer dans des mailtos ou WorkflowState.message brut.
- **Choix retenu** : VALUES ASCII pur. Pas d'accents dans les chaînes. Cela simplifie l'audit et reste lisible.

**NE PAS** :
- Modifier les keys existantes.
- Ajouter une 2e locale `en` complète si elle n'existe pas déjà ; ajouter UNIQUEMENT FR (cf. CLAUDE.md "French is the primary product language").
- Importer quoi que ce soit (lib/i18n.ts est purement déclaratif).

**Vérification** :
```powershell
(Select-String -Pattern "bonus_claim_|bonus_status_|bonus_review_|moscow_" lib/i18n.ts).Count
# Attendu : >= 30 (12 bonus + 18 moscow keys minimum)

npm run typecheck
# Attendu : 0 erreur (les `as const` types s'élargissent automatiquement)
```
  </action>
  <acceptance_criteria>
    <automated>
      (Select-String -Pattern "bonus_claim_url_label" lib/i18n.ts).Count
      # Attendu : 1

      (Select-String -Pattern "bonus_status_validated" lib/i18n.ts).Count
      # Attendu : 1

      (Select-String -Pattern "moscow_bucket_must" lib/i18n.ts).Count
      # Attendu : 1

      (Select-String -Pattern "moscow_card_created" lib/i18n.ts).Count
      # Attendu : 1

      (Select-String -Pattern "bonus_|moscow_" lib/i18n.ts).Count
      # Attendu : >= 30

      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0
    </automated>
  </acceptance_criteria>
  <done>
- 30+ keys i18n FR ajoutées (bonus + moscow)
- VALUES ASCII pur
- Keys snake_case ASCII pur
- npm checks clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 3: Audit R1/R2/R3 manuel (fallback eic-pedagogical-advisor) + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --cached app/actions.ts` (focus sur les 7 nouvelles actions)
    - `git diff --cached lib/i18n.ts`
    - `.claude/agents/eic-pedagogical-advisor.md` (3 cardinal rules R1/R2/R3)
  </read_first>
  <action>
**Stratégie audit** : étant donné la sandbox-limitation pour spawn `eic-pedagogical-advisor` (cf. STATE.md decision Phase 4), Omar fait l'audit manuel inline en suivant la checklist R1/R2/R3 ci-dessous.

**Pré-checkpoint (auto par Claude)** :
- Tasks 1+2 complétées
- npm typecheck + lint clean

**Action Claude post-approval** :
```bash
git add app/actions.ts lib/i18n.ts
git commit -m "$(cat <<'EOF'
feat(t3x-actions): add 7 server actions for bonus_events + moscow_cards (D-02 D-03 D-04)

Wave 2 / plan 06 : implementation server-side complete pour bonus events et
MoSCoW Kanban. Pattern aligne sur les actions existantes (zod safeParse,
WorkflowState return, hasSupabaseEnv check, ownership defense-in-depth, RLS
handle, revalidatePath).

- claimBonusEventFlow : Player INSERT bonus_events status='submitted',
  multiplier_factor copy depuis BONUS_DEFAULTS[type] cape a BONUS_MULTIPLIER_CAP=3.0
- reviewBonusEventFlow : Mentor UPDATE status='validated'|'rejected' + feedback
- createMoscowCardFlow / updateMoscowCardFlow / deleteMoscowCardFlow : CRUD carte
- reorderMoscowCardsFlow : batch UPDATE post-DnD (JSON serialized {id, bucket, ord}[])
- submitMoscowDeliverableFlow : INSERT submission row kind='proof_url' avec URL
  /journey/deliverable/:id/moscow-snapshot (route Plan 10) ; warn suffix R2/R3
  si <2 MUST ou <1 WONT (jamais blocage)

R1 preserve : aucune action n'expose multiplier_factor / score / rank dans
WorkflowState.message. R2 preserve : zod safeParse + return ok:false sans throw,
warn-only suffix dans submitMoscowDeliverableFlow. R3 preserve : pas de blocage
inter-mission, re-claim apres rejet autorise.

+ 30+ keys i18n FR ASCII pur (bonus_*, moscow_*) pour les UI Plans 08-10.

npm typecheck OK, npm lint OK.

Refs : BRIEF §2.A (bonus claim/review), §2.B (MoSCoW CRUD + reorder + submit),
CLAUDE.md cardinal rules R1/R2/R3.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-actions"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 2 fichiers (app/actions.ts + lib/i18n.ts)

      Select-String -Pattern "throw new" app/actions.ts | Where-Object { $_.LineNumber -gt 1230 }
      # Attendu : 0 hit dans les NOUVELLES actions (lignes > 1230, R2)

      Select-String -Pattern "multiplier_factor.*\$\{|multiplierFactor.*\$\{" app/actions.ts
      # Attendu : 0 hit (R1 — aucun message exposant le chiffre)
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → Claude commit. Sinon : décrire issues (modifier zod schema, ajouter ownership check, modifier message FR).</done>
  <what-built>
- 7 server actions complètes
- 30+ keys i18n
- R1/R2/R3 préservés
- Tests typecheck + lint clean
  </what-built>
  <how-to-verify>
**Audit checklist R1/R2/R3 manuel pour Omar** (10 minutes) :

1. **R1 — Score INVISIBLE Player** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "multiplier_factor|score|rank|/100|/140|toFixed|points"
   ```
   Attendu : matches uniquement dans le DDL (INSERT row), JAMAIS dans un `return { ok, message: ... }` ou un `template literal`. Vérifier ligne par ligne. Le mot "Boost" dans le message i18n = qualitatif, OK.

2. **R2 — Validators warn-only** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "throw|raise"
   ```
   Attendu : 0 hit dans les nouvelles actions. Toutes les erreurs zod retournent via `{ok: false, message}`. La seule "validation hard" est le DDL CHECK (DB-side, légitime invariant) — pas dans le code TS.

   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "submitMoscowDeliverableFlow" -Context 0,40
   ```
   Vérifier que `submitMoscowDeliverableFlow` retourne `ok: true` même si `counts.must < 2` (warn-only suffix dans message, pas blocage).

3. **R3 — No inter-mission blocking** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "blocks_progression|disabled.*because|status.*draft.*reject"
   ```
   Attendu : 0 hit. Aucune action ne refuse une INSERT/UPDATE selon l'état d'un AUTRE livrable (les actions sont indépendantes).

4. **Ownership check defense-in-depth** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "player_members|is_my_player|app_role" -Context 0,3
   ```
   Vérifier que :
   - claimBonusEventFlow check `player_members.user_id = auth.uid()`
   - reviewBonusEventFlow check `profile.app_role IN ('mentor', 'game_master')`
   - createMoscowCardFlow / updateMoscowCardFlow check ownership
   (Defense-in-depth : RLS DDL fait déjà le gate, mais on duplique au niveau application pour message FR clair.)

5. **revalidatePath cohérent** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "revalidatePath"
   ```
   Vérifier au moins 1 revalidate par action (`/journey`, `/mentor`, `/journey/deliverable/...`).

6. **httpsUrl reused** :
   ```powershell
   git diff --cached app/actions.ts | Select-String -Pattern "httpsUrl"
   ```
   Attendu : >= 1 hit (claimBonusEventFlow réutilise le shared schema).

7. **i18n ASCII pur** :
   ```powershell
   git diff --cached lib/i18n.ts | Select-String -Pattern "[^\x00-\x7F]"
   ```
   Attendu : 0 hit dans les nouvelles values (ou seulement caractères ASCII étendus déjà acceptés comme `'`).

8. **Keys snake_case** :
   ```powershell
   git diff --cached lib/i18n.ts | Select-String -Pattern "^\+\s+[A-Z]"
   ```
   Attendu : 0 hit (pas de keys CamelCase nouvelles).

**Si tout OK** → tape `approved`. Claude execute le commit.

**Si problème** → décrire (ex: "renommer claimBonusEventFlow en submitBonusEventFlow", "supprimer warn suffix moscow_submit", "ownership check trop strict") et Claude itère.
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (avec ligne précise du diff si possible).</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **Type safety** :
   ```powershell
   npm run typecheck
   # Attendu : exit 0
   ```

2. **Lint** :
   ```powershell
   npm run lint
   # Attendu : exit 0
   ```

3. **R1 final audit** :
   ```powershell
   git diff HEAD~1 HEAD app/actions.ts | Select-String -Pattern "multiplier_factor.*\$|toFixed|score.*\$"
   # Attendu : 0 hit dans les nouveaux template literals
   ```

4. **R2 final audit** :
   ```powershell
   git diff HEAD~1 HEAD app/actions.ts | Select-String -Pattern "^\+.*throw"
   # Attendu : 0 hit dans les ajouts
   ```
</verification>

<success_criteria>
- [ ] 7 server actions exportées
- [ ] 30+ keys i18n FR ajoutées
- [ ] Imports BONUS_DEFAULTS / BONUS_MULTIPLIER_CAP / BonusType / MoscowBucket
- [ ] Toutes actions : zod + hasSupabaseEnv + ownership + RLS + revalidatePath
- [ ] R1/R2/R3 préservés (audit checklist 8 points OK)
- [ ] npm typecheck + lint clean
- [ ] Commit atomique conforme
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-06-SUMMARY.md` :

Sections :
1. **7 actions exposées** : nom, signature, scope CRUD
2. **R1/R2/R3 audit** : 8 points checklist passés
3. **i18n keys** : 30+ added FR ASCII pur
4. **httpsUrl reused** : claimBonusEventFlow utilise shared schema
5. **Ownership defense-in-depth** : checks applicatifs en plus de RLS
6. **submitMoscowDeliverableFlow snapshot URL** : pointe vers route SSR Plan 10 (à implémenter)
7. **revalidatePath strategy** : /journey, /mentor, /journey/deliverable/:id
8. **Files modified** : 2 (app/actions.ts + lib/i18n.ts)
9. **Next consumers** : Plan 07 (lib/score.ts utilisera les bonus_events validated rows), Plans 08-10 (UI consume actions via useActionState)
</output>
