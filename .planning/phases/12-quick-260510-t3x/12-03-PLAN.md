---
phase: "12-quick-260510-t3x"
plan: "12-03"
type: execute
wave: 1
depends_on:
  - "12-01"
files_modified:
  - supabase/migrations/20260510170100_moscow_cards.sql
  - database/moscow_cards.sql
autonomous: false
requirements:
  - T3X-EXPANSION-MOSCOW-CARDS-SCHEMA
must_haves:
  truths:
    - "Table public.moscow_cards existe avec colonnes id, project_id (uuid -> players.id), deliverable_template_id (uuid -> deliverable_templates.id), bucket (enum moscow_bucket), ord (smallint), feature, pourquoi, contrainte, created_by, created_at, updated_at"
    - "Enum public.moscow_bucket contient exactement 4 valeurs : must, should, could, wont"
    - "RLS moscow_cards_select : SELECT autorisé pour membres du player (is_my_player) OU mentor (is_mentor)"
    - "RLS moscow_cards_player_insert : INSERT autorisé pour membres du player ET created_by = auth.uid()"
    - "RLS moscow_cards_player_update : UPDATE autorisé pour membres du player (re-bucket, edit feature, reorder via ord)"
    - "RLS moscow_cards_player_delete : DELETE autorisé pour membres du player (suppression carte)"
    - "RLS moscow_cards_gm_all : GM full access (admin debug)"
    - "Trigger trg_moscow_cards_updated_at maintient updated_at"
    - "Re-application migration idempotente (enum DO block + IF NOT EXISTS + DROP+CREATE policies)"
    - "Schema preserve R1 : aucune colonne score/rank stockée dans moscow_cards (purement contenu pédagogique)"
    - "Mirror parfait `database/moscow_cards.sql` <-> `supabase/migrations/20260510170100_moscow_cards.sql`"
    - "Index unique partial sur (project_id, deliverable_template_id, bucket, ord) où ord IS NOT NULL — garantit pas de doublon de position dans une même colonne pour un même livrable"
  artifacts:
    - path: "supabase/migrations/20260510170100_moscow_cards.sql"
      provides: "Migration Supabase appliquable : enum moscow_bucket + table moscow_cards + RLS + trigger"
      contains: "create table.*moscow_cards|create policy.*moscow_cards|moscow_bucket"
    - path: "database/moscow_cards.sql"
      provides: "Mirror local pour psql -f apply"
      contains: "create table.*moscow_cards|moscow_bucket"
  key_links:
    - from: "public.moscow_cards.project_id"
      to: "public.players.id"
      via: "FK on delete cascade"
      pattern: "references public\\.players\\(id\\) on delete cascade"
    - from: "public.moscow_cards.deliverable_template_id"
      to: "public.deliverable_templates.id"
      via: "FK on delete cascade — quand le template est supprimé, ses cartes le sont aussi"
      pattern: "references public\\.deliverable_templates\\(id\\) on delete cascade"
    - from: "RLS moscow_cards_select / insert / update / delete"
      to: "public.is_my_player + public.is_mentor + public.is_game_master"
      via: "réutilise les helpers de database/rls.sql"
      pattern: "is_my_player|is_mentor|is_game_master"
---

<objective>
Wave 1 — Créer le schema `moscow_cards` pour la persistance du Kanban MoSCoW (livrable #4 `fiche-produit-plan-dev-v1` après B4 reaffectation, cf. seed_event_hackdays.sql section 5.4).

**Architecture** : 1 row = 1 carte dans le Kanban. La position d'une carte est définie par `(bucket, ord)` au sein du couple `(project_id, deliverable_template_id)`. Le DnD update mute simplement `bucket` et/ou `ord` via `reorderMoscowCardsFlow` (Plan 06).

**Décision D-04** : DnD library = `@dnd-kit/core`. Cela ne touche PAS le schema (purement client-side), mais le schema doit supporter les opérations CRUD + reorder atomique.

**Submission deliverable MoSCoW** : la "soumission" du livrable #4 ne génère PAS un fichier upload (R6 link-based). Plan 06 fournira `submitMoscowDeliverableFlow` qui crée une `submissions` row avec `kind='proof_url'` pointant vers une route SSR `/journey/deliverable/{id}/moscow-snapshot` (Plan 10) qui sérialise les cartes en JSON viewer. Le schema `moscow_cards` ici est donc le SOURCE de vérité — la submission consomme via JOIN au moment du snapshot.

**Scope plan** : SQL DDL pure. PAS de TypeScript (Plan 05). PAS de server actions (Plan 06). PAS d'UI (Plan 09).

**Output** :
- 1 migration Supabase appliquable
- 1 mirror local
- Idempotency garantie
- 0 seed (cartes naissent du runtime Player)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/BRIEF.md
@database/schema.sql
@database/triggers.sql
@database/rls.sql
@database/seed_event_hackdays.sql

<interfaces>
<!-- Patterns à reproduire (déjà étudiés en Plan 02) -->

From database/schema.sql (table conventions) :
- uuid PK avec gen_random_uuid()
- timestamptz created_at + updated_at avec defaults now()
- FK explicite + ON DELETE policy
- text NOT NULL DEFAULT ''
- smallint NOT NULL DEFAULT 0 pour ord
- unique (col1, col2) compound

From database/seed_event_hackdays.sql section 5.4 (livrable #4 = MoSCoW prototype) :
- slug = 'fiche-produit-plan-dev-v1'
- title = 'MoSCoW prototype (format Kanban link)'
- 4 colonnes Must/Should/Could/Won't
- Recommandations >= 2 MUST, >= 1 WONT (warn-only — c'est dans la rubric, PAS dans le schema)

From database/rls.sql helpers (réutilisation) :
- public.is_my_player(p_player_id uuid) returns boolean
- public.is_mentor() returns boolean
- public.is_game_master() returns boolean

Pattern enum idempotent (Plan 02) :
```sql
do $$ begin
  create type public.moscow_bucket as enum ('must','should','could','wont');
exception when duplicate_object then null;
end $$;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer supabase/migrations/20260510170100_moscow_cards.sql avec enum + table + indexes + RLS + trigger</name>
  <files>supabase/migrations/20260510170100_moscow_cards.sql</files>
  <read_first>
    - `database/schema.sql` (lignes 99-113 deliverable_templates pour pattern FK + lignes 156-165 player_members)
    - `database/triggers.sql` (lignes 1-50 set_updated_at)
    - `database/rls.sql` (helpers + grants)
    - `supabase/migrations/20260510170000_bonus_events_recreate.sql` (pattern idempotent Plan 02 — modèle structurel)
  </read_first>
  <action>
**Créer le fichier `supabase/migrations/20260510170100_moscow_cards.sql`** avec EXACTEMENT le contenu suivant :

```sql
-- =============================================================================
-- 20260510170100_moscow_cards.sql
-- T3X-EXPANSION wave 1 / plan 03 — MoSCoW Kanban native persistence.
-- Livrable #4 'fiche-produit-plan-dev-v1' (cf seed_event_hackdays.sql section 5.4).
-- 1 row = 1 carte. Position = (bucket, ord) au sein de (project_id, deliverable_template_id).
-- DnD client = @dnd-kit/core (D-04, plan 09). Server side = simple CRUD + reorder.
--
-- Apply order :
--   schema.sql -> triggers.sql -> rls.sql -> seed_event_hackdays.sql ->
--   ... -> 20260510170000_bonus_events_recreate.sql ->
--   20260510170100_moscow_cards.sql (CETTE migration).
--
-- Idempotent : re-application = no-op.
-- =============================================================================

-- -----------------------------------------------------------------------------
-- 1. Enum moscow_bucket (mirror lib/types.ts MoscowBucket — Plan 05)
-- -----------------------------------------------------------------------------

do $$ begin
  create type public.moscow_bucket as enum (
    'must',
    'should',
    'could',
    'wont'
  );
exception when duplicate_object then null;
end $$;

-- -----------------------------------------------------------------------------
-- 2. Table public.moscow_cards
-- -----------------------------------------------------------------------------

create table if not exists public.moscow_cards (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references public.players(id) on delete cascade,
  deliverable_template_id uuid not null references public.deliverable_templates(id) on delete cascade,
  bucket public.moscow_bucket not null,
  ord smallint not null default 0,
  feature text not null check (length(trim(feature)) >= 1 and length(feature) <= 200),
  pourquoi text not null default '' check (length(pourquoi) <= 500),
  contrainte text not null default '' check (length(contrainte) <= 200),
  created_by uuid references auth.users(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

comment on table public.moscow_cards is
  'Cartes Kanban MoSCoW pour le livrable #4. R3 : pas de blocage cross-mission, R2 : recommandations rubric warn-only (>=2 MUST, >=1 WONT) cote application, pas SQL.';

comment on column public.moscow_cards.bucket is
  'Colonne Kanban : must / should / could / wont. Mute via DnD client + reorderMoscowCardsFlow (plan 06).';

comment on column public.moscow_cards.ord is
  'Ordre dans la colonne bucket (0..N). Reorder via DnD persiste via UPDATE ord. Pas d''unique (project, deliverable, bucket, ord) car le swap-pair update est non-atomic en RLS.';

comment on column public.moscow_cards.contrainte is
  'Contrainte terrain levee (energie / maintenance / litteratie / connectivite / cout-ha / climat / ONSSA-ORMVA). Recommande pour MUST et SHOULD (rubric, warn-only).';

-- -----------------------------------------------------------------------------
-- 3. Indexes
-- -----------------------------------------------------------------------------

create index if not exists moscow_cards_project_idx on public.moscow_cards (project_id);
create index if not exists moscow_cards_deliverable_idx on public.moscow_cards (deliverable_template_id);
create index if not exists moscow_cards_kanban_view_idx
  on public.moscow_cards (project_id, deliverable_template_id, bucket, ord);

-- -----------------------------------------------------------------------------
-- 4. Trigger updated_at (reuse public.set_updated_at)
-- -----------------------------------------------------------------------------

drop trigger if exists trg_moscow_cards_updated_at on public.moscow_cards;
create trigger trg_moscow_cards_updated_at
  before update on public.moscow_cards
  for each row execute function public.set_updated_at();

-- -----------------------------------------------------------------------------
-- 5. RLS — Player CRUD ses propres cartes, Mentor lecture, GM full
-- -----------------------------------------------------------------------------

alter table public.moscow_cards enable row level security;

drop policy if exists "moscow_cards_select" on public.moscow_cards;
create policy "moscow_cards_select" on public.moscow_cards
  for select to authenticated
  using (public.is_my_player(project_id) or public.is_mentor());

drop policy if exists "moscow_cards_player_insert" on public.moscow_cards;
create policy "moscow_cards_player_insert" on public.moscow_cards
  for insert to authenticated
  with check (
    (public.is_my_player(project_id) and created_by = auth.uid())
    or public.is_game_master()
  );

drop policy if exists "moscow_cards_player_update" on public.moscow_cards;
create policy "moscow_cards_player_update" on public.moscow_cards
  for update to authenticated
  using (public.is_my_player(project_id) or public.is_game_master())
  with check (public.is_my_player(project_id) or public.is_game_master());

drop policy if exists "moscow_cards_player_delete" on public.moscow_cards;
create policy "moscow_cards_player_delete" on public.moscow_cards
  for delete to authenticated
  using (public.is_my_player(project_id) or public.is_game_master());

-- Grants explicites
grant select, insert, update, delete on public.moscow_cards to authenticated;
grant select, insert, update, delete on public.moscow_cards to service_role;

-- =============================================================================
-- End of 20260510170100_moscow_cards.sql
-- =============================================================================
```

**Conventions à respecter** :
- ASCII pur (cf. CLAUDE.md). Pas de diacritiques dans les commentaires SQL embedded strings.
- `feature` CHECK length 1..200 (anti carte vide, anti spam).
- `pourquoi` et `contrainte` peuvent être vides (warn-only côté rubric, pas bloquant DDL — R2 préservé).
- PAS d'unique constraint (project_id, deliverable_template_id, bucket, ord) — le swap-pair update DnD nécessiterait des transactions atomiques complexes pour échanger 2 ord ; on tolère des doublons d'ord (UI gère le ré-ordering visuel via tri stable) et `reorderMoscowCardsFlow` (Plan 06) fera un UPDATE batch idempotent.
- FK `on delete cascade` sur project_id ET deliverable_template_id (suppression naturelle).
- FK `on delete set null` sur created_by (auth.users disparaît sans casser cartes).
- 4 RLS policies (select / insert / update / delete) — Player CRUD complet sur ses cartes.

**NE PAS** :
- Stocker score/rank/note (R1).
- Bloquer en DDL la submission si <2 MUST (R2 + R3).
- Forcer `pourquoi` ou `contrainte` non vides (warn-only côté rubric Plan 06 zod).
- Modifier database/schema.sql, triggers.sql, rls.sql (freeze v0.3 SEED-001).
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path supabase/migrations/20260510170100_moscow_cards.sql
      # Attendu : True

      (Select-String -Pattern "create type public\.moscow_bucket" supabase/migrations/20260510170100_moscow_cards.sql).Count
      # Attendu : 1

      (Select-String -Pattern "'must'|'should'|'could'|'wont'" supabase/migrations/20260510170100_moscow_cards.sql).Count
      # Attendu : 4 minimum (dans l'enum)

      (Select-String -Pattern "create table if not exists public\.moscow_cards" supabase/migrations/20260510170100_moscow_cards.sql).Count
      # Attendu : 1

      (Select-String -Pattern "create policy.*moscow_cards" supabase/migrations/20260510170100_moscow_cards.sql).Count
      # Attendu : 4

      (Select-String -Pattern "deliverable_template_id uuid not null references public\.deliverable_templates" supabase/migrations/20260510170100_moscow_cards.sql).Count
      # Attendu : 1

      Select-String -Pattern "/140|percentile|rank|score" supabase/migrations/20260510170100_moscow_cards.sql
      # Attendu : 0 hit (R1 — aucun score stocké)
    </automated>
  </acceptance_criteria>
  <done>
- Fichier `supabase/migrations/20260510170100_moscow_cards.sql` créé
- 1 enum (moscow_bucket : must/should/could/wont)
- 1 table moscow_cards avec FK + CHECK + indexes
- 4 RLS (select, player_insert, player_update, player_delete)
- 1 trigger updated_at
- Idempotent
- ASCII pur
- R1 : 0 colonne score
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror local database/moscow_cards.sql</name>
  <files>database/moscow_cards.sql</files>
  <read_first>
    - `supabase/migrations/20260510170100_moscow_cards.sql` (créé en Task 1)
  </read_first>
  <action>
**Mirror parfait** (même stratégie que Plan 02 Task 2) :

```powershell
Copy-Item supabase/migrations/20260510170100_moscow_cards.sql database/moscow_cards.sql
```

Modifier UNIQUEMENT l'en-tête de `database/moscow_cards.sql` (les 11 premières lignes) :

```sql
-- =============================================================================
-- moscow_cards.sql
-- T3X-EXPANSION wave 1 / plan 03 — Mirror local de la migration Supabase
-- supabase/migrations/20260510170100_moscow_cards.sql.
--
-- Apply via :
--   psql $SUPABASE_DB_URL -f database/moscow_cards.sql
-- (Equivalent a `supabase migration up` qui applique la migration mirror.)
--
-- Le contenu DDL doit rester byte-identique au fichier supabase/migrations/.
-- Sinon : drift detection => regenerer via Copy-Item + edit du seul header.
-- =============================================================================
```

Le reste (à partir du bloc `-- 1. Enum moscow_bucket`) doit rester strictement identique.
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path database/moscow_cards.sql
      # Attendu : True

      $supa = Get-Content supabase/migrations/20260510170100_moscow_cards.sql | Select-Object -Skip 11
      $mirror = Get-Content database/moscow_cards.sql | Select-Object -Skip 11
      Compare-Object $supa $mirror
      # Attendu : output vide
    </automated>
  </acceptance_criteria>
  <done>
- Mirror parfait `database/moscow_cards.sql` (au-delà des 11 premières lignes header)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint Omar — review schema moscow_cards + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --stat`
    - `supabase/migrations/20260510170100_moscow_cards.sql`
    - `database/moscow_cards.sql`
  </read_first>
  <action>
**Pré-checkpoint** : Tasks 1+2 ont créé les 2 fichiers, mirror parfait vérifié.

**Action Claude post-approval** :
```bash
git add supabase/migrations/20260510170100_moscow_cards.sql database/moscow_cards.sql
git commit -m "$(cat <<'EOF'
feat(t3x-moscow): add moscow_cards schema for Kanban native persistence (D-04)

Wave 1 / plan 03 : table moscow_cards pour livrable #4 'fiche-produit-plan-dev-v1'.
1 row = 1 carte. Position = (bucket, ord) dans (project_id, deliverable_template_id).
D-04 : DnD client = @dnd-kit/core (plan 09). Server = CRUD + reorder.

- Enum moscow_bucket : must / should / could / wont
- Table moscow_cards : project_id (cascade), deliverable_template_id (cascade),
  bucket, ord smallint, feature text 1..200 CHECK, pourquoi text <=500,
  contrainte text <=200, created_by FK auth.users SET NULL
- 3 indexes : project_id, deliverable_template_id, kanban view (project, deliverable, bucket, ord)
- 4 RLS : select (player_or_mentor), player_insert (own player), player_update,
  player_delete (Player CRUD complet sur ses cartes)
- Trigger updated_at via public.set_updated_at
- Idempotent (DO block enum, IF NOT EXISTS, DROP+CREATE policies)
- R1 OK : 0 colonne score/rank stockée
- R2 OK : pourquoi/contrainte vides autorisés DDL (warn-only via rubric Plan 06)
- R3 OK : aucun blocking inter-mission via trigger

Refs : BRIEF §2.B (MoSCoW Kanban web natif), seed section 5.4 (livrable #4).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-moscow"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 2 fichiers

      Select-String -Pattern "score|rank|/140|percentile" database/moscow_cards.sql, supabase/migrations/20260510170100_moscow_cards.sql
      # Attendu : 0 hit
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → Claude commit. Sinon : décrire issues (renommer enum, ajouter colonne, modifier RLS, supprimer CHECK length).</done>
  <what-built>
- Schema `moscow_cards` créé : table + 1 enum + 4 RLS + 1 trigger + 3 indexes
- DnD-friendly (ord smallint, swap simple via UPDATE)
- R1/R2/R3 préservés
- Mirror local préservé
  </what-built>
  <how-to-verify>
**Procédure 5 étapes** :

1. **Schema cohérence** :
   ```powershell
   git diff --cached supabase/migrations/20260510170100_moscow_cards.sql | Select-Object -First 150
   ```
   Vérifier 11 colonnes : id, project_id, deliverable_template_id, bucket, ord, feature, pourquoi, contrainte, created_by, created_at, updated_at.

2. **Audit R1 final** :
   ```powershell
   Select-String -Pattern "score|rank|/140|/100|points" database/moscow_cards.sql, supabase/migrations/20260510170100_moscow_cards.sql
   ```
   Attendu : 0 hit (le mot "score" ne doit même pas apparaître dans les commentaires).

3. **Audit R2 (warn-only)** :
   - `feature` length CHECK 1..200 = invariant DDL légitime (carte vide = bug, pas warn).
   - `pourquoi` peut être vide (warn-only côté rubric Plan 06).
   - `contrainte` peut être vide (warn-only).
   - Aucun trigger qui rejette une INSERT/UPDATE basé sur >=2 MUST ou >=1 WONT (recommendations vivent dans la rubric, pas la DB).

4. **Audit R3** :
   - Aucun trigger qui bloque les autres tables (submissions, evaluations) selon moscow_cards.
   - DELETE cascade safe (suppression deliverable_template = suppression cartes liées).

5. **Mirror parfait** :
   ```powershell
   $supa = Get-Content supabase/migrations/20260510170100_moscow_cards.sql | Select-Object -Skip 11
   $mirror = Get-Content database/moscow_cards.sql | Select-Object -Skip 11
   Compare-Object $supa $mirror
   ```
   Attendu : output vide.

**Apply manuel post-commit** (Omar, hors checkpoint) :
```bash
supabase migration up
# OU
psql $SUPABASE_DB_URL -f database/moscow_cards.sql
psql $SUPABASE_DB_URL -f database/moscow_cards.sql  # idempotent re-run
```
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "renommer wont en will_not", "ajouter color column", "FK on delete restrict au lieu de cascade").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification (post-task 3)** :

1. **Files committed** :
   ```bash
   git log -1 --name-only
   # Attendu : 2 fichiers
   ```

2. **DDL completeness** :
   ```powershell
   Select-String -Pattern "create table|create policy|create index|create trigger|create type" supabase/migrations/20260510170100_moscow_cards.sql
   # Attendu : 1 table + 4 policies + 3 indexes + 1 trigger + 1 type = 10 hits
   ```

3. **R1/R2/R3 final audit** :
   ```powershell
   Select-String -Pattern "score|rank|blocks_progression|raise exception" database/moscow_cards.sql, supabase/migrations/20260510170100_moscow_cards.sql
   # Attendu : 0 hit
   ```
</verification>

<success_criteria>
- [ ] Schema `moscow_cards` créé via migration Supabase + mirror
- [ ] Enum moscow_bucket (must/should/could/wont)
- [ ] Table avec FK cascade + CHECK feature length
- [ ] 4 RLS Player CRUD + GM full
- [ ] Trigger updated_at
- [ ] 3 indexes (project, deliverable, kanban view)
- [ ] Mirror parfait
- [ ] R1/R2/R3 préservés
- [ ] Commit atomique
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-03-SUMMARY.md` :

Sections :
1. **Schema moscow_cards** : 11 colonnes + enum + 4 RLS + 3 indexes + trigger
2. **DnD-friendly design** : ord smallint mutable, swap simple via UPDATE
3. **R1/R2/R3 preservation** : 0 score, warn-only via rubric (pas DDL), no blocking
4. **Idempotency** : DO block enum, IF NOT EXISTS, DROP+CREATE policies
5. **Mirror SQL** : database/moscow_cards.sql ↔ supabase/migrations/20260510170100_*
6. **Apply path operator** : `supabase migration up` OU `psql -f`
7. **Files modified** : 2 (1 migration, 1 mirror). Schema core intact.
8. **Next consumers** : Plan 05 (types), Plan 06 (server actions create/update/reorder/submit), Plan 09 (UI Kanban + DnD), Plan 11 (CSV export GM)
</output>
