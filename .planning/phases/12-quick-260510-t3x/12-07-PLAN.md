---
phase: "12-quick-260510-t3x"
plan: "12-07"
type: execute
wave: 2
depends_on:
  - "12-02"
  - "12-05"
files_modified:
  - lib/score.ts
autonomous: false
requirements:
  - T3X-EXPANSION-MULTIPLIER-MECHANISM
must_haves:
  truths:
    - "lib/score.ts exporte applyBonusMultiplier(rawScore, bonusEvents, submission) -> {boostedScore, applied} qui calcule le score boosté côté client UI seulement"
    - "applyBonusMultiplier filtre les bonus_events status='validated' du bonus' player, claimed_at < submission.submittedAt, multiplier_consumed_at IS NULL"
    - "Pour scope=next_deliverable : la première validated submission après claim consomme le multiplier (max factor parmi les actifs)"
    - "Pour scope=rest_of_event : le multiplier persiste jusqu'à event.endsAt (passé en paramètre optionnel à la fonction)"
    - "Cap global BONUS_MULTIPLIER_CAP = 3.00 enforced via Math.min"
    - "sumPlayerScoreProject signature INCHANGÉE — applyBonusMultiplier est une nouvelle fonction sépare, NOT une mutation de l'existante (preserve backward compat avec lib/results.ts qui consomme l'ancienne)"
    - "JSDoc explicite : 'multiplier_factor numeric NEVER displayed to Player as number — UI must render qualitative badge (Plan 08)'"
    - "Aucune mutation DB dans lib/score.ts — fonction pure, lit bonus_events depuis paramètre passé par caller (server component qui fait le fetch)"
    - "applyBonusMultiplier est pure : (rawScore, bonusEvents[], submission) -> {boostedScore, applied}, aucun side-effect, deterministe pour test unit ulterieur"
    - "npm run typecheck clean, npm run lint clean"
    - "R1 préservé : applyBonusMultiplier retourne {boostedScore, applied: bonusEventId|null} — les UI Player consomment 'applied' booléen pour badge qualitatif, JAMAIS exposent boostedScore comme nombre Player"
  artifacts:
    - path: "lib/score.ts"
      provides: "applyBonusMultiplier(rawScore, bonusEvents, submission, eventEndsAt?) function pure pour boost score côté UI"
      contains: "applyBonusMultiplier|BONUS_MULTIPLIER_CAP"
  key_links:
    - from: "lib/score.ts applyBonusMultiplier"
      to: "lib/types.ts BONUS_MULTIPLIER_CAP + BonusEvent type"
      via: "Import des types + cap constant"
      pattern: "import.*BONUS_MULTIPLIER_CAP|import.*BonusEvent"
    - from: "lib/score.ts applyBonusMultiplier"
      to: "components/results-podium.tsx + components/results-replay.tsx (Plan 08 ou 12) — GameMaster-only"
      via: "Côté GM uniquement (gated isGameMaster), affiche boostedScore. Player UI consomme 'applied' pour badge"
      pattern: "applyBonusMultiplier"
---

<objective>
Wave 2 — Implémenter le mécanisme multiplier (D-03) côté TypeScript dans `lib/score.ts`. La fonction est PURE et CLIENT-SIDE only (computation locale UI), pas un trigger DB.

**Pourquoi pure et client-side, pas un trigger DB** :
- Le score authoritative est `players.score_project` recalculé par `recalc_player_score()` SQL trigger (cf. database/triggers.sql). Modifier ce trigger pour intégrer le multiplier nécessiterait des migrations risquées en T-3.
- À la place : on garde le trigger DB intact (sum brute des max scores par template), et on applique le multiplier UNIQUEMENT au rendu UI (Mentor/GM voient le boostedScore, lib/results.ts utilise `players.score_project` brut pour le ranking).
- **Conséquence** : le multiplier n'affecte PAS le ranking final. Il est purement un signal pédagogique côté Mentor/GM ("cette équipe a un boost actif sur ce livrable") et Player ("Boost actif" badge qualitatif).

**Décision prudente T-3** : pas de modification du trigger SQL. Le multiplier est un méta-signal, pas un boost de classement. Si le owner souhaite que le multiplier impacte le ranking pour de vrai, ce sera une décision v0.3 hors scope T3X.

**Mais** : la fonction reste utile car elle :
1. Permet aux UI Mentor/GM d'afficher le score boosté à titre informatif
2. Drive le badge "Boost actif" côté Player (consomme `applied !== null`)
3. Drive le marquage `multiplier_consumed_at` côté server (qui peut être set lors de l'evaluation Mentor d'une submission qui consomme un multiplier — out of scope T3X mais préparé)

**Scope plan** : ajout d'une fonction pure dans `lib/score.ts`. Pas de mutation DB, pas de trigger. Tests unitaires non prioritaires (no test runner configured cf. CLAUDE.md).

**Output** :
- 1 fichier modifié (lib/score.ts)
- 1 nouvelle fonction exportée (applyBonusMultiplier)
- npm checks clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/score.ts
@lib/types.ts
@database/triggers.sql

<interfaces>
<!-- État actuel lib/score.ts (49 lignes complètes) -->

```typescript
import type { Evaluation, Player, Submission } from "@/lib/types";

export function scoreFromEvaluation(evaluation: Evaluation): number {
  return Object.values(evaluation.scores).reduce(
    (acc, n) => acc + (typeof n === "number" ? n : 0),
    0,
  );
}

export function sumPlayerScoreProject(
  evaluations: Evaluation[],
  submissions: Submission[],
): number {
  // ... agnostique au max_score, prend max(total_score) per template, somme.
}

export type ScoreBreakdown = {
  project: number;
  engagement: number;
  total: number;
};

export function combineScores(
  p: Pick<Player, "scoreProject" | "scoreEngagement">,
): ScoreBreakdown {
  return {
    project: p.scoreProject,
    engagement: p.scoreEngagement,
    total: p.scoreProject + p.scoreEngagement,
  };
}
```

<!-- Types à importer (Plan 05) -->

```typescript
import type { BonusEvent, MultiplierScope } from "@/lib/types";
import { BONUS_MULTIPLIER_CAP } from "@/lib/types";
```

<!-- Convention pure function (cf. CLAUDE.md "Pure helpers prefer single object parameter when 3+ fields") -->

```typescript
// 3+ fields = single object param :
export function applyBonusMultiplier(args: {
  rawScore: number;
  bonusEvents: BonusEvent[];
  submission: Pick<Submission, "submittedAt" | "playerId">;
  eventEndsAt?: string;
}): { boostedScore: number; applied: string | null };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter applyBonusMultiplier(args) à lib/score.ts</name>
  <files>lib/score.ts</files>
  <read_first>
    - `lib/score.ts` (49 lignes complètes)
    - `lib/types.ts` (BonusEvent, MultiplierScope, BONUS_MULTIPLIER_CAP — créés Plan 05)
  </read_first>
  <action>
**Stratégie** : appender en bas de `lib/score.ts` après `combineScores`. Ajouter 1 import additionnel + 1 fonction pure.

**Étape 1 — Modifier l'import en haut du fichier** (ligne 1) :

**Avant** :
```typescript
import type { Evaluation, Player, Submission } from "@/lib/types";
```

**Après** :
```typescript
import type { Evaluation, Player, Submission, BonusEvent } from "@/lib/types";
import { BONUS_MULTIPLIER_CAP } from "@/lib/types";
```

**Étape 2 — Appender en BAS du fichier** :

```typescript

/**
 * Apply bonus multiplier to a raw score for UI display purposes (Mentor/GM).
 *
 * D-03 mechanism :
 *   - bonus_events with status='validated' and multiplier_consumed_at=null
 *     are "active" for the project.
 *   - For multiplier_scope='next_deliverable' : the first validated submission
 *     after the bonus claimedAt consumes the multiplier (highest factor wins).
 *   - For multiplier_scope='rest_of_event' : the multiplier persists until
 *     eventEndsAt (passed by caller).
 *   - Global cap : BONUS_MULTIPLIER_CAP (3.0) — anti-stacking abuse.
 *
 * R1 PRESERVED : the returned `boostedScore` is a number — it MUST NOT be
 * displayed Player-side as a number. Plan 08+ UI consumes `applied` (string|null)
 * to render a qualitative "Boost actif" badge instead. Mentor/GM UIs may show
 * the numeric value (gated isGameMaster).
 *
 * Pure function : no DB mutations, no side effects. Caller must fetch
 * bonusEvents (via server component or client-side query) and pass them in.
 *
 * @param args.rawScore         The raw evaluation total_score for this submission.
 * @param args.bonusEvents      All bonus_events for the player (filtered server-side).
 * @param args.submission       The submission whose score is being boosted (needs submittedAt + playerId).
 * @param args.eventEndsAt      Optional ISO string for event.ends_at (used for rest_of_event scope).
 * @returns boostedScore (capped at rawScore * BONUS_MULTIPLIER_CAP), applied (bonusEvent.id or null).
 */
export function applyBonusMultiplier(args: {
  rawScore: number;
  bonusEvents: BonusEvent[];
  submission: Pick<Submission, "submittedAt" | "playerId">;
  eventEndsAt?: string;
}): { boostedScore: number; applied: string | null } {
  const { rawScore, bonusEvents, submission, eventEndsAt } = args;
  if (rawScore <= 0) return { boostedScore: rawScore, applied: null };

  // Filter applicable bonuses : own player, validated, claimed before submission, not consumed
  const applicable = bonusEvents.filter((b) => {
    if (b.projectId !== submission.playerId) return false;
    if (b.status !== "validated") return false;
    if (b.multiplierConsumedAt !== null) return false;
    if (b.claimedAt >= submission.submittedAt) return false;
    if (b.multiplierScope === "rest_of_event" && eventEndsAt && submission.submittedAt > eventEndsAt) {
      return false;
    }
    return true;
  });

  if (applicable.length === 0) return { boostedScore: rawScore, applied: null };

  // Take max factor (no stacking)
  const winner = applicable.reduce((best, cur) =>
    cur.multiplierFactor > best.multiplierFactor ? cur : best,
  );

  const factor = Math.min(winner.multiplierFactor, BONUS_MULTIPLIER_CAP);
  const boostedScore = Math.round(rawScore * factor * 100) / 100; // 2 decimals
  return { boostedScore, applied: winner.id };
}

/**
 * Player-facing helper : returns whether at least one validated bonus_event
 * is currently active (not consumed) for the given player.
 *
 * R1 PRESERVED : returns a BOOLEAN, never a number. Drive the qualitative
 * "Boost actif" badge in Plan 08 Player UI.
 */
export function hasActiveBonus(args: {
  bonusEvents: BonusEvent[];
  playerId: string;
  eventEndsAt?: string;
}): boolean {
  const { bonusEvents, playerId, eventEndsAt } = args;
  const now = new Date().toISOString();
  return bonusEvents.some((b) => {
    if (b.projectId !== playerId) return false;
    if (b.status !== "validated") return false;
    if (b.multiplierConsumedAt !== null) return false;
    if (b.multiplierScope === "rest_of_event" && eventEndsAt && now > eventEndsAt) {
      return false;
    }
    return true;
  });
}
```

**IMPORTANT** :
- ASCII pur dans les JSDoc (cohérent avec convention).
- Pas de mutation des arrays input (filter retourne un nouveau array).
- Pas de Date.now() arithmetic — comparer via string ISO comparison (ordre lexicographique = ordre temporel pour ISO 8601 UTC).
- Math.round à 2 décimales (cohérent avec `numeric(6,2)` côté DB).
- Le BONUS_MULTIPLIER_CAP est imported as `import { BONUS_MULTIPLIER_CAP }` (value), pas `import type` — c'est un const runtime.
- `BonusEvent` est `import type` (type only).
- Aucune référence à `multiplier_factor` snake_case dans le code TS — uniquement `multiplierFactor` camelCase (cf. types Plan 05).

**NE PAS** :
- Modifier `sumPlayerScoreProject` ni `combineScores` (backward compat lib/results.ts).
- Importer `revalidatePath` ou logique server (lib/score.ts est pure).
- Ajouter une fonction qui mute la DB (consume_multiplier) — c'est out of scope T3X (sera Plan post-pilote ou v0.3).
- Ajouter de console.log pour debug.

**Vérification post-edit** :
```powershell
npm run typecheck
# Attendu : 0 erreur

npm run lint
# Attendu : 0 erreur

(Select-String -Pattern "export function applyBonusMultiplier" lib/score.ts).Count
# Attendu : 1

(Select-String -Pattern "export function hasActiveBonus" lib/score.ts).Count
# Attendu : 1

(Select-String -Pattern "BONUS_MULTIPLIER_CAP" lib/score.ts).Count
# Attendu : >= 2 (import + usage)
```
  </action>
  <acceptance_criteria>
    <automated>
      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "export function applyBonusMultiplier" lib/score.ts).Count
      # Attendu : 1

      (Select-String -Pattern "export function hasActiveBonus" lib/score.ts).Count
      # Attendu : 1

      (Select-String -Pattern "import.*BonusEvent" lib/score.ts).Count
      # Attendu : >= 1

      (Select-String -Pattern "BONUS_MULTIPLIER_CAP" lib/score.ts).Count
      # Attendu : >= 2

      (Select-String -Pattern "sumPlayerScoreProject" lib/score.ts).Count
      # Attendu : >= 1 (existant — preserved)
    </automated>
  </acceptance_criteria>
  <done>
- 2 fonctions ajoutées (applyBonusMultiplier, hasActiveBonus)
- BONUS_MULTIPLIER_CAP importé et utilisé
- BonusEvent importé (type only)
- sumPlayerScoreProject + combineScores INCHANGÉS
- Pure functions (no side effects)
- JSDoc R1-aware
- npm checks clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 2: Audit R1 manuel (multiplier mechanism Player-safe) + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --cached lib/score.ts`
    - `.claude/agents/eic-pedagogical-advisor.md` (R1 cardinal rule)
  </read_first>
  <action>
**Pré-checkpoint (auto par Claude)** :
- Task 1 complétée
- npm typecheck + lint clean
- Pas de modif des fonctions existantes (sumPlayerScoreProject, combineScores intacts)

**Action Claude post-approval** :
```bash
git add lib/score.ts
git commit -m "$(cat <<'EOF'
feat(t3x-score): add applyBonusMultiplier + hasActiveBonus pure functions (D-03)

Wave 2 / plan 07 : mecanisme multiplier cote TypeScript pure (UI client-side
only). Le score authoritative reste players.score_project recalcule par
recalc_player_score() SQL trigger — le multiplier est un meta-signal pour les
UI Mentor/GM (numeric) et Player (boolean badge qualitatif).

- applyBonusMultiplier({rawScore, bonusEvents, submission, eventEndsAt}) :
  filtre validated + non-consumed + claimedAt<submittedAt + scope rules,
  prend max factor (no stacking), cape a BONUS_MULTIPLIER_CAP=3.0,
  retourne {boostedScore, applied: bonusEventId|null}
- hasActiveBonus({bonusEvents, playerId, eventEndsAt}) : boolean drive le
  badge "Boost actif" Player UI (Plan 08) — R1 STRICT (pas de chiffre)

R1 preserve : JSDoc explicite "MUST NOT be displayed Player-side as a number".
Pure functions (no DB mutation, no side effects). sumPlayerScoreProject et
combineScores INCHANGES (backward compat lib/results.ts).

Decision T-3 : multiplier n'impacte PAS le ranking final lib/results.ts —
c'est un signal pedagogique. Si l'owner veut que le multiplier impacte le
classement, c'est une decision v0.3 hors scope.

npm typecheck OK, npm lint OK.

Refs : BRIEF §4 D-03, lib/types.ts BONUS_MULTIPLIER_CAP, CLAUDE.md R1.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-score"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 1 fichier (lib/score.ts)

      Select-String -Pattern "applyBonusMultiplier|hasActiveBonus" lib/score.ts
      # Attendu : 2+ matches each
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → Claude commit. Sinon : décrire issues.</done>
  <what-built>
- 2 fonctions pures dans lib/score.ts
- applyBonusMultiplier (Mentor/GM display)
- hasActiveBonus (Player badge boolean)
- R1 préservé via JSDoc + retour boolean côté Player
- Backward compat préservée (sumPlayerScoreProject intact)
  </what-built>
  <how-to-verify>
**Audit checklist** :

1. **R1 — multiplier_factor JAMAIS exposé Player en nombre** :
   ```powershell
   git diff --cached lib/score.ts | Select-String -Pattern "multiplier_factor|multiplierFactor"
   ```
   Vérifier que :
   - `multiplierFactor` apparaît UNIQUEMENT dans applyBonusMultiplier (Mentor/GM-facing) et la JSDoc
   - hasActiveBonus retourne boolean — JAMAIS le factor

2. **Backward compat** :
   ```powershell
   git diff --cached lib/score.ts | Select-String -Pattern "sumPlayerScoreProject|combineScores"
   ```
   Attendu : 0 hit (les fonctions existantes ne sont PAS modifiées).

3. **Pure functions** :
   ```powershell
   git diff --cached lib/score.ts | Select-String -Pattern "createClient|supabase|fetch|revalidatePath|console"
   ```
   Attendu : 0 hit (aucun side effect ajouté).

4. **Cap enforcement** :
   ```powershell
   git diff --cached lib/score.ts | Select-String -Pattern "Math\.min.*BONUS_MULTIPLIER_CAP"
   ```
   Attendu : >= 1 hit.

5. **Typecheck + lint** :
   ```powershell
   npm run typecheck ; npm run lint
   ```
   Attendu : exit 0 chacun.

6. **JSDoc R1 disclaimer** :
   ```powershell
   Select-String -Pattern "MUST NOT be displayed|R1 PRESERVED" lib/score.ts
   ```
   Attendu : >= 2 hits (présent dans les 2 JSDoc).
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "remove rest_of_event check, scope=next_deliverable only", "applyBonusMultiplier should mutate multiplier_consumed_at — out of scope, leave for v0.3").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **TS health** :
   ```powershell
   npm run typecheck
   npm run lint
   # Attendu : exit 0
   ```

2. **R1 audit final** :
   ```powershell
   Select-String -Pattern "multiplierFactor" lib/score.ts | Where-Object { $_.Line -match "return" }
   # Attendu : 0 hit (jamais retourné côté return Player-facing — uniquement boostedScore qui est Mentor/GM)
   ```

3. **Backward compat** :
   ```powershell
   git diff HEAD~1 HEAD lib/score.ts | Select-String -Pattern "^-export function sumPlayerScoreProject|^-export function combineScores"
   # Attendu : 0 hit (fonctions existantes pas modifiées)
   ```
</verification>

<success_criteria>
- [ ] applyBonusMultiplier exporté
- [ ] hasActiveBonus exporté
- [ ] BONUS_MULTIPLIER_CAP utilisé via Math.min
- [ ] Pure functions (no side effects)
- [ ] R1 JSDoc explicit
- [ ] sumPlayerScoreProject + combineScores INCHANGÉS
- [ ] npm typecheck + lint clean
- [ ] Commit atomique
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-07-SUMMARY.md` :

Sections :
1. **2 fonctions ajoutées** : applyBonusMultiplier (Mentor/GM display), hasActiveBonus (Player badge)
2. **Décision T-3** : multiplier n'impacte PAS le ranking final lib/results.ts (méta-signal uniquement, ranking authoritative reste players.score_project SQL trigger)
3. **R1 preservation** : JSDoc explicit + hasActiveBonus retourne boolean
4. **Cap mechanism** : BONUS_MULTIPLIER_CAP = 3.0 enforced via Math.min
5. **Pure functions** : aucun side effect, deterministe
6. **Backward compat** : sumPlayerScoreProject + combineScores intacts
7. **Files modified** : 1 (lib/score.ts)
8. **Next consumers** : Plan 08 (Player UI consume hasActiveBonus pour badge), Plan 10 (Mentor UI consume applyBonusMultiplier pour score boosté affiché)
</output>
