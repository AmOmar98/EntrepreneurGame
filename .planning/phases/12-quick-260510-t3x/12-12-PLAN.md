---
phase: "12-quick-260510-t3x"
plan: "12-12"
type: execute
wave: 4
depends_on:
  - "12-02"
  - "12-03"
  - "12-06"
  - "12-08"
  - "12-09"
  - "12-10"
  - "12-11"
files_modified: []
autonomous: false
requirements:
  - T3X-EXPANSION-SMOKE-E2E
must_haves:
  truths:
    - "Smoke E2E full parcours validé : Player onboarding → 9 livrables (incl MoSCoW Kanban Wave 3) → bonus claim → pitch deck"
    - "Mentor flow validé : evaluate deliverable + bonus review (validate + reject) + feedback"
    - "GameMaster flow validé : announce + export CSV (players + moscow + cohort)"
    - "Auth flow validé : login/logout/role gating (Player ne voit pas /admin, Mentor ne voit pas /admin/players, etc.)"
    - "RLS isolation validé : Player A ne voit pas bonus_events de Player B (audit via 2 sessions Player distinctes)"
    - "RLS isolation validé : Player A ne voit pas moscow_cards de Player B"
    - "R1 STRICT audit grep zone Player : 0 hit score|rank|/100|/140|toFixed|points|multiplier_factor sur app/journey/, app/results/ Player view, components/results-* (sauf gated isGameMaster), components/bonus-* nouveaux, components/moscow-* nouveaux"
    - "R2 audit : aucune submission bloquée par validators warn-only (test soumission MoSCoW avec 0 carte → message warn mais réussit)"
    - "R3 audit : aucun blocage cross-mission codé en dur (test : bonus claim avant tout livrable validé doit fonctionner)"
    - "Migrations Supabase appliquées prod : 20260510170000_bonus_events_recreate + 20260510170100_moscow_cards"
    - "Commit final smoke summary avec screenshots/ folder bullet pour chaque flow validé"
    - "Aucune nouvelle ligne de code de feature ajoutée pendant ce plan — uniquement procédure + screenshots + summary"
    - "Si finding bloquant détecté → bug fix de hot-fix permis (commit séparé) MAIS NE FAIT PAS partie de ce plan (le plan reste smoke-only)"
  artifacts:
    - path: ".planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md"
      provides: "Findings smoke E2E avec sévérité (blocker/medium/low) et étape de reproduction"
      contains: "Finding|Blocker|Medium|Low"
    - path: "screenshots/smoke-2026-05-12-t3x/"
      provides: "Screenshots des écrans testés (12+ captures attendues)"
      contains: "(folder)"
  key_links:
    - from: "Smoke regression"
      to: "All previous plans 01-11"
      via: "End-to-end validation each feature works in prod environment"
      pattern: "smoke|E2E|regression"
---

<objective>
Wave 4 — Smoke regression E2E full parcours après merge des Plans 01-11. CHECKPOINT-ONLY plan, aucune modification de code.

**Pourquoi un plan séparé** : sans smoke, on déploie aveugle en T-3 (mardi 12/05 23h cutoff merge `main` cf. CLAUDE.md "Cutoff merge `main` = mardi 12 mai 2026 23h00"). Le smoke valide :
1. **Migrations appliquées** sur Supabase prod (bonus_events + moscow_cards)
2. **Tous les flows fonctionnent** runtime (pas juste compile)
3. **R1/R2/R3 préservés** en runtime, pas juste en source
4. **RLS isolation** entre Players (anti-fuite cross-équipe)
5. **Auth gates** toutes les routes nouvelles

**Output** :
- 1 fichier markdown findings (sévérité + repro)
- Folder screenshots/smoke-2026-05-12-t3x/ avec captures
- 1 commit final smoke summary
- Si findings bloquants → triage + commit hot-fix séparé (hors scope plan, mais documenté dans findings)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/BRIEF.md
@RETROSPECTIVE-T3-2026-05-10.md

<interfaces>
<!-- Existing smoke artifacts pour pattern -->

```
screenshots/phase-06-smoke/
screenshots/smoke-2026-05-10-260510-smoke-t3-auth/
screenshots/smoke-2026-05-10-260510-smoke-t3/
```

Pattern : 1 screenshot par étape clé, naming `01-login-desktop-1440.png`, `02-network-log.json`, etc.

<!-- Test accounts disponibles (cf. cohorte-agreentech.csv ou seed) -->

Test accounts (Omar a accès via service-role key prod ou comptes test dédiés) :
- Player : 1 par team de la cohorte AgreenTech
- Mentor : 2-3 mentors EIC (email connu)
- GameMaster : Omar lui-même
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migrations Supabase prod (bonus_events + moscow_cards)</name>
  <files>(no file modification — Supabase prod apply)</files>
  <read_first>
    - `database/bonus_events.sql` (Plan 02 mirror)
    - `database/moscow_cards.sql` (Plan 03 mirror)
    - `supabase/migrations/20260510170000_bonus_events_recreate.sql`
    - `supabase/migrations/20260510170100_moscow_cards.sql`
  </read_first>
  <action>
**MANUAL ACTION OMAR** : appliquer les 2 nouvelles migrations sur Supabase prod.

**Procédure recommandée** :

**Option A — Supabase CLI** (si configuré) :
```bash
supabase login
supabase link --project-ref <project-ref>
supabase db push
# Devrait appliquer 20260510170000_* et 20260510170100_* automatiquement
```

**Option B — psql direct (recommandé T-3 pour contrôle)** :
```bash
# Backup avant (CRITIQUE)
pg_dump $SUPABASE_DB_URL > backup-pre-t3x-$(date +%Y%m%d-%H%M).sql

# Apply
psql $SUPABASE_DB_URL -f database/bonus_events.sql
psql $SUPABASE_DB_URL -f database/moscow_cards.sql

# Idempotency test : re-apply
psql $SUPABASE_DB_URL -f database/bonus_events.sql  # Attendu : 0 erreur, 0 nouvelle row
psql $SUPABASE_DB_URL -f database/moscow_cards.sql  # Attendu : idem
```

**Vérifications post-apply** (Omar via Supabase Studio ou psql) :
```sql
-- Tables existent
\dt public.bonus_events
\dt public.moscow_cards

-- Enums existent
\dT+ public.bonus_type
\dT+ public.bonus_status
\dT+ public.multiplier_scope
\dT+ public.moscow_bucket

-- RLS active
SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('bonus_events', 'moscow_cards');
-- Attendu : rowsecurity = TRUE pour les 2

-- Policies présentes
SELECT polname, tablename FROM pg_policies WHERE tablename IN ('bonus_events', 'moscow_cards');
-- Attendu : 4 policies bonus_events + 4 policies moscow_cards = 8 lignes

-- Trigger updated_at attaché
SELECT tgname FROM pg_trigger WHERE tgrelid IN ('public.bonus_events'::regclass, 'public.moscow_cards'::regclass);
-- Attendu : trg_bonus_events_updated_at + trg_moscow_cards_updated_at
```

**Si erreur d'apply** : capturer le message psql, créer un finding HIGH dans 260510-t3x-12-SMOKE-FINDINGS.md, NE PAS continuer le smoke jusqu'à résolution.
  </action>
  <acceptance_criteria>
    <automated>
      # Pas d'automated check côté local — c'est une action prod manuelle.
      # Omar valide via Supabase Studio capture ou \dt psql output.
      # On peut au moins vérifier que les fichiers SQL existent côté repo :
      Test-Path database/bonus_events.sql
      Test-Path database/moscow_cards.sql
      # Attendu : True True
    </automated>
  </acceptance_criteria>
  <done>Omar confirme : 2 tables existent, 8 policies présentes, 2 triggers attachés, idempotency re-run OK. Tape "applied" ou décrit l'erreur d'apply.</done>
  <what-built>
- 2 migrations Supabase prod appliquées
- bonus_events + moscow_cards tables actives en prod
- RLS + triggers + indexes en place
  </what-built>
  <how-to-verify>
**Procédure 5 étapes** :

1. **Backup avant** :
   ```bash
   pg_dump $SUPABASE_DB_URL > backup-pre-t3x-$(date +%Y%m%d-%H%M).sql
   ```
   Attendu : fichier non vide.

2. **Apply bonus_events** :
   ```bash
   psql $SUPABASE_DB_URL -f database/bonus_events.sql
   ```
   Attendu : 0 erreur. Sortie inclut `CREATE TYPE`, `CREATE TABLE`, `CREATE POLICY` x4, `CREATE INDEX` x3, `CREATE TRIGGER`.

3. **Apply moscow_cards** :
   ```bash
   psql $SUPABASE_DB_URL -f database/moscow_cards.sql
   ```
   Attendu : 0 erreur. Idem pattern.

4. **Idempotency test (re-run)** :
   ```bash
   psql $SUPABASE_DB_URL -f database/bonus_events.sql
   psql $SUPABASE_DB_URL -f database/moscow_cards.sql
   ```
   Attendu : 0 erreur. Output contient des `NOTICE: ... already exists, skipping` mais pas d'`ERROR`.

5. **Verification queries** (cf. action ci-dessus) :
   - 2 tables présentes
   - 4 enums présents
   - 8 policies actives
   - 2 triggers attachés
  </how-to-verify>
  <resume-signal>Type "applied" si les 2 migrations sont appliquées et vérifiées OU décrire l'erreur d'apply (le smoke ne peut pas continuer sans).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke E2E Player flow + Mentor flow + GM flow + Auth + RLS</name>
  <files>screenshots/smoke-2026-05-12-t3x/, .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md</files>
  <read_first>
    - `BRIEF.md` §2.F (Smoke regression complet)
    - `RETROSPECTIVE-T3-2026-05-10.md` (smoke 2026-05-09 findings comme référence)
  </read_first>
  <action>
**Procédure smoke complète à exécuter par Omar (4-6h estimées)** :

**Setup** :
```bash
# 1. Build prod local
npm run build
npm start
# OU pointer le navigateur sur https://entrepreneur-game-six.vercel.app après deploy

# 2. Créer le folder screenshots
mkdir -p screenshots/smoke-2026-05-12-t3x/
```

**Initialiser le findings file** :
```powershell
@"
# Smoke E2E Findings — T3X EXPANSION (2026-05-12)

Date : 2026-05-12
Plan : .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-PLAN.md
Migrations applied : 20260510170000_bonus_events_recreate + 20260510170100_moscow_cards

## Methodologie

3 sessions paralleles : Player (Team A), Player (Team B), Mentor.
+ Session GM (Omar).
4 navigateurs profils distincts pour eviter les cookies cross.

## Findings

(remplir au fur et a mesure du smoke)

| ID | Severity | Step | Description | Repro | Status |
|----|----------|------|-------------|-------|--------|
|    |          |      |             |       |        |

## Next steps

- [ ] Hot-fix bloquant (commit separe HORS plan 12)
- [ ] Documentation finding mineurs en RETROSPECTIVE post-pilote
- [ ] Go/No-Go go-live 13/05 8h30
"@ | Out-File -FilePath .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md
```

**Étapes smoke** (chaque étape = 1+ screenshot) :

### A. Auth flow (15 min)
- [ ] A.1 Login Player Team A (capture `01-login-player-A.png`)
- [ ] A.2 Login Mentor (capture `02-login-mentor.png`)
- [ ] A.3 Login GM (capture `03-login-gm.png`)
- [ ] A.4 Logout chacun (capture `04-logout-screen.png`)
- [ ] A.5 Player A tente d'accéder à `/admin` → redirect /journey (capture `05-player-blocked-admin.png`)
- [ ] A.6 Mentor tente d'accéder à `/admin/players` → redirect /mentor (capture `06-mentor-blocked-admin-players.png`)

### B. Player flow Team A (60 min)
- [ ] B.1 Onboarding (capture `10-onboarding.png`)
- [ ] B.2 Journey rail visible avec section "Bonus disponibles" (3 liens) (capture `11-journey-bonus-rail.png`)
- [ ] B.3 Soumettre livrable #1 Persona AgriTech (URL HTTPS dummy `https://docs.google.com/...`) (capture `12-deliverable-1-submit.png`)
- [ ] B.4 Naviguer `/journey/deliverable/{id-de-fiche-produit-plan-dev-v1}` — vérifier que `<MoscowKanban>` est rendu dans la page (must_have de Plan 10 Task 7) (capture `13-moscow-kanban-empty.png`)
- [ ] B.4.5 Vérifier qu'aucun chiffre score/rank/multiplier n'apparaît sur le Kanban (R1 audit visuel)
- [ ] B.5 Cliquer sur le bouton "Ajouter une carte" colonne MUST. Saisir feature/pourquoi/contrainte. Vérifier carte ajoutée (capture `14-moscow-card-added.png`)
- [ ] B.5.5 Ajouter 2 cartes additionnelles dans SHOULD et COULD (3 cartes total)
- [ ] B.6 Drag une carte de MUST → SHOULD via DnD. Refresh page. Vérifier persistence du nouveau bucket (capture `15-moscow-dnd-persistence.png`)
- [ ] B.7 Cliquer "Soumettre livrable" sous le Kanban (ou via le formulaire ProofWorkflow fallback). Vérifier que `submitMoscowDeliverableFlow` génère le snapshot URL relatif et le mailto draft (capture `16-moscow-submit-mailto.png`)
- [ ] B.7.5 Cliquer le snapshot URL dans le mailto → la route `/journey/deliverable/{id}/moscow-snapshot?p={playerId}` rend les 3 cartes en lecture seule (capture `17-moscow-snapshot-readonly.png`)
- [ ] B.8 Naviguer `/journey/bonus/bonus_verbatims_terrain` — voir form claim (capture `18-bonus-claim-form.png`)
- [ ] B.9 Submit bonus claim avec URL HTTPS dummy (capture `19-bonus-claim-success.png`)
- [ ] B.10 Vérifier badge "En attente de validation" sur historique (capture `20-bonus-status-pending.png`)
- [ ] B.11 **R1 STRICT audit visuel** : ouvrir DevTools, inspecter HTML rendu de `/journey` et `/journey/bonus/bonus_verbatims_terrain` — chercher chaînes `score`, `multiplier`, `/100`, `/140` → capture `21-r1-audit-html.png` (devrait être 0 hit dans le contenu rendu)

### C. Player flow Team B (10 min) — RLS isolation test
- [ ] C.1 Login Player Team B (différent user/team de A)
- [ ] C.2 Naviguer `/journey/bonus/bonus_verbatims_terrain` → l'historique doit être VIDE (n'affiche PAS le claim de Team A) (capture `22-rls-bonus-isolation.png`)
- [ ] C.3 Naviguer `/journey/deliverable/[id]/moscow-snapshot?p=<playerA-id>` (URL avec ID de Team A) → 0 carte rendue (RLS gate via is_my_player) (capture `23-rls-moscow-isolation.png`)

### D. Mentor flow (30 min)
- [ ] D.1 Login Mentor
- [ ] D.2 Naviguer `/mentor` — voir submissions à reviewer incluant le MoSCoW de Team A (capture `30-mentor-dashboard.png`)
- [ ] D.3 Cliquer sur la submission MoSCoW Team A → page `/mentor/submission/[id]` (capture `31-mentor-submission.png`)
- [ ] D.4 Cliquer sur le proof_url qui pointe vers `/journey/deliverable/[id]/moscow-snapshot?p=...` → snapshot s'ouvre, 3 cartes visibles (capture `32-mentor-views-snapshot.png`)
- [ ] D.5 Évaluer la submission via rubric (5×5=25), validate v1, feedback (capture `33-mentor-evaluation.png`)
- [ ] D.6 Naviguer `/mentor/bonus/<bonus_id>` (id du claim Team A B.9) → form review (capture `34-mentor-bonus-review.png`)
- [ ] D.7 Validate le bonus → message success (capture `35-mentor-bonus-validated.png`)
- [ ] D.8 Re-naviguer en Team A : badge passe à "Boost actif" (capture `36-bonus-status-validated.png`)

### E. GM flow (20 min)
- [ ] E.1 Login GM
- [ ] E.2 Export `/admin/export/players.csv` → télécharge CSV (capture `40-gm-export-players.png`)
- [ ] E.3 Export `/api/export/moscow/<deliverableId-livrable-4>.csv` → télécharge CSV avec lignes Team A (capture `41-gm-export-moscow.png`)
- [ ] E.4 Tester /admin announce (cohérence Phase 9) (capture `42-gm-announce.png`)
- [ ] E.5 Player tente d'accéder `/api/export/moscow/...csv` → 403 Forbidden (capture `43-player-blocked-csv.png`)

### F. R1/R2/R3 final audit grep source code (15 min)
```powershell
# R1 STRICT zone Player
grep -rn "score|rank|note|/100|/140|points|toFixed|multiplier_factor" app/journey app/results components/results-* components/submission-* components/bonus-* components/moscow-* --include="*.tsx" 2>/dev/null
# Attendu : 0 hit dans Player-facing zones (sauf gated isGameMaster sur components/results-*)

# R2 audit warn-only
grep -rn "severity.*error|raise exception|throw new Error" app/actions.ts | grep -E "claimBonus|reviewBonus|MoscowCard|submitMoscow"
# Attendu : 0 hit dans les nouvelles actions

# R3 audit no blocking
grep -rn "blocks_progression_to|disabled.*because.*mission" app/ components/ --include="*.tsx" --include="*.ts"
# Attendu : 0 hit
```

### G. Documentation findings dans 260510-t3x-12-SMOKE-FINDINGS.md
Pour chaque finding détecté pendant A-F :
- ID (T3X-SMK-01, etc.)
- Severity (Blocker / Medium / Low)
- Step (A.1, B.5, etc.)
- Description courte
- Repro (étapes)
- Status (open / hot-fixed)

**Cutoff smoke** : si > 1 Blocker ou > 3 Medium détectés → STOP go-live 13/05 8h30, escalade Omar pour décision.

**Si 0 finding bloquant** : commit final smoke summary :
```bash
git add screenshots/smoke-2026-05-12-t3x/ .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md
git commit -m "$(cat <<'EOF'
chore(t3x-smoke): full E2E regression validated, go-live 13/05 unblocked

Wave 4 / plan 12 : smoke regression complete sur les flows Player + Mentor +
GM + Auth + RLS apres merge des plans 01-11. 0 blocker, X medium documente
en findings, X low documente.

Migrations Supabase prod appliquees (20260510170000_bonus_events_recreate +
20260510170100_moscow_cards) en idempotent.

Captures : screenshots/smoke-2026-05-12-t3x/ (~40 png).
Findings : .planning/.../260510-t3x-12-SMOKE-FINDINGS.md.

R1 STRICT verifie en runtime via DevTools inspection HTML /journey + audit
grep source code zone Player.
R2 verifie via test soumission MoSCoW 0 carte = warn message + ok.
R3 verifie via test bonus claim avant tout livrable valide = pas de blocage.
RLS isolation verifie via 2 sessions Player distinctes.

Refs : BRIEF §2.F (smoke regression), CLAUDE.md "Cutoff merge main mardi 12 mai 23h00".

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md
      # Attendu : True
      Test-Path screenshots/smoke-2026-05-12-t3x/
      # Attendu : True
      (Get-ChildItem screenshots/smoke-2026-05-12-t3x/ -Filter *.png).Count
      # Attendu : >= 12 (au moins 12 captures pour les flows critiques)
    </automated>
  </acceptance_criteria>
  <done>Omar tape "go-live unblocked" si 0 blocker OU "blocker found: <id>" + description. Si blocker → triage hot-fix séparé HORS plan 12.</done>
  <what-built>
- Smoke E2E full validé OU findings documentés
- ~40 screenshots
- 1 markdown findings
- Migrations prod appliquées
- R1/R2/R3 + RLS + auth gates validés
  </what-built>
  <how-to-verify>
La how-to-verify est l'action elle-même (smoke = procédure Omar). Le checkpoint vise à empêcher de pousser à go-live tant que les 4 flows critiques + R1 audit + RLS isolation ne sont pas validés.

**Critères Go/No-Go go-live 13/05** :
- Auth : 100% OK (les 6 sous-étapes A.1-A.6 réussissent)
- Player flow : 100% OK des étapes B.1-B.11
- RLS isolation : C.2 + C.3 OK (les 2 confirment isolation)
- Mentor flow : 100% OK D.1-D.8
- GM flow : 100% OK E.1-E.5
- R1 audit : 0 hit grep zone Player
- R2 audit : warn-only confirmé
- R3 audit : no blocking confirmé

**Si tout OK** → commit + tape `go-live unblocked`.

**Si finding** :
- Blocker : STOP go-live, hot-fix séparé, re-smoke partiel
- Medium x1-3 : documenter, go-live possible avec mitigation
- Medium > 3 ou Low > 5 : escalade Omar
  </how-to-verify>
  <resume-signal>Type "go-live unblocked" si 0 blocker OU "blocker found: T3X-SMK-XX <description>" + procédure hot-fix.</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **Findings file existe** :
   ```powershell
   Test-Path .planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-12-SMOKE-FINDINGS.md
   # Attendu : True
   ```

2. **Screenshots folder non vide** :
   ```powershell
   (Get-ChildItem screenshots/smoke-2026-05-12-t3x/ -Filter *.png).Count
   # Attendu : >= 12
   ```

3. **Migrations applied (Omar self-confirmation)** :
   - Documented dans findings

4. **R1/R2/R3 audit grep clean** :
   - Sections F documentées dans findings file
</verification>

<success_criteria>
- [ ] 2 migrations Supabase prod appliquées (idempotent)
- [ ] Auth flow OK (6 sous-étapes)
- [ ] Player flow OK (11 sous-étapes incluant MoSCoW DnD + bonus claim)
- [ ] RLS isolation OK (2 sous-étapes cross-team)
- [ ] Mentor flow OK (8 sous-étapes incluant bonus review + snapshot view)
- [ ] GM flow OK (5 sous-étapes incluant CSV exports)
- [ ] R1 STRICT audit grep clean (zone Player)
- [ ] R2 warn-only confirmed (test soumission 0 carte)
- [ ] R3 no blocking confirmed
- [ ] Findings documentés (sévérité + repro)
- [ ] Screenshots folder ≥12 captures
- [ ] Commit final smoke
</success_criteria>

<output>
After completion, the SUMMARY is the FINDINGS file lui-même (260510-t3x-12-SMOKE-FINDINGS.md). Compléter avec :

Sections :
1. **Tableau des findings** (ID / Severity / Step / Description / Repro / Status)
2. **Screenshots index** : liste des 40+ captures par section A/B/C/D/E
3. **Migrations apply log** : timestamps + idempotency confirmé
4. **R1/R2/R3 audit results** : grep output par section
5. **RLS isolation tests** : confirmation cross-team safe
6. **Go/No-Go decision** : go-live 13/05 8h30 unblocked OU pivot
7. **Hot-fix queue** : si findings, liste des commits séparés à faire AVANT go-live
8. **Next steps** : merge final main avant cutoff 12/05 23h, deploy Vercel, smoke prod URL final
</output>
</output>
