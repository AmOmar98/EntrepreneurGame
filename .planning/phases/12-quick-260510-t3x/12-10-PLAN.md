---
phase: "12-quick-260510-t3x"
plan: "12-10"
type: execute
wave: 3
depends_on:
  - "12-06"
  - "12-07"
  - "12-08"
  - "12-09"
files_modified:
  - app/journey/bonus/[type]/page.tsx
  - app/mentor/bonus/[id]/page.tsx
  - app/journey/deliverable/[id]/moscow-snapshot/page.tsx
  - lib/bonus.ts
  - lib/moscow.ts
  - app/journey/page.tsx
  - app/journey/deliverable/[id]/page.tsx
autonomous: false
requirements:
  - T3X-EXPANSION-ROUTES-AND-RAIL
must_haves:
  truths:
    - "app/journey/bonus/[type]/page.tsx existe : server component qui resolve user, gate role 'player', fetch player_id via player_members, render <BonusClaimForm bonusType={type}> + liste historique des claims du player pour ce type avec <BonusStatusBadge>"
    - "app/mentor/bonus/[id]/page.tsx existe : server component, gate role 'mentor' or 'game_master', fetch bonus_event by id (RLS gates), render review form (POST reviewBonusEventFlow) + display claim details (title, description, doc_url, claimed_at) + status badge"
    - "app/journey/deliverable/[id]/moscow-snapshot/page.tsx existe : server component publicly readable (RLS players_select gate via is_my_player OR is_mentor), render JSON-like snapshot des cartes MoSCoW d'un player pour un deliverable_template (consumée comme proof_url par submitMoscowDeliverableFlow)"
    - "lib/bonus.ts existe : helper getBonusEventsForPlayer(playerId) retourne BonusEvent[] (parse Supabase rows snake_case -> camelCase TS)"
    - "lib/moscow.ts existe : helper getMoscowCardsForPlayerDeliverable(playerId, deliverableTemplateId) retourne MoscowCard[]"
    - "app/journey/page.tsx ajoute une section 'Bonus disponibles' (ou utilise PlayerAnnouncementStrip ou nouveau composant) listant les 3 types BonusType avec liens vers /journey/bonus/[type]"
    - "Toutes les pages new respectent dual-mode : hasSupabaseEnv() check, fallback message FR si demo"
    - "Toutes les pages gate role via getCurrentRole() + redirect approprié"
    - "R1 préservé : aucune page ne render multiplier_factor numérique côté Player ; mentor page peut l'afficher mais via lib/score.ts applyBonusMultiplier (non requis pour T-3)"
    - "ASCII pur dans le code TS, FR via dictionaries.fr"
    - "npm run typecheck + lint clean"
    - "Aucune modification des routes existantes (app/journey/page.tsx l'unique modif = ajout section bonus, pas refacto)"
    - "Player accède au MoscowKanban éditable via /journey/deliverable/[id] quand template.slug === 'fiche-produit-plan-dev-v1' (Task 6 surfacing — résout BLOCKER revision-checker #2)"
  artifacts:
    - path: "app/journey/bonus/[type]/page.tsx"
      provides: "Player route /journey/bonus/[type] : form claim + historique"
      contains: "BonusClaimForm|BonusStatusBadge|getBonusEventsForPlayer"
    - path: "app/mentor/bonus/[id]/page.tsx"
      provides: "Mentor route /mentor/bonus/[id] : review form + claim details"
      contains: "reviewBonusEventFlow|bonus_events"
    - path: "app/journey/deliverable/[id]/moscow-snapshot/page.tsx"
      provides: "Snapshot SSR public des cartes MoSCoW (consume comme proof_url)"
      contains: "getMoscowCardsForPlayerDeliverable|moscow_cards"
    - path: "lib/bonus.ts"
      provides: "getBonusEventsForPlayer(playerId) fetch + map snake_case→camelCase"
      contains: "export.*getBonusEventsForPlayer|export.*async function"
    - path: "lib/moscow.ts"
      provides: "getMoscowCardsForPlayerDeliverable + helper map"
      contains: "export.*getMoscowCardsForPlayerDeliverable"
    - path: "app/journey/page.tsx"
      provides: "Section bonus disponibles ajoutée"
      contains: "/journey/bonus/"
  key_links:
    - from: "app/journey/page.tsx section bonus"
      to: "app/journey/bonus/[type]/page.tsx"
      via: "Link href={`/journey/bonus/${type}`}"
      pattern: "/journey/bonus/"
    - from: "app/mentor/bonus/[id]/page.tsx form"
      to: "app/actions.ts:reviewBonusEventFlow"
      via: "Form action={reviewBonusEventFlow}"
      pattern: "reviewBonusEventFlow"
    - from: "app/journey/deliverable/[id]/moscow-snapshot/page.tsx"
      to: "submissions.proof_url"
      via: "URL absolue pointe vers cette route, consommée comme proof_url côté Mentor review"
      pattern: "moscow-snapshot"
---

<objective>
Wave 3 — Surface UI : routes pages + helpers data layer + intégration journey rail.

**5 nouveaux fichiers + 1 modif** :
1. `app/journey/bonus/[type]/page.tsx` — Player route claim form
2. `app/mentor/bonus/[id]/page.tsx` — Mentor route review form
3. `app/journey/deliverable/[id]/moscow-snapshot/page.tsx` — Snapshot SSR (proof_url cible)
4. `lib/bonus.ts` — fetch helper getBonusEventsForPlayer
5. `lib/moscow.ts` — fetch helper getMoscowCardsForPlayerDeliverable
6. `app/journey/page.tsx` — surface section "Bonus disponibles" (ajout minimal)

**Pattern existant à imiter** : `app/journey/page.tsx` (server component, getCurrentUser, redirect, getCurrentRole gate, dual-mode hasSupabaseEnv).

**Helpers data** : pattern `lib/journey.ts` (getJourneyData) ou `lib/announcements.ts` (getAnnouncementsForPlayer). Map snake_case Supabase rows → camelCase TS.

**Surfacing MoscowKanban (Task 6)** : intégration ciblée dans `app/journey/deliverable/[id]/page.tsx` conditionnelle sur `template.slug === 'fiche-produit-plan-dev-v1'`. Sans cette intégration, le Kanban Plan 09 reste dead code (BLOCKER revision-checker #2). Le composant existant <ProofWorkflow> reste accessible en fallback (R3 — never block).

**Output** :
- 5 nouveaux fichiers
- 1 modif minimale `app/journey/page.tsx`
- npm checks clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/journey/page.tsx
@components/bonus-claim-form.tsx
@components/bonus-status-badge.tsx
@components/moscow-kanban.tsx
@app/actions.ts
@lib/types.ts
@lib/i18n.ts
@lib/announcements.ts

<interfaces>
<!-- Pattern app/journey/page.tsx (server component) -->

```typescript
import { redirect } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { getCurrentRole, getCurrentUser, pathForRole } from "@/lib/auth";
import { dictionaries } from "@/lib/i18n";
import { hasSupabaseEnv } from "@/lib/supabase-status";
import { createClient } from "@/utils/supabase/server";

export default async function MyPage({ params }: { params: Promise<{ ... }> }) {
  const { type } = await params; // Next 15 async params

  const user = await getCurrentUser();
  if (!user) redirect("/login");

  const role = await getCurrentRole();
  if (role !== "player") redirect(pathForRole(role));

  if (!hasSupabaseEnv()) {
    return <p>Demo mode — backend non configure.</p>;
  }

  // ... fetch via supabase, render
}
```

<!-- Helper pattern lib/announcements.ts -->

```typescript
export async function getAnnouncementsForPlayer(userId: string, limit = 5) {
  if (!hasSupabaseEnv()) return [];
  const supabase = await createClient();
  if (!supabase) return [];
  const { data, error } = await supabase
    .from("announcements")
    .select("...")
    .limit(limit);
  if (error) return [];
  return (data ?? []).map(mapRow);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer lib/bonus.ts + lib/moscow.ts (data fetchers)</name>
  <files>lib/bonus.ts, lib/moscow.ts</files>
  <read_first>
    - `lib/announcements.ts` (pattern getAnnouncementsForPlayer)
    - `lib/types.ts` (BonusEvent, MoscowCard types)
    - `lib/supabase-status.ts` (hasSupabaseEnv)
    - `utils/supabase/server.ts` (createClient)
  </read_first>
  <action>
**Créer `lib/bonus.ts`** :

```typescript
// T3X-EXPANSION wave 3 / plan 10 — Data fetcher for bonus_events.
// Server-only helper. Returns [] in demo mode (no Supabase env).
import { hasSupabaseEnv } from "@/lib/supabase-status";
import { createClient } from "@/utils/supabase/server";
import type { BonusEvent, BonusStatus, BonusType, MultiplierScope } from "@/lib/types";

type BonusEventRow = {
  id: string;
  project_id: string;
  type: BonusType;
  title: string;
  description: string;
  doc_url: string | null;
  status: BonusStatus;
  multiplier_factor: number | string;
  multiplier_scope: MultiplierScope;
  multiplier_consumed_at: string | null;
  claimed_at: string;
  claimed_by: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  feedback: string;
  created_at: string;
  updated_at: string;
};

function mapRow(row: BonusEventRow): BonusEvent {
  return {
    id: row.id,
    projectId: row.project_id,
    type: row.type,
    title: row.title,
    description: row.description,
    docUrl: row.doc_url,
    status: row.status,
    multiplierFactor:
      typeof row.multiplier_factor === "string" ? Number(row.multiplier_factor) : row.multiplier_factor,
    multiplierScope: row.multiplier_scope,
    multiplierConsumedAt: row.multiplier_consumed_at,
    claimedAt: row.claimed_at,
    claimedBy: row.claimed_by,
    reviewedBy: row.reviewed_by,
    reviewedAt: row.reviewed_at,
    feedback: row.feedback,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}

/**
 * Fetch all bonus_events for a player, ordered by claimed_at DESC.
 * Returns [] in demo mode or on error.
 * R1: caller decides what to display (badge qualitatif Player; multiplierFactor Mentor/GM).
 */
export async function getBonusEventsForPlayer(playerId: string): Promise<BonusEvent[]> {
  if (!hasSupabaseEnv()) return [];
  const supabase = await createClient();
  if (!supabase) return [];
  const { data, error } = await supabase
    .from("bonus_events")
    .select("*")
    .eq("project_id", playerId)
    .order("claimed_at", { ascending: false });
  if (error) return [];
  return (data ?? []).map((r) => mapRow(r as BonusEventRow));
}

/**
 * Fetch a single bonus_event by id (Mentor/GM review page).
 * RLS bonus_events_select gates : Mentor + GM voient tout.
 */
export async function getBonusEventById(id: string): Promise<BonusEvent | null> {
  if (!hasSupabaseEnv()) return null;
  const supabase = await createClient();
  if (!supabase) return null;
  const { data, error } = await supabase
    .from("bonus_events")
    .select("*")
    .eq("id", id)
    .maybeSingle();
  if (error || !data) return null;
  return mapRow(data as BonusEventRow);
}
```

**Créer `lib/moscow.ts`** :

```typescript
// T3X-EXPANSION wave 3 / plan 10 — Data fetcher for moscow_cards.
import { hasSupabaseEnv } from "@/lib/supabase-status";
import { createClient } from "@/utils/supabase/server";
import type { MoscowBucket, MoscowCard } from "@/lib/types";

type MoscowCardRow = {
  id: string;
  project_id: string;
  deliverable_template_id: string;
  bucket: MoscowBucket;
  ord: number;
  feature: string;
  pourquoi: string;
  contrainte: string;
  created_by: string | null;
  created_at: string;
  updated_at: string;
};

function mapRow(row: MoscowCardRow): MoscowCard {
  return {
    id: row.id,
    projectId: row.project_id,
    deliverableTemplateId: row.deliverable_template_id,
    bucket: row.bucket,
    ord: row.ord,
    feature: row.feature,
    pourquoi: row.pourquoi,
    contrainte: row.contrainte,
    createdBy: row.created_by,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}

/**
 * Fetch all moscow_cards for (player, deliverable_template), ordered by (bucket, ord).
 * Returns [] in demo mode or on error.
 */
export async function getMoscowCardsForPlayerDeliverable(
  playerId: string,
  deliverableTemplateId: string,
): Promise<MoscowCard[]> {
  if (!hasSupabaseEnv()) return [];
  const supabase = await createClient();
  if (!supabase) return [];
  const { data, error } = await supabase
    .from("moscow_cards")
    .select("*")
    .eq("project_id", playerId)
    .eq("deliverable_template_id", deliverableTemplateId)
    .order("bucket", { ascending: true })
    .order("ord", { ascending: true });
  if (error) return [];
  return (data ?? []).map((r) => mapRow(r as MoscowCardRow));
}
```

**Vérification** :
```powershell
npm run typecheck
npm run lint
# Attendu : exit 0 chacun
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path lib/bonus.ts
      # Attendu : True
      Test-Path lib/moscow.ts
      # Attendu : True
      (Select-String -Pattern "export async function getBonusEventsForPlayer" lib/bonus.ts).Count
      # Attendu : 1
      (Select-String -Pattern "export async function getBonusEventById" lib/bonus.ts).Count
      # Attendu : 1
      (Select-String -Pattern "export async function getMoscowCardsForPlayerDeliverable" lib/moscow.ts).Count
      # Attendu : 1
      npm run typecheck
      # Attendu : exit 0
    </automated>
  </acceptance_criteria>
  <done>
- lib/bonus.ts (2 helpers) créé
- lib/moscow.ts (1 helper) créé
- snake_case → camelCase mapping
- Dual-mode safe (return [] si demo)
- npm checks clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer app/journey/bonus/[type]/page.tsx (Player claim route)</name>
  <files>app/journey/bonus/[type]/page.tsx</files>
  <read_first>
    - `app/journey/page.tsx` (pattern server component)
    - `components/bonus-claim-form.tsx` + `components/bonus-status-badge.tsx` (Plan 08)
    - `lib/bonus.ts` (créé Task 1)
    - `lib/types.ts` (BonusType + BONUS_DEFAULTS)
  </read_first>
  <action>
**Créer `app/journey/bonus/[type]/page.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 10 — Player bonus claim route.
// /journey/bonus/[type] — params.type ∈ BonusType.
// Auth-gated, role 'player'. Render BonusClaimForm + history of claims for this type.
import { redirect, notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { BonusClaimForm } from "@/components/bonus-claim-form";
import { BonusStatusBadge } from "@/components/bonus-status-badge";
import { getCurrentRole, getCurrentUser, pathForRole } from "@/lib/auth";
import { getBonusEventsForPlayer } from "@/lib/bonus";
import { dictionaries } from "@/lib/i18n";
import { hasSupabaseEnv } from "@/lib/supabase-status";
import { BONUS_DEFAULTS, type BonusType } from "@/lib/types";
import { createClient } from "@/utils/supabase/server";

const t = dictionaries.fr;

const VALID_BONUS_TYPES: BonusType[] = [
  "bonus_verbatims_terrain",
  "bonus_dev_plan",
  "bonus_prototype_draft",
];

export default async function BonusClaimPage({
  params,
}: {
  params: Promise<{ type: string }>;
}) {
  const { type: rawType } = await params;
  if (!VALID_BONUS_TYPES.includes(rawType as BonusType)) {
    notFound();
  }
  const bonusType = rawType as BonusType;

  const user = await getCurrentUser();
  if (!user) redirect("/login");
  const role = await getCurrentRole();
  if (role && role !== "player") redirect(pathForRole(role));

  if (!hasSupabaseEnv()) {
    return (
      <AppShell role="player" variant="player">
        <main style={{ padding: 32, maxWidth: 720, margin: "0 auto" }}>
          <h1>{BONUS_DEFAULTS[bonusType].titleFr}</h1>
          <p>{t.onboarding_demo_disabled}</p>
        </main>
      </AppShell>
    );
  }

  const supabase = await createClient();
  let history: Awaited<ReturnType<typeof getBonusEventsForPlayer>> = [];
  if (supabase) {
    const { data: membership } = await supabase
      .from("player_members")
      .select("player_id")
      .eq("user_id", user.id)
      .maybeSingle();
    if (membership?.player_id) {
      const all = await getBonusEventsForPlayer(membership.player_id);
      history = all.filter((b) => b.type === bonusType);
    }
  }

  return (
    <AppShell role="player" variant="player">
      <main style={{ padding: 32, maxWidth: 720, margin: "0 auto" }}>
        <h1 style={{ margin: 0, fontSize: 24, fontWeight: 700 }}>
          {BONUS_DEFAULTS[bonusType].titleFr}
        </h1>
        <p style={{ marginTop: 8, fontSize: 14, color: "#475569" }}>
          {t.bonus_section_subtitle}
        </p>

        <section style={{ marginTop: 24 }}>
          <h2 style={{ fontSize: 18, fontWeight: 600 }}>{t.bonus_claim_submit}</h2>
          <BonusClaimForm bonusType={bonusType} defaultTitle={BONUS_DEFAULTS[bonusType].titleFr} />
        </section>

        {history.length > 0 ? (
          <section style={{ marginTop: 32 }}>
            <h2 style={{ fontSize: 18, fontWeight: 600 }}>Historique</h2>
            <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: 8 }}>
              {history.map((b) => (
                <li
                  key={b.id}
                  style={{
                    display: "flex",
                    alignItems: "center",
                    gap: 8,
                    padding: 12,
                    border: "1px solid #e2e8f0",
                    borderRadius: 8,
                  }}
                >
                  <span style={{ flex: 1, fontSize: 14, color: "#0f172a" }}>{b.title}</span>
                  <BonusStatusBadge status={b.status} consumedAt={b.multiplierConsumedAt} />
                </li>
              ))}
            </ul>
          </section>
        ) : null}
      </main>
    </AppShell>
  );
}
```

**Vérifications R1 STRICT** :
- `b.multiplierFactor` JAMAIS render (le badge consume seulement status + consumedAt).
- Aucun toFixed, aucun /100, aucun chiffre brut.
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path app/journey/bonus/[type]/page.tsx
      # Attendu : True
      npm run typecheck
      # Attendu : exit 0
      npm run lint
      # Attendu : exit 0
      Select-String -Pattern "multiplier|score|/100|toFixed" app/journey/bonus/[type]/page.tsx
      # Attendu : 0 hit (R1)
    </automated>
  </acceptance_criteria>
  <done>
- Route Player créée
- Auth-gated role 'player'
- Dual-mode safe
- BonusClaimForm + BonusStatusBadge consommés
- R1 STRICT
  </done>
</task>

<task type="auto">
  <name>Task 3: Créer app/mentor/bonus/[id]/page.tsx (Mentor review route)</name>
  <files>app/mentor/bonus/[id]/page.tsx</files>
  <read_first>
    - `lib/bonus.ts` getBonusEventById (Task 1)
    - `app/actions.ts` reviewBonusEventFlow (Plan 06)
    - Pattern existant page Mentor (s'il y a `app/mentor/submission/[id]/page.tsx`)
  </read_first>
  <action>
**Créer `app/mentor/bonus/[id]/page.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 10 — Mentor bonus review route.
// /mentor/bonus/[id] — auth-gated role 'mentor' or 'game_master'.
// Display claim details + form to validate/reject + feedback.
import { redirect, notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { BonusStatusBadge } from "@/components/bonus-status-badge";
import { reviewBonusEventFlow } from "@/app/actions";
import { getCurrentRole, getCurrentUser, pathForRole } from "@/lib/auth";
import { getBonusEventById } from "@/lib/bonus";
import { dictionaries } from "@/lib/i18n";
import { hasSupabaseEnv } from "@/lib/supabase-status";

const t = dictionaries.fr;

export default async function MentorBonusReviewPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;

  const user = await getCurrentUser();
  if (!user) redirect("/login");
  const role = await getCurrentRole();
  if (role !== "mentor" && role !== "game_master") redirect(pathForRole(role));

  if (!hasSupabaseEnv()) {
    return (
      <AppShell role="mentor" variant="mentor">
        <main style={{ padding: 32 }}>
          <p>{t.onboarding_demo_disabled}</p>
        </main>
      </AppShell>
    );
  }

  const bonus = await getBonusEventById(id);
  if (!bonus) notFound();

  return (
    <AppShell role="mentor" variant="mentor">
      <main style={{ padding: 32, maxWidth: 720, margin: "0 auto" }}>
        <header style={{ marginBottom: 24 }}>
          <h1 style={{ margin: 0, fontSize: 24, fontWeight: 700 }}>{t.bonus_review_title}</h1>
          <BonusStatusBadge status={bonus.status} consumedAt={bonus.multiplierConsumedAt} />
        </header>

        <dl style={{ display: "grid", gridTemplateColumns: "150px 1fr", gap: 8, fontSize: 14 }}>
          <dt style={{ fontWeight: 600 }}>Type</dt>
          <dd style={{ margin: 0 }}>{bonus.type}</dd>
          <dt style={{ fontWeight: 600 }}>Titre</dt>
          <dd style={{ margin: 0 }}>{bonus.title}</dd>
          <dt style={{ fontWeight: 600 }}>Description</dt>
          <dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{bonus.description}</dd>
          <dt style={{ fontWeight: 600 }}>Preuve</dt>
          <dd style={{ margin: 0 }}>
            {bonus.docUrl ? (
              <a href={bonus.docUrl} target="_blank" rel="noopener noreferrer">
                {bonus.docUrl}
              </a>
            ) : (
              "(aucune)"
            )}
          </dd>
          <dt style={{ fontWeight: 600 }}>Soumis le</dt>
          <dd style={{ margin: 0 }}>{bonus.claimedAt}</dd>
        </dl>

        {bonus.status === "submitted" ? (
          <form action={reviewBonusEventFlow as never} style={{ marginTop: 32, display: "flex", flexDirection: "column", gap: 12 }}>
            <input type="hidden" name="bonusEventId" value={bonus.id} />
            <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 14 }}>
              <span style={{ fontWeight: 600 }}>{t.bonus_review_feedback_label}</span>
              <textarea name="feedback" rows={4} maxLength={2000} style={{ padding: 8, border: "1px solid #cbd5e1", borderRadius: 6 }} />
            </label>
            <div style={{ display: "flex", gap: 8 }}>
              <button type="submit" name="decision" value="validated" style={{ padding: "10px 16px", background: "#15803d", color: "#fff", border: "none", borderRadius: 6, fontWeight: 600 }}>
                {t.bonus_review_validate}
              </button>
              <button type="submit" name="decision" value="rejected" style={{ padding: "10px 16px", background: "#b91c1c", color: "#fff", border: "none", borderRadius: 6, fontWeight: 600 }}>
                {t.bonus_review_reject}
              </button>
            </div>
          </form>
        ) : (
          <p style={{ marginTop: 24, fontSize: 14, color: "#475569" }}>
            Bonus deja review. Feedback : {bonus.feedback || "(aucun)"}
          </p>
        )}
      </main>
    </AppShell>
  );
}
```

**Note technique** : `form action={reviewBonusEventFlow as never}` — TypeScript peut grogner sur le type FormAction signature. Si erreur, créer un mini wrapper client component qui gère via `useActionState`. Pour T-3 simple : utiliser le simple `<form action={action}>` natif Next.js 15 (qui appelle directement la server action sans WorkflowState merging — l'output est ignoré, on revalidatePath et redirect côté server).

**Si typecheck échoue sur la signature** : créer un mini composant client `<MentorBonusReviewForm bonusId={id} />` qui wrap `useActionState(reviewBonusEventFlow, ...)`. Mais essayer d'abord la version simple direct form action.
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path app/mentor/bonus/[id]/page.tsx
      # Attendu : True
      npm run typecheck
      # Attendu : exit 0 (si typescript grogne sur form action signature, pivot vers wrapper client component)
      npm run lint
      # Attendu : exit 0
      (Select-String -Pattern "reviewBonusEventFlow" app/mentor/bonus/[id]/page.tsx).Count
      # Attendu : >= 1
    </automated>
  </acceptance_criteria>
  <done>
- Route Mentor créée
- gate role mentor/game_master
- Dual-mode safe
- Form review (validate/reject + feedback)
  </done>
</task>

<task type="auto">
  <name>Task 4: Créer app/journey/deliverable/[id]/moscow-snapshot/page.tsx (snapshot SSR proof_url)</name>
  <files>app/journey/deliverable/[id]/moscow-snapshot/page.tsx</files>
  <read_first>
    - `lib/moscow.ts` getMoscowCardsForPlayerDeliverable (Task 1)
    - `app/journey/page.tsx` pattern
  </read_first>
  <action>
**Créer `app/journey/deliverable/[id]/moscow-snapshot/page.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 10 — MoSCoW snapshot SSR.
// Consume comme proof_url par submitMoscowDeliverableFlow (Plan 06).
// URL : /journey/deliverable/[id]/moscow-snapshot?p=<playerId>
// RLS moscow_cards_select gates : Player owner OU Mentor.
import { notFound } from "next/navigation";
import { getMoscowCardsForPlayerDeliverable } from "@/lib/moscow";
import { dictionaries } from "@/lib/i18n";
import type { MoscowBucket, MoscowCard } from "@/lib/types";

const t = dictionaries.fr;

const BUCKETS: { id: MoscowBucket; label: string }[] = [
  { id: "must", label: "Must" },
  { id: "should", label: "Should" },
  { id: "could", label: "Could" },
  { id: "wont", label: "Won't" },
];

export default async function MoscowSnapshotPage({
  params,
  searchParams,
}: {
  params: Promise<{ id: string }>;
  searchParams: Promise<{ p?: string }>;
}) {
  const { id: deliverableTemplateId } = await params;
  const { p: playerId } = await searchParams;

  if (!playerId) notFound();

  const cards = await getMoscowCardsForPlayerDeliverable(playerId, deliverableTemplateId);

  const grouped: Record<MoscowBucket, MoscowCard[]> = {
    must: [],
    should: [],
    could: [],
    wont: [],
  };
  for (const c of cards) grouped[c.bucket].push(c);
  for (const b of Object.keys(grouped) as MoscowBucket[]) {
    grouped[b].sort((a, b2) => a.ord - b2.ord);
  }

  return (
    <main style={{ padding: 32, maxWidth: 960, margin: "0 auto", fontFamily: "system-ui, sans-serif" }}>
      <h1 style={{ margin: 0, fontSize: 22, fontWeight: 700 }}>{t.moscow_kanban_title} — Snapshot</h1>
      <p style={{ marginTop: 8, fontSize: 13, color: "#475569" }}>
        Snapshot lecture seule des cartes priorisees par l'equipe. Source de verite : table public.moscow_cards.
      </p>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 12, marginTop: 24 }}>
        {BUCKETS.map((b) => (
          <section
            key={b.id}
            style={{
              background: "#f8fafc",
              border: "1px solid #e2e8f0",
              borderRadius: 8,
              padding: 12,
              minHeight: 120,
            }}
          >
            <h2 style={{ margin: "0 0 8px", fontSize: 16 }}>
              {b.label} <span style={{ color: "#94a3b8", fontSize: 12 }}>({grouped[b.id].length})</span>
            </h2>
            <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: 6 }}>
              {grouped[b.id].map((c) => (
                <li
                  key={c.id}
                  style={{
                    background: "#ffffff",
                    border: "1px solid #cbd5e1",
                    borderRadius: 6,
                    padding: 8,
                    fontSize: 13,
                  }}
                >
                  <strong>{c.feature}</strong>
                  {c.pourquoi ? (
                    <p style={{ margin: "4px 0 0", color: "#475569" }}>{c.pourquoi}</p>
                  ) : null}
                  {c.contrainte ? (
                    <p style={{ margin: "4px 0 0", color: "#475569", fontStyle: "italic" }}>
                      Contrainte : {c.contrainte}
                    </p>
                  ) : null}
                </li>
              ))}
              {grouped[b.id].length === 0 ? (
                <li style={{ fontSize: 12, color: "#94a3b8" }}>(aucune carte)</li>
              ) : null}
            </ul>
          </section>
        ))}
      </div>
    </main>
  );
}
```

**Note** : pas de `<AppShell>` ici intentionnellement — c'est un snapshot proof, pas une page navigation. Layout minimaliste.
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path app/journey/deliverable/[id]/moscow-snapshot/page.tsx
      # Attendu : True
      npm run typecheck
      # Attendu : exit 0
      Select-String -Pattern "score|rank|/100" app/journey/deliverable/[id]/moscow-snapshot/page.tsx
      # Attendu : 0 hit (R1)
    </automated>
  </acceptance_criteria>
  <done>
- Route snapshot créée
- 4 colonnes lecture seule
- Pas d'AppShell (proof-only)
- R1 STRICT
  </done>
</task>

<task type="auto">
  <name>Task 5: Modifier app/journey/page.tsx — ajouter section "Bonus disponibles"</name>
  <files>app/journey/page.tsx</files>
  <read_first>
    - `app/journey/page.tsx` (lignes 100-120 où sont les compositions JSX existantes — JourneyClient, CohortPulse)
    - `lib/types.ts` BonusType + BONUS_DEFAULTS
    - `lib/i18n.ts` keys bonus_section_*
  </read_first>
  <action>
**Modification minimale** : ajouter une section discrète liens vers `/journey/bonus/[type]` après le `<JourneyClient>` (ou avant — au choix Omar).

**Localisation** : dans le `return` final (ligne ~106-119), ajouter une `<section>` après `<JourneyClient>`.

**Avant** (ligne ~106-119) :
```tsx
  return (
    <AppShell role="player" variant="player">
      <PlayerAnnouncementStrip announcements={announcements} />
      <CohortPulse entries={cohortPulse} />
      <JourneyClient
        currentLevel={data.player.currentLevel}
        hero={hero}
        levelStateEntries={levelStateEntries}
        missions={data.missions}
        objectivesByLevel={{}}
        totalEarnedXp={totalEarnedXp}
      />
    </AppShell>
  );
```

**Après** :
```tsx
  return (
    <AppShell role="player" variant="player">
      <PlayerAnnouncementStrip announcements={announcements} />
      <CohortPulse entries={cohortPulse} />
      <JourneyClient
        currentLevel={data.player.currentLevel}
        hero={hero}
        levelStateEntries={levelStateEntries}
        missions={data.missions}
        objectivesByLevel={{}}
        totalEarnedXp={totalEarnedXp}
      />
      {/* T3X-EXPANSION wave 3 / plan 10 — Bonus rail (D-02 / D-03). R3 : optionnel. */}
      <section style={{ padding: "16px 32px", maxWidth: 960, margin: "0 auto" }}>
        <h2 style={{ margin: 0, fontSize: 18, fontWeight: 700 }}>{t.bonus_section_title}</h2>
        <p style={{ margin: "4px 0 16px", fontSize: 13, color: "#475569" }}>
          {t.bonus_section_subtitle}
        </p>
        <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 12 }}>
          {(["bonus_verbatims_terrain", "bonus_dev_plan", "bonus_prototype_draft"] as const).map((type) => (
            <li key={type}>
              <a
                href={`/journey/bonus/${type}`}
                style={{
                  display: "block",
                  padding: 12,
                  background: "#f8fafc",
                  border: "1px solid #e2e8f0",
                  borderRadius: 8,
                  textDecoration: "none",
                  color: "#0f172a",
                  fontSize: 14,
                  fontWeight: 600,
                }}
              >
                {t[`bonus_card_${type.replace("bonus_", "")}` as keyof typeof t] as string}
              </a>
            </li>
          ))}
        </ul>
      </section>
    </AppShell>
  );
```

**IMPORTANT** :
- Le `t[\`bonus_card_${type.replace("bonus_", "")}\` as keyof typeof t]` accède dynamiquement aux keys i18n. Si TypeScript grogne, utiliser un `Record<BonusType, string>` typed map pour la sécurité.
- R3 : c'est un lien, pas un blocage. Player peut ignorer.
- R1 : aucun chiffre.
- ASCII pur dans le code TS.

**Vérification** :
```powershell
git diff app/journey/page.tsx
# Attendu : ~25-30 lignes ajoutées (la nouvelle section), 0 deletion (pas de modif des existants)

Select-String -Pattern "/journey/bonus/" app/journey/page.tsx
# Attendu : 1 hit (la href)
```

**Note D-04 entry point** : aucune tuile MoSCoW Kanban n'est ajoutée au rail journey — le Player accède au Kanban naturellement via la carte deliverable #4 (slug fiche-produit-plan-dev-v1) qui est déjà visible dans le rail existant. Plan 10 Task 6+7 surfacent <MoscowKanban> directement dans la page deliverable. R3 préservé : zero blocage cross-mission ajouté.
  </action>
  <acceptance_criteria>
    <automated>
      (Select-String -Pattern "/journey/bonus/" app/journey/page.tsx).Count
      # Attendu : 1
      (Select-String -Pattern "bonus_section_title" app/journey/page.tsx).Count
      # Attendu : 1
      npm run typecheck
      # Attendu : exit 0
      npm run lint
      # Attendu : exit 0
    </automated>
  </acceptance_criteria>
  <done>
- Section bonus rail ajoutée à app/journey/page.tsx
- 3 liens vers /journey/bonus/[type]
- Modification minimale (additive only)
- npm checks clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 6 (advisor-audit): R1/R2/R3 audit avant integration MoscowKanban dans deliverable page</name>
  <files>(audit only — no edit)</files>
  <action>Spawn `eic-pedagogical-advisor` agent (cf .claude/agents/eic-pedagogical-advisor.md). Si sandbox-limited : Omar performs inline R1/R2/R3 manual checklist sur le diff prévisionnel (R1 = MoscowKanban ne render aucun score/rank/multiplier numérique ; R2 = handleAddCard utilise WorkflowState.message warn-only ; R3 = aucun DOM disabled, aucun blocage cross-mission, le bouton submit livrable reste cliquable même si 0 carte).</action>
  <verify>
    <automated>echo "Advisor verdict OK / fallback inline"</automated>
  </verify>
  <done>Verdict APPROVED OU corrections listées par advisor/Omar appliquées avant Task 7.</done>
  <resume-signal>Type "approved" pour passer à Task 7</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Wire <MoscowKanban> dans app/journey/deliverable/[id]/page.tsx</name>
  <files>app/journey/deliverable/[id]/page.tsx</files>
  <read_first>
- app/journey/deliverable/[id]/page.tsx (current state — understand existing layout + ProofWorkflow integration)
- components/moscow-kanban.tsx (Plan 09 deliverable — props shape: { initialCards, deliverableTemplateId, playerId, locale })
- lib/moscow.ts (Plan 10 Task 1 — getMoscowCardsForPlayer helper)
- components/proof-workflow.tsx (existing pattern — preserve in fallback)
  </read_first>
  <action>
**Modification ciblée** : ajouter un rendu conditionnel `<MoscowKanban>` dans `app/journey/deliverable/[id]/page.tsx`.

**Étapes :**

1. Au top du fichier, ajouter imports :
   ```ts
   import { MoscowKanban } from "@/components/moscow-kanban";
   import { getMoscowCardsForPlayer } from "@/lib/moscow";
   ```

2. Dans le server component default export, après avoir resolved `template` (DeliverableTemplate) et `currentPlayer` (Player), AVANT le rendu existant :
   ```ts
   const moscowCards = template.slug === "fiche-produit-plan-dev-v1" && currentPlayer
     ? await getMoscowCardsForPlayer(currentPlayer.id, template.id)
     : null;
   ```

3. Dans le JSX, AVANT le `<ProofWorkflow>` existant (ou la zone preuve actuelle), ajouter :
   ```tsx
   {template.slug === "fiche-produit-plan-dev-v1" && currentPlayer && moscowCards !== null && (
     <section className="rounded-lg border border-slate-200 bg-white p-6">
       <h2 className="text-lg font-semibold mb-4">{t.moscow_kanban_section_title}</h2>
       <MoscowKanban
         initialCards={moscowCards}
         deliverableTemplateId={template.id}
         playerId={currentPlayer.id}
         locale={locale}
       />
       <p className="mt-4 text-sm text-slate-600">{t.moscow_kanban_helper_text}</p>
     </section>
   )}
   ```

4. **NE PAS retirer** le `<ProofWorkflow>` existant — il reste comme fallback (R3 préservé).

5. Ajouter 2 clés FR dans `lib/i18n.ts` (si pas déjà ajoutées par Plan 06) :
   - `moscow_kanban_section_title`: "Tableau MoSCoW (Must / Should / Could / Won't)"
   - `moscow_kanban_helper_text`: "Glissez-déposez les cartes pour réorganiser. Le snapshot est généré automatiquement à la soumission."

**Validation R1** : grep post-edit doit retourner 0 hit pour score/rank/multiplier dans la page modifiée.
  </action>
  <verify>
    <automated>
grep -n "MoscowKanban" app/journey/deliverable/\[id\]/page.tsx
# Doit retourner 2 hits (import + usage)
grep -nE "score|rank|/100|/140|toFixed|multiplierFactor" app/journey/deliverable/\[id\]/page.tsx || echo R1-OK
npm run typecheck
npm run build
    </automated>
  </verify>
  <done>
- MoscowKanban surfacé conditionnellement sur slug `fiche-produit-plan-dev-v1`
- ProofWorkflow fallback préservé (R3 OK)
- npm run build passe
- Audit R1 grep clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 8: Audit R1/R2/R3 + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --cached`
    - `.claude/agents/eic-pedagogical-advisor.md`
  </read_first>
  <action>
**Pré-checkpoint** : Tasks 1-7 complétées, npm clean.

**Action Claude post-approval** :
```bash
git add lib/bonus.ts lib/moscow.ts \
  app/journey/bonus/[type]/page.tsx \
  app/mentor/bonus/[id]/page.tsx \
  app/journey/deliverable/[id]/moscow-snapshot/page.tsx \
  app/journey/deliverable/[id]/page.tsx \
  app/journey/page.tsx
git commit -m "$(cat <<'EOF'
feat(t3x-routes): add bonus + moscow routes Player/Mentor + journey rail + Kanban surfacing

Wave 3 / plan 10 : surface UI complete des routes bonus events et MoSCoW
snapshot. Pattern aligne sur app/journey/page.tsx (server components,
hasSupabaseEnv check, role gate, redirect).

- lib/bonus.ts : getBonusEventsForPlayer + getBonusEventById (snake_case map)
- lib/moscow.ts : getMoscowCardsForPlayerDeliverable
- app/journey/bonus/[type]/page.tsx : Player claim form + history (badges qualitatifs)
- app/mentor/bonus/[id]/page.tsx : Mentor review form (validate/reject + feedback)
- app/journey/deliverable/[id]/moscow-snapshot/page.tsx : SSR snapshot consume
  comme proof_url par submitMoscowDeliverableFlow (lecture seule, no AppShell)
- app/journey/page.tsx : ajout section bonus rail (3 liens vers /journey/bonus/[type])
- app/journey/deliverable/[id]/page.tsx : surfacing <MoscowKanban> conditionnel
  sur template.slug === 'fiche-produit-plan-dev-v1' (BLOCKER revision-checker #2)
  ProofWorkflow fallback preserve (R3).

R1 STRICT : grep score|/100|multiplier sur les nouveaux fichiers = 0 hit.
R3 : bonus = optionnel (lien rail, pas blocage). MoSCoW snapshot = lecture seule.
MoSCoW Kanban surfacing = conditional rendering, ProofWorkflow reste accessible.

npm typecheck OK, npm lint OK, npm build OK.

Refs : BRIEF §2.A (Player claim + Mentor review), §2.B (MoSCoW snapshot URL),
revision 2026-05-10 (Task 6+7 surfacing fix), CLAUDE.md cardinal rules.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-routes"
      # Attendu : 1 hit
      git diff --name-only HEAD~1 HEAD
      # Attendu : 7 fichiers
      Select-String -Pattern "score|/100|/140|toFixed|multiplierFactor" app/journey/bonus/[type]/page.tsx, app/mentor/bonus/[id]/page.tsx, app/journey/deliverable/[id]/moscow-snapshot/page.tsx
      # Attendu : 0 hit (R1 STRICT sur le Player + snapshot routes)
      # Note : app/mentor/bonus/[id]/page.tsx peut afficher le multiplier en v0.3 ; à T-3 il ne l'expose pas.
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → commit. Sinon : décrire issues.</done>
  <what-built>
- 6 nouveaux fichiers (2 lib + 3 routes + 1 surfacing)
- 1 modif minimale journey rail
- R1/R3 préservés
- Dual-mode safe
- MoscowKanban surfacé (résout BLOCKER revision-checker #2)
  </what-built>
  <how-to-verify>
**Audit checklist** :

1. **R1 grep STRICT (Player + snapshot)** :
   ```powershell
   Select-String -Pattern "multiplier|score|rank|/100|/140|toFixed|points" app/journey/bonus/[type]/page.tsx, app/journey/deliverable/[id]/moscow-snapshot/page.tsx, app/journey/deliverable/[id]/page.tsx
   ```
   Attendu : 0 hit.

2. **R3 grep** :
   ```powershell
   Select-String -Pattern "blocks_progression|disabled.*because" app/journey/page.tsx, app/journey/bonus/[type]/page.tsx, app/journey/deliverable/[id]/page.tsx
   ```
   Attendu : 0 hit.

3. **Dual-mode safe** :
   ```powershell
   Select-String -Pattern "hasSupabaseEnv" app/journey/bonus/[type]/page.tsx, app/mentor/bonus/[id]/page.tsx
   ```
   Attendu : >= 2 hits (les 2 routes Player/Mentor checkent demo mode).

4. **Role gates** :
   ```powershell
   Select-String -Pattern "redirect.*pathForRole|role !== \"player\"|role !== \"mentor\"" app/journey/bonus/[type]/page.tsx, app/mentor/bonus/[id]/page.tsx
   ```
   Attendu : >= 2 hits.

5. **Snapshot pas dans AppShell** :
   ```powershell
   Select-String -Pattern "AppShell" app/journey/deliverable/[id]/moscow-snapshot/page.tsx
   ```
   Attendu : 0 hit (snapshot = proof minimaliste sans nav).

6. **Kanban surfaced** :
   ```powershell
   Select-String -Pattern "MoscowKanban" app/journey/deliverable/[id]/page.tsx
   ```
   Attendu : >= 2 hits (import + usage JSX).
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "ajouter AppShell snapshot", "renommer route /journey/bonus/[type]").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **TS health** :
   ```powershell
   npm run typecheck ; npm run lint
   # Attendu : exit 0
   ```

2. **R1 STRICT Player-facing** :
   ```powershell
   Select-String -Pattern "multiplier|score|/100" app/journey/bonus/[type]/page.tsx, app/journey/deliverable/[id]/moscow-snapshot/page.tsx, app/journey/page.tsx, app/journey/deliverable/[id]/page.tsx
   # Attendu : 0 hit
   ```

3. **MoscowKanban surfaced** :
   ```powershell
   (Select-String -Pattern "MoscowKanban" app/journey/deliverable/[id]/page.tsx).Count
   # Attendu : >= 2
   ```
</verification>

<success_criteria>
- [ ] lib/bonus.ts + lib/moscow.ts (helpers data)
- [ ] /journey/bonus/[type] route Player
- [ ] /mentor/bonus/[id] route Mentor
- [ ] /journey/deliverable/[id]/moscow-snapshot SSR
- [ ] app/journey/page.tsx ajoute section bonus rail (3 liens)
- [ ] app/journey/deliverable/[id]/page.tsx surface <MoscowKanban> conditionnel (Task 7)
- [ ] R1 STRICT (0 hit Player-facing)
- [ ] R3 OK (no blocking, ProofWorkflow fallback préservé)
- [ ] Dual-mode safe
- [ ] npm checks clean
- [ ] Commit atomique
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-10-SUMMARY.md` :

Sections :
1. **6 nouveaux fichiers + 1 modif** : bonus claim + mentor review + moscow snapshot + 2 lib helpers + journey rail + Kanban surfacing
2. **R1 STRICT Player** : grep clean
3. **Dual-mode preserved** : hasSupabaseEnv check sur chaque route
4. **MoSCoW snapshot URL** : consume by submitMoscowDeliverableFlow comme proof_url
5. **MoSCoW Kanban surfacing** : conditional rendering dans deliverable page (résout BLOCKER revision #2)
6. **Files modified** : 7
7. **Next consumers** : Plan 11 (CSV export GM), Plan 12 (smoke E2E full)
</output>
</output>
