---
phase: "12-quick-260510-t3x"
plan: "12-02"
type: execute
wave: 1
depends_on:
  - "12-01"
files_modified:
  - supabase/migrations/20260510170000_bonus_events_recreate.sql
  - database/bonus_events.sql
autonomous: false
requirements:
  - T3X-EXPANSION-BONUS-EVENTS-SCHEMA
must_haves:
  truths:
    - "Table public.bonus_events existe avec colonnes id, project_id (uuid -> players.id), type (enum bonus_type), title, description, doc_url, status (enum bonus_status), multiplier_factor (numeric(3,2)), multiplier_scope (enum multiplier_scope), multiplier_consumed_at (timestamptz nullable), claimed_at, reviewed_by, reviewed_at, created_at, updated_at"
    - "Enum public.bonus_type contient exactement 3 valeurs : bonus_verbatims_terrain, bonus_dev_plan, bonus_prototype_draft"
    - "Enum public.bonus_status contient exactement 4 valeurs : draft, submitted, validated, rejected"
    - "Enum public.multiplier_scope contient exactement 2 valeurs : next_deliverable, rest_of_event"
    - "RLS bonus_events_select : SELECT autorisé pour membres du player (is_my_player) OU mentor (is_mentor) — Player ne voit pas les bonus_events des autres équipes"
    - "RLS bonus_events_player_insert : INSERT autorisé pour membres du player ET project_id = player du user, status doit être 'submitted'"
    - "RLS bonus_events_mentor_update : UPDATE autorisé uniquement pour is_mentor() (validation/rejet)"
    - "Trigger trg_bonus_events_updated_at maintient updated_at automatiquement"
    - "Re-application de la migration est idempotente : create type IF NOT EXISTS via DO block, create table IF NOT EXISTS, ON CONFLICT DO UPDATE pour seed des 3 types"
    - "Aucune colonne stockant un score numérique exposable Player (R1 préservé) — multiplier_factor reste interne, présenté qualitativement côté Player UI"
    - "Schema preserve cardinal rule R1 : bonus_events ne stocke PAS rank, score_total, percentile"
    - "Mirror parfait `database/bonus_events.sql` <-> `supabase/migrations/20260510170000_bonus_events_recreate.sql`"
  artifacts:
    - path: "supabase/migrations/20260510170000_bonus_events_recreate.sql"
      provides: "Migration Supabase appliquable via supabase CLI : enums + table + RLS + trigger + 0 seed (seed des 3 types fait dans data section in-fine)"
      contains: "create table.*bonus_events|create policy.*bonus_events_select|multiplier_factor"
    - path: "database/bonus_events.sql"
      provides: "Mirror local pour psql -f apply (cohérent avec la convention database/<topic>.sql)"
      contains: "create table.*bonus_events|multiplier_factor"
  key_links:
    - from: "public.bonus_events.project_id"
      to: "public.players.id"
      via: "FK references public.players(id) on delete cascade"
      pattern: "references public\\.players\\(id\\) on delete cascade"
    - from: "RLS bonus_events_select policy"
      to: "public.is_my_player + public.is_mentor"
      via: "using clause appelle les helpers existants de database/rls.sql"
      pattern: "is_my_player|is_mentor"
    - from: "trg_bonus_events_updated_at"
      to: "public.set_updated_at"
      via: "trigger before update appelle la fonction commune définie dans database/triggers.sql"
      pattern: "execute function public\\.set_updated_at"
---

<objective>
Wave 1 — Recréer le schema `bonus_events` retiré en v0.2 (cf. CLAUDE.md "bonus XP rules removed during v0.2" / `lib/types.ts` ligne ~36 `Note: bonus XP rules removed during v0.2`). Décision D-02 : architecture propre dédiée (NOT flag-based sur deliverable_templates).

**Mécanisme bonus = MULTIPLIER (D-03)** :
- Une bonus_event validée définit un `multiplier_factor` (numeric 1.0 - 3.0) qui s'applique aux scores de submissions futures jusqu'à expiry (`multiplier_scope` = `next_deliverable` OR `rest_of_event`).
- Computation locus : `lib/score.ts` (Plan 07) lit les bonus_events validated, identifie celui applicable selon timestamps et scope, multiplie le score.
- R1 préservé : `multiplier_factor` est numeric — JAMAIS exposé Player en chiffre. Côté Player UI : badge qualitatif "Boost actif" (Plan 08).
- Cap global : 3.0× max par player (enforced dans lib/score.ts via Math.min). Pas de stacking : on prend le multiplier_factor max parmi les actifs.

**Scope plan** : SQL DDL pure. 1 fichier migration Supabase + 1 mirror local. PAS de TypeScript dans ce plan (Plan 05 fait les types). PAS de server actions (Plan 06). PAS de RLS Player insert sur reviewed_by/reviewed_at (réservé mentor-only — RLS update mentor).

**Output** :
- 1 migration Supabase appliquable
- 1 mirror database/bonus_events.sql
- Idempotency garantie (re-apply = no-op)
- 3 types seedés en fin de fichier via INSERT ON CONFLICT (DO UPDATE) avec valeurs de référence pour multiplier_factor (peuvent être surchargées par GameMaster ultérieurement, mais le seed garantit qu'un Player peut commencer à claimer dès apply)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/BRIEF.md
@database/schema.sql
@database/triggers.sql
@database/rls.sql
@supabase/migrations/20260510140001_phase09_gamemaster_live.sql

<interfaces>
<!-- Patterns à reproduire (lecture statique avant DDL) -->

From database/schema.sql (convention enums + table + unique + index) :
```sql
create type public.bonus_type as enum (...);  -- comme app_role, level_id
create type public.bonus_status as enum (...);  -- comme submission_status, verdict
create table public.bonus_events (...) ;
create index on public.bonus_events (project_id);
```

From database/triggers.sql (set_updated_at déjà défini, on l'attache simplement) :
```sql
create trigger trg_bonus_events_updated_at
  before update on public.bonus_events
  for each row execute function public.set_updated_at();
```

From database/rls.sql (helpers existants à réutiliser, NE PAS recréer) :
- `public.is_my_player(p_player_id uuid) returns boolean` — vérifie si auth.uid() est membre du player
- `public.is_mentor() returns boolean` — true si app_role IN ('mentor', 'game_master')
- `public.is_game_master() returns boolean` — true si app_role = 'game_master'

From supabase/migrations/20260510140001_phase09_gamemaster_live.sql (pattern idempotent migration applicable séparée) :
```sql
-- Apply manually after schema.sql -> triggers.sql -> rls.sql -> 08-mentor-comments.sql
-- Idempotent: safe to re-run.
alter table ... add column if not exists ...
create table if not exists ...
create index if not exists ... on ... where ...
```

Pattern enum idempotent (DO block) :
```sql
do $$ begin
  create type public.bonus_type as enum ('bonus_verbatims_terrain', 'bonus_dev_plan', 'bonus_prototype_draft');
exception when duplicate_object then null;
end $$;
```

Pattern policy idempotent :
```sql
drop policy if exists "bonus_events_select" on public.bonus_events;
create policy "bonus_events_select" on public.bonus_events
  for select to authenticated
  using (public.is_my_player(project_id) or public.is_mentor());
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer supabase/migrations/20260510170000_bonus_events_recreate.sql avec enums + table + index + RLS + trigger + seed 3 types</name>
  <files>supabase/migrations/20260510170000_bonus_events_recreate.sql</files>
  <read_first>
    - `database/schema.sql` (lignes 11-56 conventions enums, lignes 60-218 conventions tables)
    - `database/triggers.sql` (lignes 1-50 pattern set_updated_at + attachement triggers)
    - `database/rls.sql` (lignes 7-54 helpers, lignes 60-220 patterns policies, lignes 263-285 grants)
    - `supabase/migrations/20260510140001_phase09_gamemaster_live.sql` (pattern migration séparée idempotente)
  </read_first>
  <action>
**Créer le fichier `supabase/migrations/20260510170000_bonus_events_recreate.sql`** avec EXACTEMENT le contenu suivant (DDL complet, idempotent, ASCII pur dans les strings) :

```sql
-- =============================================================================
-- 20260510170000_bonus_events_recreate.sql
-- T3X-EXPANSION wave 1 / plan 02 — Recreate bonus_events (retire en v0.2).
-- Decision D-02 (BRIEF §4) : architecture dediee (NOT flag-based on deliverable_templates).
-- Mecanisme D-03 : multiplier sur scores futurs (1.0x..3.0x), scope next_deliverable
-- ou rest_of_event. R1 preserve : multiplier_factor numerique cote DB, JAMAIS expose
-- Player en chiffre (UI presente "Boost actif" qualitatif — cf plan 08).
--
-- Apply order :
--   schema.sql -> triggers.sql -> rls.sql -> seed_event_hackdays.sql ->
--   ... -> 20260510170000_bonus_events_recreate.sql (CETTE migration).
--
-- Idempotent : re-application = no-op (enums via DO block exception duplicate_object,
-- create table IF NOT EXISTS, drop policy IF EXISTS + create, ON CONFLICT DO UPDATE seed).
-- =============================================================================

-- -----------------------------------------------------------------------------
-- 1. Enums (mirror lib/types.ts BonusType / BonusStatus / MultiplierScope — Plan 05)
-- -----------------------------------------------------------------------------

do $$ begin
  create type public.bonus_type as enum (
    'bonus_verbatims_terrain',
    'bonus_dev_plan',
    'bonus_prototype_draft'
  );
exception when duplicate_object then null;
end $$;

do $$ begin
  create type public.bonus_status as enum (
    'draft',
    'submitted',
    'validated',
    'rejected'
  );
exception when duplicate_object then null;
end $$;

do $$ begin
  create type public.multiplier_scope as enum (
    'next_deliverable',
    'rest_of_event'
  );
exception when duplicate_object then null;
end $$;

-- -----------------------------------------------------------------------------
-- 2. Table public.bonus_events
--    project_id -> public.players(id) (cf. schema.sql, players = teams)
--    Pas de unique sur (project_id, type) intentionnellement : un Player peut
--    re-claim apres rejet. L'unicite "active claim" est geree dans lib/score.ts
--    en filtrant status='validated' + max(claimed_at).
-- -----------------------------------------------------------------------------

create table if not exists public.bonus_events (
  id uuid primary key default gen_random_uuid(),
  project_id uuid not null references public.players(id) on delete cascade,
  type public.bonus_type not null,
  title text not null,
  description text not null default '',
  doc_url text,
  status public.bonus_status not null default 'submitted',
  multiplier_factor numeric(3,2) not null default 1.50,
  multiplier_scope public.multiplier_scope not null default 'next_deliverable',
  multiplier_consumed_at timestamptz,
  claimed_at timestamptz not null default now(),
  claimed_by uuid references auth.users(id) on delete set null,
  reviewed_by uuid references auth.users(id) on delete set null,
  reviewed_at timestamptz,
  feedback text not null default '',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  check (multiplier_factor >= 1.00 and multiplier_factor <= 3.00),
  check (
    (status in ('draft','submitted') and reviewed_by is null and reviewed_at is null)
    or (status in ('validated','rejected') and reviewed_by is not null and reviewed_at is not null)
  )
);

comment on table public.bonus_events is
  'Bonus claim D-02/D-03 : Player soumet preuve URL, Mentor valide, multiplier applique aux scores futurs. R1 preserve.';

comment on column public.bonus_events.multiplier_factor is
  'Coefficient multiplicateur applique aux scores futurs si status=validated. Cap 3.0x. JAMAIS expose Player en chiffre (R1).';

comment on column public.bonus_events.multiplier_scope is
  'next_deliverable = consomme au 1er score post-claim. rest_of_event = persiste jusqu evenement.ends_at.';

comment on column public.bonus_events.multiplier_consumed_at is
  'Si scope=next_deliverable, timestamp ou le multiplier a ete applique a une submission validated.';

-- -----------------------------------------------------------------------------
-- 3. Indexes (hot FKs + filtre par status validated dans lib/score.ts)
-- -----------------------------------------------------------------------------

create index if not exists bonus_events_project_idx on public.bonus_events (project_id);
create index if not exists bonus_events_status_idx on public.bonus_events (status);
create index if not exists bonus_events_validated_active_idx
  on public.bonus_events (project_id, claimed_at)
  where status = 'validated' and multiplier_consumed_at is null;

-- -----------------------------------------------------------------------------
-- 4. Trigger updated_at (reuse public.set_updated_at de database/triggers.sql)
-- -----------------------------------------------------------------------------

drop trigger if exists trg_bonus_events_updated_at on public.bonus_events;
create trigger trg_bonus_events_updated_at
  before update on public.bonus_events
  for each row execute function public.set_updated_at();

-- -----------------------------------------------------------------------------
-- 5. RLS — Player voit ses bonus, Mentor/GM voient tous, Player insert ses bonus
-- -----------------------------------------------------------------------------

alter table public.bonus_events enable row level security;

drop policy if exists "bonus_events_select" on public.bonus_events;
create policy "bonus_events_select" on public.bonus_events
  for select to authenticated
  using (public.is_my_player(project_id) or public.is_mentor());

drop policy if exists "bonus_events_player_insert" on public.bonus_events;
create policy "bonus_events_player_insert" on public.bonus_events
  for insert to authenticated
  with check (
    (public.is_my_player(project_id)
     and claimed_by = auth.uid()
     and status = 'submitted'
     and reviewed_by is null
     and reviewed_at is null)
    or public.is_game_master()
  );

drop policy if exists "bonus_events_mentor_update" on public.bonus_events;
create policy "bonus_events_mentor_update" on public.bonus_events
  for update to authenticated
  using (public.is_mentor() or public.is_game_master())
  with check (public.is_mentor() or public.is_game_master());

drop policy if exists "bonus_events_gm_delete" on public.bonus_events;
create policy "bonus_events_gm_delete" on public.bonus_events
  for delete to authenticated
  using (public.is_game_master());

-- Grants explicites (cf rls.sql ligne 263-285)
grant select, insert, update, delete on public.bonus_events to authenticated;
grant select, insert, update, delete on public.bonus_events to service_role;

-- -----------------------------------------------------------------------------
-- 6. Seed : reference data des 3 types bonus avec multiplier_factor par defaut
--    Note : c'est une reference table-less (les types vivent en enum). Le seed
--    ici DOCUMENTE les multipliers de reference dans un commentaire SQL.
--    Les multiplier_factor par claim sont copies depuis ce mapping cote
--    server action claimBonusEventFlow (Plan 06) — pas dans le DDL.
--
--    Mapping de reference (D-03) :
--      bonus_verbatims_terrain  -> multiplier_factor 1.50, scope next_deliverable
--      bonus_dev_plan           -> multiplier_factor 1.50, scope next_deliverable
--      bonus_prototype_draft    -> multiplier_factor 2.00, scope next_deliverable
-- -----------------------------------------------------------------------------

-- (pas d'INSERT seed ici — les types vivent en enum, les claims naissent
--  via app/actions.ts:claimBonusEventFlow Plan 06.)

-- =============================================================================
-- End of 20260510170000_bonus_events_recreate.sql
-- =============================================================================
```

**Conventions à respecter** :
- Strings SQL en ASCII pur (pas d'accents) — cf. CLAUDE.md "avoid accented characters in code-resident strings". Les commentaires en français mais sans diacritiques.
- Enums via DO block + exception `duplicate_object` (idempotent re-apply).
- Table via `create table if not exists` (idempotent).
- Policies via `drop policy if exists ... ; create policy ...` (idempotent — recommandé Supabase).
- FK cascade `on delete cascade` sur project_id (cohérent avec submissions, evaluations qui cascade aussi sur player).
- FK `set null` sur claimed_by, reviewed_by (auth.users peut être supprimé sans casser bonus_events).
- CHECK constraints multiplier_factor [1.00..3.00] (defense-in-depth).
- CHECK constraint cohérence reviewed_by/reviewed_at vs status (status=validated OU rejected ⇒ reviewed_at non null).
- Trigger updated_at via `public.set_updated_at` (déjà défini dans database/triggers.sql, on attache pas redéfinit).

**NE PAS** :
- Stocker score brut, rank, percentile (R1).
- Ajouter unique (project_id, type) — un Player peut re-claim après rejet.
- Insérer des rows en fin de migration (les claims viennent du runtime).
- Modifier `database/schema.sql`, `database/triggers.sql`, `database/rls.sql` (freeze v0.3 SEED-001 pour ces 3 fichiers — on ajoute un fichier mirror séparé `database/bonus_events.sql` en Task 2).
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : True

      Select-String -Pattern "create type public\.bonus_type" supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : 1 hit

      (Select-String -Pattern "bonus_verbatims_terrain" supabase/migrations/20260510170000_bonus_events_recreate.sql).Count
      # Attendu : >= 1 (dans l'enum)

      (Select-String -Pattern "bonus_dev_plan" supabase/migrations/20260510170000_bonus_events_recreate.sql).Count
      # Attendu : >= 1

      (Select-String -Pattern "bonus_prototype_draft" supabase/migrations/20260510170000_bonus_events_recreate.sql).Count
      # Attendu : >= 1

      Select-String -Pattern "create table if not exists public\.bonus_events" supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : 1 hit

      (Select-String -Pattern "create policy" supabase/migrations/20260510170000_bonus_events_recreate.sql).Count
      # Attendu : 4 (select, player_insert, mentor_update, gm_delete)

      Select-String -Pattern "multiplier_factor numeric\(3,2\)" supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : 1 hit

      Select-String -Pattern "/140|percentile|rank " supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : 0 hit (R1 preserved — pas de leak)
    </automated>
  </acceptance_criteria>
  <done>
- Fichier `supabase/migrations/20260510170000_bonus_events_recreate.sql` créé
- 3 enums (bonus_type, bonus_status, multiplier_scope) idempotents
- 1 table bonus_events avec FK + CHECK + indexes
- 4 RLS policies (select, player_insert, mentor_update, gm_delete)
- 1 trigger updated_at attaché
- Aucun INSERT seed (les claims viennent du runtime via Plan 06)
- ASCII pur dans les strings SQL
- R1 audit grep clean (no rank/percentile/score brut)
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror local database/bonus_events.sql (copie identique pour psql -f apply)</name>
  <files>database/bonus_events.sql</files>
  <read_first>
    - `supabase/migrations/20260510170000_bonus_events_recreate.sql` (créé en Task 1)
    - Pattern existant : `database/schema.sql`, `database/triggers.sql`, `database/rls.sql`, `database/seed_event_hackdays.sql` (mirror existing pour seed)
  </read_first>
  <action>
**Stratégie mirror** : la convention existante est qu'un seed prod a 2 fichiers identiques :
- `database/seed_event_hackdays.sql` (copie locale apply via psql -f)
- `supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql` (Supabase CLI apply)

On reproduit le même pattern pour bonus_events :
- `supabase/migrations/20260510170000_bonus_events_recreate.sql` (créé Task 1, source de vérité Supabase CLI)
- `database/bonus_events.sql` (mirror local créé ici, Task 2)

**Implémentation** :

Sur Windows (PowerShell), copier le fichier strictement :
```powershell
Copy-Item supabase/migrations/20260510170000_bonus_events_recreate.sql database/bonus_events.sql
```

Modifier UNIQUEMENT le commentaire d'en-tête pour indiquer que c'est le mirror local (le DDL reste byte-identique).

**Header à utiliser pour database/bonus_events.sql** (les 11 premières lignes seulement, le reste est strictement identique) :

```sql
-- =============================================================================
-- bonus_events.sql
-- T3X-EXPANSION wave 1 / plan 02 — Mirror local de la migration Supabase
-- supabase/migrations/20260510170000_bonus_events_recreate.sql.
--
-- Apply via :
--   psql $SUPABASE_DB_URL -f database/bonus_events.sql
-- (Equivalent a `supabase migration up` qui applique la migration mirror.)
--
-- Le contenu DDL doit rester byte-identique au fichier supabase/migrations/.
-- Sinon : drift detection => regenerer via Copy-Item + edit du seul header.
-- =============================================================================
```

Le reste du fichier (du bloc `-- 1. Enums` jusqu'à `-- End of` final) est strictement identique au fichier Supabase migration.

**Vérification mirror parfait** :
Le diff entre les 2 fichiers doit être limité aux 11 premières lignes (header différent). Au-delà : 0 ligne de diff.
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path database/bonus_events.sql
      # Attendu : True

      # Vérifier que les 2 fichiers ont le même DDL (à partir de la ligne 12 environ)
      $supa = Get-Content supabase/migrations/20260510170000_bonus_events_recreate.sql | Select-Object -Skip 11
      $mirror = Get-Content database/bonus_events.sql | Select-Object -Skip 11
      Compare-Object $supa $mirror
      # Attendu : output vide (mirror parfait au-dela de l'en-tete)

      (Select-String -Pattern "create table if not exists public\.bonus_events" database/bonus_events.sql).Count
      # Attendu : 1

      (Select-String -Pattern "create policy" database/bonus_events.sql).Count
      # Attendu : 4
    </automated>
  </acceptance_criteria>
  <done>
- Fichier `database/bonus_events.sql` créé (mirror local)
- DDL strictement identique à `supabase/migrations/20260510170000_bonus_events_recreate.sql` (à partir de la ligne 12 — seul l'en-tête diffère)
- Pas de drift entre les 2 fichiers
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint Omar — review schema bonus_events + R1 audit + commit</name>
  <files>(no file modification — review checkpoint sur Tasks 1+2 + commit)</files>
  <read_first>
    - `git diff --stat` (les 2 fichiers nouveaux non-trackés normalement)
    - `supabase/migrations/20260510170000_bonus_events_recreate.sql`
    - `database/bonus_events.sql`
  </read_first>
  <action>
HUMAN CHECKPOINT — Omar review le schema bonus_events, valide R1/R2/R3, puis Claude commit.

**Pré-checkpoint (auto par Claude avant)** :
- Tasks 1+2 ont créé les 2 fichiers
- Mirror parfait vérifié

**Action Claude post-approval** :
```bash
git add supabase/migrations/20260510170000_bonus_events_recreate.sql database/bonus_events.sql
git commit -m "$(cat <<'EOF'
feat(t3x-bonus): recreate bonus_events schema + RLS + multiplier mechanism (D-02 D-03)

Wave 1 / plan 02 : retire en v0.2, recreate avec architecture dediee (NOT
flag-based). Mecanisme D-03 : multiplier 1.0x..3.0x sur scores futurs, scope
next_deliverable / rest_of_event. R1 preserve : multiplier_factor stocke en
DB, JAMAIS expose Player en chiffre (UI presentera 'Boost actif' qualitatif —
plan 08).

- 3 enums : bonus_type (verbatims_terrain/dev_plan/prototype_draft),
  bonus_status (draft/submitted/validated/rejected), multiplier_scope
  (next_deliverable/rest_of_event)
- Table bonus_events : project_id (cascade), multiplier_factor numeric(3,2)
  CHECK [1.00..3.00], multiplier_consumed_at, FK auth.users SET NULL,
  CHECK coherence reviewed_by/reviewed_at vs status
- 3 indexes : project_id, status, partial (project_id, claimed_at) WHERE
  validated AND multiplier_consumed_at IS NULL (hot path lib/score.ts)
- 4 RLS : select (player_or_mentor), player_insert (own player + status=submitted),
  mentor_update (validation), gm_delete
- Trigger updated_at via public.set_updated_at (reuse triggers.sql)
- Idempotent : enums DO block, table IF NOT EXISTS, policies DROP IF EXISTS+CREATE
- Mirror local : database/bonus_events.sql == supabase/migrations/20260510170000_*

Refs : BRIEF §2.A (bonus_events recreate), §4 (D-02 architecture propre,
D-03 multiplier mechanism), CLAUDE.md cardinal rules R1.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-bonus"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 2 fichiers (supabase/migrations/20260510170000_*, database/bonus_events.sql)

      Select-String -Pattern "/140|percentile|rank " database/bonus_events.sql, supabase/migrations/20260510170000_bonus_events_recreate.sql
      # Attendu : 0 hit (R1 audit final)
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → Claude commit. Sinon : Omar liste corrections (renommer enum, ajouter colonne, modifier RLS, supprimer CHECK).</done>
  <what-built>
- Schema `bonus_events` recréé : table + 3 enums + 4 RLS + 1 trigger + 3 indexes
- Architecture propre (D-02) : table dédiée, NOT flag sur deliverable_templates
- Mécanisme multiplier (D-03) : factor [1.0..3.0], scope next/rest, consumed timestamp
- R1 préservé : aucun chiffre brut score/rank en DB exposable
- Idempotency : re-apply migration = no-op
- Mirror local préservé pour psql -f apply
  </what-built>
  <how-to-verify>
**Procédure 7 étapes pour Omar avant `approved`** :

1. **Schema cohérence (lecture statique)** :
   ```powershell
   git diff --cached supabase/migrations/20260510170000_bonus_events_recreate.sql | Select-Object -First 200
   ```
   Vérifier :
   - Table `bonus_events` colonnes : id, project_id, type, title, description, doc_url, status, multiplier_factor, multiplier_scope, multiplier_consumed_at, claimed_at, claimed_by, reviewed_by, reviewed_at, feedback, created_at, updated_at (17 colonnes)
   - FK project_id → players(id) ON DELETE CASCADE
   - CHECK multiplier_factor BETWEEN 1.00 AND 3.00
   - CHECK reviewed_by/at coherence vs status

2. **Audit R1 (multiplier_factor pas exposé Player)** :
   ```powershell
   Select-String -Pattern "multiplier_factor|score|rank" database/bonus_events.sql
   ```
   `multiplier_factor` apparaît UNIQUEMENT dans : DDL column, CHECK, comment, partial index. JAMAIS dans une RLS policy SELECT publique pour Player. (Player VOIT la row entière via select policy, mais n'a pas de UI cliente exposant le champ — c'est Plan 08 qui doit garantir le rendu qualitatif.)

3. **Audit R2 (validators warn-only)** :
   - Aucun trigger ne lève `raise exception` bloquant pour Player. Le CHECK constraint est legitimate (pas warn-only) car c'est un invariant de schema (multiplier > 3 = bug, pas un warn).
   - Le RLS WITH CHECK refuse l'insert si conditions non remplies — c'est de l'authz, pas un validator pédagogique. OK R2.

4. **Audit R3 (no inter-mission blocking)** :
   - Aucune logique `blocks_progression_to`, aucun trigger qui bloque submissions selon bonus_events. OK R3.

5. **Idempotency static check** :
   - Enums : 3 DO blocks avec exception `duplicate_object` ✓
   - Table : `create table if not exists` ✓
   - Indexes : `create index if not exists` ✓
   - Policies : `drop policy if exists ... ; create policy ...` ✓
   - Trigger : `drop trigger if exists ... ; create trigger ...` ✓
   - Aucun `DROP TABLE`, `TRUNCATE`, `DELETE FROM` (migration safe).

6. **Mirror parfait check** :
   ```powershell
   $supa = Get-Content supabase/migrations/20260510170000_bonus_events_recreate.sql | Select-Object -Skip 11
   $mirror = Get-Content database/bonus_events.sql | Select-Object -Skip 11
   Compare-Object $supa $mirror
   ```
   Attendu : output vide.

7. **Audit ASCII (no diacritics in SQL strings)** :
   ```powershell
   Select-String -Pattern "[^\x00-\x7F]" supabase/migrations/20260510170000_bonus_events_recreate.sql
   ```
   Attendu : 0 hit (ASCII pur).

**Apply manuel post-commit (Omar, hors checkpoint)** — quand Wave 2 prête à consommer :
```bash
# Local Supabase test
supabase migration up
# OU psql direct sur prod avec backup avant
psql $SUPABASE_DB_URL -f database/bonus_events.sql
psql $SUPABASE_DB_URL -f database/bonus_events.sql  # 2eme run = no-op idempotent
```
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "ajouter unique (project_id, type) où status=validated", "renommer multiplier_consumed_at", "RLS player_insert trop permissive").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification (post-task 3)** :

1. **Files committed** :
   ```bash
   git log -1 --name-only
   # Attendu : 2 fichiers
   ```

2. **DDL completeness** :
   ```powershell
   Select-String -Pattern "create table|create policy|create index|create trigger|create type" supabase/migrations/20260510170000_bonus_events_recreate.sql
   # Attendu : 1 table + 4 policies + 3 indexes + 1 trigger + 3 types = 12 hits minimum
   ```

3. **R1 audit final** :
   ```powershell
   Select-String -Pattern "/140|percentile|score_total|rank " database/bonus_events.sql, supabase/migrations/20260510170000_bonus_events_recreate.sql
   # Attendu : 0 hit
   ```
</verification>

<success_criteria>
- [ ] Schema `bonus_events` créé via migration Supabase + mirror local
- [ ] 3 enums créés idempotents (bonus_type, bonus_status, multiplier_scope)
- [ ] Table `bonus_events` avec multiplier_factor numeric(3,2) CHECK [1..3]
- [ ] 4 RLS policies (select, player_insert, mentor_update, gm_delete)
- [ ] Trigger updated_at attaché
- [ ] 3 indexes (project_id, status, partial validated)
- [ ] Mirror parfait `database/` <-> `supabase/migrations/`
- [ ] R1 audit grep clean (0 leak)
- [ ] Idempotency : re-apply migration sans erreur
- [ ] Commit atomique avec message conforme
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-02-SUMMARY.md` :

Sections :
1. **Schema bonus_events** : 17 colonnes + 3 enums + 4 RLS + 3 indexes + 1 trigger
2. **Multiplier mechanism (D-03)** : factor [1..3], scope next_deliverable/rest_of_event, consumed_at
3. **R1 preservation** : où le multiplier_factor est exposé (DB only) et où il NE DOIT PAS apparaître (Player UI — responsabilité Plan 08)
4. **Idempotency strategy** : DO block enums + IF NOT EXISTS + DROP+CREATE policies
5. **Mirror SQL preserved** : database/bonus_events.sql ↔ supabase/migrations/20260510170000_*
6. **Apply path operator** : `supabase migration up` OU `psql -f database/bonus_events.sql` (idempotent)
7. **Files modified** : 2 (1 nouveau migration Supabase, 1 mirror local). Schema Postgres core (schema.sql, triggers.sql, rls.sql) intact (freeze respecté)
8. **Next consumers** : Plan 05 (TypeScript types), Plan 06 (server actions claim/review), Plan 07 (lib/score.ts multiplier integration), Plan 08 (UI Player claim form)
</output>
