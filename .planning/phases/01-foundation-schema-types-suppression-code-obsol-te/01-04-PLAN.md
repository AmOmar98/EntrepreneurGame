---
phase: 01-foundation-schema-types-suppression-code-obsol-te
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - app/coach/page.tsx
  - app/mentor/page.tsx
  - app/startup/[slug]/page.tsx
  - app/player/[slug]/page.tsx
  - components/app-shell.tsx
  - lib/i18n.ts
  - lib/types.ts
  - lib/seed/players.ts
autonomous: true
requirements: [DATA-04, DATA-06]
must_haves:
  truths:
    - "Route /coach is gone; /mentor renders the mentor placeholder"
    - "Route /startup/[slug] is gone; /player/[slug] renders the player detail placeholder"
    - "AppShell nav points to /mentor and /player (not /coach, /startup)"
    - "DB enum app_role uses 'player','mentor','game_master' (already from plan 01)"
    - "TS type AppRole already uses 'player'|'mentor'|'game_master' (from plan 03)"
    - "All grep matches of 'startup', 'coach' (case-insensitive) in app/ are intentional (e.g., none)"
  artifacts:
    - path: app/mentor/page.tsx
      provides: "Mentor stub route (renamed from /coach)"
    - path: app/player/[slug]/page.tsx
      provides: "Player detail stub route (renamed from /startup/[slug])"
  key_links:
    - from: components/app-shell.tsx
      to: "/mentor and /player routes"
      via: "navItems hrefs updated"
      pattern: "/mentor|/player"
---

<objective>
Apply the route + naming portion of the domain rename (D-12, D-13). DB-side and TS-side
renames already landed in plans 01 and 03 (PG enum `app_role` and TS `AppRole` are both
`player|mentor|game_master`); this plan finishes the job by moving the route folders
and updating any dangling FR/EN copy and seed data names.

Purpose: Single-meaning vocabulary across DB, TS, and routes — no more `coach` vs
`mentor` confusion or `Startup` vs `Player` drift.
Output: Routes `/coach` -> `/mentor`, `/startup/[slug]` -> `/player/[slug]`, navigation
updated, repo compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-CONTEXT.md
@.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-02-PLAN.md
@.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-03-PLAN.md
@CLAUDE.md
@components/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move route folders coach -> mentor and startup -> player</name>
  <files>
    app/coach/page.tsx,
    app/mentor/page.tsx,
    app/startup/[slug]/page.tsx,
    app/player/[slug]/page.tsx
  </files>
  <action>
On Windows PowerShell:

```powershell
Move-Item app/coach app/mentor
Move-Item app/startup app/player
```

(Or `git mv app/coach app/mentor && git mv app/startup app/player` if running under git
bash.)

Then open `app/mentor/page.tsx` and update the heading text from "Mentor space" to
"Mentor" if needed; same for `app/player/[slug]/page.tsx` heading "Player detail" — the
content itself stays a placeholder (Phase 3 / Phase 2 implement them).

Confirm `app/coach/` and `app/startup/` no longer exist. Also confirm no other
`app/<x>/page.tsx` references `/coach` or `/startup` paths in JSX `href`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path app/coach) { exit 1 }; if (Test-Path app/startup) { exit 1 }; if (-not (Test-Path app/mentor/page.tsx)) { exit 1 }; if (-not (Test-Path app/player/[slug]/page.tsx)) { exit 1 }; exit 0"</automated>
  </verify>
  <done>app/coach/ and app/startup/ deleted; app/mentor/page.tsx and app/player/[slug]/page.tsx exist with placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppShell navItems hrefs + sweep stale references in lib/i18n.ts and seed</name>
  <files>
    components/app-shell.tsx,
    lib/i18n.ts,
    lib/types.ts,
    lib/seed/players.ts
  </files>
  <action>
**2a — `components/app-shell.tsx`:**
Change the mentor `navItems` href from `/coach` to `/mentor`. If a player nav item references `/startup/...`, change it to `/player/...`. Keep types as `AppRole = 'player' | 'mentor' | 'game_master'` (already done in plan 03).

**2b — Repo sweep for stale strings:**
Run `grep -rni "coach\|startup" app/ components/ lib/ middleware.ts utils/`. For each match:
  - If it is a comment or copy string referring to the old concept, rewrite to use `mentor` / `player`.
  - If it is a route literal (`href="/coach"`, `redirect("/startup/...")`), rewrite to the new route.
  - Tolerated exception: `lib/seed/players.ts` may still contain English `team` / `idea` strings — those are fine. The slug `demo-alpha` etc. is fine.

After sweep, `grep -rn "/coach\|/startup" app/ components/ lib/` MUST return zero matches.

**2c — `lib/i18n.ts`:**
Ensure dictionary keys reference `mentor` and `player` consistently. Add keys if needed:
```ts
role_player: { fr: "Player", en: "Player" },
role_mentor: { fr: "Mentor", en: "Mentor" },
role_game_master: { fr: "GameMaster", en: "GameMaster" },
```

**2d — Verify:**
`npm run typecheck` + `npm run lint` + `npm run build` MUST all pass.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>AppShell hrefs updated; no /coach or /startup string remains in app/components/lib; typecheck + lint + build pass.</done>
</task>

</tasks>

<verification>
- `Test-Path app/coach` and `Test-Path app/startup` both return false.
- `Test-Path app/mentor/page.tsx` and `Test-Path "app/player/[slug]/page.tsx"` both return true.
- `grep -rn "/coach\|/startup" app/ components/ lib/` returns 0 matches.
- `grep -rn "type AppRole" lib/` shows only `player | mentor | game_master`.
- `npm run typecheck`, `npm run lint`, `npm run build` all exit 0.
</verification>

<success_criteria>
- Route folders moved (D-12 wave 3).
- AppShell + i18n + seed updated.
- No legacy `coach`/`startup` route literal remains.
- Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-04-SUMMARY.md`.
</output>
