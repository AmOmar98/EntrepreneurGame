---
phase: "10-t3-critical-gates-and-design-v2-tail-sections"
plan: "10-01"
type: "feature"
wave: 1
depends_on: ["06-04", "07-04", "08-06", "09-08"]
files_modified:
  - "lib/results.ts"
  - "components/results-podium.tsx"
  - "components/results-replay.tsx"
  - "components/submission-ticket.tsx"
  - "components/journey-client.tsx"
  - "components/journey-level-node.tsx"
  - "components/pixel-mascot.tsx"
  - "components/topbar-lite.tsx"
  - "components/journey-hero-next-step.tsx"
  - "app/results/page.tsx"
  - "app/player/[slug]/page.tsx"
  - "app/loading.tsx"
  - "app/error.tsx"
  - "app/not-found.tsx"
  - "app/journey/help/page.tsx"
  - "app/journey/pitch-prep/page.tsx"
  - "app/settings/page.tsx"
  - "components/system/SysLoading.tsx"
  - "components/system/SysEmpty.tsx"
  - "components/system/SysError.tsx"
  - "components/system/SysOffline.tsx"
  - "components/help/StuckHelp.tsx"
  - "components/pitch-prep/PitchPrep.tsx"
  - "components/menu/SideMenu.tsx"
  - "components/settings/SettingsForm.tsx"
  - "lib/journey.ts"
  - "lib/pitch-prep.ts"
  - "lib/profile.ts"
  - "lib/seed/profile.ts"
  - "lib/seed/missions.ts"
  - "lib/i18n.ts"
  - "eic-tokens.css"
  - "app/globals.css"
  - "database/seed_event_hackdays.sql"
  - "database/migrations/08-mentor-comments.sql"
  - "database/migrations/09-gamemaster-live.sql"
  - ".planning/REQUIREMENTS.md"
autonomous: false
must_haves:
  truths:
    - "B2 résolu : `lib/results.ts:30 DEFAULT_PITCH_WEIGHT = 0.8` (pas 0.5)"
    - "B2 résolu : commentaire `lib/results.ts:3` reflète 0.20/0.80 et non plus 50/50"
    - "B1 résolu : `combined.toFixed(1)` n'est rendu côté Player nulle part (R1)"
    - "B3 résolu : migrations SQL Phase 8+9 appliquées en prod Supabase, vérifiables via mcp list_migrations"
    - "B4 résolu : 7 missions AgreenTech (L1 hypothèse VP / L2.1 Persona / L2.2 Verbatims / L3 MoSCoW / L4 ROI/ha / L5 Plan acquisition / L6 Pitch + Bonus B) seedées avec rubric AgriTech"
    - "R1 préservé sur tous écrans Player (audit grep `+\\$\\{.*xp.*\\}` clean dans pages Player)"
    - "R2 préservé : aucun validator avec `severity: \"error\"` ou `blocking: true` sur les 7 missions AgreenTech"
    - "R3 préservé : aucun `disabled` ni `pointer-events: none` sur les missions verrouillées de `journey-level-node.tsx`"
    - "REQUIREMENTS.md JURY-03 mis à jour pour cohérence avec décision T-3 (0.20/0.80)"
  artifacts:
    - "components/system/SysLoading.tsx + SysEmpty.tsx + SysError.tsx + SysOffline.tsx existent et sont importables"
    - "app/journey/help/page.tsx (Coup de pouce) existe avec timeline 5 actions + 3 portes de sortie"
    - "app/journey/pitch-prep/page.tsx (Pitch prep H-2) existe avec compte à rebours + brief jury"
    - "app/player/[slug]/page.tsx (Profil joueur) refondu (plus placeholder) — gated par publication ranking"
    - "app/settings/page.tsx + components/menu/SideMenu.tsx existent, dark mode togglable"
    - "components/pixel-mascot.tsx étendu avec moods loading + error"
    - ".eic-toast class dans app/globals.css + 16 mood vars dans eic-tokens.css"
    - "npm run typecheck + npm run lint + npm run build clean"
---

# Plan 10-01 — T-3 Critical Gates + Design v2 Tail Sections

> Plan complet importé depuis `C:\Users\omara\.claude\plans\glimmering-sauteeing-wilkinson.md` via `/gsd-import --from`. **Spawn `eic-pedagogical-advisor` avant** toute édition des zones sensibles (cf. CONTEXT.md `<decisions>`).

## Context

**Bundle source** : `https://api.anthropic.com/v1/design/h/I7NlTUTyicbeewAq3zcWOg` (identique au bundle `PG-1aljQekQKK5m01U-9Yw` — même contenu après diff). Bundle gzip → `entrepreneur-game/` avec `README.md`, transcript de chat, `Entrepreneur Game v2 - Wireframes.html` + 13 fichiers JSX/CSS de wireframes mid-fi.

**Pourquoi ce plan** : v0.2 a livré 12 des 14 sections du bundle (parcours, mission, mentor, régie, jury, replay, mascotte Pixel — voir mémoire `project_v02_status.md`). Le pilote AgreenTech démarre dans 3 jours (13 mai 2026 — T-3). Deux fronts à boucler pour tenir la promesse pédagogique :

1. **Bloquants T-3 confirmés par la rétro CLAUDE.md du 10/05** (`B1`–`B5`) : percée R1 sur `/results` (`combined.toFixed(1)` rendu à TOUS les rôles), pondération AgreenTech 20/80 non implémentée (`DEFAULT_PITCH_WEIGHT = 0.5` dans `lib/results.ts:30`), migrations SQL Phase 8 + 9 non appliquées en prod, 7 missions AgreenTech absentes du seed (génériques au lieu de L1 hypothèse VP / L2.1 Persona / etc.), `member_emails` à compléter sur les 11 lignes de `cohorte-agreentech.csv` (B5, hors scope tech).
2. **5 sections design absentes ou partielles** dans le bundle Claude Design : 10·Pitch prep H-2, 11·Coup de pouce, 12·Profil joueur, 13·États système, 14·Menu & réglages.

**Outcome attendu** : un repo qui couvre 100 % des écrans du bundle design, respecte les 3 règles cardinales sans ambiguïté, applique la pondération AgreenTech correcte, et reste navigable en mode démo + Supabase. Aucune mention « démo » / `atlas-soil` ne doit fuiter en prod.

## Décisions arbitrées

| Question | Décision validée |
|---|---|
| Scope T-3 | **Tout (5 sections absentes)** |
| Ticket `+100 XP` (R1) | **Spawn `eic-pedagogical-advisor` avant tout edit de `submission-ticket.tsx`** — l'agent arbitre XP confirmé individuel vs score comparatif |
| Pondération AgreenTech | **Forcer `pitchWeight = 0.80` par défaut + audit de tous les appelants** + corriger le commentaire `lib/results.ts:3` |

## Phases d'exécution

> Chaque sous-phase = un commit GSD via `/gsd-quick` ou `/gsd-execute-phase`. **Spawn `eic-pedagogical-advisor` avant** toute édition de `submission-ticket.tsx`, `lib/seed/*` deliverable_templates, `app/jury/page.tsx`, `app/results/page.tsx`, ou tout fichier touchant scoring rubric / journey gating (cf. mémoire `reference_eic_advisor_agent.md`).

### Phase 0 — T-3 Critical Gates (rétro CLAUDE.md du 10/05) — PRIORITÉ ABSOLUE

> Aligné sur les 5 bloquants `B1`–`B5` de `CLAUDE.md § T-3 Critical Gates` + `RETROSPECTIVE-T3-2026-05-10.md`. **À traiter avant toute nouvelle section design.**

- **0.1 — B2 · Pondération 20/80** (`lib/results.ts:30`)
  - Patcher `DEFAULT_PITCH_WEIGHT = 0.5` → `0.8`.
  - Corriger le commentaire `lib/results.ts:3` qui dit encore `weighted 50/50` → `weighted 0.20 project + 0.80 pitch (AgreenTech 13-14 mai 2026)`.
  - `Grep computeRanking\(` pour vérifier qu'aucun appelant n'écrase à une autre valeur ; sinon propager explicitement `pitchWeight: 0.8` depuis `app/results/page.tsx`.
  - **Mettre à jour `REQUIREMENTS.md` JURY-03** : « par défaut 50/50 » → « par défaut 0.20 projet + 0.80 pitch (AgreenTech) ».
- **0.2 — B1 · Percée R1 sur `/results`** (`components/results-podium.tsx:64-66`, `components/results-replay.tsx:126-134`)
  - Gater le rendu de `combined.toFixed(1)` par un prop `isGameMaster` (passé depuis la page server). Côté Player, remplacer par annonce qualitative (badges « Excellence / Trajectoire / Wildcards », sans chiffres).
  - Spawn `eic-pedagogical-advisor` AVANT édition pour valider la copy qualitative et le mapping (qui obtient quel badge).
- **0.3 — B4 · 7 missions AgreenTech dans le seed** (`database/seed_event_hackdays.sql`)
  - Régénérer les 7 missions `L1 Hypothèse VP / L2.1 Persona / L2.2 Verbatims / L3 MoSCoW / L4 ROI/ha / L5 Plan acquisition / L6 Pitch + Bonus B` avec rubric AgriTech (Innovation / Faisabilité / Modèle éco / Qualité × 5pts).
  - Spawn `eic-pedagogical-advisor` OBLIGATOIRE — c'est exactement le périmètre que l'agent garde (deliverable_templates + scoring rubric).
  - Si l'app a un seed in-memory (`lib/seed/missions.ts` ou équivalent), le mettre à jour en parallèle (les deux modes démo + Supabase doivent coller).
- **0.4 — B3 · Migrations SQL Phase 8 + 9 en prod**
  - Appliquer `database/migrations/08-mentor-comments.sql` + `database/migrations/09-gamemaster-live.sql` sur le projet Supabase prod via MCP `mcp__plugin_supabase_supabase__apply_migration` (ou via dashboard Supabase si Omar préfère manuel).
  - Vérifier RLS `announcements` post-commit `8352ffc` : `target_kind='teams'` doit autoriser GameMaster à insérer et Player à lire ses annonces ciblées uniquement.
- **0.5 — Stamped ticket `+XP` Player** (R1 résiduel sur `submission-ticket.tsx:66-67`)
  - Spawn `eic-pedagogical-advisor` avec le diff prévu (gate `+XP` par flag `revealXp` ou refactor en `Mission envoyée ✓`).
  - Appliquer l'arbitrage de l'advisor (vraisemblablement : XP confirmé individuel post-soumission = tolérable, mais à confirmer formellement).
- **0.6 — R2 validators warn-only**
  - Vérifier que `lib/seed/missions.ts` (ou table `deliverable_templates` côté SQL) n'a aucun `validation_rules[].severity === "error"` ni `blocking: true` sur les 7 missions AgreenTech (post-0.3).
- **0.7 — R3 tooltip ambre journey**
  - Confirmation rapide : `components/journey-client.tsx:140-154` utilise déjà `journey_v2_locked_hint_amber` (commit `4733406`). Vérifier qu'aucun `aria-disabled="true"` ni `pointer-events: none` ne fuit dans `journey-level-node.tsx`.
- **0.8 — Audit fuites « démo » prod**
  - `Grep` `atlas-soil|Atlas Soil|demo|seed_demo` dans `app/`, `components/`, `lib/i18n.ts`. Tout résultat dans une page Player/Mentor/Jury/Results = à retirer.
- **0.9 — Hors scope tech (à signaler à Omar)** : **B5 `member_emails`** sur les 11 lignes de `cohorte-agreentech.csv` reste à compléter manuellement (collecte email AgreenTech). Ne pas inventer les emails.

Critical files : `lib/results.ts:3,30`, `components/results-podium.tsx:64-66`, `components/results-replay.tsx:126-134`, `database/seed_event_hackdays.sql`, `database/migrations/08-mentor-comments.sql`, `database/migrations/09-gamemaster-live.sql`, `lib/seed/missions.ts` (si présent), `components/submission-ticket.tsx:66-67`, `components/journey-client.tsx:140-154`, `components/journey-level-node.tsx`, `app/results/page.tsx`, `.planning/REQUIREMENTS.md`.

### Phase 1 — Patch design system (foundations partagées)

Goal : pousser les briques manquantes dans `app/globals.css` + `eic-tokens.css` pour que toutes les phases suivantes consomment les mêmes primitives.

- **1.1 — Variables mood Pixel** : ajouter dans `eic-tokens.css` (sous `--gradient-border`) les 16 variables `--mood-{serein,concentre,inquiet,euphorique}-{fill,ring,halo,bg}`. Refactorer `components/pixel-mascot.tsx:19-47` (`MOOD_TO_PALETTE`) pour consommer les `var(--mood-*)`.
- **1.2 — Classe `.eic-toast`** : ajouter dans `app/globals.css` (après `.eic-submission-ticket`) la classe `.eic-toast` + keyframe `slideInUp` (sera consommée par Phase 2 et par les toasts régie déjà présents dans `components/admin-live-view.tsx`).
- **1.3 — Variantes Pixel `loading` + `error`** : étendre `pixel-mascot.tsx` pour accepter `mood: "loading" | "error" | HackStatus` (besoin pour Phase 2).

Critical files : `eic-tokens.css`, `app/globals.css`, `components/pixel-mascot.tsx`.

### Phase 2 — Section 13 · États système (Loading, Empty, 503, Offline)

Goal : composants partagés réutilisables par toutes les pages App Router + error boundaries.

- **2.1 — `components/system/SysLoading.tsx`** : Pixel respire (svg mascotte + dots qui rebondissent), checklist d'étapes 5 items (profil, équipe, missions, commentaires, mascotte) avec une seule active. Citation Pixel en pied. Consomme `PixelMascot` mood `loading` ou `serein`.
- **2.2 — `components/system/SysEmpty.tsx`** : compte à rebours `2j 17h 42min` (props : `targetDate`), ligne de stations en pointillés (réutilise tokens journey), 3 cartes « que faire en attendant » (slots `children` ou `actions`).
- **2.3 — `components/system/SysError.tsx`** : Pixel triste (mood `error`), code mono `503 service indisponible`, message « régie notifiée auto », retry compte à rebours `00:05`. Brancher sur `app/error.tsx` (App Router error boundary global).
- **2.4 — `components/system/SysOffline.tsx`** : bandeau bleu marine, 2 colonnes ✓/✗, file de sync (props `pendingActions`). Branchement client via `navigator.onLine` + listener.
- **2.5 — Brancher `app/loading.tsx`, `app/error.tsx`, `app/not-found.tsx`** racines App Router pour utiliser ces composants.

Critical files : `components/system/*` (nouveau dossier), `app/loading.tsx`, `app/error.tsx`, `app/not-found.tsx`, `lib/i18n.ts` (clés `system_loading_*`, `system_empty_*`, `system_error_*`, `system_offline_*`).

### Phase 3 — Section 11 · Coup de pouce (Player bloqué) — sensible R1

Goal : route `/journey/help` (ou drawer ouvrable depuis `/journey`) avec timeline auto-remplie + 3 portes de sortie.

- **Spawn EIC advisor obligatoire** : la mention « -0 XP » dans le design est ambiguë. L'advisor décide entre (a) afficher `« sans coût »` (mot, pas chiffre) côté Player, (b) garder neutre sans mention XP du tout, ou (c) supprimer cette annotation.
- **3.1 — `app/journey/help/page.tsx`** + `components/help/StuckHelp.tsx` : hero empathique « Tu sembles bloquée. C'est normal. », timeline 5 dernières actions (`recentPlayerActions(playerId)` à ajouter dans `lib/journey.ts`), 3 portes : indice écrit / mentor (CTA mailto vers mentor assigné, recommandé) / pause 5 min. Bandeau social « D'autres l'ont vécu » (générique, pas de stats live R1-risquées).
- **3.2 — Trigger** : ajouter un bouton « Bloquée ? » dans `components/journey-hero-next-step.tsx` qui ouvre `/journey/help`.
- **3.3 — i18n** : clés `help_stuck_title`, `help_stuck_doors_*` dans `lib/i18n.ts`.

Critical files : `app/journey/help/page.tsx` (nouveau), `components/help/StuckHelp.tsx` (nouveau), `lib/journey.ts` (helper `recentPlayerActions`), `components/journey-hero-next-step.tsx`, `lib/i18n.ts`.

### Phase 4 — Section 10 · Pitch prep H-2 (Player J-1 pitch)

Goal : route `/journey/pitch-prep` accessible le 14 mai matin avec compte à rebours et brief jury — entièrement non-bloquante (R3).

- **4.1 — `app/journey/pitch-prep/page.tsx`** + `components/pitch-prep/PitchPrep.tsx` : hero compte à rebours `01:38:12` (server-side `targetDate` depuis `lib/seed/hack-config.ts` ou table Supabase équivalente), checklist 5 items (deck, pitcheuse, brief, répétition, backup), ordre de passage (lecture seule, donné par GameMaster), brief jury (italique, focus de chaque juré), 3 rappels numérotés.
- **4.2 — Helper** : `lib/pitch-prep.ts` exposant `getPitchPrep(playerId)` qui agrège (a) ordre de passage assigné, (b) brief jury (lecture seule depuis `jury` table), (c) checklist persistée par player (col JSONB `pitch_prep_checklist` à ajouter à `players` ou état local seulement). **Recommandation** : checklist en `localStorage` côté Player (pas de migration SQL T-3).
- **4.3 — i18n** : clés `pitch_prep_*`.
- **4.4 — Spawn EIC advisor** sur le brief jury (vérifier qu'il ne révèle pas de pondération à un Player).

Critical files : `app/journey/pitch-prep/page.tsx` (nouveau), `components/pitch-prep/PitchPrep.tsx` (nouveau), `lib/pitch-prep.ts` (nouveau), `lib/i18n.ts`.

### Phase 5 — Section 12 · Profil joueur (long-terme)

Goal : remplacer le placeholder `app/player/[slug]/page.tsx` par la vue profil long-terme. R1-sensible : stats long-terme (« 1 victoire / 3 hacks ») = c'est OK car post-événement par définition.

- **5.1 — Refondre `app/player/[slug]/page.tsx`** : identité (avatar, niveau actuel), 5 stats vie (hacks, victoires, mentors), compétences signature (barres Discovery/Collaboration), galerie 6 badges + 1 verrouillé, historique des hacks chronologique, réseau de mentors en chips. CTA vers prochain hack.
- **5.2 — Helpers** : `lib/profile.ts` exposant `getPlayerProfile(slug)`. En mode démo : seed dans `lib/seed/profile.ts`. En mode Supabase : agrégation depuis `players`, `submissions`, `evaluations`, `hacks` (table à créer si absente — fallback : 1 seul hack pour le pilote AgreenTech, donc « 1 hack · pilote en cours »).
- **5.3 — Visibilité** : route publique `/player/[slug]` mais protégée par RLS. Ne révéler le profil aux Players qu'après publication ranking (R1). Avant publication, page = `Profil disponible après le pitch` (réutilise SysEmpty).

Critical files : `app/player/[slug]/page.tsx`, `lib/profile.ts` (nouveau), `lib/seed/profile.ts` (nouveau), `lib/i18n.ts`.

### Phase 6 — Section 14 · Menu latéral + Réglages

Goal : slide-in droite + page `/settings`. Pas de migration SQL — préférences en `localStorage`.

- **6.1 — `components/menu/SideMenu.tsx`** : slide-in droite (overlay flouté), header avec avatar + barre de progression L4, 4 groupes (Le jeu / Communauté / Mon parcours / Réglages) avec badges de notif. Trigger : nouveau bouton hamburger dans `components/topbar-lite.tsx` (ou `app-shell.tsx`).
- **6.2 — `app/settings/page.tsx`** + `components/settings/SettingsForm.tsx` : 4 sections (Apparence dark/light, Notifications toggles, Compte & confidentialité, Avancé). Persist : `localStorage` (clé `entrepreneur-game.settings`). CTA dangereux « Supprimer compte » → `mailto:` à EIC admin (pas de soft-delete pour le pilote).
- **6.3 — Brancher dark mode** : le toggle `Apparence` doit poser `data-theme="dark"` sur `<html>` (déjà supporté par tokens `eic-tokens.css:89-105`).

Critical files : `components/menu/SideMenu.tsx` (nouveau), `app/settings/page.tsx` (nouveau), `components/settings/SettingsForm.tsx` (nouveau), `components/topbar-lite.tsx`, `lib/i18n.ts`.

### Phase 7 — Smoke E2E + commits

- **7.1** : `npm run typecheck` + `npm run lint` à zéro erreur.
- **7.2** : Smoke navigation manuel via Chrome DevTools MCP : login Player → /journey → /journey/help → /journey/pitch-prep → /settings ; login Mentor → /mentor ; login GameMaster → /admin → /admin/announce → /jury → /jury?theater=1 → /results. Capturer 1 screenshot par phase clé sous `screenshots/phase-10-smoke/`.
- **7.3** : Vérifier `npm run build` réussit. Pas de push direct sur main — utiliser `/gsd-pr-branch` pour préparer une PR propre filtrant les commits `.planning/`.

## Composants & utilitaires existants à réutiliser (NE PAS recréer)

- `components/pixel-mascot.tsx` — étendre, ne pas dupliquer (Phase 1 + 2).
- `components/proof-workflow.tsx`, `components/submission-form.tsx`, `components/submission-readonly.tsx`, `components/submission-ticket.tsx` — ne toucher au ticket qu'après advisor (Phase 0.5).
- `components/journey-track.tsx`, `components/journey-level-node.tsx`, `components/journey-drawer.tsx` — design tokens journey à réutiliser pour SysEmpty (Phase 2).
- `components/admin-live-view.tsx`, `components/admin-team-circle.tsx`, `components/admin-radar.tsx` — déjà conformes design.
- `components/jury-pitch-theater.tsx`, `components/jury-pitch-grid.tsx` — déjà conformes.
- `components/results-replay.tsx`, `components/results-podium.tsx` — déjà conformes (mais à gater isGameMaster en Phase 0.2).
- `lib/i18n.ts` — single source of truth pour la copy. **Toute** nouvelle chaîne y va.
- `lib/score.ts` (`sumPlayerScoreProject`, `combineScores`) — appelé seulement côté staff/jury/results, ne pas exposer aux Player components.
- `lib/results.ts:computeRanking()` — modifier le défaut `pitchWeight` à 0.80 (Phase 0.1) ; ne pas réécrire la fonction.
- `utils/supabase/server.ts` `createClient()` — toujours wrapper `if (supabase) { ... }` (mode démo).

## Verification

À exécuter dans cet ordre après chaque sous-phase :

1. `npm run typecheck` — zéro erreur TS.
2. `npm run lint` — zéro warning Next/TS-ESLint.
3. `npm run dev` — démarrer sur `http://localhost:3000`.
4. **Smoke Player** :
   - `/login` (mode démo : auto-bypass) → `/journey` : carte mission, pas de score visible (R1).
   - Cliquer une mission verrouillée : tooltip ambre warn-only (R3), pas de cursor disabled.
   - Soumettre un livrable : ticket affiché (R1 : conforme à l'arbitrage advisor de Phase 0.5).
   - `/journey/help` : timeline + 3 portes de sortie, pas de chiffre XP visible (R1).
   - `/journey/pitch-prep` : compte à rebours + brief jury non-révélateur de pondération (R1).
   - `/settings` : toggle dark mode applique `data-theme="dark"`, persiste après reload.
5. **Smoke Mentor** : `/mentor` → `/mentor/submission/[id]` : commentaires async tagués `remarque`/`à corriger`, pas de chat.
6. **Smoke GameMaster** : `/admin?live=1` : Pixel mood adaptatif, toasts, fil du jeu. `/admin/announce` : composer + preview phone. `/jury?theater=1` : grille /5 sur 5 critères. `/results` : calcul vérifié à 0.20 projet + 0.80 pitch + chiffres visibles uniquement à GameMaster.
7. **Build** : `npm run build` réussit sans erreur.
8. **Audit cardinaux** : `Grep` `\+\$\{.*xp.*\}` et `\bXP\b.*Player\|player` dans `app/`, `components/` — aucun résultat dans une page Player avant publication ranking.

## Hors scope (explicitement)

- Pas de refonte `app/globals.css` (95 % conforme déjà).
- Pas de migration SQL pour Phase 4 (checklist pitch-prep) ni Phase 6 (settings) — `localStorage` suffit pour le pilote.
- Pas de refresh login/onboarding (déjà conformes au design).
- Pas de doc `docs/DESIGN-SYSTEM.md` — peut être ajouté post-pilote.
- Pas de refresh `components/topbar-lite.tsx` au-delà de l'ajout du trigger menu (Phase 6).
- B5 `member_emails` collecte = manuel par Omar, hors scope tech.
