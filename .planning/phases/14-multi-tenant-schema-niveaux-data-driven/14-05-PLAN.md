---
phase: 14-multi-tenant-schema-niveaux-data-driven
plan: 05
type: execute
wave: 5
depends_on: ["14-01", "14-04"]
files_modified:
  - .planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-VERIFICATION.md
autonomous: false
requirements: [TENANT-01, TENANT-02, TENANT-04, LEVELS-02]
must_haves:
  truths:
    - "All four Plan 01 migrations are applied to PROD (operator-run db push or SQL Editor) in one batched checkpoint"
    - "A Player of org A receives zero rows from an org-B event query (cross-session RLS proof)"
    - "AgreenTech and Digi event rows are unchanged beyond the new nullable/default columns; their rankings recompute identically"
    - "missions.level_id_text and players.current_level_text are fully backfilled (no NULLs) and match the enum source"
    - "database/ declarative views (schema.sql/triggers.sql/rls.sql) are mirrored from the applied migrations via the Omar-authorized Node-script pattern and flagged in SUMMARY"
  artifacts:
    - path: ".planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-VERIFICATION.md"
      provides: "PROD apply log + RLS cross-org proof + archive-intact proof + backfill counts"
      contains: "is_active"
  key_links:
    - from: "PROD database"
      to: "supabase/migrations/2026061112*"
      via: "supabase db push --linked (operator)"
      pattern: "db push"
---

<objective>
Apply the four Plan 01 migrations to PROD in ONE batched operator checkpoint (MCP Supabase unavailable per CONTEXT — Omar runs `supabase db push --linked` or pastes into SQL Editor), then verify the four cardinal outcomes: inter-org RLS isolation (TENANT-02), archives intact (TENANT-04), levels backfill complete with zero loss (LEVELS-02), and default-org adoption (TENANT-01). Finally mirror the applied migrations into the `database/` declarative views via the Omar-authorized Node-script pattern.

Purpose: This is the only PROD-touching step in the phase, deliberately sequenced last so all app code (Plans 02-04) already tolerates both pre- and post-migration states. The checkpoint batches all DB applies into a single operator interaction. Verification proves the success criteria with read-only queries; the declarative-view mirror keeps `database/` aligned with PROD (OPS-01 source-of-truth discipline).

Output: 14-VERIFICATION.md (apply log + proofs) and mirrored database/*.sql changes (via authorized script, flagged in SUMMARY). `autonomous: false` — contains a blocking operator checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-CONTEXT.md
@.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-PATTERNS.md
@.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-01-SUMMARY.md
@.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-04-SUMMARY.md
@.planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md

<interfaces>
Migrations to apply (from Plan 01):
  supabase/migrations/20260611120000_organizations.sql
  supabase/migrations/20260611120100_events_is_active.sql
  supabase/migrations/20260611120200_levels_data_driven.sql
  supabase/migrations/20260611120300_rls_org_scope.sql
Read-only verification query templates: OPS-01-drift-report.md "Annexe" (pg_proc / proacl shapes).
Declarative-view mirror: CONTEXT rule 4 — database/** is Write-deny for Edit/Write tools; the Omar-authorized Node-script pattern writes the declarative mirror after review. Each application must be flagged in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator applies the 4 migrations to PROD (batched)</name>
  <what-built>Plans 01-04 authored four additive migrations and made all app code tolerate pre- and post-migration state. CI is green. PROD has NOT been touched.</what-built>
  <how-to-verify>
    Omar applies the four migrations to PROD in order, in ONE session (MCP Supabase unavailable — use the CLI you trust):
    1. Safety tag first: `git tag v0.4.0-pre-phase14-migrate && git push origin v0.4.0-pre-phase14-migrate`.
    2. Apply (preferred): `supabase db push --linked` from the repo (picks up the 4 new files in supabase/migrations/). OR paste each file's contents into the Supabase SQL Editor in order: 120000_organizations → 120100_events_is_active → 120200_levels_data_driven → 120300_rls_org_scope.
    3. Each migration is BEGIN/COMMIT wrapped and idempotent — re-running is safe.
    4. After apply, run the read-only checks below (Task 2 will also script them) and paste results back:
       - `select count(*) from public.organizations;` (≥ 1, includes 'eic')
       - `select id, slug, is_active, organization_id from public.events order by starts_at;` (every row has organization_id = eic org; exactly one is_active = true)
       - `select count(*) from public.players where current_level_text is null;` (must be 0)
       - `select count(*) from public.missions where level_id_text is null;` (must be 0)
    Confirm no errors during apply. If any migration errors, do NOT proceed — report the error; the additive design means partial state is recoverable via the per-step rollback comments.
  </how-to-verify>
  <resume-signal>Paste the four query results and confirm "applied, no errors" — or describe the error to trigger rollback.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify TENANT-02 RLS isolation, TENANT-04 archives intact, LEVELS-02 backfill — write 14-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/13-db-consolidation-test-infrastructure/OPS-01-drift-report.md (Annexe read-only query shapes)
    - scripts/smoke-rls-prod.mjs and scripts/test-rls-cross-cohort.sql (existing RLS test analogs)
    - supabase/migrations/20260611120300_rls_org_scope.sql (the policies + is_in_org just applied)
  </read_first>
  <action>
    Using the operator-pasted results from Task 1 plus the existing RLS test harness (`scripts/smoke-rls-prod.mjs` / `scripts/test-rls-cross-cohort.sql` as analogs), produce read-only verification and write `.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-VERIFICATION.md` with these sections:
    1. APPLY LOG — the four migration filenames, apply method (db push / SQL Editor), timestamp, "no errors" confirmation.
    2. TENANT-01 — organizations row count + the events→org backfill table (every event maps to the eic org).
    3. TENANT-03 — exactly one events.is_active = true; the partial unique index present (`select indexname from pg_indexes where indexname='uniq_events_single_active'`).
    4. TENANT-02 (RLS cross-org) — since PROD is mono-org, the structural proof: (a) `is_in_org` exists with `prosecdef = true` and PUBLIC absent from `proacl` (reuse OPS-01 Annexe A2 query shape); (b) the `events_org_scope_select` policy exists (`select policyname from pg_policies where tablename='events'`); (c) a two-session simulation OR a documented argument that a hypothetical org-B user fails `is_in_org(orgA)` — if a second org cannot be created safely in PROD, document the test as "structural: policy + helper verified; multi-org runtime test deferred to a staging seed" and note this is acceptable given mono-org launch (CONTEXT).
    5. TENANT-04 (archives intact) — recompute AgreenTech + Digi rankings post-migration and assert identical to pre-migration: run the results calculation (lib/results.ts path or the archived ranking query) for both events and confirm row order/scores unchanged; confirm no event row's pre-existing columns changed (only organization_id/is_active added).
    6. LEVELS-02 (backfill complete) — `players.current_level_text` and `missions.level_id_text` have zero NULLs and each equals its `::text` enum source (`select count(*) from public.players where current_level_text <> current_level::text;` must be 0; same for missions).
    All queries are READ-ONLY. Do not modify PROD data. If any assertion fails, record it as a BLOCK and stop (do not mark the phase complete).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-VERIFICATION.md','utf8').toLowerCase(); const must=['apply log','tenant-01','tenant-02','tenant-03','tenant-04','levels-02','is_active','is_in_org','current_level_text']; const miss=must.filter(m=>!s.includes(m)); if(miss.length){console.error('MISSING SECTIONS:',miss);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 14-VERIFICATION.md exists with APPLY LOG + TENANT-01/02/03/04 + LEVELS-02 sections.
    - TENANT-02: is_in_org has prosecdef=true and no PUBLIC in proacl; events_org_scope_select policy present; cross-org isolation proven or explicitly documented as structural (mono-org).
    - TENANT-04: AgreenTech + Digi rankings recompute identically; no pre-existing event column changed.
    - LEVELS-02: zero NULLs in current_level_text/level_id_text; both equal their enum `::text` source.
    - Any failed assertion recorded as BLOCK (phase not marked complete).
  </acceptance_criteria>
  <done>PROD apply verified; RLS isolation, archive integrity, and levels backfill proven read-only; 14-VERIFICATION.md written.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror applied migrations into database/ declarative views (Omar-authorized Node-script pattern)</name>
  <read_first>
    - .planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-CONTEXT.md (rule 4 — database/** Write-deny + authorized Node-script pattern, flag in SUMMARY)
    - database/schema.sql lines 60-90 (events + levels structures to extend)
    - .planning/phases/13-db-consolidation-test-infrastructure/13-NEW.sql (precedent for phase-dir SQL → declarative mirror)
    - OPS-01-drift-report.md Synthèse (the 6 PARTIAL objects schema.sql is already behind — note Phase 14 is the agreed point to reflect them)
  </read_first>
  <action>
    Reflect the four applied migrations into the `database/` declarative views so `database/` stays the source of truth (OPS-01 discipline). Because `database/**` is Write/Edit-denied for the Write/Edit tools, use the Omar-authorized Node-script pattern: write a small Node script (e.g. `scripts/mirror-phase14-declarative.cjs`) that reads the migration SQL and appends/updates the declarative statements in `database/schema.sql` and `database/rls.sql`, run it, and FLAG this application explicitly in the SUMMARY.
    Declarative additions:
    - `database/schema.sql`: add `public.organizations` table; add `events.organization_id` + `events.is_active` columns; add `public.levels_v2` table; add `missions.level_id_text` + `players.current_level_text` columns + their indexes. (Mirror only — declarative form, no data backfill statements.)
    - `database/rls.sql`: add `is_in_org(uuid)` function + REVOKE/GRANT pair + the `events_org_scope_select` and organizations select policies.
    Scope guard: mirror ONLY the four Phase 14 migrations. Do NOT attempt to also resolve the 6 pre-existing PARTIAL drift objects (jurors, help_requests, etc.) here unless trivially adjacent — that broader reconciliation is its own concern; note it as remaining drift in SUMMARY.
    Run `npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e` to confirm the mirror introduced no app regressions (declarative SQL is not compiled by TS, but the gate confirms nothing else broke). Commit on `milestone/v0.4-scale-foundation` and push (no merge to main, per CONTEXT).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('database/schema.sql','utf8').toLowerCase(); const r=require('fs').readFileSync('database/rls.sql','utf8').toLowerCase(); const must=[['schema:organizations',s.includes('create table public.organizations')],['schema:is_active',s.includes('is_active')],['schema:levels_v2',s.includes('levels_v2')],['schema:level_id_text',s.includes('level_id_text')],['rls:is_in_org',r.includes('is_in_org')],['rls:events_org_scope',r.includes('events_org_scope_select')]]; const miss=must.filter(([,ok])=>!ok).map(([n])=>n); if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `database/schema.sql` declaratively contains organizations, events.organization_id, events.is_active, levels_v2, missions.level_id_text, players.current_level_text.
    - `database/rls.sql` declaratively contains is_in_org + REVOKE/GRANT + events_org_scope_select policy.
    - The mirror was produced via a Node script (database/** not edited by Write/Edit tools) and is FLAGGED in SUMMARY.
    - Full quality gate green; committed + pushed on milestone/v0.4-scale-foundation (no main merge).
    - Remaining pre-existing PARTIAL drift (if not reconciled) noted in SUMMARY.
  </acceptance_criteria>
  <done>database/ declarative views mirror the four applied migrations via the authorized Node-script pattern; gate green; pushed; mirror flagged in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → PROD | only Omar applies migrations (MCP unavailable); agent never writes PROD data |
| migration apply → archived data | AgreenTech/Digi rankings must survive identically (cardinal: zero data loss) |
| Write/Edit tools → database/** | denied; declarative mirror only via authorized Node script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Tampering | PROD migration apply | mitigate | safety git tag before apply; BEGIN/COMMIT + idempotent migrations; per-step rollback comments; operator-gated |
| T-14-12 | Information Disclosure | inter-org RLS in PROD | mitigate | verify is_in_org prosecdef + no PUBLIC + events_org_scope_select policy present (Task 2) |
| T-14-13 | Repudiation | archive integrity | mitigate | recompute AgreenTech/Digi rankings post-apply, assert identical; no pre-existing column changed (TENANT-04) |
| T-14-14 | Tampering | database/** declarative edit | mitigate | Node-script pattern (no Write/Edit on database/**), flagged in SUMMARY per CONTEXT rule 4 |
| T-14-SC | Tampering | npm installs | accept | no new dependencies |
</threat_model>

<verification>
- All four migrations applied to PROD with no errors (operator-confirmed).
- 14-VERIFICATION.md proves TENANT-01/02/03/04 + LEVELS-02 with read-only queries.
- AgreenTech + Digi rankings identical post-migration; no archived row mutated beyond new nullable/default columns.
- current_level_text / level_id_text fully backfilled (zero NULLs, equal enum source).
- database/ declarative views mirrored via Node script and flagged; full gate green; pushed (no main merge).
</verification>

<success_criteria>
- One batched operator checkpoint applied the four migrations to PROD.
- TENANT-01 (org adoption), TENANT-02 (RLS isolation structural+verified), TENANT-04 (archives intact), LEVELS-02 (backfill no-loss) all proven.
- Declarative mirror committed; quality gate green; branch pushed without merge to main.
</success_criteria>

<sc4_note>
plan-checker W-1 — VERIFICATION.md and SUMMARY must state explicitly: ROADMAP success criterion 4 (« plus d'enum PG ») is satisfied by the READ-PATH switch to levels_v2 text FKs; the physical enum columns remain populated-but-dormant, their DROP is the deferred post-July destructive migration (CONTEXT deferred section).
</sc4_note>

<output>
Create `.planning/phases/14-multi-tenant-schema-niveaux-data-driven/14-05-SUMMARY.md`. Record the apply method + timestamp, the verification outcomes (with the four cardinal proofs), the Node-script mirror flag (CONTEXT rule 4), any remaining drift, and the commit SHA(s) pushed.
</output>
