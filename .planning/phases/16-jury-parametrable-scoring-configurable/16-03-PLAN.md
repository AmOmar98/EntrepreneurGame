---
phase: 16-jury-parametrable-scoring-configurable
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/actions.ts
  - lib/schemas.ts
  - lib/i18n.ts
  - components/admin-jury-grid-editor.tsx
  - components/admin-event-settings-editor.tsx
  - app/admin/events/[id]/settings/page.tsx
  - components/admin-events-table.tsx
autonomous: true
requirements: [JURY-09, SETTINGS-04]
must_haves:
  truths:
    - "GM can define N pitch criteria (key/label/max/ord) for an event in the editor and save them"
    - "GM can edit XP rules, engagement thresholds, and pitch weight for an event in the editor and save them"
    - "Both editors are demo read-only (banner + write returns demo message)"
    - "Saving criteria revalidates /jury and /results; saving settings revalidates affected routes"
  artifacts:
    - path: "components/admin-jury-grid-editor.tsx"
      provides: "criteria rows builder (key/label/max + add/remove) + criteriaJson hidden input"
      contains: "criteriaJson"
    - path: "components/admin-event-settings-editor.tsx"
      provides: "numeric settings form (7 fields) bound to saveEventSettingsFlow"
      contains: "saveEventSettingsFlow"
    - path: "app/admin/events/[id]/settings/page.tsx"
      provides: "GM-gated event settings + jury grid editor surface"
      contains: "AdminJuryGridEditor"
  key_links:
    - from: "components/admin-jury-grid-editor.tsx"
      to: "saveJuryGridFlow"
      via: "useActionState + criteriaJson hidden input"
      pattern: "saveJuryGridFlow"
    - from: "components/admin-event-settings-editor.tsx"
      to: "saveEventSettingsFlow"
      via: "useActionState numeric form"
      pattern: "saveEventSettingsFlow"
    - from: "app/admin/events/[id]/settings/page.tsx"
      to: "getPitchCriteria / getEventSettings"
      via: "server fetch initial values"
      pattern: "getEventSettings"
---

<objective>
Give the GameMaster a no-code editor for the jury grid (pitch criteria) and the scoring settings, integrated into the event editor using phase-15 editor patterns.

Purpose: JURY-09 (GM edits the jury grid), SETTINGS-04 (GM edits XP rules, engagement thresholds, pitch weight). Both write through GM-gated Flow actions and reuse the established admin editor primitives.
Output: 2 new Flow actions (saveJuryGridFlow, saveEventSettingsFlow), their Zod schemas, 2 new client editor components, a GM-gated settings page route, an entry link from the events table, and i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-CONTEXT.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-PATTERNS.md
@.planning/phases/15-mission-engine-no-code-editeur-gm/15-UI-SPEC.md

<interfaces>
From lib/pitch-criteria.ts (16-01): getPitchCriteria(eventId), PitchCriterion { id, eventId, key, label, max, ord }.
From lib/event-settings.ts (16-01): getEventSettings(eventId), EventSettings, DEFAULT_EVENT_SETTINGS.
From lib/schemas.ts (existing): slugifyToKey(label) — reuse to derive criterion key from label.
From app/actions.ts (existing): WorkflowState { ok, message, mailto?, severity? }; saveDeliverableTemplateFlow GM-only pattern (lines 3157-3265 region); profiles app_role gate (lines ~520-527 region).
</interfaces>

<read_first>
- app/actions.ts (the saveDeliverableTemplateFlow region + a game_master-only gate like the publish action ~line 843) — copy demo guard + auth + role gate + delete-then-insert/upsert + revalidatePath
- components/admin-deliverable-template-editor.tsx — rubric builder rows (add/remove/update + JSON.stringify hidden input + Trash2 + aria-labels + status message)
- components/admin-missions-editor.tsx — numeric input + defaultValue form pattern for the settings editor
- components/admin-events-table.tsx — events list row actions (add a "Reglages" link to /admin/events/[id]/settings)
- app/admin/events/[id]/missions/page.tsx (lines 1-50) — GM-gate + demo banner + breadcrumb pattern to mirror in the new settings page
- lib/schemas.ts — slugifyToKey export + existing rubricSchema shape for the jury grid schema mirror
- 15-UI-SPEC.md — copy keys table + primitives (.input, .button.primary, .eic-pill--amber demo banner, aria-labels) + destructive/inline confirmation rules
- 16-PATTERNS.md sections: "saveJuryGridFlow", "saveEventSettingsFlow", "admin-jury-grid-editor.tsx", "admin-event-settings-editor.tsx", Shared Patterns (GM role gate, demo guard, JSON hidden input, revalidatePath)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saveJuryGridFlow + saveEventSettingsFlow actions + schemas</name>
  <files>app/actions.ts, lib/schemas.ts</files>
  <behavior>
    - saveJuryGridSchema rejects 0 criteria and >10 criteria; accepts 1..10 with {key,label,max 1..100}.
    - saveEventSettingsSchema rejects pitchWeight >1 or <0; accepts XP/eng ints 0..500.
    - saveJuryGridFlow in demo mode returns { ok:false } with the demo message; never throws.
    - saveEventSettingsFlow with non-game_master role returns { ok:false, message acces reserve }.
  </behavior>
  <read_first>app/actions.ts saveDeliverableTemplateFlow region + game_master gate (~843); lib/schemas.ts rubricSchema + slugifyToKey; 16-PATTERNS.md "saveJuryGridFlow" + "saveEventSettingsFlow" + Shared Patterns</read_first>
  <action>
    lib/schemas.ts: export juryGridCriterionSchema = z.object({ key: z.string().min(1), label: z.string().min(1), max: z.coerce.number().int().min(1).max(100) }) and saveJuryGridSchema = z.object({ eventId: z.string().uuid(), criteria: z.array(juryGridCriterionSchema).min(1).max(10) }). Export saveEventSettingsSchema = z.object({ eventId: z.string().uuid(), xpFirstSubmission/xpValidateV1/xpValidateV2/engSubmitted/engReviewed/engValidated: z.coerce.number().int().min(0).max(500), pitchWeight: z.coerce.number().min(0).max(1) }).
    app/actions.ts: add `export async function saveJuryGridFlow(_prev, formData)` — demo guard → createClient → parse criteriaJson from formData (try/catch, "Criteria JSON invalide." on failure) → safeParse saveJuryGridSchema with eventId from formData → auth.getUser → profiles app_role game_master gate (return "Acces reserve au GameMaster." otherwise) → delete-then-insert: delete pitch_criteria where event_id, then insert rows mapping each criterion to { event_id, key, label, max, ord: index } → revalidatePath('/admin/events'), revalidatePath('/jury'), revalidatePath('/results') → { ok:true, message:"Grille jury enregistree." }.
    Add `export async function saveEventSettingsFlow(_prev, formData)` — same demo/auth/game_master gate → safeParse saveEventSettingsSchema from formData fields → upsert into event_settings { event_id: eventId, xp_first_submission, xp_validate_v1, xp_validate_v2, eng_submitted, eng_reviewed, eng_validated, pitch_weight } onConflict "event_id" → revalidatePath('/admin/events'), '/jury', '/results' → { ok:true, message:"Reglages enregistres." }. All strings plain ASCII (no diacritics) per CLAUDE.md.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:unit -- schemas</automated>
  </verify>
  <acceptance_criteria>
    - Both schemas enforce the documented bounds; both actions enforce demo guard + game_master role gate.
    - saveJuryGridFlow does delete-then-insert; saveEventSettingsFlow upserts onConflict event_id.
    - revalidatePath covers /admin/events, /jury, /results.
    - typecheck + schema unit tests green.
  </acceptance_criteria>
  <done>Two GM-gated Flow actions + schemas exported, validating and writing per spec.</done>
</task>

<task type="auto">
  <name>Task 2: AdminJuryGridEditor + AdminEventSettingsEditor components + i18n</name>
  <files>components/admin-jury-grid-editor.tsx, components/admin-event-settings-editor.tsx, lib/i18n.ts</files>
  <read_first>components/admin-deliverable-template-editor.tsx (rubric rows); components/admin-missions-editor.tsx (numeric form); 15-UI-SPEC.md copy table + primitives; 16-PATTERNS.md "admin-jury-grid-editor.tsx" + "admin-event-settings-editor.tsx"</read_first>
  <action>
    components/admin-jury-grid-editor.tsx ("use client"): props { eventId: string; initialCriteria?: PitchCriterion[]; demo: boolean }. useActionState(saveJuryGridFlow, initialState). Local CriterionRow[] state seeded from initialCriteria or one empty row. addCriterion / removeCriterion (block removing the last, min 1) / updateCriterion(idx, field, value). Render each row as an .admin-form-grid with key input (placeholder "ex: innovation"), label input, max number input (1..100), and a Trash2 .button.icon with aria-label, disabled when only 1 row. Serialize to criteriaJson = JSON.stringify(rows.map(c => ({ key: c.key.trim() || slugifyToKey(c.label), label: c.label, max: c.max }))) into a hidden input name="criteriaJson"; hidden input name="eventId". Submit .eic-button--primary "Ajouter un critere" / "Enregistrer la grille" using new i18n keys. Demo banner (.eic-pill--amber) when demo; status/error message via .form-status/.form-error role="status". All aria-labels per 15-UI-SPEC.
    components/admin-event-settings-editor.tsx ("use client"): props { eventId: string; settings: EventSettings; demo: boolean }. useActionState(saveEventSettingsFlow, initialState). One <form action={formAction}> with hidden eventId + 7 numeric inputs (name=xpFirstSubmission/xpValidateV1/xpValidateV2/engSubmitted/engReviewed/engValidated min 0 max 500, pitchWeight min 0 max 1 step 0.05) each with a <label> showing the default in parentheses and defaultValue from settings. Submit .eic-button--primary "Enregistrer les reglages". Demo banner + status message same pattern. Pending state disables submit.
    lib/i18n.ts: add fr keys (plain ASCII): admin_jury_grid_title, admin_jury_add_criterion, admin_jury_save_grid, admin_jury_grid_saved, admin_jury_grid_empty, admin_settings_title, admin_settings_save, admin_settings_saved, admin_settings_xp_first, admin_settings_xp_v1, admin_settings_xp_v2, admin_settings_eng_submitted, admin_settings_eng_reviewed, admin_settings_eng_validated, admin_settings_pitch_weight, admin_settings_demo_disabled. Mirror the same keys in the en dictionary (English copy) to keep dictionaries in sync.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Jury grid editor renders dynamic criterion rows with add/remove/update + criteriaJson hidden input.
    - Settings editor renders 7 numeric inputs with defaults from EventSettings.
    - Both have demo banner + status messaging + aria-labels; i18n keys present in fr and en, no inline French.
    - typecheck + lint green.
  </acceptance_criteria>
  <done>Both editor components built from EIC primitives, wired to their Flow actions, i18n complete.</done>
</task>

<task type="auto">
  <name>Task 3: GM-gated settings page route + events-table entry link</name>
  <files>app/admin/events/[id]/settings/page.tsx, components/admin-events-table.tsx</files>
  <read_first>app/admin/events/[id]/missions/page.tsx:1-50 (GM gate + demo + breadcrumb); components/admin-events-table.tsx (row actions); 15-UI-SPEC.md staff shell + breadcrumb</read_first>
  <action>
    app/admin/events/[id]/settings/page.tsx (server component): mirror the missions page guard exactly — await params { id }, getCurrentUser → redirect /login if absent, getCurrentRole → redirect pathForRole if role && !== game_master. When hasSupabaseEnv(): fetch initial criteria via getPitchCriteria(eventId) and settings via getEventSettings(eventId); when demo, pass DEMO_PITCH_CRITERIA / DEFAULT_EVENT_SETTINGS. Render <AppShell role="game_master" variant="staff">, breadcrumb "Admin → Events → {event name} → Reglages" (fetch event name like missions page does), the demo .eic-pill--amber banner when !hasSupabaseEnv(), then <AdminJuryGridEditor eventId initialCriteria demo={!hasSupabaseEnv()} /> and <AdminEventSettingsEditor eventId settings demo={!hasSupabaseEnv()} />. Use t.* keys; no inline French.
    components/admin-events-table.tsx: add a "Reglages" Link to /admin/events/[id]/settings in each event row's actions, next to the existing "Voir missions"/clone/activate actions, styled with the existing .button class and an aria-label. Surgical change only — do not refactor surrounding actions.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - /admin/events/[id]/settings is GM-gated (same guard as missions page), renders both editors, demo-safe.
    - events table exposes a "Reglages" link to the settings route.
    - typecheck + lint + build green.
  </acceptance_criteria>
  <done>GM can reach both editors from the events list; route is role-gated and demo-safe; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(GM)→saveJuryGridFlow/saveEventSettingsFlow | GM form input crosses into pitch_criteria/event_settings; Zod + role gate + RLS |
| route /admin/events/[id]/settings | GM-only surface; non-GM redirected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Elevation | saveJuryGridFlow / saveEventSettingsFlow | mitigate | demo guard + auth.getUser + profiles app_role === game_master defense-in-depth alongside RLS GM-only policy |
| T-16-08 | Tampering | criteriaJson hidden input | mitigate | try/catch JSON.parse + Zod array min1/max10 + per-criterion bounds before insert |
| T-16-09 | Spoofing | /admin/events/[id]/settings route | mitigate | getCurrentRole gate redirects non-game_master (mirrors missions page) |
| T-16-SC | Tampering | npm installs | accept | no new packages added |
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run build` green.
- i18n keys present in both fr and en dictionaries; no inline French in new components (grep).
- Demo mode: both editors show the amber banner and writes return the demo message.
</verification>

<success_criteria>
- GM defines N pitch criteria in the editor (JURY-09).
- GM edits XP rules + engagement thresholds + pitch weight in the editor (SETTINGS-04).
- Editors reuse phase-15 patterns and stay demo read-only.
</success_criteria>

<output>
Create `.planning/phases/16-jury-parametrable-scoring-configurable/16-03-SUMMARY.md` when done
</output>
