---
phase: 16-jury-parametrable-scoring-configurable
plan: 05
type: execute
wave: 5
depends_on: ["16-01", "16-04"]
files_modified:
  - database/schema.sql
  - database/triggers.sql
  - database/rls.sql
  - scripts/mirror-phase16-declarative.cjs
  - .planning/phases/16-jury-parametrable-scoring-configurable/16-05-OPERATOR-LOG.md
autonomous: false
requirements: [JURY-06, SETTINGS-03]
must_haves:
  truths:
    - "The 5 pending migrations (20260611220000, 230000, 240000, 240100, 240200) are applied to PROD via supabase db push --linked"
    - "RLS smoke passes post-apply (a Player still reads only their own data, no cross-org leak)"
    - "Spot-checks confirm no archive data drift (phase 14 NULL counts, DIGI-08 count exact, archived scores all NULL)"
    - "database/ declarative views mirror the applied phase-16 DDL (no hand Write/Edit on database/**)"
  artifacts:
    - path: ".planning/phases/16-jury-parametrable-scoring-configurable/16-05-OPERATOR-LOG.md"
      provides: "dry-run diff + apply output + smoke + spot-check evidence"
      contains: "db push"
    - path: "scripts/mirror-phase16-declarative.cjs"
      provides: "Omar-authorized node script mirroring phase-16 DDL into database/"
      contains: "schema.sql"
  key_links:
    - from: "scripts/mirror-phase16-declarative.cjs"
      to: "database/schema.sql"
      via: "idempotent marker-guarded append"
      pattern: "pitch_criteria"
---

<objective>
End-of-phase operator checkpoint: apply the batched pending migrations to PROD, smoke RLS + 3 surfaces, verify archives are intact, and mirror the DDL into the declarative database/ source.

Purpose: completes the PROD-apply deferred since phases 14/15 (JURY-06 + SETTINGS-03 need the tables/functions live in PROD). database/** is Write/Edit-denied for agent tools — the declarative mirror runs through an Omar-authorized node script (same pattern as scripts/mirror-phase14-declarative.cjs). This plan is NOT autonomous: a blocking human checkpoint gates the destructive PROD apply.
Output: applied migrations, RLS + 3-surface smoke evidence, archive spot-check counts, declarative mirror, operator log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-CONTEXT.md

<read_first>
- scripts/mirror-phase14-declarative.cjs — the exact Omar-authorized mirror-script pattern (idempotent marker guard, DDL-only, skips if present) to copy for phase 16
- scripts/smoke-rls-prod.mjs — the RLS smoke script invoked post-apply (env-driven: SMOKE_EMAIL / SMOKE_PWD)
- scripts/smoke-http.mjs — HTTP smoke for the 3 surfaces
- database/MANIFEST.md — the database/ source-of-truth rules (rule 4: mirror applied migrations into declarative views)
- supabase/migrations/20260611220000_events_org_scope_enforce.sql + 20260611230000_phase15_engine_columns.sql — the 2 already-pending migrations that batch with phase 16's 3
- .planning/phases/16-jury-parametrable-scoring-configurable/16-01-PLAN.md — the 3 phase-16 migrations (240000/240100/240200) to apply
</read_first>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Authorize batched PROD migration apply</name>
  <action>Operator gate before the destructive PROD apply — review dry-run, confirm safety tag, approve or hold.</action>
  <what-built>
    All phase-16 code is merged on milestone/v0.4-scale-foundation: pitch_criteria + pitch_scores.scores + event_settings + parameterized recalc_player_engagement migrations are staged in supabase/migrations/ (240000/240100/240200), batched with the 2 pending from phases 14/15 (220000 org-scope-enforce, 230000 engine columns). The full gate is green and archives recompute identically in tests. PROD has NOT yet received any of these 5 migrations.
  </what-built>
  <how-to-verify>
    1. Confirm you (Omar) authorize the batched PROD apply now (delegation acquired per 16-CONTEXT.md).
    2. Confirm a safety tag exists: `git tag v0.4-pre-phase16-apply` pushed before apply (executor creates it if absent).
    3. Confirm the dry-run is acceptable: executor runs `npx supabase db push --linked --dry-run` and pastes the planned migration list — it MUST list exactly the 5 pending migrations and nothing destructive (no DROP of c1..c5, no data deletes).
    4. Reply "approved" to proceed with the live apply, or describe concerns to abort.
  </how-to-verify>
  <resume-signal>Type "approved" to apply, or describe concerns to hold.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply migrations + RLS smoke + 3-surface smoke + archive spot-checks</name>
  <files>.planning/phases/16-jury-parametrable-scoring-configurable/16-05-OPERATOR-LOG.md</files>
  <read_first>scripts/smoke-rls-prod.mjs; scripts/smoke-http.mjs; the dry-run output from the checkpoint; 16-CONTEXT.md operator-checkpoint constraints</read_first>
  <action>
    After approval: create/push tag v0.4-pre-phase16-apply if not present. Run `npx supabase db push --linked` to apply the 5 pending migrations to PROD (delegated CLI). Capture full output. Then:
    (a) RLS smoke: run scripts/smoke-rls-prod.mjs with a Player credential (env SMOKE_EMAIL/SMOKE_PWD from the Digi creds) — must pass with no "permission denied for table".
    (b) 3-surface smoke: verify Player /journey, Mentor eval, GM dashboard load against PROD (scripts/smoke-http.mjs or curl of the 3 routes returning 200) — record outcomes.
    (c) Archive spot-checks (Supabase MCP execute_sql, read-only SELECT count): confirm phase-14 NULL-count invariants unchanged vs the pre-apply baseline; confirm the DIGI-08 pitch_scores count for the Digi event equals the known value; confirm pitch_scores rows for AgreenTech + Digi all have scores IS NULL (no archive mutation).
    Write 16-05-OPERATOR-LOG.md with: tag SHA, dry-run list, db push output, RLS smoke result, 3-surface results, and the spot-check counts with pass/fail. Plain ASCII.
    If any smoke or spot-check FAILS: stop, do not mirror, document the failure and the rollback option (supabase migration repair / restore from the tag) in the log.
  </action>
  <verify>
    <human-check>16-05-OPERATOR-LOG.md shows db push applied the 5 migrations, RLS smoke passed, the 3 surfaces returned 200, and archive spot-check counts match the pre-apply baseline (scores IS NULL on all archived rows).</human-check>
  </verify>
  <acceptance_criteria>
    - 5 migrations applied to PROD; output captured.
    - RLS smoke passes; no cross-org / cross-player leak.
    - 3 surfaces load; archive counts unchanged; archived pitch_scores.scores all NULL.
  </acceptance_criteria>
  <done>PROD carries the phase-16 schema; smoke + spot-checks green and logged.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror phase-16 DDL into declarative database/ via authorized script</name>
  <files>scripts/mirror-phase16-declarative.cjs, database/schema.sql, database/triggers.sql, database/rls.sql</files>
  <read_first>scripts/mirror-phase14-declarative.cjs (the pattern to copy); database/MANIFEST.md rule 4; the 3 phase-16 migration sources</read_first>
  <action>
    Create scripts/mirror-phase16-declarative.cjs copying the mirror-phase14 pattern: idempotent, marker-guarded (skip if the phase-16 markers already present), DDL-only (no data backfills — those stay in supabase/migrations). It appends: to database/schema.sql the pitch_criteria table DDL + pitch_scores.scores column + event_settings table; to database/triggers.sql the get_event_setting_int helper + the parameterized recalc_player_engagement body + the event_settings updated_at trigger; to database/rls.sql the pitch_criteria + event_settings RLS policies/grants. Wrap each block in a phase-16 marker comment. Run `node scripts/mirror-phase16-declarative.cjs` to apply the mirror (the script — not the agent — writes to database/**, the Omar-authorized workaround for the Write/Edit deny). Confirm idempotence by running it twice (second run a no-op).
  </action>
  <verify>
    <automated>node scripts/mirror-phase16-declarative.cjs && node scripts/mirror-phase16-declarative.cjs && node -e "const s=require('fs').readFileSync('database/schema.sql','utf8'); if(!/pitch_criteria/.test(s)||!/event_settings/.test(s)) throw new Error('mirror incomplete'); console.log('OK idempotent')"</automated>
  </verify>
  <acceptance_criteria>
    - mirror-phase16-declarative.cjs exists, idempotent, DDL-only, marker-guarded.
    - database/schema.sql + triggers.sql + rls.sql carry the phase-16 DDL via the script (no hand edits).
    - Second script run is a no-op.
  </acceptance_criteria>
  <done>Declarative database/ source matches PROD for phase 16; mirror idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→PROD (supabase db push) | destructive DDL crosses into PROD; blocking human checkpoint + dry-run + safety tag |
| archived rows ← migration | archives must remain untouched (additive only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Tampering | PROD db push apply | mitigate | dry-run review + safety tag v0.4-pre-phase16-apply + blocking human checkpoint before apply |
| T-16-13 | Denial of Service | archive data drift | mitigate | post-apply spot-checks (NULL counts, DIGI-08 count, scores IS NULL) gate completion; rollback via tag |
| T-16-14 | Elevation | new tables RLS post-apply | mitigate | RLS smoke (scripts/smoke-rls-prod.mjs) confirms no cross-org/player leak after apply |
| T-16-SC | Tampering | npm installs | accept | no new packages |
</threat_model>

<verification>
- 16-05-OPERATOR-LOG.md records dry-run + apply + RLS smoke + 3-surface smoke + archive spot-checks.
- mirror-phase16-declarative.cjs idempotent; database/ carries phase-16 DDL.
</verification>

<success_criteria>
- Phase-16 schema live in PROD with archives intact (JURY-06 + SETTINGS-03 SQL side operational).
- RLS + 3-surface smoke green post-apply.
- Declarative database/ mirrors PROD.
</success_criteria>

<output>
Create `.planning/phases/16-jury-parametrable-scoring-configurable/16-05-SUMMARY.md` when done
</output>
