---
phase: 16-jury-parametrable-scoring-configurable
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - tests/unit/event-settings.test.ts
  - tests/unit/jury-grid-schemas.test.ts
  - tests/e2e/jury-pitch.spec.ts
  - .planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md
autonomous: true
requirements: [JURY-08, SETTINGS-03]
must_haves:
  truths:
    - "Unit tests prove DEFAULT_EVENT_SETTINGS equals the current hardcoded values and getEventSettings falls back in demo"
    - "Unit tests prove saveJuryGridSchema + saveEventSettingsSchema bounds (min/max, criteria count)"
    - "The jury E2E demo spec asserts the demo-disabled banner is visible and no criteria form is rendered"
    - "R1 grep audit passes on lib/score.ts, lib/results.ts, lib/journey.ts, and touched Player-facing surfaces"
    - "16-VERIFICATION.md traces all 8 requirements + 4 ROADMAP success criteria to evidence"
  artifacts:
    - path: "tests/unit/event-settings.test.ts"
      provides: "DEFAULT_EVENT_SETTINGS + fallback parsing tests"
      contains: "DEFAULT_EVENT_SETTINGS"
    - path: "tests/unit/jury-grid-schemas.test.ts"
      provides: "schema bound tests for jury grid + event settings"
      contains: "saveJuryGridSchema"
    - path: ".planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md"
      provides: "requirement + success-criteria traceability"
      contains: "JURY-08"
  key_links:
    - from: "tests/unit/event-settings.test.ts"
      to: "lib/event-settings.ts DEFAULT_EVENT_SETTINGS"
      via: "import + deep-equal assertion"
      pattern: "DEFAULT_EVENT_SETTINGS"
    - from: "16-VERIFICATION.md"
      to: "ROADMAP success criteria 1-4"
      via: "evidence mapping"
      pattern: "Success Criteria"
---

<objective>
Lock in the quality filet: settings fallbacks, schema bounds, the demo-mode jury E2E, the R1 audit, and a verification ledger.

Purpose: SETTINGS-03 (parameterized scoring is parsed/fallback-correct) and the schema bounds are proven by tests, not asserted. R1 cardinal audit on the hot zones is executed and recorded. NOTE: the retro-compat normalizePitchScore fixtures (4-crit ×1.25, 5-crit, dynamic) are NOT created here — they were moved to 16-02 Task 1 (co-located with normalizePitchScore at its creation point) per plan-checker; this plan consumes their passing status in the verification ledger.
Output: 2 new unit test files, the jury E2E demo-disabled assertion, and 16-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-CONTEXT.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-PATTERNS.md

<interfaces>
From lib/results.ts (16-02): normalizePitchScore is EXPORTED and already has its co-located fixtures in tests/unit/results-dynamic.test.ts (created in 16-02 Task 1). Do NOT recreate that file here.
From lib/event-settings.ts (16-01): DEFAULT_EVENT_SETTINGS (incl. bonusMultiplierCap 3.0), getEventSettings.
From lib/schemas.ts (16-03): saveJuryGridSchema, saveEventSettingsSchema.
</interfaces>

<read_first>
- tests/unit/score.test.ts — vitest import style, fixture shape, describe/it/expect conventions (mirror exactly)
- tests/unit/results-dynamic.test.ts (created in 16-02) — confirm it exists and exercises normalizePitchScore; this plan only references it, does not edit it
- tests/e2e/jury-pitch.spec.ts — existing jury demo E2E flow to extend
- app/jury/page.tsx (demo early-exit branch rendering t.jury_demo_disabled) — the demo behavior the E2E asserts
- lib/event-settings.ts — DEFAULT_EVENT_SETTINGS + getEventSettings under test
- 15-VERIFICATION.md (phase 15) — verification document format to mirror for 16-VERIFICATION.md
- CLAUDE.md "Pre-edit guards" — the exact R1 grep command to run and record
- 16-PATTERNS.md Critical Pitfalls 3 + 5 + 6 (retro-compat + R1 audit + demo early-exit)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event-settings unit tests</name>
  <files>tests/unit/event-settings.test.ts</files>
  <behavior>
    - DEFAULT_EVENT_SETTINGS deep-equals { xpFirstSubmission:100, xpValidateV1:50, xpValidateV2:100, engSubmitted:100, engReviewed:25, engValidated:50, pitchWeight:0.8, bonusMultiplierCap:3.0 }.
    - getEventSettings(null) resolves to DEFAULT_EVENT_SETTINGS (demo path; no Supabase env in the test runner).
  </behavior>
  <read_first>tests/unit/score.test.ts (style); lib/event-settings.ts</read_first>
  <action>
    tests/unit/event-settings.test.ts: import DEFAULT_EVENT_SETTINGS + getEventSettings from @/lib/event-settings; assert the deep-equal default (including bonusMultiplierCap: 3.0) and that getEventSettings(null) returns the defaults (the vitest runner has no Supabase env, so the demo branch is exercised). Plain ASCII strings. Mirror tests/unit/score.test.ts style. Do NOT create results-dynamic.test.ts here — it lives in 16-02 Task 1.
  </action>
  <verify>
    <automated>npm run test:unit -- event-settings</automated>
  </verify>
  <acceptance_criteria>
    - DEFAULT_EVENT_SETTINGS deep-equal (incl. bonusMultiplierCap) + getEventSettings(null) fallback cases pass.
    - This file does not duplicate the normalizePitchScore fixtures (those are in 16-02's results-dynamic.test.ts).
  </acceptance_criteria>
  <done>Settings default + demo fallback unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jury grid + settings schema tests + jury E2E demo-disabled assertion</name>
  <files>tests/unit/jury-grid-schemas.test.ts, tests/e2e/jury-pitch.spec.ts</files>
  <behavior>
    - saveJuryGridSchema rejects criteria=[] and a 11-item array; accepts a 3-item valid array.
    - juryGridCriterionSchema rejects max=0 and max=101; accepts max=20.
    - saveEventSettingsSchema rejects pitchWeight=1.5 and pitchWeight=-0.1; accepts 0.8; rejects xpFirstSubmission=600.
    - Existing jury-pitch E2E demo flow still passes; the added assertion confirms that in demo mode /jury renders the demo-disabled banner (t.jury_demo_disabled) and renders ZERO criteria inputs (the form is NOT shown in demo mode).
  </behavior>
  <read_first>tests/unit/score.test.ts (style); tests/e2e/jury-pitch.spec.ts (existing demo flow); app/jury/page.tsx demo early-exit (t.jury_demo_disabled, no JuryForm render); lib/schemas.ts saveJuryGridSchema + saveEventSettingsSchema</read_first>
  <action>
    tests/unit/jury-grid-schemas.test.ts: import saveJuryGridSchema, juryGridCriterionSchema, saveEventSettingsSchema from @/lib/schemas; assert safeParse success/failure for each documented case.
    tests/e2e/jury-pitch.spec.ts: extend (do not rewrite) the existing demo spec — after navigating to /jury in demo mode, assert (a) the demo-disabled banner text (t.jury_demo_disabled, FR copy) is visible, AND (b) zero criteria number inputs are present (the criteria form does not render in demo mode — the page early-exits before any JuryForm). Do NOT assert that legacy criterion labels render in demo mode (the form is intentionally not shown). Keep the existing assertions intact so the E2E baseline does not regress.
  </action>
  <verify>
    <automated>npm run test:unit -- jury-grid-schemas</automated>
  </verify>
  <acceptance_criteria>
    - All schema-bound cases pass.
    - jury-pitch E2E spec extended: demo-disabled banner visible + zero criteria inputs asserted; existing assertions intact.
  </acceptance_criteria>
  <done>Schema bounds covered; jury demo-disabled behavior asserted correctly; test counts grow (no regression).</done>
</task>

<task type="auto">
  <name>Task 3: R1 audit + full gate + 16-VERIFICATION.md</name>
  <files>.planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md</files>
  <read_first>CLAUDE.md "Pre-edit guards" (R1 grep command); 15-VERIFICATION.md (format); ROADMAP Phase 16 success criteria 1-4; REQUIREMENTS.md JURY-06..09 + SETTINGS-01..04</read_first>
  <action>
    Run the full gate: `npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e`. Run the R1 grep audit verbatim from CLAUDE.md against lib/score.ts / lib/results.ts / lib/journey.ts touched surfaces: `grep -rn "score\|rank\|note\|/100\|/140\|points\|toFixed" app/journey app/results components/results-* components/submission-* --include="*.tsx" | grep -v "app/journey/deliverable/" | grep -v "components/deliverable-score-block"` and confirm no NEW Player-facing match was introduced by phase 16 (compare against the known-clean baseline; record any pre-existing matches as out-of-scope). Also run `grep -rn "rank\|classement\|percentile\|leaderboard" app/journey/deliverable/` — must be empty.
    Write 16-VERIFICATION.md mirroring the 15-VERIFICATION.md format: a table mapping each of JURY-06, JURY-07, JURY-08, JURY-09, SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04 to the plan(s) and file(s) that satisfy it, and each of the 4 ROADMAP success criteria to its evidence (test name, file, or grep result). Note explicitly that JURY-08 retro-compat (4/5-crit ×1.25) is proven by tests/unit/results-dynamic.test.ts which is created in 16-02 Task 1 (co-located with normalizePitchScore), and that SETTINGS-01 bonus_multiplier_cap is proven by the applyBonusMultiplier opts test in 16-02 + the bonus_multiplier_cap column in 16-01. Record the gate result (unit count, e2e count) and the R1 audit outcome. Flag the PROD-apply dependency: success criterion 4 (SQL literals removed) is fully verifiable only after the 16-05 operator checkpoint applies the migration — note this explicitly with the SQL-side proof being the migration source itself (get_event_setting_int usage, no free 100/25/50 literals in the engagement computation).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Full gate green (typecheck/lint/build/unit/e2e).
    - R1 audit recorded with no new Player-facing leak; deliverable/ rank grep empty.
    - 16-VERIFICATION.md maps all 8 requirements + 4 success criteria to evidence, noting the 16-05 PROD-apply dependency for SC4 and the 16-02 co-location of the retro-compat fixtures.
  </acceptance_criteria>
  <done>Gate green, R1 clean, verification ledger complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player surface ← results/score (R1) | the test suite + grep audit is the regression boundary for R1 leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information Disclosure | lib/results.ts / lib/score.ts / lib/journey.ts Player leak | mitigate | R1 grep audit in Task 3 must pass before completion |
| T-16-11 | Tampering | archived ranking recompute drift | mitigate | results-dynamic.test.ts fixtures (created in 16-02) lock the ×1.25 / 5-crit outputs; verified green in this plan's gate |
| T-16-SC | Tampering | npm installs | accept | no new packages |
</threat_model>

<verification>
- Full gate (typecheck/lint/build/unit/e2e) green.
- R1 grep audit clean; deliverable/ rank grep empty.
- event-settings + jury-grid-schemas unit fixtures pass; jury E2E demo-disabled assertion passes; E2E count not regressed.
</verification>

<success_criteria>
- Settings parsing/fallbacks proven (SETTINGS-03 read side; incl. bonusMultiplierCap default).
- Schema bounds proven; jury demo-disabled behavior asserted correctly.
- 16-VERIFICATION.md traces all 8 requirements + 4 success criteria (retro-compat fixtures referenced from 16-02).
</success_criteria>

<output>
Create `.planning/phases/16-jury-parametrable-scoring-configurable/16-04-SUMMARY.md` when done
</output>
