---
phase: 16-jury-parametrable-scoring-configurable
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - tests/unit/results-dynamic.test.ts
  - tests/unit/event-settings.test.ts
  - tests/unit/jury-grid-schemas.test.ts
  - tests/e2e/jury-pitch.spec.ts
  - .planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md
autonomous: true
requirements: [JURY-08, SETTINGS-03]
must_haves:
  truths:
    - "Unit tests prove normalizePitchScore recomputes AgreenTech 4-crit (x1.25) and Digi 5-crit legacy rankings identically"
    - "Unit tests prove the dynamic jsonb path normalizes scores to /100 using criteria maxes"
    - "Unit tests prove DEFAULT_EVENT_SETTINGS equals the current hardcoded values and getEventSettings falls back in demo"
    - "Unit tests prove saveJuryGridSchema + saveEventSettingsSchema bounds (min/max, criteria count)"
    - "R1 grep audit passes on lib/score.ts, lib/results.ts, and touched Player-facing surfaces"
    - "16-VERIFICATION.md traces all 8 requirements + 4 ROADMAP success criteria to evidence"
  artifacts:
    - path: "tests/unit/results-dynamic.test.ts"
      provides: "retro-compat 4/5-crit + dynamic normalization fixtures"
      contains: "1.25"
    - path: "tests/unit/event-settings.test.ts"
      provides: "DEFAULT_EVENT_SETTINGS + fallback parsing tests"
      contains: "DEFAULT_EVENT_SETTINGS"
    - path: "tests/unit/jury-grid-schemas.test.ts"
      provides: "schema bound tests for jury grid + event settings"
      contains: "saveJuryGridSchema"
    - path: ".planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md"
      provides: "requirement + success-criteria traceability"
      contains: "JURY-08"
  key_links:
    - from: "tests/unit/results-dynamic.test.ts"
      to: "lib/results.ts normalizePitchScore"
      via: "import + fixture assertions"
      pattern: "normalizePitchScore"
    - from: "16-VERIFICATION.md"
      to: "ROADMAP success criteria 1-4"
      via: "evidence mapping"
      pattern: "Success Criteria"
---

<objective>
Lock in the quality filet: regression fixtures proving archives recompute identically, dynamic-path correctness, settings fallbacks, schema bounds, the R1 audit, and a verification ledger.

Purpose: JURY-08 (retro-compat verifiable by recomputing archived data) and SETTINGS-03 (parameterized scoring is parsed/fallback-correct) are proven by tests, not asserted. R1 cardinal audit on the hot zones is executed and recorded.
Output: 3 new unit test files, an extended jury E2E demo spec, and 16-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-CONTEXT.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-PATTERNS.md

<interfaces>
From lib/results.ts (16-02): export normalizePitchScore(r: PitchScoreLite, criteria: PitchCriterion[]): number — ensure it is exported for unit testing (export it in 16-02 if not already; if 16-02 left it module-private, this plan's first action is to export it).
From lib/event-settings.ts (16-01): DEFAULT_EVENT_SETTINGS, getEventSettings.
From lib/schemas.ts (16-03): saveJuryGridSchema, saveEventSettingsSchema.
</interfaces>

<read_first>
- tests/unit/score.test.ts — vitest import style, fixture shape, describe/it/expect conventions (mirror exactly)
- tests/e2e/jury-pitch.spec.ts — existing jury demo E2E flow to extend (dynamic criteria render assertion)
- lib/results.ts (normalizePitchScore + DEFAULT_PITCH_WEIGHT) — the function under test
- 15-VERIFICATION.md (phase 15) — verification document format to mirror for 16-VERIFICATION.md
- CLAUDE.md "Pre-edit guards" — the exact R1 grep command to run and record
- 16-PATTERNS.md Critical Pitfalls 3 + 5 (retro-compat + R1 audit)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: results-dynamic + event-settings unit tests</name>
  <files>tests/unit/results-dynamic.test.ts, tests/unit/event-settings.test.ts, lib/results.ts</files>
  <behavior>
    - AgreenTech legacy fixture (scores=null, c5=0, total_score=80) -> normalizePitchScore returns 100 (×1.25).
    - Digi legacy fixture (scores=null, c5=12, total_score=95) -> returns 95 (no scaling).
    - NaN total_score legacy fixture -> returns 0.
    - Dynamic fixture (scores={innovation:18,equipe:16}, criteria max 20+20) -> returns 85 ((34/40)*100).
    - Dynamic fixture with empty criteria total (maxTotal 0) -> returns 0.
    - DEFAULT_EVENT_SETTINGS deep-equals { xpFirstSubmission:100, xpValidateV1:50, xpValidateV2:100, engSubmitted:100, engReviewed:25, engValidated:50, pitchWeight:0.8 }.
    - getEventSettings(null) resolves to DEFAULT_EVENT_SETTINGS (demo path; no Supabase env in test runner).
  </behavior>
  <read_first>tests/unit/score.test.ts (style); lib/results.ts normalizePitchScore; lib/event-settings.ts</read_first>
  <action>
    If 16-02 did not `export` normalizePitchScore, add the export keyword in lib/results.ts (pure function, no other change). tests/unit/results-dynamic.test.ts: import normalizePitchScore + PitchCriterion; write describe blocks covering the 5 normalization cases above with explicit numeric expectations, labeling the legacy cases "retro-compat AgreenTech 4-crit" / "Digi 5-crit". tests/unit/event-settings.test.ts: import DEFAULT_EVENT_SETTINGS + getEventSettings; assert the deep-equal default and that getEventSettings(null) returns the defaults (the vitest runner has no Supabase env, so the demo branch is exercised). Plain ASCII strings.
  </action>
  <verify>
    <automated>npm run test:unit -- results-dynamic event-settings</automated>
  </verify>
  <acceptance_criteria>
    - All 5 normalization cases + 2 settings cases pass.
    - normalizePitchScore is exported and imported by the test.
    - Archived 4/5-crit fixtures recompute to their original normalized values (JURY-08 regression guard).
  </acceptance_criteria>
  <done>Retro-compat + dynamic + settings unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jury grid + settings schema tests + jury E2E demo extension</name>
  <files>tests/unit/jury-grid-schemas.test.ts, tests/e2e/jury-pitch.spec.ts</files>
  <behavior>
    - saveJuryGridSchema rejects criteria=[] and a 11-item array; accepts a 3-item valid array.
    - juryGridCriterionSchema rejects max=0 and max=101; accepts max=20.
    - saveEventSettingsSchema rejects pitchWeight=1.5 and pitchWeight=-0.1; accepts 0.8; rejects xpFirstSubmission=600.
    - Existing jury-pitch E2E demo flow still passes; an added assertion confirms the form renders the legacy criteria labels in demo mode (criteria fallback path).
  </behavior>
  <read_first>tests/unit/score.test.ts (style); tests/e2e/jury-pitch.spec.ts (existing demo flow); lib/schemas.ts saveJuryGridSchema + saveEventSettingsSchema</read_first>
  <action>
    tests/unit/jury-grid-schemas.test.ts: import saveJuryGridSchema, juryGridCriterionSchema, saveEventSettingsSchema from @/lib/schemas; assert safeParse success/failure for each documented case. tests/e2e/jury-pitch.spec.ts: extend (do not rewrite) the existing demo spec — after navigating to /jury in demo mode, assert the dynamic form renders the 4 legacy criterion labels (fallback path). Keep the existing assertions intact so the 23-test E2E baseline does not regress.
  </action>
  <verify>
    <automated>npm run test:unit -- jury-grid-schemas</automated>
  </verify>
  <acceptance_criteria>
    - All schema-bound cases pass.
    - jury-pitch E2E spec extended with a criteria-render assertion without breaking existing assertions.
  </acceptance_criteria>
  <done>Schema bounds + jury demo render covered; test counts grow (no regression).</done>
</task>

<task type="auto">
  <name>Task 3: R1 audit + full gate + 16-VERIFICATION.md</name>
  <files>.planning/phases/16-jury-parametrable-scoring-configurable/16-VERIFICATION.md</files>
  <read_first>CLAUDE.md "Pre-edit guards" (R1 grep command); 15-VERIFICATION.md (format); ROADMAP Phase 16 success criteria 1-4; REQUIREMENTS.md JURY-06..09 + SETTINGS-01..04</read_first>
  <action>
    Run the full gate: `npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e`. Run the R1 grep audit verbatim from CLAUDE.md against lib/score.ts/lib/results.ts touched surfaces: `grep -rn "score\|rank\|note\|/100\|/140\|points\|toFixed" app/journey app/results components/results-* components/submission-* --include="*.tsx" | grep -v "app/journey/deliverable/" | grep -v "components/deliverable-score-block"` and confirm no NEW Player-facing match was introduced by phase 16 (compare against the known-clean baseline; record any pre-existing matches as out-of-scope). Also run `grep -rn "rank\|classement\|percentile\|leaderboard" app/journey/deliverable/` — must be empty.
    Write 16-VERIFICATION.md mirroring the 15-VERIFICATION.md format: a table mapping each of JURY-06, JURY-07, JURY-08, JURY-09, SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04 to the plan(s) and file(s) that satisfy it, and each of the 4 ROADMAP success criteria to its evidence (test name, file, or grep result). Record the gate result (unit count, e2e count) and the R1 audit outcome. Flag the PROD-apply dependency: success criterion 4 (SQL literals removed) is fully verifiable only after the 16-05 operator checkpoint applies the migration — note this explicitly with the SQL-side proof being the migration source itself (get_event_setting_int usage, no free 100/25/50 literals in the engagement computation).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm run test:unit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Full gate green (typecheck/lint/build/unit/e2e).
    - R1 audit recorded with no new Player-facing leak; deliverable/ rank grep empty.
    - 16-VERIFICATION.md maps all 8 requirements + 4 success criteria to evidence, noting the 16-05 PROD-apply dependency for SC4.
  </acceptance_criteria>
  <done>Gate green, R1 clean, verification ledger complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player surface ← results/score (R1) | the test suite + grep audit is the regression boundary for R1 leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information Disclosure | lib/results.ts / lib/score.ts Player leak | mitigate | R1 grep audit in Task 3 must pass before completion |
| T-16-11 | Tampering | archived ranking recompute drift | mitigate | results-dynamic.test.ts fixtures lock the ×1.25 / 5-crit outputs |
| T-16-SC | Tampering | npm installs | accept | no new packages |
</threat_model>

<verification>
- Full gate (typecheck/lint/build/unit/e2e) green.
- R1 grep audit clean; deliverable/ rank grep empty.
- All new unit fixtures pass; E2E count not regressed.
</verification>

<success_criteria>
- Archived 4/5-crit rankings recompute identically (JURY-08 proven by fixtures).
- Settings parsing/fallbacks proven (SETTINGS-03 read side).
- 16-VERIFICATION.md traces all 8 requirements + 4 success criteria.
</success_criteria>

<output>
Create `.planning/phases/16-jury-parametrable-scoring-configurable/16-04-SUMMARY.md` when done
</output>
