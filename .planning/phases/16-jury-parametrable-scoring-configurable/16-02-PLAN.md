---
phase: 16-jury-parametrable-scoring-configurable
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/results.ts
  - lib/score.ts
  - lib/jury.ts
  - lib/journey.ts
  - app/jury/jury-form.tsx
  - app/jury/page.tsx
  - app/results/page.tsx
  - app/actions.ts
  - tests/unit/results-dynamic.test.ts
autonomous: true
requirements: [JURY-07, JURY-08, SETTINGS-01, SETTINGS-03]
must_haves:
  truths:
    - "The /jury form renders the active event's pitch_criteria dynamically; falls back to legacy 4 criteria when none defined"
    - "Submitting the jury form writes scores jsonb when dynamic criteria exist, and always writes c1..c5 for legacy compat"
    - "computeRanking reads scores jsonb when present, else the c1..c5 path with the c5=0 -> *1.25 retro-compat hack"
    - "AgreenTech (4-crit) and Digi (5-crit) archived rankings recompute identically (no scores column on those rows)"
    - "Combined ranking pitch weight comes from event_settings (default 0.8) via the page caller"
    - "lib/journey.ts XP rules (+100/+50/+100) read from EventSettings with DEFAULT_EVENT_SETTINGS fallback — no free literals remain"
    - "lib/score.ts applyBonusMultiplier clamps with the cap passed via opts (default = BONUS_MULTIPLIER_CAP)"
  artifacts:
    - path: "lib/results.ts"
      provides: "normalizePitchScore dynamic+legacy + scores in fetch query + pitchWeight from settings"
      contains: "scores"
    - path: "lib/journey.ts"
      provides: "getJourneyData XP literals replaced by EventSettings values"
      contains: "settings"
    - path: "lib/jury.ts"
      provides: "scores in PitchScoreRow + select + criteriaAvg aggregate"
      contains: "criteriaAvg"
    - path: "app/jury/jury-form.tsx"
      provides: "dynamic criteria rendering + scoresJson hidden input + legacy c1..c4"
      contains: "scoresJson"
    - path: "tests/unit/results-dynamic.test.ts"
      provides: "retro-compat 4/5-crit + dynamic normalization fixtures at creation point"
      contains: "1.25"
  key_links:
    - from: "app/jury/jury-form.tsx"
      to: "savePitchScoreFlow"
      via: "scoresJson + c1..c5 form submit"
      pattern: "scoresJson"
    - from: "lib/results.ts"
      to: "pitch_scores.scores"
      via: "fetchPitchScores select scores + normalizePitchScore"
      pattern: "normalizePitchScore"
    - from: "lib/journey.ts"
      to: "event-settings"
      via: "getEventSettings / settings param with DEFAULT_EVENT_SETTINGS fallback"
      pattern: "xpFirstSubmission"
    - from: "app/results/page.tsx"
      to: "getEventSettings"
      via: "pitchWeight passed to computeRanking"
      pattern: "pitchWeight"
---

<objective>
Make the jury form dynamic and the ranking calculation criteria-aware, wire all scoring literals (pitch weight, engagement, XP rules, bonus cap) to EventSettings, while preserving archived rankings bit-for-bit.

Purpose: JURY-07 (the /jury form renders the active event's criteria), JURY-08 (computeRanking absorbs dynamic criteria, archives retro-compat preserved), SETTINGS-01 (XP rules + bonus multiplier cap read from settings on the TS side), SETTINGS-03 (no double hardcode — TS reads the same parameters PL/pgSQL reads). The dynamic jsonb path is additive; the legacy c1..c5 + c5=0->*1.25 path survives unchanged for any row without a scores value. Pitch weight now flows from event_settings (default 0.8) so SETTINGS-02 is honored on the read side.
Output: modified lib/results.ts, lib/score.ts, lib/jury.ts, lib/journey.ts, app/jury/jury-form.tsx, app/jury/page.tsx, app/results/page.tsx, savePitchScoreFlow in app/actions.ts, and the co-located tests/unit/results-dynamic.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-CONTEXT.md
@.planning/phases/16-jury-parametrable-scoring-configurable/16-PATTERNS.md

<interfaces>
From lib/pitch-criteria.ts (created in 16-01):
  export type PitchCriterion = { id: string; eventId: string; key: string; label: string; max: number; ord: number };
  export async function getPitchCriteria(eventId: string): Promise<PitchCriterion[]>;
  export const DEMO_PITCH_CRITERIA: PitchCriterion[];
From lib/event-settings.ts (created in 16-01):
  export type EventSettings = { xpFirstSubmission; xpValidateV1; xpValidateV2; engSubmitted; engReviewed; engValidated; pitchWeight; bonusMultiplierCap: number };
  export const DEFAULT_EVENT_SETTINGS: EventSettings;
  export async function getEventSettings(eventId: string | null): Promise<EventSettings>;
From lib/types.ts:
  export const BONUS_MULTIPLIER_CAP = 3.0; // current clamp value; becomes the applyBonusMultiplier opts default
</interfaces>

<read_first>
- lib/results.ts (lines 24-33, 71-90, 148-160, 245-305) — RankingRow type, DEFAULT_PITCH_WEIGHT, clampWeight, computeRanking signature, fetchPitchScores select, aggregate loop with c5=0->*1.25 hack
- lib/score.ts (lines 2, 72-74, 186-221) — BONUS_MULTIPLIER_CAP import, SUBMITTED/REVIEWED/VALIDATED_POINTS constants, sumPlayerScoreEngagement signature, applyBonusMultiplier clamp at line 218
- lib/journey.ts (lines 346-356) — getJourneyData XP literals earnedXp += 100 / += 50 / += 100 (the SETTINGS-01 wiring target)
- lib/jury.ts (lines 64-72, 119-130, 270-320) — JuryAggregate type, PitchScoreRow columns, getJuryOverview select + mapPitchScore
- app/jury/jury-form.tsx (lines 1-120) — current props, the c1..c4 fields array, individual state vars, hidden inputs, submit
- app/jury/page.tsx (lines 40-60, 100-110, 175-250) — getJuryOverview call, demo early-exit (t.jury_demo_disabled), JuryForm render site
- app/results/page.tsx (lines 125-140) — computeRanking caller (extend with pitchWeight from getEventSettings)
- app/actions.ts (lines 1111-1260) — pitchScoreSchema + savePitchScoreFlow upsert payload + onConflict
- tests/unit/score.test.ts — vitest import style, fixture shape, describe/it/expect conventions (mirror exactly for results-dynamic.test.ts)
- 16-PATTERNS.md sections: "lib/results.ts", "lib/score.ts", "lib/jury.ts", "app/jury/jury-form.tsx", "savePitchScoreFlow"; Critical Pitfalls 1-6
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/results.ts dynamic + retro-compat + pitchWeight from settings; lib/score.ts parameterized (engagement + bonus cap); co-located results-dynamic tests</name>
  <files>lib/results.ts, lib/score.ts, app/results/page.tsx, tests/unit/results-dynamic.test.ts</files>
  <behavior>
    - normalizePitchScore on a legacy row (scores null, c5=0, total_score=80) returns 100 (80 * 1.25 — 4-crit hack preserved).
    - normalizePitchScore on a legacy row (scores null, c5=10, total_score=90) returns 90 (5-crit, no scaling).
    - normalizePitchScore on a dynamic row (scores {a:15,b:18}, criteria maxes 20+20=40) returns 82.5 ((33/40)*100).
    - normalizePitchScore on a NaN total_score legacy row returns 0.
    - normalizePitchScore on a dynamic row with empty criteria total (maxTotal 0) returns 0.
    - sumPlayerScoreEngagement with no opts returns the same value as before (defaults 100/25/50).
    - computeRanking with pitchWeight 0.5 weights pitch and project equally; absent/invalid weight clamps to DEFAULT_PITCH_WEIGHT (0.8).
    - applyBonusMultiplier with no opts clamps the factor at BONUS_MULTIPLIER_CAP (3.0); with opts.bonusMultiplierCap=2 clamps at 2.
  </behavior>
  <read_first>lib/results.ts:24-33,71-90,148-160,245-305; lib/score.ts:2,72-74,186-221 + sumPlayerScoreEngagement signature; app/results/page.tsx:125-140; tests/unit/score.test.ts (style); 16-PATTERNS.md "lib/results.ts" + "lib/score.ts" + Critical Pitfall 3</read_first>
  <action>
    lib/results.ts: extend the internal PitchScoreLite type with `scores: Record<string, number> | null`. Add an EXPORTED pure function normalizePitchScore(r, criteria: PitchCriterion[]): number that (a) if r.scores has keys, sums its values and returns (rawSum / sum(criteria.max)) * 100 (0 when maxTotal is 0), else (b) runs the existing legacy branch: parse total_score, if NaN return 0, return c5Raw greater than 0 ? totalRaw : totalRaw * 1.25. Export it (not module-private) so the co-located test imports it directly. Replace the inline normalization in the aggregate loop with a call to normalizePitchScore, passing the event's criteria (fetched once via getPitchCriteria(eventId) inside computeRanking). Add `scores` to the fetchPitchScores select string ("player_id, total_score, c5, scores"). Keep DEFAULT_PITCH_WEIGHT and clampWeight as-is — computeRanking already accepts opts.pitchWeight; do NOT change its signature beyond what already exists. Import PitchCriterion + getPitchCriteria from @/lib/pitch-criteria.
    lib/score.ts: rename the 3 module constants to exported DEFAULT_SUBMITTED_POINTS/DEFAULT_REVIEWED_POINTS/DEFAULT_VALIDATED_POINTS (keep values 100/25/50). Extend sumPlayerScoreEngagement with an optional 3rd param opts containing submittedPoints/reviewedPoints/validatedPoints, resolving each via the nullish-coalescing fallback to the renamed DEFAULT_* constants. Update all in-file references to the renamed constants. ALSO extend applyBonusMultiplier: add an optional opts containing bonusMultiplierCap and replace the clamp line (lib/score.ts:218 `const factor = Math.min(winner.multiplierFactor, BONUS_MULTIPLIER_CAP)`) with: resolve `const cap = opts?.bonusMultiplierCap ?? BONUS_MULTIPLIER_CAP;` then `const factor = Math.min(winner.multiplierFactor, cap);` — keep the BONUS_MULTIPLIER_CAP import as the default so existing callers are byte-for-byte unchanged (SETTINGS-01: cap now configurable, default = current constant). Do NOT change existing call sites' behavior — both new params are optional and backwards-compatible.
    app/results/page.tsx: fetch event settings (getEventSettings(eventId) — eventId already resolved on this page; if not, resolve via getActiveEvent) and pass `pitchWeight: settings.pitchWeight` into the existing computeRanking({...}) call. Keep ceremony/page.tsx untouched (it calls computeRanking() with no opts so defaults apply).
    tests/unit/results-dynamic.test.ts (CO-LOCATED here so the verify command exercises normalizePitchScore at the creation point — moved from 16-04 per plan-checker): import normalizePitchScore + PitchCriterion from @/lib/results + @/lib/pitch-criteria; write describe blocks covering the 5 normalization behavior cases above with explicit numeric expectations, labeling the legacy cases "retro-compat AgreenTech 4-crit" (×1.25) / "Digi 5-crit". Mirror tests/unit/score.test.ts style; plain ASCII strings.
    R1 GUARD: lib/results.ts and lib/score.ts are HOT ZONES. After editing, run the R1 grep audit (see verification) — no new Player-facing score/rank leak permitted.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:unit -- score results-dynamic</automated>
  </verify>
  <acceptance_criteria>
    - normalizePitchScore is EXPORTED and handles dynamic + both legacy branches; results-dynamic.test.ts (created here) passes all 5 normalization cases.
    - sumPlayerScoreEngagement default-path output unchanged; optional override works.
    - applyBonusMultiplier clamps with opts.bonusMultiplierCap when provided, defaults to BONUS_MULTIPLIER_CAP otherwise.
    - app/results/page.tsx passes pitchWeight from event_settings; ceremony page unchanged.
    - typecheck + existing score unit tests + results-dynamic tests green.
  </acceptance_criteria>
  <done>Dynamic + retro-compat normalization in place and tested at source, engagement + bonus cap parameterized, pitch weight wired from settings, R1 audit clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/journey.ts XP literals -> EventSettings; lib/jury.ts dynamic columns + savePitchScoreFlow scoresJson</name>
  <files>lib/journey.ts, lib/jury.ts, app/actions.ts</files>
  <behavior>
    - getJourneyData with DEFAULT_EVENT_SETTINGS computes the same earnedXp as before (+100 first submission, +50 validate_v1, +100 validate_v2) — no behavior change at defaults.
    - getJourneyData with settings { xpFirstSubmission: 80, xpValidateV1: 40, xpValidateV2: 90 } uses 80/40/90 in place of the literals.
    - getJuryOverview maps a row with scores jsonb into PitchScoreWithComments.scores; a row without scores yields scores=null.
    - savePitchScoreFlow with scoresJson '{"innovation":15}' upserts payload.scores = {innovation:15} AND keeps c1..c5.
    - savePitchScoreFlow without scoresJson upserts c1..c5 only (legacy path), scores absent.
    - savePitchScoreFlow on a backend missing the scores column retries the upsert without scores and still succeeds (pre-migration tolerance).
  </behavior>
  <read_first>lib/journey.ts:346-356; lib/jury.ts:64-72,119-130,270-320; app/actions.ts:1111-1260; 16-PATTERNS.md "lib/jury.ts" + "savePitchScoreFlow" + Shared Pattern "Tolerant pre-migration column access"</read_first>
  <action>
    lib/journey.ts: wire the three XP literals at lines ~348-353 (earnedXp += 100 first submission / += 50 validate_v1 / += 100 validate_v2) to EventSettings. getJourneyData must obtain settings — either by calling getEventSettings(eventId) inside getJourneyData (resolve eventId already available on the journey path) OR by accepting settings as a new param threaded from the caller; pick whichever matches the existing function shape with least surface change (note the choice in the SUMMARY). Replace `earnedXp += 100` (first submission) with `earnedXp += settings.xpFirstSubmission`, `earnedXp += 50` (validate_v1) with `earnedXp += settings.xpValidateV1`, and `earnedXp += 100` (validate_v2) with `earnedXp += settings.xpValidateV2`. Use DEFAULT_EVENT_SETTINGS as the fallback so demo + pre-migration behavior is byte-identical. The `earnedXp += evalEntry.totalScore` line is NOT a literal — leave it untouched. SETTINGS-01/03: after this edit, grep for the literal `earnedXp += 100` and `earnedXp += 50` must return 0 matches in lib/journey.ts.
    lib/jury.ts: add optional `scores?: Record<string, number> | null` to PitchScoreRow and to the public PitchScoreWithComments type; map it in mapPitchScore as `scores: row.scores ?? null`. Add `scores` to the getJuryOverview pitch_scores select string. Extend JuryAggregate with optional `criteriaAvg?: Record<string, number>` and, when rows carry scores, compute per-key averages (key -> mean across jurors who scored). Keep the existing c1Avg..c4Avg/avg100 computation untouched for the legacy path.
    app/actions.ts savePitchScoreFlow: add `scoresJson: z.string().optional().nullable()` to pitchScoreSchema (parallel to the existing optional comment fields — do NOT remove c1..c5). In the action body, after building the existing payload with c1..c5, parse scoresJson tolerantly (try JSON.parse, ignore on failure -> legacy path) and, if it yields a non-empty object, set payload.scores. Keep onConflict: "event_id,player_id,juror_id" unchanged. Wrap the upsert in the tolerant pre-migration retry: if the upsert error message mentions "scores" or "column", delete payload.scores and retry once; otherwise return the error. Never INSERT/UPDATE total_score.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:unit -- actions-schemas</automated>
  </verify>
  <acceptance_criteria>
    - lib/journey.ts XP literals replaced by settings.xpFirstSubmission/xpValidateV1/xpValidateV2 with DEFAULT_EVENT_SETTINGS fallback; the `earnedXp += 100` literal returns 0 grep matches in lib/journey.ts.
    - PitchScoreWithComments + PitchScoreRow carry scores; getJuryOverview selects + maps it.
    - JuryAggregate.criteriaAvg computed when dynamic scores present; legacy averages untouched.
    - savePitchScoreFlow writes scores when present, always writes c1..c5, retries without scores on pre-migration backend.
    - typecheck + actions-schemas unit tests green.
  </acceptance_criteria>
  <done>Journey XP parameterized, jury read layer + save action dynamic-aware with full legacy compat and pre-migration tolerance.</done>
</task>

<task type="auto">
  <name>Task 3: app/jury/jury-form.tsx dynamic criteria rendering</name>
  <files>app/jury/jury-form.tsx, app/jury/page.tsx</files>
  <read_first>app/jury/jury-form.tsx:1-120; app/jury/page.tsx:40-60,100-110,175-250; 16-PATTERNS.md "app/jury/jury-form.tsx" + Critical Pitfall 6 (demo early-exit)</read_first>
  <action>
    app/jury/jury-form.tsx: add an optional `criteria?: PitchCriterion[]` prop (import type from @/lib/pitch-criteria). Compute activeCriteria = (criteria && criteria.length > 0) ? criteria : LEGACY_CRITERIA, where LEGACY_CRITERIA is derived from the existing dict.jury_c1_label..jury_c4_label (4 criteria, max 20, keys c1..c4) — this preserves the current form verbatim when no event criteria exist. Replace the 4 individual c1..c4 state vars with a single `const [scores, setScores] = useState<Record<string, number>>(existing?.scores ?? Object.fromEntries(activeCriteria.map(c => [c.key, 0])))`. Render one numeric input per activeCriterion (label = criterion.label, max = criterion.max, aria-label per criterion), updating the scores map.
    Legacy c1..c4 mapping (positional algorithm — verbatim, per plan-checker): on submit, serialize the scores map into a hidden input name="scoresJson"; ALSO emit hidden inputs c1..c5 derived POSITIONALLY from activeCriteria so the GENERATED total_score column and the lib/results.ts retro-compat path stay consistent. Mapping:
      c1 = scores[activeCriteria[0]?.key] ?? 0
      c2 = scores[activeCriteria[1]?.key] ?? 0
      c3 = scores[activeCriteria[2]?.key] ?? 0
      c4 = scores[activeCriteria[3]?.key] ?? 0
      c5 = 0 (ALWAYS — keeps the ×1.25 legacy normalization consistent for non-jsonb readers; the authoritative dynamic total is computed from the scores jsonb in lib/results.ts)
    Clamp each c1..c4 value to the smallint range before emitting (0..32767; in practice criteria max ≤ 100). For a 5-criteria dynamic event this means c1..c4 carry the first 4 positional values, c5=0, and the scores jsonb carries all 5 keys — the jsonb path is authoritative for the ranking; c1..c4 only feed total_score/legacy readers.
    Keep all existing comment/verdict/is_draft fields and the existing submit/useActionState wiring. Reuse EIC primitives (.input, aria-labels) per 15-UI-SPEC.
    app/jury/page.tsx: when hasSupabaseEnv(), fetch criteria via getPitchCriteria(eventId) (eventId already resolved from getJuryOverview) and pass `criteria={criteria}` to JuryForm. PRESERVE the demo early-exit (the !hasSupabaseEnv() branch rendering t.jury_demo_disabled must remain — do NOT add data fetches before that guard).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Jury form renders N dynamic criteria when present, falls back to the exact legacy 4-criteria form otherwise.
    - Submit emits scoresJson AND positional c1..c4 (c5=0 always); 5-criteria event -> c1..c4 = first 4 positional values, c5=0, scores jsonb carries all 5.
    - existing comment/verdict/draft fields preserved.
    - page.tsx demo early-exit intact; criteria passed only in Supabase mode.
    - typecheck + lint green.
  </acceptance_criteria>
  <done>Dynamic jury form wired end to end with positional legacy fallback and demo-mode safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(Juror)→savePitchScoreFlow | juror-supplied numeric scores + scoresJson cross into pitch_scores; Zod + RLS gate |
| Player surface ← results/score modules | R1 boundary: rank/score must never leak to Player |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | scoresJson in savePitchScoreFlow | mitigate | tolerant JSON.parse (ignore on failure), DB CHECK rejects empty/non-object; c1..c5 still validated by Zod bounds |
| T-16-05 | Information Disclosure | lib/results.ts / lib/score.ts / lib/journey.ts (R1) | mitigate | R1 grep audit post-edit; rank stays GM/Jury-only; ceremony/results pages already role+publication gated |
| T-16-06 | Tampering | pitch_scores.total_score GENERATED | accept | never written by app; computed total path lives in TS — no DB write possible |
</threat_model>

<verification>
- `npm run typecheck`, `npm run lint`, `npm run test:unit` green (score + results-dynamic + actions-schemas).
- lib/journey.ts XP-literal removal: grep for `earnedXp += 100` and `earnedXp += 50` in lib/journey.ts returns 0 matches (literals now read from settings).
- R1 grep audit (CLAUDE.md): `grep -rn "score\|rank\|note\|/100\|/140\|points\|toFixed" app/journey app/results components/results-* components/submission-* --include="*.tsx" | grep -v "app/journey/deliverable/" | grep -v "components/deliverable-score-block"` — no new Player-side match introduced by this plan.
- Retro-compat: normalizePitchScore unit cases (legacy 4-crit ×1.25, 5-crit, dynamic) pass in tests/unit/results-dynamic.test.ts (co-located in Task 1).
</verification>

<success_criteria>
- /jury renders dynamic criteria with legacy fallback (JURY-07).
- computeRanking absorbs dynamic scores while archived 4/5-crit rankings recompute identically (JURY-08).
- pitch weight read from event_settings (SETTINGS-02 read side); XP rules + bonus cap read from event_settings (SETTINGS-01/03 TS side).
</success_criteria>

<output>
Create `.planning/phases/16-jury-parametrable-scoring-configurable/16-02-SUMMARY.md` when done
</output>
