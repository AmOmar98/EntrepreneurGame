---
phase: 05-pitch-jury-results-smoke-test-e2e-deploiement-vercel-prod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/jury/page.tsx
  - app/jury/jury-form.tsx
  - app/actions.ts
  - lib/jury.ts
  - lib/i18n.ts
autonomous: true
requirements: [JURY-01, JURY-02]
must_haves:
  truths:
    - "Un Mentor connecte voit la liste de tous les Players de la cohorte sur /jury"
    - "Pour chaque Player, le Mentor saisit 5 notes (c1..c5) entre 0 et 20"
    - "Soumettre cree une PitchScore (juror_id=user, player_id) ; resoumission update la meme row (unique event_id, player_id, juror_id)"
    - "Le total (somme c1..c5 sur 100) s'affiche apres sauvegarde"
    - "Un Player ou GameMaster non-mentor ne peut pas atteindre /jury (redirige selon role)"
  artifacts:
    - path: "lib/jury.ts"
      provides: "getJuryOverview() - liste Players + PitchScore existante du juror connecte (event courant)"
      exports: ["getJuryOverview", "JuryPlayerRow"]
    - path: "app/jury/page.tsx"
      provides: "Page server-component /jury (Mentor only) qui rend la liste"
    - path: "app/jury/jury-form.tsx"
      provides: "Client component formulaire 5 notes par Player avec useActionState"
    - path: "app/actions.ts"
      provides: "savePitchScoreFlow server action (Zod, upsert pitch_scores)"
      exports: ["savePitchScoreFlow"]
  key_links:
    - from: "app/jury/jury-form.tsx"
      to: "savePitchScoreFlow"
      via: "useActionState"
      pattern: "savePitchScoreFlow"
    - from: "app/actions.ts:savePitchScoreFlow"
      to: "supabase.from('pitch_scores').upsert"
      via: "supabase server client"
      pattern: "pitch_scores"
    - from: "app/jury/page.tsx"
      to: "lib/jury.ts:getJuryOverview"
      via: "import + await dans server component"
      pattern: "getJuryOverview"
---

<objective>
Livrer la page `/jury` (acces Mentor) permettant a un Mentor-Jure de saisir, pour chaque Player de la cohorte, 5 notes pitch (clarte / structure deck / credibilite / qualite roadmap / qualite oral) sur 20 chacune. Implementer la server action `savePitchScoreFlow` qui upsert dans `pitch_scores` (contrainte unique `event_id × player_id × juror_id`).

Purpose: couvrir JURY-01 et JURY-02 - pre-requis du jour 2 du Hack-Days.
Output: route `/jury` fonctionnelle, action upsert validee Zod, lib data-layer dual-mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pitch-jury-results-smoke-test-e2e-deploiement-vercel-prod/05-CONTEXT.md
@CLAUDE.md
@database/schema.sql
@database/rls.sql
@lib/types.ts
@lib/auth.ts
@lib/mentor.ts
@app/actions.ts
@app/mentor/page.tsx
@lib/i18n.ts

<interfaces>
From lib/types.ts:
```typescript
export type PitchScore = {
  id: string; eventId: string; playerId: string; jurorId: string;
  c1: number; c2: number; c3: number; c4: number; c5: number; totalScore: number;
};
export type Player = { id: string; cohortId: string; slug: string; name: string; idea: string | null; ... };
```

From database/schema.sql (table pitch_scores):
- columns: id, event_id, player_id, juror_id, c1..c5 (smallint, check 0..20), total_score (generated)
- unique (event_id, player_id, juror_id)

From database/rls.sql:
- pitch_scores_mentor_self_insert: is_mentor() AND juror_id = auth.uid()
- pitch_scores_mentor_self_update: is_mentor() AND juror_id = auth.uid()

From lib/auth.ts:
```typescript
export async function getCurrentRole(): Promise<AppRole | null>;
export function pathForRole(role: AppRole): string;
```

From app/actions.ts (existing pattern):
```typescript
export type WorkflowState = { ok: boolean; message: string };
// All actions: Zod safeParse -> hasSupabaseEnv -> createClient -> revalidatePath -> WorkflowState
```

From lib/mentor.ts: pattern pour resolution event courant + cohorts -> players.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lib/jury.ts data layer + i18n keys</name>
  <files>lib/jury.ts, lib/i18n.ts</files>
  <action>
Creer `lib/jury.ts` exportant :
- type `JuryPlayerRow = { player: Player; existing: PitchScore | null }`
- `async function getJuryOverview(): Promise<{ eventId: string | null; rows: JuryPlayerRow[] }>` :
  1. createClient(); si null -> retourner `{ eventId: null, rows: [] }` (DATA-03)
  2. supabase.auth.getUser() ; si pas user -> `{ eventId: null, rows: [] }`
  3. Resoudre event courant (mirror pattern `lib/mentor.ts`: events order by starts_at desc limit 1)
  4. cohorts of event -> players via cohort_id (order by name)
  5. Charger pitch_scores du juror connecte pour cet event (juror_id = user.id, event_id) en un round-trip
  6. Mapper en `JuryPlayerRow[]` (existing pour chaque player ou null)
- Type guard / mapper `mapPitchScore(row): PitchScore` (snake_case -> camelCase, score_project numeric -> number)
- Console.error sur chaque erreur Supabase, retour `[]` (pattern lib/mentor.ts).

Ajouter dans `lib/i18n.ts` (FR + EN) les cles :
- `jury_title`, `jury_subtitle`, `jury_col_team`, `jury_col_idea`
- `jury_c1_label` ("Clarte du pitch / Pitch clarity"), `jury_c2_label` ("Structure du deck / Deck structure"), `jury_c3_label` ("Credibilite / Credibility"), `jury_c4_label` ("Qualite de la roadmap / Roadmap quality"), `jury_c5_label` ("Qualite oral / Oral quality")
- `jury_each_max_20`, `jury_total_label`, `jury_save`, `jury_saving`, `jury_saved_at`
- `jury_already_scored`, `jury_empty`, `jury_demo_disabled`, `jury_back`
- `jury_unauthorized` ("Acces reserve aux Mentors / Mentors only")

Strings sans accents (convention etablie dans i18n.ts existant).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>lib/jury.ts compile, getJuryOverview retourne `{ eventId: null, rows: [] }` en mode demo. i18n keys presentes en `fr` et `en`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: savePitchScoreFlow server action (JURY-02)</name>
  <files>app/actions.ts</files>
  <behavior>
- Test conceptuel 1: Zod safeParse echoue (c3=25) -> WorkflowState.ok=false avec message.
- Test 2: Mode demo (hasSupabaseEnv=false) -> WorkflowState.ok=false "Auth backend not configured."
- Test 3: User non-mentor -> WorkflowState.ok=false "Acces reserve aux Mentors."
- Test 4: Insert pour (event,player,juror) sans row existante -> insert reussi -> ok=true.
- Test 5: Re-soumettre meme (event,player,juror) avec valeurs differentes -> upsert (onConflict event_id,player_id,juror_id) update la row existante.
  </behavior>
  <action>
Ajouter dans `app/actions.ts` (a la fin, suivant le pattern existant) :

```typescript
const pitchScoreSchema = z.object({
  playerId: z.string().uuid(),
  eventId: z.string().uuid(),
  c1: z.coerce.number().int().min(0).max(20),
  c2: z.coerce.number().int().min(0).max(20),
  c3: z.coerce.number().int().min(0).max(20),
  c4: z.coerce.number().int().min(0).max(20),
  c5: z.coerce.number().int().min(0).max(20),
});

export async function savePitchScoreFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> { ... }
```

Sequence :
1. `hasSupabaseEnv()` faux -> `{ ok: false, message: "Auth backend not configured." }`
2. Zod `safeParse` sur les 7 champs (FormData.get) ; si fail -> `{ ok: false, message: parsed.error.issues[0]?.message }`
3. `createClient()` ; si null -> meme message demo.
4. `supabase.auth.getUser()` ; si pas user -> `{ ok: false, message: "Not authenticated." }`
5. Verifier role : SELECT app_role FROM profiles WHERE user_id = user.id ; si pas dans `('mentor','game_master')` -> `{ ok: false, message: "Acces reserve aux Mentors." }`
6. `supabase.from('pitch_scores').upsert({ event_id, player_id, juror_id: user.id, c1, c2, c3, c4, c5 }, { onConflict: 'event_id,player_id,juror_id' })`
7. Si erreur Supabase -> `{ ok: false, message: error.message }` (DATA-04: jamais silencieuse).
8. `revalidatePath('/jury')` puis `revalidatePath('/results')` ; retour `{ ok: true, message: "Notes enregistrees." }`.

Conformer au pattern existant : `"use server"` deja en tete, suffix `Flow`, return `WorkflowState`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Action exportee, Zod schema borne 0..20, upsert avec onConflict triple-cle, errors Supabase remontees dans message, role-check applicatif present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Page /jury + formulaire client</name>
  <files>app/jury/page.tsx, app/jury/jury-form.tsx</files>
  <action>
Creer `app/jury/page.tsx` (server component) :
1. `getCurrentRole()` ; si null -> `redirect('/login')` ; si role !== 'mentor' && role !== 'game_master' -> `redirect(pathForRole(role))`.
2. `getLocale(...)` (cookie/header pattern existant si utilise, sinon fallback fr).
3. `const { eventId, rows } = await getJuryOverview()`.
4. Rendre header (`jury_title`, `jury_subtitle`) + tableau / liste de cards. Pour chaque row -> rendre `<JuryForm player={row.player} existing={row.existing} eventId={eventId} dict={d} />`.
5. Si `rows.length === 0` -> message `jury_empty`.
6. Si pas de Supabase env (eventId null + rows vide en local) -> bandeau `jury_demo_disabled`.

Creer `app/jury/jury-form.tsx` (`"use client"`) :
- Props : `{ player, existing, eventId, dict }`.
- `useActionState(savePitchScoreFlow, { ok: false, message: "" })`.
- Champs hidden `playerId`, `eventId` ; 5 inputs `type="number" min=0 max=20 step=1 required` (defaultValue depuis `existing?.cN ?? ""`).
- Total live cote client (sum des 5 inputs controled state) -> affichage "Total : NN/100".
- Bouton submit (`jury_save` / `jury_saving`).
- Affichage ok/erreur depuis state.message.
- Pas de redirect post-submit (l'utilisateur reste sur /jury).

Style Tailwind tokens EIC (--brand-primary etc.) cohérent avec /mentor.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>`/jury` rend la liste des Players, chaque ligne propose un formulaire 5×20, total visible, soumission redonne `ok: true`. Acces redirige les non-mentors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> server action | FormData controllee par utilisateur (cN, playerId, eventId) |
| Server action -> Postgres | Upsert avec RLS (juror_id = auth.uid() force par policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | savePitchScoreFlow inputs | mitigate | Zod borne c1..c5 a [0,20] int + uuid sur playerId/eventId |
| T-05-02 | Elevation of Privilege | non-mentor poste vers /jury | mitigate | Check applicatif role (profiles.app_role) + RLS pitch_scores_mentor_self_insert |
| T-05-03 | Spoofing | un Mentor injecte juror_id != auth.uid() | mitigate | juror_id force cote server depuis user.id (jamais lu de FormData) ; RLS check (juror_id = auth.uid()) |
| T-05-04 | Information Disclosure | Player atteint /jury et voit cohorte | mitigate | redirect cote page server selon role |
| T-05-05 | Repudiation | "qui a note quoi" | accept | updated_at + juror_id present, suffisant pour pilote |
</threat_model>

<verification>
- `/jury` accessible Mentor + GameMaster, redirige Player.
- Saisie 5 notes -> ligne pitch_scores creee.
- Re-saisie meme Player -> meme row mise a jour (verifier en SQL: count row par event,player,juror = 1).
- Inputs c1=21 rejete (Zod + DB check).
- typecheck + lint clean.
</verification>

<success_criteria>
- JURY-01: 5 notes ×20 saisissables sur /jury par Mentor.
- JURY-02: PitchScore upsert (juror_id × player_id × event_id unique).
- DATA-04: action retourne WorkflowState, jamais silencieuse sur erreur Supabase.
</success_criteria>

<output>
Apres execution, creer `.planning/phases/05-pitch-jury-results-smoke-test-e2e-deploiement-vercel-prod/05-01-SUMMARY.md`.
</output>
