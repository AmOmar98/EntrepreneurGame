---
phase: 01-foundation-schema-types-suppression-code-obsol-te
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified:
  - package.json
  - package-lock.json
  - lib/icons.ts
  - eslint.config.mjs
autonomous: true
requirements: [DATA-04, M12]
must_haves:
  truths:
    - "lucide-react is pinned to a real published stable version (latest 0.x), not the suspect ^1.14.0"
    - "All icon imports in lib/icons.ts resolve at build time"
    - "npm run lint passes with zero warnings (or only acceptable, documented exceptions)"
    - "npm run typecheck passes with zero errors"
    - "npm run build passes with zero warnings about deprecated APIs or unresolved modules"
  artifacts:
    - path: package.json
      provides: "Pinned lucide-react version"
      contains: "lucide-react"
  key_links:
    - from: lib/icons.ts
      to: package.json (lucide-react)
      via: "import { ... } from 'lucide-react' resolves"
      pattern: "lucide-react"
---

<objective>
Close Phase 1 ROADMAP success criteria #5: lucide-react repinned to a real version, and
the full toolchain (`npm run lint`, `npm run typecheck`, `npm run build`) clean of
warnings. This plan is intentionally last — it touches a leaf concern after all other
refactors have stabilized.

Purpose: D-18 — the current `^1.14.0` pin is suspect (lucide has not reached v1 stable as
of 2026-05). Move to a real latest stable 0.x release that resolves all icon names used
in `lib/icons.ts`.
Output: Clean dependency pins + clean tooling output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-CONTEXT.md
@CLAUDE.md
@package.json
@lib/icons.ts
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repin lucide-react to latest stable 0.x</name>
  <files>package.json, package-lock.json, lib/icons.ts</files>
  <action>
**1a — Discover the correct version:**
Run `npm view lucide-react versions --json | tail -20` to see published versions. Pick the
latest stable 0.x (e.g. `0.460.0`-ish at time of writing — confirm via npm). Avoid any
pre-1.x release candidates.

**1b — Update `package.json`:**
Replace `"lucide-react": "^1.14.0"` with the pinned version, e.g. `"lucide-react": "^0.460.0"`
(use whatever the actual latest stable returns). Use `^` caret per CLAUDE.md convention.

**1c — Reinstall:**
```
rm -rf node_modules package-lock.json
npm install
```
This regenerates `package-lock.json` cleanly. Commit both `package.json` and `package-lock.json`.

**1d — Validate icon names:**
Open `lib/icons.ts` from plan 03. The icons imported were:
  `Compass, Target, Lightbulb, BarChart3, Wallet, Mic, Rocket, Trophy, FileText, CircleDot, Clock, CheckCircle2, XCircle, RefreshCcw`.

Run `npm run typecheck`. If any icon name no longer exists in the new lucide version, find
its closest replacement at https://lucide.dev/icons/ and update the import + the
`levelIcon` / `submissionStatusIcon` mapping in `lib/icons.ts`. Document the substitution
in the SUMMARY.md.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>package.json has lucide-react pinned to a real ^0.x version; npm install regenerates lock file cleanly; all icon imports in lib/icons.ts resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Clean npm run lint + npm run build (zero warnings)</name>
  <files>eslint.config.mjs, lib/icons.ts</files>
  <action>
Run `npm run lint`. For each warning/error:
  - If it is a real bug, fix the offending file.
  - If it is a stylistic ESLint-plugin-next rule that is acceptable (e.g. `@next/next/no-html-link-for-pages` on a deliberate placeholder), document in `eslint.config.mjs` with an inline-disable on the specific line and a comment explaining why. Do NOT mass-disable rules at config level.
  - Tolerate `react/no-unescaped-entities` only if explicitly fixed by escaping (`l&apos;`).

Run `npm run build`. Address any warnings the same way. The expected output should be:
  - `Compiled successfully` line.
  - Only static page warnings about routes that are dynamic-by-design (e.g., `/journey` reads cookies; warning about `force-dynamic` is acceptable).

Document any tolerated warning in the plan SUMMARY.md so future phases know not to chase it.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>npm run lint exits 0 with zero unexplained warnings; npm run build exits 0 and prints "Compiled successfully"; any tolerated warning is documented in SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: Final cross-cutting verification + Phase 1 acceptance summary</name>
  <files>.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-PHASE-SUMMARY.md</files>
  <action>
Cross-check all 5 ROADMAP Phase 1 success criteria:

  1. Schema applied on fresh Supabase: confirmed by plan 01 task 4 + plan 05 task 3 manual.
  2. lib/data.ts split into types/seed/score/icons: confirmed by plan 03.
  3. Code mort supprimé: confirmed by plan 02 + plan 03.
  4. Login email/password redirect per role: confirmed by plan 05 task 3 manual.
  5. Lucide repinned + lint + typecheck clean: confirmed by plan 06 tasks 1-2.

Write `.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-PHASE-SUMMARY.md` listing:
  - Each success criterion with link to the plan SUMMARY.md that closes it.
  - List of files added / removed / renamed across all 6 plans.
  - List of canonical exports now provided by `lib/types.ts`.
  - The lucide-react version actually pinned (and any icon substitutions made).
  - Any tolerated lint/build warning with rationale.
  - Open items for Phase 2 (e.g., seed real Hack-Days event, build /journey content).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path .planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-PHASE-SUMMARY.md)) { exit 1 }; npm run typecheck; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run lint; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run build; exit $LASTEXITCODE"</automated>
  </verify>
  <done>Phase summary file exists; typecheck + lint + build all pass; all 5 ROADMAP Phase 1 success criteria checked off.</done>
</task>

</tasks>

<verification>
- `npm install` completes without warnings about peer deps for lucide-react.
- `lib/icons.ts` icon imports all resolve.
- `npm run typecheck` exits 0.
- `npm run lint` exits 0 with no unexplained warnings.
- `npm run build` exits 0 and prints "Compiled successfully".
- `.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-PHASE-SUMMARY.md` exists.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #5 closed.
- Phase 1 fully shipped: 5/5 ROADMAP success criteria done.
- Repo ready for Phase 2 (Player flow).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema-types-suppression-code-obsol-te/01-06-SUMMARY.md` plus the cross-plan `01-PHASE-SUMMARY.md`.
</output>
