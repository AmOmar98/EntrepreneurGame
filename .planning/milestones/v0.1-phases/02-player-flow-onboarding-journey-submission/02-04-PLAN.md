---
phase: 02-player-flow-onboarding-journey-submission
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seed_event_hackdays.sql
  - lib/seed/index.ts
  - lib/seed/missions.ts
  - lib/seed/deliverableTemplates.ts
  - lib/seed/players.ts
  - database/README.md
autonomous: true
requirements:
  - EVENT-01
  - EVENT-02
  - DATA-03
must_haves:
  truths:
    - "Un script SQL idempotent seed_event_hackdays.sql cree l'Event Hack-Days Fes-Meknes Mai 2026, ses 6 Missions et ~9 DeliverableTemplates"
    - "Chaque DeliverableTemplate seede a une scoring_rubric JSONB de 4-5 criteres ponderes"
    - "Chaque Mission a un scheduled_at aligne sur la timeline 13-14 mai 2026"
    - "En mode Supabase prod (hasSupabaseEnv()=true), aucun seed demo (atlas-soil, demo-*) ne sort des accesseurs lib/seed/index.ts"
    - "lib/seed/players.ts ne contient plus de noms apparentes en prod (deja conforme — verification + renforcement)"
  artifacts:
    - path: "database/seed_event_hackdays.sql"
      provides: "INSERT idempotents (ON CONFLICT DO NOTHING / DO UPDATE) pour event + missions + deliverable_templates + cohorte"
      contains: "Hack-Days Fes-Meknes"
    - path: "lib/seed/index.ts"
      provides: "Accesseurs dual-mode confirmes : hasSupabaseEnv() -> []"
      contains: "hasSupabaseEnv"
    - path: "database/README.md"
      provides: "Documente l'ordre d'application : schema -> triggers -> rls -> seed_event_hackdays"
  key_links:
    - from: "database/seed_event_hackdays.sql"
      to: "events / missions / deliverable_templates / cohorts"
      via: "INSERT ... ON CONFLICT (slug) DO UPDATE"
      pattern: "on conflict"
    - from: "lib/seed/index.ts"
      to: "hasSupabaseEnv()"
      via: "ternaire de retour"
      pattern: "hasSupabaseEnv\\(\\)"
---

<objective>
Seeder l'Event pilote en SQL idempotent et verrouiller le DATA-03 (anti-leak seed en prod).

Purpose: Sans event/missions/templates en DB, /journey est vide en prod. Sans anti-leak, les noms demo fuiteraient devant les partenaires.
Output: Fichier SQL applicable sur Supabase prod, idempotent, + verification que `lib/seed/*` ne fuit pas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-CONTEXT.md
@database/schema.sql
@database/seed_bootcamp.sql
@lib/seed/index.ts
@lib/seed/missions.ts
@lib/seed/deliverableTemplates.ts
@lib/seed/players.ts

<interfaces>
Schema attendu (cf database/schema.sql) :
- events(slug UNIQUE, name, starts_at, ends_at)
- cohorts(event_id, slug UNIQUE per event, name)
- missions(event_id, level_id, ord) UNIQUE -> ord
- deliverable_templates(mission_id, slug) UNIQUE

Programme Hack-Days (PROJECT.md / spec) :
- Jour 1 (13 mai 2026) : 6 ateliers
- Jour 2 (14 mai 2026) : pitch + resultats

Mapping ateliers -> niveaux (ROADMAP / EVENT-01) :
1. Atelier 1 — L1 Probleme (Personae, Probleme)
2. Atelier 2 — L2 Solution (Esquisse solution, Fiche Produit)
3. Atelier 3 — L3 Marche (Etude marche, BMC partie 1)
4. Atelier 4 — L4 Business model (Couts, prevision ventes)
5. Atelier 5 — L4/L5 (Strategie prix, canaux)
6. Atelier 6 — L5 Pitch (Pitch deck V1)

Spec ROADMAP cite ~9 DeliverableTemplates : BMC, Etude marche, Personae, Fiche Produit + Plan Dev, Couts & previsions, Strategie prix/ventes/canaux, + livrables ateliers 3-4-5 a confirmer. Executor : seeder 9 templates plausibles, slugs descriptifs, rubrics 4-5 criteres ponderes (somme = max_score=100).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ecrire database/seed_event_hackdays.sql idempotent</name>
  <files>database/seed_event_hackdays.sql, database/README.md</files>
  <action>
Creer `database/seed_event_hackdays.sql` :

1. INSERT levels (L0..L7) si non present, ON CONFLICT (id) DO NOTHING — uniquement si pas deja seede par schema.sql/triggers.sql. Verifier dans schema.sql si levels deja popule ; sinon inclure ici.

2. INSERT event :
```sql
insert into public.events (slug, name, starts_at, ends_at)
values ('hack-days-fes-meknes-mai-2026', 'Hack-Days Fes-Meknes Mai 2026',
        '2026-05-13 08:30:00+01', '2026-05-14 17:00:00+01')
on conflict (slug) do update set name = excluded.name, starts_at = excluded.starts_at, ends_at = excluded.ends_at;
```

3. INSERT cohort :
```sql
insert into public.cohorts (event_id, slug, name)
select id, 'cohorte-mai-2026', 'Cohorte Mai 2026' from public.events where slug='hack-days-fes-meknes-mai-2026'
on conflict (event_id, slug) do update set name = excluded.name;
```

4. INSERT 6 missions (atelier 1..6 + presentation jour 2 + pitch jour 2 = peut etre 6 ateliers + 1 pitch + 1 presentation = 6-8 missions ; au choix executor mais minimum 6 cf spec).
   Pour chaque mission : ON CONFLICT (event_id, level_id, ord) DO UPDATE SET title, kind, scheduled_at.
   scheduled_at exact : 13/05 09:00, 11:00, 14:00, 16:00, 14/05 09:00, 11:00, etc. (fuseau Maroc UTC+1).

5. INSERT 9 deliverable_templates avec rubric JSONB :
   Exemple :
```sql
insert into public.deliverable_templates (mission_id, slug, title, description, rubric, max_score, ord)
select m.id, 'personae-v1', 'Fiche Personae',
       'Decrire 1-2 personae du marche cible.',
       '[{"key":"clarity","label":"Clarte","max":25},{"key":"specificity","label":"Specificite","max":25},{"key":"evidence","label":"Preuves","max":25},{"key":"actionable","label":"Actionnable","max":25}]'::jsonb,
       100, 1
from public.missions m join public.events e on e.id=m.event_id
where e.slug='hack-days-fes-meknes-mai-2026' and m.level_id='L1_problem' and m.ord=1
on conflict (mission_id, slug) do update set title=excluded.title, description=excluded.description, rubric=excluded.rubric, max_score=excluded.max_score, ord=excluded.ord;
```

   Repeter pour les 9 templates : Personae, Enonce du probleme, Esquisse solution, Fiche Produit + Plan Dev, Etude marche, Business Model Canvas, Couts & previsions ventes, Strategie prix/ventes/canaux, Pitch deck V1. Slugs descriptifs ASCII.

6. Le script DOIT etre re-executable sans erreur (idempotent). Tester mentalement chaque INSERT avec ON CONFLICT.

7. Mettre a jour `database/README.md` (ou le creer si absent) avec l'ordre d'application :
```
1. (fresh project) drop schema public cascade; create schema public; grant ...
2. database/schema.sql
3. database/triggers.sql
4. database/rls.sql
5. database/seed_event_hackdays.sql  # nouveau Phase 2
6. (optionnel) database/seed_bootcamp.sql  # legacy demo, NE PAS appliquer en prod
```
Mentionner explicitement que `seed_bootcamp.sql` est legacy/demo et NE DOIT PAS etre applique en Supabase prod (BRAND-05 / DATA-03).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('database/seed_event_hackdays.sql','utf8');if(!s.includes('on conflict'))throw new Error('non-idempotent');if((s.match(/insert into public.deliverable_templates/g)||[]).length<9)throw new Error('moins de 9 templates');console.log('ok')"</automated>
  </verify>
  <done>
- `database/seed_event_hackdays.sql` existe et est idempotent (chaque INSERT a un ON CONFLICT).
- Au moins 6 missions et 9 deliverable_templates avec rubrics JSONB de 4-5 criteres ponderes (somme = max_score).
- `database/README.md` documente l'ordre d'application et la mise en garde sur seed_bootcamp.sql.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verifier + renforcer DATA-03 anti-leak (lib/seed/*)</name>
  <files>lib/seed/index.ts, lib/seed/missions.ts, lib/seed/deliverableTemplates.ts, lib/seed/players.ts</files>
  <action>
1. Auditer `lib/seed/index.ts` : confirmer que les 3 accesseurs `seedPlayers`, `seedMissions`, `seedDeliverableTemplates` retournent `[]` quand `hasSupabaseEnv()` est true. (Code actuel deja conforme — verification + commentaire d'intention DATA-03.)

2. Auditer `lib/seed/players.ts`, `lib/seed/missions.ts`, `lib/seed/deliverableTemplates.ts` :
   - Aucun nom contenant "atlas-soil", "atlas", references partenaires (Tamwilcom, Bank of Africa, Innov Invest, Bluespace).
   - Tous les noms doivent etre neutres ("Demo Team Alpha", "Demo - Atelier Probleme", etc.) — code actuel deja conforme.
   - Si un seul fichier contient une fuite, le corriger.

3. Ajouter en tete de chaque fichier `lib/seed/*.ts` un commentaire explicite :
```typescript
// DEMO DATA ONLY — sert uniquement quand hasSupabaseEnv() est false.
// En mode Supabase prod, lib/seed/index.ts retourne [] (cf DATA-03).
// Ne JAMAIS y referencer de partenaire reel ni de seed de pre-prod.
```

4. Ajouter un test runtime simple dans `lib/seed/index.ts` (commentaire explicatif suffit ; pas de test runner installe). Documenter dans le SUMMARY que la verification se fait par grep sur le repo.

5. Verifier qu'aucune autre source du repo n'importe directement `demoPlayers`, `demoMissions`, `demoDeliverableTemplates` en bypassant les accesseurs `seedXxx`. Si tel cas existe, corriger.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>
- Accesseurs `seedPlayers/seedMissions/seedDeliverableTemplates` retournent [] en mode Supabase prod.
- Aucun nom seed apparent dans le repo (atlas-soil, partenaires).
- Commentaires DATA-03 ajoutes en tete des fichiers seed/*.
- typecheck + lint clean.
  </done>
</task>

</tasks>

<verification>
- Appliquer `database/seed_event_hackdays.sql` sur Supabase prod (post schema/triggers/rls).
- Re-appliquer le meme script -> aucune erreur (idempotent).
- Verifier 6+ missions et 9 deliverable_templates en DB.
- En mode prod (env vars set), `seedPlayers()`/`seedMissions()`/`seedDeliverableTemplates()` retournent [].
- Grep "atlas-soil|atlas-" -> aucun match dans le repo (hors README documentant).
</verification>

<success_criteria>
- EVENT-01 : Event seed Hack-Days + 6 missions + 9 deliverable_templates en SQL idempotent. ✓
- EVENT-02 : Chaque template a rubric JSONB 4-5 criteres ponderes + due_at via mission.scheduled_at. ✓
- DATA-03 : aucune fuite seed en prod, accesseurs dual-mode confirmes. ✓
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-player-flow-onboarding-journey-submission/02-04-SUMMARY.md` documentant :
- Liste exacte des 9 templates seedes avec rubrics et ponderation.
- Liens missions <-> niveaux.
- Procedure de verification anti-leak (commande grep + checklist).
</output>
</content>
