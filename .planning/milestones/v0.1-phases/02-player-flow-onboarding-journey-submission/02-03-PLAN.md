---
phase: 02-player-flow-onboarding-journey-submission
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/actions.ts
  - app/journey/deliverable/[id]/page.tsx
  - components/submission-form.tsx
  - components/submission-readonly.tsx
  - lib/i18n.ts
autonomous: true
requirements:
  - SUBMIT-01
  - SUBMIT-02
  - SUBMIT-04
  - DATA-04
must_haves:
  truths:
    - "Sur /journey/deliverable/[id], le Player voit le titre, la description, la rubric, et un formulaire de soumission V1"
    - "Le formulaire impose au moins un de proof_url (https://) ou proof_text (markdown <=4000 chars), avec validation Zod serveur"
    - "Apres soumission V1, le formulaire est verrouille et affiche le contenu en lecture seule + status submitted_v1"
    - "Un Player ne peut pas acceder a une page deliverable d'un autre Player (404 / forbidden)"
    - "Un Player ne peut pas soumettre pour un autre Player (server action retourne erreur)"
  artifacts:
    - path: "app/journey/deliverable/[id]/page.tsx"
      provides: "Page detail deliverable + form submission OU lecture seule selon status"
    - path: "components/submission-form.tsx"
      provides: "Client form (useActionState) avec champs proof_url, proof_text, kind toggle"
      contains: "useActionState"
    - path: "components/submission-readonly.tsx"
      provides: "Affichage lecture seule de la submission existante"
    - path: "app/actions.ts"
      provides: "submitDeliverable server action"
      exports: ["submitDeliverable"]
  key_links:
    - from: "components/submission-form.tsx"
      to: "submitDeliverable"
      via: "useActionState(submitDeliverable, initialState)"
      pattern: "submitDeliverable"
    - from: "submitDeliverable"
      to: "submissions table"
      via: "supabase.from('submissions').insert(...)"
      pattern: "from\\(.submissions.\\)"
    - from: "submitDeliverable"
      to: "auth.uid + player_members ownership check"
      via: "select player_members where player_id=? and user_id=auth.uid()"
      pattern: "player_members"
---

<objective>
Livrer la page /journey/deliverable/[id] et l'action submitDeliverable V1, avec verrouillage post-soumission et garde d'ownership.

Purpose: C'est l'acte productif principal du Player pendant les ateliers — sans ce flow, aucun livrable ne peut etre note.
Output: Page detail fonctionnelle, action `submitDeliverable` avec validation Zod, ownership check applicatif + RLS, UI verrouillee apres V1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-CONTEXT.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-01-PLAN.md
@.planning/phases/02-player-flow-onboarding-journey-submission/02-02-PLAN.md
@app/actions.ts
@lib/types.ts
@lib/journey.ts
@database/schema.sql
@database/rls.sql

<interfaces>
From lib/types.ts:
```typescript
export type SubmissionKind = "proof_url" | "proof_text";
export type SubmissionStatus = "draft" | "submitted_v1" | "feedback_received" | "submitted_v2" | "validated" | "rejected";
export type Submission = SubmissionBase & ({ version: 1 } | { version: 2 });
```

DB submissions schema:
- (player_id, deliverable_template_id, version) UNIQUE
- check : (kind='proof_url' AND proof_url NOT NULL AND proof_text NULL) OR (kind='proof_text' AND proof_text NOT NULL AND proof_url NULL)
- status default 'submitted_v1'

RLS (database/rls.sql) deja en place :
- submissions_member_self_insert : (is_my_player(player_id) AND submitted_by = auth.uid()) OR is_game_master()
- submissions_member_self_update : idem
- submissions_member_or_mentor_select : is_my_player(player_id) OR is_mentor()

Phase 3 gerera V2 (request_v2 verdict) — Plan 03 livre uniquement V1 (SUBMIT-02 : verrouillage tant que V2 non demandee).

httpsUrl pattern (Phase 1) : z.string().url().refine(u => u.startsWith("https://"), "URL doit commencer par https://")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: submitDeliverable server action + Zod schema</name>
  <files>app/actions.ts</files>
  <action>
Ajouter dans `app/actions.ts` :

1. Schema Zod :
```typescript
const httpsUrl = z.string().url().refine(u => u.startsWith("https://"), "URL doit commencer par https://");
const submissionSchema = z.object({
  deliverableTemplateId: z.string().uuid(),
  kind: z.enum(["proof_url", "proof_text"]),
  proofUrl: z.string().optional(),
  proofText: z.string().max(4000).optional(),
}).superRefine((data, ctx) => {
  if (data.kind === "proof_url") {
    const r = httpsUrl.safeParse(data.proofUrl);
    if (!r.success) ctx.addIssue({ code: "custom", message: "URL https:// requise", path: ["proofUrl"] });
  } else {
    if (!data.proofText || data.proofText.trim().length < 10) {
      ctx.addIssue({ code: "custom", message: "Texte de preuve requis (>=10 chars)", path: ["proofText"] });
    }
  }
});
```

2. Action :
```typescript
export async function submitDeliverable(_prev: WorkflowState, formData: FormData): Promise<WorkflowState> {
  if (!hasSupabaseEnv()) return { ok:false, message:"Backend non configure." };
  const supabase = await createClient();
  if (!supabase) return { ok:false, message:"Backend non configure." };

  const parsed = submissionSchema.safeParse({
    deliverableTemplateId: formData.get("deliverableTemplateId"),
    kind: formData.get("kind"),
    proofUrl: formData.get("proofUrl") ?? undefined,
    proofText: formData.get("proofText") ?? undefined,
  });
  if (!parsed.success) return { ok:false, message: parsed.error.issues[0]?.message ?? "Donnees invalides" };

  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok:false, message:"Non authentifie." };

  // Resolve player + ownership check (SUBMIT-04 applicative)
  const { data: membership, error: memErr } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (memErr) return { ok:false, message: memErr.message };
  if (!membership) return { ok:false, message:"Aucun Player rattache." };

  // Block if V1 already exists (SUBMIT-02)
  const { data: existing, error: selErr } = await supabase
    .from("submissions")
    .select("id, version, status")
    .eq("player_id", membership.player_id)
    .eq("deliverable_template_id", parsed.data.deliverableTemplateId)
    .order("version", { ascending: false });
  if (selErr) return { ok:false, message: selErr.message };

  if (existing && existing.length > 0) {
    const top = existing[0];
    // Only allow V2 if top is feedback_received (Phase 3) — for Plan 03, refuse all re-submits
    return { ok:false, message:"Une soumission V1 existe deja. Attendez le feedback du Mentor." };
  }

  const { error: insErr } = await supabase.from("submissions").insert({
    player_id: membership.player_id,
    deliverable_template_id: parsed.data.deliverableTemplateId,
    version: 1,
    kind: parsed.data.kind,
    proof_url: parsed.data.kind === "proof_url" ? parsed.data.proofUrl : null,
    proof_text: parsed.data.kind === "proof_text" ? parsed.data.proofText : null,
    status: "submitted_v1",
    submitted_by: user.id,
  });
  if (insErr) return { ok:false, message: insErr.message };

  revalidatePath("/journey");
  revalidatePath(`/journey/deliverable/${parsed.data.deliverableTemplateId}`);
  return { ok:true, message:"Livrable soumis." };
}
```

DATA-04 : aucun `return;` silencieux ; chaque erreur retournee dans WorkflowState.message.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>
- `submitDeliverable` exportee, validation Zod stricte (au moins un de proof_url/proof_text + https://).
- Refus si une V1 existe deja (verrouillage SUBMIT-02).
- Ownership check applicatif via player_members (SUBMIT-04).
- typecheck + lint clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Page detail deliverable + form/readonly switch</name>
  <files>app/journey/deliverable/[id]/page.tsx, components/submission-form.tsx, components/submission-readonly.tsx, lib/i18n.ts</files>
  <action>
1. `components/submission-form.tsx` ("use client") :
   - useActionState(submitDeliverable, { ok:false, message:"" })
   - Toggle radio kind (proof_url / proof_text)
   - Conditionnel : input url (si proof_url) ou textarea maxLength=4000 (si proof_text)
   - hidden input deliverableTemplateId
   - Bouton submit + state.message
   - useEffect : si state.ok -> router.refresh() (la page server re-render en mode lecture seule)

2. `components/submission-readonly.tsx` (server component) :
   - Props : `{ submission: Submission, statusLabel: string }`
   - Affiche kind, proof_url (link cliquable target=_blank rel=noopener) ou proof_text (pre-formatted), status, submittedAt formate FR.
   - Banniere : "Livrable verrouille — en attente de feedback Mentor." pour status=submitted_v1.

3. `app/journey/deliverable/[id]/page.tsx` (server component) :
   - Params : `{ id: string }` (deliverable_template_id)
   - getCurrentUser() ; null -> redirect /login
   - getCurrentRole() ; != 'player' -> redirect pathForRole
   - hasSupabaseEnv() guard ; sinon afficher "Mode demo : soumission desactivee."
   - Resolve membership : select player_members where user_id; si null -> notFound() (404)
   - Fetch deliverable_template by id ; si null -> notFound()
   - Fetch latest submission for (player_id, deliverable_template_id) ordered by version desc limit 1
   - Si submission && submission.status IN (submitted_v1, submitted_v2, validated) -> render `<SubmissionReadonly />`
   - Si submission && submission.status === feedback_received -> Phase 3 (V2) — pour Plan 03 afficher "Feedback recu, V2 a venir Phase 3."
   - Sinon -> render `<SubmissionForm deliverableTemplateId=id />` avec titre, description, rubric (liste lisible).
   - Garde supplementaire ownership : la query par RLS retournera 0 rows si Player non proprietaire — `notFound()` cote app.

4. Ajouter cles i18n FR ASCII : `submission.title`, `submission.kind.proof_url`, `submission.kind.proof_text`, `submission.locked`, `submission.submit`, `submission.urlPlaceholder`, etc.

NOTE : creer le dossier `app/journey/deliverable/[id]/` (n'existe pas encore).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
- /journey/deliverable/[id] rend le formulaire ou la version readonly selon submission existante.
- Acces a un id d'un autre Player -> notFound (RLS + check applicatif).
- Soumission V1 -> insert submissions, revalidate, retour avec banniere verrouille.
- Tentative double V1 -> message d'erreur explicite.
- Build clean.
  </done>
</task>

</tasks>

<verification>
- Login Player -> /journey -> click "Soumettre" sur un deliverable a_rendre -> formulaire.
- Submit https:// valide -> page se re-render en lecture seule.
- Refresh -> banniere "verrouille".
- Acces direct a un /journey/deliverable/<id-autre-player> -> notFound.
- typecheck + lint + build clean.
</verification>

<success_criteria>
- SUBMIT-01 : proof_url (https://) OU proof_text (<=4000) avec Zod serveur. ✓
- SUBMIT-02 : verrouillage post V1, refus deuxieme submit. ✓
- SUBMIT-04 : RLS + check applicatif via player_members. ✓
- DATA-04 : pas de swallow, WorkflowState avec message systematique. ✓
</success_criteria>

<output>
Apres completion, creer `.planning/phases/02-player-flow-onboarding-journey-submission/02-03-SUMMARY.md` documentant :
- Mecanisme verrouillage exact (V1 -> readonly tant que verdict pas request_v2).
- Comportement notFound vs forbidden pour un id d'un autre Player.
- Strategie router.refresh() apres submit reussi.
</output>
</content>
