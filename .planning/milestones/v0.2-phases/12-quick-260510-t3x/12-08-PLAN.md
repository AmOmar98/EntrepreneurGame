---
phase: "12-quick-260510-t3x"
plan: "12-08"
type: execute
wave: 3
depends_on:
  - "12-06"
files_modified:
  - components/bonus-claim-form.tsx
  - components/bonus-status-badge.tsx
autonomous: false
requirements:
  - T3X-EXPANSION-BONUS-UI-PLAYER
must_haves:
  truths:
    - "components/bonus-claim-form.tsx existe : client component 'use client' qui consomme claimBonusEventFlow via useActionState"
    - "Form contient input type='url' name='docUrl' required pattern='https://.*' (defense-in-depth client + serveur)"
    - "Form contient input type='hidden' name='type' value={bonusType prop}"
    - "Form contient input type='text' name='title' required minLength=3 maxLength=200"
    - "Form contient textarea name='description' optional maxLength=2000"
    - "Form affiche state.message en role='status' (ok) ou role='alert' (error)"
    - "components/bonus-status-badge.tsx existe : pure presentational badge qualitatif (Boost actif / En attente / Rejete) — JAMAIS chiffre"
    - "Badge consomme prop status: BonusStatus + optional consumedAt: string|null"
    - "Badge utilise i18n keys bonus_status_validated / bonus_status_submitted / bonus_status_rejected"
    - "Aucun rendu numerique dans bonus-status-badge.tsx (R1 STRICT — grep score|/100|toFixed|points|multiplier_factor|number = 0)"
    - "Form utilise pattern submission-form.tsx existant (useActionState, useEffect router.refresh, dictionaries.fr)"
    - "ASCII pur dans le code TS — i18n FR consume via t.bonus_xxx"
    - "npm run typecheck + lint clean"
    - "R1 préservé : aucun multiplierFactor, score, rank, /100 dans le JSX rendu Player"
    - "R3 préservé : pas de DOM disabled, pas de blocage cross-mission"
  artifacts:
    - path: "components/bonus-claim-form.tsx"
      provides: "Client form Player pour soumettre bonus claim (URL HTTPS + title + description) via claimBonusEventFlow"
      contains: "use client|claimBonusEventFlow|useActionState|httpsUrl"
    - path: "components/bonus-status-badge.tsx"
      provides: "Badge qualitatif Player (Boost actif / En attente / Rejete) sans chiffre"
      contains: "bonus_status_validated|bonus_status_submitted|bonus_status_rejected"
  key_links:
    - from: "components/bonus-claim-form.tsx"
      to: "app/actions.ts:claimBonusEventFlow"
      via: "useActionState(claimBonusEventFlow, initialState) — Plan 06 server action"
      pattern: "useActionState\\(claimBonusEventFlow"
    - from: "components/bonus-status-badge.tsx"
      to: "lib/i18n.ts dictionaries.fr.bonus_status_*"
      via: "switch/case ou map by status -> i18n key"
      pattern: "bonus_status_validated|bonus_status_submitted|bonus_status_rejected"
---

<objective>
Wave 3 — UI Player pour le claim bonus (D-02/D-03). 2 composants :
1. **`bonus-claim-form.tsx`** : form client-side pour soumettre un bonus (URL HTTPS preuve + titre + description optionnelle) via `claimBonusEventFlow` Plan 06.
2. **`bonus-status-badge.tsx`** : badge qualitatif R1-strict (pas de chiffre) pour afficher l'état (`Boost actif` / `En attente de validation` / `Bonus rejete`).

**Pattern à reproduire** : `components/submission-form.tsx` (cf. context Plan 08). Même conventions :
- `'use client'` directive
- `useActionState(action, initialState)`
- `useEffect(() => { if (state.ok) router.refresh(); }, [state.ok, router])`
- Inline styles + `dictionaries.fr` consume

**R1 STRICT** : le badge `bonus-status-badge.tsx` est notre frontline pour la garde R1. Aucun chiffre. Aucun multiplier_factor. Juste un libellé qualitatif + une couleur.

**Scope plan** : 2 composants nouveaux. Pas d'intégration dans `/journey` page (Plan 10 fera la surface). Pas de Mentor side (Plan 10 fera `app/mentor/bonus/[id]/page.tsx`).

**Output** :
- 2 nouveaux fichiers `.tsx`
- npm checks clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@components/submission-form.tsx
@app/actions.ts
@lib/types.ts
@lib/i18n.ts

<interfaces>
<!-- Pattern submission-form.tsx -->

```typescript
"use client";

import { useActionState, useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { submitDeliverable, type WorkflowState } from "@/app/actions";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;

const initialState: WorkflowState = { ok: false, message: "" };

export function SubmissionForm({ deliverableTemplateId, version = 1 }: { ... }) {
  const router = useRouter();
  const [state, formAction, pending] = useActionState(submitDeliverable, initialState);

  useEffect(() => {
    if (state.ok) router.refresh();
  }, [state.ok, router]);

  return (
    <form action={formAction} style={{...}}>
      <input type="hidden" name="deliverableTemplateId" value={deliverableTemplateId} />
      <input type="url" name="proofUrl" required pattern="https://.*" .../>
      <button type="submit" disabled={pending}>{pending ? "..." : "Submit"}</button>
      {state.message ? <p role={state.ok ? "status" : "alert"}>{state.message}</p> : null}
    </form>
  );
}
```

<!-- Props attendues -->

```typescript
// bonus-claim-form.tsx
type BonusClaimFormProps = {
  bonusType: BonusType;
  defaultTitle?: string;     // from BONUS_DEFAULTS[type].titleFr
};

// bonus-status-badge.tsx
type BonusStatusBadgeProps = {
  status: BonusStatus;
  consumedAt?: string | null;  // optional — if status='validated' && consumedAt!==null, label "Boost consomme"
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer components/bonus-claim-form.tsx (Player form claim bonus)</name>
  <files>components/bonus-claim-form.tsx</files>
  <read_first>
    - `components/submission-form.tsx` (179 lignes complètes — pattern de référence)
    - `app/actions.ts` lignes ajoutées Plan 06 (claimBonusEventFlow signature)
    - `lib/types.ts` (BonusType + BONUS_DEFAULTS — Plan 05)
    - `lib/i18n.ts` (keys bonus_claim_* — Plan 06 Task 2)
  </read_first>
  <action>
**Créer `components/bonus-claim-form.tsx`** avec EXACTEMENT le contenu suivant :

```typescript
// T3X-EXPANSION wave 3 / plan 08 — Player bonus claim form (D-02 / D-03).
// Pattern aligned on components/submission-form.tsx (link-based proof, mailto-free here).
// R1 preserved : no score / multiplier_factor in render.
// R3 preserved : no DOM disabled, no cross-mission gate.
"use client";

import { useActionState, useEffect } from "react";
import { useRouter } from "next/navigation";
import { claimBonusEventFlow, type WorkflowState } from "@/app/actions";
import { dictionaries } from "@/lib/i18n";
import { BONUS_DEFAULTS, type BonusType } from "@/lib/types";

const t = dictionaries.fr;

const initialState: WorkflowState = { ok: false, message: "" };

export function BonusClaimForm({
  bonusType,
  defaultTitle,
}: {
  bonusType: BonusType;
  defaultTitle?: string;
}) {
  const router = useRouter();
  const [state, formAction, pending] = useActionState(claimBonusEventFlow, initialState);

  useEffect(() => {
    if (state.ok) {
      router.refresh();
    }
  }, [state.ok, router]);

  const initialTitle = defaultTitle ?? BONUS_DEFAULTS[bonusType]?.titleFr ?? "";

  return (
    <form
      action={formAction}
      style={{
        display: "flex",
        flexDirection: "column",
        gap: 16,
        padding: 16,
        border: "1px solid #e2e8f0",
        borderRadius: 8,
        maxWidth: 640,
      }}
    >
      <input type="hidden" name="type" value={bonusType} />

      <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 14 }}>
        <span style={{ fontWeight: 600, color: "#0f172a" }}>{t.bonus_claim_title_label}</span>
        <input
          type="text"
          name="title"
          defaultValue={initialTitle}
          required
          minLength={3}
          maxLength={200}
          style={{
            padding: "8px 12px",
            border: "1px solid #cbd5e1",
            borderRadius: 6,
            fontSize: 14,
          }}
        />
      </label>

      <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 14 }}>
        <span style={{ fontWeight: 600, color: "#0f172a" }}>{t.bonus_claim_url_label}</span>
        <input
          type="url"
          name="docUrl"
          placeholder={t.bonus_claim_url_placeholder}
          required
          pattern="https://.*"
          style={{
            padding: "8px 12px",
            border: "1px solid #cbd5e1",
            borderRadius: 6,
            fontSize: 14,
          }}
        />
      </label>

      <label style={{ display: "flex", flexDirection: "column", gap: 6, fontSize: 14 }}>
        <span style={{ fontWeight: 600, color: "#0f172a" }}>
          {t.bonus_claim_description_label}
        </span>
        <textarea
          name="description"
          rows={4}
          maxLength={2000}
          style={{
            padding: "8px 12px",
            border: "1px solid #cbd5e1",
            borderRadius: 6,
            fontSize: 14,
            fontFamily: "inherit",
            resize: "vertical",
          }}
        />
      </label>

      <button
        type="submit"
        disabled={pending}
        style={{
          padding: "10px 16px",
          background: pending ? "#94a3b8" : "#1d4ed8",
          color: "#fff",
          border: "none",
          borderRadius: 6,
          fontSize: 14,
          fontWeight: 600,
          cursor: pending ? "wait" : "pointer",
          alignSelf: "flex-start",
        }}
      >
        {pending ? t.bonus_claim_submitting : t.bonus_claim_submit}
      </button>

      {state.message ? (
        <p
          role={state.ok ? "status" : "alert"}
          style={{
            margin: 0,
            fontSize: 13,
            color: state.ok ? "#15803d" : "#b91c1c",
          }}
        >
          {state.message}
        </p>
      ) : null}
    </form>
  );
}
```

**IMPORTANT** :
- `'use client'` directive ligne 5.
- `disabled={pending}` est OK ici — c'est le pattern submission-form (button disabled pendant submit, NOT un blocage cross-mission). R3 OK.
- `pattern="https://.*"` defense-in-depth (zod côté serveur avec httpsUrl shared schema fait le vrai gate).
- Pas de logique de score, pas d'affichage de multiplier_factor.
- ASCII pur dans le code TS lui-même. Les valeurs FR avec accents passent par `t.bonus_claim_xxx`.

**NE PAS** :
- Importer `useState` si pas utilisé.
- Inline `style.color: #...` qui changerait selon le status (utiliser le badge dédié).
- Render le `multiplierFactor` même en commentaire JSX dans le code source.
- Faire un fetch client-side (le form est purement form action — server-side handles).

**Vérification** :
```powershell
npm run typecheck
# Attendu : 0 erreur

npm run lint
# Attendu : 0 erreur

(Select-String -Pattern "claimBonusEventFlow|useActionState" components/bonus-claim-form.tsx).Count
# Attendu : >= 2

Select-String -Pattern "multiplier|score|rank|/100|toFixed" components/bonus-claim-form.tsx
# Attendu : 0 hit (R1 STRICT)
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path components/bonus-claim-form.tsx
      # Attendu : True

      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "useActionState\(claimBonusEventFlow" components/bonus-claim-form.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "type=\"url\"" components/bonus-claim-form.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "pattern=\"https://" components/bonus-claim-form.tsx).Count
      # Attendu : 1

      Select-String -Pattern "multiplier|/100|toFixed" components/bonus-claim-form.tsx
      # Attendu : 0 hit (R1)
    </automated>
  </acceptance_criteria>
  <done>
- bonus-claim-form.tsx créé
- Pattern submission-form aligned
- 'use client' directive
- useActionState + useEffect router.refresh
- httpsUrl pattern client-side
- R1 STRICT (0 hit score/multiplier)
- R3 OK (disabled={pending} = submit button only)
- npm checks clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer components/bonus-status-badge.tsx (badge qualitatif R1-strict)</name>
  <files>components/bonus-status-badge.tsx</files>
  <read_first>
    - `lib/types.ts` (BonusStatus type)
    - `lib/i18n.ts` (keys bonus_status_validated / submitted / rejected)
  </read_first>
  <action>
**Créer `components/bonus-status-badge.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 08 — Player-facing bonus status badge.
// R1 STRICT : QUALITATIVE LABEL ONLY. Aucun chiffre, aucun multiplier_factor,
// aucun score. Si l'on doit un jour exposer plus d'info Player, ajouter une
// nouvelle prop typee literally (pas de number).
//
// Server component — aucun state runtime, juste un switch sur status.
import { dictionaries } from "@/lib/i18n";
import type { BonusStatus } from "@/lib/types";

const t = dictionaries.fr;

const STYLE_BY_STATUS: Record<
  BonusStatus,
  { background: string; color: string; border: string }
> = {
  draft: { background: "#f1f5f9", color: "#475569", border: "#cbd5e1" },
  submitted: { background: "#fef3c7", color: "#92400e", border: "#fcd34d" },
  validated: { background: "#dcfce7", color: "#166534", border: "#86efac" },
  rejected: { background: "#fee2e2", color: "#991b1b", border: "#fca5a5" },
};

const LABEL_BY_STATUS: Record<BonusStatus, string> = {
  draft: t.bonus_status_submitted, // draft is rare — show "in waiting" same as submitted
  submitted: t.bonus_status_submitted,
  validated: t.bonus_status_validated,
  rejected: t.bonus_status_rejected,
};

export function BonusStatusBadge({
  status,
  consumedAt,
}: {
  status: BonusStatus;
  consumedAt?: string | null;
}) {
  const style = STYLE_BY_STATUS[status];
  const label =
    status === "validated" && consumedAt !== null && consumedAt !== undefined
      ? `${t.bonus_status_validated} (consomme)`
      : LABEL_BY_STATUS[status];

  return (
    <span
      style={{
        display: "inline-flex",
        alignItems: "center",
        gap: 6,
        padding: "4px 10px",
        background: style.background,
        color: style.color,
        border: `1px solid ${style.border}`,
        borderRadius: 999,
        fontSize: 12,
        fontWeight: 600,
        textTransform: "none",
        letterSpacing: 0,
      }}
      role="status"
      aria-label={label}
    >
      {label}
    </span>
  );
}
```

**IMPORTANT** :
- Server component (pas `'use client'`) — aucun state runtime, juste un switch.
- `consumedAt` prop optionnelle pour distinguer "Boost actif" (validated, non consumed) vs "Boost consomme" (validated, consumed).
- Pas de prop `multiplierFactor` (R1 STRICT).
- Pas d'affichage chiffré.
- ASCII pur dans le code (`consomme` pas `consommé` — note : le `t.bonus_status_validated` peut contenir des accents, le suffix string concat est ASCII).

**NE PAS** :
- Importer `clsx` (les inline styles suffisent ici).
- Ajouter une prop `factor: number` (R1).
- Ajouter une prop `score: number` (R1).
- Faire conditional render de chiffres.

**Vérification** :
```powershell
Select-String -Pattern "multiplier|score|rank|/100|number|toFixed|points" components/bonus-status-badge.tsx
# Attendu : 0 hit STRICT
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path components/bonus-status-badge.tsx
      # Attendu : True

      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "BonusStatusBadge" components/bonus-status-badge.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "import.*BonusStatus" components/bonus-status-badge.tsx).Count
      # Attendu : 1

      Select-String -Pattern "multiplier|score|rank|/100|/140|toFixed|points" components/bonus-status-badge.tsx
      # Attendu : 0 hit STRICT (R1)

      Select-String -Pattern "use client" components/bonus-status-badge.tsx
      # Attendu : 0 hit (server component)
    </automated>
  </acceptance_criteria>
  <done>
- bonus-status-badge.tsx créé
- Server component (no 'use client')
- BonusStatus prop avec switch via map
- consumedAt prop optionnelle
- 0 hit R1-violation (multiplier/score/rank)
- npm checks clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 3: Audit R1/R3 manuel + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --cached components/bonus-claim-form.tsx components/bonus-status-badge.tsx`
    - `.claude/agents/eic-pedagogical-advisor.md` (R1/R3)
  </read_first>
  <action>
**Pré-checkpoint** : Tasks 1+2 complétées, npm clean.

**Action Claude post-approval** :
```bash
git add components/bonus-claim-form.tsx components/bonus-status-badge.tsx
git commit -m "$(cat <<'EOF'
feat(t3x-bonus-ui): add Player bonus claim form + qualitative status badge (R1-strict)

Wave 3 / plan 08 : 2 composants Player UI pour bonus events. Pattern aligne
sur components/submission-form.tsx (useActionState, link-based proof).

- bonus-claim-form.tsx : 'use client', useActionState(claimBonusEventFlow),
  inputs hidden type / text title / url docUrl pattern=https / textarea desc,
  defaultValue title via BONUS_DEFAULTS[type].titleFr (Plan 05 const)
- bonus-status-badge.tsx : server component, switch sur BonusStatus, libelle
  qualitatif via i18n (Boost actif / En attente / Bonus rejete), distinction
  consumedAt pour suffix "(consomme)". STRICT R1 : aucun chiffre,
  aucun multiplier_factor, aucun score expose.

R1 : grep multiplier|score|rank|/100|toFixed sur les 2 composants = 0 hit.
R3 : disabled={pending} sur submit button = pattern standard, pas de blocage
cross-mission (pas de blocks_progression_to).

npm typecheck OK, npm lint OK.

Refs : BRIEF §2.A (UI bonus claim Player), CLAUDE.md cardinal rule R1
"Scores INVISIBLE to Players".

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-bonus-ui"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 2 fichiers

      Select-String -Pattern "multiplier|/100|/140|score|rank" components/bonus-claim-form.tsx, components/bonus-status-badge.tsx
      # Attendu : 0 hit (R1 final)
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → commit. Sinon : décrire issues.</done>
  <what-built>
- 2 composants Player UI (form claim + badge qualitatif)
- R1 STRICT préservé (audit grep clean)
- R3 préservé (no blocking)
- Pattern submission-form aligné
  </what-built>
  <how-to-verify>
**Audit checklist** (5 minutes) :

1. **R1 grep STRICT sur les 2 composants** :
   ```powershell
   Select-String -Pattern "multiplier|multiplierFactor|score|rank|/100|/140|toFixed|points|number" components/bonus-claim-form.tsx, components/bonus-status-badge.tsx
   ```
   Attendu : 0 hit. Si match — investiguer ligne par ligne.

2. **R3 grep sur les 2 composants** :
   ```powershell
   Select-String -Pattern "blocks_progression|disabled.*because|locked" components/bonus-claim-form.tsx, components/bonus-status-badge.tsx
   ```
   Attendu : 0 hit. Le seul `disabled={pending}` sur submit button = OK (UX standard).

3. **i18n keys exists** :
   ```powershell
   Select-String -Pattern "bonus_claim_title_label|bonus_claim_url_label|bonus_claim_submit|bonus_status_validated|bonus_status_submitted|bonus_status_rejected" lib/i18n.ts
   ```
   Attendu : 6+ hits (les keys consommées par les composants existent dans i18n).

4. **Pattern submission-form alignment** :
   ```powershell
   Select-String -Pattern "useActionState|useEffect.*router\.refresh|state\.ok|state\.message" components/bonus-claim-form.tsx
   ```
   Attendu : >= 4 hits (pattern complet présent).

5. **Server component status badge** :
   ```powershell
   Select-String -Pattern "use client" components/bonus-status-badge.tsx
   ```
   Attendu : 0 hit.

6. **Visuel rapide review** (Omar lit le JSX) :
   - Le form a 3 fields : title text, docUrl URL, description textarea.
   - Le bouton submit affiche "Soumettre le bonus" (ou "Soumission en cours...").
   - Le badge a 4 styles (draft/submitted/validated/rejected) avec couleurs distinctes.
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "ajouter prefix '\\u26A1' badge", "supprimer suffix consomme — out of scope T3X").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **TS health** :
   ```powershell
   npm run typecheck ; npm run lint
   # Attendu : exit 0
   ```

2. **R1 final** :
   ```powershell
   Select-String -Pattern "multiplier|score|/100|/140" components/bonus-claim-form.tsx, components/bonus-status-badge.tsx
   # Attendu : 0 hit
   ```
</verification>

<success_criteria>
- [ ] bonus-claim-form.tsx (client) — pattern submission-form
- [ ] bonus-status-badge.tsx (server) — qualitatif R1-strict
- [ ] R1 grep clean (0 hit)
- [ ] R3 grep clean (no blocking)
- [ ] npm checks clean
- [ ] Commit atomique
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-08-SUMMARY.md` :

Sections :
1. **2 composants livrés**
2. **R1 STRICT audit** : 0 hit grep
3. **Pattern submission-form aligned**
4. **Server vs client split** : badge SSR, form client
5. **Files modified** : 2 nouveaux
6. **Next consumers** : Plan 10 routes journey/bonus/[type] + journey rail surface
</output>
