---
phase: quick-260510-jm8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/pixel-mascot.tsx
  - components/pixel-mascot-player.tsx
  - hooks/use-pixel-trigger.ts
  - components/journey-client.tsx
  - components/submission-form.tsx
  - lib/i18n.ts
autonomous: false
requirements:
  - T3-A5
must_haves:
  truths:
    - "Trigger (a) — Quand un Player soumet son tout premier livrable depuis ce navigateur (V1, status.ok === true sur SubmissionForm), une mascotte Pixel apparaît bottom-right en mood euphorique avec la copie '« Première hypothèse posée »' (FR validée par agent EIC). Elle ne se redéclenche pas au 2e livrable depuis le même navigateur."
    - "Trigger (b) — Quand le Player reste inactif (aucun pointermove ni keydown ni scroll) sur /journey pendant >15min ET que l'onglet est visible, une mascotte Pixel apparaît en mood inquiet avec la copie '« Une astuce t'attend à droite ◊ »'. Le timer se reset à chaque interaction et est pausé quand le document devient hidden (changement d'onglet, écran verrouillé)."
    - "Trigger (c) — Câblage prêt mais dormant aujourd'hui : un form contenant 2+ inputs avec attribut data-pixel-verbatim-counter (ou un compteur exposé via window event 'pixel:verbatim-count') déclenche la mascotte en mood concentré avec '« Encore un et L3 prend de la profondeur »' au passage 1→2. Aucun form existant n'a cet attribut aujourd'hui (M2.2 cartes_repetables est dans T3-IMPROVEMENTS section F, plan distinct), donc le trigger reste prêt-à-câbler sans surfacer."
    - "Aucune des 3 copies ne mentionne un score, un rang, un classement, un percentile ou un chiffre brut (R1 score invisible Player respecté)."
    - "Le mood inquiet utilise la palette ambre/warn existante (ring #C44536), aucune copie n'utilise un ton 'erreur', 'panique' ou 'rouge' (R2 warn-only respecté)."
    - "Aucune logique de progression n'est modifiée : grep montre que `lib/journey-progression.ts`, `lib/journey.ts` (scoring/gating) et `database/` ne sont pas dans `files_modified` (R3 respecté)."
    - "L'admin live mode (?live=1 sur /admin) continue d'afficher PixelMascot avec ses moods admin (HackStatusResult) sans régression : la signature publique `PixelMascot({ result, forceMood })` est préservée."
    - "Aucune dépendance npm ajoutée : `package.json` inchangé."
    - "L'apparition respecte prefers-reduced-motion (pas de slide animé si l'utilisateur préfère réduire) et le mascot a un z-index < 50 pour rester sous le drawer journey (z-index ≥ 50)."
  artifacts:
    - path: "components/pixel-mascot.tsx"
      provides: "Composant admin existant + export nommé `PixelAvatar` désormais public pour réutilisation."
      contains: "export function PixelAvatar"
    - path: "components/pixel-mascot-player.tsx"
      provides: "Wrapper Player-facing. Props minimales {mood, message, onDismiss?, ariaLabel}. Ne dépend ni de HackStatusResult, ni de microAction, ni de admin/announce. Auto-hide ~6s + click pour dismisser. Bouton 'Replier' garde la cohérence pill-mode admin."
      min_lines: 50
    - path: "hooks/use-pixel-trigger.ts"
      provides: "3 hooks purs côté client : (1) useFirstDeliveryTrigger(submissionOk) — lit/écrit localStorage `eg_pixel_a_first_delivery` ; (2) useStagnationTrigger() — listeners pointermove/keydown/scroll + visibilitychange, timer 15min, expose {triggered, dismiss} ; (3) useVerbatimCountTrigger() — écoute window CustomEvent 'pixel:verbatim-count' avec detail.count, déclenche au passage 1→2 par session de page."
      min_lines: 80
    - path: "components/journey-client.tsx"
      provides: "Monte <PixelMascotPlayer> + cable useStagnationTrigger + useVerbatimCountTrigger. Aucune modification du layout 3-col existant (hero/track/tip). Mascot rendu en bottom-right via la classe .eic-pixel-mascot existante (déjà sized fixed)."
    - path: "components/submission-form.tsx"
      provides: "Cable useFirstDeliveryTrigger sur state.ok pour fire trigger (a). Le mascot s'affiche AU-DESSUS du form pendant ~6s, puis le router.refresh() existant continue à exécuter. Le clear() de l'auto-save et le router.refresh() ne sont PAS bloqués par l'apparition du mascot."
    - path: "lib/i18n.ts"
      provides: "3 nouvelles clés FR (et EN équivalentes ASCII-safe) : pixel_player_first_delivery_quote, pixel_player_stagnation_quote, pixel_player_verbatim_count_quote. Copies FR validées par agent EIC pédagogique avant figeage."
  key_links:
    - from: "components/submission-form.tsx"
      to: "hooks/use-pixel-trigger.ts (useFirstDeliveryTrigger)"
      via: "import + useEffect sur state.ok"
      pattern: "useFirstDeliveryTrigger"
    - from: "components/journey-client.tsx"
      to: "components/pixel-mascot-player.tsx + hooks/use-pixel-trigger.ts"
      via: "import + 2 hooks (stagnation, verbatim)"
      pattern: "PixelMascotPlayer"
    - from: "components/pixel-mascot-player.tsx"
      to: "components/pixel-mascot.tsx (PixelAvatar export)"
      via: "import { PixelAvatar }"
      pattern: "import.*PixelAvatar.*from.*pixel-mascot"
    - from: "components/pixel-mascot-player.tsx"
      to: "lib/i18n.ts"
      via: "import dictionaries.fr"
      pattern: "pixel_player_.*_quote"
---

<objective>
A5 (T3-IMPROVEMENTS) — Câbler le composant `PixelMascot` (livré Phase 9 GMR-07, actuellement uniquement monté côté admin live) sur le parcours Player, avec **3 triggers événementiels déterministes** :

(a) 1er livrable soumis depuis ce navigateur → mood **euphorique** « Première hypothèse posée »
(b) Stagnation >15min sur /journey → mood **inquiet** « Une astuce t'attend à droite ◊ »
(c) Verbatim n°2 saisi → mood **concentré** « Encore un et L3 prend de la profondeur » (câblage prêt, dormant tant que M2.2 cartes_repetables n'existe pas)

Purpose : créer un signal pédagogique léger qui célèbre le 1er pas (a) et signale la stagnation (b) sans devenir Clippy — et préparer le terrain pour le futur écran verbatim (c). 100% conforme R1/R2/R3.

Output : 2 nouveaux fichiers (`pixel-mascot-player.tsx`, `hooks/use-pixel-trigger.ts`), 3 modifications légères (`pixel-mascot.tsx` export, `journey-client.tsx` montage, `submission-form.tsx` trigger a, `lib/i18n.ts` 3 clés).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@T3-IMPROVEMENTS.md

@components/pixel-mascot.tsx
@components/admin-live-view.tsx
@components/journey-client.tsx
@components/submission-form.tsx
@hooks/use-auto-save.ts
@lib/i18n.ts
@app/journey/page.tsx
@.claude/agents/eic-pedagogical-advisor.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase pre-plan. -->

From components/pixel-mascot.tsx (existing — admin):
```typescript
import type { HackStatus, HackStatusResult } from "@/lib/hack-status";

type Props = {
  result: HackStatusResult;
  forceMood?: HackStatus;
};
export function PixelMascot({ result, forceMood }: Props): JSX.Element;

// PixelAvatar is currently a PRIVATE function — must be exported for player wrapper to reuse.
function PixelAvatar({ mood, size }: { mood: HackStatus; size: number }): JSX.Element;

// HackStatus = "serein" | "concentre" | "inquiet" | "euphorique"
// MOOD_TO_PALETTE is private — keep private, players go through PixelAvatar which already
// applies palette internally. The player wrapper just picks a HackStatus value.
```

From hooks/use-auto-save.ts (existing — pattern to mirror for use-pixel-trigger):
```typescript
import { useEffect, useState } from "react";
// Exemplary pattern: SSR-safe localStorage (typeof window !== "undefined" guards),
// try/catch around every storage call, useState for the public flag, useEffect for setup.
```

From components/submission-form.tsx (existing — wiring point for trigger a):
```typescript
const [state, formAction, pending] = useActionState(submitDeliverable, initialState);
// state.ok === true → first-delivery trigger candidate
useEffect(() => {
  if (state.ok) { clear(); router.refresh(); }
}, [state.ok, clear, router]);
```

From components/journey-client.tsx (existing — wiring point for triggers b + c):
```typescript
"use client";
export function JourneyClient(props: JourneyClientProps): JSX.Element;
// Already client component. Hooks can be added directly. Render <PixelMascotPlayer /> at the end of the returned <div className="eic-journey">.
```

From app/globals.css (existing — reuse-only, no edits):
```css
.eic-pixel-mascot { /* fixed bottom-right, z-index handled */ }
.eic-pixel-mascot__card, .eic-pixel-mascot__pill { /* shells */ }
.eic-pixel-mascot__card--{serein|concentre|inquiet|euphorique} { /* palettes */ }
@media (prefers-reduced-motion: reduce) { .eic-pixel-mascot__pill-dot { animation: none; } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Export PixelAvatar + créer le hook use-pixel-trigger + le wrapper Player</name>
  <files>
    components/pixel-mascot.tsx,
    hooks/use-pixel-trigger.ts,
    components/pixel-mascot-player.tsx,
    lib/i18n.ts
  </files>
  <action>
**Étape 1.1 — `components/pixel-mascot.tsx` : exporter PixelAvatar**

Modifier UNIQUEMENT la signature du sous-composant interne pour le rendre exporté :

```diff
-function PixelAvatar({
+export function PixelAvatar({
   mood,
   size,
 }: {
   mood: HackStatus;
   size: number;
 }) {
```

Aucune autre modification dans ce fichier. La signature `PixelMascot({ result, forceMood })` reste strictement identique (admin live ne doit pas régresser).

**Étape 1.2 — `lib/i18n.ts` : 3 nouvelles clés FR + EN (ASCII-safe, copies à VALIDER PAR AGENT EIC AVANT FIGEAGE — voir Task 3)**

Ajouter dans `dictionaries.fr` (juste après `pixel_mascot_aria` ligne 517) :

```typescript
// ---- Pixel mascot — Player triggers (T3 A5 / quick 260510-jm8) -----------
pixel_player_first_delivery_quote: "Première hypothèse posée.",
pixel_player_stagnation_quote: "Une astuce t'attend à droite ◊",
pixel_player_verbatim_count_quote: "Encore un et L3 prend de la profondeur.",
```

Ajouter les MÊMES clés dans `dictionaries.en` (placeholder ASCII-safe — l'app est FR-first, EN n'est pas surfacé en pilote) :

```typescript
pixel_player_first_delivery_quote: "First hypothesis logged.",
pixel_player_stagnation_quote: "A tip is waiting on your right.",
pixel_player_verbatim_count_quote: "One more and L3 gains depth.",
```

NB : ces 3 copies FR sont des **placeholders à valider par l'agent EIC pédagogique** (Task 3 ci-dessous). Si l'agent renvoie une copie différente, mettre à jour `lib/i18n.ts` AVANT le commit final.

**Étape 1.3 — `hooks/use-pixel-trigger.ts` (NOUVEAU FICHIER)**

Créer le fichier avec 3 hooks. Pattern : SSR-safe (typeof window guard), try/catch sur localStorage, listeners cleanup propre. Inspirer du pattern de `hooks/use-auto-save.ts`.

```typescript
"use client";
// T3-A5 (quick 260510-jm8) — 3 triggers événementiels Player pour la mascotte Pixel.
// Tous les triggers sont DÉTERMINISTES (jamais random) et conformes R1 (aucune
// donnée score/rang/percentile manipulée), R2 (warn-only sur le mood inquiet —
// la copie reste encourageante), R3 (aucun blocage de mission).

import { useCallback, useEffect, useState } from "react";

const FIRST_DELIVERY_KEY = "eg_pixel_a_first_delivery";
const STAGNATION_THRESHOLD_MS = 15 * 60 * 1000; // 15 min

export type PixelTriggerState = {
  /** When true, the player wrapper should render the mascot. */
  triggered: boolean;
  /** Programmatically dismiss the mascot (also called on auto-hide timer). */
  dismiss: () => void;
};

/**
 * Trigger (a) — fires once per browser the first time `submissionOk` flips true.
 * Persistence: localStorage flag (acceptable trade-off for pilot — magic-link
 * sessions are 1 user per browser in practice). After dismiss, will not fire
 * again on this browser.
 */
export function useFirstDeliveryTrigger(submissionOk: boolean): PixelTriggerState {
  const [triggered, setTriggered] = useState(false);

  useEffect(() => {
    if (!submissionOk) return;
    if (typeof window === "undefined") return;
    try {
      const already = window.localStorage.getItem(FIRST_DELIVERY_KEY);
      if (already === "1") return; // déjà tiré sur ce navigateur
      window.localStorage.setItem(FIRST_DELIVERY_KEY, "1");
      setTriggered(true);
    } catch {
      // private browsing / quota → skip silently (acceptable, R3 — pas de blocage)
    }
  }, [submissionOk]);

  const dismiss = useCallback(() => setTriggered(false), []);
  return { triggered, dismiss };
}

/**
 * Trigger (b) — fires after >15min of no interaction (pointermove + keydown +
 * scroll). Pauses when document.visibilityState === "hidden" so background
 * tabs don't fire false positives. Resets on any interaction. Can re-trigger
 * after dismiss + new stagnation period (no persistence — session-scoped).
 */
export function useStagnationTrigger(): PixelTriggerState {
  const [triggered, setTriggered] = useState(false);

  useEffect(() => {
    if (typeof window === "undefined") return;
    let timeoutId: number | null = null;
    let paused = false;

    const arm = () => {
      if (timeoutId !== null) window.clearTimeout(timeoutId);
      if (paused) return;
      timeoutId = window.setTimeout(() => {
        setTriggered(true);
      }, STAGNATION_THRESHOLD_MS);
    };

    const onActivity = () => {
      // Toute activité → reset (et close si déjà ouvert? non, laisse l'utilisateur dismiss)
      arm();
    };

    const onVisibility = () => {
      if (document.visibilityState === "hidden") {
        paused = true;
        if (timeoutId !== null) {
          window.clearTimeout(timeoutId);
          timeoutId = null;
        }
      } else {
        paused = false;
        arm();
      }
    };

    window.addEventListener("pointermove", onActivity, { passive: true });
    window.addEventListener("keydown", onActivity);
    window.addEventListener("scroll", onActivity, { passive: true });
    document.addEventListener("visibilitychange", onVisibility);
    arm();

    return () => {
      if (timeoutId !== null) window.clearTimeout(timeoutId);
      window.removeEventListener("pointermove", onActivity);
      window.removeEventListener("keydown", onActivity);
      window.removeEventListener("scroll", onActivity);
      document.removeEventListener("visibilitychange", onVisibility);
    };
  }, []);

  const dismiss = useCallback(() => setTriggered(false), []);
  return { triggered, dismiss };
}

/**
 * Trigger (c) — fires when a form publishes a CustomEvent 'pixel:verbatim-count'
 * on window with detail.count transitioning 1 → 2. Câblage prêt, DORMANT
 * aujourd'hui (M2.2 cartes_repetables n'existe pas encore — voir T3-IMPROVEMENTS
 * section F). Aucun form actuel ne dispatch cet event ; lorsque le futur
 * `verbatim-cards-form` sera livré (plan séparé), il devra simplement faire :
 *
 *   window.dispatchEvent(new CustomEvent("pixel:verbatim-count", { detail: { count } }));
 *
 * à chaque ajout/validation de carte.
 */
export function useVerbatimCountTrigger(): PixelTriggerState {
  const [triggered, setTriggered] = useState(false);
  const [lastCount, setLastCount] = useState(0);

  useEffect(() => {
    if (typeof window === "undefined") return;
    const handler = (e: Event) => {
      const ce = e as CustomEvent<{ count?: number }>;
      const next = typeof ce.detail?.count === "number" ? ce.detail.count : 0;
      // Trigger uniquement au passage 1 → 2 (déterministe, jamais random)
      if (lastCount === 1 && next === 2) {
        setTriggered(true);
      }
      setLastCount(next);
    };
    window.addEventListener("pixel:verbatim-count", handler as EventListener);
    return () => {
      window.removeEventListener("pixel:verbatim-count", handler as EventListener);
    };
  }, [lastCount]);

  const dismiss = useCallback(() => setTriggered(false), []);
  return { triggered, dismiss };
}
```

**Étape 1.4 — `components/pixel-mascot-player.tsx` (NOUVEAU FICHIER)**

Wrapper Player-facing. Réutilise `PixelAvatar` (fraîchement exporté). Props minimales — aucune dépendance à `HackStatusResult`. Auto-hide ~6s. ESC ou click pour dismisser. Respecte prefers-reduced-motion (pas de slide animé). Z-index 40 (< drawer journey à 50).

```typescript
"use client";
// T3-A5 (quick 260510-jm8) — Wrapper Player de la mascotte Pixel.
// Reuses PixelAvatar from pixel-mascot.tsx (admin) but exposes a minimal
// shape: {mood, message, onDismiss}. Auto-hide ~6s, dismiss-on-click, ESC,
// reduced-motion safe.

import { useEffect } from "react";
import { PixelAvatar } from "@/components/pixel-mascot";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;

const AUTO_HIDE_MS = 6000;

const MOOD_LABEL: Record<"euphorique" | "inquiet" | "concentre", string> = {
  euphorique: t.pixel_mascot_euphorique_label,
  inquiet: t.pixel_mascot_inquiet_label,
  concentre: t.pixel_mascot_concentre_label,
};

const MOOD_RING: Record<"euphorique" | "inquiet" | "concentre", string> = {
  euphorique: "#D97706",
  inquiet: "#C44536",
  concentre: "#1B3A5C",
};

type Props = {
  mood: "euphorique" | "inquiet" | "concentre";
  message: string;
  onDismiss: () => void;
};

export function PixelMascotPlayer({ mood, message, onDismiss }: Props) {
  // Auto-hide après ~6s
  useEffect(() => {
    const id = window.setTimeout(onDismiss, AUTO_HIDE_MS);
    return () => window.clearTimeout(id);
  }, [onDismiss]);

  // ESC pour dismisser
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") onDismiss();
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [onDismiss]);

  return (
    <div
      className="eic-pixel-mascot"
      role="status"
      aria-live="polite"
      aria-label={t.pixel_mascot_aria}
      style={{ zIndex: 40 }}
    >
      <button
        type="button"
        onClick={onDismiss}
        className={`eic-pixel-mascot__card eic-pixel-mascot__card--${mood}`}
        style={{ cursor: "pointer", border: "none", textAlign: "left" }}
        aria-label={t.pixel_mascot_collapse}
      >
        <div className="eic-pixel-mascot__body">
          <div className="eic-pixel-mascot__row">
            <PixelAvatar mood={mood} size={48} />
            <div style={{ flex: 1, minWidth: 0 }}>
              <div
                className="eic-pixel-mascot__label"
                style={{ color: MOOD_RING[mood] }}
              >
                {MOOD_LABEL[mood]}
              </div>
              <p className="eic-pixel-mascot__quote">« {message} »</p>
            </div>
          </div>
        </div>
      </button>
    </div>
  );
}
```

NB : L'animation slide-in vient des classes CSS existantes `.eic-pixel-mascot` (déjà sized + positionnée, déjà reduced-motion-safe via le @media query existant). Aucun ajout CSS requis.
  </action>
  <verify>
    <automated>cd C:/Users/omara/Desktop/EntrepreneurGame && npm run typecheck</automated>
    Plus :
    - `grep -n "export function PixelAvatar" components/pixel-mascot.tsx` doit matcher.
    - `grep -n "useFirstDeliveryTrigger\|useStagnationTrigger\|useVerbatimCountTrigger" hooks/use-pixel-trigger.ts` doit matcher 3 fois.
    - `grep -n "PixelMascotPlayer" components/pixel-mascot-player.tsx` doit matcher (export).
    - `grep -n "pixel_player_first_delivery_quote\|pixel_player_stagnation_quote\|pixel_player_verbatim_count_quote" lib/i18n.ts` doit matcher 6 fois (3 FR + 3 EN).
    - `git diff components/pixel-mascot.tsx` doit montrer UNIQUEMENT le passage de `function` à `export function` sur PixelAvatar.
  </verify>
  <done>
    - `PixelAvatar` exporté depuis `components/pixel-mascot.tsx` sans autre changement (signature `PixelMascot` admin intacte).
    - `hooks/use-pixel-trigger.ts` créé avec 3 hooks SSR-safe + cleanup propre.
    - `components/pixel-mascot-player.tsx` créé, props {mood, message, onDismiss}, auto-hide 6s, ESC + click dismiss, z-index 40, role status + aria-live polite.
    - 3 clés FR + 3 clés EN ajoutées à `lib/i18n.ts` (placeholders, à valider Task 3).
    - `npm run typecheck` passe sans erreur.
  </done>
</task>

<task type="auto">
  <name>Task 2 — Câbler les 3 triggers : SubmissionForm (a) + JourneyClient (b + c)</name>
  <files>
    components/submission-form.tsx,
    components/journey-client.tsx
  </files>
  <action>
**Étape 2.1 — `components/submission-form.tsx` : trigger (a)**

Ajouter en haut des imports :
```typescript
import { useFirstDeliveryTrigger } from "@/hooks/use-pixel-trigger";
import { PixelMascotPlayer } from "@/components/pixel-mascot-player";
import { dictionaries } from "@/lib/i18n";
```

(Note : `dictionaries` est déjà importé via `t` à la ligne 13 sous le nom `dictionaries`. Réutiliser la const `t` existante.)

Dans le corps de `SubmissionForm`, après la déclaration `useActionState` (ligne 27) :

```typescript
// T3-A5 (a) — Pixel mascot 1er livrable soumis (mood euphorique).
// Déclenchement déterministe : flip true UNE FOIS quand state.ok === true
// pour la première soumission de ce navigateur (localStorage flag).
const firstDelivery = useFirstDeliveryTrigger(state.ok);
```

Avant le `return (...)` du composant, c'est OK ; le mascot sera rendu DANS le composant après le form. Modifier le `return` pour wrapper en fragment :

```diff
   return (
+    <>
     <form
       ref={formRef}
       action={formAction}
       ...
     >
       ...
     </form>
+    {firstDelivery.triggered ? (
+      <PixelMascotPlayer
+        mood="euphorique"
+        message={t.pixel_player_first_delivery_quote}
+        onDismiss={firstDelivery.dismiss}
+      />
+    ) : null}
+    </>
   );
```

**ATTENTION ordre d'exécution** : le `useEffect` existant fait `clear() + router.refresh()` quand `state.ok` devient true. Le `router.refresh()` va re-render la page server : `app/journey/deliverable/[id]/page.tsx` va alors basculer en mode `SubmissionTicket` (latest.status === "submitted_v1"). À ce moment-là, `SubmissionForm` est démonté → le mascot disparaît AVANT son auto-hide 6s.

**C'est acceptable et même souhaitable** : le ticket SOUMIS (`SubmissionTicket`) est le nouveau "réceptacle" visuel de la célébration. Le mascot a juste le temps d'apparaître pendant la transition (~200-500ms). Pour donner plus de visibilité, on peut PUSH le mascot dans le `SubmissionTicket` aussi, MAIS scope-wise c'est out (pas dans `files_modified`). On reste sur l'apparition transitoire — la pastille `eg_pixel_a_first_delivery=1` empêchera tout retrigger lors d'une soumission ultérieure. Documenter ce comportement dans le SUMMARY post-exec.

**Étape 2.2 — `components/journey-client.tsx` : triggers (b) + (c)**

Ajouter en haut des imports (après ligne 9 qui importe `JourneyTrack`) :

```typescript
import { PixelMascotPlayer } from "@/components/pixel-mascot-player";
import {
  useStagnationTrigger,
  useVerbatimCountTrigger,
} from "@/hooks/use-pixel-trigger";
```

Dans le corps de `JourneyClient`, juste après la ligne `const [hovered, setHovered] = useState<LevelId | null>(null);` :

```typescript
// T3-A5 (b) — Stagnation >15min sur /journey (mood inquiet).
const stagnation = useStagnationTrigger();
// T3-A5 (c) — Verbatim n°2 saisi (mood concentré). Câblage prêt, DORMANT
// tant que M2.2 cartes_repetables n'existe pas (T3-IMPROVEMENTS section F).
const verbatim = useVerbatimCountTrigger();
```

Modifier le `return` final de `JourneyClient` pour ajouter le mascot juste avant le `</div>` final racine (juste avant la ligne `}` de fermeture du return). Un seul mascot peut être affiché à la fois ; si plusieurs triggers sont actifs, priorité = euphorique > inquiet > concentré (mais euphorique ne peut pas être déclenché ici — il vit dans SubmissionForm). Donc priorité ici : inquiet > concentré.

```diff
       {openLevel && openState ? (
         <JourneyDrawer
           levelId={openLevel}
           missions={missions}
           objective={objectivesByLevel[openLevel] ?? null}
           onClose={handleClose}
           state={openState}
         />
       ) : null}
+      {stagnation.triggered ? (
+        <PixelMascotPlayer
+          mood="inquiet"
+          message={t.pixel_player_stagnation_quote}
+          onDismiss={stagnation.dismiss}
+        />
+      ) : verbatim.triggered ? (
+        <PixelMascotPlayer
+          mood="concentre"
+          message={t.pixel_player_verbatim_count_quote}
+          onDismiss={verbatim.dismiss}
+        />
+      ) : null}
     </div>
   );
 }
```

**Vérifier qu'aucun import inutilisé n'est laissé en bas du fichier après modif.**
  </action>
  <verify>
    <automated>cd C:/Users/omara/Desktop/EntrepreneurGame && npm run typecheck && npm run lint && npm run build</automated>
    Plus :
    - `grep -n "useFirstDeliveryTrigger" components/submission-form.tsx` doit matcher.
    - `grep -n "useStagnationTrigger\|useVerbatimCountTrigger" components/journey-client.tsx` doit matcher 2 fois.
    - `grep -rn "lib/journey-progression\|lib/journey\.ts\|database/" components/submission-form.tsx components/journey-client.tsx components/pixel-mascot-player.tsx hooks/use-pixel-trigger.ts` ne doit produire AUCUN match (R3 — aucune logique de progression touchée).
    - `grep -n "score\|rank\|points\|percentile\|classement" components/pixel-mascot-player.tsx hooks/use-pixel-trigger.ts` ne doit produire aucun match côté chaînes user-facing (R1 audit).
    - `git diff components/submission-form.tsx components/journey-client.tsx` doit être contenu : ~10-15 lignes ajoutées chacun, aucune ligne retirée fonctionnelle.
  </verify>
  <done>
    - `useFirstDeliveryTrigger` câblé dans `SubmissionForm`, mascot rendu en fragment au-dessus/après le form (auto-disparition acceptée via router.refresh).
    - `useStagnationTrigger` + `useVerbatimCountTrigger` câblés dans `JourneyClient`, mascot rendu après le drawer avec priorité inquiet > concentré.
    - `npm run typecheck && npm run lint && npm run build` passent (build doit produire au moins une page Player sans crash — la page /journey est SSR/server component qui rend `<JourneyClient>` côté client, tester via build sans server runtime suffit).
    - Aucun fichier hors `files_modified` du frontmatter n'a été touché (vérifier via `git status`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 — Validation copies FR par agent EIC pédagogique + smoke E2E manuel</name>
  <what-built>
    Tâches 1+2 livrées :
    - `PixelAvatar` exporté
    - `hooks/use-pixel-trigger.ts` (3 hooks)
    - `components/pixel-mascot-player.tsx` (wrapper Player)
    - 3 clés i18n FR/EN PLACEHOLDER
    - Wiring dans `SubmissionForm` (trigger a) + `JourneyClient` (triggers b + c)
    - Build + typecheck + lint passent
  </what-built>
  <how-to-verify>
**3.A — Validation copies FR par agent EIC pédagogique (BLOQUANT avant figeage)**

Spawn l'agent défini dans `.claude/agents/eic-pedagogical-advisor.md` avec ce prompt :

> Zone : founder-facing (Player /journey + /journey/deliverable/[id]). Sensibilité : haute (R1 score invisible, R2 warn-only, R3 pas de blocage). Contexte : 3 messages courts d'une mascotte Pixel qui apparaît en bottom-right pendant ~6 secondes. La mascotte ne dialogue pas, ne demande rien. Elle SIGNALE.
>
> Évalue les 3 copies suivantes (placeholders dans `lib/i18n.ts` — clés `pixel_player_first_delivery_quote` / `pixel_player_stagnation_quote` / `pixel_player_verbatim_count_quote`) :
>
> - (a) Mood EUPHORIQUE, déclenchement = 1ère soumission de livrable du Player. Copie : « Première hypothèse posée. »
> - (b) Mood INQUIET, déclenchement = aucune interaction sur /journey pendant >15min. Copie : « Une astuce t'attend à droite ◊ »
> - (c) Mood CONCENTRÉ, déclenchement = 2e verbatim saisi en M2.2 (câblage dormant aujourd'hui). Copie : « Encore un et L3 prend de la profondeur. »
>
> Pour chaque copie, juge : (1) ton encourageant/non-jugeant, (2) absence de score/rang/chiffre brut (R1), (3) le mood "inquiet" reste warn-only et non-anxiogène (R2), (4) cohérence avec la philosophie "Lean Startup / boucle courte" du jeu, (5) longueur (≤80 caractères idéal pour une bulle 6s).
>
> Renvoie soit "OK figer" soit une copie alternative par message avec justification 1 ligne.

**Si l'agent renvoie une copie alternative**, mettre à jour `lib/i18n.ts` (FR uniquement — EN reste placeholder ASCII) AVANT le commit final.

**3.B — Smoke E2E manuel sur dev local (Supabase mode si possible, sinon demo mode)**

1. `npm run dev` → ouvrir `http://localhost:3000/login` → se logguer (ou demo mode si pas de Supabase env).

2. **Trigger (a) — 1er livrable** :
   - Ouvrir DevTools → Application → Local Storage → supprimer toute clé `eg_pixel_a_first_delivery` si présente.
   - Naviguer vers une mission `À rendre` (premier livrable disponible). Soumettre une URL `https://example.com`.
   - **Attendu** : juste avant le router.refresh, la mascotte apparaît bottom-right en mood euphorique avec « Première hypothèse posée. ». Disparition rapide possible (router.refresh) — c'est OK.
   - Re-soumettre un autre livrable → la mascotte ne réapparaît PAS (localStorage flag persistant).

3. **Trigger (b) — Stagnation 15min** :
   - Sur `/journey`, **temporairement réduire `STAGNATION_THRESHOLD_MS`** à `30_000` (30s) dans `hooks/use-pixel-trigger.ts` pour le test. Reload.
   - Ne pas bouger la souris ni taper de touche pendant 30s. Surveiller bottom-right.
   - **Attendu** : mascot apparaît en mood inquiet avec « Une astuce t'attend à droite ◊ ». Auto-hide 6s. ESC ou click dismisse également.
   - Bouger la souris : timer reset (vérifiable en gardant la fenêtre ouverte 30s à nouveau → re-déclenche).
   - Changer d'onglet pendant 30s puis revenir : timer NE doit PAS s'être déclenché en arrière-plan.
   - **REMETTRE `STAGNATION_THRESHOLD_MS = 15 * 60 * 1000` AVANT COMMIT.**

4. **Trigger (c) — Verbatim n°2 (test manuel via Console)** :
   - Sur `/journey`, ouvrir DevTools → Console.
   - Exécuter : `window.dispatchEvent(new CustomEvent("pixel:verbatim-count", { detail: { count: 1 }}))` → **rien** ne doit apparaître.
   - Exécuter : `window.dispatchEvent(new CustomEvent("pixel:verbatim-count", { detail: { count: 2 }}))` → **mascot apparaît** en mood concentré avec « Encore un et L3 prend de la profondeur. ».
   - Exécuter encore avec `count: 3` → **rien** (déjà déclenché, attendu).

5. **Régression admin live mode** :
   - Naviguer vers `/admin?live=1` (en tant que GameMaster). PixelMascot admin doit toujours s'afficher avec `forceMood?` ou le `result.status` calculé. Aucune régression visuelle.
   - Vérifier : pas de double mascotte (admin + player) — `/admin` n'est pas une page Player, donc seul le mascot admin apparaît.

6. **A11y / reduced-motion** :
   - DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`.
   - Re-déclencher trigger (b) — pas d'animation slide-in invasive (les CSS existants `@media (prefers-reduced-motion: reduce)` neutralisent déjà l'animation pulse-dot).

7. **Audit R1 grep** :
   - `grep -rn "score\|rank\|points\|classement\|percentile" components/pixel-mascot-player.tsx hooks/use-pixel-trigger.ts lib/i18n.ts` → seuls hits acceptés = ceux dans les sections d'i18n NON liées aux 3 nouvelles clés (les clés admin existantes peuvent contenir "score", c'est OK car admin uniquement).

8. **Audit R3 grep** :
   - `git diff --name-only HEAD` doit afficher EXACTEMENT : `components/pixel-mascot.tsx`, `components/pixel-mascot-player.tsx`, `hooks/use-pixel-trigger.ts`, `components/journey-client.tsx`, `components/submission-form.tsx`, `lib/i18n.ts`. Aucun fichier sous `lib/journey-progression.ts`, `lib/journey.ts`, `database/` ou `app/actions.ts`.

**Issues à reporter** : screenshot bottom-right + description si une copie ne respecte pas R1/R2 ou si la mascotte masque un drawer/modal en cours d'édition.
  </how-to-verify>
  <resume-signal>Type "approved" pour committer (avec copies finales validées EIC) — ou décris les ajustements à faire (copie alternative, threshold différent, dismiss prematuré sur trigger a, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → localStorage | localStorage flag `eg_pixel_a_first_delivery` est lu/écrit côté client. Aucun secret n'y transite. |
| client → window CustomEvent | `pixel:verbatim-count` peut être dispatché par n'importe quel script s'exécutant sur la page (devtools, extension, future feature). |
| client UI → Player perception | Les copies FR sont founder-facing — un mauvais ton (pression, comparaison, score visible) viole R1/R2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jm8-01 | Tampering | localStorage flag `eg_pixel_a_first_delivery` | accept | Pilot scale, no security implication. Si user vide le storage, la mascotte se redéclenche au prochain submit — comportement acceptable (au pire = redondance célébrative, jamais de fuite de score/donnée). |
| T-jm8-02 | Spoofing | window CustomEvent `pixel:verbatim-count` | accept | N'importe quel script peut dispatcher l'event → mascot apparaît. Aucun impact sécurité (pas de mutation serveur, pas de XP attribué, juste une bulle UI). Documenté dans le hook. |
| T-jm8-03 | Information disclosure | Copies FR de la mascotte | mitigate | Validation OBLIGATOIRE par agent EIC pédagogique avant figeage (Task 3). Audit grep R1 sur `score/rank/points/classement/percentile` dans la zone modifiée. |
| T-jm8-04 | Denial of service | Stagnation timer en arrière-plan | mitigate | `visibilitychange` listener PAUSE le timer quand l'onglet est hidden — empêche les faux positifs et la fuite mémoire timer. Cleanup propre dans return du useEffect. |
| T-jm8-05 | Repudiation | N/A | accept | Pas de mutation serveur, pas d'audit nécessaire. |
| T-jm8-06 | Elevation of privilege | N/A | accept | Pas de role check impliqué (le mascot Player ne se monte que sur /journey + /journey/deliverable/* via leurs server components qui font déjà le role gate). |
</threat_model>

<verification>
**Tests automatiques agrégés (commande unique) :**

```bash
cd C:/Users/omara/Desktop/EntrepreneurGame && npm run typecheck && npm run lint && npm run build
```

**Audits grep post-implémentation :**

1. R1 — score invisible Player :
   ```bash
   grep -rn "score\|rank\|classement\|percentile\|points\b" components/pixel-mascot-player.tsx hooks/use-pixel-trigger.ts
   ```
   Doit ne rien matcher dans des chaînes user-facing.

2. R3 — pas de blocage progression :
   ```bash
   git diff --name-only HEAD
   ```
   Doit afficher EXACTEMENT 6 fichiers : `components/pixel-mascot.tsx`, `components/pixel-mascot-player.tsx`, `hooks/use-pixel-trigger.ts`, `components/submission-form.tsx`, `components/journey-client.tsx`, `lib/i18n.ts`. Aucun autre.

3. Aucune dépendance ajoutée :
   ```bash
   git diff package.json package-lock.json
   ```
   Doit être vide.

4. PixelMascot admin signature préservée :
   ```bash
   grep -n "export function PixelMascot" components/pixel-mascot.tsx
   ```
   Doit toujours matcher `export function PixelMascot({ result, forceMood }: Props)`.

**Tests manuels (Task 3 checkpoint)** : voir `<how-to-verify>` Task 3.
</verification>

<success_criteria>
1. `npm run typecheck && npm run lint && npm run build` passent sans erreur ni warning nouveau.
2. Les 3 triggers fonctionnent en local (vérifié manuellement Task 3).
3. Les 3 copies FR sont validées par l'agent EIC pédagogique (ou ajustées selon retour) — R1/R2 confirmés.
4. Admin live mode (`/admin?live=1`) ne régresse pas — PixelMascot admin s'affiche identiquement.
5. Aucun fichier hors `files_modified` n'est modifié (vérifié `git diff --name-only`).
6. Aucune dépendance npm ajoutée.
7. Le commit est atomique avec message conventionnel : `feat(a5): pixel mascotte 3 triggers Player (T3-IMPROVEMENTS A5)` — co-author Claude Opus 4.7.
</success_criteria>

<output>
After completion, create `.planning/quick/260510-jm8-a5-pixel-mascotte-3-triggers-evenementie/260510-jm8-SUMMARY.md` au format quick standard, contenant :

- Fichiers modifiés (6) avec lignes ajoutées/retirées
- Copies FR finales (validées EIC) si différentes des placeholders
- Résultats Task 3 smoke E2E (OK / issues)
- Note sur trigger (c) DORMANT — à câbler dans le futur plan M2.2 cartes_repetables
- Hash du commit
</output>
