---
phase: quick-260510-t3x
plan: 09
type: execute
wave: 3
depends_on:
  - 06
files_modified:
  - package.json
  - package-lock.json
  - components/moscow-card.tsx
  - components/moscow-kanban.tsx
autonomous: false
requirements:
  - T3X-EXPANSION-MOSCOW-KANBAN-DND
must_haves:
  truths:
    - "package.json declare @dnd-kit/core@^6.1.0 + @dnd-kit/sortable@^8.0.0 (PIN major 8 strict, éviter v9/v10 breaking) + @dnd-kit/utilities@^3.2.2 dans dependencies (issue revision #5)"
    - "npm run build PASSE (issue revision #5 — catches React 19 peer-dep runtime issues que typecheck rate)"
    - "Si --legacy-peer-deps utilisé : overrides block explicite ajouté à package.json + tracé dans commit message"
    - "handleAddCard appelle router.refresh() après succès (issue revision #7 — pas d'optimistic state update T-3, revalidatePath + refresh suffit)"
    - "package-lock.json reflete les nouvelles dependances + peer dep React 19 satisfait"
    - "components/moscow-card.tsx existe : presentational card 'use client' (drag-handle + feature/pourquoi/contrainte)"
    - "components/moscow-kanban.tsx existe : main board 'use client', 4 colonnes (must/should/could/wont), DndContext, SortableContext per column, useSortable per card"
    - "Reorder DnD onDragEnd appelle reorderMoscowCardsFlow via FormData {deliverableTemplateId, items: JSON.stringify([{id, bucket, ord}])}"
    - "Crée carte via createMoscowCardFlow trigger via 'Ajouter carte' button par bucket — formAction"
    - "Edit carte inline OU dialog — appelle updateMoscowCardFlow"
    - "Delete carte via icon button — appelle deleteMoscowCardFlow avec confirmation natif window.confirm()"
    - "Mobile fallback : @dnd-kit/core fournit TouchSensor par défaut, on l'active explicitement (sensors useSensors(KeyboardSensor, PointerSensor, TouchSensor))"
    - "R3 préservé : aucun DOM disabled hors button submit pending, aucun blocage cross-mission"
    - "R1 préservé : aucun rendu chiffre score/rank/multiplier sur les cartes"
    - "Mobile-first responsive : grid 1 colonne <768px, grid 2 colonnes 768-1024px, grid 4 colonnes >=1024px"
    - "ASCII pur dans le code TS (FR consume via t.moscow_*)"
    - "npm run typecheck + lint clean"
    - "Pas de @dnd-kit/modifiers ajoute (pas necessaire pour le scope MoSCoW simple)"
  artifacts:
    - path: "package.json"
      provides: "@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities ajoutes a dependencies"
      contains: "@dnd-kit/core|@dnd-kit/sortable"
    - path: "components/moscow-card.tsx"
      provides: "Carte sortable individuelle avec feature/pourquoi/contrainte + handle DnD + edit/delete buttons"
      contains: "use client|useSortable|MoscowCard"
    - path: "components/moscow-kanban.tsx"
      provides: "Board 4 colonnes Must/Should/Could/Wont avec DndContext, onDragEnd -> reorderMoscowCardsFlow, ajout carte + soumission deliverable"
      contains: "use client|DndContext|SortableContext|reorderMoscowCardsFlow|createMoscowCardFlow"
  key_links:
    - from: "components/moscow-kanban.tsx onDragEnd"
      to: "app/actions.ts:reorderMoscowCardsFlow"
      via: "FormData avec items JSON.stringify"
      pattern: "reorderMoscowCardsFlow"
    - from: "components/moscow-kanban.tsx createCard handler"
      to: "app/actions.ts:createMoscowCardFlow + updateMoscowCardFlow + deleteMoscowCardFlow"
      via: "useActionState ou form action="
      pattern: "createMoscowCardFlow|updateMoscowCardFlow|deleteMoscowCardFlow"
---

<objective>
Wave 3 — UI MoSCoW Kanban native avec DnD `@dnd-kit/core` (D-04). 2 composants client + ajout dependency.

**Architecture client** :
- `MoscowKanban` : board principal, 4 colonnes (must/should/could/wont), DndContext, sensors (Pointer + Touch + Keyboard pour a11y), gère onDragEnd qui post à `reorderMoscowCardsFlow` Plan 06.
- `MoscowCard` : carte individuelle, useSortable hook de `@dnd-kit/sortable`, drag handle visible, contenu (feature, pourquoi, contrainte), boutons Edit/Delete.

**État local optimiste** : drag-end met à jour le state local immédiatement (UX fluide), puis fire l'action serveur. Si l'action échoue (rare), revalidatePath va re-fetch et corriger.

**Pourquoi `@dnd-kit/core` (D-04)** :
- Compatible React 19 (cf. package.json release v6.3.0+ supporte React 18+19)
- Bundle léger ~120kb total core+sortable+utilities
- a11y native (KeyboardSensor pour navigation clavier)
- Touch support natif (mobile)
- Pas de `react-beautiful-dnd` legacy (deprecated, react 18 only).

**R3 préservé** : aucun blocage cross-mission, aucun DOM `disabled` hors submit button.
**R1 préservé** : aucun chiffre score/rank rendu sur les cartes.
**R2 préservé** : la submission deliverable du Kanban (`submitMoscowDeliverableFlow`) retourne warn-only suffix si <2 MUST ou <1 WONT (cf. Plan 06) — l'UI affiche le message tel quel.

**Scope plan** : 2 composants nouveaux + 1 modif `package.json` + lock. Pas de surface dans `/journey/deliverable/[id]/` page (Plan 10 fera l'intégration). Pas de CSV export (Plan 11).

**Output** :
- 2 fichiers `.tsx` nouveaux
- `package.json` + `package-lock.json` updated avec @dnd-kit
- npm install + typecheck + lint clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@package.json
@components/submission-form.tsx
@app/actions.ts
@lib/types.ts
@lib/i18n.ts

<interfaces>
<!-- @dnd-kit API patterns -->

```typescript
// MoscowKanban.tsx
import {
  DndContext,
  closestCenter,
  KeyboardSensor,
  PointerSensor,
  TouchSensor,
  useSensor,
  useSensors,
  type DragEndEvent,
} from "@dnd-kit/core";
import {
  arrayMove,
  SortableContext,
  sortableKeyboardCoordinates,
  verticalListSortingStrategy,
} from "@dnd-kit/sortable";

// MoscowCard.tsx
import { useSortable } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";

const { attributes, listeners, setNodeRef, transform, transition } = useSortable({ id: card.id });
const style = {
  transform: CSS.Transform.toString(transform),
  transition,
};
```

<!-- Props attendues -->

```typescript
// MoscowKanban
type MoscowKanbanProps = {
  deliverableTemplateId: string;
  initialCards: MoscowCard[];  // fetched server-side, passed as prop
};

// MoscowCard
type MoscowCardProps = {
  card: MoscowCardType;        // from lib/types.ts (renamed import to avoid conflict with component name)
  onEdit: (card: MoscowCardType) => void;  // opens dialog or inline edit
  onDelete: (cardId: string) => void;       // calls deleteMoscowCardFlow
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer @dnd-kit/core + @dnd-kit/sortable + @dnd-kit/utilities (npm install)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `package.json` (vérifier dependencies actuelles, peer deps React 19)
  </read_first>
  <action>
**Action principale** : `npm install` les 3 paquets @dnd-kit nécessaires.

```powershell
npm install @dnd-kit/core@^6.1.0 @dnd-kit/sortable@^8.0.0 @dnd-kit/utilities@^3.2.2
```

**Versions PINNÉES** (issue revision #5 — éviter @dnd-kit/sortable v8→v10 breaking changes silencieux) :
- `@dnd-kit/core@^6.1.0` (supporte React 19, testé pinné inférieur à 7 pour stabilité)
- `@dnd-kit/sortable@^8.0.0` (PIN strict major 8 — v9/v10 ont breaking changes)
- `@dnd-kit/utilities@^3.2.2` (utilitaires CSS.Transform stables)

**Si peer dep React 19 bloquant** :
1. Tenter sans flag d'abord : `npm install @dnd-kit/core@^6.1.0 @dnd-kit/sortable@^8.0.0 @dnd-kit/utilities@^3.2.2`
2. Si erreur peer-dep React 19 : RE-tenter avec `--legacy-peer-deps`
3. Si --legacy-peer-deps utilisé : ajouter au package.json un `overrides` block explicite documentant la décision :
   ```json
   "overrides": {
     "@dnd-kit/core": { "react": "$react", "react-dom": "$react-dom" },
     "@dnd-kit/sortable": { "react": "$react", "react-dom": "$react-dom" }
   }
   ```
4. Documenter le choix (`--legacy-peer-deps` ou `overrides`) dans le commit message ET dans must_haves.truths via update du SUMMARY Plan 09.

**Vérifier le lock** :
```powershell
Select-String -Pattern '"@dnd-kit/core"' package.json
# Attendu : 1 hit dans dependencies (pas devDependencies)

Select-String -Pattern '"@dnd-kit/sortable"' package.json
# Attendu : 1 hit

Select-String -Pattern '"@dnd-kit/utilities"' package.json
# Attendu : 1 hit
```

**Vérifier compat React 19** :
```powershell
npm run typecheck
# Si erreur peer dep React → essayer @dnd-kit/core@^7 si disponible
# Sinon : npm install @dnd-kit/core@latest
```

**Note historique conservée (cf. block ci-dessus pour la procédure officielle revision)** : `@dnd-kit` core 6.1.x déclare React >=16, testé production avec 18+19. La résolution `--legacy-peer-deps` ou `overrides` est explicite et tracée.

**NE PAS** :
- Installer @dnd-kit/modifiers (pas nécessaire pour MoSCoW simple).
- Installer en devDependencies (les composants client utilisent en runtime).
- Pin une version exacte (utiliser ^).
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path node_modules/@dnd-kit/core
      # Attendu : True

      Test-Path node_modules/@dnd-kit/sortable
      # Attendu : True

      Test-Path node_modules/@dnd-kit/utilities
      # Attendu : True

      (Select-String -Pattern '"@dnd-kit/core"' package.json).Count
      # Attendu : 1

      # Issue revision #5 — vérifier les versions pinnées exactes
      (Select-String -Pattern '"@dnd-kit/sortable":\s*"\^8' package.json).Count
      # Attendu : 1 (sortable PIN major 8 obligatoire)

      npm run typecheck
      # Attendu : exit 0 (sans erreur peer deps fatale)

      npm run lint
      # Attendu : exit 0

      # Issue revision #5 — npm run build catches React 19 peer-dep runtime issues que typecheck rate
      npm run build
      # Attendu : exit 0 (build complet Next 15 incluant Server Components compilation)
    </automated>
  </acceptance_criteria>
  <done>
- 3 packages @dnd-kit installés AVEC versions pinnées (^6.1.0 / ^8.0.0 / ^3.2.2)
- package.json + package-lock.json updated
- npm typecheck clean (peer dep React 19 satisfait OU --legacy-peer-deps + overrides documenté)
- npm run build PASSE (validation runtime/peer-dep React 19, plus strict que typecheck)
- node_modules contient les 3 packages
- Si --legacy-peer-deps utilisé : `overrides` block ajouté à package.json + commit message documente la décision
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer components/moscow-card.tsx (carte sortable individuelle)</name>
  <files>components/moscow-card.tsx</files>
  <read_first>
    - `lib/types.ts` (MoscowCard type — Plan 05)
    - `lib/i18n.ts` (keys moscow_card_* — Plan 06 Task 2)
    - @dnd-kit docs : useSortable, CSS.Transform.toString
  </read_first>
  <action>
**Créer `components/moscow-card.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 09 — MoSCoW Kanban card (D-04).
// Sortable card via @dnd-kit/sortable. Drag handle, content, edit/delete buttons.
// R1 STRICT : 0 chiffre score/rank dans le rendu. R3 : pas de DOM disabled.
"use client";

import { useSortable } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";
import type { MoscowCard as MoscowCardType } from "@/lib/types";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;

const BUCKET_COLOR: Record<string, string> = {
  must: "#dc2626",
  should: "#ea580c",
  could: "#0284c7",
  wont: "#64748b",
};

export function MoscowCard({
  card,
  onEdit,
  onDelete,
}: {
  card: MoscowCardType;
  onEdit: (card: MoscowCardType) => void;
  onDelete: (cardId: string) => void;
}) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
    id: card.id,
  });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
    cursor: isDragging ? "grabbing" : "grab",
  };

  return (
    <article
      ref={setNodeRef}
      style={{
        ...style,
        background: "#ffffff",
        border: `2px solid ${BUCKET_COLOR[card.bucket] ?? "#cbd5e1"}`,
        borderRadius: 8,
        padding: 12,
        marginBottom: 8,
        display: "flex",
        flexDirection: "column",
        gap: 6,
        boxShadow: isDragging ? "0 8px 24px rgba(0,0,0,0.12)" : "0 1px 2px rgba(0,0,0,0.04)",
      }}
      aria-label={`Carte ${card.bucket}`}
    >
      {/* Drag handle - top of card */}
      <div
        {...attributes}
        {...listeners}
        style={{
          alignSelf: "flex-start",
          padding: "2px 6px",
          fontSize: 14,
          color: "#94a3b8",
          userSelect: "none",
        }}
        aria-label="Deplacer la carte"
        role="button"
        tabIndex={0}
      >
        {"☰"}
      </div>

      <p style={{ margin: 0, fontSize: 14, fontWeight: 600, color: "#0f172a" }}>{card.feature}</p>

      {card.pourquoi ? (
        <p style={{ margin: 0, fontSize: 12, color: "#475569" }}>
          <strong>{t.moscow_card_pourquoi_label} :</strong> {card.pourquoi}
        </p>
      ) : null}

      {card.contrainte ? (
        <p style={{ margin: 0, fontSize: 12, color: "#475569" }}>
          <strong>{t.moscow_card_contrainte_label} :</strong> {card.contrainte}
        </p>
      ) : null}

      <div style={{ display: "flex", gap: 6, marginTop: 4 }}>
        <button
          type="button"
          onClick={() => onEdit(card)}
          style={{
            padding: "4px 10px",
            background: "#f1f5f9",
            color: "#334155",
            border: "1px solid #cbd5e1",
            borderRadius: 6,
            fontSize: 12,
            cursor: "pointer",
          }}
        >
          {t.moscow_card_save /* placeholder edit label, real "Edit" key not added; reuse save */}
        </button>
        <button
          type="button"
          onClick={() => {
            if (window.confirm(`Supprimer cette carte ?`)) onDelete(card.id);
          }}
          style={{
            padding: "4px 10px",
            background: "#fef2f2",
            color: "#991b1b",
            border: "1px solid #fca5a5",
            borderRadius: 6,
            fontSize: 12,
            cursor: "pointer",
          }}
        >
          {t.moscow_card_delete}
        </button>
      </div>
    </article>
  );
}
```

**IMPORTANT** :
- `'use client'` directive ligne 1.
- Import `MoscowCard as MoscowCardType` pour éviter conflit avec le composant name.
- `☰` = caractère hamburger handle (☰), Unicode escape pour rester ASCII pur dans la source.
- `window.confirm` natif pour la suppression (pas de dialog custom — KISS T-3).
- Inline styles only (cohérent avec submission-form.tsx convention).
- `BUCKET_COLOR` basé sur Tailwind palette (red-600, orange-600, sky-600, slate-500).
- Pas de prop `score`, `rank`, `multiplier` (R1).

**Vérification** :
```powershell
Select-String -Pattern "score|rank|multiplier|/100|toFixed" components/moscow-card.tsx
# Attendu : 0 hit STRICT (R1)
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path components/moscow-card.tsx
      # Attendu : True

      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "useSortable" components/moscow-card.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "import.*from \"@dnd-kit/sortable\"" components/moscow-card.tsx).Count
      # Attendu : 1

      Select-String -Pattern "score|rank|multiplier|/100" components/moscow-card.tsx
      # Attendu : 0 hit (R1)
    </automated>
  </acceptance_criteria>
  <done>
- moscow-card.tsx créé
- useSortable + drag handle
- Edit/Delete buttons callbacks
- Couleurs par bucket
- R1 STRICT
- npm checks clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Créer components/moscow-kanban.tsx (board 4 colonnes + DnD orchestration)</name>
  <files>components/moscow-kanban.tsx</files>
  <read_first>
    - `components/moscow-card.tsx` (créé Task 2)
    - `app/actions.ts` Plan 06 (createMoscowCardFlow, updateMoscowCardFlow, deleteMoscowCardFlow, reorderMoscowCardsFlow, submitMoscowDeliverableFlow)
    - `lib/types.ts` (MoscowCard, MoscowBucket)
    - `lib/i18n.ts` (keys moscow_*)
  </read_first>
  <action>
**Créer `components/moscow-kanban.tsx`** :

```typescript
// T3X-EXPANSION wave 3 / plan 09 — MoSCoW Kanban board (D-04).
// 4 colonnes Must/Should/Could/Wont, DnD via @dnd-kit/core, persist via
// app/actions.ts (Plan 06).
// R1 STRICT : 0 chiffre score sur les cartes ou board. R3 : aucun blocking.
"use client";

import { useMemo, useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import {
  DndContext,
  closestCenter,
  KeyboardSensor,
  PointerSensor,
  TouchSensor,
  useSensor,
  useSensors,
  type DragEndEvent,
} from "@dnd-kit/core";
import {
  SortableContext,
  arrayMove,
  sortableKeyboardCoordinates,
  verticalListSortingStrategy,
} from "@dnd-kit/sortable";
import {
  createMoscowCardFlow,
  deleteMoscowCardFlow,
  reorderMoscowCardsFlow,
  submitMoscowDeliverableFlow,
} from "@/app/actions";
import { dictionaries } from "@/lib/i18n";
import type { MoscowBucket, MoscowCard as MoscowCardType } from "@/lib/types";
import { MoscowCard } from "@/components/moscow-card";

const t = dictionaries.fr;

const BUCKETS: { id: MoscowBucket; label: string }[] = [
  { id: "must", label: "Must" },
  { id: "should", label: "Should" },
  { id: "could", label: "Could" },
  { id: "wont", label: "Won't" },
];

export function MoscowKanban({
  deliverableTemplateId,
  initialCards,
}: {
  deliverableTemplateId: string;
  initialCards: MoscowCardType[];
}) {
  const router = useRouter();
  const [cards, setCards] = useState<MoscowCardType[]>(initialCards);
  const [, startTransition] = useTransition();
  const [submitMessage, setSubmitMessage] = useState<string>("");

  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 4 } }),
    useSensor(TouchSensor, { activationConstraint: { delay: 200, tolerance: 5 } }),
    useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }),
  );

  const cardsByBucket = useMemo(() => {
    const groups: Record<MoscowBucket, MoscowCardType[]> = {
      must: [],
      should: [],
      could: [],
      wont: [],
    };
    for (const c of cards) {
      groups[c.bucket].push(c);
    }
    for (const b of Object.keys(groups) as MoscowBucket[]) {
      groups[b].sort((a, b2) => a.ord - b2.ord);
    }
    return groups;
  }, [cards]);

  function findContainer(cardId: string): MoscowBucket | null {
    const card = cards.find((c) => c.id === cardId);
    return card?.bucket ?? null;
  }

  function handleDragEnd(event: DragEndEvent) {
    const { active, over } = event;
    if (!over || active.id === over.id) return;

    const activeBucket = findContainer(String(active.id));
    if (!activeBucket) return;

    // Determine target bucket : if over.id is a bucket-id (column), it's cross-bucket.
    // Otherwise, infer from the bucket of the over card.
    const overIsBucket = (BUCKETS.map((b) => b.id) as string[]).includes(String(over.id));
    const targetBucket = overIsBucket ? (String(over.id) as MoscowBucket) : findContainer(String(over.id));
    if (!targetBucket) return;

    setCards((prev) => {
      const activeIdx = prev.findIndex((c) => c.id === active.id);
      if (activeIdx < 0) return prev;
      const overIdx = prev.findIndex((c) => c.id === over.id);
      let next = [...prev];

      if (activeBucket === targetBucket && overIdx >= 0) {
        // Reorder within same bucket
        next = arrayMove(next, activeIdx, overIdx);
      } else {
        // Move to other bucket
        next[activeIdx] = { ...next[activeIdx], bucket: targetBucket };
        if (overIdx >= 0) {
          next = arrayMove(next, activeIdx, overIdx);
        }
      }

      // Reassign ord per bucket
      const ordsByBucket: Record<MoscowBucket, number> = { must: 0, should: 0, could: 0, wont: 0 };
      next = next.map((c) => {
        const newOrd = ordsByBucket[c.bucket]++;
        return { ...c, ord: newOrd };
      });

      // Persist reorder via server action (optimistic UI already applied)
      const items = next.map((c) => ({ id: c.id, bucket: c.bucket, ord: c.ord }));
      const fd = new FormData();
      fd.set("deliverableTemplateId", deliverableTemplateId);
      fd.set("items", JSON.stringify(items));
      startTransition(async () => {
        const res = await reorderMoscowCardsFlow({ ok: false, message: "" }, fd);
        if (!res.ok) setSubmitMessage(res.message);
      });

      return next;
    });
  }

  async function handleAddCard(bucket: MoscowBucket) {
    const feature = window.prompt(t.moscow_card_feature_label);
    if (!feature) return;
    const fd = new FormData();
    fd.set("deliverableTemplateId", deliverableTemplateId);
    fd.set("bucket", bucket);
    fd.set("ord", String(cardsByBucket[bucket].length));
    fd.set("feature", feature);
    fd.set("pourquoi", "");
    fd.set("contrainte", "");
    const res = await createMoscowCardFlow({ ok: false, message: "" }, fd);
    if (!res.ok) {
      setSubmitMessage(res.message);
      return;
    }
    // Issue revision #7 (option a) : pas d'optimistic state update à T-3.
    // revalidatePath en server action invalide le cache, router.refresh() force
    // le re-fetch côté client → la nouvelle carte apparaît avec son vrai UUID.
    setSubmitMessage(res.message);
    router.refresh();
  }

  async function handleDelete(cardId: string) {
    const fd = new FormData();
    fd.set("cardId", cardId);
    const res = await deleteMoscowCardFlow({ ok: false, message: "" }, fd);
    if (res.ok) {
      setCards((prev) => prev.filter((c) => c.id !== cardId));
    }
    setSubmitMessage(res.message);
  }

  async function handleEdit(card: MoscowCardType) {
    // Minimal inline-edit: prompt-driven. Real dialog out of scope T-3.
    const newFeature = window.prompt(t.moscow_card_feature_label, card.feature);
    if (newFeature === null) return;
    const newPourquoi = window.prompt(t.moscow_card_pourquoi_label, card.pourquoi) ?? "";
    const newContrainte = window.prompt(t.moscow_card_contrainte_label, card.contrainte) ?? "";

    const fd = new FormData();
    fd.set("cardId", card.id);
    fd.set("bucket", card.bucket);
    fd.set("ord", String(card.ord));
    fd.set("feature", newFeature);
    fd.set("pourquoi", newPourquoi);
    fd.set("contrainte", newContrainte);
    // updateMoscowCardFlow imported at runtime via dynamic import to keep top-level imports lean
    const { updateMoscowCardFlow } = await import("@/app/actions");
    const res = await updateMoscowCardFlow({ ok: false, message: "" }, fd);
    if (res.ok) {
      setCards((prev) =>
        prev.map((c) =>
          c.id === card.id
            ? { ...c, feature: newFeature, pourquoi: newPourquoi, contrainte: newContrainte }
            : c,
        ),
      );
    }
    setSubmitMessage(res.message);
  }

  async function handleSubmitDeliverable() {
    const fd = new FormData();
    fd.set("deliverableTemplateId", deliverableTemplateId);
    const res = await submitMoscowDeliverableFlow({ ok: false, message: "" }, fd);
    setSubmitMessage(res.message);
  }

  return (
    <section style={{ padding: 16 }}>
      <header style={{ marginBottom: 16 }}>
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 700 }}>{t.moscow_kanban_title}</h2>
        <p style={{ margin: "4px 0 0", fontSize: 14, color: "#475569" }}>
          {t.moscow_kanban_subtitle}
        </p>
      </header>

      <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
        <div
          style={{
            display: "grid",
            gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
            gap: 12,
          }}
        >
          {BUCKETS.map((b) => (
            <div
              key={b.id}
              id={b.id}
              style={{
                background: "#f8fafc",
                border: "1px solid #e2e8f0",
                borderRadius: 8,
                padding: 8,
                minHeight: 200,
              }}
            >
              <header
                style={{
                  display: "flex",
                  justifyContent: "space-between",
                  alignItems: "center",
                  marginBottom: 8,
                  fontWeight: 700,
                  color: "#0f172a",
                }}
              >
                <span>{b.label}</span>
                <button
                  type="button"
                  onClick={() => handleAddCard(b.id)}
                  style={{
                    padding: "2px 8px",
                    background: "#1d4ed8",
                    color: "#fff",
                    border: "none",
                    borderRadius: 4,
                    fontSize: 12,
                    cursor: "pointer",
                  }}
                >
                  + {t.moscow_card_add}
                </button>
              </header>
              <SortableContext
                items={cardsByBucket[b.id].map((c) => c.id)}
                strategy={verticalListSortingStrategy}
              >
                {cardsByBucket[b.id].map((card) => (
                  <MoscowCard key={card.id} card={card} onEdit={handleEdit} onDelete={handleDelete} />
                ))}
              </SortableContext>
            </div>
          ))}
        </div>
      </DndContext>

      <footer style={{ marginTop: 24, display: "flex", flexDirection: "column", gap: 8 }}>
        <button
          type="button"
          onClick={handleSubmitDeliverable}
          style={{
            padding: "10px 16px",
            background: "#15803d",
            color: "#fff",
            border: "none",
            borderRadius: 6,
            fontSize: 14,
            fontWeight: 600,
            cursor: "pointer",
            alignSelf: "flex-start",
          }}
        >
          {t.moscow_submit_deliverable}
        </button>
        {submitMessage ? (
          <p
            role="status"
            style={{ margin: 0, fontSize: 13, color: submitMessage.toLowerCase().includes("recommandation") ? "#92400e" : "#15803d" }}
          >
            {submitMessage}
          </p>
        ) : null}
      </footer>
    </section>
  );
}
```

**IMPORTANT** :
- `'use client'` directive ligne 1.
- Sensors : Pointer + Touch (mobile) + Keyboard (a11y).
- DnD persist via `reorderMoscowCardsFlow` avec FormData.
- `handleAddCard` utilise `window.prompt` (KISS T-3, dialog custom out of scope).
- `handleEdit` idem (3 prompts successifs feature/pourquoi/contrainte). UX rough mais fonctionnel.
- `submitMessage` affiche le message warn-only de `submitMoscowDeliverableFlow` (R2 OK).
- `gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))"` = mobile-first responsive (1 col <440px, 2 cols 440-660px, 4 cols >=880px).
- Optimistic update : `setCards` immédiat, `startTransition` async server.

**NE PAS** :
- Modifier les composants existants.
- Importer `@dnd-kit/modifiers` (out of scope).
- Faire de fetch GET cards client-side — les `initialCards` viennent du parent server component (Plan 10).
- Ajouter de l'animation lib externe (CSS.Transform de @dnd-kit/utilities suffit).

**Vérification** :
```powershell
Select-String -Pattern "score|rank|multiplier|/100|toFixed" components/moscow-kanban.tsx
# Attendu : 0 hit (R1)

Select-String -Pattern "blocks_progression|disabled.*because" components/moscow-kanban.tsx
# Attendu : 0 hit (R3)

npm run typecheck
# Attendu : exit 0
```
  </action>
  <acceptance_criteria>
    <automated>
      Test-Path components/moscow-kanban.tsx
      # Attendu : True

      npm run typecheck
      # Attendu : exit 0

      npm run lint
      # Attendu : exit 0

      (Select-String -Pattern "DndContext" components/moscow-kanban.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "SortableContext" components/moscow-kanban.tsx).Count
      # Attendu : 1

      (Select-String -Pattern "reorderMoscowCardsFlow|createMoscowCardFlow|deleteMoscowCardFlow|submitMoscowDeliverableFlow" components/moscow-kanban.tsx).Count
      # Attendu : >= 4

      (Select-String -Pattern "TouchSensor|KeyboardSensor" components/moscow-kanban.tsx).Count
      # Attendu : >= 2 (mobile + a11y)

      Select-String -Pattern "score|rank|multiplier|/100" components/moscow-kanban.tsx
      # Attendu : 0 hit (R1)
    </automated>
  </acceptance_criteria>
  <done>
- moscow-kanban.tsx créé
- DnDKit Pointer + Touch + Keyboard sensors
- onDragEnd persist via reorderMoscowCardsFlow
- 4 actions consumed (create / update / delete / reorder / submit)
- Mobile-first responsive grid
- R1 STRICT (0 hit grep)
- R3 OK (no blocking)
- npm checks clean
  </done>
</task>

<task type="checkpoint:advisor-audit" gate="blocking">
  <name>Task 4: Audit R1/R2/R3 + commit</name>
  <files>(no file modification — review checkpoint + commit)</files>
  <read_first>
    - `git diff --cached package.json package-lock.json components/moscow-card.tsx components/moscow-kanban.tsx`
    - `.claude/agents/eic-pedagogical-advisor.md`
  </read_first>
  <action>
**Pré-checkpoint** : Tasks 1-3 complétées, npm clean.

**Action Claude post-approval** :
```bash
git add package.json package-lock.json components/moscow-card.tsx components/moscow-kanban.tsx
git commit -m "$(cat <<'EOF'
feat(t3x-moscow-ui): add MoSCoW Kanban native UI with @dnd-kit DnD (D-04)

Wave 3 / plan 09 : 2 composants Player UI pour le Kanban MoSCoW livrable #4.
DnD via @dnd-kit/core (D-04 figee owner) — accessible (Keyboard sensor),
mobile (Touch sensor), bundle leger ~120kb total.

Dependencies ajoutees :
- @dnd-kit/core ^6.x
- @dnd-kit/sortable ^8.x ou ^10.x
- @dnd-kit/utilities ^3.x

Composants :
- moscow-card.tsx ('use client') : useSortable + drag handle + edit/delete
  buttons + couleur par bucket
- moscow-kanban.tsx ('use client') : DndContext + 4 SortableContext (must/should/
  could/wont), onDragEnd persist via reorderMoscowCardsFlow optimistic UI,
  handleAddCard/Edit/Delete via window.prompt KISS, handleSubmitDeliverable
  consume submitMoscowDeliverableFlow (warn-only suffix R2/R3)

R1 STRICT : grep score|rank|multiplier|/100 sur les 2 composants = 0 hit.
R2 : warn message via submitMessage state, jamais blocage.
R3 : pas de DOM disabled, pas de blocs_progression. Mobile-first grid responsive.

npm typecheck OK, npm lint OK.

Refs : BRIEF §2.B (MoSCoW Kanban native), §4 D-04 (@dnd-kit/core lib),
CLAUDE.md cardinal rules.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3x-moscow-ui"
      # Attendu : 1 hit

      git diff --name-only HEAD~1 HEAD
      # Attendu : 4 fichiers (package.json, package-lock.json, moscow-card.tsx, moscow-kanban.tsx)

      Select-String -Pattern "score|rank|multiplier|/100" components/moscow-card.tsx, components/moscow-kanban.tsx
      # Attendu : 0 hit
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → commit. Sinon : décrire issues.</done>
  <what-built>
- 2 composants Kanban + DnD
- 3 deps @dnd-kit installés
- R1/R2/R3 préservés
- npm checks clean
  </what-built>
  <how-to-verify>
**Audit checklist** :

1. **R1 grep STRICT** :
   ```powershell
   Select-String -Pattern "score|rank|multiplier|multiplierFactor|/100|/140|toFixed|points" components/moscow-card.tsx, components/moscow-kanban.tsx
   ```
   Attendu : 0 hit.

2. **R3 grep** :
   ```powershell
   Select-String -Pattern "blocks_progression|disabled.*because" components/moscow-card.tsx, components/moscow-kanban.tsx
   ```
   Attendu : 0 hit.

3. **DnD a11y** :
   ```powershell
   Select-String -Pattern "KeyboardSensor|sortableKeyboardCoordinates" components/moscow-kanban.tsx
   ```
   Attendu : >= 1 hit (a11y nav clavier).

4. **Mobile fallback** :
   ```powershell
   Select-String -Pattern "TouchSensor" components/moscow-kanban.tsx
   ```
   Attendu : 1 hit (mobile DnD).

5. **R2 warn-only suffix** :
   - Dans `handleSubmitDeliverable`, le message de `submitMoscowDeliverableFlow` peut contenir des warnings (cf. Plan 06). Le composant l'affiche tel quel via `<p>{submitMessage}</p>` — c'est warn-only, pas blocking.

6. **Test runtime rapide** (Omar — optionnel à T-3) :
   - `npm run dev`
   - Naviguer `/journey/deliverable/{id-livrable-4}` (Plan 10 ajoutera la route)
   - Pour l'instant la composition `<MoscowKanban deliverableTemplateId="..." initialCards={[...]} />` n'est pas surfaced ; ce test est valide en Plan 10 ou Plan 12 smoke.
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "remplacer window.prompt par dialog custom", "split moscow-kanban en 2 composants").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification** :

1. **TS health** :
   ```powershell
   npm run typecheck ; npm run lint
   # Attendu : exit 0
   ```

2. **R1 final** :
   ```powershell
   Select-String -Pattern "score|rank|multiplier|/100" components/moscow-card.tsx, components/moscow-kanban.tsx
   # Attendu : 0 hit
   ```

3. **deps installed** :
   ```powershell
   Test-Path node_modules/@dnd-kit/core
   Test-Path node_modules/@dnd-kit/sortable
   # Attendu : True True
   ```
</verification>

<success_criteria>
- [ ] @dnd-kit/core + sortable + utilities installés
- [ ] moscow-card.tsx (useSortable + drag handle)
- [ ] moscow-kanban.tsx (DndContext + 4 SortableContext + sensors mobile + a11y)
- [ ] 4 actions consumed (create/update/delete/reorder/submit)
- [ ] R1 STRICT (0 hit grep)
- [ ] R3 OK (no blocking)
- [ ] Mobile-first responsive grid
- [ ] npm checks clean
- [ ] Commit atomique
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-09-SUMMARY.md` :

Sections :
1. **Deps installées** : @dnd-kit/core + sortable + utilities (versions)
2. **2 composants livrés** : moscow-card + moscow-kanban
3. **DnD UX** : Pointer + Touch + Keyboard sensors, optimistic update, persist via reorderMoscowCardsFlow
4. **R1/R2/R3 audit** : tous OK
5. **Mobile-first** : grid auto-fit, breakpoints implicites
6. **Limitations T-3** : window.prompt pour add/edit, dialog custom out-of-scope (v0.3 candidate)
7. **Files modified** : 4
8. **Next consumers** : Plan 10 (route /journey/deliverable/[id] surface MoscowKanban), Plan 11 (CSV export)
</output>
