---
phase: quick-260510-t3x
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - CLAUDE.md
  - app/journey/page.tsx
  - database/seed_event_hackdays.sql
  - supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
autonomous: false
requirements:
  - T3X-EXPANSION-WAVE0-COMMIT
must_haves:
  truths:
    - "Le tree git est propre (aucun fichier modifié non staged) dans les 4 fichiers du polish T-3 live avant que les Waves 1-4 n'introduisent de nouveaux edits"
    - "Le commit single-shot est intitulé 'feat(t3-polish): commit live edits seed AgreenTech 5/5bis + journey hero subtitle' avec coauthor Claude"
    - "database/seed_event_hackdays.sql et supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql restent strictement identiques entre disque et HEAD post-commit (mirror parfait)"
    - "app/journey/page.tsx ligne 85 (subtitle hero = 1er paragraphe description) committed"
    - "CLAUDE.md sections 'T-3 Critical Gates' + 'Pre-edit guards' + 'Freeze feat()' committed"
    - "Aucun fichier hors les 4 polish T-3 n'est inclus (zero leak des screenshots/, .planning/ui-reviews/, EIC-MANAGER-*.md, RETROSPECTIVE-T3-*.md, T3-IMPROVEMENTS.md, cohorte-agreentech.csv)"
    - "Aucun nouveau code source ajouté — ce plan committe uniquement les edits déjà sur disque"
    - "Section 5.9 (pitch-deck-v1) DÉJÀ refondue dans les edits live (12 slides AgriTech avec slide 4 PREUVE TERRAIN obligatoire) — Plan 04 supprimé suite revision (la refonte est incluse dans CE commit)"
    - "Plan 01 commit suffit pour finaliser TOUTES les sections 5.1-5.9 du seed_event_hackdays.sql — aucun plan ultérieur ne re-touche ce fichier"
  artifacts:
    - path: "database/seed_event_hackdays.sql"
      provides: "Seed AgreenTech committed (#5 reaffectation analyse concurrentielle, #5bis TAM/SAM/SOM, #1-#9 polish ASCII incluant la refonte complète de #9 Pitch deck final AgriTech avec 12 slides + slide 4 PREUVE TERRAIN)"
      contains: "Analyse concurrentielle + carte de positionnement|TAM / SAM / SOM|Pitch deck final AgriTech \(10-12 slides\)|PREUVE TERRAIN"
    - path: "supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql"
      provides: "Mirror migration Supabase (identique au database/seed_event_hackdays.sql)"
      contains: "Analyse concurrentielle + carte de positionnement|TAM / SAM / SOM"
    - path: "app/journey/page.tsx"
      provides: "Hero subtitle polish (split sur premier paragraphe description)"
      contains: "split.*\\\\n\\\\n"
    - path: "CLAUDE.md"
      provides: "T-3 Critical Gates + Pre-edit guards + Freeze sections committed"
      contains: "T-3 Critical Gates|Pre-edit guards|Freeze feat"
  key_links:
    - from: "database/seed_event_hackdays.sql"
      to: "supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql"
      via: "Mirror parfait byte-pour-byte (database/ = source de vérité, supabase/migrations/ = copie applicable via Supabase CLI)"
      pattern: "diff -q database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql"
---

<objective>
Wave 0 — committer atomiquement les 4 fichiers de polish T-3 live (déjà modifiés sur disque, NON staged) AVANT que les Waves 1-4 (bonus_events, MoSCoW Kanban, etc.) introduisent de nouveaux edits sur les mêmes fichiers SQL.

**Pourquoi un plan dédié** : les Waves 1-4 (bonus_events recreate, MoSCoW Kanban, helpers, routes) vont introduire de nouveaux fichiers SQL/TS. Si l'on ne committe pas d'abord les 4 fichiers polish déjà sur disque, ils se mélangeraient avec les diffs des waves suivantes — impossible à reviewer ou rollback proprement.

**Note revision (2026-05-10)** : Plan 04 (qui devait polir la section 5.9 `pitch-deck-v1`) a été SUPPRIMÉ suite à audit checker — la refonte 12 slides AgriTech + slide 4 PREUVE TERRAIN est DÉJÀ présente dans les edits live de `database/seed_event_hackdays.sql` (cf. lignes 477-520 actuelles). Ce Plan 01 commit suffit donc à finaliser TOUTES les sections 5.1-5.9. Aucun autre plan ne re-touche ce fichier.

**Scope** : 4 fichiers, 1 commit, 0 nouveau code. Aucune autre modification (pas d'ajout, pas de suppression de ligne).

**Output** :
- Commit unique sur main avec les 4 polish edits
- Tree git propre sur ces 4 fichiers avant Wave 1
- Vérification mirror parfait `database/` <-> `supabase/migrations/`
- Section 5.9 `pitch-deck-v1` (refonte 12 slides + slide 4 PREUVE TERRAIN) inclus dans CE commit (Plan 04 supprimé)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/BRIEF.md

<interfaces>
<!-- Pas d'interfaces TS / contracts à exposer ici — ce plan committe uniquement -->
<!-- des edits SQL/markdown/TSX déjà finalisés sur disque par Omar avant la session. -->

État git attendu au début du plan :
```
M CLAUDE.md
M app/journey/page.tsx
M database/seed_event_hackdays.sql
M supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
```

Stats diff attendues (cf. git diff --stat HEAD au lancement) :
```
CLAUDE.md                                          |  37 ++-
app/journey/page.tsx                               |   2 +-
database/seed_event_hackdays.sql                   | 251 +++++++++++++++------
supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql | 251 +++++++++++++++------
4 files changed, 411 insertions(+), 130 deletions(-)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vérifier état git, mirror SQL et stage atomique des 4 fichiers polish</name>
  <files>(read-only verification + git add — no file content modification)</files>
  <read_first>
    - `git status --short` (état initial)
    - `git diff --stat HEAD -- CLAUDE.md app/journey/page.tsx database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql`
    - `database/seed_event_hackdays.sql` (lignes 285-360 pour vérifier section 5.5 et 5.5bis présentes)
    - `supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql` (mêmes lignes pour mirror parfait)
    - `app/journey/page.tsx` ligne 85 (subtitle = `next.template.description?.split("\\n\\n")[0]`)
    - `CLAUDE.md` sections "T-3 Critical Gates" + "Pre-edit guards" + "Freeze feat()"
  </read_first>
  <action>
**Étape 1 — Vérifier l'état git initial** (READ-ONLY, ne pas modifier) :
```powershell
git status --short
# Attendu : exactement ces 4 lignes M parmi d'autres ?? :
#   M CLAUDE.md
#   M app/journey/page.tsx
#   M database/seed_event_hackdays.sql
#   M supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
```

**Étape 2 — Vérifier mirror parfait SQL** (CRITIQUE — si le diff existe, STOPPER avant commit) :
```powershell
# Sur PowerShell Windows (cf env), utiliser fc.exe ou diff via Git Bash
git diff --no-index database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
# Attendu : 0 ligne de diff (les 2 fichiers DOIVENT être strictement identiques byte-pour-byte).
# Si différence : NE PAS COMMITTER — output le diff et passer en Task 2 (checkpoint).
```

**Étape 3 — Vérifier qu'aucun autre fichier candidat ne s'est glissé dans M** :
```powershell
# Lister explicitement les M (modified, pas ?? untracked) :
git diff --name-only HEAD
# Attendu EXACTEMENT 4 lignes (ordre alphabétique git) :
#   CLAUDE.md
#   app/journey/page.tsx
#   database/seed_event_hackdays.sql
#   supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
# Si > 4 lignes : NE PAS COMMITTER — output la liste et passer en Task 2 (checkpoint).
# Si < 4 lignes : un fichier a déjà été committé entre-temps — output et passer en Task 2.
```

**Étape 4 — Stage les 4 fichiers explicitement** (jamais `git add .` ni `git add -A`) :
```powershell
git add CLAUDE.md app/journey/page.tsx database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
git status --short
# Attendu : 4 lignes commençant par "M  " (M + 2 espaces = staged).
```

**Étape 5 — Vérification grep R1 sur le diff staged** (defense-in-depth — ce polish ne doit pas leaker chiffres bruts côté Player) :
```powershell
git diff --cached -- database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql | Select-String -Pattern "/140|percentile|rank\b|/100"
# Attendu : 0 hit (aucun chiffre brut suspect dans le polish seed).
```

**Étape 6 — Vérification grep R3 sur app/journey/page.tsx diff** :
```powershell
git diff --cached -- app/journey/page.tsx | Select-String -Pattern "disabled|blocks_progression"
# Attendu : 0 hit (le polish hero subtitle ne touche pas à la progression).
```

**NE PAS** :
- Committer encore (le commit se fait en Task 2 après checkpoint Omar)
- Modifier le contenu des 4 fichiers — ce plan committe l'état CURRENT du disque
- Ajouter d'autres fichiers (screenshots/, .planning/, EIC-MANAGER-*.md, etc. — tous sont ?? untracked et restent untracked)

**Output attendu** : 4 fichiers staged, 0 leak R1/R3 dans diff, mirror SQL parfait, prêt pour commit en Task 2.
  </action>
  <acceptance_criteria>
    <automated>
      git diff --name-only --cached
      # Attendu : 4 lignes exactement (CLAUDE.md, app/journey/page.tsx, database/seed_event_hackdays.sql, supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql)
      git diff --cached --shortstat
      # Attendu : "4 files changed, ~411 insertions(+), ~130 deletions(-)"
      git diff --cached -- database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql | Select-String -SimpleMatch -Pattern "/140" -Quiet
      # Attendu : False (no R1 leak)
    </automated>
  </acceptance_criteria>
  <done>
- 4 fichiers staged exactement (ni plus, ni moins)
- Mirror SQL parfait `database/seed_event_hackdays.sql` == `supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql`
- Diff staged grep clean (R1 + R3)
- Prêt pour commit Task 2
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint Omar — review diff polish T-3 + commit + verify clean tree</name>
  <files>(no file modification — review-only checkpoint sur staged diff)</files>
  <read_first>
    - `git diff --cached --stat`
    - `git diff --cached CLAUDE.md` (37 lignes diff attendu)
    - `git diff --cached app/journey/page.tsx` (2 lignes diff attendu — split sur description premier paragraphe)
    - `git diff --cached database/seed_event_hackdays.sql` (251 lignes diff attendu — sections 5.1/5.2/5.3/5.4/5.5/5.5bis refondues)
  </read_first>
  <action>
HUMAN CHECKPOINT — Omar review le diff staged complet, valide le polish T-3, puis Claude execute le commit. Voir <how-to-verify> pour la procédure 6 étapes.

**Pré-checkpoint (auto par Claude)** : Task 1 a déjà staged les 4 fichiers et fait les grep R1/R3 clean.

**Action Claude post-approval** :
```powershell
# Commit avec message HEREDOC pour formatting + co-author Claude
git commit -m @"
feat(t3-polish): commit live edits seed AgreenTech 5/5bis + journey hero subtitle

Wave 0 du scope expansion T-3 (cf .planning/quick/260510-t3x-...)
Committe atomiquement les 4 fichiers polish déjà sur disque AVANT que
Wave 1 (bonus_events recreate + MoSCoW Kanban) introduise de nouveaux edits.

- database/seed_event_hackdays.sql + mirror supabase/migrations/20260510160000_*
  - 5.5 reaffecté : 'etude-marche-v1' MoSCoW prototype -> Analyse concurrentielle + carte positionnement (D-01 t3x BRIEF §2.E)
  - 5.5bis ajouté : 'tam-sam-som-v1' nouveau livrable L3 ord 2 (TAM/SAM/SOM avec hypotheses calcul)
  - 5.1/5.2/5.3/5.4 polish ASCII et descriptions enrichies (champs minimum, anti-fabrication)
  - Slugs preserves (Option 1 idempotency) — 0 row orphelin garanti
- app/journey/page.tsx ligne 85 : subtitle hero = description.split('\n\n')[0] (1er paragraphe au lieu de full description)
- CLAUDE.md : T-3 Critical Gates B1-B5 + Pre-edit guards + Freeze feat() jusqu'au 14/05 soir

R1 OK (audit grep cached clean). R3 OK (no DOM disabled added). Schema Postgres intact.

Refs: BRIEF section 7 'Status edits live deja appliques (NON commites)'

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
"@
```

Note PowerShell : utiliser `@"..."@` here-string OU `git commit -F` avec un fichier temp. Si Bash dispo via outil Bash : utiliser `git commit -m "$(cat <<'EOF' ... EOF)"` standard.

**Post-commit verification** :
```powershell
git status --short
# Attendu : les 4 M ont disparu. Reste les ?? untracked (normal, pas notre scope).
git log --oneline -1
# Attendu : commit hash + "feat(t3-polish): commit live edits seed AgreenTech 5/5bis + journey hero subtitle"
```
  </action>
  <acceptance_criteria>
    <automated>
      git log --oneline -1 | Select-String -Pattern "t3-polish"
      # Attendu : 1 hit (commit présent)
      git diff --name-only HEAD~1 HEAD
      # Attendu : 4 fichiers exactement
      git status --short -- CLAUDE.md app/journey/page.tsx database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
      # Attendu : output vide (les 4 fichiers sont clean post-commit)
    </automated>
  </acceptance_criteria>
  <done>Omar tape "approved" → Claude execute git commit. Sinon Omar liste les corrections (rollback partiel, exclure 1 fichier, ajouter contexte au message commit) et Claude itère.</done>
  <what-built>
- 4 fichiers polish T-3 committed atomiquement sur main
- Tree git clean sur ces 4 fichiers (Wave 1 peut désormais éditer database/seed_event_hackdays.sql sans diff mélangé)
- Aucun nouveau code, aucune nouvelle fonctionnalité, aucun nouveau test
- Mirror `database/` <-> `supabase/migrations/` préservé pour application Supabase CLI ultérieure
  </what-built>
  <how-to-verify>
**Procédure 6 étapes pour Omar avant `approved`** :

1. **Diff stat sanity check** :
   ```powershell
   git diff --cached --stat
   ```
   Attendu : 4 fichiers, ~411 insertions, ~130 deletions. Si très différent → quelque chose d'autre a fuité dans le stage.

2. **Diff CLAUDE.md (37 lignes)** :
   ```powershell
   git diff --cached CLAUDE.md
   ```
   Vérifier que ce sont bien les sections "T-3 Critical Gates", "Pre-edit guards", "Freeze feat() jusqu'au 14/05" qui sont ajoutées (pas de modification d'autres sections style "Architecture" ou "Conventions").

3. **Diff app/journey/page.tsx (2 lignes)** :
   ```powershell
   git diff --cached app/journey/page.tsx
   ```
   Attendu : ligne ~85 modifie `subtitle: next.template.description || t.journey_v2_hero_subtitle_default,` → `subtitle: next.template.description?.split("\\n\\n")[0] || t.journey_v2_hero_subtitle_default,` (split sur premier paragraphe). Aucune autre ligne touchée.

4. **Diff database/seed_event_hackdays.sql (review FR pédagogique)** :
   ```powershell
   git diff --cached database/seed_event_hackdays.sql
   ```
   Attendu :
   - Section 5.1 (Persona AgriTech) : description enrichie avec 7 champs minimum + 2 sources distinctes (anti-fabrication)
   - Section 5.2 (Hypothèse VP cible) : completion-based (full points si pitch 1min réalisé)
   - Section 5.3 (BMC 9 blocs) : 9 blocs détaillés + rubric hybride completion 15 + coherence 10
   - Section 5.4 (MoSCoW prototype) : verbatims migrent vers bonus, format Kanban link Trello/Miro
   - Section 5.5 reaffectée : `etude-marche-v1` → "Analyse concurrentielle + carte de positionnement" (slug PRÉSERVÉ — D-01)
   - Section 5.5bis NEW : `tam-sam-som-v1` (TAM/SAM/SOM)
   - Tous les ON CONFLICT (mission_id, slug) DO UPDATE intacts (idempotency Option 1 préservée)

5. **Diff mirror parfait** (CRITIQUE) :
   ```powershell
   git diff --cached database/seed_event_hackdays.sql > _t1.txt
   git diff --cached supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql > _t2.txt
   fc.exe _t1.txt _t2.txt
   Remove-Item _t1.txt, _t2.txt
   ```
   Attendu : "FC : aucune différence rencontrée" (les 2 diffs sont identiques car les 2 fichiers sont mirror).

6. **Audit R1 grep cached** (defense-in-depth) :
   ```powershell
   git diff --cached | Select-String -Pattern "/140|percentile|rank\b|toFixed"
   ```
   Attendu : 0 hit côté Player-facing.

**Si tout OK** → tape `approved`. Claude execute le commit + verify post-commit clean.

**Si problème** → décrire (ex: "extraire CLAUDE.md du commit", "ajouter référence BRIEF dans le message", "renommer 5.5bis slug en `marche-tam-sam-som-v1`") et Claude itère.

**Side-effect attendu post-commit** : aucun. Le polish est déjà sur disque, le commit ne change rien à l'application runtime — il sert uniquement à snapshoter l'état avant Wave 1.
  </how-to-verify>
  <resume-signal>Type "approved" pour commit OU décrire issues (ex: "exclure CLAUDE.md de ce commit", "rollback diff 5.5bis", "split en 2 commits seed/journey").</resume-signal>
</task>

</tasks>

<verification>
**Phase verification (post-task 2)** :

1. **Tree clean sur les 4 fichiers polish** :
   ```powershell
   git status --short CLAUDE.md app/journey/page.tsx database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
   # Attendu : output vide
   ```

2. **Commit atomique présent** :
   ```powershell
   git log -1 --pretty=format:"%h %s"
   # Attendu : <hash> feat(t3-polish): commit live edits seed AgreenTech 5/5bis + journey hero subtitle
   git log -1 --name-only
   # Attendu : 4 fichiers listés exactement
   ```

3. **Mirror SQL préservé post-commit** :
   ```powershell
   git diff --no-index database/seed_event_hackdays.sql supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql
   # Attendu : 0 ligne (mirror parfait)
   ```

4. **Aucun autre fichier dans le commit** (sécurité) :
   ```powershell
   git show --name-only HEAD | Select-Object -Skip 6
   # Attendu : 4 lignes
   ```
</verification>

<success_criteria>
- [ ] 4 fichiers polish T-3 committed atomiquement sur main
- [ ] Mirror parfait `database/seed_event_hackdays.sql` <-> `supabase/migrations/20260510160000_seed_event_hackdays_agritech.sql` préservé
- [ ] Tree git propre sur ces 4 fichiers (Wave 1 peut éditer sans diff mélangé)
- [ ] Aucun fichier hors scope inclus (screenshots/, .planning/, EIC-MANAGER-*.md, etc. restent untracked)
- [ ] Audit R1/R3 grep cached clean
- [ ] Commit message conforme (feat(t3-polish): ... + co-author Claude)
- [ ] Omar approved la procédure 6 étapes (Task 2 checkpoint)
</success_criteria>

<output>
After completion, create `.planning/quick/260510-t3x-scope-expansion-moscow-kanban-bonus-events/260510-t3x-01-SUMMARY.md` :

Sections obligatoires :
1. **Commit hash + message final** (1 ligne)
2. **Stats diff** (insertions, deletions, files changed)
3. **Confirmation mirror SQL** (database/ ↔ supabase/migrations/)
4. **Files NOT included** (liste explicite des ?? untracked qui restent untracked — sécurité audit)
5. **Wave 1 prêt** : confirmer que `database/seed_event_hackdays.sql` est désormais clean pour les Plans 02/03/04 qui vont y ajouter `bonus_events` insertions et idempotency check #5/#5bis officiel
</output>
