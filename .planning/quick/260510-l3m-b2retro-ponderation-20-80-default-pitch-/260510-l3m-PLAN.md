---
phase: quick-260510-l3m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/results.ts
autonomous: true
requirements:
  - B2-RETRO
must_haves:
  truths:
    - "DEFAULT_PITCH_WEIGHT = 0.8 (au lieu de 0.5) dans lib/results.ts:30 — décision EIC manager 2026-05-10."
    - "Le commentaire en-tête du fichier (ligne 3) reflète la pondération 20/80 (project/pitch) au lieu de 50/50."
    - "Aucun appelant ne passe pitchWeight explicite — la constante propage automatiquement."
    - "computeRanking() côté GameMaster (app/results/page.tsx:113) utilise désormais 20/80 par défaut."
    - "npm run typecheck && npm run lint && npm run build passent sans nouvelle erreur."
  artifacts:
    - path: "lib/results.ts"
      provides: "DEFAULT_PITCH_WEIGHT = 0.8 ; commentaire header reflète AgreenTech 20/80"
      contains: "DEFAULT_PITCH_WEIGHT = 0.8"
---

<objective>
B2 rétro (CLAUDE.md T-3 Critical Gates) — Patcher `lib/results.ts:30` `DEFAULT_PITCH_WEIGHT` de `0.5` à `0.8`, conforme décision EIC manager rétro 2026-05-10 (AgreenTech 2026 : final = projet × 0.2 + pitch × 0.8).
</objective>

<context>
T3-IMPROVEMENTS.md ligne 11 figé :
> Calcul `final_score` dans logique scoring `/admin` : `final = projet * 0.2 + pitch * 0.8`

Discovery préalable :
- `computeRanking(opts?: { pitchWeight?: number })` accepte un override mais n'est appelé QUE depuis `app/results/page.tsx:113` SANS opts → utilise `DEFAULT_PITCH_WEIGHT`.
- Aucun autre call site `computeRanking` ou `pitchWeight` dans le codebase.
- Changer la constante propage 100% automatiquement à toute la branche /results (admin + Player gating B1retro déjà en place).

Pre-edit guard CLAUDE.md (zone sensible `lib/results.ts`) :
- EIC pédagogique advisor : décision déjà figée par EIC manager 10/05 — pas de jugement à porter, mention explicite dans commit body suffit.
- Audit grep R1 post-edit : la pondération ne change pas la visibilité des chiffres (déjà colmatée par B1retro) — recalibre seulement les valeurs côté GM. Audit sera vide.
- Dual-mode demo : `lib/results.ts` retourne `{rows:[]}` quand !supabase, donc pas affecté.
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Patch DEFAULT_PITCH_WEIGHT 0.5 → 0.8 + commentaire header</name>
  <files>lib/results.ts</files>
  <action>
1. Edit ligne 3 : `weighted 50/50 by default` → `weighted 20/80 (project/pitch) by default — AgreenTech 2026 décision EIC manager 2026-05-10.`
2. Edit ligne 30 : `export const DEFAULT_PITCH_WEIGHT = 0.5;` → `export const DEFAULT_PITCH_WEIGHT = 0.8;`
3. Run `npm run typecheck && npm run lint && npm run build`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
- `lib/results.ts:30` contient `DEFAULT_PITCH_WEIGHT = 0.8`
- Commentaire ligne 3 mentionne 20/80 (project/pitch)
- typecheck + lint + build PASS
- Aucun autre fichier modifié
  </done>
</task>

</tasks>

<verification>
**Audit final :**

1. `git diff --name-only HEAD` → `lib/results.ts` uniquement.
2. `grep -n "DEFAULT_PITCH_WEIGHT" lib/results.ts` → ligne 30 = `0.8`, ligne 78 = référence dans clampWeight (inchangée).
3. `npm run typecheck && npm run lint && npm run build` → exit 0.
4. Audit R1 (héritage B1retro) `grep -rn "toFixed\|formatNumber" components/results-replay.tsx components/results-podium.tsx --include="*.tsx"` → tous gates `isGameMaster` (régression check).
</verification>

<success_criteria>
- [ ] DEFAULT_PITCH_WEIGHT = 0.8 dans lib/results.ts:30
- [ ] Header comment reflète 20/80
- [ ] typecheck + lint + build PASS
- [ ] 1 fichier modifié exactement
- [ ] Commit atomic avec message conventionnel mentionnant la décision EIC manager 10/05
</success_criteria>
