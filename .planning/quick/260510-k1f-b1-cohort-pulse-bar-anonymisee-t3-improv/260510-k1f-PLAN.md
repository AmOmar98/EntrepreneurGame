---
phase: 260510-k1f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cohort-pulse.ts                # NEW — server-side aggregator (dual-mode, anonymised)
  - components/cohort-pulse.tsx        # NEW — server component, pure render of {levelId, count, total}[]
  - app/globals.css                    # ADD — .eic-cohort-pulse* utilities (~30 lignes, fin de fichier)
  - lib/i18n.ts                        # ADD — keys cohort_pulse_*
  - app/journey/page.tsx               # MODIFY — fetch cohort pulse + mount <CohortPulse> au-dessus de <JourneyClient>
autonomous: true
requirements:
  - T3-B1
must_haves:
  truths:
    - "Le Player voit une barre 'X/N équipes ont soumis [level]' en haut de /journey"
    - "Aucun nom d'équipe, aucun score, aucun rang individuel n'est exposé au Player (R1)"
    - "Le composant fonctionne en mode demo (sans Supabase) avec un agrégat plausible issu du seed"
    - "Le composant fonctionne en mode Supabase avec un agrégat réel sur la table submissions"
    - "Les 6 levels L0..L5 sont affichés, une ligne par level (L6/L7 alumni/traction exclus du pilote)"
    - "Les transitions CSS respectent prefers-reduced-motion"
  artifacts:
    - path: "lib/cohort-pulse.ts"
      provides: "getCohortPulse() server fetcher returning {levelId, count, total}[] (jamais de players)"
      exports: ["getCohortPulse", "CohortPulseEntry"]
    - path: "components/cohort-pulse.tsx"
      provides: "<CohortPulse entries={...}> server component, render only"
      exports: ["CohortPulse"]
    - path: "lib/i18n.ts"
      contains: "cohort_pulse_"
    - path: "app/journey/page.tsx"
      provides: "Mount <CohortPulse entries={pulse}> au-dessus de <JourneyClient>"
      contains: "CohortPulse"
  key_links:
    - from: "app/journey/page.tsx"
      to: "lib/cohort-pulse.ts:getCohortPulse"
      via: "server-side await fetch in page render"
      pattern: "getCohortPulse"
    - from: "components/cohort-pulse.tsx"
      to: "lib/i18n.ts:dictionaries.fr"
      via: "import { dictionaries }"
      pattern: "cohort_pulse_"
    - from: "lib/cohort-pulse.ts"
      to: "lib/seed/index.ts"
      via: "fallback demo mode when hasSupabaseEnv() === false"
      pattern: "seedPlayers"
---

<objective>
Implémenter B1 (T3-IMPROVEMENTS ligne 58) : Cohort Pulse Bar anonymisée sur la page /journey.

Affiche une barre fine par level (L0..L5) au format `X/N équipes ont soumis [level]` — progression collective non-comparative qui contourne R1 (pas de classement individuel) tout en signalant la dynamique de la cohorte.

Purpose:
- Convertir la solitude écran en motivation collective (FOMO positif)
- Strict respect R1 : aucun nom d'équipe, aucun score, aucun rang Player
- Cohérent avec la convention dual-mode du codebase (Supabase + demo seed)

Output:
- 1 helper agrégat (`lib/cohort-pulse.ts`)
- 1 composant server (`components/cohort-pulse.tsx`)
- Styles `.eic-cohort-pulse*` dans `app/globals.css`
- Clés FR dans `lib/i18n.ts`
- Mount dans `app/journey/page.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@T3-IMPROVEMENTS.md

@CLAUDE.md

# Existing files referenced (lecture seule par l'executor)
@lib/journey.ts
@lib/team-activity.ts
@lib/types.ts
@lib/i18n.ts
@lib/seed/index.ts
@lib/seed/players.ts
@app/journey/page.tsx
@components/journey-client.tsx
@database/schema.sql

<interfaces>
<!-- Contracts extraits du codebase. L'executor doit utiliser ces signatures. -->

From lib/types.ts:
```ts
export type LevelId =
  | "L0_diagnostic" | "L1_problem" | "L2_solution"
  | "L3_market" | "L4_business_model" | "L5_pitch"
  | "L6_traction" | "L7_alumni";

export type SubmissionStatus =
  | "draft" | "submitted_v1" | "feedback_received"
  | "submitted_v2" | "validated" | "rejected";

export type Player = {
  id: string;
  cohortId: string;
  slug: string;
  name: string;
  idea: string | null;
  currentLevel: LevelId;
  status: PlayerStatus;
  scoreProject: number;
  scoreEngagement: number;
  onboardedAt: string | null;
};
```

From lib/seed/index.ts:
```ts
export function seedPlayers(): Player[];   // [] when hasSupabaseEnv() === true
```

From lib/journey-progression.ts:
```ts
export const LEVEL_IDS: LevelId[];          // ordered L0..L7
export function getShortLevelLabel(levelId: LevelId): string;
```

From lib/supabase-status.ts:
```ts
export function hasSupabaseEnv(): boolean;
```

From utils/supabase/server.ts:
```ts
export async function createClient(): Promise<SupabaseClient | null>;
```

From database/schema.sql (relevant tables):
```sql
table public.players (id uuid, cohort_id uuid, current_level level_id, status player_status, ...)
table public.deliverable_templates (id uuid, mission_id uuid, ...)
table public.missions (id uuid, event_id uuid, level_id level_id, ord, kind, title, scheduled_at, ...)
table public.submissions (
  id uuid, player_id uuid, deliverable_template_id uuid,
  version smallint, kind, status submission_status,
  submitted_at timestamptz, ...
)
table public.cohorts (id uuid, event_id uuid, slug, name, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper agrégat anonyme `lib/cohort-pulse.ts` (dual-mode)</name>
  <files>lib/cohort-pulse.ts</files>
  <action>
    Créer un nouveau fichier `lib/cohort-pulse.ts` (NE PAS modifier `lib/journey.ts` ni `lib/team-activity.ts` — contraintes du brief).

    Exports requis :
    ```ts
    export type CohortPulseEntry = {
      levelId: LevelId;        // L0..L5 uniquement
      count: number;            // équipes distinctes ayant soumis ≥1 livrable de ce level (status ∈ submitted/feedback/validated)
      total: number;            // taille cohorte (cohorte du Player connecté en mode Supabase, total seed en demo)
    };

    export async function getCohortPulse(userId: string | null, now?: Date): Promise<CohortPulseEntry[]>;
    ```

    Convention R1 : la fonction NE retourne JAMAIS d'identifiants ou noms de Player. Le retour est strictement {levelId, count, total}.

    Levels visibles (pilote) :
    ```ts
    const PULSE_LEVELS: LevelId[] = [
      "L0_diagnostic", "L1_problem", "L2_solution",
      "L3_market",     "L4_business_model", "L5_pitch",
    ]; // L6/L7 exclus (post-event)
    ```

    **Mode demo** (`!hasSupabaseEnv()`) :
    - `total = seedPlayers().length` (2 dans le seed actuel — c'est OK, le composant doit aussi gérer total=0 → "0/0").
    - Pour chaque levelId dans PULSE_LEVELS :
      - `count = seedPlayers().filter(p => levelOrdOf(p.currentLevel) >= levelOrdOf(levelId)).length`
      - Heuristique : un Player au level X est considéré "a soumis" tous les levels strictement < X (donc count = nombre de Players qui ont passé ce level). Pour le level courant, on n'incrémente pas (pas encore soumis).
    - `levelOrdOf` : import `levelOrd` from `@/lib/journey` (déjà exporté ligne 96).

    **Mode Supabase** (`hasSupabaseEnv()` && supabase non null) :
    - Si `userId` null → fallback à `total=0, count=0` pour chaque level (pas crasher, pas de leak).
    - Étape 1 : récupérer `cohort_id` du Player connecté via `player_members` → `players.cohort_id` (mêmes 2 queries que `lib/journey.ts:getPlayerForUser` puis `players.select("cohort_id")`).
      - Si pas de cohort_id résolvable → renvoyer entries `{count:0, total:0}` (silencieux, pas de throw).
    - Étape 2 : `total = supabase.from("players").select("id", { count:"exact", head:true }).eq("cohort_id", cohortId).eq("status","active")` — récupérer `count` du résultat.
    - Étape 3 : agrégat per level. Approche en DEUX queries pour rester simple (pas de RPC) :
      ```
      const { data: tplRows } = await supabase
        .from("deliverable_templates")
        .select("id, mission_id, missions!inner(level_id, event_id)")
        .in("missions.level_id", PULSE_LEVELS);
      // construire Map<levelId, Set<templateId>>
      ```
      Puis :
      ```
      const { data: subRows } = await supabase
        .from("submissions")
        .select("player_id, deliverable_template_id, players!inner(cohort_id)")
        .eq("players.cohort_id", cohortId)
        .in("status", ["submitted_v1","feedback_received","submitted_v2","validated"]);
      ```
      - Filtrer par `players.cohort_id == cohortId` (déjà fait via inner join eq).
      - Pour chaque levelId : `count = unique(subRows.filter(r => templateIdsOfLevel.has(r.deliverable_template_id)).map(r => r.player_id)).size`.
      - SI les noms de tables `missions!inner` ou `players!inner` ne fonctionnent pas avec ce projet Supabase (à tester) : fallback en faisant 3 queries séparées (templates + submissions + players cohort filter en mémoire). Choisir la version qui passe `npm run typecheck && npm run build`.
    - Si Supabase retourne une erreur → swallow + return `entries vides`. NE PAS throw (pattern existant — voir `lib/journey.ts` qui retourne `EMPTY` silencieusement).

    INTERDICTIONS de la fonction (audit R1) :
    - JAMAIS retourner `player.name`, `player.slug`, `player.id`, `score_project`, `score_engagement`, `current_level` du Player connecté
    - JAMAIS retourner les rows individuelles de `submissions` ou `players`

    **Convention codebase à suivre** :
    - Plain-ASCII dans les commentaires (cf `lib/i18n.ts` qui évite les diacritiques)
    - Named exports uniquement (jamais default)
    - Commentaire d'en-tête "Quick 260510-k1f / B1 - Cohort Pulse aggregator (anonymised, dual-mode)" mentionnant R1
    - Path alias `@/lib/...` jamais relatif
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    `lib/cohort-pulse.ts` existe ; exports `getCohortPulse` + `CohortPulseEntry` ; `tsc --noEmit` passe ;
    grep `name|slug|score|rank|percentile` dans le fichier → seulement dans commentaires/audit, jamais dans le retour ;
    grep `select.*name` ou `select.*slug` sur submissions/players dans ce fichier → vide (sauf `players.cohort_id` ou `players.id` strictement nécessaires au filtrage).
  </done>
</task>

<task type="auto">
  <name>Task 2: Composant server `<CohortPulse>` + styles + i18n</name>
  <files>components/cohort-pulse.tsx, app/globals.css, lib/i18n.ts</files>
  <action>
    **2a. Clés i18n** dans `lib/i18n.ts` (ajouter dans le bloc `fr` ET le bloc `en`, conserver convention plain-ASCII et style v2 EIC) :
    ```
    cohort_pulse_aria: "Pouls de la cohorte",
    cohort_pulse_kicker: "POULS DE LA COHORTE",
    cohort_pulse_label_template: "equipes ont soumis",   // FR — completed by short level label, ex: "equipes ont soumis Solution"
    cohort_pulse_empty: "Pas encore de soumission dans la cohorte.",
    ```
    Variantes EN :
    ```
    cohort_pulse_aria: "Cohort pulse",
    cohort_pulse_kicker: "COHORT PULSE",
    cohort_pulse_label_template: "teams submitted",
    cohort_pulse_empty: "No submission in the cohort yet.",
    ```
    Pas de variante dramatique selon ratio bas/haut au pilote — copie sobre uniforme (cohérent avec la sobriété v2 EIC, pas Clippy). Si l'EIC pedagogical advisor est sollicité plus tard, élargir alors.

    **2b. Composant server** `components/cohort-pulse.tsx` :
    ```tsx
    // Quick 260510-k1f / B1 - Cohort Pulse Bar (anonymised, R1).
    // Server component - no useState/useEffect. Pure render of {levelId, count, total}[].
    // Designed by intent : never receives nor renders player names, scores or ranks.
    import { dictionaries } from "@/lib/i18n";
    import { getShortLevelLabel } from "@/lib/journey-progression";
    import type { CohortPulseEntry } from "@/lib/cohort-pulse";

    const t = dictionaries.fr;

    export type CohortPulseProps = {
      entries: CohortPulseEntry[];
    };

    export function CohortPulse({ entries }: CohortPulseProps) {
      // Garde anti-leak : si quelqu'un passe un total = 0 et que toutes
      // les counts sont nulles (mode demo Supabase vide ou cohorte vide),
      // afficher le message "empty" au lieu de barres a 0%.
      const total = entries[0]?.total ?? 0;
      const anyCount = entries.some((e) => e.count > 0);

      if (total === 0 || !anyCount) {
        return (
          <section className="eic-cohort-pulse" aria-label={t.cohort_pulse_aria}>
            <p className="eic-cohort-pulse__kicker">{t.cohort_pulse_kicker}</p>
            <p className="eic-cohort-pulse__empty">{t.cohort_pulse_empty}</p>
          </section>
        );
      }

      return (
        <section className="eic-cohort-pulse" aria-label={t.cohort_pulse_aria}>
          <p className="eic-cohort-pulse__kicker">{t.cohort_pulse_kicker}</p>
          <ul className="eic-cohort-pulse__list" role="list">
            {entries.map((e) => {
              const ratio = total > 0 ? Math.round((e.count / total) * 100) : 0;
              return (
                <li key={e.levelId} className="eic-cohort-pulse__row">
                  <span className="eic-cohort-pulse__label">
                    {`${t.cohort_pulse_label_template} ${getShortLevelLabel(e.levelId)}`}
                  </span>
                  <div
                    className="eic-cohort-pulse__bar"
                    role="progressbar"
                    aria-valuenow={e.count}
                    aria-valuemin={0}
                    aria-valuemax={e.total}
                    aria-label={`${getShortLevelLabel(e.levelId)} : ${e.count} / ${e.total}`}
                  >
                    <div
                      className="eic-cohort-pulse__bar-fill"
                      style={{ width: `${ratio}%` }}
                    />
                  </div>
                  <span className="eic-cohort-pulse__count">
                    {e.count}/{e.total}
                  </span>
                </li>
              );
            })}
          </ul>
        </section>
      );
    }
    ```

    INTERDICTIONS du composant (audit R1) :
    - JAMAIS importer `lib/seed/players` directement (le helper s'en charge)
    - JAMAIS rendre `player.name`, `player.slug`, `score`, `rank`
    - JAMAIS de tooltip "vous etes plus avance que X" ni "votre position"
    - JAMAIS de tri "leader/laggard" — l'ordre est l'ordre fixe L0->L5 (cohérent avec `LEVEL_IDS` mais limité au sous-ensemble pilote)

    **2c. Styles** dans `app/globals.css`, AJOUTER en fin de fichier (~30 lignes), aucune modification d'utilities existantes :
    ```css
    /* === Cohort Pulse (Quick 260510-k1f / B1) ============================= */
    /* Barre fine, anonyme, en haut de /journey. R1 : aucun signal individuel. */
    .eic-cohort-pulse {
      max-width: 720px;
      margin: 0 auto 20px auto;
      padding: 12px 16px;
      border-radius: 12px;
      background: var(--eic-surface, rgba(255, 255, 255, 0.6));
      border: 1px solid var(--eic-border, rgba(0, 0, 0, 0.08));
    }
    .eic-cohort-pulse__kicker {
      font-size: 11px;
      letter-spacing: 0.12em;
      color: var(--eic-muted, #6b7280);
      margin: 0 0 8px 0;
    }
    .eic-cohort-pulse__list {
      display: flex;
      flex-direction: column;
      gap: 6px;
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .eic-cohort-pulse__row {
      display: grid;
      grid-template-columns: 1fr 100px 56px;
      align-items: center;
      gap: 12px;
      font-size: 13px;
    }
    @media (max-width: 540px) {
      .eic-cohort-pulse__row {
        grid-template-columns: 1fr 60px 48px;
        font-size: 12px;
        gap: 8px;
      }
    }
    .eic-cohort-pulse__label { color: var(--eic-fg, #1f2937); }
    .eic-cohort-pulse__bar {
      position: relative;
      height: 6px;
      border-radius: 3px;
      background: var(--eic-track, rgba(0, 0, 0, 0.08));
      overflow: hidden;
    }
    .eic-cohort-pulse__bar-fill {
      height: 100%;
      background: var(--eic-blue, #1e6eef);
      transition: width 320ms ease-out;
    }
    @media (prefers-reduced-motion: reduce) {
      .eic-cohort-pulse__bar-fill { transition: none; }
    }
    .eic-cohort-pulse__count {
      text-align: right;
      font-variant-numeric: tabular-nums;
      color: var(--eic-muted, #6b7280);
    }
    .eic-cohort-pulse__empty {
      font-size: 13px;
      color: var(--eic-muted, #6b7280);
      margin: 0;
    }
    ```
    NB : si les variables CSS `--eic-blue`, `--eic-surface`, `--eic-border`, `--eic-muted`, `--eic-track`, `--eic-fg` n'existent pas dans le design system, les fallbacks inline (`rgba(...)`, `#1e6eef`, etc.) prennent le relais. Vérifier avant d'inventer de nouveaux tokens.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    `components/cohort-pulse.tsx` existe en server component (pas de "use client") ;
    clés i18n FR + EN ajoutées (4 chacune) ;
    block CSS `.eic-cohort-pulse*` ajouté en fin de `app/globals.css` avec guard `prefers-reduced-motion` ;
    `tsc --noEmit` + `eslint` passent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount dans `/journey` + audit R1 final + build</name>
  <files>app/journey/page.tsx</files>
  <action>
    Modifier `app/journey/page.tsx` (page server component existante) :

    1. Ajouter l'import :
       ```ts
       import { CohortPulse } from "@/components/cohort-pulse";
       import { getCohortPulse } from "@/lib/cohort-pulse";
       ```

    2. Dans le corps de `JourneyPage()`, après `const data = await getJourneyData(user.id);` et AVANT le `if (data.empty || !data.player)` :
       ```ts
       // Quick 260510-k1f / B1 - cohort pulse (anonymised, R1).
       const cohortPulse = await getCohortPulse(user.id);
       ```
       NB : on calcule cohortPulse même quand `data.empty` est vrai — un Player non-onboarde voit quand-meme la dynamique cohorte (utile pour la motivation onboarding). Si le helper retourne `[]` (mode demo Supabase vide), le composant affiche son etat empty.

    3. Dans la branche `if (data.empty || !data.player)` (early return), AJOUTER `<CohortPulse entries={cohortPulse} />` au-dessus du titre. Format :
       ```tsx
       return (
         <AppShell role="player" variant="player">
           <main className="eic-journey">
             <div aria-hidden="true" className="eic-journey__bg" />
             <div className="eic-journey__main">
               <CohortPulse entries={cohortPulse} />
               <div style={{ padding: 32, maxWidth: 720, margin: "0 auto" }}>
                 ...
               </div>
             </div>
           </main>
         </AppShell>
       );
       ```

    4. Dans la branche normale (return final), AJOUTER `<CohortPulse entries={cohortPulse} />` IMMÉDIATEMENT après `<PlayerAnnouncementStrip />` et AVANT `<JourneyClient ...>` :
       ```tsx
       return (
         <AppShell role="player" variant="player">
           <PlayerAnnouncementStrip announcements={announcements} />
           <CohortPulse entries={cohortPulse} />
           <JourneyClient
             ...
           />
         </AppShell>
       );
       ```

    5. **Audit R1 final** (à exécuter manuellement par l'executor avant commit) :
       Lancer ces 3 greps et coller la sortie dans le SUMMARY :
       ```
       grep -nE "name|slug|score|rank|percentile" lib/cohort-pulse.ts
       grep -nE "name|slug|score|rank|percentile" components/cohort-pulse.tsx
       grep -nE "Player|player\." components/cohort-pulse.tsx
       ```
       - Le premier ne doit retourner que :
         - les commentaires de garde R1 ("anonymised", "no name/score/rank...")
         - le filtrage `cohort_id` ou la sélection `id` strictement utilisée pour count/distinct
         - PAS de `select(... name ...)` ni `select(... slug ...)` ni `score_project`
       - Le second ne doit retourner que les commentaires de garde (le composant n'a aucune raison de manipuler ces tokens).
       - Le troisième : aucune référence à `Player` ou `player.` côté props/render (le type `CohortPulseEntry` ne contient pas de Player).

    6. Lancer le build complet :
       ```
       npm run typecheck && npm run lint && npm run build
       ```
       Si build échoue à cause d'une variante `missions!inner` ou `players!inner` non supportée : revenir à Task 1 et appliquer la stratégie 3-queries-séparées (templates + submissions + cohort filter en mémoire).

    NE PAS modifier :
    - `lib/journey.ts` (interdiction explicite du brief)
    - `lib/journey-progression.ts` (interdiction explicite du brief)
    - `database/` (interdiction explicite du brief)
    - `components/journey-client.tsx` (le composant client existant ne touche pas au pulse — celui-ci est server)
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
    `app/journey/page.tsx` modifié : import + fetch `getCohortPulse(user.id)` + mount `<CohortPulse>` dans les deux branches (empty + normal) ;
    `npm run build` passe sans erreur ;
    audit grep R1 vide (aucune fuite name/slug/score/rank dans `lib/cohort-pulse.ts` ni `components/cohort-pulse.tsx`) ;
    Player connecté en mode demo voit la barre avec les 6 lignes L0..L5 ; en mode Supabase prod, voit le ratio réel.
  </done>
</task>

</tasks>

<verification>
**Audit R1 (CRITIQUE — non négociable) :**
- `lib/cohort-pulse.ts` ne SELECT jamais `players.name`, `players.slug`, `players.score_*`, `players.idea`. Seuls `players.id` (count) et `players.cohort_id` (filter) sont autorisés.
- `components/cohort-pulse.tsx` ne reçoit qu'un type `CohortPulseEntry[]` strictement `{levelId, count, total}`. Aucun champ Player en props.
- L'audit grep listé en Task 3.5 doit ressortir vide (sauf garde-fous en commentaires).

**Pattern dual-mode :**
- Mode demo (`!hasSupabaseEnv()`) : helper retourne `entries` calculées sur `seedPlayers()` — déterministe, plausible.
- Mode Supabase : helper retourne `entries` agrégées sur `submissions` filtrée par cohorte — anonymisé serveur-side.

**Build & lint :**
- `npm run typecheck && npm run lint && npm run build` doit passer après Task 3.

**Cohérence i18n :**
- 4 clés ajoutées dans `fr` ET `en` (`cohort_pulse_aria`, `cohort_pulse_kicker`, `cohort_pulse_label_template`, `cohort_pulse_empty`). Pas de string FR inline dans le composant.

**Reduced-motion :**
- `.eic-cohort-pulse__bar-fill { transition: width 320ms }` est neutralisé sous `@media (prefers-reduced-motion: reduce)`.

**Anti-régression :**
- `/login`, `/mentor`, `/admin`, `/jury`, `/results` non touchés (les 3 fichiers modifiés sont `app/journey/page.tsx`, `app/globals.css` ajout en fin, `lib/i18n.ts` ajout au dictionnaire).
- `<JourneyClient>` non modifié — le pulse est mounté en server au-dessus.
</verification>

<success_criteria>
- [ ] Le Player connecté voit `<CohortPulse>` en haut de `/journey` (au-dessus du grid 3-col)
- [ ] La barre montre 6 lignes : L0 Diagnostic, L1 Probleme, L2 Solution, L3 Marche, L4 Modele eco., L5 Pitch
- [ ] Chaque ligne expose UNIQUEMENT `{label level} | bar fine | X/N`
- [ ] Aucun nom d'équipe, aucun score, aucun rang individuel exposé (audit R1 grep vide)
- [ ] Mode demo (sans env Supabase) : agrégat plausible issu de `seedPlayers()` (level 1 visible pour Alpha au L1, Beta au L0)
- [ ] Mode Supabase : agrégat réel cohorte du Player connecté
- [ ] `npm run typecheck && npm run lint && npm run build` passent
- [ ] `prefers-reduced-motion` désactive la transition CSS
- [ ] Aucun fichier dans la liste INTERDITE n'est modifié (`lib/journey.ts`, `lib/journey-progression.ts`, `database/`, `components/journey-client.tsx`)
- [ ] Aucune nouvelle dépendance npm
</success_criteria>

<output>
After completion, create `.planning/quick/260510-k1f-b1-cohort-pulse-bar-anonymisee-t3-improv/260510-k1f-SUMMARY.md` with :
- Files created (2) + files modified (3) + LOC delta
- Audit R1 grep output (proof of anonymisation)
- Mode demo : capture du ratio rendu (text)
- Build status (typecheck / lint / build PASS)
- Commit ref
</output>
