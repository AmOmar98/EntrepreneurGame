# Deferred items — Quick 260511-l3p

Aucun item différé. Tout le scope demandé par Omar (split-screen 3 rôles, retrait SSO + "Oublié?", nouvelles citations) a été implémenté dans la même session.

## Idées explicitement hors-scope (non promises)

- Page `/login/forgot` pour reset password — non demandée, reset reste manuel via GameMaster.
- SSO UEMF — retiré explicitement par Omar.
- Visual review responsive multi-viewport (mobile/tablet) — vérifié par CSS media query mais pas smoke browser réel.
- Sync visuel pixel-perfect avec mockups DevTools — les mockups sont des dumps style-inlined, on s'est aligné sur l'intention structurelle (couleurs/typo/layout) pas sur les valeurs exactes de padding/shadow.
