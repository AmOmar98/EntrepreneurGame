---
phase: "quick-260510-l3a"
plan: "260510-l3a"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md"
  - ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md"
  - ".planning/ROADMAP.md"
autonomous: true
requirements: ["PATCH-PHASE-10-POST-QUICKS-260510"]
must_haves:
  truths:
    - "Sub-tâche 10.0.2 (B1 percée R1 /results) marquée ✅ DONE avec ref commits c740d48 + 16aa0f7 + ref quick 260510-kpw"
    - "Sub-tâche 10.0.7 (R3 tooltip ambre journey) marquée ✅ DONE avec ref commits 8f46892 + 25f830e + 4733406 + d49ad1b + ref quick 260510-j2j"
    - "Path `eic-tokens.css` retiré du frontmatter `files_modified` de 10-01-PLAN.md (le fichier n'existe pas, tokens vivent dans `app/globals.css`)"
    - "Sub-tâche 10.1.1 (mood vars Pixel) ré-ancrée dans `app/globals.css` (section :root ou nouveau bloc /* ---- Mascot moods ---- */)"
    - "Sub-tâche 10.1.3 (Pixel loading/error extension) note explicitement que A5 a déjà livré PixelMascotPlayer + use-pixel-trigger ; Phase 1.3 = extension moods uniquement (pas de doublon)"
    - "10-CONTEXT.md `<scope>` block contient nouvelle sous-section `## Hors scope (traité par /gsd-quick séparé)` listant B5 + C1 + C2 + C4 (ref T3-IMPROVEMENTS.md sections B et C)"
    - "10-01-PLAN.md section `## Hors scope (explicitement)` ajoute la même ligne renvoyant vers T3-IMPROVEMENTS.md B5/C1/C2/C4"
    - "Sub-tâche 10.0.3 (régénération 7 missions AgreenTech) note explicitement absorption A2 + B3 + B4 (data-side) avec refs T3-IMPROVEMENTS.md L1/L2.1/L4/L6 lignes 99-251"
    - "Sub-tâche 10.0.3 précise que la part UI B4 (`Slide4Editor` bouton 'Citer ce verbatim') reste à traiter en sous-task séparé ou plus tard"
    - "Nouvelle sub-tâche 10.0.10 — C3 ordre randomisé pitch + ancres milieu — ajoutée dans Phase 0 de 10-01-PLAN.md"
    - "ROADMAP.md Phase 10 status enrichi d'une ligne 'Patch 2026-05-10 (quick 260510-l3a)' résumant les 4 ajustements"
    - "ROADMAP.md liste 'Plans:' garde 10-01-PLAN.md mais ajoute une mention de patch 2026-05-10 → quick 260510-l3a"
  artifacts:
    - path: ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md"
      provides: "Updated CONTEXT with hors-scope sub-section + scope adjustments"
    - path: ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md"
      provides: "Patched plan: 10.0.2/10.0.7 DONE, paths corrected, A2/B3/B4 absorbed in 10.0.3, new 10.0.10, hors-scope updated, frontmatter files_modified cleaned"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 10 status note + plans line annotated with patch reference"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 10 section)"
      to: ".planning/quick/260510-l3a-patch-phase-10-plan-roadmap-post-quick-s/"
      via: "patch reference line under Phase 10 status"
      pattern: "260510-l3a"
    - from: ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md (10.0.3)"
      to: "T3-IMPROVEMENTS.md sections L1/L2.1/L4/L6 + A2/B3/B4 rows"
      via: "absorption note with line refs"
      pattern: "T3-IMPROVEMENTS\\.md"
    - from: ".planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md (<scope>)"
      to: "T3-IMPROVEMENTS.md sections B (B5) + C (C1/C2/C4)"
      via: "hors-scope sub-section"
      pattern: "Hors scope \\(traité par /gsd-quick séparé\\)"
---

<objective>
Patch documentaire de la planification Phase 10 (T-3 Critical Gates + Design v2 Tail Sections) pour refléter l'état réel du repo après les sessions `/gsd-quick` du 10/05/2026 (260510-heu, 260510-hzv, 260510-iee, 260510-j2j, 260510-jm8, 260510-k1f, 260510-kpw).

**Purpose** : éviter de relancer du travail déjà fait (10.0.2 + 10.0.7), corriger un path inexistant (`eic-tokens.css`), absorber les T3-IMPROVEMENTS data-side (A2/B3/B4) dans la régénération seed AgreenTech (10.0.3), ajouter une sous-tâche manquante (C3 ordre randomisé pitch = 10.0.10), et déclarer hors scope pilote AgreenTech les items lourds (B5/C1/C2/C4) qui partiront en quick séparés.

**Output** : 3 fichiers .md patchés (10-CONTEXT.md, 10-01-PLAN.md, ROADMAP.md). **Aucun fichier source touché.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md
@.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md
@T3-IMPROVEMENTS.md

<interfaces>
<!-- État réel du repo confirmé par grep / Read avant édition. -->

**Path constat (à corriger dans 10-01-PLAN.md frontmatter)** :
- `eic-tokens.css` : NON-EXISTANT comme fichier séparé. Tous les tokens (`--eic-blue`, `--gradient-border`, `--brand-*`, etc.) vivent dans `app/globals.css` (vu via `Grep --eic-blue|--gradient-border` → 9 hits dans app/globals.css uniquement).
- `app/globals.css` est ~1300 lignes avec sections nommées (`/* EIC brand tokens */`, `/* Legacy tokens */`, etc.). Les mood vars Pixel doivent y être ajoutées comme nouvelle section `/* ---- Mascot moods ---- */` sous le bloc `:root`.

**Quick 260510-jm8 (A5) a déjà livré** :
- `components/pixel-mascot-player.tsx` (NEW, wrapper Player avec triggers)
- `hooks/use-pixel-trigger.ts` (NEW, 3 triggers événementiels A5)
- Extension de `components/pixel-mascot.tsx` avec export `PixelAvatar`
→ Phase 10.1.3 NE DOIT PAS recréer ces fichiers ; uniquement étendre `pixel-mascot.tsx` avec moods `loading` + `error` (pour Phase 2 SysLoading/SysError).

**Quick 260510-kpw (B1 rétro) a déjà livré 10.0.2** :
- Commit `c740d48` : gate `isGameMaster` sur podium scores + ranking table
- Commit `16aa0f7` : annonce qualitative Players + EIC-validated FR copy /results
→ 10.0.2 = ✅ DONE.

**Quick 260510-j2j (B2 banner L3) a déjà livré 10.0.7** :
- Commits `8f46892` (key i18n + CSS) + `25f830e` (tooltip wired + remove disabled) + `4733406` (HoveredHint amber warn-only) + `d49ad1b` (R2/R3 docs)
→ 10.0.7 = ✅ DONE.

**T3-IMPROVEMENTS.md content map (pour absorption 10.0.3)** :
- A2 (validators warn-only L1 mots noirs + ROI cohérence L4↔L2.1) → spec dans T3-IMPROVEMENTS.md L1.validation_rules lignes 116-118 + L4.validation_rules ligne 213-214
- B3 (extra_field `hypothese_invalider` + `hypothese_revisee` après L2.2) → spec dans T3-IMPROVEMENTS.md L1.extra_fields lignes 111-114
- B4 data-side (helper `cite_from_M2.2` slide 4 L6) → spec dans T3-IMPROVEMENTS.md L6.fields ligne 244 (`ui_helper: "cite_from_M2.2"`)
- B4 UI-side (`Slide4Editor` bouton « Citer ce verbatim ») → composant React PAS dans 10.0.3 — à reporter en sous-task séparé ou Phase 10 ultérieure

**T3-IMPROVEMENTS.md hors scope (pour <scope> block)** :
- B5 (Excellence/Trajectoire 2 classements internes) → T3-IMPROVEMENTS.md ligne 62
- C1 (notation différée jury) → T3-IMPROVEMENTS.md ligne 68
- C2 (décomposition 5 critères × /20 + σ jury) → T3-IMPROVEMENTS.md ligne 69
- C4 (lettre retour PDF signée) → T3-IMPROVEMENTS.md ligne 71 + section G (template lignes 257-280)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch 10-01-PLAN.md (frontmatter + Phase 0 sub-tasks + Hors scope section)</name>
  <files>.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md</files>
  <action>
Edit `10-01-PLAN.md` with the following surgical changes (use `Edit` tool, ne PAS réécrire tout le fichier) :

**A. Frontmatter `files_modified` (around line 39)**
- REMOVE line `  - "eic-tokens.css"` entirely.
- ADD line `  - "hooks/use-pixel-trigger.ts"` AND `  - "components/pixel-mascot-player.tsx"` under the existing `components/pixel-mascot.tsx` entry (Phase 1.3 may need to touch them if mood extension propagates).

**B. Phase 0 sub-tasks (lines ~99-123)**

For **0.2 — B1 · Percée R1 sur `/results`** (lines 104-106) : append a new line at the end of the bullet block :
```
  - **Status: ✅ DONE** par quick session [`260510-kpw`](../../quick/260510-kpw-b1retro-r1-leak-results-gate-isgamemaste/) (commits `c740d48` gate isGameMaster sur podium + ranking table, `16aa0f7` FR copy qualitative validée EIC). Ne pas re-traiter.
```

For **0.3 — B4 · 7 missions AgreenTech dans le seed** (lines 107-110) : insert AFTER the existing 3 bullets (before 0.4) :
```
  - **Absorbe T3-IMPROVEMENTS.md** (à appliquer dans le même seed `database/seed_event_hackdays.sql` + miroir in-memory `lib/seed/missions.ts`) :
    - **A2** : `validation_rules` warn-only sur L1 (mots noirs `agriculteurs`/`fermiers`/`exploitants`) + cohérence ROI L4↔L2.1 (cf. `T3-IMPROVEMENTS.md` L1 lignes 116-118 + L4 ligne 213-214). TOUS en `severity: "warn"`.
    - **B3** : extra_fields L1 `hypothese_invalider` (apparaît à `M1.1_open`) + `hypothese_revisee` (apparaît à `M2.2_submitted`) — cf. `T3-IMPROVEMENTS.md` L1 lignes 111-114.
    - **B4 data-side** : helper `ui_helper: "cite_from_M2.2"` sur slide4_preuve L6 (cf. `T3-IMPROVEMENTS.md` L6 ligne 244). **Note** : la sous-tâche UI B4 (`Slide4Editor` bouton « Citer ce verbatim ») reste à faire dans une sous-task séparée ou en Phase 10 ultérieure — pas dans 10.0.3 qui couvre la couche data uniquement.
```

For **0.7 — R3 tooltip ambre journey** (lines 119-120) : append at the end of the bullet block :
```
  - **Status: ✅ DONE** par quick session [`260510-j2j`](../../quick/260510-j2j-b2-retirer-banner-rouge-l3-et-remplacer-/) (commits `8f46892` + `25f830e` + `4733406` + `d49ad1b` — R2/R3 conformes). Ne pas re-traiter.
```

**C. NEW sub-tâche 0.10** : insert AFTER 0.9 (line ~123, before "Critical files :" line) :
```
- **0.10 — C3 · Ordre randomisé pitch + équipes ancres milieu** (couplé 0.3, même seed)
  - Modifier `database/seed_event_hackdays.sql` (et miroir in-memory `lib/seed/event.ts` si présent) pour figer un `pitch_order` JSON dans la table event ou la config seed.
  - Spec : ordre tiré au sort sur les 11 équipes AgreenTech, **équipes "ancres" (top présélection) placées en milieu** (slot 6 idéalement), JAMAIS slot 1 ni slot 11. Annoncé aux équipes : "ordre tiré au sort".
  - Ref `T3-IMPROVEMENTS.md` ligne 70 (section C3).
  - **Spawn `eic-pedagogical-advisor`** avant édition seed event (zone sensible, cf. CONTEXT.md `<decisions>`).
```

**D. Phase 1 sub-tasks (lines ~131-133)**

For **1.1 — Variables mood Pixel** (line 131) : edit the file path reference :
- Replace `dans `eic-tokens.css` (sous `--gradient-border`)` with `dans `app/globals.css` (nouveau bloc `/* ---- Mascot moods ---- */` sous le bloc `:root` existant — `eic-tokens.css` n'existe pas comme fichier séparé, tous les tokens vivent dans `globals.css`)`.

For **1.3 — Variantes Pixel `loading` + `error`** (line 133) : append a note clarifying scope :
```
  - **Note** : quick session `260510-jm8` (A5) a déjà livré `components/pixel-mascot-player.tsx` (NEW wrapper Player) + `hooks/use-pixel-trigger.ts` (NEW 3 triggers événementiels) + extension de `pixel-mascot.tsx` avec export `PixelAvatar`. **Phase 1.3 = extension moods `loading`/`error` uniquement sur `pixel-mascot.tsx` — ne pas recréer `pixel-mascot-player.tsx` ni `use-pixel-trigger.ts`.**
```

**E. Phase 1 Critical files line (line 135)** :
- Replace `Critical files : `eic-tokens.css`, `app/globals.css`, `components/pixel-mascot.tsx`.` with `Critical files : `app/globals.css` (mood vars + `.eic-toast` class), `components/pixel-mascot.tsx` (extension moods `loading`/`error`). **Note** : `pixel-mascot-player.tsx` + `use-pixel-trigger.ts` déjà livrés par quick `260510-jm8` (A5).`

**F. Section "Hors scope (explicitement)" (lines 229-236)** : ADD a new bullet at the end :
```
- **B5 (Excellence/Trajectoire scoring), C1 (notation différée jury), C2 (décomposition 5×/20 + σ jury), C4 (lettre retour PDF signée)** de `T3-IMPROVEMENTS.md` (sections B + C) sont **hors scope Phase 10** — partiront en `/gsd-quick` séparés post-pilote ou en V2 dédiée. Ref T3-IMPROVEMENTS.md lignes 62 (B5), 68 (C1), 69 (C2), 71 + section G lignes 257-280 (C4).
```

**G. Top metadata block (right after the title `# Plan 10-01 — T-3 Critical Gates...`, line ~68)** : ADD a new italicized note line :
```
> **Patch 2026-05-10** (quick session `260510-l3a`) : 0.2 + 0.7 marqués ✅ DONE post-quicks `260510-kpw` + `260510-j2j` ; paths corrigés (`eic-tokens.css` → `app/globals.css`) ; 0.3 absorbe A2/B3/B4 data-side ; 0.10 (C3 ordre randomisé pitch) ajouté ; B5/C1/C2/C4 déclarés hors scope (refs T3-IMPROVEMENTS.md sections B+C).
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md' -Pattern '260510-kpw|260510-j2j|0.10 — C3|hooks/use-pixel-trigger|app/globals.css. \\(nouveau bloc|Patch 2026-05-10' | Measure-Object -Line | Select-Object -ExpandProperty Lines"</automated>
    <!-- Expected: ≥6 distinct match lines (one per anchor pattern). -->
    <manual>Open the file, scroll to frontmatter (no `eic-tokens.css`), Phase 0 (0.2 + 0.7 DONE annotated, 0.3 absorption block, 0.10 inserted), Phase 1 (1.1 path corrected, 1.3 A5 note, Critical files updated), Hors scope (B5/C1/C2/C4 line). Visual diff readable.</manual>
  </verify>
  <done>
    - Frontmatter `files_modified` no longer contains `eic-tokens.css` ; contains `hooks/use-pixel-trigger.ts` + `components/pixel-mascot-player.tsx`.
    - 0.2 and 0.7 each carry a "Status: ✅ DONE" line with commit + quick refs.
    - 0.3 has an "Absorbe T3-IMPROVEMENTS.md" sub-block (A2/B3/B4 with line refs, plus B4 UI-side caveat).
    - 0.10 sub-task exists with C3 spec + advisor mention.
    - 1.1 path now points to `app/globals.css`, not `eic-tokens.css`.
    - 1.3 has explicit A5 reuse note.
    - Phase 1 Critical files line updated.
    - Hors scope section gets a B5/C1/C2/C4 bullet.
    - Top metadata gets a "Patch 2026-05-10" italic line.
    - File still parses as Markdown (frontmatter intact, no broken bullets).
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch 10-CONTEXT.md (<scope> block hors-scope sub-section)</name>
  <files>.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md</files>
  <action>
Edit `10-CONTEXT.md` with the following targeted change (use `Edit` tool) :

**A. `<scope>` block (lines 70-84)** : INSERT a new sub-section AFTER the line `Détail complet : voir `10-01-PLAN.md`.` (line 82) and BEFORE the closing `</scope>` (line 84) :

```
## Hors scope (traité par /gsd-quick séparé ou v0.3)

Confirmé patch 2026-05-10 (quick session `260510-l3a`) — éléments T3-IMPROVEMENTS.md NE seront PAS traités dans Phase 10 :

- **B5** — 2 classements internes Excellence + Trajectoire (formule `Score_traj = Score_final - Score_présélection × 5`, filtre Trajectoire = équipes Idée + Premier Prototype) → ref `T3-IMPROVEMENTS.md` ligne 62. Quick séparé post-pilote.
- **C1** — Notation jury différée + révisable (brouillons pendant pitch, réajustement ±10pts post-session) → ref `T3-IMPROVEMENTS.md` ligne 68. Quick séparé OU v0.3.
- **C2** — Décomposition obligatoire 5 critères × /20 + affichage σ jury en temps réel → ref `T3-IMPROVEMENTS.md` ligne 69. Quick séparé OU v0.3.
- **C4** — Lettre retour personnalisée signée jury (template 3 champs `[force]/[risque]/[next step]` + export PDF) → ref `T3-IMPROVEMENTS.md` ligne 71 + section G template lignes 257-280. Quick séparé post-pilote (canal officiel feedback chiffré, peut être manuel au pilote).

**Décision** : ces 4 items sont reportés pour préserver la priorité absolue Phase 10 (B1-B4 + 5 sections design absentes). Ils n'impactent pas le go-live AgreenTech 13/05.

**Sous-tâche AJOUTÉE en Phase 0 (post-patch)** : `0.10 — C3 ordre randomisé pitch + équipes ancres milieu` (cf. `10-01-PLAN.md` Phase 0). Couplée à `0.3` (même seed event).
```

**B. Top metadata (after line 6, before `<domain>`)** : ADD a patch annotation line right after `**Source bundle design**` line :
```
**Patch 2026-05-10** (quick session [`260510-l3a`](../../quick/260510-l3a-patch-phase-10-plan-roadmap-post-quick-s/)) : 0.2 + 0.7 marqués ✅ DONE post-quicks `260510-kpw` + `260510-j2j` ; 0.3 absorbe A2/B3/B4 data-side T3-IMPROVEMENTS.md ; ajout 0.10 (C3 ordre randomisé pitch) ; déclaration hors scope B5/C1/C2/C4 (cf. bloc `<scope>` ci-dessous).
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md' -Pattern 'Hors scope \\(traité par /gsd-quick séparé|260510-l3a|0\\.10 — C3' | Measure-Object -Line | Select-Object -ExpandProperty Lines"</automated>
    <!-- Expected: ≥3 match lines (one per anchor pattern). -->
    <manual>File renders correctly as Markdown ; `<scope>` and `<domain>` tags still balanced ; new sub-section visible inside `<scope>` block.</manual>
  </verify>
  <done>
    - `<scope>` block contains a `## Hors scope (traité par /gsd-quick séparé ou v0.3)` sub-section listing B5/C1/C2/C4 with T3-IMPROVEMENTS.md line refs.
    - Top metadata has a "Patch 2026-05-10" line linking back to the quick directory.
    - YAML frontmatter intact ; XML-like tags `<scope>`, `<domain>`, `<decisions>`, `<dependencies>` all still balanced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch ROADMAP.md (Phase 10 status + Plans line)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit `.planning/ROADMAP.md` Phase 10 section (lines 290-321) with two surgical patches (use `Edit` tool) :

**A. "Plans:" list (line 317)** : the existing line is :
```
- [ ] 10-01-PLAN.md — T-3 Critical Gates + Design v2 Tail Sections (8 sous-phases : T-3 Gates → Design system → Section 13 → 11 → 10 → 12 → 14 → Smoke E2E)
```

REPLACE with :
```
- [ ] 10-01-PLAN.md — T-3 Critical Gates + Design v2 Tail Sections (8 sous-phases : T-3 Gates → Design system → Section 13 → 11 → 10 → 12 → 14 → Smoke E2E) — *patched 2026-05-10 via quick `260510-l3a`*
```

**B. "Phase 10 status" line (line 319)** : the existing line is :
```
**Phase 10 status** : 📥 Imported via `/gsd-import --from C:\Users\omara\.claude\plans\glimmering-sauteeing-wilkinson.md` (2026-05-10). En attente exécution. Spawn `eic-pedagogical-advisor` AVANT toute édition des zones sensibles (cf. CONTEXT.md `<decisions>`).
```

APPEND on a new paragraph (blank line then) :
```

**Patch 2026-05-10** (quick [`260510-l3a`](./quick/260510-l3a-patch-phase-10-plan-roadmap-post-quick-s/)) :
- Sub-tâches `10.0.2` (B1 percée R1 /results) et `10.0.7` (R3 tooltip ambre journey) marquées ✅ **DONE** post-quicks `260510-kpw` (commits `c740d48` + `16aa0f7`) et `260510-j2j` (commits `8f46892` + `25f830e` + `4733406` + `d49ad1b`).
- Path `eic-tokens.css` corrigé en `app/globals.css` (le fichier séparé n'existe pas — tokens dans `globals.css`).
- Phase 1.3 (Pixel `loading`/`error`) précisée : A5 a déjà livré `pixel-mascot-player.tsx` + `use-pixel-trigger.ts` via quick `260510-jm8`. Phase 10 = extension moods uniquement.
- Sub-tâche `10.0.3` (régénération 7 missions AgreenTech) absorbe A2/B3/B4 data-side de `T3-IMPROVEMENTS.md` (validators warn-only mots noirs L1 + ROI cohérence L4↔L2.1, extra_fields `hypothese_invalider`/`hypothese_revisee`, helper `cite_from_M2.2` slide 4 L6). UI side B4 (`Slide4Editor`) reportée en sous-task séparée.
- **Nouvelle sub-tâche `10.0.10`** : C3 ordre randomisé pitch + équipes ancres milieu (couplé 10.0.3, même seed event).
- **Hors scope Phase 10** : B5 (Excellence/Trajectoire scoring), C1 (notation différée jury), C2 (décomposition 5×/20 + σ), C4 (lettre retour PDF signée) — partent en `/gsd-quick` séparés ou v0.3.
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/ROADMAP.md' -Pattern 'patched 2026-05-10 via quick .260510-l3a|Patch 2026-05-10. \\(quick|10\\.0\\.10|Hors scope Phase 10' | Measure-Object -Line | Select-Object -ExpandProperty Lines"</automated>
    <!-- Expected: ≥4 match lines. -->
    <manual>ROADMAP.md Phase 10 section renders cleanly as Markdown ; the patch paragraph sits BEFORE the next horizontal rule `---` separating Phase 10 from Phases SHOULD ; `Plans:` line annotated ; existing Phase 10 status line preserved (NOT replaced).</manual>
  </verify>
  <done>
    - `Plans:` line for 10-01-PLAN.md gets an italic patch annotation.
    - A new "Patch 2026-05-10" paragraph follows the existing "Phase 10 status" line, listing the 5 changes (DONE 0.2/0.7, path fix, Phase 1.3 clarification, 0.3 absorption, 0.10 add, hors scope).
    - Existing import note preserved (we APPEND, do not REPLACE).
    - Markdown renders cleanly ; section break `---` between Phase 10 and "Phases SHOULD" still in place.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks executed, verify globally :

1. **Cross-file consistency** : the 3 patched files agree on
   - Same patch date `2026-05-10`
   - Same quick session ID `260510-l3a`
   - Same statement that 10.0.2 + 10.0.7 are DONE
   - Same hors-scope list (B5 / C1 / C2 / C4)
   - Same new sub-task ID `10.0.10`

2. **No source code touched** : `git status --short` should show only 3 files modified, all under `.planning/` :
   ```
   M .planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-CONTEXT.md
   M .planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md
   M .planning/ROADMAP.md
   ```
   (Plus the new quick directory `.planning/quick/260510-l3a-.../260510-l3a-PLAN.md` itself — that's the plan file, expected.)

3. **Markdown integrity** : open each file in editor, frontmatter (---...---) parses cleanly, XML-like blocks (`<scope>`, `<domain>`, `<decisions>`, `<dependencies>`) still balanced in `10-CONTEXT.md`, no broken bullet indentation in `10-01-PLAN.md`.

4. **Reachability check** : the patch paragraph in ROADMAP.md links to `./quick/260510-l3a-patch-phase-10-plan-roadmap-post-quick-s/` — verify the quick directory actually exists at this path.
</verification>

<success_criteria>
1. `10-01-PLAN.md` frontmatter `files_modified` no longer lists `eic-tokens.css` and adds `hooks/use-pixel-trigger.ts` + `components/pixel-mascot-player.tsx`.
2. `10-01-PLAN.md` Phase 0 section visibly shows ✅ DONE on 0.2 (with `260510-kpw` + commits c740d48 + 16aa0f7) and 0.7 (with `260510-j2j` + commits 8f46892 + 25f830e + 4733406 + d49ad1b).
3. `10-01-PLAN.md` 0.3 has an "Absorbe T3-IMPROVEMENTS.md" sub-block referencing A2/B3/B4 with explicit T3-IMPROVEMENTS.md line ranges and the B4-UI carveout.
4. `10-01-PLAN.md` has a new sub-task `0.10 — C3 · Ordre randomisé pitch + équipes ancres milieu`.
5. `10-01-PLAN.md` Phase 1.1 path is now `app/globals.css` (not `eic-tokens.css`), and 1.3 has the A5 reuse note.
6. `10-01-PLAN.md` Hors scope section adds the B5/C1/C2/C4 bullet referencing T3-IMPROVEMENTS.md.
7. `10-CONTEXT.md` `<scope>` block contains a `## Hors scope (traité par /gsd-quick séparé ou v0.3)` sub-section.
8. `10-CONTEXT.md` top metadata has a Patch 2026-05-10 annotation linking to the quick directory.
9. `ROADMAP.md` Phase 10 section has a "Patch 2026-05-10" paragraph + `Plans:` line annotation, both referencing quick `260510-l3a`.
10. No source code (.ts, .tsx, .sql, .css) modified — only 3 .md files under `.planning/`.
</success_criteria>

<output>
After completion, create `.planning/quick/260510-l3a-patch-phase-10-plan-roadmap-post-quick-s/260510-l3a-SUMMARY.md` with :
- Date + commit SHA of the patch commit
- Bullet list of the 3 files patched and the key edits per file
- Confirmation that no source code was touched
- Cross-link back to `.planning/phases/10-t3-critical-gates-and-design-v2-tail-sections/10-01-PLAN.md` (now patched) and to the relevant prior quick sessions (`260510-kpw`, `260510-j2j`, `260510-jm8`).
</output>
