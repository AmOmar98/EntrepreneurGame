---
quick_id: 260512-hw0
date: 2026-05-12
slug: provision-p12-ezzouzi-agrivision-in-prod
description: Provision P12 = Fatima Ezzahrae Ezzouzi / Agrivision in PROD Supabase + add cred row
mode: quick (orchestrator-direct, no executor agent — gsd-executor lacks Supabase MCP tools)
risk: HIGH — PROD DB mutation at T-1 of AgreenTech pilot (13-14 May 2026)
must_haves:
  truths:
    - PROD auth.users count goes from 20 to 21
    - P12 login works on https://entrepreneur-game-six.vercel.app
    - 11 other porteurs P01-P11 untouched (RLS + triggers unaffected)
  artifacts:
    - cohorte-agreentech-creds.csv (line 13 added between P11 and M01)
    - 260512-hw0-PLAN.md / 260512-hw0-SUMMARY.md
  key_links:
    - scripts/provision-agreentech-cohort.cjs (reference for INSERT shape)
    - database/schema.sql (auth.users / players / player_members schema)
---

# Quick Task 260512-hw0 — Plan

## Goal
Provisionner P12 = Fatima Ezzahrae Ezzouzi (porteuse Agrivision, Kelaat des Sraghna) en PROD Supabase et l'ajouter au CSV creds. Confirmation Omar 2026-05-12 : Ezzouzi reste dans la cohorte malgré son absence dans la liste initiale des 11 porteurs.

## Context Snapshot (PROD verified via MCP)
- Project ID Supabase : `vzzbjxmfkmvqkaqxalhr` (EntrepreneurGame, eu-west-1, Postgres 17, ACTIVE_HEALTHY)
- auth.users actuels : **20** (11 players P01-P11 + 2 mentors + 3 jurys + 4 GM)
- Ezzouzi inexistante (`SELECT COUNT(*) WHERE email = 'ezzouzifatimaezzahrae@gmail.com'` = 0)
- Cohort `cohorte-mai-2026` existe : `id = ea5aae0b-3abb-4341-90f2-56e924a499ad`
- Slug `p12` libre dans `public.players`
- Format auth.users existant vérifié sur P01 (tadarti2004) : `raw_app_meta_data = {provider:email, providers:[email]}`, `raw_user_meta_data = {city, source, full_name, project_code, email_verified}`, `auth.identities` row avec `identity_data = {sub, email, email_verified:false, phone_verified:false}`, provider = `email`, provider_id = user_id

## Decisions

### Approche : INSERT SQL direct via Supabase MCP `apply_migration`
- `.env.local` n'a pas `SUPABASE_SERVICE_ROLE_KEY` → impossible de lancer `scripts/provision-agreentech-cohort.cjs` qui utilise `supabase.auth.admin.createUser`
- Supabase MCP a `apply_migration` (admin-level via plateforme), suffisant pour INSERT atomique
- Password bcrypt via `crypt('mdp', gen_salt('bf', 10))` (pgcrypto, built-in Supabase)

### Password
- Format : `EIC-<plante>-<NNN>` cohérent avec P01-P11
- Plantes déjà utilisées : argan, cedrus, olive, palmier, atlas, saffran, menthe, figue, vigne, dattier, jujube
- Choix P12 : **`EIC-caroubier-247`** (caroubier = plante méditerranéenne typique du Maroc, non-utilisée)

### Pas de spawn gsd-executor agent
- `gsd-executor` tool list = `Read, Write, Edit, Bash, Grep, Glob, mcp__context7__*` — pas de Supabase MCP
- Orchestrator (claude-code session courante) a Supabase MCP → exécution directe plus propre
- Justification CLAUDE.md : route via GSD respectée (quick orchestrator officiel) ; seul l'agent executor délégué est sauté pour cause d'outillage incompatible

## Tasks

### T1. Tag rollback anchor (pre-mutation)
- **Files :** none (git only)
- **Action :** `git tag v0.2.1-pre-p12 && git push origin v0.2.1-pre-p12`
- **Verify :** `git tag --list "v0.2.1*"` → contient `v0.2.1-pre-p12`
- **Done :** Tag visible sur origin

### T2. Apply migration INSERT P12 (atomic, single transaction)
- **Files :** none (Supabase MCP only)
- **Action :** `mcp__plugin_supabase_supabase__apply_migration` avec name `provision_p12_ezzouzi_agrivision` et SQL :

```sql
-- Provision P12 = Ezzouzi/Agrivision (single atomic block)
DO $$
DECLARE
  v_user_id uuid := gen_random_uuid();
  v_player_id uuid;
  v_cohort_id uuid := 'ea5aae0b-3abb-4341-90f2-56e924a499ad';
BEGIN
  -- 1. auth.users
  INSERT INTO auth.users (
    instance_id, id, aud, role, email, encrypted_password,
    email_confirmed_at, raw_app_meta_data, raw_user_meta_data,
    created_at, updated_at, confirmation_token, email_change,
    email_change_token_new, recovery_token
  ) VALUES (
    '00000000-0000-0000-0000-000000000000',
    v_user_id,
    'authenticated',
    'authenticated',
    'ezzouzifatimaezzahrae@gmail.com',
    crypt('EIC-caroubier-247', gen_salt('bf', 10)),
    NOW(),
    '{"provider":"email","providers":["email"]}'::jsonb,
    jsonb_build_object(
      'city', 'Kelaat des Sraghna',
      'source', 'agreentech-2026-bulk-provision',
      'full_name', 'Fatima Ezzahrae Ezzouzi',
      'project_code', 'P12',
      'email_verified', true
    ),
    NOW(), NOW(), '', '', '', ''
  );

  -- 2. auth.identities (mirror existing P01 shape)
  INSERT INTO auth.identities (
    id, user_id, identity_data, provider, provider_id,
    last_sign_in_at, created_at, updated_at
  ) VALUES (
    gen_random_uuid(),
    v_user_id,
    jsonb_build_object(
      'sub', v_user_id::text,
      'email', 'ezzouzifatimaezzahrae@gmail.com',
      'email_verified', false,
      'phone_verified', false
    ),
    'email',
    v_user_id::text,
    NULL, NOW(), NOW()
  );

  -- 3. public.profiles
  INSERT INTO public.profiles (user_id, app_role, full_name, email)
  VALUES (v_user_id, 'player', 'Fatima Ezzahrae Ezzouzi', 'ezzouzifatimaezzahrae@gmail.com')
  ON CONFLICT (user_id) DO NOTHING;

  -- 4. public.players
  INSERT INTO public.players (cohort_id, slug, name, idea, current_level, status)
  VALUES (
    v_cohort_id,
    'p12',
    'Fatima Ezzahrae Ezzouzi',
    'Agrivision — Application IA de détection précoce des maladies des cultures à partir d''images et analyses de terrain.',
    'L0_diagnostic',
    'active'
  )
  RETURNING id INTO v_player_id;

  -- 5. public.player_members
  INSERT INTO public.player_members (player_id, user_id, role, team_role)
  VALUES (v_player_id, v_user_id, 'player', 'owner');

  -- Emit for orchestrator capture
  RAISE NOTICE 'P12_USER_ID=%', v_user_id;
  RAISE NOTICE 'P12_PLAYER_ID=%', v_player_id;
END $$;
```

- **Verify :** Migration applies sans erreur ; aucun trigger Supabase ne casse
- **Done :** `apply_migration` returns success

### T3. Capture UUIDs + verify state
- **Files :** none
- **Action :** `execute_sql` :
```sql
SELECT u.id AS user_id, u.email, u.email_confirmed_at IS NOT NULL AS confirmed,
       p.id AS player_id, p.slug, p.name, p.current_level, p.status,
       pm.team_role,
       (u.encrypted_password = crypt('EIC-caroubier-247', u.encrypted_password)) AS password_roundtrip_ok,
       (SELECT COUNT(*) FROM auth.users) AS total_users
FROM auth.users u
JOIN public.profiles pr ON pr.user_id = u.id
JOIN public.player_members pm ON pm.user_id = u.id
JOIN public.players p ON p.id = pm.player_id
WHERE u.email = 'ezzouzifatimaezzahrae@gmail.com';
```
- **Verify :** 1 ligne returned, `confirmed=true`, `slug=p12`, `team_role=owner`, `password_roundtrip_ok=true`, `total_users=21`
- **Done :** UUIDs captured et écrits dans le SUMMARY

### T4. Update cohorte-agreentech-creds.csv
- **Files :** `cohorte-agreentech-creds.csv` (line 13 inserted between P11 row and M01 row)
- **Action :** Insert line :
```
P12,Fatima Ezzahrae Ezzouzi,Kelaat des Sraghna,ezzouzifatimaezzahrae@gmail.com,EIC-caroubier-247,0695387519,,player,Agrivision — Application IA de détection précoce des maladies des cultures à partir d'images et analyses de terrain.,<user_uuid_from_t3>,<player_uuid_from_t3>
```
- **Verify :** `grep -c "^P12," cohorte-agreentech-creds.csv` returns 1 ; total porteur rows = 12
- **Done :** CSV diff montre une seule ligne ajoutée à la bonne position

### T5. Write SUMMARY.md
- **Files :** `.planning/quick/260512-hw0-provision-p12-ezzouzi-agrivision-in-prod/260512-hw0-SUMMARY.md`
- **Action :** Document password, UUIDs, before/after counts, verification results, rollback procedure
- **Verify :** File exists, frontmatter complete
- **Done :** Markdown valid

### T6. Update STATE.md
- **Files :** `.planning/STATE.md`
- **Action :** Add row in Quick Tasks Completed table
- **Verify :** Row visible avec quick_id `260512-hw0`
- **Done :** STATE.md updated

### T7. Commit + push
- **Files :** `cohorte-agreentech-creds.csv`, `.planning/quick/260512-hw0-.../*`, `.planning/STATE.md`
- **Action :** Commit `feat(quick-260512-hw0): provision P12 Ezzouzi/Agrivision (T-1 pilot)` ; push origin/main
- **Verify :** `git log -1 --oneline` shows commit ; `git push` succeeds
- **Done :** SHA visible sur origin

### T8. Update auto-memory
- **Files :** `C:\Users\omara\.claude\projects\C--Users-omara-Desktop-EntrepreneurGame\memory\project_prod_pilot_state.md`, `reference_cohort_csvs.md`
- **Action :** Bump auth.users count 20 → 21 ; note P12 addition
- **Verify :** Files reflect new state
- **Done :** Memory aligned avec PROD

## Rollback Procedure (if T2-T3 fails or post-T7 regression detected)

```sql
-- Atomic delete (reverse order)
DELETE FROM public.player_members WHERE user_id = '<v_user_id>';
DELETE FROM public.players WHERE slug = 'p12';
DELETE FROM public.profiles WHERE user_id = '<v_user_id>';
DELETE FROM auth.identities WHERE user_id = '<v_user_id>';
DELETE FROM auth.users WHERE id = '<v_user_id>';
```

CSV : `git checkout HEAD -- cohorte-agreentech-creds.csv` (avant le commit T7)
Git distant : `git push origin --delete v0.2.1-pre-p12` n'est pas nécessaire (tag reste pour future référence)

## Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Trigger Supabase casse RLS / XP existants pour P01-P11 | T3 vérifie `total_users=21` (cohérent ; aucun user existant supprimé). Smoke supplémentaire : test login P01 après T7 (manuel via PROD URL). |
| Password bcrypt format incompatible avec Supabase auth | T3 vérifie `crypt(password, encrypted_password) = encrypted_password` (roundtrip). Si false → rollback. |
| `auth.identities` schema diffère de production attendue | T3 query confirme `provider=email` + `identity_data.sub` matches user_id. |
| Photo finale Omar invalide la décision P12 | Tag `v0.2.1-pre-p12` permet rollback distant ; CSV revert via `git revert <commit-sha>` ; DELETE SQL ci-dessus. |

## Out of Scope
- HOUENHA (P02) idea_seed update → quick séparé après réception photo Omar
- Bump `current_level` retroactif pour Players déjà onboardés (deferred item `project_onboarding_level_bump_sql.md` reste applicable)
