---
quick: 260520-124-jury-v1v3
type: quick
mode: ship
autonomous: false
files_modified:
  - app/jury/jury-form.tsx
  - app/jury/jury-dial-form.tsx
  - app/jury/page.tsx
must_haves:
  truths:
    - "Un juré ouvre /jury et voit la variation V1 (sliders horizontaux 0-20) par défaut"
    - "Un juré peut cliquer un toggle dans le header pour basculer en V3 (molettes rotatives SVG)"
    - "Le toggle persiste via le query param ?ui=dial (server-side, pas localStorage)"
    - "V1 et V3 soumettent toujours via savePitchScoreFlow avec c1..c5 (0-20 chacun, total /100)"
    - "Le score total est affiché en double : /100 (DB) ET /20 (moyenne, pour cohérence avec le mockup)"
    - "Sur mobile (<640px), V1 et V3 stackent en vertical (un seul composant responsive)"
    - "Le score reste anonyme entre jurés jusqu'à clôture pitches (banner jury_pitch_mode_live_banner conservé, aucun rang affiché)"
    - "Demo mode (sans Supabase env) reste navigable — pas de redirect avant check hasSupabaseEnv"
    - "Theater mode (?theater=1) + juror gate (notInvited) + variant=staff AppShell : tous préservés"
  artifacts:
    - path: "app/jury/jury-form.tsx"
      provides: "V1 sliders horizontaux 0-20 par critère, total /100 + équivalent /20, branding EIC tokens"
    - path: "app/jury/jury-dial-form.tsx"
      provides: "V3 molettes rotatives SVG (composant Dial), grille 5 colonnes desktop / 2 colonnes mobile"
    - path: "app/jury/page.tsx"
      provides: "Swap V1/V3 via searchParams.ui + toggle UI dans le header"
  key_links:
    - from: "app/jury/page.tsx"
      to: "savePitchScoreFlow"
      via: "JuryForm / JuryDialForm props (player, existing, eventId, dict)"
      pattern: "name=\"c[1-5]\" + name=\"playerId\" + name=\"eventId\""
    - from: "app/jury/page.tsx"
      to: "?ui=dial / ?ui=slider"
      via: "searchParams.ui readout + conditional component swap"
      pattern: "ui === \"dial\" \\? <JuryDialForm/> : <JuryForm/>"
---

<objective>
Remplacer l'écran `/jury` actuel (inputs number basiques c1..c5 max 20) par 2 variations UI :
- **V1 (par défaut)** : sliders horizontaux 0-20 par critère, total /100 + équivalent /20, branding EIC tokens `--wf-*` (`app/globals.css` confirme leur présence).
- **V3 (toggle `?ui=dial`)** : molettes rotatives SVG (composant `Dial` du mockup ligne 309-360), grille 5 colonnes desktop / 2 colonnes mobile.

Le toggle vit dans le header de la page (lien `Sliders` / `Molettes` qui swap `?ui=slider` ↔ `?ui=dial`). Server-side via searchParams, pas de localStorage.

Purpose: Donner au jury Digi-Hackathon un écran de notation expressif (V3) tout en gardant un fallback sobre (V1) pour partenaires moins technophiles. Identique côté DB : `savePitchScoreFlow(c1..c5)` avec chacun 0-20, total /100.

Output: 3 fichiers modifiés, signature server action intacte, type `PitchScore` intact, dual-mode demo préservé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260520-124-jury-v1v3/260520-124-PLAN.md

# Existing files (already read by planner)
@app/jury/page.tsx
@app/jury/jury-form.tsx
@lib/jury.ts
@lib/i18n.ts

# Mockup source (only V1 + V3 retained)
# Path: C:\Users\omara\Downloads\jury-screens.jsx
# - V1 SliderRow → ligne 53-90
# - V1 layout grid 1fr 320px (recap right column) → ligne 100-131
# - V3 Dial component → ligne 309-360 (SVG complete)
# - V3 grid 5 cols → ligne 378-380
# - Mobile responsive guidance → ligne 136-173 (V1 mobile) + 401-428 (V3 mobile)

<interfaces>
<!-- Server action signature MUST NOT change -->
From app/actions.ts (line 1164):
```ts
export async function savePitchScoreFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState>;
// Reads from FormData: playerId, eventId, c1, c2, c3, c4, c5
// Validates via pitchScoreSchema (Zod), c1..c5 each min(0).max(20)
// Returns WorkflowState { ok, message, mailto? }
```

From lib/types.ts:
```ts
export type PitchScore = {
  id: string;
  eventId: string;
  playerId: string;
  jurorId: string;
  c1: number; c2: number; c3: number; c4: number; c5: number;
  totalScore: number;
};
```

From lib/i18n.ts (existing jury_* keys — DO NOT add new ones for this quick):
- jury_c1_label .. jury_c5_label (c5_label is "" intentionally — legacy 5th critère retired, DB conserve c5=0)
- jury_c1_help .. jury_c5_help (c5_help is "" — guard on empty)
- jury_total_label, jury_save, jury_saving, jury_title, jury_subtitle, jury_each_max_20
- jury_pitch_mode_live_banner, jury_pitch_mode_closed_banner, jury_already_scored
- jury_pitch_theater_toggle_on (header link "Mode pitch théâtre" — existing)

From app/jury/page.tsx (props passed to current JuryForm):
```tsx
<JuryForm
  player={row.player}       // Player {id, name, idea, slug, ...}
  existing={row.existing}   // PitchScore | null
  eventId={eventId}         // string
  dict={t}                  // dictionaries.fr
/>
```
</interfaces>

<css_tokens>
Confirmed present in app/globals.css :
- --wf-blue, --wf-blue-tint, --wf-paper, --wf-paper-deep, --wf-line, --wf-line-strong
- --wf-ink, --wf-ink-soft, --wf-ink-faint
- Classes : .eic-button, .eic-button--primary, .eic-jury-grid, .eic-jury-card, .eic-jury-card__*

Le mockup utilise aussi `.wf-card`, `.wf-row`, `.wf-pill`, `.wf-mono`, `.wf-kicker`, `.wf-grow`, `.wf-faint`, `.wf-btn`. NE PAS supposer qu'ils existent — préférer inline style + classes EIC existantes (`eic-jury-card`, `eic-button`) + nouvelles classes BEM `eic-jury-form-v1__*` et `eic-jury-form-v3__*` si nécessaire.

**Important** : Le quick ne touche PAS `app/globals.css`. Si une classe spécifique manque, utiliser `style={{}}` inline avec les tokens CSS vars (`var(--wf-blue)` etc.).
</css_tokens>
</context>

<constraints>
**LOCKED (Q&A user résolu):**
1. V1 = défaut (route `/jury`), V3 = toggle `?ui=dial`
2. Desktop + mobile responsive — un seul composant par variation (V1 OU V3) qui adapte via CSS media query (pas 2 composants Desktop/Mobile séparés)
3. Remplacer `app/jury/jury-form.tsx` — PAS de route parallèle
4. Garder scoring DB `/100` (5 critères × 20). Le mockup affiche `/20 pondéré` (poids 20% × 5 = équivalent à `total / 5`) — afficher LES DEUX : `XX/100` (canonique) et `YY.Y/20` (cosmétique pondéré). Signature `savePitchScoreFlow` et type `PitchScore` (c1..c5) INTACTS.
5. **R1 révisée 11/05** : score VISIBLE côté jury (surface légitime). **MAIS** aucun rang/classement entre équipes côté jury. Le mockup ligne 129 dit "Le score est anonyme jusqu'à la cérémonie" → respecter via banner `jury_pitch_mode_live_banner` déjà existant.
6. **R2 warn-only** : si on touche Zod validators (on ne devrait pas — la signature ne change pas), garder `severity: "warn"`.
7. **R3** sans objet ici (pas de progression inter-mission)
8. **Dual-mode demo guard** : JAMAIS `redirect("/login")` ou `getCurrentUser()` avant check `hasSupabaseEnv()`. La page actuelle a déjà ce pattern (ligne 24-30) → CONSERVER tel quel.
9. **Branding EIC** : utiliser `player.name` + `player.idea` réels en mode Supabase. PAS de "Atlas/Maraya" du mockup (ces noms étaient des placeholders). Pas de "démo" visible.
10. **Préserver** : juror invitation gate (notInvited screen ligne 54-78), theater mode (?theater=1, ligne 83-107), `variant="staff"` AppShell.
11. **PAS de commentaire libre** : le mockup ligne 105 affiche un commentaire qualitatif — c'est cosmétique, le backend `pitchScoreSchema` n'a pas de champ commentaire → SKIP pour ce quick (ne pas étendre le schema, hors scope).
12. **Pas de critère 5 affiché** : `jury_c5_label = ""` (legacy retired). N'afficher QUE c1..c4 dans l'UI, mais envoyer `c5=0` en hidden input pour rester compatible Zod (qui exige c5 dans `pitchScoreSchema`). Ce pattern est déjà à respecter — vérifier le comportement actuel dans `jury-form.tsx` ligne 52-58 (les 5 champs sont tous render — à fixer dans V1+V3).

**Convention quick (CLAUDE.md + memory feedback_quick_orchestrator_convention) :**
- 1-2 commits atomiques max, préfixe `(quick-260520-124)`
- Artefacts dans `.planning/quick/260520-124-jury-v1v3/` : PLAN.md, AUDIT.md, ADVISOR-VERDICT.md, SUMMARY.md, deferred-items.md
- Default = ship + push (post-smoke OK)

**Pre-edit guards** : zone `app/jury/` est sensible (cf. CLAUDE.md "Pre-edit guards"). Spawn `eic-pedagogical-advisor` AVANT de commencer Task 1.
</constraints>

<tasks>

<task type="auto">
  <name>Task 0: Spawn EIC advisor pre-flight (R1/R2/R3 + branding)</name>
  <files>.planning/quick/260520-124-jury-v1v3/ADVISOR-VERDICT.md</files>
  <action>
Lancer l'agent `eic-pedagogical-advisor` (cf. memory `reference_eic_advisor_agent`) AVANT toute édition. Lui passer en contexte :
- Le mockup `C:\Users\omara\Downloads\jury-screens.jsx` (variations V1 + V3 uniquement)
- Le brief : "Remplacer `app/jury/jury-form.tsx` par 2 variations UI (sliders horizontaux V1 par défaut, molettes SVG V3 via ?ui=dial). Mobile responsive 1 composant. Garder savePitchScoreFlow signature, scoring c1..c4 affichés (c5=0 hidden), total /100 + équivalent /20."
- Demander verdict sur :
  - R1 : Le score visible côté jury sur SA propre grille est OK (surface jury légitime). Aucun rang/classement entre équipes ne sera affiché. Banner `jury_pitch_mode_live_banner` conservé.
  - R2 : Aucun nouveau validator Zod ajouté (signature `savePitchScoreFlow` intacte).
  - R3 : Sans objet.
  - Branding : `player.name` + `player.idea` réels en mode Supabase. PAS de "Atlas/Maraya".
  - Cohérence pédagogique : V3 molettes "style régie audio" — est-ce que le ton "jeu télévisé" risque de décrédibiliser devant Tamwilcom/Bank of Africa/Innov Invest ? Demander avis.

Écrire le verdict dans `.planning/quick/260520-124-jury-v1v3/ADVISOR-VERDICT.md` au format standard (OK / WARN / BLOCK + notes). Si BLOCK → ajuster le plan avant Task 1. Si WARN with notes → incorporer les notes dans Task 1/2/3.
  </action>
  <verify>
    <automated>test -f .planning/quick/260520-124-jury-v1v3/ADVISOR-VERDICT.md</automated>
  </verify>
  <done>ADVISOR-VERDICT.md écrit avec verdict explicite. Si BLOCK : plan ajusté ou stoppé. Si WARN/OK : Task 1 peut démarrer.</done>
</task>

<task type="auto">
  <name>Task 1: Réécrire app/jury/jury-form.tsx en V1 (sliders horizontaux 0-20)</name>
  <files>app/jury/jury-form.tsx</files>
  <action>
Réécrire ENTIÈREMENT `app/jury/jury-form.tsx` (client component, `"use client"`).

**Structure (basé sur mockup V1 ligne 53-134) :**
- Props identiques au composant actuel : `{ player, existing, eventId, dict }`.
- `useActionState(savePitchScoreFlow, initialState)` — inchangé.
- 4 critères affichés (c1..c4), `c5=0` envoyé via hidden input pour compatibilité Zod (cf. constraint 12).
- Chaque critère = une "SliderRow" :
  - Header : nom du critère (`dict.jury_c1_label` etc.) + valeur grosse à droite `{value}` style Baskervville/serif, suffixe `/ 20`
  - Slider `<input type="range" min={0} max={20} step={1} value={value} onChange={...}>` — utiliser `type="range"` natif HTML (accessible, KB nav par défaut). Style custom via inline ou classes nouvelles. Pas besoin du SVG track du mockup — `<input type="range">` natif suffit, plus accessible.
  - Sous le slider : 4 pills "Faible / Moyen / Bon / Excellent" qui s'allument selon `Math.floor(value/5.5)` (cf. mockup ligne 81-87)
  - Aide texte `{dict.jury_c1_help}` (existing) avec `aria-describedby`
- Panneau récapitulatif (à droite desktop, en bas mobile) :
  - **Score total grand format** : `{total}/100` (canonique DB) ET `{(total/5).toFixed(1)}/20` (équivalent pondéré, cosmétique)
  - Détail par critère (liste compacte)
  - Bouton "Valider mon vote" = `<button type="submit">` avec `dict.jury_save` / `dict.jury_saving` selon `pending`
  - Petit footer 10px : "Le score est anonyme jusqu'à la cérémonie." en fr (string inline acceptée OU réutiliser `jury_pitch_mode_live_banner` qui dit "Vos notes restent privées jusqu'à la clôture des pitches.")
- Branding : utiliser tokens `var(--wf-blue)`, `var(--wf-blue-tint)`, `var(--wf-paper)`, `var(--wf-line)`, `var(--wf-ink-soft)`, `var(--wf-ink-faint)` via inline `style={{}}`. PAS de modification de `app/globals.css` dans ce quick.
- **Responsive** : grid `1fr 320px` desktop → `grid-template-columns` qui passe à `1fr` (stack vertical) sous 640px. Faire via `@media` inline pas possible → utiliser CSS-in-JS minimal via classes nouvelles (BEM `eic-jury-form-v1__*`) ou solution la plus simple : inline desktop, et conteneur parent dans `page.tsx` qui gère le breakpoint via `eic-jury-grid` existant. Recommandation : un seul container `<div>` avec style flexible, le wrapper `<article class="eic-jury-card">` gère déjà le layout par carte ; nos sliders restent dans la card.
- **Warn 0/0/0/0** : conserver le warning existant "Verifie : tous les criteres sont a 0" (ligne 120-136 du fichier actuel) — pattern R2 warn-only.

**État pendant** : ne PAS toucher la signature de `savePitchScoreFlow`. Le `<form action={formAction}>` envoie `playerId`, `eventId`, `c1`, `c2`, `c3`, `c4`, `c5=0` (hidden).

**A11y** : 
- `<input type="range">` natif gère KB par défaut.
- `<label htmlFor={f.key}>` ou `<label><span/>...<input/></label>` (préférer le wrap, conforme au pattern actuel ligne 66).
- `aria-describedby={\`${f.key}-help\`}` sur input range avec `<span id="...-help">` adjacent.
- Bouton submit a un nom accessible via texte visible (`dict.jury_save`).

Export : `export function JuryForm({...}: Props)` — nom du composant inchangé.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- `app/jury/jury-form.tsx` réécrit, exporte `JuryForm` avec props identiques.
- 4 sliders affichés (c1..c4), c5=0 hidden.
- Score total double affichage : `/100` + `/20 pondéré`.
- Tokens `--wf-*` utilisés via inline style.
- `npm run typecheck` passe.
- Branding pro (pas de mention "démo", pas de Atlas/Maraya).
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer app/jury/jury-dial-form.tsx (V3 molettes rotatives SVG)</name>
  <files>app/jury/jury-dial-form.tsx</files>
  <action>
Créer un NOUVEAU fichier `app/jury/jury-dial-form.tsx` (client component, `"use client"`).

**Structure (basé sur mockup V3 Dial ligne 309-360 + grid ligne 378) :**
- Props identiques à `JuryForm` : `{ player, existing, eventId, dict }`.
- `useActionState(savePitchScoreFlow, initialState)` — même action.
- 4 critères affichés (c1..c4) sous forme de molettes SVG, `c5=0` hidden.
- **Composant interne `Dial`** (inspiration mockup ligne 309) :
  - SVG 120x120 avec :
    - Outer ring (cercle r=54, stroke `var(--wf-paper-deep)`)
    - Tick marks (21 ticks de -135° à +135°, tous les 13.5°) — array.from({length:21}) → 5 ticks majeurs (i%5===0), reste minor
    - Track arc (stroke `var(--wf-paper-deep)`, dash 270°/360°)
    - Progress arc (stroke `var(--wf-blue)`, `strokeLinecap="round"`, longueur = `(value/20)*270` degrés)
    - Knob central (cercle r=32, fill #fff + gradient radial `dialgrad`)
    - Indicator (ligne du centre vers angle = -135 + (value/20)*270, stroke `var(--wf-blue)` width=3)
    - Texte central : `{value}` font Baskervville 20px (à la place du `text` ligne 353)
  - Interaction : **pas de drag rotatif** (trop complexe pour ce quick + memory mockup ligne 573 dit "drag rotatif demande quelques essais"). Utiliser à la place :
    - **Input range invisible** par-dessus la molette (`<input type="range" min={0} max={20}>` avec `opacity:0; position:absolute; inset:0; cursor:pointer;`) → KB accessible + click/drag native + screen readers
    - Boutons +/- visibles en dessous (cf. mockup ligne 257 fallback "+/-" sage)
- **Layout desktop** : `display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px;` (4 molettes côte à côte ; pas 5 puisque c5 retiré)
- **Layout mobile (<640px)** : `grid-template-columns: 1fr 1fr;` (2x2) — via classe nouvelle ou media query inline. Si inline impossible → classe BEM `eic-jury-form-v3__grid` + une règle `@media` ajoutée dans `app/globals.css` (acceptable cette fois car ce token n'existe pas — sinon préférer Tailwind-style utility si déjà importé… mais le projet n'a pas Tailwind actif sur cette zone, on a `globals.css`).
  - **Décision finale** : ajouter UNE règle CSS dans `app/globals.css` :
    ```css
    .eic-jury-form-v3__grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
    @media (max-width: 640px) { .eic-jury-form-v3__grid { grid-template-columns: repeat(2, 1fr); } }
    .eic-jury-form-v1__layout { display: grid; grid-template-columns: 1fr 320px; gap: 16px; }
    @media (max-width: 640px) { .eic-jury-form-v1__layout { grid-template-columns: 1fr; } }
    ```
  - **EXCEPTION constraint** : ajouter ces 4 lignes à `app/globals.css` est admis car (a) limité et localisé, (b) responsive nécessaire, (c) pas de zone sensible R1/R2/R3.
  - Mettre à jour le `files_modified` frontmatter si on fait ça → **AJOUTER `app/globals.css` aux files_modified** (cf. modification ci-dessous).
- **Récapitulatif en haut** (mockup V3 ligne 370-377) : card "Tableau de bord juré" avec score total `{total}/100` ET `{(total/5).toFixed(1)}/20`.
- **Footer actions** : bouton "Verrouiller mon vote" = `<button type="submit">` standard. SKIP les pills "Pas convaincu / À retravailler / Convaincu / Coup de cœur" du mockup (ligne 388-391) — pas de champ DB pour ça, hors scope.
- **Warn 0/0/0/0** : conserver le même pattern que V1.

Export : `export function JuryDialForm({...}: Props)`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- `app/jury/jury-dial-form.tsx` créé, exporte `JuryDialForm`.
- 4 molettes SVG affichées (c1..c4), c5=0 hidden.
- `<input type="range">` invisible par-dessus chaque molette pour KB+a11y.
- Boutons +/- visibles en dessous de chaque molette.
- Score total double affichage `/100` + `/20`.
- `npm run typecheck` passe.
- Classes CSS responsive ajoutées dans `app/globals.css` (4 règles, ~6 lignes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Modifier app/jury/page.tsx — searchParams.ui swap + toggle header</name>
  <files>app/jury/page.tsx</files>
  <action>
Modifier `app/jury/page.tsx` pour swap entre V1 et V3 selon `searchParams.ui`.

**Changements :**
1. Étendre `SearchParams` type :
   ```ts
   type SearchParams = {
     theater?: string;
     ui?: string;  // ← NEW: "slider" (default) | "dial"
   };
   ```
2. Lire `ui` dans le `await searchParams` :
   ```ts
   const { theater, ui } = await searchParams;
   const variant: "slider" | "dial" = ui === "dial" ? "dial" : "slider";
   ```
3. Importer le nouveau composant :
   ```ts
   import { JuryDialForm } from "./jury-dial-form";
   ```
4. Dans le rendu Standard mode (ligne 112-170, BLOC non-theater), ajouter dans le header un toggle UI à côté du bouton "Mode pitch théâtre" :
   ```tsx
   <div style={{ display: "flex", gap: 8 }}>
     <Link
       href={`/jury?ui=${variant === "dial" ? "slider" : "dial"}`}
       className="eic-button"
       aria-label={variant === "dial" ? "Basculer en sliders" : "Basculer en molettes"}
     >
       {variant === "dial" ? "↔ Sliders" : "↔ Molettes"}
     </Link>
     <Link className="eic-button eic-button--primary" href="/jury?theater=1">
       {t.jury_pitch_theater_toggle_on}
     </Link>
   </div>
   ```
   (Strings "Sliders" / "Molettes" inline acceptées — `lib/i18n.ts` n'a pas de clé dédiée et on ne veut pas étendre i18n dans ce quick. Si l'advisor demande une clé, ajouter `jury_ui_toggle_slider` / `jury_ui_toggle_dial` dans `lib/i18n.ts`.)
5. Dans `<article class="eic-jury-card">` (ligne 148), swap le composant selon variant :
   ```tsx
   {variant === "dial" ? (
     <JuryDialForm player={row.player} existing={row.existing} eventId={eventId} dict={t} />
   ) : (
     <JuryForm player={row.player} existing={row.existing} eventId={eventId} dict={t} />
   )}
   ```
6. **NE PAS toucher** : theater mode block (ligne 83-107), notInvited block (ligne 54-78), variant="staff" AppShell, juror gate, dual-mode check. Tous préservés tels quels.

**Pre-edit guards check** :
- Pas de `redirect("/login")` ajouté avant `hasSupabaseEnv()` — le `getCurrentUser()` ligne 24-25 reste avant le check `hasSupabaseEnv()` MAIS c'était déjà comme ça dans le fichier actuel (cf. memory `feedback_dual_mode_demo_guard`). Ne pas régresser, ne pas "fixer" non plus dans ce quick (hors scope).
- Aucun affichage de rang/classement entre équipes côté jury.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- `app/jury/page.tsx` lit `searchParams.ui`, swap V1↔V3.
- Toggle visible dans le header (lien EIC button qui flip le param).
- `npm run typecheck` passe.
- Theater mode + notInvited + variant="staff" + juror gate INTACTS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
2 variations UI scoring jury implémentées :
- V1 (par défaut sur `/jury`) : sliders horizontaux 0-20 par critère + récap droite avec total `/100` + `/20 pondéré`.
- V3 (`/jury?ui=dial`) : 4 molettes SVG rotatives, total en haut, grille 4 cols desktop / 2 cols mobile.
- Toggle dans le header (lien "↔ Molettes" / "↔ Sliders").
- Theater mode, juror gate, dual-mode demo, branding pro : tous préservés.
- `c5=0` hidden envoyé pour compatibilité Zod (legacy retired).
  </what-built>
  <how-to-verify>
**Smoke local (Omar) :**

1. **Typecheck/lint/build** :
   ```
   npm run typecheck && npm run lint && npm run build
   ```
   Tous doivent passer.

2. **Dev server** : `npm run dev` puis ouvrir http://localhost:3000/jury (ou 3001/3002).

3. **Mode demo (sans Supabase env)** :
   - `/jury` doit afficher le AppShell variant="staff" et le message `jury_demo_disabled` (pas de crash, pas de redirect).
   - Ajouter `?ui=dial` à l'URL → même comportement (demo, le toggle ne casse rien).

4. **Mode Supabase (avec env)** : se connecter en tant que GameMaster ou juror invité :
   - `/jury` → V1 par défaut (sliders) — vérifier chaque slider 0-20, total recalcule, `/100` + `/20`.
   - Clic toggle "↔ Molettes" → URL devient `/jury?ui=dial`, molettes affichées.
   - Bouger un slider (V1) OU un range invisible sur molette (V3) → valeur change, total recalcule live.
   - Submit V1 : score sauvegardé via `savePitchScoreFlow`, message OK.
   - Submit V3 : idem.
   - Reload `/jury?ui=dial` → la note existante (c1..c4) est rechargée correctement.

5. **Mobile** : DevTools responsive mode largeur 375px :
   - V1 : récap passe en dessous (stack vertical).
   - V3 : grille 2x2 molettes au lieu de 4 cols.

6. **Gardrails** :
   - Theater mode : `/jury?theater=1` → écran théâtre toujours OK, pas affecté par `ui=dial`.
   - Mentor non invité (notInvited screen) : toujours le message `jury_not_invited`, le toggle ne s'affiche pas (vu qu'on est avant la grille).
   - GameMaster : voit toujours la grille (bypass juror gate).

7. **R1 audit grep** :
   ```
   grep -rn "rank\|classement\|percentile\|leaderboard" app/jury/
   ```
   Attendu : aucun match côté jury (le rang n'apparaît jamais sur `/jury`).

8. **Branding** : vérifier qu'aucune chaîne "Atlas", "Maraya", "demo" n'apparaît dans les nouveaux fichiers :
   ```
   grep -rn "Atlas\|Maraya\|démo\|demo" app/jury/jury-form.tsx app/jury/jury-dial-form.tsx
   ```
   Attendu : aucun match (sauf si "demo" est dans une clé i18n existante réutilisée).

**Si tout OK → commit + push.**
**Si BLOCK / régression → revenir au planner avec feedback dans `.planning/quick/260520-124-jury-v1v3/AUDIT.md`.**
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (with file:line refs)</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit + push + ship artefacts</name>
  <files>
    .planning/quick/260520-124-jury-v1v3/SUMMARY.md
    .planning/quick/260520-124-jury-v1v3/AUDIT.md
    .planning/quick/260520-124-jury-v1v3/deferred-items.md
  </files>
  <action>
**Précondition** : Task 4 (checkpoint) approved.

1. Écrire les 3 artefacts restants (ADVISOR-VERDICT.md déjà écrit Task 0) :
   - `AUDIT.md` : checks effectués (typecheck/lint/build, grep R1, grep branding, smoke desktop+mobile, theater+notInvited gardrails), résultats.
   - `SUMMARY.md` : 1-2 paragraphes résumé technique, SHA du/des commit(s), fichiers touchés, breaking changes (aucun — savePitchScoreFlow signature intacte).
   - `deferred-items.md` : items déférés intentionnellement :
     - Commentaire libre qualitatif (mockup V1 ligne 105) — nécessite extension `pitchScoreSchema` + DB column → quick séparé.
     - Drag rotatif SVG sur V3 — input range invisible suffit pour ce pilote ; drag natif quick séparé si l'advisor le demande.
     - Verdict pills (V3 ligne 388-391 "Pas convaincu / À retravailler / Convaincu / Coup de cœur") — nécessite champ DB séparé.
     - Pondération customisable par critère — actuellement 20% × 5 hardcodé, suffisant pour Digi-Hackathon 2026-05.
     - Bouton "Brouillon" séparé du "Valider" — `useActionState` ne distingue pas, hors scope.

2. **Commits atomiques** (1 commit max si tâche unique cohérente, 2 si tu sépares V1/V3) :
   Préfixe : `(quick-260520-124)`
   Exemple :
   ```
   git add app/jury/jury-form.tsx app/jury/jury-dial-form.tsx app/jury/page.tsx app/globals.css .planning/quick/260520-124-jury-v1v3/
   git commit -m "$(cat <<'EOF'
   (quick-260520-124) feat(jury): V1 sliders + V3 molettes UI variations
   
   - jury-form.tsx réécrit en V1 sliders horizontaux 0-20 + récap /100+/20
   - jury-dial-form.tsx nouveau en V3 molettes SVG 4 cols desktop / 2x2 mobile
   - page.tsx swap via searchParams.ui (slider défaut / dial toggle)
   - globals.css 4 règles responsive (eic-jury-form-v1__layout + v3__grid)
   - savePitchScoreFlow signature intacte, c5=0 hidden (legacy retired)
   - dual-mode demo + theater mode + juror gate + R1 anonymat tous préservés
   
   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   EOF
   )"
   ```

3. **Push origin** : `git push origin main` (default = ship + push policy).

4. **Smoke prod post-push** : ouvrir https://entrepreneur-game-six.vercel.app/jury (après Vercel deploy ~2 min) :
   - V1 default OK
   - `?ui=dial` swap OK
   - Aucun crash console
   - Logs Vercel propres (pas d'erreur build)

5. Si smoke prod KO → hotfix protocol (CLAUDE.md "Hotfix protocol événement live").
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "quick-260520-124"</automated>
  </verify>
  <done>
- 5 artefacts présents dans `.planning/quick/260520-124-jury-v1v3/` (PLAN, ADVISOR-VERDICT, AUDIT, SUMMARY, deferred-items)
- 1-2 commits atomiques préfixés `(quick-260520-124)` pushés sur origin/main
- Smoke prod Vercel OK
- Aucune régression R1/R2/R3 / theater / notInvited / dual-mode
  </done>
</task>

</tasks>

<verification>
**Smoke obligatoire (CLAUDE.md merge policy + memory feedback_smoke_minimal_2p_1m_1gm partiel) :**
- `npm run typecheck` ✓
- `npm run lint` ✓
- `npm run build` ✓
- Grep R1 jury : `grep -rn "rank\|classement\|percentile\|leaderboard" app/jury/` → vide ✓
- Grep branding leak : `grep -rn "Atlas\|Maraya" app/jury/` → vide ✓
- Manual desktop : V1 default OK, toggle V3 OK, total recalcule live, submit OK
- Manual mobile (375px) : V1 stack vertical, V3 grille 2x2
- Gardrails : theater mode OK, notInvited OK, demo mode OK (pas de crash)
- Prod Vercel post-push OK

**Pre-edit guards (CLAUDE.md) :**
- R1 : score visible jury sur SA grille = OK (surface jury légitime). Aucun rang/classement affiché. Banner anonymat préservé via `jury_pitch_mode_live_banner`.
- R2 : aucun validator Zod nouveau (signature `savePitchScoreFlow` intacte). Warn 0/0/0/0 conservé.
- R3 : sans objet.
- Dual-mode guard : `hasSupabaseEnv()` check préservé (ligne 35 page.tsx existante).
</verification>

<success_criteria>
- [ ] `/jury` affiche V1 sliders horizontaux par défaut sur desktop + mobile
- [ ] `/jury?ui=dial` affiche V3 molettes SVG sur desktop (4 cols) + mobile (2x2)
- [ ] Toggle dans header swap V1 ↔ V3 via le query param
- [ ] `savePitchScoreFlow(c1..c5)` reçoit toujours c1..c4 valides + c5=0 hidden
- [ ] Total affiché en double : `XX/100` (canonique) + `YY.Y/20` (cosmétique)
- [ ] Banner anonymat (`jury_pitch_mode_live_banner` ou inline) présent sur les 2 variations
- [ ] Theater mode (`?theater=1`) inchangé
- [ ] notInvited screen inchangé
- [ ] Demo mode (sans Supabase env) : pas de crash, pas de redirect prématuré
- [ ] `npm run typecheck && npm run lint && npm run build` passent
- [ ] Grep R1 + grep branding : vides côté `app/jury/`
- [ ] Commit `(quick-260520-124)` pushé sur origin/main
- [ ] Smoke prod Vercel OK
- [ ] 5 artefacts dans `.planning/quick/260520-124-jury-v1v3/`
</success_criteria>

<output>
After completion, the quick directory will contain:
- `260520-124-PLAN.md` (this file)
- `ADVISOR-VERDICT.md` (Task 0 output)
- `AUDIT.md` (Task 5 — smoke results)
- `SUMMARY.md` (Task 5 — SHAs + summary)
- `deferred-items.md` (Task 5 — items deferred intentionally)
</output>
