---
phase: 260510-j2j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/journey-level-node.tsx
  - components/journey-client.tsx
  - components/journey-drawer.tsx
  - lib/i18n.ts
  - app/globals.css
autonomous: true
requirements:
  - T3-B2
  - EIC-R2
  - EIC-R3

must_haves:
  truths:
    - "Sur /journey, AUCUNE surface Player n'utilise le rouge ou un ton danger pour signaler un niveau verrouillé (en particulier L3)."
    - "Cliquer / survoler / focus le node L3 verrouillé affiche un tooltip ambre warn-only avec la copie 'Astuce : ...' (pas 'Niveau verrouille. Terminez...' en hard-stop)."
    - "Le node L3 verrouillé reste interactif au clavier (focus visible, pas d'attribut disabled qui retire le node du tab-order) — conforme R3 pas-de-blocage-en-dur."
    - "La progression réelle (currentLevel, getLevelStates, lib/journey-progression.ts) reste inchangée — le changement est purement présentationnel."
    - "npm run lint + npm run typecheck passent sans nouvelle erreur."
  artifacts:
    - path: "components/journey-level-node.tsx"
      provides: "Node interactif même quand state=locked (disabled retiré ou conditionnel non-bloquant), tooltip ambre rendu pour locked"
      contains: "role=\"tooltip\""
    - path: "components/journey-client.tsx"
      provides: "HoveredHint rend la copie warn-only (amber) pour state=locked au lieu du texte hard-stop"
    - path: "components/journey-drawer.tsx"
      provides: "Body locked rend la copie warn-only (amber) au lieu de t.journey_v2_drawer_locked actuel"
    - path: "lib/i18n.ts"
      provides: "Nouvelles clés journey_v2_locked_hint_amber (FR + EN), copie warn-only 'Astuce : ...'"
      contains: "journey_v2_locked_hint_amber"
    - path: "app/globals.css"
      provides: "Classe .eic-track__node-tooltip (et variant amber) — palette existante --wf-amber / --wf-amber-tint, focus/hover trigger, prefers-reduced-motion guard"
      contains: ".eic-track__node-tooltip"
  key_links:
    - from: "components/journey-level-node.tsx"
      to: "app/globals.css .eic-track__node-tooltip"
      via: "className composition"
      pattern: "eic-track__node-tooltip"
    - from: "components/journey-level-node.tsx"
      to: "lib/i18n.ts"
      via: "import dictionaries.fr.journey_v2_locked_hint_amber"
      pattern: "journey_v2_locked_hint_amber"
    - from: "components/journey-client.tsx HoveredHint"
      to: "lib/i18n.ts journey_v2_locked_hint_amber"
      via: "stateText branch state==='locked'"
      pattern: "journey_v2_locked_hint_amber"
---

<objective>
B2 (T3-IMPROVEMENTS) — Convertir le verrouillage L3 (et tous les niveaux verrouillés Player) d'un ton hard-stop / banner rouge implicite vers un tooltip ambre **warn-only** non-bloquant. Conforme R2 (validators warn-only) et R3 (pas de blocage codé en dur).

Purpose:
- R3 : retirer le blocage UI inter-mission (le `disabled` sur le node + la copie "Terminez les niveaux precedents" agissent ensemble comme un hard block visuel — c'est exactement ce que R3 interdit).
- R2 : surfacer le même message en tonalité warn (ambre), pas error/danger.
- B2 (T3-IMPROVEMENTS section H) : "MissionBreadcrumb retire E.bloqué (remplace par tooltip ambre)" — appliqué à la zone réelle (journey track + drawer + hovered hint).

Output: 5 fichiers modifiés, 0 nouvelle dépendance, comportement progression intact, ton visuel basculé danger→warn pour tous les niveaux verrouillés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@T3-IMPROVEMENTS.md
@CLAUDE.md

# Surfaces directement modifiées
@components/journey-level-node.tsx
@components/journey-client.tsx
@components/journey-drawer.tsx
@lib/i18n.ts
@app/globals.css

# Référence non-modifiée (logique progression — NE PAS TOUCHER)
@lib/journey-progression.ts

<interfaces>
<!-- Contrats clés extraits du codebase. L'exécutor doit s'appuyer dessus directement. -->

From components/journey-level-node.tsx (état actuel) :
```ts
export type JourneyLevelNodeProps = {
  levelId: string;
  number: string;
  state: LevelState;          // "done" | "current" | "locked"
  topPct: number;
  ariaLabel: string;
  onClick?: () => void;
  onMouseEnter?: () => void;
  onMouseLeave?: () => void;
};
```
Le `<button disabled={state === "locked"} ...>` doit être conservé en click-handler (déjà géré côté parent : `onClick={() => state !== "locked" && onLevelClick?.(id)}` dans journey-track.tsx) MAIS on retire `disabled` de l'élément DOM pour préserver le tab-order et l'interactivité (focus + tooltip déclenchable au clavier). Le no-op du onClick reste géré par le parent.

From lib/i18n.ts (clé existante, à NE PLUS UTILISER pour locked Player surfaces) :
```ts
journey_v2_drawer_locked: "Niveau verrouille. Terminez les niveaux precedents.",
```
→ Remplacée par la nouvelle clé `journey_v2_locked_hint_amber` (warn-only). L'ancienne clé peut rester en place pour compat (utilisée nulle part ailleurs après ce patch — mais ne pas la retirer pour éviter de casser EN si l'audit grep rate quelque chose ; elle deviendra dead code purement, OK pour pilote).

From app/globals.css (palette amber existante, à RÉUTILISER) :
```
--wf-amber: <token>;
--wf-amber-tint: <token>;
.eic-pill--amber { background: var(--wf-amber-tint); color: var(--wf-amber); border-color: #DCC394; }
```
Le tooltip doit utiliser **exactement** cette palette (background = `var(--wf-amber-tint)`, color = `var(--wf-amber)`, border = `#DCC394`) — cohérence stricte avec la pill amber existante.

From components/journey-track.tsx (caller — NE PAS modifier) :
```ts
onClick={() => state !== "locked" && onLevelClick?.(id)}
```
Le no-op locked-click est déjà géré côté parent. Retirer `disabled` du node ne va PAS ouvrir le drawer pour un node locked — preuve à vérifier dans la tâche 1.
</interfaces>

<rules_cardinales>
- R2 (validators warn-only) : tooltip = palette ambre, jamais rouge/rose, jamais ton "erreur".
- R3 (pas de blocage codé en dur) : le node L3 locked DOIT être focus-able et déclencher le tooltip — pas un `disabled` qui retire l'élément du tab-order.
- INTERDIT : modifier `lib/journey-progression.ts`, `lib/journey.ts` scoring/gating, `database/*.sql`, ou tout fichier de logique de progression. Pure surface visuelle.
- INTERDIT : ajouter une nouvelle dépendance (radix, headless-ui, react-tooltip…). Tooltip inline accessible, point.
- L'agent EIC pédagogique (`.claude/agents/eic-pedagogical-advisor.md`) DOIT être consulté avant de figer la copie FR du tooltip (zone copy founder-facing sensible).
</rules_cardinales>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter clé i18n + classe CSS tooltip ambre</name>
  <files>lib/i18n.ts, app/globals.css</files>
  <action>
1. **lib/i18n.ts** — ajouter dans le dictionnaire `fr` ET `en` :
   - `journey_v2_locked_hint_amber` :
     - FR : `"Astuce : completez les niveaux precedents pour maximiser la qualite de celui-ci."`
     - EN : `"Tip: complete the previous levels to maximize the quality of this one."`
   - Respecter la convention plain-ASCII existante (pas d'accents/diacritiques dans la valeur — voir `t.journey_v2_drawer_locked` qui dit "verrouille" pas "verrouillé"). C'est intentionnel (anti-leak diacritiques mailto/CSV — cf. CONVENTIONS.md).
   - Insérer la nouvelle clé immédiatement à côté de `journey_v2_drawer_locked` dans chaque dico.
   - **AVANT de figer la copie FR**, consulter l'agent EIC pédagogique (`.claude/agents/eic-pedagogical-advisor.md`) — la copie est founder-facing et touche la zone L3 (sensible R2/R3). Si l'agent propose une reformulation R2-compliant plus précise, l'adopter.

2. **app/globals.css** — ajouter (juste après le bloc `.eic-track__node-pulse` / `@keyframes eic-track-pulse`, autour ligne 1660) :
   ```css
   /* === Locked-level tooltip (R2/R3 — warn-only ambre) === */
   .eic-track__node-tooltip {
     position: absolute;
     left: calc(100% + 14px);
     top: 50%;
     transform: translateY(-50%);
     background: var(--wf-amber-tint);
     color: var(--wf-amber);
     border: 1px solid #DCC394;
     border-radius: 8px;
     padding: 8px 12px;
     font-size: 12px;
     line-height: 1.35;
     font-weight: 500;
     max-width: 240px;
     white-space: normal;
     box-shadow: 0 4px 14px rgba(0, 0, 0, 0.10);
     opacity: 0;
     pointer-events: none;
     transition: opacity 0.18s ease;
     z-index: 6;
   }
   .eic-track__node:hover .eic-track__node-tooltip,
   .eic-track__node:focus-visible .eic-track__node-tooltip,
   .eic-track__node:focus .eic-track__node-tooltip {
     opacity: 1;
   }
   @media (prefers-reduced-motion: reduce) {
     .eic-track__node-tooltip { transition: none; }
   }
   /* Mobile: tooltip remonte au-dessus pour ne pas sortir du viewport */
   @media (max-width: 720px) {
     .eic-track__node-tooltip {
       left: 50%;
       top: auto;
       bottom: calc(100% + 10px);
       transform: translateX(-50%);
     }
   }
   /* Variante drawer/hint inline (pas absolute) — réutilisable depuis HoveredHint + drawer body */
   .eic-locked-hint--amber {
     background: var(--wf-amber-tint);
     color: var(--wf-amber);
     border: 1px solid #DCC394;
     border-radius: 8px;
     padding: 10px 12px;
     font-size: 13px;
     line-height: 1.4;
     font-weight: 500;
   }
   ```
   - **NE PAS modifier** `.eic-track__node.is-locked` existant (cursor: not-allowed; border-style: dashed) — le visuel locked dashed reste, c'est le ton ambre du tooltip qui porte la sémantique warn.
   - Vérifier que `--wf-amber` et `--wf-amber-tint` sont définis dans `app/globals.css` (voir grep ligne 1030 : `.eic-pill--amber` les utilise). Si absents en :root, c'est qu'ils sont dans un autre bloc — ne PAS les redéclarer, juste les référencer.

3. Lancer `npm run typecheck` après modification i18n.ts pour confirmer que les types `dictionaries.fr.journey_v2_locked_hint_amber` et `.en` sont bien inférés.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>
- `lib/i18n.ts` contient `journey_v2_locked_hint_amber` dans les 2 dicos (`fr` ET `en`).
- `app/globals.css` contient `.eic-track__node-tooltip` (variante absolute pour le node) ET `.eic-locked-hint--amber` (variante inline pour drawer/hint).
- `npm run typecheck` exit 0.
- `npm run lint` exit 0.
- Aucune nouvelle dépendance dans `package.json` (grep rapide pour confirmer).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Câbler le tooltip ambre sur JourneyLevelNode + retirer le blocage clavier</name>
  <files>components/journey-level-node.tsx</files>
  <action>
Modifier `components/journey-level-node.tsx` :

1. **Retirer l'attribut `disabled`** du `<button>` :
   - Avant : `disabled={state === "locked"}`
   - Après : retiré complètement (le no-op locked-click est déjà géré côté parent via `onClick={() => state !== "locked" && onLevelClick?.(id)}` dans journey-track.tsx — vérifié dans `<interfaces>`).
   - Justification R3 : `disabled` retire le node du tab-order et empêche le focus → impossible de déclencher le tooltip au clavier → blocage. R3 interdit.
   - Conserver `aria-current={state === "current" ? "step" : undefined}` et `aria-label={ariaLabel}` (déjà présents).
   - Optionnel : ajouter `aria-disabled={state === "locked" ? "true" : undefined}` pour signaler aux AT le statut sémantique sans bloquer l'interactivité (R3 friendly).

2. **Importer la nouvelle clé i18n** (en haut du fichier) :
   ```ts
   import { dictionaries } from "@/lib/i18n";
   const t = dictionaries.fr;
   ```

3. **Rendre le tooltip ambre uniquement pour locked** (à l'intérieur du `<button>`, après le `<span eic-track__node-pulse>` existant pour le current state) :
   ```tsx
   {state === "locked" ? (
     <span
       aria-hidden="false"
       className="eic-track__node-tooltip"
       id={`eic-track-tooltip-${levelId}`}
       role="tooltip"
     >
       {t.journey_v2_locked_hint_amber}
     </span>
   ) : null}
   ```

4. **Câbler `aria-describedby`** sur le `<button>` quand locked :
   ```tsx
   aria-describedby={state === "locked" ? `eic-track-tooltip-${levelId}` : undefined}
   ```
   → Les screen readers annoncent l'astuce au focus du node locked, sans avoir besoin de hover.

5. Conserver le `cursor: not-allowed` (déjà dans `.eic-track__node.is-locked` CSS) — c'est un signal visuel valide non-bloquant (l'utilisateur PEUT focus + voir le tooltip, juste qu'un click ne fera rien sur ce node — c'est conforme R3 car ça n'empêche PAS d'aller faire la mission L3 via un autre chemin si jamais — mais surtout ça reflète honnêtement l'état "rien à faire ici tant que pas current").

**Contrainte stricte** : NE PAS importer / créer un nouveau composant Tooltip wrapper. Tooltip = un `<span role="tooltip">` enfant du `<button>`, déclenché en CSS pur (`:hover`, `:focus`, `:focus-visible`) via la classe `.eic-track__node-tooltip` ajoutée tâche 1.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>
- `components/journey-level-node.tsx` n'a plus `disabled={state === "locked"}` sur le `<button>`.
- Quand `state === "locked"`, un `<span role="tooltip" id="eic-track-tooltip-{levelId}">` est rendu enfant du button avec `t.journey_v2_locked_hint_amber` comme contenu.
- Le button locked a `aria-describedby="eic-track-tooltip-{levelId}"` (et optionnellement `aria-disabled="true"`).
- `npm run typecheck` exit 0, `npm run lint` exit 0.
- Aucune modif dans `lib/journey-progression.ts` (grep `git diff lib/journey-progression.ts` doit être vide).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Basculer HoveredHint + Drawer body en variante ambre warn-only</name>
  <files>components/journey-client.tsx, components/journey-drawer.tsx</files>
  <action>
**A. components/journey-client.tsx** — fonction `HoveredHint` (lignes 139-152) :

Remplacer le ternaire actuel :
```tsx
const stateText =
  state === "current"
    ? t.journey_status_a_rendre
    : state === "done"
      ? t.journey_status_validated
      : t.journey_v2_drawer_locked;   // ← AVANT : copie hard-stop
```

Par :
```tsx
const stateText =
  state === "current"
    ? t.journey_status_a_rendre
    : state === "done"
      ? t.journey_status_validated
      : t.journey_v2_locked_hint_amber;   // ← APRÈS : copie warn-only
```

Et appliquer la classe ambre warn UNIQUEMENT pour locked :
```tsx
return (
  <div className="eic-journey__tip" style={{ opacity: 1 }}>
    <p className="eic-journey__kicker">{label}</p>
    <p
      className={
        state === "locked"
          ? "eic-journey__tip-body eic-locked-hint--amber"
          : "eic-journey__tip-body"
      }
    >
      {stateText}
    </p>
  </div>
);
```

**B. components/journey-drawer.tsx** — body fallback (lignes 140-145) :

Remplacer :
```tsx
<p style={{ fontSize: 13, color: "var(--wf-ink-soft)", margin: 0 }}>
  {state === "locked"
    ? t.journey_v2_drawer_locked
    : t.journey_no_deliverables}
</p>
```

Par :
```tsx
{state === "locked" ? (
  <p
    className="eic-locked-hint--amber"
    role="note"
    style={{ margin: 0 }}
  >
    {t.journey_v2_locked_hint_amber}
  </p>
) : (
  <p style={{ fontSize: 13, color: "var(--wf-ink-soft)", margin: 0 }}>
    {t.journey_no_deliverables}
  </p>
)}
```

**Contraintes** :
- Ne PAS basculer le ton pour les états `current` ou `done` — eux gardent leur palette neutre/blue/green existante.
- Ne PAS ajouter de nouvelle prop au drawer/HoveredHint — la condition `state === "locked"` est déjà en scope.
- Le `role="note"` sur le drawer body locked est plus correct sémantiquement qu'un simple `<p>` — c'est une annotation contextuelle warn-only, pas un message d'alerte (qui serait `role="alert"` et qui violerait R2).

**Vérification visuelle (post-build)** : démarrer `npm run dev`, naviguer sur `/journey`, cliquer un node locked (L3 si la seed met currentLevel < L3, sinon n'importe quel level locked) → le drawer s'ouvre, le body affiche un encadré ambre avec la copie "Astuce : completez les niveaux precedents...". Hover un node locked sur la track → tooltip ambre apparaît à droite (desktop) ou au-dessus (mobile <720px).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
- `components/journey-client.tsx` HoveredHint : pour `state === "locked"`, rend `t.journey_v2_locked_hint_amber` avec classe `eic-locked-hint--amber` (warn ambre), pas `t.journey_v2_drawer_locked`.
- `components/journey-drawer.tsx` body locked : rend `<p className="eic-locked-hint--amber" role="note">` avec `t.journey_v2_locked_hint_amber`.
- Aucune utilisation de `t.journey_v2_drawer_locked` reste sur les surfaces Player journey (grep `journey_v2_drawer_locked` dans `components/journey-*.tsx` doit retourner 0 match).
- `npm run typecheck` exit 0, `npm run lint` exit 0, `npm run build` exit 0.
- Aucun fichier dans `lib/`, `database/`, `utils/supabase/` modifié (grep `git diff --stat` confirme : 5 fichiers max — i18n.ts + globals.css + 3 composants journey-*).
- Audit visuel manuel : aucune surface Player journey n'utilise `var(--red)`, `var(--red-soft)`, `var(--brand-danger)`, `#DC2626`, `#C44536`, `#a63d2f` (grep ces tokens dans les 3 composants journey-* modifiés → 0 match).
  </done>
</task>

</tasks>

<verification>
**Audit final automatique :**

1. **Lint + typecheck + build** :
   ```bash
   npm run lint && npm run typecheck && npm run build
   ```
   Tous les 3 exit 0.

2. **Audit anti-rouge sur surfaces Player journey** :
   ```bash
   grep -nE "red-soft|--red|--brand-danger|#DC2626|#C44536|#a63d2f|tone=\"rose\"|tone=\"danger\"|severity.*error|className=.*danger" \
     components/journey-level-node.tsx \
     components/journey-client.tsx \
     components/journey-drawer.tsx \
     components/journey-track.tsx \
     components/journey-deliverable-card.tsx \
     components/journey-hero-next-step.tsx
   ```
   → 0 match attendu.

3. **Audit anti-blocage** :
   ```bash
   grep -n "disabled" components/journey-level-node.tsx
   ```
   → 0 match attendu (l'attribut `disabled` doit être complètement retiré du node).

4. **Audit non-régression progression** :
   ```bash
   git diff --stat lib/journey-progression.ts lib/journey.ts database/
   ```
   → 0 ligne modifiée.

5. **Audit copie nouvelle clé présente** :
   ```bash
   grep -n "journey_v2_locked_hint_amber" lib/i18n.ts components/journey-*.tsx
   ```
   → ≥4 matches (2 dans i18n.ts pour fr+en, ≥3 dans composants).

6. **Audit aucune nouvelle dépendance** :
   ```bash
   git diff package.json package-lock.json
   ```
   → vide.
</verification>

<success_criteria>
- [ ] `lib/i18n.ts` contient la nouvelle clé `journey_v2_locked_hint_amber` (FR + EN, plain-ASCII).
- [ ] `app/globals.css` contient `.eic-track__node-tooltip` (variante absolute) ET `.eic-locked-hint--amber` (variante inline) — palette `--wf-amber*` réutilisée, pas de nouveau token.
- [ ] `components/journey-level-node.tsx` : `disabled` retiré, tooltip `<span role="tooltip">` rendu pour locked, `aria-describedby` câblé.
- [ ] `components/journey-client.tsx` HoveredHint locked : copie warn-only ambre.
- [ ] `components/journey-drawer.tsx` body locked : copie warn-only ambre dans `<p role="note" className="eic-locked-hint--amber">`.
- [ ] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.
- [ ] 0 token rouge/danger sur les 6 surfaces Player journey listées dans la vérification.
- [ ] 0 modif dans `lib/journey-progression.ts`, `lib/journey.ts` (logique progression), `database/`.
- [ ] 0 nouvelle dépendance dans `package.json` / `package-lock.json`.
- [ ] Conforme R2 (warn-only) + R3 (pas de blocage codé en dur — node locked focus-able + tooltip déclenchable au clavier).
</success_criteria>

<output>
After completion, create `.planning/quick/260510-j2j-b2-retirer-banner-rouge-l3-et-remplacer-/260510-j2j-SUMMARY.md` documenting :
- Avant/après ton visuel des 3 surfaces (node hover tooltip, hovered hint dans la 3rd col, drawer body locked).
- Audit anti-rouge (grep tokens) résultat 0 match.
- Confirmation `disabled` retiré du node, focus clavier OK, tooltip déclenché au focus.
- Mention explicite : aucune touche à la logique de progression (`lib/journey-progression.ts` / `lib/journey.ts` / `database/` non modifiés).
- Commit hash final.
</output>
