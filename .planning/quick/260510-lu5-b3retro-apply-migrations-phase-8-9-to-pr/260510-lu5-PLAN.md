---
phase: quick-260510-lu5
plan: 01
type: ops
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260508222155_initial_schema.sql
  - supabase/migrations/20260508222224_initial_rls.sql
  - supabase/migrations/20260510140000_phase08_mentor_comments.sql
  - supabase/migrations/20260510140001_phase09_gamemaster_live.sql
  - supabase/.gitignore
autonomous: true
requirements:
  - B3-RETRO
must_haves:
  truths:
    - "`evaluation_comments` table existe en prod Supabase (Phase 8 / MNT-03)."
    - "`evaluations.expected_action` column existe en prod (Phase 8 / MNT-04) avec CHECK constraint NOT VALID."
    - "`announcements` table existe en prod (Phase 9 / GMR-09) avec 4 RLS policies."
    - "`deliverable_templates.is_active` column existe en prod (Phase 9 / GMR-06) avec default true."
    - "Supabase CLI scaffolding committé pour futures migrations versionnées."
    - "Local migration history alignée avec remote (4 entries)."
---

<objective>
B3 rétro (CLAUDE.md T-3 Critical Gates) — Apply migrations Phase 8 + 9 sur prod Supabase. Bloquant runtime au 13/05 8h30 si non appliqué (`addEvaluationCommentFlow` et `/admin/announce` crash sans ces tables).
</objective>

<context>
**Toolchain initial** :
- `supabase` CLI : non installé global (npm i -g non supporté par Supabase)
- `psql` : non installé
- `supabase/` directory : `.temp/` only (project déjà linké à `vzzbjxmfkmvqkaqxalhr`)
- `.env.local` : 3 keys (URL + ANON + SERVICE_ROLE) — pas de DATABASE_URL direct

**Solution toolchain retenue** : `npx supabase` (run-on-demand, pas d'install permanent, v2.98.2 confirmée).

**Migrations source** :
- `database/migrations/08-mentor-comments.sql` (Phase 8, MNT-03 + MNT-04)
- `database/migrations/09-gamemaster-live.sql` (Phase 9, GMR-04..09 / Agent 9A)

**Migrations cibles CLI format** (timestamp prefix obligatoire) :
- `supabase/migrations/20260510140000_phase08_mentor_comments.sql` (copie 08)
- `supabase/migrations/20260510140001_phase09_gamemaster_live.sql` (copie 09)

**Stubs d'alignement** : 2 entries remote pré-existantes (`20260508222155`, `20260508222224`) dans `supabase_migrations.schema_migrations` — créées par un init CLI antérieur ou lors de l'apply manuel de schema/triggers/rls.sql. Stubs locaux vides créés pour aligner sans casser l'historique.
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Setup Supabase CLI scaffolding + stage migrations</name>
  <action>
1. `npx supabase init` → crée `supabase/config.toml`
2. `mkdir -p supabase/migrations`
3. Copy `database/migrations/08-mentor-comments.sql` → `supabase/migrations/20260510140000_phase08_mentor_comments.sql`
4. Copy `database/migrations/09-gamemaster-live.sql` → `supabase/migrations/20260510140001_phase09_gamemaster_live.sql`
5. Create alignment stubs `20260508222155_initial_schema.sql` + `20260508222224_initial_rls.sql` (vide avec commentaire)
6. Add `supabase/.gitignore` (`.temp/`, `.branches/`)
  </action>
  <done>
- `supabase/config.toml` présent
- `supabase/migrations/` contient 4 .sql files
- `npx supabase migration list --linked` montre alignement local↔remote (4 entries chacun)
  </done>
</task>

<task type="auto">
  <name>Task 2 — Apply migrations to PROD Supabase</name>
  <action>
1. `export SUPABASE_ACCESS_TOKEN=<PAT>` (Omar fournit, à révoquer après)
2. `npx supabase db push --linked --password $DB_PWD --dry-run` → vérifie que les 2 nouvelles migrations sont les seules pending
3. `npx supabase db push --linked --password $DB_PWD` → vrai apply
4. `npx supabase migration list --linked` → vérifie que les 4 entries sont alignées local=remote
  </action>
  <verify>
    <automated>npx supabase migration list --linked --password $DB_PWD</automated>
  </verify>
  <done>
- 2 migrations appliquées sans erreur (NOTICEs `does not exist, skipping` normaux pour 1ère apply)
- `migration list` montre 4 entries alignées
- `evaluation_comments`, `announcements` tables existent en prod
- `evaluations.expected_action`, `deliverable_templates.is_active` columns existent en prod
- 7 RLS policies créées (3 evaluation_comments + 4 announcements)
  </done>
</task>

</tasks>

<verification>
**Audit final :**

1. **Migration alignment** : `npx supabase migration list --linked` → 4 entries Local=Remote.
2. **Idempotency** : un re-apply via `db push` doit être no-op (les `if not exists` / `drop policy if exists` garantissent).
3. **Pas de fichier secret committé** : `.env.local`, `supabase/.temp/` exclus du commit.
4. **Repo state** : `git diff --stat` montre uniquement supabase/* + .planning/* (pas de touche au code application).
</verification>

<security_notes>
**Credentials manipulées** :
- Personal Access Token Supabase (PAT) : utilisé pour authentifier le CLI auprès de l'API Supabase. **À RÉVOQUER après usage** sur https://supabase.com/dashboard/account/tokens. Le PAT est stocké uniquement en mémoire de session (non commité).
- DB password : utilisé pour le push direct DDL via le pooler. Reste local. **Reset recommandé après pilote** si sensibilité élevée.

**Surface d'exposition** :
- Le PAT et le DB password ont été passés dans le contexte conversation (loggé Anthropic). Révocation post-session = mitigation propre.
- Aucun secret n'est committé dans le repo.
- `supabase/.temp/` (linked-project.json, project-ref) gitignored — pas de fuite.
</security_notes>

<success_criteria>
- [ ] `supabase/config.toml` committé
- [ ] 4 migration files committés dans `supabase/migrations/`
- [ ] `supabase/.gitignore` exclut `.temp/` et `.branches/`
- [ ] Apply confirmée par `migration list --linked` (4 alignées)
- [ ] PAT révoqué après session (action Omar)
</success_criteria>
