---
id: 260512-24v
title: "FAB \"Call mentor\" Player full in-app — help_requests table + RLS + bell badges + composer"
status: planned
scope: quick
created: 2026-05-12
type: feature
risk: medium
context_target: ~35%
commits: 5
files_modified:
  - supabase/migrations/20260512XXXXXX_help_requests.sql
  - lib/types.ts
  - lib/i18n.ts
  - app/actions.ts
  - components/app-shell.tsx
  - components/help/HelpCallFab.tsx
  - components/help/HelpRequestComposer.tsx
  - components/help/HelpInbox.tsx
  - components/help/HelpInboxBell.tsx
  - app/mentor/page.tsx
  - app/admin/page.tsx
  - lib/help-requests.ts
cardinal_audit:
  R1: NA (no score/rank surfaces; help feature is pedagogical-empathic, never displays XP, scores or ranking)
  R2: NA (no validators introduced)
  R3: OK — FAB is always-active across Player pages; NO conditional `disabled`, NO `blocks_progression_to`, NO hardcoded inter-mission gating. Feature is purely additive.
  dual_mode: ENFORCED — `hasSupabaseEnv()` checked before every Supabase call. Demo mode keeps FAB visible & composer interactive; submit returns `{ ok: true, message: "Mode démo — requête simulée." }` and inbox lists are empty in demo.
---

## Goal

Donner au Player une voie directe et empathique pour appeler un mentor depuis n'importe quelle page du parcours, sans quitter l'app. Mentor et GameMaster reçoivent les demandes dans une boîte de réception persistante avec badge non-lu temps-réel (via revalidatePath).

**Pourquoi maintenant (T-1 pilote 13/05)** : la `mailto:` flow existante quitte l'app, perd la trace, et n'a pas de badge. Pendant un Hack-Days de 6-15 équipes, un Player bloqué doit pouvoir appeler à l'aide en 1 clic, le mentor doit voir ça immédiatement. Le risque T-1 est borné : feature 100% additive (1 table neuve, 0 modif existant), rollback trivial via `git revert <sha>`.

**Empathie pédagogique** (R1/R2/R3) :
- Aucune mention d'XP, score, pénalité, "tu perds des points si…"
- Tone : "Besoin d'un coup de pouce ?", "Reçu — un mentor va te répondre rapidement."
- Pas de timeout/escalation auto (manuel-mentor only)
- Acknowledged ≠ Resolved (mentor confirme réception, puis confirme résolution séparément)

## Tasks

### Task 1 — DB schema + RLS migration

<files>
- supabase/migrations/20260512XXXXXX_help_requests.sql (new — substitute XXXXXX with timestamp HHMMSS like 100000)
</files>

<action>
Créer fichier migration nommé `supabase/migrations/20260512100000_help_requests.sql` avec :

```sql
-- help_requests : Player calls for mentor support during the Hack-Days.
-- Pedagogical-empathic feature (R1 NA, R2 NA, R3 OK — purely additive, never gates progression).

create type public.help_request_status as enum ('open', 'acknowledged', 'resolved');

create table public.help_requests (
  id uuid primary key default gen_random_uuid(),
  player_id uuid not null references public.players(id) on delete cascade,
  requested_by uuid not null references public.profiles(user_id) on delete set null,
  message text not null check (char_length(message) between 1 and 500),
  status public.help_request_status not null default 'open',
  created_at timestamptz not null default now(),
  acknowledged_at timestamptz,
  acknowledged_by uuid references public.profiles(user_id),
  resolved_at timestamptz,
  resolved_by uuid references public.profiles(user_id),
  updated_at timestamptz not null default now()
);
comment on table public.help_requests is 'Player calls for mentor support — in-app inbox with status lifecycle (open → acknowledged → resolved).';

create index help_requests_status_created_idx on public.help_requests(status, created_at desc);
create index help_requests_player_idx on public.help_requests(player_id);

-- updated_at trigger (reuse existing helper if defined, else inline)
create or replace function public.set_help_requests_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.updated_at := now();
  return new;
end;
$$;

create trigger trg_help_requests_updated_at
  before update on public.help_requests
  for each row execute function public.set_help_requests_updated_at();

-- RLS
alter table public.help_requests enable row level security;

-- Player: SELECT + INSERT only their own (via player_members membership)
create policy "help_requests_player_select_own" on public.help_requests
  for select to authenticated
  using (public.is_my_player(player_id));

create policy "help_requests_player_insert_own" on public.help_requests
  for insert to authenticated
  with check (
    public.is_my_player(player_id)
    and requested_by = auth.uid()
  );

-- Mentor + GameMaster: SELECT all (mentor sees all open requests cohort-wide for J1/J2 pilote)
create policy "help_requests_mentor_select_all" on public.help_requests
  for select to authenticated
  using (public.is_mentor());

-- Mentor + GameMaster: UPDATE (acknowledge / resolve)
create policy "help_requests_mentor_update" on public.help_requests
  for update to authenticated
  using (public.is_mentor())
  with check (public.is_mentor());

-- Table-level GRANT (lesson learned from F-16-01 evaluation_comments)
grant select, insert, update on public.help_requests to authenticated;
```

**Notes** :
- `is_my_player(player_id)` réutilise le helper existant `database/rls.sql:41-54`.
- `is_mentor()` couvre déjà `mentor` + `game_master` (cf. `database/rls.sql:31-39`).
- `grant select, insert, update` table-level **obligatoire** (F-16-01 rétro 2026-05-11 : RLS seule ne suffit pas, il faut le GRANT).
- Pas de policy DELETE — GameMaster pourra purger via service role ou ne purgera pas (volume pilote négligeable).

Appliquer via `supabase mcp apply_migration` (preferred) OU `npx supabase db push` si CLI déjà câblé.
</action>

<verify>
- `npx supabase migration list` affiche le nouveau timestamp local + remote
- Via MCP : `list_tables` retourne `help_requests` avec 4 policies + 3 GRANTs
- Manual SQL test : `select * from help_requests limit 1;` ne renvoie pas d'erreur RLS pour un user mentor connecté
</verify>

<done>
Migration appliquée local + PROD Supabase. Table `help_requests` visible avec RLS activée et 4 policies (player_select_own, player_insert_own, mentor_select_all, mentor_update). Commit atomique : `feat(quick-260512-24v): add help_requests table + RLS for in-app mentor calls`.
</done>

---

### Task 2 — Domain types + i18n keys + server actions

<files>
- lib/types.ts (add `HelpRequestStatus` + `HelpRequest` types)
- lib/i18n.ts (add 10 keys under `fr` + `en` if EN dict exists)
- app/actions.ts (add 3 server actions)
- lib/help-requests.ts (new — query helpers for inbox + unread count)
</files>

<action>
**1. `lib/types.ts`** — ajouter après le bloc MoscowCard (fin du fichier) :

```ts
// ============================================================================
// Help Requests (Player → Mentor in-app calls — quick-260512-24v)
// Mirror supabase/migrations/20260512100000_help_requests.sql
// R1 NA · R2 NA · R3 OK (purely additive, never gates progression)
// ============================================================================

export type HelpRequestStatus = "open" | "acknowledged" | "resolved";

export type HelpRequest = {
  id: string;
  playerId: string;
  requestedBy: string;
  message: string;
  status: HelpRequestStatus;
  createdAt: string;
  acknowledgedAt: string | null;
  acknowledgedBy: string | null;
  resolvedAt: string | null;
  resolvedBy: string | null;
  updatedAt: string;
};
```

**2. `lib/i18n.ts`** — ajouter sous bloc `fr:` (chercher la fin de la section player/mobile_tab_help et y greffer). Toutes les valeurs FR sans diacritique problématique (cf. convention CLAUDE.md i18n — strings ASCII-safe pour mailto/CSV) :

```ts
help_fab_label: "Besoin d'un coup de pouce ?",
help_fab_aria: "Appeler un mentor",
help_composer_title: "Demande de coup de pouce",
help_composer_subtitle: "Un mentor te répondra rapidement. Pas de pénalité, c'est fait pour ça.",
help_composer_placeholder: "Sur quelle mission tu bloques ? Décris en 1-2 phrases ce qui te bloque…",
help_composer_submit: "Envoyer au mentor",
help_composer_submitting: "Envoi en cours…",
help_composer_success: "Reçu — un mentor va te répondre rapidement.",
help_composer_demo: "Mode démo — requête simulée.",
help_composer_cancel: "Annuler",
help_composer_char_remaining: "caractères restants",
help_inbox_bell_aria: "Demandes d'aide",
help_inbox_title: "Demandes d'aide",
help_inbox_empty: "Aucune demande d'aide ouverte.",
help_inbox_status_open: "Ouverte",
help_inbox_status_acknowledged: "Vue",
help_inbox_status_resolved: "Résolue",
help_inbox_acknowledge: "Marquer comme vue",
help_inbox_resolve: "Marquer comme résolue",
help_inbox_from: "de",
```

**3. `lib/help-requests.ts` (NEW)** — query layer dual-mode :

```ts
import { createClient } from "@/utils/supabase/server";
import { hasSupabaseEnv } from "@/lib/supabase-status";
import type { HelpRequest, HelpRequestStatus } from "@/lib/types";

type RawHelpRequest = {
  id: string;
  player_id: string;
  requested_by: string;
  message: string;
  status: HelpRequestStatus;
  created_at: string;
  acknowledged_at: string | null;
  acknowledged_by: string | null;
  resolved_at: string | null;
  resolved_by: string | null;
  updated_at: string;
};

function mapRow(row: RawHelpRequest): HelpRequest {
  return {
    id: row.id,
    playerId: row.player_id,
    requestedBy: row.requested_by,
    message: row.message,
    status: row.status,
    createdAt: row.created_at,
    acknowledgedAt: row.acknowledged_at,
    acknowledgedBy: row.acknowledged_by,
    resolvedAt: row.resolved_at,
    resolvedBy: row.resolved_by,
    updatedAt: row.updated_at,
  };
}

export async function listHelpRequests(opts?: { onlyUnresolved?: boolean }): Promise<HelpRequest[]> {
  if (!hasSupabaseEnv()) return [];
  const supabase = await createClient();
  if (!supabase) return [];
  let q = supabase.from("help_requests").select("*").order("created_at", { ascending: false });
  if (opts?.onlyUnresolved) {
    q = q.in("status", ["open", "acknowledged"]);
  }
  const { data, error } = await q;
  if (error || !data) return [];
  return (data as RawHelpRequest[]).map(mapRow);
}

export async function countUnreadHelpRequests(): Promise<number> {
  if (!hasSupabaseEnv()) return 0;
  const supabase = await createClient();
  if (!supabase) return 0;
  const { count, error } = await supabase
    .from("help_requests")
    .select("*", { count: "exact", head: true })
    .eq("status", "open");
  if (error) return 0;
  return count ?? 0;
}

export async function getPlayerIdForCurrentUser(): Promise<string | null> {
  if (!hasSupabaseEnv()) return null;
  const supabase = await createClient();
  if (!supabase) return null;
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return null;
  const { data } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  return data?.player_id ?? null;
}
```

**4. `app/actions.ts`** — ajouter à la fin du fichier 3 server actions :

```ts
// ============================================================================
// Help Requests (quick-260512-24v) — Player calls for mentor support
// ============================================================================

const helpRequestSchema = z.object({
  message: z.string().min(1).max(500),
});

export async function createHelpRequestFlow(
  _prev: WorkflowState,
  formData: FormData,
): Promise<WorkflowState> {
  const parsed = helpRequestSchema.safeParse({ message: formData.get("message") });
  if (!parsed.success) {
    return { ok: false, message: parsed.error.issues[0]?.message ?? "Message invalide.", severity: "error" };
  }
  if (!hasSupabaseEnv()) {
    return { ok: true, message: "Mode démo — requête simulée.", severity: "warn" };
  }
  const supabase = await createClient();
  if (!supabase) {
    return { ok: false, message: "Backend non configuré.", severity: "error" };
  }
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "Non authentifié.", severity: "error" };

  const { data: membership } = await supabase
    .from("player_members")
    .select("player_id")
    .eq("user_id", user.id)
    .maybeSingle();
  if (!membership) {
    return { ok: false, message: "Aucun Player rattaché à votre compte.", severity: "error" };
  }

  const { error } = await supabase.from("help_requests").insert({
    player_id: membership.player_id,
    requested_by: user.id,
    message: parsed.data.message,
  });
  if (error) {
    return { ok: false, message: error.message, severity: "error" };
  }
  revalidatePath("/mentor");
  revalidatePath("/admin");
  revalidatePath("/journey");
  return { ok: true, message: "Reçu — un mentor va te répondre rapidement.", severity: "ok" };
}

const helpIdSchema = z.object({ id: z.string().uuid() });

export async function acknowledgeHelpRequest(formData: FormData): Promise<void> {
  if (!hasSupabaseEnv()) return;
  const parsed = helpIdSchema.safeParse({ id: formData.get("id") });
  if (!parsed.success) return;
  const supabase = await createClient();
  if (!supabase) return;
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return;
  await supabase
    .from("help_requests")
    .update({ status: "acknowledged", acknowledged_at: new Date().toISOString(), acknowledged_by: user.id })
    .eq("id", parsed.data.id)
    .eq("status", "open"); // idempotent — only transitions open→acknowledged
  revalidatePath("/mentor");
  revalidatePath("/admin");
}

export async function resolveHelpRequest(formData: FormData): Promise<void> {
  if (!hasSupabaseEnv()) return;
  const parsed = helpIdSchema.safeParse({ id: formData.get("id") });
  if (!parsed.success) return;
  const supabase = await createClient();
  if (!supabase) return;
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return;
  await supabase
    .from("help_requests")
    .update({ status: "resolved", resolved_at: new Date().toISOString(), resolved_by: user.id })
    .eq("id", parsed.data.id)
    .in("status", ["open", "acknowledged"]);
  revalidatePath("/mentor");
  revalidatePath("/admin");
}
```

**Notes** :
- `createHelpRequestFlow` retourne `WorkflowState` pour `useActionState` côté composer.
- `acknowledgeHelpRequest` / `resolveHelpRequest` retournent `void` (legacy pattern, invoked as `<form action={...}>` button-only in inbox).
- Idempotence via `.eq("status", "open")` / `.in("status", [...])` — re-clic ne casse pas l'état.
</action>

<verify>
- `npm run typecheck` → 0 erreur
- `npm run lint` → 0 erreur sur les fichiers touchés
- Grep R3 sanity : `grep -rn "blocks_progression\|disabled" lib/help-requests.ts app/actions.ts | grep -i help` doit être vide
</verify>

<done>
Types + i18n + 3 server actions + query helpers livrés. `npm run typecheck && npm run lint` clean. Commit atomique : `feat(quick-260512-24v): help_requests types, i18n, server actions + query layer`.
</done>

---

### Task 3 — Player FAB + composer modal

<files>
- components/help/HelpCallFab.tsx (new — client component)
- components/help/HelpRequestComposer.tsx (new — client component, modal)
- components/app-shell.tsx (mount FAB in player variant)
- app/globals.css (FAB positioning utility class .eic-help-fab + modal .eic-help-modal — minimal additive CSS)
</files>

<action>
**1. `components/help/HelpRequestComposer.tsx` (NEW)** — modal client avec `useActionState` :

```tsx
"use client";

import { useActionState, useEffect, useRef, useState } from "react";
import { createHelpRequestFlow } from "@/app/actions";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;
const initialState = { ok: false, message: "" };
const MAX_LEN = 500;

export function HelpRequestComposer({ onClose }: { onClose: () => void }) {
  const [state, formAction, pending] = useActionState(createHelpRequestFlow, initialState);
  const [message, setMessage] = useState("");
  const dialogRef = useRef<HTMLDivElement>(null);

  // Close on success after 1.5s so user sees feedback
  useEffect(() => {
    if (state.ok) {
      const t = setTimeout(onClose, 1500);
      return () => clearTimeout(t);
    }
  }, [state.ok, onClose]);

  // ESC to close
  useEffect(() => {
    const handler = (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, [onClose]);

  const remaining = MAX_LEN - message.length;

  return (
    <div className="eic-help-modal" role="dialog" aria-modal="true" aria-labelledby="help-modal-title" ref={dialogRef}>
      <div className="eic-help-modal__backdrop" onClick={onClose} aria-hidden="true" />
      <div className="eic-help-modal__panel">
        <h2 id="help-modal-title" className="eic-help-modal__title">{t.help_composer_title}</h2>
        <p className="eic-help-modal__subtitle">{t.help_composer_subtitle}</p>
        <form action={formAction} className="eic-help-modal__form">
          <label className="eic-help-modal__label">
            <textarea
              name="message"
              className="eic-help-modal__textarea"
              placeholder={t.help_composer_placeholder}
              maxLength={MAX_LEN}
              minLength={1}
              required
              rows={4}
              value={message}
              onChange={(e) => setMessage(e.target.value)}
              disabled={state.ok || pending}
              aria-describedby="help-char-count"
            />
            <span id="help-char-count" className="eic-help-modal__counter" aria-live="polite">
              {remaining} {t.help_composer_char_remaining}
            </span>
          </label>
          {state.message ? (
            <p className={state.ok ? "eic-help-modal__success" : "eic-help-modal__error"} role="status">
              {state.message}
            </p>
          ) : null}
          <div className="eic-help-modal__actions">
            <button type="button" className="eic-help-modal__cancel" onClick={onClose} disabled={pending}>
              {t.help_composer_cancel}
            </button>
            <button type="submit" className="eic-help-modal__submit" disabled={pending || state.ok || message.length === 0}>
              {pending ? t.help_composer_submitting : t.help_composer_submit}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
}
```

**2. `components/help/HelpCallFab.tsx` (NEW)** — FAB fixed bottom-right :

```tsx
"use client";

import { LifeBuoy } from "lucide-react";
import { useState } from "react";
import { HelpRequestComposer } from "@/components/help/HelpRequestComposer";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;

export function HelpCallFab() {
  const [open, setOpen] = useState(false);
  return (
    <>
      <button
        type="button"
        className="eic-help-fab"
        aria-label={t.help_fab_aria}
        title={t.help_fab_label}
        onClick={() => setOpen(true)}
      >
        <LifeBuoy aria-hidden="true" size={20} />
        <span className="eic-help-fab__label">{t.help_fab_label}</span>
      </button>
      {open ? <HelpRequestComposer onClose={() => setOpen(false)} /> : null}
    </>
  );
}
```

**3. `components/app-shell.tsx`** — ajouter mount FAB dans le bloc `variant === "player"` :

```tsx
import { HelpCallFab } from "@/components/help/HelpCallFab";
// ...
if (resolvedVariant === "player") {
  return (
    <div className="eic-shell eic-shell--player">
      <TopbarLite ... />
      <main className="eic-shell__main">{children}</main>
      {hideTabBar ? null : <MobileTabBar items={playerTabs} />}
      <HelpCallFab />
    </div>
  );
}
```

**Important** : NE PAS toucher au `StaffShell` (mentor/admin n'ont pas besoin du FAB côté Player).

**4. `app/globals.css`** — ajouter à la fin (additif, pas de modif existant) :

```css
/* quick-260512-24v: help FAB + composer modal */
.eic-help-fab {
  position: fixed;
  right: 1.25rem;
  bottom: 5rem; /* above MobileTabBar */
  z-index: 40;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.1rem;
  border-radius: 999px;
  background: var(--eic-orange, #f97316);
  color: #fff;
  font-weight: 600;
  font-size: 0.875rem;
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.18);
  border: none;
  cursor: pointer;
  transition: transform 120ms ease, box-shadow 120ms ease;
}
.eic-help-fab:hover { transform: translateY(-1px); box-shadow: 0 8px 24px rgba(0, 0, 0, 0.22); }
.eic-help-fab:focus-visible { outline: 2px solid #fff; outline-offset: 2px; }
@media (max-width: 640px) {
  .eic-help-fab__label { display: none; }
  .eic-help-fab { padding: 0.85rem; bottom: 5.5rem; }
}

.eic-help-modal { position: fixed; inset: 0; z-index: 60; display: grid; place-items: center; padding: 1rem; }
.eic-help-modal__backdrop { position: absolute; inset: 0; background: rgba(15, 23, 42, 0.55); }
.eic-help-modal__panel { position: relative; background: #fff; border-radius: 1rem; padding: 1.5rem; max-width: 480px; width: 100%; box-shadow: 0 20px 60px rgba(0, 0, 0, 0.28); }
.eic-help-modal__title { font-size: 1.25rem; font-weight: 700; margin: 0 0 0.25rem; }
.eic-help-modal__subtitle { font-size: 0.9rem; color: #64748b; margin: 0 0 1rem; }
.eic-help-modal__form { display: flex; flex-direction: column; gap: 0.75rem; }
.eic-help-modal__textarea { width: 100%; padding: 0.75rem; border-radius: 0.5rem; border: 1px solid #cbd5e1; font: inherit; resize: vertical; min-height: 96px; }
.eic-help-modal__textarea:focus { outline: 2px solid var(--eic-orange, #f97316); outline-offset: 1px; }
.eic-help-modal__counter { display: block; font-size: 0.75rem; color: #64748b; text-align: right; margin-top: 0.25rem; }
.eic-help-modal__success { color: #15803d; font-weight: 600; margin: 0; }
.eic-help-modal__error { color: #b91c1c; font-weight: 600; margin: 0; }
.eic-help-modal__actions { display: flex; gap: 0.5rem; justify-content: flex-end; margin-top: 0.5rem; }
.eic-help-modal__cancel, .eic-help-modal__submit { padding: 0.6rem 1rem; border-radius: 0.5rem; font-weight: 600; cursor: pointer; border: none; }
.eic-help-modal__cancel { background: #e2e8f0; color: #0f172a; }
.eic-help-modal__submit { background: var(--eic-orange, #f97316); color: #fff; }
.eic-help-modal__submit:disabled { opacity: 0.5; cursor: not-allowed; }
@media (prefers-reduced-motion: reduce) {
  .eic-help-fab { transition: none; }
}
```

**Pre-edit R3 guard** : FAB est always-visible côté Player, JAMAIS conditionnel sur stuck-state ou level. C'est de l'aide proactive, pas du blocage réactif. Confirmer absence de `disabled` lié à level/mission dans `HelpCallFab.tsx`.
</action>

<verify>
- `npm run typecheck` → 0 erreur
- `npm run lint` → 0 erreur
- `npm run build` → succès
- Visual local (`npm run dev`) : FAB visible bottom-right sur `/journey`, `/journey/deliverable/[id]`, `/onboarding` — invisible sur `/login`, `/mentor`, `/admin`, `/jury`
- Click FAB → modal s'ouvre, ESC ferme, backdrop-click ferme, char counter live, submit en mode démo → message "Mode démo — requête simulée." puis auto-close 1.5s
- Grep R3 sanity : `grep -rn "level\|status\|stuck\|disabled" components/help/HelpCallFab.tsx` → seule la prop `disabled` du bouton submit (lié à pending/empty message) doit apparaître, AUCUNE référence à level/status/stuck
</verify>

<done>
FAB orange bottom-right visible toutes les pages Player. Composer modal fonctionnel avec char counter live + a11y (ESC, aria-modal, aria-live). Demo mode renvoie message simulé. Commit atomique : `feat(quick-260512-24v): Player FAB + help composer modal (dual-mode safe)`.
</done>

---

### Task 4 — Mentor + Admin inbox + bell badge

<files>
- components/help/HelpInbox.tsx (new — server component)
- components/help/HelpInboxBell.tsx (new — server component, fetches count)
- app/mentor/page.tsx (insert <HelpInboxBell /> + <HelpInbox /> at top of page)
- app/admin/page.tsx (insert <HelpInboxBell /> + <HelpInbox /> at top of page)
</files>

<action>
**1. `components/help/HelpInboxBell.tsx` (NEW)** — server component avec count badge :

```tsx
import { Bell } from "lucide-react";
import { countUnreadHelpRequests } from "@/lib/help-requests";
import { dictionaries } from "@/lib/i18n";

const t = dictionaries.fr;

export async function HelpInboxBell() {
  const count = await countUnreadHelpRequests();
  return (
    <span
      className="eic-help-bell"
      aria-label={`${t.help_inbox_bell_aria} (${count})`}
      role="status"
    >
      <Bell aria-hidden="true" size={18} />
      {count > 0 ? <span className="eic-help-bell__badge">{count}</span> : null}
      <span className="eic-help-bell__label">{t.help_inbox_title}</span>
    </span>
  );
}
```

**2. `components/help/HelpInbox.tsx` (NEW)** — server component liste + actions :

```tsx
import { listHelpRequests } from "@/lib/help-requests";
import { acknowledgeHelpRequest, resolveHelpRequest } from "@/app/actions";
import { dictionaries } from "@/lib/i18n";
import type { HelpRequest } from "@/lib/types";

const t = dictionaries.fr;

function formatRelative(iso: string): string {
  const ts = new Date(iso).getTime();
  const diff = Date.now() - ts;
  const min = Math.floor(diff / 60000);
  if (min < 1) return "à l'instant";
  if (min < 60) return `il y a ${min} min`;
  const h = Math.floor(min / 60);
  if (h < 24) return `il y a ${h}h`;
  return new Date(iso).toLocaleDateString("fr-FR");
}

function StatusBadge({ status }: { status: HelpRequest["status"] }) {
  const label =
    status === "open" ? t.help_inbox_status_open :
    status === "acknowledged" ? t.help_inbox_status_acknowledged :
    t.help_inbox_status_resolved;
  return <span className={`eic-help-inbox__status eic-help-inbox__status--${status}`}>{label}</span>;
}

export async function HelpInbox() {
  const requests = await listHelpRequests({ onlyUnresolved: true });
  if (requests.length === 0) {
    return (
      <section className="eic-help-inbox eic-help-inbox--empty" aria-label={t.help_inbox_title}>
        <h3 className="eic-help-inbox__title">{t.help_inbox_title}</h3>
        <p className="eic-help-inbox__empty">{t.help_inbox_empty}</p>
      </section>
    );
  }
  return (
    <section className="eic-help-inbox" aria-label={t.help_inbox_title}>
      <h3 className="eic-help-inbox__title">{t.help_inbox_title} ({requests.length})</h3>
      <ul className="eic-help-inbox__list">
        {requests.map((r) => (
          <li key={r.id} className="eic-help-inbox__item">
            <header className="eic-help-inbox__header">
              <StatusBadge status={r.status} />
              <span className="eic-help-inbox__time">{formatRelative(r.createdAt)}</span>
            </header>
            <p className="eic-help-inbox__message">{r.message}</p>
            <footer className="eic-help-inbox__actions">
              {r.status === "open" ? (
                <form action={acknowledgeHelpRequest}>
                  <input type="hidden" name="id" value={r.id} />
                  <button type="submit" className="eic-help-inbox__btn eic-help-inbox__btn--ack">
                    {t.help_inbox_acknowledge}
                  </button>
                </form>
              ) : null}
              <form action={resolveHelpRequest}>
                <input type="hidden" name="id" value={r.id} />
                <button type="submit" className="eic-help-inbox__btn eic-help-inbox__btn--resolve">
                  {t.help_inbox_resolve}
                </button>
              </form>
            </footer>
          </li>
        ))}
      </ul>
    </section>
  );
}
```

**3. `app/mentor/page.tsx`** — repérer l'entête de la page (probablement un `<h1>` ou un wrapper container), insérer **avant** le contenu existant :

```tsx
import { HelpInboxBell } from "@/components/help/HelpInboxBell";
import { HelpInbox } from "@/components/help/HelpInbox";
// ...
// In the page body, near top:
<div className="eic-mentor-helpbar">
  <HelpInboxBell />
</div>
<HelpInbox />
{/* existing content below */}
```

**4. `app/admin/page.tsx`** — même pattern : insertion en haut de page.

**5. `app/globals.css`** — ajouter styles inbox+bell (additif) :

```css
/* quick-260512-24v: help inbox + bell */
.eic-help-bell { display: inline-flex; align-items: center; gap: 0.4rem; padding: 0.4rem 0.7rem; background: #fff7ed; color: #c2410c; border-radius: 999px; font-weight: 600; font-size: 0.85rem; position: relative; }
.eic-help-bell__badge { display: inline-grid; place-items: center; min-width: 1.25rem; height: 1.25rem; padding: 0 0.35rem; border-radius: 999px; background: #dc2626; color: #fff; font-size: 0.7rem; font-weight: 700; }
.eic-help-inbox { margin: 1rem 0; padding: 1rem; background: #fff; border-radius: 0.75rem; border: 1px solid #e2e8f0; }
.eic-help-inbox--empty { background: #f8fafc; }
.eic-help-inbox__title { margin: 0 0 0.75rem; font-size: 1rem; font-weight: 700; color: #0f172a; }
.eic-help-inbox__empty { margin: 0; color: #64748b; font-size: 0.9rem; }
.eic-help-inbox__list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.75rem; }
.eic-help-inbox__item { padding: 0.85rem; border: 1px solid #e2e8f0; border-radius: 0.5rem; background: #fefefe; }
.eic-help-inbox__header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem; }
.eic-help-inbox__status { font-size: 0.7rem; font-weight: 700; padding: 0.15rem 0.5rem; border-radius: 999px; text-transform: uppercase; letter-spacing: 0.04em; }
.eic-help-inbox__status--open { background: #fee2e2; color: #b91c1c; }
.eic-help-inbox__status--acknowledged { background: #fef3c7; color: #92400e; }
.eic-help-inbox__status--resolved { background: #dcfce7; color: #166534; }
.eic-help-inbox__time { font-size: 0.75rem; color: #64748b; }
.eic-help-inbox__message { margin: 0 0 0.6rem; color: #0f172a; font-size: 0.9rem; white-space: pre-wrap; }
.eic-help-inbox__actions { display: flex; gap: 0.5rem; }
.eic-help-inbox__btn { padding: 0.4rem 0.75rem; border-radius: 0.4rem; border: none; font-weight: 600; font-size: 0.8rem; cursor: pointer; }
.eic-help-inbox__btn--ack { background: #fef3c7; color: #92400e; }
.eic-help-inbox__btn--resolve { background: #dcfce7; color: #166534; }
```

**Note** : `<HelpInboxBell />` côté mentor/admin s'auto-refresh via `revalidatePath("/mentor")` + `revalidatePath("/admin")` déclenché par `createHelpRequestFlow` côté Player. Pas de WebSocket / Realtime nécessaire (cf. constraint "No-Realtime").
</action>

<verify>
- `npm run typecheck` → 0 erreur
- `npm run lint` → 0 erreur
- `npm run build` → succès
- Visual local (`npm run dev`) : connecté en mentor → `/mentor` affiche `<HelpInboxBell />` (badge 0 ou count) + `<HelpInbox />` (liste vide en mode démo)
- Si Supabase configuré : Player envoie une demande → après `revalidatePath` côté mentor (refresh manuel suffit), inbox affiche la demande avec boutons "Marquer comme vue" + "Marquer comme résolue"
- Clic "Marquer comme vue" → status passe `open` → `acknowledged`, badge décrémente
- Clic "Marquer comme résolue" → disparaît de la liste (filtre `onlyUnresolved`)
</verify>

<done>
Inbox + bell visibles sur `/mentor` et `/admin`. Liste vide en demo. Server actions acknowledge/resolve fonctionnelles, idempotentes. Commit atomique : `feat(quick-260512-24v): mentor + admin help inbox with bell badge`.
</done>

---

### Task 5 — Smoke regression + visual sanity + commit final

<files>
- (no code edits — verification only, then SUMMARY.md + deferred-items.md committed in Step 6 outside this plan)
</files>

<action>
**Smoke régression complet** (T-1 du pilote, critique) :

1. **Static checks** :
   - `npm run typecheck` → 0 erreur
   - `npm run lint` → 0 erreur sur tous les fichiers
   - `npm run build` → succès, taille bundle raisonnable (le FAB ajoute <5KB gzipped attendus)

2. **Visual régression Player** (mode démo, `npm run dev`) :
   - `/login` → FAB **invisible** (pas dans AppShell)
   - `/onboarding` → FAB **visible** bottom-right
   - `/journey` → FAB visible + Cohort Pulse Bar intact + Pixel mascotte intact (z-index OK, pas de chevauchement)
   - `/journey/deliverable/[any-id]` → FAB visible, n'occulte pas le DeliverableScoreBlock ni les boutons de soumission
   - `/mission/[id]` (si présent) → FAB visible
   - Pas de console error / warning React

3. **Visual régression Mentor** :
   - `/mentor` → bell badge en haut, inbox vide en démo
   - `/mentor/submission/[id]` → page existante intacte, pas de FAB

4. **Visual régression Admin** :
   - `/admin` → bell badge + inbox visibles en haut, reste de la page intact
   - `/admin/deliverables`, `/admin/announce`, `/admin/players` → pas de FAB (StaffShell only)

5. **Visual régression Jury/Results** :
   - `/jury` → pas de FAB (mentor role login probable, mais surtout pas Player variant)
   - `/results` → pas de FAB

6. **A11y rapide** :
   - Tab vers FAB → focus ring visible
   - Enter → ouvre modal
   - Tab dans modal → focus circule textarea → cancel → submit
   - ESC → ferme modal
   - aria-live counter annonce le nb caractères restants
   - prefers-reduced-motion : FAB transition désactivée

7. **Dual-mode sanity** (le plus important) :
   - Mode démo (sans env Supabase) : FAB ouvre modal, submit retourne "Mode démo — requête simulée.", inbox vide
   - Mode prod (avec env) : submit insère en DB, inbox affiche

8. **R1/R2/R3 audit final** :
   - Grep R1 : `grep -rn "score\|rank\|note\|/100\|points" components/help/ lib/help-requests.ts | grep -v "// "` → vide (aucune mention score)
   - Grep R2 : `grep -rn "severity.*error" components/help/` → vide (aucun validator error-level, juste warn pour dual-mode)
   - Grep R3 : `grep -rn "disabled\|blocks_progression\|stuck\|level" components/help/HelpCallFab.tsx` → seule la prop `disabled` du submit conditionné sur `pending || message.length === 0` doit exister

9. **Decision** :
   - **PASS** → merge `polish/design-v3-mockups` → `main` autorisé (rappel : policy NO MERGE levée 2026-05-12, smoke OK = green light)
   - **FAIL** → revert dernier commit, investiguer, ne pas merger

10. Si PASS, **commit final** (du quick wrapper, hors PLAN.md) avec SUMMARY.md + deferred-items.md. Pushes par l'orchestrator.
</action>

<verify>
- Tous les checks ci-dessus PASS
- `git status` → seuls les fichiers du PLAN modifiés (pas de fichier orphelin)
- `git log --oneline -5` → 4 commits atomiques task1+task2+task3+task4 sur `polish/design-v3-mockups`
</verify>

<done>
Smoke régression complet OK. Feature shipped en quick atomique 4 commits. Prêt pour merge `polish/design-v3-mockups` → `main` + push origin (autorisé par CLAUDE.md policy 2026-05-12). Si Omar préfère merger plus tard, le tag `v0.2-pilot-ready` reste disponible pour rollback distant.
</done>

---

## Risks

| # | Risk | Likelihood | Impact | Mitigation |
|---|------|-----------|--------|------------|
| R1 | Migration RLS casse un test PROD existant (régression F-16-01 style) | Low | High | GRANT explicite `to authenticated` + reuse helpers `is_my_player` / `is_mentor` éprouvés. Test SELECT/INSERT manuel via psql MCP avant Task 3. |
| R2 | FAB chevauche un autre overlay (Pixel mascotte, drawer, Cohort Pulse) | Medium | Low | z-index FAB=40, modal=60, Pixel typiquement 30. Bottom-right + bottom=5rem au-dessus du MobileTabBar. Visual check Task 5 obligatoire. |
| R3 | Demo mode casse (régression dual-mode) | Low | High | `hasSupabaseEnv()` checké en tête de chaque action + helper. Smoke régression Task 5 #7 vérifie explicitement. |
| R4 | Player envoie spam via composer (pas de rate limit) | Medium | Low | char_length CHECK 1-500 niveau DB. Pas de rate-limit codé (volume pilote 11 Players, mentor peut "résoudre" en 1 clic). À considérer post-pilote pour v0.3 (deferred). |
| R5 | Bell badge ne se rafraîchit pas instantané pour mentor (pas de Realtime) | Medium | Low | `revalidatePath("/mentor")` + `revalidatePath("/admin")` déclenché côté Player suffit pour Next 15 (mentor reload page → fresh count). Acceptable pour pilote J1/J2 (mentor refresh natif toutes les 5-10 min). Realtime deferred v0.3. |
| R6 | Migration timestamp conflit avec migration parallèle (260512_020000 deliverable_is_bonus existe déjà) | Low | Medium | Utiliser timestamp 100000 (10h00 UTC) > 020000. Vérifier `supabase migration list` avant push. |
| R7 | Surface mentor/admin déjà chargée — insertion HelpInbox déstructure layout | Medium | Low | Insertion en TOP de page dans un wrapper `<div>`, pas dans une grid existante. Visual check Task 5 #3 #4. Si conflit, fallback : créer route `/mentor/help` séparée (deferred). |

## Verification

**Niveau plan (gate avant merge)** :
- [ ] R1 audit (clean — feature sans score/rank)
- [ ] R2 audit (clean — pas de validators)
- [ ] R3 audit (clean — feature additive, FAB never disabled-by-state)
- [ ] dual-mode preserved (`hasSupabaseEnv()` checked everywhere)
- [ ] No-Realtime preserved (no `supabase.channel\|subscribe`)
- [ ] `npm run typecheck && npm run lint && npm run build` all PASS
- [ ] Visual sanity Player/Mentor/Admin/Jury/Results
- [ ] 4 commits atomiques (1 par task) — pas de squash
- [ ] SUMMARY.md livré avec SHA des 4 commits
- [ ] deferred-items.md livré (peut être : rate-limit, Realtime bell refresh, mentor assignment scope policy, mailto fallback en plus de in-app, attach project context à la demande)
- [ ] ADVISOR-VERDICT.md déjà produit par advisor en parallèle (cf. constraints)
- [ ] AUDIT.md livré (grep R1/R2/R3 results)

**Niveau pilote (T-1)** :
- [ ] `v0.2-pilot-ready` tag intact pour rollback distant en dernier recours
- [ ] Migration appliquée PROD Supabase (verified via `supabase migration list`)
- [ ] Smoke E2E manuel : 1 Player demande aide → 1 Mentor voit + acknowledge + resolve (3 clicks)

## Cardinal Rules Audit Summary

| Rule | Verdict | Evidence |
|------|---------|----------|
| **R1** (score visible Player only on deliverable detail) | **NA** | Help feature has zero score/rank/XP surface. Composer empathic copy: "Pas de pénalité, c'est fait pour ça." Inbox shows message text only. |
| **R2** (validators warn-only) | **NA** | No validators introduced. Server action returns `severity: "error"` only for genuine technical failures (auth, DB), never for pedagogical content. |
| **R3** (no hardcoded inter-mission blocking) | **OK** | FAB visible 100% of the time on Player pages (no `disabled` based on level/status/stuck/mission). The only `disabled` is submit-button conditional on empty message or pending submission (UX-standard, not pedagogical blocking). |
| **Dual-mode** | **OK** | `hasSupabaseEnv()` guards every Supabase call in `app/actions.ts` + `lib/help-requests.ts`. Demo mode returns synthetic-success and empty inboxes. No `redirect("/login")` / `getCurrentUser()` injected before guard. |
| **No-Realtime** | **OK** | Zero usage of `supabase.channel` / `subscribe`. Bell refresh via `revalidatePath` only. |
