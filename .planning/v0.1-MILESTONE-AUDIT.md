---
milestone: v0.1
milestone_name: Pilot Hack-Days Fès-Meknès (13-14 mai 2026)
audit_date: 2026-05-08
status: passed
phases_complete: 5/5
plans_complete: 26/26
requirements_validated_v1: 35/35
requirements_should_pending: 4/4 (NOTIF-01, SCORE-03, EVENT-05, RESOURCE-01 — Phase 6/7 si buffer)
build_status: typecheck PASS / lint PASS / build PASS (17 routes)
open_items_for_operator: 7
---

# v0.1 Milestone Audit — Pilot Hack-Days Fès-Meknès

## Verdict global

**PASSED — Pilot-ready (sous réserve d'exécution opérateur des actions UAT).**

Les 5 phases livrent structurellement les 12 MUST (M1..M12) du brief produit et les 35 requirements v1 (AUTH/ONBOARD/EVENT/JOURNEY/SUBMIT/EVAL/SCORE/JURY/ADMIN/BRAND/DATA/DEPLOY). Le code est build-green : `npm run typecheck`, `npm run lint`, `npm run build` passent (Phase 5 closeout, 17 routes générées). Les artefacts de déploiement et de test (vercel.json, docs/DEPLOY.md, database/rls_test.sql, SMOKE-TEST-E2E.md) sont en place.

Les items restants ne sont pas des gaps de code — ce sont des **actions humaines opérateur** (Omar) attendues avant le 13 mai 2026 : application schema sur Supabase prod, exécution RLS test, deploy Vercel push, smoke test E2E sur URL prod, envoi magic links testeurs, remplacement assets logo placeholder.

---

## Cross-check des 10 livrables structurels

| # | Livrable attendu | Statut | Evidence |
|---|------------------|--------|----------|
| 1 | Auth + role redirect (player→/journey, mentor→/mentor, game_master→/admin) | ✅ VERIFIED | `lib/auth.ts:30-39` `pathForRole` switch exhaustif sur AppRole ; `app/actions.ts:signIn` lit `profiles.app_role` puis `redirect(pathForRole(role))` ; middleware whitelist `/login`, `/api`, `/_next`, `/auth/callback`. |
| 2 | Onboarding Niveau 0 form | ✅ VERIFIED | `app/onboarding/page.tsx` (99 l.) + `components/onboarding-form.tsx` ; action `saveOnboardingFlow` dans `app/actions.ts` ; middleware redirige Players sans `onboarded_at`. (ONBOARD-02, ONBOARD-03) |
| 3 | /journey avec timeline + deliverables list + status | ✅ VERIFIED | `app/journey/page.tsx` + `components/journey-header.tsx` / `journey-timeline.tsx` / `journey-deliverables.tsx` ; data layer `lib/journey.ts`. (JOURNEY-01..03) |
| 4 | /journey/deliverable/[id] V1+V2 submission | ✅ VERIFIED | `app/journey/deliverable/[id]/page.tsx` (260 l.) + `submission-form.tsx` + `submission-feedback-card.tsx` ; `submitDeliverable` action route V1/V2 server-side selon statut courant (Phase 3 décision). (SUBMIT-01..04) |
| 5 | /mentor list + /mentor/submission/[id] eval form | ✅ VERIFIED | `app/mentor/page.tsx` + `mentor-players-table.tsx` + `mentor-pending-filter.tsx` ; `app/mentor/submission/[id]/page.tsx` (355 l.) + `evaluation-form.tsx` ; `evaluateSubmission` + recalcul score via trigger PG `recalc_player_score`. (EVAL-01..03, SCORE-01..02) |
| 6 | /admin dashboard + import + detail + export CSV | ✅ VERIFIED | `app/admin/page.tsx` (cohort + counters via `lib/admin.ts`) ; `app/admin/players/import/page.tsx` + `csv-import-form.tsx` + `lib/admin-import.ts` (idempotent, magic-link via service-role) ; `app/admin/players/[id]/page.tsx` + `lib/admin-player-detail.ts` ; `app/admin/export/players.csv/route.ts` (RFC 4180, role gate). (ADMIN-01..04, ONBOARD-01) |
| 7 | Branding EIC + partner banner /login | ✅ VERIFIED | `app/login/page.tsx` import `PartnerBanner` ; `components/partner-banner.tsx` ; 6 SVGs partenaires dans `public/brand/partners/` (Tamwilcom, Bank of Africa, Innov Invest, Bluespace, EIC, UEMF) ; `public/brand/logo-eic.svg` ; tokens `--brand-*` dans `app/globals.css`. ⚠️ Logos = placeholders texte (voir open items). (BRAND-01..05) |
| 8 | /jury page + /results page avec publish gate | ✅ VERIFIED | `app/jury/page.tsx` + `jury-form.tsx` + `lib/jury.ts` (5×20, upsert juror×player) ; `app/results/page.tsx` (167 l.) + `publish-button.tsx` + `lib/results.ts` (50/50 ranking, dense ranking, render gate `events.results_published_at`) ; `publishResultsFlow` idempotent. (JURY-01..05) |
| 9 | Vercel deploy config (vercel.json, docs/DEPLOY.md) | ✅ VERIFIED | `vercel.json` (15 l.) : framework nextjs, region `cdg1`, 3 headers sécurité (X-Frame DENY / X-Content-Type nosniff / Referrer-Policy strict-origin-when-cross-origin) ; `docs/DEPLOY.md` (119 l.) ; `.env.example` mis à jour avec 4 vars + flag service-role Sensitive. (DEPLOY-01, DEPLOY-02) |
| 10 | RLS test SQL + smoke test E2E template | ✅ VERIFIED | `database/rls_test.sql` (373 l. — 10 scénarios, pattern `set_config('request.jwt.claims', ...)` + helper `_rls_impersonate`) ; `RLS-TEST-RESULTS.md` template ALL PASS gate ; `SMOKE-TEST-E2E.md` (10 sections, verdict, 0..9 pré-conditions à publication) ; `INTERNAL-TESTERS.md` tracker 6-15 testeurs. (DATA-02, DEPLOY-03) |

**10/10 livrables structurels présents et câblés.**

---

## Cross-check requirements v1 vs goal milestone

Le goal v0.1 — « 6-15 équipes vivent un Hack-Days 2j où chaque livrable est soumis, évalué, scoré en ligne, classement publié, sans perte de données, sans honte devant Tamwilcom / Bank of Africa Academy / Innov Invest / Bluespace » — se traduit en 4 chaînes critiques. Toutes vérifiées :

| Chaîne | Implémentation | Phase |
|--------|----------------|-------|
| GameMaster bulk-import → magic link → Player login → onboarding | `admin-import.ts` + `auth/callback` + middleware redirect onboarding | 4, 1, 2 |
| Player submit V1 → Mentor évalue → V2 si demandé → score final = V2 | `submitDeliverable` (route V1/V2) + `evaluateSubmission` + trigger `recalc_player_score` (max validated) | 2, 3 |
| Pitch jury jour 2 → ranking calculé → publication contrôlée 15h | `savePitchScoreFlow` (5×20) + `lib/results.ts` (combined 50/50 dense rank) + `publishResultsFlow` (gate) | 5 |
| Persistance + isolation Player A vs B + zéro fuite seed | `database/schema.sql` (event_id NOT NULL, FK indexes) + `triggers.sql` + `rls.sql` + `lib/seed/index.ts` (gate `hasSupabaseEnv()`) + `database/rls_test.sql` (10 scénarios) | 1, 2, 5 |

Branding EIC + bandeau partenaires (= « sans honte devant les partenaires ») = Phase 4. Aucune mention `atlas-soil` / `EIC Venture Journey` / `bonusEvent` dans le code rendu (audit Phase 1+4 par grep).

---

## Build & qualité (Phase 5 closeout)

- `npx tsc --noEmit` : exit 0
- `npm run lint` (eslint flat config) : 0 warning
- `npm run build` : success, 17 routes compilées, middleware 89.6 kB
- i18n parity FR/EN : 195/195 keys (Phase 4)
- Anti-leak grep `atlas-soil` / vocabulaire démo : 0 hits dans code rendu
- `vercel.json` : valide JSON

---

## Open Items — actions opérateur (Omar) avant 13 mai 2026

Ces items requièrent un environnement live (Supabase prod, Vercel prod, boîtes mail) ou des assets fournis par EIC. Ce ne sont **pas des gaps de code** — les artefacts sont livrés ; reste l'exécution.

### Critiques (gate du pilote)

1. **Apply schema Supabase prod fresh** — exécuter dans l'ordre `database/schema.sql` → `database/triggers.sql` → `database/rls.sql` sur le projet Supabase prod (cf. `database/README.md` + `docs/DEPLOY.md`). Bootstrap d'au moins 1 ligne `profiles` GameMaster. _(DATA-01 live)_

2. **Seed event Hack-Days** — appliquer `database/seed_event_hackdays.sql` (idempotent, livré Phase 2 plan 04) pour créer l'Event « Hack-Days Fès-Meknès Mai 2026 » + 6 missions + ~9 deliverable_templates. _(EVENT-01, EVENT-02 live)_

3. **Run RLS test** — substituer placeholders `:user_a/b/m/g` dans `database/rls_test.sql`, exécuter dans SQL editor Supabase, remplir `RLS-TEST-RESULTS.md` avec verdict ALL PASS. **Gate hard du deploy.** _(DATA-02 live)_

4. **Vercel deploy** — `vercel push` après config env vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` Sensitive Production-only, `NEXT_PUBLIC_APP_URL`). _(DEPLOY-01, DEPLOY-02 live)_

5. **Smoke test E2E sur URL prod** — exécuter les 10 sections de `SMOKE-TEST-E2E.md` (GM import → magic link → Player login → onboarding → V1 → Mentor évaluation → V2 → jury 5×20 → publication results → RLS spot-checks). Verdict PASS = green-light pilote. _(DEPLOY-03 live)_

### Polish (recommandés J-1)

6. **Magic links 6-15 testeurs internes** — utiliser le tracker `INTERNAL-TESTERS.md` ; tester délivrabilité Gmail/Outlook/UEMF avant J-1 (12 mai) pour répétition. _(Risque résiduel #1 Phase 5)_

7. **Remplacer assets placeholder** — déposer logos officiels SVG aux chemins `public/brand/logo-eic.svg` + `public/brand/partners/{tamwilcom,bank-of-africa,innov-invest,bluespace,eic,uemf}.svg` (filenames imposés). Procédure dans `public/brand/partners/README.md`. Texte placeholder actuel = lisible mais peu pro côté partenaires. _(BRAND-01, BRAND-02 polish)_

### Optionnel

- Tag git `v0.1-pilot-ready` post-deploy.
- Custom domain EIC sur Vercel (sous-domaine `*.vercel.app` suffit pour pilote — décision §7 STATE).
- Cosmetic Phase 4 carry-over : `app/layout.tsx:6` metadata description encore "founders, reviewers, and EIC staff" (vocabulaire pré-Phase-1) — non bloquant.

---

## Tech debt connue (non bloquante v0.1)

- 2 advisories npm-audit moderate transitives `@supabase/*` — différé Phase 5 (out of scope pilote).
- Warning Next.js 15 `webpack.cache.PackFileCacheStrategy` ("big strings 101kB / 231kB") — perf hint cache deserialization, pas actionable côté projet.
- Pas de tests automatisés (Vitest/Playwright/pgTAP) — décision pilote (solo dev T-5j) ; smoke test manuel suffit. **Revisit V2.**
- Audit log table existe mais write non livré — V2.
- Rate limiting server actions — V2.
- Sentry / observabilité — V2.
- Lucide-react upgrade 0.x → 1.x différé jusqu'à présence de tests — V2.
- SHOULD reportés Phase 6/7 si buffer (4 items) : NOTIF-01 (badges), SCORE-03 (engagement serveur), EVENT-05 (multi-event ready), RESOURCE-01 (page resources).

---

## Conclusion

**v0.1 est livré.** Tous les MUST (M1..M12) et tous les v1 requirements (35) sont validés structurellement et build-green. Le pilote est prêt techniquement pour le 13 mai 2026 — sous condition que les 5 actions opérateur critiques (apply schema, seed event, RLS test, Vercel deploy, smoke E2E prod) soient exécutées par Omar avant J-1 (12 mai).

Aucun gap de code. Audit clos.

---

*Audit produit le 2026-05-08 par gsd-verifier (cross-check ROADMAP / REQUIREMENTS / PHASE-SUMMARY × 5 / structure codebase × 10 livrables).*
