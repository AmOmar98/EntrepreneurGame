node_modules
.next
out
dist
.env
.env*.local
.env.local.*
*.log
*.tsbuildinfo

.vercel

# Smoketest local-only artifacts (test accounts with creds)
smoketest/TEST-ACCOUNTS.local.md
smoketest/*.local.*
**/*.local.md

# Cohort credentials & roster — contain PROD passwords + personal emails + phones (RGPD)
cohorte-agreentech-creds.csv
cohorte-agreentech.csv
cohorte-*-creds.csv

# Playwright MCP local console logs (debug artefacts)
.playwright-mcp/

# Playwright smoke harness (quick-260517-psd) artefacts
playwright-report/
test-results/
playwright/.cache/

# Archive locale exports PROD (data porteurs, RGPD)
.planning/exports/

