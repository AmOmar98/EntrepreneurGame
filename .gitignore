node_modules
.next
out
dist
.env
.env*.local
*.log
*.tsbuildinfo

.vercel

# Smoketest local-only artifacts (test accounts with creds)
smoketest/TEST-ACCOUNTS.local.md
smoketest/*.local.*
**/*.local.md

# Cohort credentials & roster — contain PROD passwords + personal emails + phones (RGPD)
cohorte-agreentech-creds.csv
cohorte-agreentech.csv
cohorte-*-creds.csv

# Playwright MCP local console logs (debug artefacts)
.playwright-mcp/

